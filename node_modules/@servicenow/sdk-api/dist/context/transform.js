"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MultiProjectTransform = void 0;
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const project_1 = require("../project");
class Transform {
    project;
    constructor(project) {
        this.project = project;
    }
    async toShape(node, ...plugins) {
        if (!(0, sdk_build_core_1.isSupportedNode)(node)) {
            if ((0, sdk_build_core_1.isFluentNode)(node)) {
                this.project
                    .getDiagnosticsService()
                    .error(node, `Node kind "${(0, sdk_build_core_1.getUnsupportedKindName)(node)}" is not allowed in Fluent files.`);
            }
            return { success: false };
        }
        // First, just try to get a shape
        const shapeResult = await this.project.getPlugins().nodeToShape(node, this.project.getContext(), ...plugins);
        if (!shapeResult.success) {
            return shapeResult;
        }
        // Then, let plugins try to produce a subclass for this shape
        const subclass = await this.toSubclass(shapeResult.value, ...plugins);
        // Then, let plugins try to turn the subclassed shape into a record
        const recordResult = await this.toRecord(subclass, ...plugins);
        // Return the record if we got one
        if (recordResult.success) {
            return recordResult;
        }
        // Otherwise just return the subclassed shape
        return { success: true, value: subclass };
    }
    async toSubclass(shape, ...plugins) {
        return this.project.getPlugins().shapeToSubclass(shape, this.project.getContext(), ...plugins);
    }
    async toRecord(source, ...plugins) {
        if (sdk_build_core_1.ts.Node.isNode(source)) {
            const shapeResult = await this.toShape(source, ...plugins);
            return shapeResult.success ? this.toRecord(shapeResult.value, ...plugins) : shapeResult;
        }
        else if (source instanceof sdk_build_core_1.Shape) {
            return this.project.getPlugins().shapeToRecord(source, this.project.getContext(), ...plugins);
        }
        else {
            for (const plugin of plugins.length > 0 ? plugins : this.project.getPlugins()) {
                try {
                    const result = await plugin.fileToRecord(source, this.project.getContext());
                    if (result.success) {
                        return {
                            success: true,
                            value: result.value,
                        };
                    }
                }
                catch (error) {
                    this.project
                        .getLogger()
                        .error(`Plugin "${plugin.getName()}" failed to transform "${source.path}" into records:`, error);
                }
            }
            return { success: false };
        }
    }
    async recordToShape(record, database, ...plugins) {
        for (const plugin of plugins.length > 0 ? plugins : this.project.getPlugins()) {
            const result = await plugin.recordToShape({
                record,
                database,
                context: this.project.getContext(),
            });
            if (result.success) {
                return result;
            }
        }
        return { success: false };
    }
    async getUpdateName(record, ...plugins) {
        for (const plugin of plugins.length > 0 ? plugins : this.project.getPlugins()) {
            try {
                const result = await plugin.getUpdateName(record, this.project.getContext());
                if (result.success) {
                    return result.value;
                }
            }
            catch (error) {
                this.project
                    .getLogger()
                    .error(`Plugin "${plugin.getName()}" failed to get update name for record: ${record.getTable()}`, error);
            }
        }
        throw new Error(`Failed to get update name for record: ${record.getTable()}`);
    }
}
class MultiProjectTransform {
    rootProject;
    fallbackTransform;
    rootTransform;
    transformMap = {};
    constructor(rootProject) {
        this.rootProject = rootProject;
        this.rootTransform = new Transform(rootProject);
        this.transformMap[rootProject.getRootDir()] = this.rootTransform;
    }
    async toShape(node, ...plugins) {
        return this.getTransformForPath(node.getSourceFile().getFilePath()).toShape(node, ...plugins);
    }
    async toSubclass(shape, ...plugins) {
        return this.getTransformForPath(shape.getOriginalFilePath()).toSubclass(shape, ...plugins);
    }
    async toRecord(source, ...plugins) {
        return this.getTransformForPath(sdk_build_core_1.ts.Node.isNode(source)
            ? source.getSourceFile().getFilePath()
            : source instanceof sdk_build_core_1.Shape
                ? source.getOriginalFilePath()
                : source.path).toRecord(source, ...plugins);
    }
    async recordToShape(record, database, ...plugins) {
        return this.getTransformForPath(record.getOriginalFilePath()).recordToShape(record, database, ...plugins);
    }
    async getUpdateName(record, ...plugins) {
        return this.getTransformForPath(record.getOriginalFilePath()).getUpdateName(record, ...plugins);
    }
    createTransform(rootDir) {
        const { plugins, fs: fileSystem, logger, diagnostics, transform } = this.rootProject.getContext();
        return new Transform(new project_1.Project({ rootDir, plugins, fileSystem, logger, diagnostics, transform }));
    }
    getFallbackTransform() {
        return (this.fallbackTransform ??= this.createTransform(this.rootProject.getRootDir()));
    }
    getTransformForPath(filePath) {
        if (this.transformMap[filePath]) {
            return this.transformMap[filePath];
        }
        // If it's in the root project's files, it must've been added explicitly regardless of path
        if (this.rootProject.getFileIfExists(filePath)) {
            return (this.transformMap[filePath] = this.rootTransform);
        }
        const projectRoot = this.findProjectRootForPath(filePath);
        if (projectRoot && this.transformMap[projectRoot]) {
            return this.transformMap[projectRoot];
        }
        if (!projectRoot) {
            return this.getFallbackTransform();
        }
        return (this.transformMap[projectRoot] = this.createTransform(projectRoot));
    }
    findProjectRootForPath(filePath) {
        if (this.isProjectRoot(filePath)) {
            return filePath;
        }
        const parent = sdk_build_core_1.path.dirname(filePath);
        if (parent === filePath) {
            return undefined;
        }
        return this.findProjectRootForPath(parent);
    }
    isProjectRootCache = {};
    isProjectRoot(filePath) {
        if (typeof this.isProjectRootCache[filePath] === 'boolean') {
            return this.isProjectRootCache[filePath];
        }
        const nowConfigPath = sdk_build_core_1.path.join(filePath, 'now.config.json');
        return (this.isProjectRootCache[filePath] =
            filePath in this.transformMap ||
                !!this.rootProject.getCompiler().getSourceFile(nowConfigPath) ||
                sdk_build_core_1.FileSystem.existsSync(this.rootProject.getFileSystem(), nowConfigPath));
    }
}
exports.MultiProjectTransform = MultiProjectTransform;
//# sourceMappingURL=transform.js.map