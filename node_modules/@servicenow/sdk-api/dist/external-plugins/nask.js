"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/defaults.js
var require_defaults = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/defaults.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.DEFAULT_POSTPROCESSOR_SCRIPT = exports2.DEFAULT_PREPROCESSOR_SCRIPT = exports2.INVALID_DATATYPES_FOR_SUBFLOW_AND_FLOWACTION = exports2.SUPPORTED_FILTER_CONDITION_DATATYPES = exports2.ALLOWED_DATATYPES_FOR_NAP = exports2.STANDARD_OUTPUT_DEFAULTS = exports2.STANDARD_OUTPUT_NAMES = exports2.SYS_GEN_AI_SKILL_CONFIG_DEFAULTS = exports2.SYS_GEN_AI_SKILL_APPLICABILITY_DEFAULTS = exports2.DEFAULT_CONDITION_SCRIPT = exports2.SYS_GEN_AI_SKILL_DEFAULTS = exports2.EXCLUDED_METRIC_CAPABILITY_IDS = exports2.UNSUPPORTED_TOOL_CAPABILITY_IDS = exports2.RESOURCE_EDGE_ERROR_HANDLER_SCRIPT = exports2.RESOURCE_EDGE_APPLICABILITY_SCRIPT = exports2.RESOURCE_ATTRIBUTE_VALUE_SCRIPT = exports2.RESOURCE_MAPPING_APPLICABILITY_SCRIPT = exports2.RESOURCE_MAPPING_DEFAULTS = exports2.DEFAULT_ADMIN_ROLE_SYS_ID = exports2.AI_SEARCH_PROVIDERS = exports2.SKILL_TOOL_CONFIG = exports2.WEBSEARCH_SCRAPING_CONFIG = exports2.WEBSEARCH_AI_ANSWERS_CONFIG = exports2.TOOL_ATTRIBUTE_IDS = exports2.TOOL_RESOURCE_CAPABILITIES = exports2.MAX_TOKENS_UPPER_BOUND = exports2.SKILL_NAME_MAX_LENGTH = exports2.RESTRICTED_ROLE_SYS_IDS = exports2.RESTRICTED_ROLE_NAMES = exports2.MAINT_ROLE_SYS_ID = exports2.SECURITY_TABLE_NAMES = exports2.AGENT_ACCESS_ROLE_CONFIG_DEFAULTS = exports2.ACL_DEFAULTS = exports2.ACL_CONSTANTS = exports2.VAR_SET = exports2.PROVIDER_MAPPINGS = exports2.NOW_LLM_GENERIC_PROVIDER_API = exports2.VALID_TOOL_DATA_TYPES = exports2.INVALID_TOOL_INTERNAL_TYPES = exports2.TOOL_TYPES = exports2.SCRIPT_TOOL_API_ID = exports2.SYS_ONE_EXTEND_CAPABILITY_DEFINITION_API_TYPES = exports2.SYS_GEN_AI_SKILL_APPLICABILITY_CREATE_ONLY_DEFAULTS = exports2.TABLE_DEFAULTS = exports2.OTHERS_SKILL_FAMILY_SYS_ID = exports2.TABLE_NAMES = void 0;
    var AclAttributes = {
      user_is_authenticated: "239bdf0c47d23110ba46ff58936d4330",
      has_admin_role: "8637f144b732111074f706dd1e11a981"
    };
    var AclOperations = {
      execute: "execute"
    };
    exports2.TABLE_NAMES = {
      SN_NOWASSIST_SKILL_CONFIG: "sn_nowassist_skill_config",
      SN_NOWASSIST_SKILL_CONFIG_STATUS: "sn_nowassist_skill_config_status",
      SN_NOWASSIST_SKILL_CONFIG_VAR_SET: "sn_nowassist_skill_config_var_set",
      SN_NOWASSIST_SKILL_CONFIG_APPLICABILITY: "sn_nowassist_skill_config_applicability",
      SN_NOWASSIST_SKILL_CONFIG_UI: "sn_ns_skill_config_ui",
      SYS_GENERATIVE_AI_PROMPT_CONFIG: "sys_generative_ai_prompt_config",
      SYS_GENERATIVE_AI_CONFIG: "sys_generative_ai_config",
      SYS_GENERATIVE_AI_VALIDATOR: "sys_generative_ai_validator",
      SYS_GENERATIVE_AI_REQUEST_VALIDATOR: "sys_generative_ai_request_validator",
      SYS_GENERATIVE_AI_RESPONSE_VALIDATOR: "sys_generative_ai_response_validator",
      SYS_GEN_AI_FEATURE_MAPPING: "sys_gen_ai_feature_mapping",
      SYS_GEN_AI_STRATEGY_MAPPING: "sys_gen_ai_strategy_mapping",
      SYS_GEN_AI_SKILL: "sys_gen_ai_skill",
      SYS_GEN_AI_SKILL_APPLICABILITY: "sys_gen_ai_skill_applicability",
      SYS_GEN_AI_SKILL_CONFIG: "sys_gen_ai_skill_config",
      SYS_ONE_EXTEND_CAPABILITY: "sys_one_extend_capability",
      SYS_ONE_EXTEND_CAPABILITY_DEFINITION: "sys_one_extend_capability_definition",
      SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE: "sys_one_extend_definition_attribute",
      SYS_ONE_EXTEND_DEFINITION_CONFIG: "sys_one_extend_definition_config",
      SYS_VARIABLE_VALUE: "sys_variable_value",
      // Tool-related tables
      SYS_ONE_EXTEND_RESOURCE_MAPPING: "sys_one_extend_resource_mapping",
      SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING: "sys_one_extend_resource_attribute_mapping",
      SYS_ONE_EXTEND_RESOURCE_EDGE: "sys_one_extend_resource_edge"
    };
    exports2.OTHERS_SKILL_FAMILY_SYS_ID = "a5efaf1c93764210aa5730f16489186d";
    exports2.TABLE_DEFAULTS = {
      [exports2.TABLE_NAMES.SN_NOWASSIST_SKILL_CONFIG]: {
        state: "0",
        skill_family: exports2.OTHERS_SKILL_FAMILY_SYS_ID
      },
      [exports2.TABLE_NAMES.SN_NOWASSIST_SKILL_CONFIG_STATUS]: {},
      [exports2.TABLE_NAMES.SYS_ONE_EXTEND_CAPABILITY]: {
        active: true,
        type: "skill"
      },
      [exports2.TABLE_NAMES.SYS_ONE_EXTEND_CAPABILITY_DEFINITION]: {
        truncate_strategy: "2b99e1137fb20210f465c3b76d86650d"
      },
      [exports2.TABLE_NAMES.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE]: {
        data_type: "string",
        type: "input",
        active: true
      },
      [exports2.TABLE_NAMES.SYS_GENERATIVE_AI_CONFIG]: {
        max_tokens: "500",
        min_word_count: "-1",
        state: "draft",
        active: false,
        version: 1,
        temperature: 0.2
      },
      [exports2.TABLE_NAMES.SYS_GENERATIVE_AI_PROMPT_CONFIG]: {
        is_default: false
      },
      [exports2.TABLE_NAMES.SYS_GEN_AI_FEATURE_MAPPING]: {
        active: true
      },
      [exports2.TABLE_NAMES.SYS_GEN_AI_STRATEGY_MAPPING]: {
        active: true
      },
      [exports2.TABLE_NAMES.SYS_GENERATIVE_AI_VALIDATOR]: {},
      [exports2.TABLE_NAMES.SYS_GENERATIVE_AI_REQUEST_VALIDATOR]: {},
      [exports2.TABLE_NAMES.SYS_GENERATIVE_AI_RESPONSE_VALIDATOR]: {},
      [exports2.TABLE_NAMES.SYS_ONE_EXTEND_DEFINITION_CONFIG]: {
        active: true
      },
      [exports2.TABLE_NAMES.SN_NOWASSIST_SKILL_CONFIG_VAR_SET]: {
        hide_step: false
      },
      [exports2.TABLE_NAMES.SYS_VARIABLE_VALUE]: {
        document: "sn_nowassist_skill_config_var_set"
      },
      [exports2.TABLE_NAMES.SYS_GEN_AI_SKILL_APPLICABILITY]: {
        condition_mode: "simple"
      },
      // Tool-related table defaults
      [exports2.TABLE_NAMES.SYS_ONE_EXTEND_RESOURCE_MAPPING]: {
        active: true
      },
      [exports2.TABLE_NAMES.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING]: {
        attribute_value_type: "template"
      },
      [exports2.TABLE_NAMES.SYS_ONE_EXTEND_RESOURCE_EDGE]: {
        name: "edge",
        active: true,
        order: "0"
      }
    };
    exports2.SYS_GEN_AI_SKILL_APPLICABILITY_CREATE_ONLY_DEFAULTS = {
      roles: "now_assist_panel_user"
    };
    exports2.SYS_ONE_EXTEND_CAPABILITY_DEFINITION_API_TYPES = {
      SYS_SCRIPT_INCLUDE: "sys_script_include",
      SYS_HUB_FLOW: "sys_hub_flow",
      SYS_HUB_ACTION_TYPE_DEFINITION: "sys_hub_action_type_definition"
    };
    exports2.SCRIPT_TOOL_API_ID = "4d9a83679f9202107c0e2305fa0a1c8c";
    exports2.TOOL_TYPES = {
      INLINE_SCRIPT: "inline-script",
      SCRIPT: "script",
      FLOW_ACTION: "flow-action",
      SUBFLOW: "subflow",
      SKILL: "skill",
      WEB_SEARCH: "web-search",
      DECISION: "decision"
    };
    exports2.INVALID_TOOL_INTERNAL_TYPES = ["document_id", "reference", "snapshot_template_value"];
    exports2.VALID_TOOL_DATA_TYPES = [
      // OneExtend types
      "string",
      "boolean",
      "numeric",
      "json_object",
      "json_array",
      "simple_array",
      "glide_record",
      "table",
      "object",
      // Legacy support - maps to json_object
      // Flow Designer types (passed through as-is)
      "choice",
      "date",
      "datetime",
      "duration",
      "html",
      "conditions",
      "script"
    ];
    exports2.NOW_LLM_GENERIC_PROVIDER_API = "936e514a53b3b110f028ddeeff7b128c";
    exports2.PROVIDER_MAPPINGS = {
      // Now LLM Service (ServiceNow's default provider)
      "Now LLM Service": {
        provider_api: "Now LLM Generic",
        flow_id: "936e514a53b3b110f028ddeeff7b128c",
        flow_type: "sys_hub_flow"
      },
      // Azure OpenAI (multiple APIs available - using Chat Completions as default)
      "Azure OpenAI": {
        provider_api: "Chat Completions",
        flow_id: "a55c1c6f530221106b38ddeeff7b12b1",
        flow_type: "sys_hub_flow"
      },
      // Open AI
      "Open AI": {
        provider_api: "Chat Completions",
        flow_id: "3b6a84c4878621104f1c8f59dabb3525",
        flow_type: "sys_hub_flow"
      },
      // Google Gemini (multiple APIs - using Vertex AI Chat Completions as default)
      "Google Gemini": {
        provider_api: "Chat Completions",
        flow_id: "9666f54b2dca7110f877b951e3e5bf53",
        flow_type: "sys_hub_flow"
      },
      // AWS Claude (Amazon Bedrock)
      "AWS Claude": {
        provider_api: "Amazon Bedrock Chat Completions",
        flow_id: "52f46aeb9f2c5210da502fca9a0a1cda",
        flow_type: "sys_hub_flow"
      },
      // IBM Watson
      "IBM Watson": {
        provider_api: "Completions",
        flow_id: "e841ec9053190210ea08ddeeff7b12ae",
        flow_type: "sys_hub_flow"
      },
      // Perplexity
      Perplexity: {
        provider_api: "Perplexity",
        flow_id: "2983f6889fbad21029ead63a6a0a1cd6",
        flow_type: "sys_hub_flow"
      },
      // Custom LLM Provider
      "Custom LLM Provider": {
        provider_api: "custom_api",
        flow_id: "af9702d0c3110210b0939bc8a840dd2c",
        flow_type: "sys_hub_flow"
      },
      // Aleph Alpha
      "Aleph Alpha": {
        provider_api: "Completions",
        flow_id: "4c88a81955ea7110f877466af34cb583",
        flow_type: "sys_hub_flow"
      }
    };
    exports2.VAR_SET = {
      ENABLE_UI_ACTION: "f09e3f9893b64210aa5730f16489189f",
      UI_ACTION_ID: "4fee37d893b64210aa5730f164891896",
      ENABLE_UI_BUILDER: "e3f313ddff6522103a2bffffffffff48",
      ENABLE_NAP: "ab5ef79893b64210aa5730f16489185a",
      SKILL_BUILDER_DELIVERY_STEP: "7dfdff5893b64210aa5730f164891837",
      CONFIG_TYPE_VIEW_TRIGGER_STEP: "b89b1f0b7f821210076726d6ac86650e",
      ENABLE_WORKFLOW_TRIGGER: "5c5623df7f025210076726d6ac8665f9"
    };
    exports2.ACL_CONSTANTS = {
      // ACL Type for Gen AI Skills (not in core - specific to NowAssist)
      GEN_AI_SKILL_TYPE: "0d986410ffdd2210c0e9ffffffffffc9",
      // Security Attributes (from core)
      USER_IS_AUTHENTICATED: AclAttributes.user_is_authenticated,
      HAS_ADMIN_ROLE: AclAttributes.has_admin_role,
      // Operations (from core)
      OPERATION_EXECUTE: AclOperations.execute,
      // Decision Types
      DECISION_TYPE_ALLOW: "allow",
      DECISION_TYPE_DENY: "deny",
      // Local or Existing
      LOCAL_OR_EXISTING: "Existing"
    };
    exports2.ACL_DEFAULTS = {
      active: true,
      admin_overrides: true,
      // needs to verify
      advanced: false,
      decision_type: "allow",
      local_or_existing: "Existing",
      operation: "execute"
    };
    exports2.AGENT_ACCESS_ROLE_CONFIG_DEFAULTS = {
      action: "limit_to_roles",
      allow_all_session_roles: false,
      agent_table: "sn_nowassist_skill_config"
    };
    exports2.SECURITY_TABLE_NAMES = {
      SYS_AGENT_ACCESS_ROLE_CONFIGURATION: "sys_agent_access_role_configuration",
      SYS_AGENT_ACCESS_ROLE_MAPPING: "sys_agent_access_role_mapping",
      SYS_USER_ROLE: "sys_user_role"
    };
    exports2.MAINT_ROLE_SYS_ID = "b05947ca0a0a0aa700fdef030c6bca07";
    exports2.RESTRICTED_ROLE_NAMES = ["maint"];
    exports2.RESTRICTED_ROLE_SYS_IDS = [exports2.MAINT_ROLE_SYS_ID];
    exports2.SKILL_NAME_MAX_LENGTH = 40;
    exports2.MAX_TOKENS_UPPER_BOUND = 130722;
    exports2.TOOL_RESOURCE_CAPABILITIES = {
      INLINE_SCRIPT: "51d095f77f211210210ca65c4d866591",
      WEB_SEARCH: "5817eb2b9f36121029ead63a6a0a1c7c",
      DECISION: "5cedd7147ff1921032d293f10d866506",
      SKILL: "51d095f77f211210210ca65c4d866595"
      // Skill-as-Tool
    };
    exports2.TOOL_ATTRIBUTE_IDS = {
      // InlineScript
      INLINE_SCRIPT_INPUT: "0bd1117b7f211210210ca65c4d8665ed",
      // "inputscript" attribute
      INLINE_SCRIPT_OUTPUT: "a3911d3b7f211210210ca65c4d86651b"
      // "output" attribute
    };
    exports2.WEBSEARCH_AI_ANSWERS_CONFIG = {
      INPUT_MAPPINGS: [
        { fluentName: "query", platformName: "query", attributeId: "9659ebef9f36121029ead63a6a0a1ce8", required: true },
        {
          fluentName: "searchType",
          platformName: "search_type",
          attributeId: "c11ece269fc3121029ead63a6a0a1cd2",
          required: true
        },
        {
          fluentName: "aiSearchProviders",
          platformName: "aisearch_providers",
          attributeId: "bcfc3961ffad6210a81effffffffffe0",
          required: false,
          default: ""
        }
      ],
      OUTPUT_MAPPINGS: [
        { name: "provider", attributeId: "a017ef2b9f36121029ead63a6a0a1cec" },
        { name: "response", attributeId: "a417ef2b9f36121029ead63a6a0a1cee" },
        { name: "error", attributeId: "2c17ef2b9f36121029ead63a6a0a1cda" },
        { name: "errorcode", attributeId: "ac17ef2b9f36121029ead63a6a0a1ce9" },
        { name: "status", attributeId: "e017ef2b9f36121029ead63a6a0a1cf3" }
      ]
    };
    exports2.WEBSEARCH_SCRAPING_CONFIG = {
      INPUT_MAPPINGS: [
        { fluentName: "query", platformName: "query", attributeId: "9659ebef9f36121029ead63a6a0a1ce8", required: true },
        {
          fluentName: "searchType",
          platformName: "search_type",
          attributeId: "c11ece269fc3121029ead63a6a0a1cd2",
          required: true
        },
        {
          fluentName: "numResults",
          platformName: "num",
          attributeId: "790a67639f76121029ead63a6a0a1c7b",
          required: false,
          default: 3
        },
        {
          fluentName: "sites",
          platformName: "site",
          attributeId: "e03aa7639f76121029ead63a6a0a1c35",
          required: false,
          default: ""
        },
        {
          fluentName: "country",
          platformName: "country",
          attributeId: "3e5a67639f76121029ead63a6a0a1c65",
          required: false,
          default: ""
        },
        {
          fluentName: "maxTokens",
          platformName: "max_tokens",
          attributeId: "5aa03aba9f0f121029ead63a6a0a1c32",
          required: false,
          default: 1e3
        }
      ],
      OUTPUT_MAPPINGS: [
        { name: "provider", attributeId: "a017ef2b9f36121029ead63a6a0a1cec" },
        { name: "response", attributeId: "a417ef2b9f36121029ead63a6a0a1cee" },
        { name: "error", attributeId: "2c17ef2b9f36121029ead63a6a0a1cda" },
        { name: "errorcode", attributeId: "ac17ef2b9f36121029ead63a6a0a1ce9" },
        { name: "status", attributeId: "e017ef2b9f36121029ead63a6a0a1cf3" }
      ]
    };
    exports2.SKILL_TOOL_CONFIG = {
      /** Fixed output names for Skill tools - always created */
      FIXED_OUTPUT_NAMES: ["provider", "response", "error", "errorcode", "status"],
      /** Map output names to FixedOutputTruncateConfig keys */
      OUTPUT_NAME_TO_TRUNCATE_KEY: {
        provider: "provider",
        response: "response",
        error: "error",
        errorcode: "errorcode",
        status: "status"
      }
    };
    exports2.AI_SEARCH_PROVIDERS = {
      perplexity: {
        value: "perplexity",
        label: "Perplexity",
        capId: "d569b70d9fa2521029ead63a6a0a1c11",
        pId: "2983f6889fbad21029ead63a6a0a1cd6"
      },
      openai: {
        value: "openai",
        label: "OpenAI",
        capId: "d569b70d9fa2521029ead63a6a0a1c11",
        pId: "d54762c77fd42210a3aed719dc8665c3"
      },
      gemini: {
        value: "gemini",
        label: "Gemini",
        capId: "d569b70d9fa2521029ead63a6a0a1c11",
        pId: "9666f54b2dca7110f877b951e3e5bf53",
        isDefault: true
      },
      azure_openAI: {
        value: "azure_openAI",
        label: "Azure Open AI",
        capId: "d569b70d9fa2521029ead63a6a0a1c11",
        pId: "b4a8cb46ffa82210a3aeffffffffff9a"
      }
    };
    exports2.DEFAULT_ADMIN_ROLE_SYS_ID = "2831a114c611228501d4ea6c309d626d";
    exports2.RESOURCE_MAPPING_DEFAULTS = {
      active: true
    };
    exports2.RESOURCE_MAPPING_APPLICABILITY_SCRIPT = `(function(currentInputs, context) {
    /* @param {Object} currentInputs - Inputs provided for the current step.*/
    /* @param {Object} context - Execution context, which may include:*/
    /*                         - Previous tool outputs/inputs (e.g., context['ToolName.attributeName'] or context.getValue('ToolName.attributeName')).*/
    /*                         - Additional context information (e.g., context.getAllValues()).*/
    /* @returns {Boolean} - Return 'true' if the condition is met; otherwise 'false'.*/
    return true;
})(currentInputs, context);`;
    exports2.RESOURCE_ATTRIBUTE_VALUE_SCRIPT = `(function execute(currentInputs, context) {
    /* @param {Object} currentInputs - Inputs provided for the current step.*/
    /* @param {Object} context - Execution context, which may include:*/
    /*                         - Previous tool outputs/inputs (e.g., context['ToolName.attributeName'] or context.getValue('ToolName.attributeName')).*/
    /*                         - Additional context information (e.g., context.getAllValues()).*/
    /* @returns {Object} - The computed value for the attribute.*/
    return '';
})(currentInputs, context);`;
    exports2.RESOURCE_EDGE_APPLICABILITY_SCRIPT = `(function(currentInputs, context) {  
    /* @param {Object} currentInputs - Inputs provided for the current step.*/  
    /* @param {Object} context - Execution context, which may include:*/     
    /*         - Previous tool outputs/inputs (e.g., context['ToolName.attributeName'] or context.getValue('ToolName.attributeName')).*/     
    /*         - Additional context information (e.g., context.getAllValues()).*/  
    /* @returns {Boolean} - Return 'true' if the condition is met; otherwise 'false'.*/  
    return true;
  })(currentInputs, context);`;
    exports2.RESOURCE_EDGE_ERROR_HANDLER_SCRIPT = `(function handleError() {  
    // customize condition logic here,  
    return ;
})();`;
    exports2.UNSUPPORTED_TOOL_CAPABILITY_IDS = /* @__PURE__ */ new Set([
      "739b26e553e5e21016f7ddeeff7b1237",
      // DocIntel Tool
      "5c6292cdff675210f465ffffffffffd9",
      // Predictive Intelligence Tool
      // Retriever Search Tool
      "5345d14277e81210e9c41345ba5a9933",
      "2da6bcffa30012104429a138e31e61ef",
      "69e287e7431202101ed803295bb8f285",
      // Websearch (maint only) - from sn_ai_websearch
      "a3e7b0189fe2121029ead63a6a0a1c90",
      // Web Search APIs
      "f04ccd149f62121029ead63a6a0a1c51",
      // Web Scraping APIs
      "759c5b709faa121029ead63a6a0a1c45",
      // QnA
      "d569b70d9fa2521029ead63a6a0a1c11"
      // AI Search answers
    ]);
    exports2.EXCLUDED_METRIC_CAPABILITY_IDS = /* @__PURE__ */ new Set([
      // Evaluation metrics
      "fbf714e97f41121046719fbefc866574",
      // Completeness Metric
      "8ccacaa77f1202105bc49fbefc86656d",
      // Recall@K Metric
      "f5fa89d57f14121071cab6257d8665a6",
      // Correctness Metric with Golden Response
      "e45b7c297f92121071cab6257d86651e",
      // Fluency JoJ metric
      "53fed71f7fad521071cab6257d8665c6",
      // Offensiveness Guardian Metric
      "885e57732bdb1e10802dfd78ce91bf05",
      // Response Faithfulness
      "71114a5d3bf7a210205eb7e643e45a75",
      // ERR Faithfulness Metric
      "b1a4347e3bd1aa10654f4fb643e45a0c",
      // DocIntel Key Extraction metric
      "f74fd7b32bdb1e10802dfd78ce91bfad",
      // VA Actions Helpfulness
      "a28f0e067f0202105bc49fbefc8665ec",
      // Correctness Metric
      "41f9b88eff892210cc64ffffffffffa9",
      // Number of Words Metric
      "6a1e78b2938426105bc8d455748918b1",
      // NumberOfTurnsMetric
      "c49fb05247bf22100ebeeb40706d4308",
      // ERR Fluency Metric
      "605d6c0f7ffe02100a03b6257d8665d3",
      // Fluency Metric
      "f22bf8657f52121071cab6257d8665e7",
      // Correctness w Golden Response JoJ metric
      "954ad0599393a610f3a0b8027cba10fe",
      // KM Fluency Metric
      "729c4ad33b195210e3ddac9a04e45af2",
      // (Experimental) QnA Relevancy Metric
      "ab055042a37b1210883c25d1d71e61f0",
      // VA Actions Match Metric
      "3de042ab7f9602105bc49fbefc866571",
      // Precision@K Metric
      "82900c917fd6521071cab6257d86658f",
      // Faithfulness JoJ metric
      "63015aeda3920210883c25d1d71e6124",
      // Faithfulness Metric
      "775b7ca57f92121071cab6257d86657b",
      // Correctness JoJ metric
      "222a5bfb2b9b1e10802dfd78ce91bf1c",
      // Response Level Fluency
      "8d0964757f9202105bc49fbefc8665f7",
      // F1Score@K Metric
      "864066e7a3d64210883c25d1d71e61e7",
      // (Experimental) Helpfulness Metric
      "625fe4e97f12121071cab6257d86658a",
      // Completeness JoJ metric
      "2f1e2fea9373e210c64cf5559803d668",
      // ERR Relevancy Metric
      "39889392a3760210883c25d1d71e617f",
      // MatchMetric
      "bcdfdfe69333e210c64cf5559803d68c",
      // ERR Completeness Metric
      "ac7364442b8656105866f6046e91bfb9"
      // Task Completeness with Ground truth
    ]);
    exports2.SYS_GEN_AI_SKILL_DEFAULTS = {
      enable_followup: false,
      skill_description: "",
      skill_table: "sys_one_extend_capability",
      sys_domain: "global",
      sys_domain_path: "/",
      sys_overrides: ""
    };
    exports2.DEFAULT_CONDITION_SCRIPT = "(function executeCondition(/* glide record */ current) {   \n	// customize condition logic here, return true or false   \n	return false;\n})(current);";
    exports2.SYS_GEN_AI_SKILL_APPLICABILITY_DEFAULTS = {
      condition_mode: "simple"
    };
    exports2.SYS_GEN_AI_SKILL_CONFIG_DEFAULTS = {
      channels: "",
      discoverable: true,
      supervision: "",
      use_knowledge_graph: false,
      visible: true,
      sys_domain: "global",
      sys_domain_path: "/",
      sys_overrides: ""
    };
    exports2.STANDARD_OUTPUT_NAMES = ["response", "provider", "errorcode", "status", "error"];
    exports2.STANDARD_OUTPUT_DEFAULTS = {
      response: { label: "response", description: "Response Text", dataType: "string" },
      provider: { label: "provider", description: "Provider name", dataType: "string" },
      errorcode: { label: "errorCode", description: "Error Code", dataType: "string" },
      status: { label: "status", description: "Status", dataType: "string" },
      error: { label: "error", description: "Error Message", dataType: "string" }
    };
    exports2.ALLOWED_DATATYPES_FOR_NAP = ["glide_record", "numeric", "boolean", "string"];
    exports2.SUPPORTED_FILTER_CONDITION_DATATYPES = ["string", "numeric", "boolean", "glide_record"];
    exports2.INVALID_DATATYPES_FOR_SUBFLOW_AND_FLOWACTION = ["Document ID", "Reference", "Snapshot Template Value"];
    exports2.DEFAULT_PREPROCESSOR_SCRIPT = `(function(inputs) {
    /* write code here to transform capability input to definition input.*/

    inputs = JSON.parse(inputs);

    return inputs;
})(inputs);`;
    exports2.DEFAULT_POSTPROCESSOR_SCRIPT = `(function(outputs) {
    /* write code here to transform definition output to capability output.*/

    outputs = JSON.parse(outputs);

    return outputs;
})(outputs);`;
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/skill-plugin-helpers.js
var require_skill_plugin_helpers = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/skill-plugin-helpers.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.getField = getField;
    exports2.sanitizeSkillName = sanitizeSkillName;
    exports2.applyDefaults = applyDefaults;
    exports2.buildDependsOnList = buildDependsOnList;
    exports2.createDependencyEdges = createDependencyEdges;
    var defaults_1 = require_defaults();
    function getField(source, tableName, fieldName, defaultValue) {
      const tableValue = source?.[tableName];
      if (tableValue && typeof tableValue === "object") {
        const fieldValue = tableValue[fieldName];
        if (fieldValue !== void 0) {
          return fieldValue;
        }
      }
      if (defaultValue !== void 0) {
        return defaultValue;
      }
      const tableDefaults = defaults_1.TABLE_DEFAULTS[tableName];
      if (tableDefaults && fieldName in tableDefaults) {
        return tableDefaults[fieldName];
      }
      return void 0;
    }
    __name(getField, "getField");
    function sanitizeSkillName(name) {
      if (!name) {
        return "";
      }
      return name.toString().trim().replace(/\\/g, "").replace(/\s+/g, "_").toLowerCase();
    }
    __name(sanitizeSkillName, "sanitizeSkillName");
    function applyDefaults(tableName, $) {
      const defaults = defaults_1.TABLE_DEFAULTS[tableName] || {};
      return Object.fromEntries(Object.entries(defaults).map(([key, value]) => [key, $.val(value)]));
    }
    __name(applyDefaults, "applyDefaults");
    function buildDependsOnList(tool, toolRecordsMap, arg, diagnostics, decisionToolNames) {
      if (!tool.depends || tool.depends.length === 0) {
        return "";
      }
      const depIds = [];
      for (const depName of tool.depends) {
        const depRecord = toolRecordsMap.get(depName);
        if (!depRecord) {
          if (!decisionToolNames?.has(depName)) {
            diagnostics.warn(arg, `Dependency '${depName}' not found for tool '${tool.name}'`);
          }
          continue;
        }
        depIds.push(depRecord.getId().toString().getValue());
      }
      return depIds.join(",");
    }
    __name(buildDependsOnList, "buildDependsOnList");
    async function createDependencyEdges(tool, toolRecordsMap, resourceMappingRecord, arg, factory, includeScripts = true, source) {
      const edgeRecords = [];
      if (!tool.depends || tool.depends.length === 0) {
        return edgeRecords;
      }
      for (const depName of tool.depends) {
        const depRecord = toolRecordsMap.get(depName);
        if (!depRecord) {
          continue;
        }
        const depResourceCapability = depRecord.get("resource_capability")?.getValue();
        if (depResourceCapability === defaults_1.TOOL_RESOURCE_CAPABILITIES.DECISION) {
          continue;
        }
        const edgeRecord = await factory.createRecord({
          source: source ?? arg,
          table: "sys_one_extend_resource_edge",
          properties: arg.transform(({ $ }) => ({
            ...applyDefaults("sys_one_extend_resource_edge", $),
            ...includeScripts ? {
              applicability_script: $.val(defaults_1.RESOURCE_EDGE_APPLICABILITY_SCRIPT).toCdata(),
              error_handler_script: $.val(defaults_1.RESOURCE_EDGE_ERROR_HANDLER_SCRIPT).toCdata()
            } : {},
            from_resource: $.val(depRecord.getId()),
            internal_name: $.val(`${depName}__${tool.name}`),
            to_resource: $.val(resourceMappingRecord.getId())
          }))
        });
        edgeRecords.push(edgeRecord);
      }
      return edgeRecords;
    }
    __name(createDependencyEdges, "createDependencyEdges");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/security-processor.js
var require_security_processor = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/security-processor.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processSecurityControls = processSecurityControls;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var defaults_1 = require_defaults();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var { SYS_AGENT_ACCESS_ROLE_CONFIGURATION, SYS_AGENT_ACCESS_ROLE_MAPPING, SYS_USER_ROLE } = defaults_1.SECURITY_TABLE_NAMES;
    var SYS_SECURITY_ACL = "sys_security_acl";
    var SYS_SECURITY_ACL_ROLE = "sys_security_acl_role";
    async function processSecurityControls(securityControls, skillName, skillRecordId, arg, factory, diagnostics) {
      const records = [];
      if (!securityControls || !securityControls.isObject()) {
        diagnostics.error(arg, "securityControls is mandatory and must be provided");
      }
      const userAccess = securityControls.get("userAccess");
      if (!userAccess || !userAccess.isObject()) {
        diagnostics.error(securityControls, "securityControls.userAccess is required and must be an object");
      }
      const userAccessObj = userAccess.asObject();
      const accessType = userAccessObj.get("type").asString().getValue();
      const aclExplicitId = userAccessObj.get("$id");
      if (accessType !== "authenticated" && accessType !== "roles") {
        diagnostics.error(userAccessObj, `securityControls.userAccess.type must be 'authenticated' or 'roles'. Got: ${accessType}`);
      }
      let aclRoles = [];
      let securityAttribute = "";
      if (accessType === "authenticated") {
        securityAttribute = defaults_1.ACL_CONSTANTS.USER_IS_AUTHENTICATED;
      }
      const rolesField = userAccessObj.get("roles");
      if (accessType === "roles") {
        if (!rolesField.isArray()) {
          diagnostics.error(userAccessObj, 'securityControls.userAccess.roles must be an array when type is "roles"');
        }
        aclRoles = rolesField.asArray().getElements();
        if (aclRoles.length === 0) {
          diagnostics.error(rolesField, "securityControls.userAccess.roles must contain at least one role");
        }
      } else if (accessType === "authenticated" && rolesField && rolesField.isArray()) {
        aclRoles = rolesField.asArray().getElements();
      }
      if (aclRoles.length > 0) {
        for (const role of aclRoles) {
          if (role.isString()) {
            const roleValue = role.asString().getValue();
            if (defaults_1.RESTRICTED_ROLE_NAMES.includes(roleValue)) {
              diagnostics.error(rolesField, `The '${roleValue}' role cannot be used for ACL roles. This role has special system-level privileges and is restricted.`);
            }
            if (defaults_1.RESTRICTED_ROLE_SYS_IDS.includes(roleValue)) {
              diagnostics.error(rolesField, `The 'maint' role (sys_id: ${roleValue}) cannot be used for ACL roles. This role has special system-level privileges and is restricted.`);
            }
          }
        }
      }
      const sanitizedSkillName = (0, skill_plugin_helpers_1.sanitizeSkillName)(skillName);
      const aclName = `${sanitizedSkillName}.${skillRecordId}`;
      const aclRecord = await factory.createRecord({
        source: securityControls,
        table: SYS_SECURITY_ACL,
        ...aclExplicitId instanceof sdk_build_plugins_1.NowIdShape ? { explicitId: aclExplicitId } : {},
        properties: arg.transform(({ $ }) => ({
          name: $.val(aclName),
          type: $.val(defaults_1.ACL_CONSTANTS.GEN_AI_SKILL_TYPE),
          operation: $.val(defaults_1.ACL_CONSTANTS.OPERATION_EXECUTE),
          active: $.val(defaults_1.ACL_DEFAULTS.active),
          admin_overrides: $.val(defaults_1.ACL_DEFAULTS.admin_overrides),
          advanced: $.val(defaults_1.ACL_DEFAULTS.advanced),
          decision_type: $.val(defaults_1.ACL_DEFAULTS.decision_type),
          local_or_existing: $.val(defaults_1.ACL_DEFAULTS.local_or_existing),
          security_attribute: $.val(securityAttribute)
        }))
      });
      records.push(aclRecord);
      if (aclRoles.length > 0) {
        for (const role of aclRoles) {
          const roleReference = role.isString() ? await factory.createReference({
            source: role,
            table: SYS_USER_ROLE,
            keys: (0, sdk_build_core_1.isGUID)(role.asString().getValue()) ? void 0 : { name: role.asString().getValue() },
            guid: (0, sdk_build_core_1.isGUID)(role.asString().getValue()) ? role.asString().getValue() : void 0
          }) : role;
          const aclRoleRecord = await factory.createRecord({
            source: role,
            table: SYS_SECURITY_ACL_ROLE,
            properties: {
              sys_security_acl: aclRecord,
              sys_user_role: roleReference
            }
          });
          records.push(aclRoleRecord);
        }
      }
      const roleRestrictionsField = securityControls.get("roleRestrictions");
      const roleRestrictionsArray = roleRestrictionsField?.ifArray()?.getElements() ?? [];
      const roleListSysIds = [];
      for (const role of roleRestrictionsArray) {
        if (role.isString()) {
          const roleValue = role.asString().getValue();
          if ((0, sdk_build_core_1.isGUID)(roleValue)) {
            roleListSysIds.push(roleValue);
          } else {
            diagnostics.error(roleRestrictionsField, `roleRestrictions must contain only role sys_ids (GUIDs), not role names. Got: '${roleValue}'. Use the role sys_id when using roleRestrictions (e.g., '${defaults_1.DEFAULT_ADMIN_ROLE_SYS_ID}' for admin).`);
          }
        } else {
          const roleId = role.isRecordId?.() ? role.getValue() : role.toString();
          if (roleId && typeof roleId === "string") {
            roleListSysIds.push(roleId);
          }
        }
      }
      const roleMapField = securityControls.get("roleMap");
      const roleMapArray = roleMapField?.ifArray()?.getElements() ?? [];
      const validatedRoleMapItems = [];
      for (const role of roleMapArray) {
        if (role.isString()) {
          const roleValue = role.asString().getValue();
          if ((0, sdk_build_core_1.isGUID)(roleValue)) {
            diagnostics.error(roleMapField, `roleMap must contain role names, not sys_ids. Got: '${roleValue}'. Use the role name (e.g., 'admin'), or move the sys_id to securityControls.roleRestrictions.`);
          } else if (defaults_1.RESTRICTED_ROLE_NAMES.includes(roleValue)) {
            diagnostics.error(roleMapField, `The '${roleValue}' role cannot be used in roleMap. This role has special system-level privileges and is restricted.`);
          } else {
            validatedRoleMapItems.push(role);
          }
        } else {
          validatedRoleMapItems.push(role);
        }
      }
      if (roleListSysIds.length === 0 && validatedRoleMapItems.length === 0) {
        diagnostics.error(securityControls, "securityControls must have at least one of roleRestrictions or roleMap defined.");
      }
      const roleListValue = roleListSysIds.join(",");
      const roleConfigRecord = await factory.createRecord({
        source: securityControls,
        table: SYS_AGENT_ACCESS_ROLE_CONFIGURATION,
        properties: arg.transform(({ $ }) => ({
          name: $.val(skillName),
          agent: $.val(skillRecordId),
          agent_table: $.val(defaults_1.AGENT_ACCESS_ROLE_CONFIG_DEFAULTS.agent_table),
          action: $.val(defaults_1.AGENT_ACCESS_ROLE_CONFIG_DEFAULTS.action),
          // roleRestrictions → role_list. Empty string when only roleMap is used.
          role_list: $.val(roleListValue)
        }))
      });
      records.push(roleConfigRecord);
      for (const role of validatedRoleMapItems) {
        const roleReference = role.isString() ? await factory.createReference({
          source: role,
          table: SYS_USER_ROLE,
          keys: { name: role.asString().getValue() }
        }) : role;
        const mappingRecord = await factory.createRecord({
          source: role,
          table: SYS_AGENT_ACCESS_ROLE_MAPPING,
          properties: {
            agent_access_config: roleConfigRecord,
            role: roleReference
          }
        });
        records.push(mappingRecord);
      }
      return records;
    }
    __name(processSecurityControls, "processSecurityControls");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tool-utils.js
var require_tool_utils = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tool-utils.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.RawCodeShape = exports2.SYS_GEN_AI_STRATEGY_MAPPING = exports2.SYS_GEN_AI_FEATURE_MAPPING = exports2.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE = exports2.SYS_ONE_EXTEND_DEFINITION_CONFIG = exports2.SYS_ONE_EXTEND_CAPABILITY_DEFINITION = exports2.SYS_ONE_EXTEND_CAPABILITY = exports2.SYS_ONE_EXTEND_RESOURCE_EDGE = exports2.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING = exports2.SYS_ONE_EXTEND_RESOURCE_MAPPING = void 0;
    exports2.normalizeInputFieldName = normalizeInputFieldName;
    exports2.inputChainToTemplate = inputChainToTemplate;
    exports2.resolveTemplateValue = resolveTemplateValue;
    exports2.getResourceCapability = getResourceCapability;
    exports2.wrapConditionScript = wrapConditionScript;
    exports2.generateInputAttributeValueScript = generateInputAttributeValueScript;
    exports2.getApplicabilityProperties = getApplicabilityProperties;
    exports2.extractDependsFromEdges = extractDependsFromEdges;
    exports2.extractConditionFromMapping = extractConditionFromMapping;
    exports2.getAttributeMappingsForTool = getAttributeMappingsForTool;
    exports2.isSysId = isSysId;
    exports2.validateToolDataType = validateToolDataType;
    exports2.resolveAttributeName = resolveAttributeName;
    exports2.topologicalSortTools = topologicalSortTools;
    exports2.sanitizeVarName = sanitizeVarName;
    exports2.templateValueToShape = templateValueToShape;
    var defaults_1 = require_defaults();
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    exports2.SYS_ONE_EXTEND_RESOURCE_MAPPING = defaults_1.TABLE_NAMES.SYS_ONE_EXTEND_RESOURCE_MAPPING, exports2.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING = defaults_1.TABLE_NAMES.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, exports2.SYS_ONE_EXTEND_RESOURCE_EDGE = defaults_1.TABLE_NAMES.SYS_ONE_EXTEND_RESOURCE_EDGE, exports2.SYS_ONE_EXTEND_CAPABILITY = defaults_1.TABLE_NAMES.SYS_ONE_EXTEND_CAPABILITY, exports2.SYS_ONE_EXTEND_CAPABILITY_DEFINITION = defaults_1.TABLE_NAMES.SYS_ONE_EXTEND_CAPABILITY_DEFINITION, exports2.SYS_ONE_EXTEND_DEFINITION_CONFIG = defaults_1.TABLE_NAMES.SYS_ONE_EXTEND_DEFINITION_CONFIG, exports2.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE = defaults_1.TABLE_NAMES.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, exports2.SYS_GEN_AI_FEATURE_MAPPING = defaults_1.TABLE_NAMES.SYS_GEN_AI_FEATURE_MAPPING, exports2.SYS_GEN_AI_STRATEGY_MAPPING = defaults_1.TABLE_NAMES.SYS_GEN_AI_STRATEGY_MAPPING;
    var RawCodeShape = class extends sdk_build_core_1.Shape {
      static {
        __name(this, "RawCodeShape");
      }
      code;
      constructor({ source, code }) {
        super({ source });
        this.code = code;
      }
      getCode() {
        return this.code;
      }
    };
    exports2.RawCodeShape = RawCodeShape;
    function normalizeInputFieldName(name) {
      return name.toLowerCase().replace(/ +/g, "_");
    }
    __name(normalizeInputFieldName, "normalizeInputFieldName");
    function inputChainToTemplate(chain) {
      const firstSep = chain.search(/[.[]/);
      if (firstSep === -1) {
        return normalizeInputFieldName(chain);
      }
      const inputName = chain.substring(0, firstSep);
      let rest = chain.substring(firstSep);
      rest = rest.replace(/(?<!\.)\[/g, ".[");
      return normalizeInputFieldName(inputName) + rest;
    }
    __name(inputChainToTemplate, "inputChainToTemplate");
    function resolveTemplateValue(shape, toolNameToVarName) {
      if (!shape) {
        return "";
      }
      if (shape instanceof sdk_build_core_1.TemplateExpressionShape) {
        let result = shape.getLiteralText();
        for (const span of shape.getSpans()) {
          const expression = span.getExpression();
          const replacement = resolveExpression(expression, toolNameToVarName);
          result += replacement + span.getLiteralText();
        }
        return result;
      }
      if (shape.isString()) {
        return shape.asString().getValue();
      }
      return resolveExpression(shape, toolNameToVarName);
    }
    __name(resolveTemplateValue, "resolveTemplateValue");
    function resolveExpression(expression, toolNameToVarName) {
      if (expression instanceof sdk_build_core_1.PropertyAccessShape) {
        const elements = expression.getElements();
        const firstElement = elements[0]?.getName();
        if (elements.length >= 3 && elements[1]?.getName() === "input") {
          const fieldName = elements[2]?.getName();
          if (fieldName) {
            const normalized = normalizeInputFieldName(fieldName);
            if (elements.length === 3) {
              return `{{${normalized}}}`;
            }
            const rest = elements.slice(3).map((e) => e.getName()).join(".");
            return `{{${normalized}.${rest}}}`;
          }
        }
        if (elements.length === 2 && firstElement) {
          const toolName = toolNameToVarName.get(firstElement);
          if (toolName) {
            const outputField = elements[1]?.getName();
            if (outputField) {
              return `{{${toolName}.${outputField}}}`;
            }
          }
        }
      }
      if (expression instanceof sdk_build_core_1.ElementAccessExpressionShape) {
        const callee = expression.getCallee();
        const arg = expression.getArg();
        if (callee.endsWith(".input") && arg.isString()) {
          return `{{${normalizeInputFieldName(arg.asString().getValue())}}}`;
        }
        if (callee.includes(".input.")) {
          const match = callee.match(/\.input\.(.+)$/);
          if (match && match[1]) {
            if (arg.isString()) {
              return `{{${inputChainToTemplate(match[1])}.${arg.asString().getValue()}}}`;
            }
            return `{{${inputChainToTemplate(match[1])}.[${arg.getCode()}]}}`;
          }
        }
        if (arg.isString()) {
          const toolName = toolNameToVarName.get(callee);
          if (toolName) {
            return `{{${toolName}.${arg.asString().getValue()}}}`;
          }
        }
      }
      const code = expression.getCode();
      if (code) {
        const inputMatch = code.match(/^[a-zA-Z_$]+\.input\.(.+)$/);
        if (inputMatch && inputMatch[1]) {
          return `{{${inputChainToTemplate(inputMatch[1])}}}`;
        }
      }
      return code || "";
    }
    __name(resolveExpression, "resolveExpression");
    function getResourceCapability(toolType, externalId) {
      switch (toolType) {
        case defaults_1.TOOL_TYPES.INLINE_SCRIPT:
          return defaults_1.TOOL_RESOURCE_CAPABILITIES.INLINE_SCRIPT;
        case defaults_1.TOOL_TYPES.WEB_SEARCH:
          return defaults_1.TOOL_RESOURCE_CAPABILITIES.WEB_SEARCH;
        case defaults_1.TOOL_TYPES.DECISION:
          return defaults_1.TOOL_RESOURCE_CAPABILITIES.DECISION;
        case defaults_1.TOOL_TYPES.SKILL:
          return externalId || defaults_1.TOOL_RESOURCE_CAPABILITIES.SKILL;
        case defaults_1.TOOL_TYPES.SCRIPT:
        case defaults_1.TOOL_TYPES.SUBFLOW:
        case defaults_1.TOOL_TYPES.FLOW_ACTION:
          return externalId || "";
        default:
          throw new Error(`Unknown tool type: ${toolType}`);
      }
    }
    __name(getResourceCapability, "getResourceCapability");
    function wrapConditionScript(scriptBody) {
      const trimmed = scriptBody.trim();
      if (trimmed.startsWith("(function")) {
        return trimmed;
      }
      return `(function(currentInputs, context) { /* @param {Object} currentInputs - Inputs provided for the current step.*/ /* @param {Object} context - Execution context, which may include:*/ /* - Previous tool outputs/inputs (e.g., context['ToolName.attributeName'] or context.getValue('ToolName.attributeName')).*/ /* - Additional context information (e.g., context.getAllValues()).*/ /* @returns {Boolean} - Return 'true' if the condition is met; otherwise 'false'.*/ ${trimmed} })(currentInputs, context);`;
    }
    __name(wrapConditionScript, "wrapConditionScript");
    function generateInputAttributeValueScript(inputName, templateValue) {
      const skillInputMatch = templateValue.match(/^\{\{([^.}]+)\}\}$/);
      if (skillInputMatch) {
        return `(function execute(currentInputs, context) {
    return currentInputs.${inputName} || '';
})(currentInputs, context);`;
      }
      const toolOutputMatch = templateValue.match(/^\{\{([^.}]+)\.([^}]+)\}\}$/);
      if (toolOutputMatch) {
        const toolRef = `${toolOutputMatch[1]}.${toolOutputMatch[2]}`;
        return `(function execute(currentInputs, context) {
    return context.getValue('${toolRef}') || '';
})(currentInputs, context);`;
      }
      return defaults_1.RESOURCE_ATTRIBUTE_VALUE_SCRIPT;
    }
    __name(generateInputAttributeValueScript, "generateInputAttributeValueScript");
    function getApplicabilityProperties(condition, $) {
      const applicabilityType = condition?.type || "";
      const applicabilityScript = condition?.script ? wrapConditionScript(condition.script) : defaults_1.RESOURCE_MAPPING_APPLICABILITY_SCRIPT;
      return {
        applicability_type: $.val(applicabilityType),
        applicability_script: $.val(applicabilityScript).toCdata()
      };
    }
    __name(getApplicabilityProperties, "getApplicabilityProperties");
    function extractDependsFromEdges(mappingId, edges, mappingIdToName, decisionMappingIds) {
      const deps = [];
      for (const edge of edges) {
        const toId = edge.get("to_resource")?.getValue();
        const fromId = edge.get("from_resource")?.getValue();
        if (toId === mappingId && fromId) {
          if (decisionMappingIds?.has(fromId)) {
            continue;
          }
          const depName = mappingIdToName.get(fromId);
          if (depName && !deps.includes(depName)) {
            deps.push(depName);
          }
        }
      }
      return deps.length > 0 ? deps : void 0;
    }
    __name(extractDependsFromEdges, "extractDependsFromEdges");
    function extractConditionFromMapping(mapping) {
      const applicabilityType = mapping.get("applicability_type")?.getValue();
      if (!applicabilityType || applicabilityType === "none" || applicabilityType === "") {
        return void 0;
      }
      const applicabilityScript = mapping.get("applicability_script")?.getValue();
      const isDefaultScript = applicabilityScript?.includes("return true;") && applicabilityScript?.includes("currentInputs, context");
      if (applicabilityType === "script" && applicabilityScript && !isDefaultScript) {
        return { type: "script", script: applicabilityScript };
      }
      return void 0;
    }
    __name(extractConditionFromMapping, "extractConditionFromMapping");
    function getAttributeMappingsForTool(mappingId, attributeMappings) {
      const inputs = [];
      const outputs = [];
      for (const attr of attributeMappings) {
        const rmId = attr.get("resource_mapping")?.getValue();
        if (rmId === mappingId) {
          const type = attr.get("type")?.getValue();
          if (type === "input") {
            inputs.push(attr);
          } else if (type === "output") {
            outputs.push(attr);
          }
        }
      }
      return { inputs, outputs };
    }
    __name(getAttributeMappingsForTool, "getAttributeMappingsForTool");
    function isSysId(value) {
      return /^[0-9a-f]{32}$/i.test(value);
    }
    __name(isSysId, "isSysId");
    function validateToolDataType(type, fieldName, toolName, isInput) {
      if (!type) {
        return null;
      }
      const fieldType = isInput ? "input" : "output";
      if (/\s/.test(type)) {
        const suggestedType = type.toLowerCase().replace(/\s+/g, "_");
        return `Tool '${toolName}' has ${fieldType} '${fieldName}' with invalid type '${type}'. Type values should not contain spaces. Did you mean '${suggestedType}'?`;
      }
      const normalizedType = type.toLowerCase();
      if (defaults_1.INVALID_TOOL_INTERNAL_TYPES.includes(normalizedType)) {
        return `Tool '${toolName}' has ${fieldType} '${fieldName}' with invalid type '${type}'. The following types are not supported for FlowAction/Subflow tools: ${defaults_1.INVALID_TOOL_INTERNAL_TYPES.join(", ")}. Please use a valid type: ${defaults_1.VALID_TOOL_DATA_TYPES.join(", ")}.`;
      }
      return null;
    }
    __name(validateToolDataType, "validateToolDataType");
    function resolveAttributeName(rawValue, defAttrNameMap) {
      if (!defAttrNameMap || defAttrNameMap.size === 0) {
        return rawValue;
      }
      const resolved = defAttrNameMap.get(rawValue);
      if (resolved) {
        return resolved;
      }
      if (isSysId(rawValue)) {
        return void 0;
      }
      return rawValue;
    }
    __name(resolveAttributeName, "resolveAttributeName");
    function topologicalSortTools(tools) {
      const toolMap = /* @__PURE__ */ new Map();
      for (const tool of tools) {
        toolMap.set(tool.name, tool);
      }
      const sorted = [];
      const visited = /* @__PURE__ */ new Set();
      const visiting = /* @__PURE__ */ new Set();
      function visit(tool) {
        if (visited.has(tool.name)) {
          return;
        }
        if (visiting.has(tool.name)) {
          return;
        }
        visiting.add(tool.name);
        if (tool.depends && tool.depends.length > 0) {
          for (const depName of tool.depends) {
            const dep = toolMap.get(depName);
            if (dep) {
              visit(dep);
            }
          }
        }
        visiting.delete(tool.name);
        visited.add(tool.name);
        sorted.push(tool);
      }
      __name(visit, "visit");
      for (const tool of tools) {
        visit(tool);
      }
      return sorted;
    }
    __name(topologicalSortTools, "topologicalSortTools");
    function sanitizeVarName(name) {
      let varName = name.replace(/[^a-zA-Z0-9_$]/g, "_");
      if (/^[0-9]/.test(varName)) {
        varName = "_" + varName;
      }
      return varName;
    }
    __name(sanitizeVarName, "sanitizeVarName");
    function templateValueToShape(source, value, paramName, toolNameToVarName, inputNameMap) {
      const match = value.match(/^\{\{(.+)\}\}$/);
      if (match && match[1]) {
        const inner = match[1];
        const dotIdx = inner.indexOf(".");
        if (dotIdx > 0) {
          const firstSegment = inner.substring(0, dotIdx);
          const outputField = inner.substring(dotIdx + 1);
          const varName = toolNameToVarName.get(firstSegment);
          if (varName) {
            return new sdk_build_core_1.PropertyAccessShape({
              source,
              elements: [varName, outputField]
            });
          }
          if (firstSegment === "input") {
            const originalOutputField = inputNameMap?.get(outputField) ?? outputField;
            if (/[^a-zA-Z0-9_$]/.test(originalOutputField)) {
              return new RawCodeShape({
                source,
                code: `${paramName}.input['${originalOutputField}']`
              });
            }
            return new sdk_build_core_1.PropertyAccessShape({
              source,
              elements: [paramName, "input", originalOutputField]
            });
          }
          return new sdk_build_core_1.StringLiteralShape({ source, literalText: value });
        }
        const originalName = inputNameMap?.get(inner) ?? inner;
        if (/[^a-zA-Z0-9_$]/.test(originalName)) {
          return new RawCodeShape({
            source,
            code: `${paramName}.input['${originalName}']`
          });
        }
        return new sdk_build_core_1.PropertyAccessShape({
          source,
          elements: [paramName, "input", originalName]
        });
      }
      return new sdk_build_core_1.StringLiteralShape({ source, literalText: value });
    }
    __name(templateValueToShape, "templateValueToShape");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/inline-script-tool.js
var require_inline_script_tool = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/inline-script-tool.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processInlineScriptTool = processInlineScriptTool;
    var defaults_1 = require_defaults();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var tool_utils_1 = require_tool_utils();
    async function processInlineScriptTool(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames) {
      const records = [];
      if (tool.type !== defaults_1.TOOL_TYPES.INLINE_SCRIPT) {
        diagnostics.error(arg, `Expected inline-script tool but got ${tool.type}`);
        return records;
      }
      if (!tool.name) {
        diagnostics.error(arg, "InlineScript tool requires a name");
        return records;
      }
      const dependsOnList = (0, skill_plugin_helpers_1.buildDependsOnList)(tool, toolRecordsMap, arg, diagnostics, decisionToolNames);
      const resourceMappingRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING,
        ...tool.$id ? { explicitId: tool.$id } : {},
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING, $),
          resource_name: $.val(tool.name),
          parent_capability: $.val(capabilityRecord.getId()),
          resource_capability: $.val((0, tool_utils_1.getResourceCapability)(tool.type)),
          ...(0, tool_utils_1.getApplicabilityProperties)(tool.condition, $),
          depends_on_list: $.val(dependsOnList)
        }))
      });
      records.push(resourceMappingRecord);
      toolRecordsMap.set(tool.name, resourceMappingRecord);
      const scriptContent = tool.script;
      const wrappedScript = scriptContent?.startsWith("(function") ? scriptContent : `(function runScript(context) {
    ${scriptContent}
})(context);`;
      const inputAttributeRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
          resource_mapping: $.val(resourceMappingRecord.getId()),
          resource_attribute_name: $.val(defaults_1.TOOL_ATTRIBUTE_IDS.INLINE_SCRIPT_INPUT),
          attribute_value_script: $.val(wrappedScript).toCdata(),
          attribute_value_template: $.val(wrappedScript).toCdata(),
          type: $.val("input")
        }))
      });
      records.push(inputAttributeRecord);
      const outputAttributeRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
          resource_attribute_name: $.val(defaults_1.TOOL_ATTRIBUTE_IDS.INLINE_SCRIPT_OUTPUT),
          resource_mapping: $.val(resourceMappingRecord.getId()),
          truncate: $.val(tool.truncate ?? false),
          type: $.val("output")
        }))
      });
      records.push(outputAttributeRecord);
      const edgeRecords = await (0, skill_plugin_helpers_1.createDependencyEdges)(tool, toolRecordsMap, resourceMappingRecord, arg, factory, false, tool._source);
      records.push(...edgeRecords);
      return records;
    }
    __name(processInlineScriptTool, "processInlineScriptTool");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/tool-capability-helpers.js
var require_tool_capability_helpers = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/tool-capability-helpers.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.createToolCapabilityInfra = createToolCapabilityInfra;
    exports2.createInputDefinitionAttributes = createInputDefinitionAttributes;
    exports2.createInputAttributeMappings = createInputAttributeMappings;
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var tool_utils_1 = require_tool_utils();
    async function createToolCapabilityInfra(tool, capabilityRecord, capabilityName, capDefConfig, arg, factory) {
      const records = [];
      const toolCapabilityExplicitId = tool.$capabilityId ? tool.$capabilityId : `${capabilityRecord.getId().toString().getValue()}__tool_${tool.name}`;
      const toolCapabilityRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_CAPABILITY,
        explicitId: toolCapabilityExplicitId,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_CAPABILITY, $),
          name: $.val(capabilityName),
          type: $.val("datasource")
        }))
      });
      records.push(toolCapabilityRecord);
      const toolCapDefRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_CAPABILITY_DEFINITION,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_CAPABILITY_DEFINITION, $),
          name: $.val(capDefConfig.name),
          capability: $.val(toolCapabilityRecord.getId()),
          api: $.val(capDefConfig.api),
          api_type: $.val(capDefConfig.apiType),
          ...capDefConfig.metadata ? { metadata: $.val(capDefConfig.metadata) } : {}
        }))
      });
      records.push(toolCapDefRecord);
      const toolDefConfigRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_DEFINITION_CONFIG,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_DEFINITION_CONFIG, $),
          capability: $.val(toolCapabilityRecord.getId()),
          definition: $.val(toolCapDefRecord.getId()),
          default: $.val("1")
        }))
      });
      records.push(toolDefConfigRecord);
      const featureMappingRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_GEN_AI_FEATURE_MAPPING,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_GEN_AI_FEATURE_MAPPING, $),
          document: $.val(toolCapabilityRecord.getId()),
          document_table: $.val("sys_one_extend_capability"),
          feature_name: $.val(capabilityName)
        }))
      });
      records.push(featureMappingRecord);
      const strategyMappingRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_GEN_AI_STRATEGY_MAPPING,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_GEN_AI_STRATEGY_MAPPING, $),
          feature: $.val(featureMappingRecord.getId()),
          strategy: $.val("CAPABILITY_EXECUTION")
        }))
      });
      records.push(strategyMappingRecord);
      return {
        records,
        toolCapabilityRecord,
        toolCapabilityExplicitId,
        toolCapDefRecord,
        featureMappingRecord
      };
    }
    __name(createToolCapabilityInfra, "createToolCapabilityInfra");
    async function createInputDefinitionAttributes(tool, toolCapabilityRecord, arg, factory) {
      const records = [];
      const inputDefAttrRecordsMap = /* @__PURE__ */ new Map();
      if (tool.inputs) {
        for (const input of tool.inputs) {
          const internalName = (0, tool_utils_1.normalizeInputFieldName)(input.name);
          const inputDefAttrRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
            explicitId: input.$id,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
              capability: $.val(toolCapabilityRecord.getId()),
              name: $.val(internalName),
              label: $.val(input.name),
              type: $.val("input"),
              ...input.type ? { data_type: $.val(input.type) } : {}
            }))
          });
          records.push(inputDefAttrRecord);
          inputDefAttrRecordsMap.set(input.name, inputDefAttrRecord);
        }
      }
      return { inputDefAttrRecordsMap, records };
    }
    __name(createInputDefinitionAttributes, "createInputDefinitionAttributes");
    async function createInputAttributeMappings(tool, resourceMappingRecord, arg, factory, inputDefAttrRecordsMap) {
      const records = [];
      if (tool.inputs) {
        for (const input of tool.inputs) {
          const isScripted = input.valueType === "script";
          const inputScript = isScripted ? input.scriptValue : (0, tool_utils_1.generateInputAttributeValueScript)(input.name, input.value);
          const inputDefAttrRecord = inputDefAttrRecordsMap?.get(input.name);
          const inputAttributeRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
              attribute_value_script: $.val(inputScript).toCdata(),
              attribute_value_template: $.val(input.value),
              attribute_value_type: $.val(isScripted ? "script" : "template"),
              resource_attribute_name: inputDefAttrRecord ? $.val(inputDefAttrRecord.getId()) : $.val(input.name),
              resource_mapping: $.val(resourceMappingRecord.getId()),
              type: $.val("input")
            }))
          });
          records.push(inputAttributeRecord);
        }
      }
      return records;
    }
    __name(createInputAttributeMappings, "createInputAttributeMappings");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/script-tool.js
var require_script_tool = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/script-tool.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processScriptTool = processScriptTool;
    var defaults_1 = require_defaults();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var tool_utils_1 = require_tool_utils();
    var tool_capability_helpers_1 = require_tool_capability_helpers();
    async function processScriptTool(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames) {
      const records = [];
      if (tool.type !== defaults_1.TOOL_TYPES.SCRIPT) {
        diagnostics.error(arg, `Expected script tool but got ${tool.type}`);
        return records;
      }
      if (!tool.name) {
        diagnostics.error(arg, "Script tool requires a name");
        return records;
      }
      const dependsOnList = (0, skill_plugin_helpers_1.buildDependsOnList)(tool, toolRecordsMap, arg, diagnostics, decisionToolNames);
      const metadata = {
        toolType: defaults_1.TOOL_TYPES.SCRIPT
      };
      if (tool.scriptId) {
        metadata["scriptId"] = tool.scriptId;
      }
      if (tool.scriptFunctionName) {
        metadata["scriptFunctionName"] = tool.scriptFunctionName;
      }
      const capDefMetadata = {};
      if (tool.scriptFunctionName) {
        capDefMetadata["scriptFunctionName"] = tool.scriptFunctionName;
      }
      if (tool.scriptId) {
        capDefMetadata["scriptId"] = tool.scriptId;
      }
      const capabilityName = tool.scriptId ? `${tool.scriptId}_${tool.name}` : tool.name;
      const capDefName = tool.scriptFunctionName ? `${capabilityName}.${tool.scriptFunctionName}` : capabilityName;
      const infraResult = await (0, tool_capability_helpers_1.createToolCapabilityInfra)(tool, capabilityRecord, capabilityName, {
        name: capDefName,
        api: defaults_1.SCRIPT_TOOL_API_ID,
        apiType: defaults_1.SYS_ONE_EXTEND_CAPABILITY_DEFINITION_API_TYPES.SYS_HUB_ACTION_TYPE_DEFINITION,
        metadata: JSON.stringify(capDefMetadata)
      }, arg, factory);
      records.push(...infraResult.records);
      const { toolCapabilityRecord, toolCapabilityExplicitId } = infraResult;
      const { inputDefAttrRecordsMap, records: inputDefAttrRecords } = await (0, tool_capability_helpers_1.createInputDefinitionAttributes)(tool, toolCapabilityRecord, arg, factory);
      records.push(...inputDefAttrRecords);
      const outputExplicitId = tool.output?.$id ?? (toolCapabilityExplicitId ? `${toolCapabilityExplicitId}__output` : void 0);
      const outputName = tool.output?.name ?? "output";
      const outputLabel = outputName.charAt(0).toUpperCase() + outputName.slice(1);
      const outputDefAttrRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
        ...outputExplicitId ? { explicitId: outputExplicitId } : {},
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
          capability: $.val(toolCapabilityRecord.getId()),
          name: $.val(outputName),
          label: $.val(outputLabel),
          type: $.val("output"),
          mandatory: $.val(true)
        }))
      });
      records.push(outputDefAttrRecord);
      const resourceMappingRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING,
        ...tool.$id ? { explicitId: tool.$id } : {},
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING, $),
          ...(0, tool_utils_1.getApplicabilityProperties)(tool.condition, $),
          depends_on_list: $.val(dependsOnList),
          metadata: $.val(JSON.stringify(metadata)),
          parent_capability: $.val(capabilityRecord.getId()),
          resource_capability: $.val(toolCapabilityRecord.getId()),
          resource_name: $.val(tool.name)
        }))
      });
      records.push(resourceMappingRecord);
      toolRecordsMap.set(tool.name, resourceMappingRecord);
      const inputAttrRecords = await (0, tool_capability_helpers_1.createInputAttributeMappings)(tool, resourceMappingRecord, arg, factory, inputDefAttrRecordsMap);
      records.push(...inputAttrRecords);
      const truncate = tool.truncate ?? false;
      const outputAttributeRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
          attribute_value_script: $.val(defaults_1.RESOURCE_ATTRIBUTE_VALUE_SCRIPT).toCdata(),
          resource_attribute_name: $.val(outputDefAttrRecord.getId()),
          resource_mapping: $.val(resourceMappingRecord.getId()),
          truncate: $.val(truncate),
          type: $.val("output")
        }))
      });
      records.push(outputAttributeRecord);
      const edgeRecords = await (0, skill_plugin_helpers_1.createDependencyEdges)(tool, toolRecordsMap, resourceMappingRecord, arg, factory, true, tool._source);
      records.push(...edgeRecords);
      return records;
    }
    __name(processScriptTool, "processScriptTool");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/subflow-tool.js
var require_subflow_tool = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/subflow-tool.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processSubflowTool = processSubflowTool;
    var defaults_1 = require_defaults();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var tool_utils_1 = require_tool_utils();
    async function processSubflowTool(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames) {
      const records = [];
      if (tool.type !== defaults_1.TOOL_TYPES.SUBFLOW) {
        diagnostics.error(arg, `Expected subflow tool but got ${tool.type}`);
        return records;
      }
      if (!tool.name) {
        diagnostics.error(arg, "Subflow tool requires a name");
        return records;
      }
      if (!tool.subflowId) {
        diagnostics.error(arg, "Subflow tool requires a subflowId");
        return records;
      }
      const dependsOnList = (0, skill_plugin_helpers_1.buildDependsOnList)(tool, toolRecordsMap, arg, diagnostics, decisionToolNames);
      const capabilityName = `${tool.subflowId}_${tool.name}`;
      const toolCapabilityExplicitId = tool.$capabilityId ? tool.$capabilityId : `${capabilityRecord.getId().toString().getValue()}__tool_${tool.name}`;
      const toolCapabilityRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_CAPABILITY,
        explicitId: toolCapabilityExplicitId,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_CAPABILITY, $),
          name: $.val(capabilityName),
          type: $.val("datasource")
        }))
      });
      records.push(toolCapabilityRecord);
      const toolCapDefRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_CAPABILITY_DEFINITION,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_CAPABILITY_DEFINITION, $),
          name: $.val(capabilityName),
          capability: $.val(toolCapabilityRecord.getId()),
          api: $.val(tool.subflowId),
          api_type: $.val(defaults_1.SYS_ONE_EXTEND_CAPABILITY_DEFINITION_API_TYPES.SYS_HUB_FLOW)
        }))
      });
      records.push(toolCapDefRecord);
      const toolDefConfigRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_DEFINITION_CONFIG,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_DEFINITION_CONFIG, $),
          capability: $.val(toolCapabilityRecord.getId()),
          definition: $.val(toolCapDefRecord.getId()),
          default: $.val("1")
        }))
      });
      records.push(toolDefConfigRecord);
      const inputDefAttrRecordsMap = /* @__PURE__ */ new Map();
      if (tool.inputs) {
        for (const input of tool.inputs) {
          const inputTypeError = (0, tool_utils_1.validateToolDataType)(input.type, input.name, tool.name, true);
          if (inputTypeError) {
            diagnostics.error(arg, inputTypeError);
            return records;
          }
          const internalName = (0, tool_utils_1.normalizeInputFieldName)(input.name);
          const inputDataType = input.type || "string";
          const inputDefAttrRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
            explicitId: input.$id,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
              capability: $.val(toolCapabilityRecord.getId()),
              name: $.val(internalName),
              label: $.val(input.name),
              type: $.val("input"),
              data_type: $.val(inputDataType)
            }))
          });
          records.push(inputDefAttrRecord);
          inputDefAttrRecordsMap.set(input.name, inputDefAttrRecord);
        }
      }
      const outputDefAttrRecordsMap = /* @__PURE__ */ new Map();
      if (tool.outputs && tool.outputs.length > 0) {
        for (const output of tool.outputs) {
          const outputTypeError = (0, tool_utils_1.validateToolDataType)(output.type, output.name, tool.name, false);
          if (outputTypeError) {
            diagnostics.error(arg, outputTypeError);
            return records;
          }
          const internalName = (0, tool_utils_1.normalizeInputFieldName)(output.name);
          const outputDataType = output.type || "object";
          const outputDefAttrRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
            explicitId: output.$id,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
              capability: $.val(toolCapabilityRecord.getId()),
              name: $.val(internalName),
              label: $.val(output.name),
              type: $.val("output"),
              data_type: $.val(outputDataType)
            }))
          });
          records.push(outputDefAttrRecord);
          outputDefAttrRecordsMap.set(output.name, outputDefAttrRecord);
        }
      } else {
        const defaultOutputDefAttrRecord = await factory.createRecord({
          source: tool._source,
          table: tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
          explicitId: `${toolCapabilityExplicitId}__defattr_output`,
          properties: arg.transform(({ $ }) => ({
            ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
            capability: $.val(toolCapabilityRecord.getId()),
            name: $.val("output"),
            label: $.val("Output"),
            type: $.val("output")
          }))
        });
        records.push(defaultOutputDefAttrRecord);
        outputDefAttrRecordsMap.set("Output", defaultOutputDefAttrRecord);
      }
      const featureMappingRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_GEN_AI_FEATURE_MAPPING,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_GEN_AI_FEATURE_MAPPING, $),
          document: $.val(toolCapabilityRecord.getId()),
          document_table: $.val("sys_one_extend_capability"),
          feature_name: $.val(capabilityName)
        }))
      });
      records.push(featureMappingRecord);
      const strategyMappingRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_GEN_AI_STRATEGY_MAPPING,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_GEN_AI_STRATEGY_MAPPING, $),
          feature: $.val(featureMappingRecord.getId()),
          strategy: $.val("CAPABILITY_EXECUTION")
        }))
      });
      records.push(strategyMappingRecord);
      const metadata = {
        toolType: defaults_1.TOOL_TYPES.SUBFLOW,
        subflowId: tool.subflowId
      };
      const resourceMappingRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING,
        ...tool.$id ? { explicitId: tool.$id } : {},
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING, $),
          ...(0, tool_utils_1.getApplicabilityProperties)(tool.condition, $),
          depends_on_list: $.val(dependsOnList),
          metadata: $.val(JSON.stringify(metadata)),
          parent_capability: $.val(capabilityRecord.getId()),
          resource_capability: $.val(toolCapabilityRecord.getId()),
          resource_name: $.val(tool.name)
        }))
      });
      records.push(resourceMappingRecord);
      toolRecordsMap.set(tool.name, resourceMappingRecord);
      if (tool.inputs) {
        for (const input of tool.inputs) {
          const isScripted = input.valueType === "script";
          const inputScript = isScripted ? input.scriptValue : (0, tool_utils_1.generateInputAttributeValueScript)(input.name, input.value);
          const inputDefAttrRecord = inputDefAttrRecordsMap.get(input.name);
          const inputAttributeRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
              attribute_value_script: $.val(inputScript).toCdata(),
              attribute_value_template: $.val(input.value),
              attribute_value_type: $.val(isScripted ? "script" : "template"),
              resource_attribute_name: inputDefAttrRecord ? $.val(inputDefAttrRecord.getId()) : $.val(input.name),
              resource_mapping: $.val(resourceMappingRecord.getId()),
              type: $.val("input")
            }))
          });
          records.push(inputAttributeRecord);
        }
      }
      if (tool.outputs && tool.outputs.length > 0) {
        for (const output of tool.outputs) {
          const outputTruncate = output.truncate ?? false;
          const outputDefAttrRecord = outputDefAttrRecordsMap.get(output.name);
          const outputAttributeRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
              attribute_value_script: $.val(defaults_1.RESOURCE_ATTRIBUTE_VALUE_SCRIPT).toCdata(),
              resource_attribute_name: outputDefAttrRecord ? $.val(outputDefAttrRecord.getId()) : $.val(output.name),
              resource_mapping: $.val(resourceMappingRecord.getId()),
              truncate: $.val(outputTruncate),
              type: $.val("output")
            }))
          });
          records.push(outputAttributeRecord);
        }
      } else {
        const truncate = tool.truncate ?? false;
        const defaultOutputDefAttrRecord = outputDefAttrRecordsMap.get("Output");
        const outputAttributeRecord = await factory.createRecord({
          source: tool._source,
          table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
          properties: arg.transform(({ $ }) => ({
            ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
            attribute_value_script: $.val(defaults_1.RESOURCE_ATTRIBUTE_VALUE_SCRIPT).toCdata(),
            resource_attribute_name: defaultOutputDefAttrRecord ? $.val(defaultOutputDefAttrRecord.getId()) : $.val("Output"),
            resource_mapping: $.val(resourceMappingRecord.getId()),
            truncate: $.val(truncate),
            type: $.val("output")
          }))
        });
        records.push(outputAttributeRecord);
      }
      const edgeRecords = await (0, skill_plugin_helpers_1.createDependencyEdges)(tool, toolRecordsMap, resourceMappingRecord, arg, factory, true, tool._source);
      records.push(...edgeRecords);
      return records;
    }
    __name(processSubflowTool, "processSubflowTool");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/web-search-tool.js
var require_web_search_tool = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/web-search-tool.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processWebSearchTool = processWebSearchTool;
    var defaults_1 = require_defaults();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var tool_utils_1 = require_tool_utils();
    async function processWebSearchTool(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames) {
      const records = [];
      if (tool.type !== defaults_1.TOOL_TYPES.WEB_SEARCH) {
        diagnostics.error(arg, `Expected web-search tool but got ${tool.type}`);
        return records;
      }
      if (!tool.name) {
        diagnostics.error(arg, "WebSearch tool requires a name");
        return records;
      }
      if (!tool.searchType) {
        diagnostics.error(arg, `WebSearch tool '${tool.name}' requires a searchType ('ai_answers' or 'searching_and_scraping')`);
        return records;
      }
      if (!tool.query && !tool.queryScriptValue) {
        diagnostics.error(arg, `WebSearch tool '${tool.name}' requires a query`);
        return records;
      }
      const dependsOnList = (0, skill_plugin_helpers_1.buildDependsOnList)(tool, toolRecordsMap, arg, diagnostics, decisionToolNames);
      const resourceMappingRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING, $),
          resource_name: $.val(tool.name),
          parent_capability: $.val(capabilityRecord.getId()),
          resource_capability: $.val((0, tool_utils_1.getResourceCapability)(tool.type)),
          ...(0, tool_utils_1.getApplicabilityProperties)(tool.condition, $),
          depends_on_list: $.val(dependsOnList)
        }))
      });
      records.push(resourceMappingRecord);
      toolRecordsMap.set(tool.name, resourceMappingRecord);
      const inputMappingConfig = tool.searchType === "searching_and_scraping" ? defaults_1.WEBSEARCH_SCRAPING_CONFIG.INPUT_MAPPINGS : defaults_1.WEBSEARCH_AI_ANSWERS_CONFIG.INPUT_MAPPINGS;
      const toolValues = {
        query: tool.query,
        searchType: tool.searchType,
        aiSearchProviders: tool.aiSearchProviders,
        sites: tool.sites,
        numResults: tool.numResults,
        country: tool.country,
        maxTokens: tool.maxTokens
      };
      const scriptedFields = {};
      if (tool.queryValueType === "script" && tool.queryScriptValue) {
        scriptedFields["query"] = { scriptValue: tool.queryScriptValue, valueType: "script" };
      }
      if (tool.sitesValueType === "script" && tool.sitesScriptValue) {
        scriptedFields["sites"] = { scriptValue: tool.sitesScriptValue, valueType: "script" };
      }
      for (const attrConfig of inputMappingConfig) {
        const scripted = scriptedFields[attrConfig.fluentName];
        let attributeValueTemplate = "";
        let attributeValueScript = defaults_1.RESOURCE_ATTRIBUTE_VALUE_SCRIPT;
        let attributeValueType = "template";
        if (scripted) {
          attributeValueScript = scripted.scriptValue;
          attributeValueType = "script";
        } else {
          const rawValue = toolValues[attrConfig.fluentName];
          if (rawValue !== void 0 && rawValue !== "") {
            attributeValueTemplate = String(rawValue);
          } else if ("default" in attrConfig && attrConfig.default !== void 0) {
            attributeValueTemplate = String(attrConfig.default);
          }
          if (!attrConfig.required && !attributeValueTemplate) {
            continue;
          }
          if (attrConfig.fluentName === "aiSearchProviders" && attributeValueTemplate) {
            const providerKey = attributeValueTemplate;
            const providerConfig = defaults_1.AI_SEARCH_PROVIDERS[providerKey];
            if (providerConfig) {
              attributeValueTemplate = JSON.stringify({ ...providerConfig, elm: {} });
            }
          }
        }
        const inputAttributeRecord = await factory.createRecord({
          source: tool._source,
          table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
          properties: arg.transform(({ $ }) => ({
            ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
            attribute_value_script: $.val(attributeValueScript).toCdata(),
            attribute_value_template: $.val(attributeValueTemplate),
            attribute_value_type: $.val(attributeValueType),
            resource_attribute_name: $.val(attrConfig.attributeId),
            resource_mapping: $.val(resourceMappingRecord.getId()),
            type: $.val("input")
          }))
        });
        records.push(inputAttributeRecord);
      }
      const outputMappingConfig = defaults_1.WEBSEARCH_AI_ANSWERS_CONFIG.OUTPUT_MAPPINGS;
      const outputNameToTruncateKey = {
        provider: "provider",
        response: "response",
        error: "error",
        errorcode: "errorcode",
        status: "status"
      };
      for (const outputAttr of outputMappingConfig) {
        const truncateKey = outputNameToTruncateKey[outputAttr.name] || outputAttr.name;
        const truncateValue = tool.truncateConfig?.[truncateKey] ?? false;
        const outputAttributeRecord = await factory.createRecord({
          source: tool._source,
          table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
          properties: arg.transform(({ $ }) => ({
            ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
            attribute_value_script: $.val(defaults_1.RESOURCE_ATTRIBUTE_VALUE_SCRIPT).toCdata(),
            resource_attribute_name: $.val(outputAttr.attributeId),
            resource_mapping: $.val(resourceMappingRecord.getId()),
            truncate: $.val(truncateValue),
            type: $.val("output")
          }))
        });
        records.push(outputAttributeRecord);
      }
      const edgeRecords = await (0, skill_plugin_helpers_1.createDependencyEdges)(tool, toolRecordsMap, resourceMappingRecord, arg, factory, true, tool._source);
      records.push(...edgeRecords);
      return records;
    }
    __name(processWebSearchTool, "processWebSearchTool");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/flow-action-tool.js
var require_flow_action_tool = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/flow-action-tool.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processFlowActionTool = processFlowActionTool;
    var defaults_1 = require_defaults();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var tool_utils_1 = require_tool_utils();
    var tool_capability_helpers_1 = require_tool_capability_helpers();
    async function processFlowActionTool(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames) {
      const records = [];
      if (tool.type !== defaults_1.TOOL_TYPES.FLOW_ACTION) {
        diagnostics.error(arg, `Expected flow-action tool but got ${tool.type}`);
        return records;
      }
      if (!tool.name) {
        diagnostics.error(arg, "FlowAction tool requires a name");
        return records;
      }
      if (!tool.actionId) {
        diagnostics.error(arg, "FlowAction tool requires an actionId");
        return records;
      }
      const dependsOnList = (0, skill_plugin_helpers_1.buildDependsOnList)(tool, toolRecordsMap, arg, diagnostics, decisionToolNames);
      const capabilityName = `${tool.actionId}_${tool.name}`;
      const infraResult = await (0, tool_capability_helpers_1.createToolCapabilityInfra)(tool, capabilityRecord, capabilityName, {
        name: capabilityName,
        api: tool.actionId,
        apiType: defaults_1.SYS_ONE_EXTEND_CAPABILITY_DEFINITION_API_TYPES.SYS_HUB_ACTION_TYPE_DEFINITION
      }, arg, factory);
      records.push(...infraResult.records);
      const { toolCapabilityRecord, toolCapabilityExplicitId } = infraResult;
      const inputDefAttrRecordsMap = /* @__PURE__ */ new Map();
      if (tool.inputs) {
        for (const input of tool.inputs) {
          const inputTypeError = (0, tool_utils_1.validateToolDataType)(input.type, input.name, tool.name, true);
          if (inputTypeError) {
            diagnostics.error(arg, inputTypeError);
            return records;
          }
          const internalName = (0, tool_utils_1.normalizeInputFieldName)(input.name);
          const inputDataType = input.type || "string";
          const inputDefAttrRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
            explicitId: input.$id,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
              capability: $.val(toolCapabilityRecord.getId()),
              name: $.val(internalName),
              label: $.val(input.name),
              type: $.val("input"),
              data_type: $.val(inputDataType)
            }))
          });
          records.push(inputDefAttrRecord);
          inputDefAttrRecordsMap.set(input.name, inputDefAttrRecord);
        }
      }
      const outputDefAttrRecordsMap = /* @__PURE__ */ new Map();
      if (tool.outputs && tool.outputs.length > 0) {
        for (const output of tool.outputs) {
          const outputTypeError = (0, tool_utils_1.validateToolDataType)(output.type, output.name, tool.name, false);
          if (outputTypeError) {
            diagnostics.error(arg, outputTypeError);
            return records;
          }
          const internalName = (0, tool_utils_1.normalizeInputFieldName)(output.name);
          const outputDataType = output.type || "string";
          const outputDefAttrRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
            explicitId: output.$id,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
              capability: $.val(toolCapabilityRecord.getId()),
              name: $.val(internalName),
              label: $.val(output.name),
              type: $.val("output"),
              data_type: $.val(outputDataType)
            }))
          });
          records.push(outputDefAttrRecord);
          outputDefAttrRecordsMap.set(output.name, outputDefAttrRecord);
        }
      }
      const metadata = {
        toolType: defaults_1.TOOL_TYPES.FLOW_ACTION,
        actionId: tool.actionId
      };
      const resourceMappingRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING,
        ...tool.$id ? { explicitId: tool.$id } : {},
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING, $),
          applicability_script: $.val(defaults_1.RESOURCE_MAPPING_APPLICABILITY_SCRIPT).toCdata(),
          depends_on_list: $.val(dependsOnList),
          metadata: $.val(JSON.stringify(metadata)),
          parent_capability: $.val(capabilityRecord.getId()),
          resource_capability: $.val(toolCapabilityRecord.getId()),
          resource_name: $.val(tool.name)
        }))
      });
      records.push(resourceMappingRecord);
      toolRecordsMap.set(tool.name, resourceMappingRecord);
      if (tool.inputs) {
        for (const input of tool.inputs) {
          const isScripted = input.valueType === "script";
          const inputScript = isScripted ? input.scriptValue : (0, tool_utils_1.generateInputAttributeValueScript)(input.name, input.value);
          const inputDefAttrRecord = inputDefAttrRecordsMap.get(input.name);
          const inputAttributeRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
              attribute_value_script: $.val(inputScript).toCdata(),
              attribute_value_template: $.val(input.value),
              attribute_value_type: $.val(isScripted ? "script" : "template"),
              resource_attribute_name: inputDefAttrRecord ? $.val(inputDefAttrRecord.getId()) : $.val(input.name),
              resource_mapping: $.val(resourceMappingRecord.getId()),
              type: $.val("input")
            }))
          });
          records.push(inputAttributeRecord);
        }
      }
      if (tool.outputs) {
        for (const output of tool.outputs) {
          const outputTruncate = output.truncate ?? false;
          const outputDefAttrRecord = outputDefAttrRecordsMap.get(output.name);
          const outputAttributeRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
              attribute_value_script: $.val(defaults_1.RESOURCE_ATTRIBUTE_VALUE_SCRIPT).toCdata(),
              resource_attribute_name: outputDefAttrRecord ? $.val(outputDefAttrRecord.getId()) : $.val(output.name),
              resource_mapping: $.val(resourceMappingRecord.getId()),
              truncate: $.val(outputTruncate),
              type: $.val("output")
            }))
          });
          records.push(outputAttributeRecord);
        }
      }
      const edgeRecords = await (0, skill_plugin_helpers_1.createDependencyEdges)(tool, toolRecordsMap, resourceMappingRecord, arg, factory, true, tool._source);
      records.push(...edgeRecords);
      return records;
    }
    __name(processFlowActionTool, "processFlowActionTool");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/skill-tool.js
var require_skill_tool = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/skill-tool.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processSkillTool = processSkillTool;
    var defaults_1 = require_defaults();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var tool_utils_1 = require_tool_utils();
    async function processSkillTool(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames) {
      const records = [];
      if (tool.type !== defaults_1.TOOL_TYPES.SKILL) {
        diagnostics.error(arg, `Expected skill tool but got ${tool.type}`);
        return records;
      }
      if (!tool.name) {
        diagnostics.error(arg, "Skill tool requires a name");
        return records;
      }
      diagnostics.warn(arg, "Skills used as tools must be published to work at runtime. Publish the referenced skill from the NASK UI .");
      const dependsOnList = (0, skill_plugin_helpers_1.buildDependsOnList)(tool, toolRecordsMap, arg, diagnostics, decisionToolNames);
      const metadata = {
        toolType: defaults_1.TOOL_TYPES.SKILL
      };
      let resourceCapabilityShape;
      if (tool.skillIdIsReference && tool.skillIdExplicitId) {
        resourceCapabilityShape = new sdk_build_plugins_1.NowIdShape({
          source: tool._source,
          id: tool.skillIdExplicitId
        });
      }
      const resourceMappingRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING,
        ...tool.$id ? { explicitId: tool.$id } : {},
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING, $),
          ...(0, tool_utils_1.getApplicabilityProperties)(tool.condition, $),
          depends_on_list: $.val(dependsOnList),
          metadata: $.val(JSON.stringify(metadata)),
          parent_capability: $.val(capabilityRecord.getId()),
          resource_capability: resourceCapabilityShape ? $.val(resourceCapabilityShape) : $.val((0, tool_utils_1.getResourceCapability)(tool.type, tool.skillId)),
          resource_name: $.val(tool.name)
        }))
      });
      records.push(resourceMappingRecord);
      toolRecordsMap.set(tool.name, resourceMappingRecord);
      if (tool.inputs) {
        for (const input of tool.inputs) {
          const isScripted = input.valueType === "script";
          const inputScript = isScripted ? input.scriptValue : (0, tool_utils_1.generateInputAttributeValueScript)(input.name, input.value);
          const inputAttributeRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
              attribute_value_script: $.val(inputScript).toCdata(),
              attribute_value_template: $.val(input.value),
              attribute_value_type: $.val(isScripted ? "script" : "template"),
              resource_attribute_name: $.val(input.name),
              resource_mapping: $.val(resourceMappingRecord.getId()),
              type: $.val("input")
            }))
          });
          records.push(inputAttributeRecord);
        }
      }
      if (tool.skillOutputs) {
        for (const [_outputKey, output] of Object.entries(tool.skillOutputs)) {
          const outputTruncate = output.truncate ?? false;
          const outputAttributeRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, $),
              attribute_value_script: $.val(defaults_1.RESOURCE_ATTRIBUTE_VALUE_SCRIPT).toCdata(),
              resource_attribute_name: $.val(output.definitionAttributeId),
              resource_mapping: $.val(resourceMappingRecord.getId()),
              truncate: $.val(outputTruncate),
              type: $.val("output")
            }))
          });
          records.push(outputAttributeRecord);
        }
      }
      const edgeRecords = await (0, skill_plugin_helpers_1.createDependencyEdges)(tool, toolRecordsMap, resourceMappingRecord, arg, factory, true, tool._source);
      records.push(...edgeRecords);
      return records;
    }
    __name(processSkillTool, "processSkillTool");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/decision-tool.js
var require_decision_tool = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tools/decision-tool.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processDecisionTool = processDecisionTool;
    exports2.createDecisionEdges = createDecisionEdges;
    var defaults_1 = require_defaults();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var tool_utils_1 = require_tool_utils();
    async function processDecisionTool(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames) {
      const records = [];
      if (tool.type !== defaults_1.TOOL_TYPES.DECISION) {
        diagnostics.error(arg, `Expected decision tool but got ${tool.type}`);
        return records;
      }
      if (!tool.name) {
        diagnostics.error(arg, "Decision tool requires a name");
        return records;
      }
      const dependsOnList = (0, skill_plugin_helpers_1.buildDependsOnList)(tool, toolRecordsMap, arg, diagnostics, decisionToolNames);
      const resourceMappingRecord = await factory.createRecord({
        source: tool._source,
        table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING,
        ...tool.$id ? { explicitId: tool.$id } : {},
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING, $),
          applicability_script: $.val(defaults_1.RESOURCE_MAPPING_APPLICABILITY_SCRIPT).toCdata(),
          depends_on_list: $.val(dependsOnList),
          parent_capability: $.val(capabilityRecord.getId()),
          resource_capability: $.val((0, tool_utils_1.getResourceCapability)(tool.type)),
          resource_name: $.val(tool.name)
        }))
      });
      records.push(resourceMappingRecord);
      toolRecordsMap.set(tool.name, resourceMappingRecord);
      const edgeRecords = await (0, skill_plugin_helpers_1.createDependencyEdges)(tool, toolRecordsMap, resourceMappingRecord, arg, factory, true, tool._source);
      records.push(...edgeRecords);
      return records;
    }
    __name(processDecisionTool, "processDecisionTool");
    async function createDecisionEdges(decisionTools, toolRecordsMap, parentSkillMappingRecord, arg, factory, diagnostics) {
      const records = [];
      for (const tool of decisionTools) {
        if (tool.type !== defaults_1.TOOL_TYPES.DECISION || !tool.name || !tool.targets) {
          continue;
        }
        const decisionRecord = toolRecordsMap.get(tool.name);
        if (!decisionRecord) {
          continue;
        }
        for (const targetName of tool.targets) {
          const isEndTarget = targetName === "_end";
          let targetRecord;
          if (isEndTarget) {
            targetRecord = parentSkillMappingRecord;
            if (!targetRecord) {
              diagnostics.warn(arg, `Decision default target '_end' requires parent skill mapping for tool '${tool.name}'`);
              continue;
            }
          } else {
            targetRecord = toolRecordsMap.get(targetName);
            if (!targetRecord) {
              diagnostics.warn(arg, `Decision target '${targetName}' not found for tool '${tool.name}'`);
              continue;
            }
          }
          const branch = tool.branches?.find((b) => b.target === targetName);
          const isDefault = tool.default === targetName;
          const branchIndex = tool.branches?.findIndex((b) => b.target === targetName) ?? -1;
          const edgeName = isDefault ? "Default branch" : branch?.name || `Branch ${branchIndex + 1}`;
          const edgeOrder = isDefault ? 1e3 : branchIndex >= 0 ? branchIndex + 1 : 0;
          const hasCondition = branch?.condition && !isDefault;
          const isScriptCondition = hasCondition && branch?.conditionType === "script";
          const parentSkillName = parentSkillMappingRecord?.get("resource_name")?.getValue();
          const internalNameTarget = isEndTarget ? parentSkillName || "prompt" : targetName;
          const edgeRecord = await factory.createRecord({
            source: tool._source,
            table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_EDGE,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_EDGE, $),
              ...isScriptCondition ? { applicability_script: $.val(branch.condition).toCdata() } : {},
              applicability_type: $.val(hasCondition ? isScriptCondition ? "script" : "condition_expression" : ""),
              ...isScriptCondition ? {} : { applicability_condition_expression: $.val(hasCondition ? branch.condition : "") },
              error_handler_script: $.val(defaults_1.RESOURCE_EDGE_ERROR_HANDLER_SCRIPT).toCdata(),
              from_resource: $.val(decisionRecord.getId()),
              internal_name: $.val(`${tool.name}__${internalNameTarget}`),
              name: $.val(edgeName),
              order: $.val(edgeOrder),
              to_resource: $.val(targetRecord.getId())
            }))
          });
          records.push(edgeRecord);
        }
      }
      return records;
    }
    __name(createDecisionEdges, "createDecisionEdges");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/arrow-function-helpers.js
var require_arrow_function_helpers = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/arrow-function-helpers.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.getArrowExpressionBodyText = getArrowExpressionBodyText;
    exports2.extractPropertyNameFromArrowBody = extractPropertyNameFromArrowBody;
    exports2.extractBranchesFromArrowBody = extractBranchesFromArrowBody;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    function getArrowBodyNode(shape) {
      if (shape.getStatements().length > 0) {
        return void 0;
      }
      try {
        const source = shape.getSource?.() ?? shape.getOriginalSource?.();
        if (source && sdk_build_core_1.ts.Node.isArrowFunction(source)) {
          const body = source.getBody();
          if (body && !sdk_build_core_1.ts.Node.isBlock(body)) {
            return body;
          }
        }
      } catch {
      }
      return void 0;
    }
    __name(getArrowBodyNode, "getArrowBodyNode");
    function getArrowExpressionBodyText(shape) {
      const body = getArrowBodyNode(shape);
      if (body && typeof body.getText === "function") {
        return body.getText();
      }
      return void 0;
    }
    __name(getArrowExpressionBodyText, "getArrowExpressionBodyText");
    function extractPropertyNameFromArrowBody(shape) {
      const bodyText = getArrowExpressionBodyText(shape);
      if (!bodyText) {
        return void 0;
      }
      const match = bodyText.match(/\.(\w+)\s*$/);
      return match?.[1];
    }
    __name(extractPropertyNameFromArrowBody, "extractPropertyNameFromArrowBody");
    function extractBranchesFromArrowBody(shape, toolNameToVarName) {
      const body = getArrowBodyNode(shape);
      if (!body || !sdk_build_core_1.ts.Node.isArrayLiteralExpression(body)) {
        return void 0;
      }
      const branches = [];
      for (const element of body.getElements()) {
        if (!sdk_build_core_1.ts.Node.isObjectLiteralExpression(element)) {
          continue;
        }
        let branchName = "";
        let target;
        let conditionStr = "";
        let conditionType = "filter";
        for (const prop of element.getProperties()) {
          if (!sdk_build_core_1.ts.Node.isPropertyAssignment(prop)) {
            continue;
          }
          const propName = prop.getName();
          const initializer = prop.getInitializer();
          if (!initializer) {
            continue;
          }
          switch (propName) {
            case "name":
              if (sdk_build_core_1.ts.Node.isStringLiteral(initializer)) {
                branchName = initializer.getLiteralValue();
              }
              break;
            case "to":
            case "target":
              if (!target) {
                if (sdk_build_core_1.ts.Node.isPropertyAccessExpression(initializer)) {
                  target = initializer.getName();
                } else if (sdk_build_core_1.ts.Node.isStringLiteral(initializer)) {
                  target = initializer.getLiteralValue();
                }
              }
              break;
            case "condition":
              if (sdk_build_core_1.ts.Node.isStringLiteral(initializer)) {
                conditionStr = initializer.getLiteralValue();
              } else if (sdk_build_core_1.ts.Node.isObjectLiteralExpression(initializer)) {
                const result = extractConditionFromObjectLiteral(initializer, toolNameToVarName);
                conditionStr = result.condition;
                conditionType = result.conditionType;
              }
              break;
          }
        }
        if (target) {
          branches.push({ name: branchName, target, condition: conditionStr, conditionType });
        }
      }
      return branches.length > 0 ? branches : void 0;
    }
    __name(extractBranchesFromArrowBody, "extractBranchesFromArrowBody");
    function extractConditionFromObjectLiteral(obj, toolNameToVarName) {
      const props = {};
      for (const prop of obj.getProperties()) {
        if (!sdk_build_core_1.ts.Node.isPropertyAssignment(prop)) {
          continue;
        }
        const name = prop.getName();
        const init = prop.getInitializer();
        if (!init) {
          continue;
        }
        if (sdk_build_core_1.ts.Node.isStringLiteral(init)) {
          props[name] = init.getLiteralValue();
        } else if (sdk_build_core_1.ts.Node.isNumericLiteral(init)) {
          props[name] = init.getLiteralValue().toString();
        } else if (sdk_build_core_1.ts.Node.isPropertyAccessExpression(init)) {
          const text = init.getText();
          if (text.includes(".input.")) {
            const parts = text.split(".");
            const fieldName = parts[parts.length - 1] ?? "";
            props[name] = fieldName;
          } else {
            const parts = text.split(".");
            const varName = parts[0] ?? "";
            let resolvedName = varName;
            for (const [toolName, vName] of toolNameToVarName) {
              if (vName === varName) {
                resolvedName = toolName;
                break;
              }
            }
            props[name] = `${resolvedName}.${parts.slice(1).join(".")}`;
          }
        }
      }
      if (props.script) {
        return { condition: props.script, conditionType: "script" };
      }
      if (props.expression) {
        return { condition: props.expression, conditionType: "script" };
      }
      if (props.field && props.operator) {
        const platformOperator = props.operator === "is" ? "=" : props.operator === "is_not" ? "!=" : props.operator;
        const fieldValue = props.field.startsWith("{{") && props.field.endsWith("}}") ? props.field.slice(2, -2) : props.field;
        return { condition: `{{${fieldValue}}}${platformOperator}${props.value ?? ""}`, conditionType: "filter" };
      }
      return { condition: "", conditionType: "filter" };
    }
    __name(extractConditionFromObjectLiteral, "extractConditionFromObjectLiteral");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tool-parser.js
var require_tool_parser = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tool-parser.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processTools = processTools;
    var defaults_1 = require_defaults();
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var arrow_function_helpers_1 = require_arrow_function_helpers();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var tool_utils_1 = require_tool_utils();
    var inline_script_tool_1 = require_inline_script_tool();
    var script_tool_1 = require_script_tool();
    var subflow_tool_1 = require_subflow_tool();
    var web_search_tool_1 = require_web_search_tool();
    var flow_action_tool_1 = require_flow_action_tool();
    var skill_tool_1 = require_skill_tool();
    var decision_tool_1 = require_decision_tool();
    function extractToolDefinitions(arrowFunc, diagnostics) {
      const toolParseData = [];
      const toolNameToVarName = /* @__PURE__ */ new Map();
      const statements = arrowFunc.getStatements();
      for (const statement of statements) {
        let callExpression;
        let variableName;
        if (statement instanceof sdk_build_core_1.VariableStatementShape) {
          const initializer = statement.getInitializer();
          if (initializer instanceof sdk_build_core_1.CallExpressionShape) {
            callExpression = initializer;
            variableName = statement.getVariableName()?.getName();
          }
        } else if (statement instanceof sdk_build_core_1.CallExpressionShape) {
          callExpression = statement;
        }
        if (!callExpression) {
          continue;
        }
        const callee = callExpression.getCallee();
        const parts = callee.split(".");
        if (parts.length !== 2) {
          continue;
        }
        const [builderParam, methodName] = parts;
        if (!builderParam || !methodName) {
          continue;
        }
        const nameArg = callExpression.getArgument(0);
        if (!nameArg) {
          diagnostics.warn(callExpression, `Tool ${methodName} call missing name argument`);
          continue;
        }
        const toolName = nameArg.ifString()?.getValue();
        if (!toolName) {
          diagnostics.warn(callExpression, `Tool ${methodName} name must be a string literal`);
          continue;
        }
        const configArg = callExpression.getArgument(1);
        if (!configArg || !configArg.isObject()) {
          diagnostics.warn(callExpression, `Tool ${methodName} call missing config argument`);
          continue;
        }
        const config = configArg.asObject();
        if (variableName) {
          toolNameToVarName.set(variableName, toolName);
        }
        toolParseData.push({ methodName, toolName, variableName, config });
      }
      const tools = [];
      for (const { methodName, toolName, config } of toolParseData) {
        const tool = parseToolConfig(methodName, toolName, config, toolNameToVarName, diagnostics);
        if (tool) {
          tool._source = config;
          tools.push(tool);
        }
      }
      return tools;
    }
    __name(extractToolDefinitions, "extractToolDefinitions");
    function parseToolConfig(methodName, toolName, config, toolNameToVarName, diagnostics) {
      const $idShape = config.get("$id", false);
      let toolExplicitId;
      if ($idShape instanceof sdk_build_plugins_1.NowIdShape) {
        toolExplicitId = String($idShape.getValue());
      } else if ($idShape?.ifString()) {
        toolExplicitId = $idShape.ifString().getValue();
      } else {
        diagnostics.error(config, "Tool config is missing required $id property");
        return null;
      }
      const $capabilityIdShape = config.get("$capabilityId", false);
      let toolCapabilityId;
      if ($capabilityIdShape instanceof sdk_build_plugins_1.NowIdShape) {
        toolCapabilityId = String($capabilityIdShape.getValue());
      } else if ($capabilityIdShape?.ifString()) {
        toolCapabilityId = $capabilityIdShape.ifString()?.getValue();
      }
      const truncate = config.get("truncate")?.ifBoolean()?.getValue() ?? false;
      const depends = extractDependencies(config.get("depends", false), toolNameToVarName);
      const conditionShape = config.get("condition")?.ifObject();
      let conditionScript;
      if (conditionShape) {
        const scriptShape = conditionShape.get("script");
        if (scriptShape?.is(sdk_build_plugins_1.NowIncludeShape)) {
          conditionScript = scriptShape.getValue();
        } else if (scriptShape?.ifString()?.getValue()) {
          conditionScript = scriptShape.ifString().getValue();
        } else if (scriptShape instanceof sdk_build_plugins_1.ArrowFunctionShape) {
          const returnVal = scriptShape.getReturnValue();
          const stmts = scriptShape.getStatements();
          if (returnVal) {
            conditionScript = `return ${returnVal.getCode()};`;
          } else if (stmts.length > 0) {
            conditionScript = stmts.map((s) => s.getCode()).join("\n");
          }
        }
      }
      const condition = conditionShape ? {
        ...conditionShape.get("type")?.ifString()?.getValue() ? { type: conditionShape.get("type").ifString().getValue() } : {},
        ...conditionScript ? { script: conditionScript } : {}
      } : void 0;
      switch (methodName) {
        case "InlineScript": {
          const scriptShape = config.get("script");
          let script;
          if (scriptShape?.is(sdk_build_plugins_1.NowIncludeShape)) {
            script = scriptShape.getValue();
          } else if (scriptShape?.isString()) {
            script = scriptShape.asString().getValue();
          } else if (scriptShape instanceof sdk_build_plugins_1.ArrowFunctionShape) {
            const returnVal = scriptShape.getReturnValue?.();
            const stmts = scriptShape.getStatements();
            if (returnVal) {
              script = `return ${returnVal.getCode()};`;
            } else if (stmts.length > 0) {
              script = stmts.map((s) => s.getCode()).join("\n");
            } else {
              script = "";
            }
          } else if (scriptShape) {
            script = scriptShape.getCode?.() || "";
          }
          return {
            type: defaults_1.TOOL_TYPES.INLINE_SCRIPT,
            name: toolName,
            $id: toolExplicitId,
            script,
            truncate,
            depends,
            condition
          };
        }
        case "Script": {
          const scriptIdShape = config.get("scriptId");
          let scriptId;
          if (scriptIdShape?.ifRecord()) {
            scriptId = scriptIdShape.ifRecord()?.getId().getValue();
          } else if (scriptIdShape?.isObject()) {
            const $idShape2 = scriptIdShape.asObject().get("$id");
            if ($idShape2 instanceof sdk_build_plugins_1.NowIdShape) {
              scriptId = String($idShape2.getValue());
            } else if ($idShape2?.ifRecord()) {
              scriptId = $idShape2.ifRecord()?.getId().getValue();
            } else if ($idShape2) {
              scriptId = String($idShape2.getValue());
            }
          } else if (scriptIdShape?.isString()) {
            scriptId = scriptIdShape.asString().getValue();
          } else if (scriptIdShape) {
            scriptId = String(scriptIdShape.getValue());
          }
          const scriptFunctionName = config.get("scriptFunctionName")?.ifString()?.getValue();
          const inputs = extractInputs(config.get("inputs"), toolNameToVarName);
          let output;
          const outputShape = config.get("output", false);
          if (outputShape?.isObject()) {
            const outputObj = outputShape.asObject();
            const $outputId = String(outputObj.get("$id").getValue());
            const outputName = outputObj.get("name", false)?.ifString()?.getValue();
            const outputType = outputObj.get("type", false)?.ifString()?.getValue();
            output = {
              $id: $outputId,
              ...outputName ? { name: outputName } : {},
              ...outputType ? { type: outputType } : {}
            };
          }
          return {
            type: defaults_1.TOOL_TYPES.SCRIPT,
            name: toolName,
            $id: toolExplicitId,
            $capabilityId: toolCapabilityId,
            output,
            scriptId,
            scriptFunctionName,
            inputs,
            truncate,
            depends,
            condition
          };
        }
        case "FlowAction": {
          const inputs = extractFlowActionInputs(config.get("inputs"), toolNameToVarName);
          const outputs = extractFlowActionOutputs(config.get("outputs"));
          const actionIdShape = config.get("actionId", false);
          let actionId;
          if (actionIdShape instanceof sdk_build_plugins_1.NowIdShape) {
            const nowIdValue = actionIdShape.getValue();
            if (typeof nowIdValue === "string") {
              actionId = nowIdValue;
            }
          } else if (actionIdShape instanceof sdk_build_core_1.IdentifierShape) {
            const resolvedShape = actionIdShape.resolve(true);
            const resolvedRecord = resolvedShape?.ifRecord();
            if (resolvedRecord) {
              const recordId = resolvedRecord.getId();
              if (recordId instanceof sdk_build_plugins_1.NowIdShape) {
                const idValue = recordId.getValue();
                if (typeof idValue === "string") {
                  actionId = idValue;
                }
              } else {
                const idValue = recordId.getValue();
                if (typeof idValue === "string") {
                  actionId = idValue;
                }
              }
            }
          } else if (actionIdShape?.ifRecord()) {
            const recordId = actionIdShape.ifRecord()?.getId();
            if (recordId instanceof sdk_build_plugins_1.NowIdShape) {
              const idValue = recordId.getValue();
              if (typeof idValue === "string") {
                actionId = idValue;
              }
            } else if (recordId) {
              const idValue = recordId.getValue();
              if (typeof idValue === "string") {
                actionId = idValue;
              }
            }
          } else if (actionIdShape?.ifString()) {
            actionId = actionIdShape.ifString()?.getValue();
          }
          return {
            type: defaults_1.TOOL_TYPES.FLOW_ACTION,
            name: toolName,
            $id: toolExplicitId,
            $capabilityId: toolCapabilityId,
            actionId,
            inputs,
            outputs,
            depends,
            condition
          };
        }
        case "Subflow": {
          const inputs = extractInputs(config.get("inputs"), toolNameToVarName);
          const outputs = extractFlowActionOutputs(config.get("outputs"));
          const subflowIdShape = config.get("subflowId", false);
          let subflowId;
          let subflowIdIsReference = false;
          let subflowIdExplicitId;
          if (subflowIdShape instanceof sdk_build_plugins_1.NowIdShape) {
            const nowIdValue = subflowIdShape.getValue();
            if (typeof nowIdValue === "string") {
              subflowIdExplicitId = nowIdValue;
              subflowIdIsReference = true;
            }
          } else if (subflowIdShape instanceof sdk_build_core_1.IdentifierShape) {
            const resolvedShape = subflowIdShape.resolve(true);
            const resolvedRecord = resolvedShape?.ifRecord();
            if (resolvedRecord) {
              const recordId = resolvedRecord.getId();
              if (recordId instanceof sdk_build_plugins_1.NowIdShape) {
                const idValue = recordId.getValue();
                if (typeof idValue === "string") {
                  subflowIdExplicitId = idValue;
                  subflowIdIsReference = true;
                }
              } else {
                const idValue = recordId.getValue();
                if (typeof idValue === "string") {
                  subflowId = idValue;
                }
              }
            }
          } else if (subflowIdShape?.ifRecord()) {
            const recordId = subflowIdShape.ifRecord()?.getId();
            if (recordId instanceof sdk_build_plugins_1.NowIdShape) {
              const idValue = recordId.getValue();
              if (typeof idValue === "string") {
                subflowIdExplicitId = idValue;
                subflowIdIsReference = true;
              }
            } else if (recordId) {
              const idValue = recordId.getValue();
              if (typeof idValue === "string") {
                subflowId = idValue;
              }
            }
          } else if (subflowIdShape?.ifString()) {
            subflowId = subflowIdShape.ifString()?.getValue();
          }
          return {
            type: defaults_1.TOOL_TYPES.SUBFLOW,
            name: toolName,
            $id: toolExplicitId,
            $capabilityId: toolCapabilityId,
            subflowId,
            subflowIdIsReference,
            subflowIdExplicitId,
            inputs,
            outputs: outputs && outputs.length > 0 ? outputs : void 0,
            truncate,
            depends,
            condition
          };
        }
        case "Skill": {
          const truncateShape = config.get("truncate")?.ifObject();
          const truncateConfig = truncateShape ? {
            provider: truncateShape.get("provider")?.ifBoolean()?.getValue() ?? false,
            response: truncateShape.get("response")?.ifBoolean()?.getValue() ?? false,
            error: truncateShape.get("error")?.ifBoolean()?.getValue() ?? false,
            errorcode: truncateShape.get("errorcode")?.ifBoolean()?.getValue() ?? false,
            status: truncateShape.get("status")?.ifBoolean()?.getValue() ?? false
          } : void 0;
          const skillIdShape = config.get("skillId", false);
          let skillId;
          let skillIdIsReference = false;
          let skillIdExplicitId;
          if (skillIdShape instanceof sdk_build_plugins_1.NowIdShape) {
            const baseId = skillIdShape.getValue();
            skillIdExplicitId = `${baseId}__capability`;
            skillIdIsReference = true;
          } else if (skillIdShape instanceof sdk_build_core_1.IdentifierShape) {
            const resolvedShape = skillIdShape.resolve(true);
            const resolvedRecord = resolvedShape?.ifRecord();
            if (resolvedRecord) {
              const capabilitySysId = resolvedRecord.getId().getValue();
              if (capabilitySysId) {
                skillId = capabilitySysId;
              }
            } else if (resolvedShape?.isArray()) {
              const firstElement = resolvedShape.asArray().getElements()[0];
              if (firstElement?.isObject()) {
                const defObj = firstElement.asObject();
                const $idShape2 = defObj.get("$id");
                if ($idShape2 instanceof sdk_build_plugins_1.NowIdShape) {
                  const baseId = $idShape2.getValue();
                  skillIdExplicitId = `${baseId}__capability`;
                  skillIdIsReference = true;
                }
              }
            }
          } else if (skillIdShape?.ifRecord()) {
            const recordId = skillIdShape.ifRecord()?.getId().getValue();
            if (recordId) {
              skillIdExplicitId = `${recordId}__capability`;
              skillIdIsReference = true;
            }
          } else if (skillIdShape?.isArray()) {
            const firstElement = skillIdShape.asArray().getElements()[0];
            if (firstElement?.isObject()) {
              const defObj = firstElement.asObject();
              const $idShape2 = defObj.get("$id");
              if ($idShape2 instanceof sdk_build_plugins_1.NowIdShape) {
                const baseId = $idShape2.getValue();
                skillIdExplicitId = `${baseId}__capability`;
                skillIdIsReference = true;
              }
            }
          } else if (skillIdShape?.isString()) {
            const rawSkillId = skillIdShape.asString().getValue();
            skillId = rawSkillId;
          }
          const inputs = extractSkillInputs(config.get("inputs"), toolNameToVarName);
          const skillOutputs = extractSkillOutputs(config.get("outputs"));
          return {
            type: defaults_1.TOOL_TYPES.SKILL,
            name: toolName,
            $id: toolExplicitId,
            skillId,
            skillIdIsReference,
            skillIdExplicitId,
            inputs,
            skillOutputs,
            depends,
            condition
          };
        }
        case "WebSearch": {
          const searchType = config.get("searchType")?.ifString()?.getValue();
          const queryShape = config.get("query", false);
          const queryScript = extractScriptTypeToolInput(queryShape);
          let query;
          let queryScriptValue;
          let queryValueType;
          if (queryScript) {
            query = "";
            queryScriptValue = queryScript;
            queryValueType = "script";
          } else {
            query = (0, tool_utils_1.resolveTemplateValue)(queryShape, toolNameToVarName);
          }
          const sitesShape = config.get("sites", false);
          const sitesScript = extractScriptTypeToolInput(sitesShape);
          let sites;
          let sitesScriptValue;
          let sitesValueType;
          if (sitesScript) {
            sites = "";
            sitesScriptValue = sitesScript;
            sitesValueType = "script";
          } else {
            sites = (0, tool_utils_1.resolveTemplateValue)(sitesShape, toolNameToVarName) || void 0;
          }
          const aiSearchProviders = config.get("aiSearchProviders")?.ifString()?.getValue();
          const numResults = config.get("numResults")?.ifNumber()?.getValue();
          const country = config.get("country")?.ifString()?.getValue();
          const maxTokens = config.get("maxTokens")?.ifNumber()?.getValue();
          const truncateShape = config.get("truncate")?.ifObject();
          let truncateConfig;
          if (truncateShape) {
            truncateConfig = {};
            const outputKeys = ["provider", "response", "error", "errorcode", "status"];
            for (const key of outputKeys) {
              const val = truncateShape.get(key)?.ifBoolean()?.getValue();
              if (val !== void 0) {
                truncateConfig[key] = val;
              }
            }
          }
          return {
            type: defaults_1.TOOL_TYPES.WEB_SEARCH,
            name: toolName,
            $id: toolExplicitId,
            searchType,
            query,
            queryScriptValue,
            queryValueType,
            sites,
            sitesScriptValue,
            sitesValueType,
            aiSearchProviders,
            numResults,
            country,
            maxTokens,
            truncate: false,
            // WebSearch uses truncateConfig instead of simple boolean
            truncateConfig,
            depends,
            condition
          };
        }
        case "Decision": {
          const targets = extractTargets(config.get("targets"));
          const branches = extractBranches(config.get("branches"), toolNameToVarName);
          const defaultTarget = extractDefaultTarget(config.get("default"));
          return {
            type: defaults_1.TOOL_TYPES.DECISION,
            name: toolName,
            $id: toolExplicitId,
            targets,
            branches,
            default: defaultTarget,
            depends
          };
        }
        default:
          diagnostics.warn(config, `Unknown tool type: ${methodName}`);
          return null;
      }
    }
    __name(parseToolConfig, "parseToolConfig");
    function extractSingleDependency(dependsShape, toolNameToVarName) {
      if (!dependsShape || dependsShape.isUndefined?.()) {
        return void 0;
      }
      const code = dependsShape.getCode?.();
      if (code && code !== "undefined") {
        const toolName = toolNameToVarName.get(code);
        if (toolName) {
          return toolName;
        } else {
          if (dependsShape.isResolvable?.() && !dependsShape.isIdentifier?.()) {
            const resolved = dependsShape.resolve?.(true);
            if (resolved instanceof sdk_build_core_1.CallExpressionShape) {
              const nameArg = resolved.getArgument?.(0);
              const name = nameArg?.ifString?.()?.getValue();
              if (name) {
                return name;
              }
            }
          }
          return code;
        }
      }
      return void 0;
    }
    __name(extractSingleDependency, "extractSingleDependency");
    function extractDependencies(dependsShape, toolNameToVarName) {
      if (!dependsShape || dependsShape.isUndefined?.()) {
        return void 0;
      }
      if (dependsShape.isArray?.()) {
        const deps = [];
        const elements = dependsShape.asArray().getElements(false);
        for (const element of elements) {
          const dep = extractSingleDependency(element, toolNameToVarName);
          if (dep) {
            deps.push(dep);
          }
        }
        return deps.length > 0 ? deps : void 0;
      }
      const singleDep = extractSingleDependency(dependsShape, toolNameToVarName);
      return singleDep ? [singleDep] : void 0;
    }
    __name(extractDependencies, "extractDependencies");
    function extractScriptTypeToolInput(valueShape) {
      if (!valueShape || !valueShape.isObject()) {
        return void 0;
      }
      const valueObj = valueShape.asObject();
      const typeVal = valueObj.get("type")?.ifString()?.getValue();
      if (typeVal !== "script") {
        return void 0;
      }
      const scriptShape = valueObj.get("script");
      if (scriptShape?.is(sdk_build_plugins_1.NowIncludeShape)) {
        return scriptShape.getValue();
      }
      if (scriptShape?.ifString()?.getValue()) {
        return scriptShape.ifString().getValue();
      }
      return void 0;
    }
    __name(extractScriptTypeToolInput, "extractScriptTypeToolInput");
    function extractInputs(inputsShape, toolNameToVarName) {
      if (!inputsShape || !inputsShape.isArray()) {
        return void 0;
      }
      const inputs = [];
      const elements = inputsShape.asArray().getElements();
      for (const element of elements) {
        if (!element.isObject()) {
          continue;
        }
        const obj = element.asObject();
        const name = obj.get("name")?.ifString()?.getValue();
        const type = obj.get("type")?.ifString()?.getValue();
        const idShape = obj.get("$id");
        const $id = idShape instanceof sdk_build_plugins_1.NowIdShape ? idShape.getValue() : idShape?.ifString()?.getValue();
        const valueShape = obj.get("value", false);
        const scriptInput = extractScriptTypeToolInput(valueShape);
        if (name && scriptInput) {
          inputs.push({
            ...$id ? { $id } : {},
            name,
            ...type ? { type } : {},
            value: "",
            scriptValue: scriptInput,
            valueType: "script"
          });
        } else {
          const value = (0, tool_utils_1.resolveTemplateValue)(valueShape, toolNameToVarName);
          if (name) {
            inputs.push({ ...$id ? { $id } : {}, name, ...type ? { type } : {}, value });
          }
        }
      }
      return inputs.length > 0 ? inputs : void 0;
    }
    __name(extractInputs, "extractInputs");
    function extractSkillInputs(inputsShape, toolNameToVarName) {
      if (!inputsShape || !inputsShape.isArray()) {
        return void 0;
      }
      const inputs = [];
      const elements = inputsShape.asArray().getElements();
      for (const element of elements) {
        if (!element.isObject()) {
          continue;
        }
        const obj = element.asObject();
        const definitionAttributeId = obj.get("definitionAttributeId")?.ifString()?.getValue();
        const idShape = obj.get("$id");
        const $id = idShape instanceof sdk_build_plugins_1.NowIdShape ? idShape.getValue() : idShape?.ifString()?.getValue();
        const valueShape = obj.get("value", false);
        const scriptInput = extractScriptTypeToolInput(valueShape);
        if (definitionAttributeId && scriptInput) {
          inputs.push({
            ...$id ? { $id } : {},
            name: definitionAttributeId,
            value: "",
            scriptValue: scriptInput,
            valueType: "script"
          });
        } else {
          const value = (0, tool_utils_1.resolveTemplateValue)(valueShape, toolNameToVarName);
          if (definitionAttributeId) {
            inputs.push({ ...$id ? { $id } : {}, name: definitionAttributeId, value });
          }
        }
      }
      return inputs.length > 0 ? inputs : void 0;
    }
    __name(extractSkillInputs, "extractSkillInputs");
    function extractSkillOutputs(outputsShape) {
      if (!outputsShape || !outputsShape.isObject()) {
        return void 0;
      }
      const outputs = {};
      const obj = outputsShape.asObject();
      const props = obj.properties();
      for (const [key, entryShape] of Object.entries(props)) {
        if (!entryShape || !entryShape.isObject()) {
          continue;
        }
        const entryObj = entryShape.asObject();
        const defAttrIdShape = entryObj.get("definitionAttributeId");
        const definitionAttributeId = defAttrIdShape?.ifString()?.getValue();
        if (!definitionAttributeId) {
          continue;
        }
        const truncate = entryObj.get("truncate")?.ifBoolean()?.getValue() ?? false;
        const idShape = entryObj.get("$id");
        const $id = idShape instanceof sdk_build_plugins_1.NowIdShape ? idShape.getValue() : idShape?.ifString()?.getValue();
        outputs[key] = {
          ...$id ? { $id } : {},
          definitionAttributeId,
          truncate
        };
      }
      return Object.keys(outputs).length > 0 ? outputs : void 0;
    }
    __name(extractSkillOutputs, "extractSkillOutputs");
    function extractFlowActionInputs(inputsShape, toolNameToVarName) {
      if (!inputsShape || !inputsShape.isArray()) {
        return void 0;
      }
      const inputs = [];
      const elements = inputsShape.asArray().getElements();
      for (const element of elements) {
        if (!element.isObject()) {
          continue;
        }
        const obj = element.asObject();
        const name = obj.get("name")?.ifString()?.getValue();
        const type = obj.get("type")?.ifString()?.getValue() || "string";
        const idShape = obj.get("$id");
        const $id = idShape instanceof sdk_build_plugins_1.NowIdShape ? idShape.getValue() : idShape?.ifString()?.getValue();
        const valueShape = obj.get("value", false);
        const scriptInput = extractScriptTypeToolInput(valueShape);
        if (name && scriptInput) {
          inputs.push({
            ...$id ? { $id } : {},
            name,
            type,
            value: "",
            scriptValue: scriptInput,
            valueType: "script"
          });
        } else {
          const value = (0, tool_utils_1.resolveTemplateValue)(valueShape, toolNameToVarName);
          if (name) {
            inputs.push({ ...$id ? { $id } : {}, name, type, value });
          }
        }
      }
      return inputs.length > 0 ? inputs : void 0;
    }
    __name(extractFlowActionInputs, "extractFlowActionInputs");
    function extractFlowActionOutputs(outputsShape) {
      if (!outputsShape || !outputsShape.isArray()) {
        return void 0;
      }
      const outputs = [];
      const elements = outputsShape.asArray().getElements();
      for (const element of elements) {
        if (!element.isObject()) {
          continue;
        }
        const obj = element.asObject();
        const name = obj.get("name")?.ifString()?.getValue();
        const type = obj.get("type")?.ifString()?.getValue() || "string";
        const truncate = obj.get("truncate")?.ifBoolean()?.getValue();
        const outIdShape = obj.get("$id");
        const $id = outIdShape instanceof sdk_build_plugins_1.NowIdShape ? outIdShape.getValue() : outIdShape?.ifString()?.getValue();
        if (name) {
          outputs.push({ ...$id ? { $id } : {}, name, type, ...truncate !== void 0 ? { truncate } : {} });
        }
      }
      return outputs.length > 0 ? outputs : void 0;
    }
    __name(extractFlowActionOutputs, "extractFlowActionOutputs");
    function extractTargets(targetsShape) {
      if (!targetsShape || !targetsShape.isArray()) {
        return void 0;
      }
      const targets = [];
      const elements = targetsShape.asArray().getElements();
      for (const element of elements) {
        const value = element.ifString()?.getValue();
        if (value) {
          targets.push(value);
        }
      }
      return targets.length > 0 ? targets : void 0;
    }
    __name(extractTargets, "extractTargets");
    function extractBranches(branchesShape, toolNameToVarName) {
      if (!branchesShape || !(branchesShape instanceof sdk_build_plugins_1.ArrowFunctionShape)) {
        return void 0;
      }
      const statements = branchesShape.getStatements();
      const arrayShape = statements[0] ?? branchesShape.getReturnValue?.();
      if (!arrayShape || !arrayShape.isArray()) {
        return (0, arrow_function_helpers_1.extractBranchesFromArrowBody)(branchesShape, toolNameToVarName);
      }
      const branches = [];
      const elements = arrayShape.asArray().getElements();
      for (const element of elements) {
        if (!element.isObject()) {
          continue;
        }
        const obj = element.asObject();
        const branchName = obj.get("name")?.ifString()?.getValue() ?? "";
        let target;
        const toShape = obj.get("to", false);
        const targetShape = toShape?.isDefined() ? toShape : obj.get("target", false);
        if (targetShape instanceof sdk_build_core_1.PropertyAccessShape) {
          const parts = targetShape.getElements();
          if (parts.length >= 2) {
            target = parts[parts.length - 1]?.getName();
          }
        } else if (targetShape?.isString()) {
          target = targetShape.asString().getValue();
        }
        if (!target) {
          continue;
        }
        let conditionStr = "";
        let conditionType = "filter";
        const rawConditionShape = obj.get("condition");
        const conditionAsString = rawConditionShape?.ifString()?.getValue();
        if (conditionAsString) {
          conditionStr = conditionAsString;
        }
        const conditionShape = rawConditionShape?.ifObject();
        if (!conditionStr && conditionShape) {
          const scriptVal = conditionShape.get("script")?.ifString()?.getValue();
          if (scriptVal) {
            conditionStr = scriptVal;
            conditionType = "script";
          } else {
            const exprVal = conditionShape.get("expression")?.ifString()?.getValue();
            if (exprVal) {
              conditionStr = exprVal;
            } else {
              const fieldShape = conditionShape.get("field", false);
              const field = (0, tool_utils_1.resolveTemplateValue)(fieldShape, toolNameToVarName);
              const operator = conditionShape.get("operator")?.ifString()?.getValue();
              const valueShape = conditionShape.get("value");
              const value = valueShape?.ifString()?.getValue() ?? valueShape?.ifNumber()?.getValue()?.toString() ?? "";
              if (field && operator) {
                const platformOperator = operator === "is" ? "=" : operator === "is not" ? "!=" : operator;
                conditionStr = `${field}${platformOperator}${value}`;
              }
            }
          }
        }
        branches.push({ name: branchName, target, condition: conditionStr, conditionType });
      }
      return branches.length > 0 ? branches : void 0;
    }
    __name(extractBranches, "extractBranches");
    function extractDefaultTarget(defaultShape) {
      if (!defaultShape || !(defaultShape instanceof sdk_build_plugins_1.ArrowFunctionShape)) {
        return void 0;
      }
      const statements = defaultShape.getStatements();
      const expr = statements[0] ?? defaultShape.getReturnValue?.();
      if (expr instanceof sdk_build_core_1.PropertyAccessShape) {
        const parts = expr.getElements();
        if (parts.length >= 2) {
          return parts[parts.length - 1]?.getName();
        }
      }
      if (expr?.isString()) {
        return expr.asString().getValue();
      }
      const propName = (0, arrow_function_helpers_1.extractPropertyNameFromArrowBody)(defaultShape);
      if (propName) {
        return propName;
      }
      return void 0;
    }
    __name(extractDefaultTarget, "extractDefaultTarget");
    function validateToolDefinitions(toolDefinitions, inputNames) {
      const toolNames = /* @__PURE__ */ new Set();
      for (const tool of toolDefinitions) {
        if (toolNames.has(tool.name)) {
          return {
            isValid: false,
            errorMessage: `Duplicate tool name '${tool.name}'. Each tool must have a unique name within the skill.`
          };
        }
        if (inputNames.has(tool.name)) {
          return {
            isValid: false,
            errorMessage: `Tool name '${tool.name}' conflicts with a skill input name. Tool names and input names must be unique.`
          };
        }
        toolNames.add(tool.name);
      }
      for (const tool of toolDefinitions) {
        if (!tool.depends || tool.depends.length === 0) {
          continue;
        }
        for (const depName of tool.depends) {
          if (depName === tool.name) {
            return {
              isValid: false,
              errorMessage: `Tool '${tool.name}' cannot depend on itself. Remove '${tool.name}' from its depends.`
            };
          }
          if (!toolNames.has(depName)) {
            return {
              isValid: false,
              errorMessage: `Tool '${tool.name}' depends on '${depName}' which does not exist. Dependencies must reference tools defined in this skill.`
            };
          }
        }
      }
      const cycle = detectDependencyCycle(toolDefinitions);
      if (cycle) {
        return {
          isValid: false,
          errorMessage: `Circular dependency detected: ${cycle.join(" \u2192 ")}. Tools cannot have circular dependencies.`
        };
      }
      return { isValid: true };
    }
    __name(validateToolDefinitions, "validateToolDefinitions");
    function detectDependencyCycle(toolDefinitions) {
      const visited = /* @__PURE__ */ new Set();
      const recursionStack = /* @__PURE__ */ new Set();
      const path = [];
      const toolDepsMap = /* @__PURE__ */ new Map();
      for (const tool of toolDefinitions) {
        toolDepsMap.set(tool.name, tool.depends || []);
      }
      function dfs(toolName) {
        visited.add(toolName);
        recursionStack.add(toolName);
        path.push(toolName);
        const deps = toolDepsMap.get(toolName) || [];
        for (const dep of deps) {
          if (!visited.has(dep)) {
            if (dfs(dep)) {
              return true;
            }
          } else if (recursionStack.has(dep)) {
            path.push(dep);
            return true;
          }
        }
        path.pop();
        recursionStack.delete(toolName);
        return false;
      }
      __name(dfs, "dfs");
      for (const tool of toolDefinitions) {
        if (!visited.has(tool.name)) {
          if (dfs(tool.name)) {
            const lastNode = path[path.length - 1];
            if (lastNode) {
              const cycleStart = path.indexOf(lastNode);
              return path.slice(cycleStart);
            }
            return path;
          }
        }
      }
      return null;
    }
    __name(detectDependencyCycle, "detectDependencyCycle");
    function getAncestors(toolName, toolDefinitions) {
      const ancestors = /* @__PURE__ */ new Set();
      const toolMap = /* @__PURE__ */ new Map();
      for (const tool of toolDefinitions) {
        toolMap.set(tool.name, tool);
      }
      function collectAncestors(name) {
        const tool = toolMap.get(name);
        if (!tool?.depends || tool.depends.length === 0) {
          return;
        }
        for (const dep of tool.depends) {
          if (!ancestors.has(dep)) {
            ancestors.add(dep);
            collectAncestors(dep);
          }
        }
      }
      __name(collectAncestors, "collectAncestors");
      collectAncestors(toolName);
      return ancestors;
    }
    __name(getAncestors, "getAncestors");
    function getDescendants(toolName, toolDefinitions) {
      const descendants = /* @__PURE__ */ new Set();
      for (const tool of toolDefinitions) {
        if (tool.name === toolName) {
          continue;
        }
        const ancestors = getAncestors(tool.name, toolDefinitions);
        if (ancestors.has(toolName)) {
          descendants.add(tool.name);
        }
      }
      return descendants;
    }
    __name(getDescendants, "getDescendants");
    function emitAncestorOnlyInputReferenceHints(toolDefinitions, arg, diagnostics) {
      const toolNames = new Set(toolDefinitions.map((t) => t.name));
      for (const tool of toolDefinitions) {
        if (!tool.inputs) {
          continue;
        }
        const ancestors = getAncestors(tool.name, toolDefinitions);
        for (const input of tool.inputs) {
          const toolOutputMatch = input.value.match(/^\{\{([^.}]+)\.([^}]+)\}\}$/);
          if (toolOutputMatch) {
            const referencedTool = toolOutputMatch[1];
            if (referencedTool && toolNames.has(referencedTool) && !ancestors.has(referencedTool)) {
              diagnostics.hint(arg, `Tool '${tool.name}' references output of '${referencedTool}' which is not an ancestor. Add '${referencedTool}' to the depends array of '${tool.name}' to ensure correct execution order.`);
            }
          }
        }
      }
    }
    __name(emitAncestorOnlyInputReferenceHints, "emitAncestorOnlyInputReferenceHints");
    function emitDecisionDescendantTargetHints(toolDefinitions, arg, diagnostics) {
      const toolNames = new Set(toolDefinitions.map((t) => t.name));
      for (const tool of toolDefinitions) {
        if (tool.type !== "decision" || !tool.targets) {
          continue;
        }
        const descendants = getDescendants(tool.name, toolDefinitions);
        for (const target of tool.targets) {
          if (target === "_end") {
            continue;
          }
          if (!toolNames.has(target)) {
            continue;
          }
          if (!descendants.has(target)) {
            diagnostics.hint(arg, `Decision '${tool.name}' targets '${target}' which is not a descendant in the dependency graph. Decision branches typically route to tools that come after the decision node.`);
          }
        }
      }
    }
    __name(emitDecisionDescendantTargetHints, "emitDecisionDescendantTargetHints");
    async function processTools(toolsShape, capabilityRecord, skillName, arg, callExpression, factory, diagnostics, inputNames) {
      const records = [];
      const toolRecordsMap = /* @__PURE__ */ new Map();
      const toolSourceMap = /* @__PURE__ */ new Map();
      const toolsWithDependents = /* @__PURE__ */ new Set();
      let decisionToolsForEdges = [];
      const leafToolIds = [];
      if (toolsShape && !toolsShape.isUndefined()) {
        const arrowFunc = toolsShape.if(sdk_build_plugins_1.ArrowFunctionShape);
        if (!arrowFunc) {
          diagnostics.error(arg, "tools must be an arrow function");
          return records;
        }
        const toolDefinitions = extractToolDefinitions(arrowFunc, diagnostics);
        const toolByName = /* @__PURE__ */ new Map();
        for (const tool of toolDefinitions) {
          toolByName.set(tool.name, tool);
        }
        for (const tool of toolDefinitions) {
          if (tool.type === defaults_1.TOOL_TYPES.DECISION && tool.targets) {
            for (const targetName of tool.targets) {
              if (targetName === "_end") {
                continue;
              }
              const targetTool = toolByName.get(targetName);
              if (targetTool) {
                if (!targetTool.depends) {
                  targetTool.depends = [tool.name];
                } else if (!targetTool.depends.includes(tool.name)) {
                  targetTool.depends.push(tool.name);
                }
              }
            }
          }
        }
        for (const tool of toolDefinitions) {
          if (tool.depends && tool.depends.length > 0) {
            for (const dep of tool.depends) {
              toolsWithDependents.add(dep);
            }
          }
          if (tool.type === "decision" && tool.name) {
            const hasEndTarget = tool.targets?.includes("_end");
            if (!hasEndTarget) {
              toolsWithDependents.add(tool.name);
            }
          }
        }
        const validationResult = validateToolDefinitions(toolDefinitions, inputNames);
        if (!validationResult.isValid) {
          diagnostics.error(arg, validationResult.errorMessage);
          return records;
        }
        emitAncestorOnlyInputReferenceHints(toolDefinitions, arg, diagnostics);
        emitDecisionDescendantTargetHints(toolDefinitions, arg, diagnostics);
        for (const tool of toolDefinitions) {
          if (tool.name && tool._source) {
            toolSourceMap.set(tool.name, tool._source);
          }
        }
        const decisionToolNames = /* @__PURE__ */ new Set();
        for (const tool of toolDefinitions) {
          if (tool.type === defaults_1.TOOL_TYPES.DECISION) {
            decisionToolNames.add(tool.name);
          }
        }
        const sortedToolDefinitions = (0, tool_utils_1.topologicalSortTools)(toolDefinitions);
        for (const tool of sortedToolDefinitions) {
          let toolRecords = [];
          switch (tool.type) {
            case defaults_1.TOOL_TYPES.INLINE_SCRIPT:
              toolRecords = await (0, inline_script_tool_1.processInlineScriptTool)(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames);
              break;
            case defaults_1.TOOL_TYPES.SCRIPT:
              toolRecords = await (0, script_tool_1.processScriptTool)(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames);
              break;
            case defaults_1.TOOL_TYPES.SUBFLOW:
              toolRecords = await (0, subflow_tool_1.processSubflowTool)(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames);
              break;
            case defaults_1.TOOL_TYPES.WEB_SEARCH:
              toolRecords = await (0, web_search_tool_1.processWebSearchTool)(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames);
              break;
            case defaults_1.TOOL_TYPES.FLOW_ACTION:
              toolRecords = await (0, flow_action_tool_1.processFlowActionTool)(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames);
              break;
            case defaults_1.TOOL_TYPES.SKILL:
              toolRecords = await (0, skill_tool_1.processSkillTool)(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames);
              break;
            case defaults_1.TOOL_TYPES.DECISION:
              toolRecords = await (0, decision_tool_1.processDecisionTool)(tool, capabilityRecord, toolRecordsMap, arg, factory, diagnostics, decisionToolNames);
              break;
          }
          records.push(...toolRecords);
        }
        decisionToolsForEdges = toolDefinitions.filter((t) => t.type === defaults_1.TOOL_TYPES.DECISION);
        const decisionToolsWithEndTarget = /* @__PURE__ */ new Set();
        for (const decisionTool of decisionToolsForEdges) {
          if (decisionTool.targets?.includes("_end") && decisionTool.name) {
            decisionToolsWithEndTarget.add(decisionTool.name);
          }
        }
        for (const [toolName, record] of toolRecordsMap) {
          if (!toolsWithDependents.has(toolName) || decisionToolsWithEndTarget.has(toolName)) {
            leafToolIds.push(record.getId());
          }
        }
      }
      const dependsOnList = leafToolIds.length > 0 ? leafToolIds.map((id) => id.toString().getValue()).join(",") : "";
      const parentSkillResourceMapping = await factory.createRecord({
        source: {
          path: callExpression.getOriginalFilePath(),
          content: ""
        },
        table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_MAPPING, $),
          resource_name: $.val(skillName),
          parent_capability: $.val(capabilityRecord.getId()),
          resource_capability: $.val(capabilityRecord.getId()),
          applicability_script: $.val(defaults_1.RESOURCE_MAPPING_APPLICABILITY_SCRIPT),
          depends_on_list: $.val(dependsOnList),
          // Always set metadata to empty object so the platform regenerates the
          // skill diagram when the skill is opened in the UI. Without this, a
          // stale diagramInstanceId from a previous install prevents the platform
          // from picking up newly added or removed tools.
          metadata: $.val("{}")
        }))
      });
      records.push(parentSkillResourceMapping);
      if (decisionToolsForEdges.length > 0) {
        const decisionEdges = await (0, decision_tool_1.createDecisionEdges)(decisionToolsForEdges, toolRecordsMap, parentSkillResourceMapping, arg, factory, diagnostics);
        records.push(...decisionEdges);
      }
      if (toolRecordsMap.size > 0) {
        for (const [toolName, toolRecord] of toolRecordsMap) {
          if (toolsWithDependents.has(toolName)) {
            continue;
          }
          const defaultEdge = await factory.createRecord({
            source: toolSourceMap.get(toolName) ?? arg,
            table: tool_utils_1.SYS_ONE_EXTEND_RESOURCE_EDGE,
            properties: arg.transform(({ $ }) => ({
              ...(0, skill_plugin_helpers_1.applyDefaults)(tool_utils_1.SYS_ONE_EXTEND_RESOURCE_EDGE, $),
              from_resource: $.val(toolRecord.getId()),
              internal_name: $.val(`${toolName}__${skillName}`),
              to_resource: $.val(parentSkillResourceMapping.getId())
            }))
          });
          records.push(defaultEdge);
        }
      }
      return records;
    }
    __name(processTools, "processTools");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tool-transform.js
var require_tool_transform = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tool-transform.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.generateTools = generateTools;
    var defaults_1 = require_defaults();
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var tool_utils_1 = require_tool_utils();
    function generateTools(resourceMappings, attributeMappings, edges, capabilities, capabilityDefinitions, parentCapabilityId, definitionAttributes) {
      const unsupportedTools = [];
      const unsupportedMetrics = [];
      const defAttrInfoMap = /* @__PURE__ */ new Map();
      if (definitionAttributes) {
        for (const attr of definitionAttributes) {
          const attrId = attr.getId().getValue();
          const attrName = attr.get("name")?.getValue();
          const dataTypeValue = attr.get("data_type")?.getValue();
          const dataType = typeof dataTypeValue === "string" ? dataTypeValue : "string";
          if (attrId && attrName) {
            defAttrInfoMap.set(attrId, { name: attrName, dataType });
          }
        }
      }
      const defAttrNameMap = /* @__PURE__ */ new Map();
      for (const [id, info] of defAttrInfoMap) {
        defAttrNameMap.set(id, info.name);
      }
      const capabilityMap = /* @__PURE__ */ new Map();
      for (const cap of capabilities) {
        capabilityMap.set(cap.getId().getValue(), cap);
      }
      const capDefMap = /* @__PURE__ */ new Map();
      for (const def of capabilityDefinitions) {
        const capId = def.get("capability")?.getValue();
        if (typeof capId === "string") {
          capDefMap.set(capId, def);
        }
      }
      const mappingIdsWithEdges = /* @__PURE__ */ new Set();
      for (const edge of edges) {
        const fromId = edge.get("from_resource")?.getValue();
        const toId = edge.get("to_resource")?.getValue();
        if (typeof fromId === "string") {
          mappingIdsWithEdges.add(fromId);
        }
        if (typeof toId === "string") {
          mappingIdsWithEdges.add(toId);
        }
      }
      const supportedToolMappings = [];
      const parentMappingIds = /* @__PURE__ */ new Set();
      for (const mapping of resourceMappings) {
        const resourceCapId = mapping.get("resource_capability")?.getValue();
        const mappingId = mapping.getId().getValue();
        if (resourceCapId === parentCapabilityId) {
          parentMappingIds.add(mappingId);
          continue;
        }
        const mappingParentCapId = mapping.get("parent_capability")?.toString().getValue();
        if (mappingParentCapId && mappingParentCapId !== parentCapabilityId) {
          continue;
        }
        if (defaults_1.UNSUPPORTED_TOOL_CAPABILITY_IDS.has(resourceCapId)) {
          unsupportedTools.push(mapping);
          continue;
        }
        if (resourceCapId === defaults_1.TOOL_RESOURCE_CAPABILITIES.INLINE_SCRIPT) {
          supportedToolMappings.push({ mapping, type: defaults_1.TOOL_TYPES.INLINE_SCRIPT });
          continue;
        }
        if (resourceCapId === defaults_1.TOOL_RESOURCE_CAPABILITIES.DECISION) {
          supportedToolMappings.push({ mapping, type: defaults_1.TOOL_TYPES.DECISION });
          continue;
        }
        if (resourceCapId === defaults_1.TOOL_RESOURCE_CAPABILITIES.WEB_SEARCH) {
          supportedToolMappings.push({ mapping, type: defaults_1.TOOL_TYPES.WEB_SEARCH });
          continue;
        }
        const cap = capabilityMap.get(resourceCapId);
        const capDef = capDefMap.get(resourceCapId);
        if (cap || capDef) {
          const capType = cap?.get("type")?.getValue()?.toUpperCase();
          if (capType === "SKILL") {
            supportedToolMappings.push({ mapping, type: defaults_1.TOOL_TYPES.SKILL });
            continue;
          }
          const apiType = capDef?.get("api_type")?.getValue();
          const api = capDef?.get("api")?.getValue();
          if (api === defaults_1.SCRIPT_TOOL_API_ID) {
            supportedToolMappings.push({ mapping, type: defaults_1.TOOL_TYPES.SCRIPT });
            continue;
          }
          if (apiType === defaults_1.SYS_ONE_EXTEND_CAPABILITY_DEFINITION_API_TYPES.SYS_HUB_FLOW) {
            supportedToolMappings.push({ mapping, type: defaults_1.TOOL_TYPES.SUBFLOW });
            continue;
          }
          if (apiType === defaults_1.SYS_ONE_EXTEND_CAPABILITY_DEFINITION_API_TYPES.SYS_HUB_ACTION_TYPE_DEFINITION) {
            supportedToolMappings.push({ mapping, type: defaults_1.TOOL_TYPES.FLOW_ACTION });
            continue;
          }
        }
        if (resourceCapId === defaults_1.TOOL_RESOURCE_CAPABILITIES.SKILL) {
          supportedToolMappings.push({ mapping, type: defaults_1.TOOL_TYPES.SKILL });
          continue;
        }
        const metadataStr = mapping.get("metadata")?.getValue();
        if (metadataStr) {
          try {
            const metadata = JSON.parse(metadataStr);
            const toolType = metadata.toolType;
            const knownToolTypes = [
              defaults_1.TOOL_TYPES.SCRIPT,
              defaults_1.TOOL_TYPES.SUBFLOW,
              defaults_1.TOOL_TYPES.FLOW_ACTION,
              defaults_1.TOOL_TYPES.SKILL,
              defaults_1.TOOL_TYPES.DECISION
            ];
            if (toolType && knownToolTypes.includes(toolType)) {
              supportedToolMappings.push({ mapping, type: toolType });
              continue;
            }
          } catch {
          }
        }
        if (mappingIdsWithEdges.has(mappingId)) {
          unsupportedTools.push(mapping);
        } else {
          unsupportedMetrics.push(mapping);
        }
      }
      if (unsupportedTools.length > 0) {
        return { tools: [], unsupportedTools, unsupportedMetrics, supportedToolsToDelete: [] };
      }
      const tools = [];
      const mappingIdToName = /* @__PURE__ */ new Map();
      for (const mapping of resourceMappings) {
        const id = mapping.getId().getValue();
        const name = mapping.get("resource_name")?.getValue();
        if (id && name) {
          mappingIdToName.set(id, name);
        }
      }
      const decisionMappingIds = /* @__PURE__ */ new Set();
      for (const { mapping, type } of supportedToolMappings) {
        if (type === defaults_1.TOOL_TYPES.DECISION) {
          decisionMappingIds.add(mapping.getId().getValue());
        }
      }
      for (const { mapping, type } of supportedToolMappings) {
        switch (type) {
          case defaults_1.TOOL_TYPES.INLINE_SCRIPT: {
            const tool = buildInlineScriptFromRecord(mapping, attributeMappings, edges, mappingIdToName, decisionMappingIds);
            if (tool) {
              tools.push(tool);
            }
            break;
          }
          case defaults_1.TOOL_TYPES.WEB_SEARCH: {
            const tool = buildWebSearchFromRecord(mapping, attributeMappings, edges, mappingIdToName, decisionMappingIds);
            if (tool) {
              tools.push(tool);
            }
            break;
          }
          case defaults_1.TOOL_TYPES.SCRIPT: {
            const tool = buildScriptFromRecord(mapping, attributeMappings, edges, mappingIdToName, capDefMap, decisionMappingIds, defAttrNameMap, capabilityMap);
            if (tool) {
              tools.push(tool);
            }
            break;
          }
          case defaults_1.TOOL_TYPES.SUBFLOW: {
            const tool = buildSubflowFromRecord(mapping, attributeMappings, edges, mappingIdToName, capDefMap, decisionMappingIds, defAttrInfoMap, capabilityMap);
            if (tool) {
              tools.push(tool);
            }
            break;
          }
          case defaults_1.TOOL_TYPES.FLOW_ACTION: {
            const tool = buildFlowActionFromRecord(mapping, attributeMappings, edges, mappingIdToName, decisionMappingIds, capDefMap, defAttrInfoMap, capabilityMap);
            if (tool) {
              tools.push(tool);
            }
            break;
          }
          case defaults_1.TOOL_TYPES.SKILL: {
            const tool = buildSkillFromRecord(mapping, attributeMappings, edges, mappingIdToName, decisionMappingIds, defAttrNameMap);
            if (tool) {
              tools.push(tool);
            }
            break;
          }
          case defaults_1.TOOL_TYPES.DECISION: {
            const tool = buildDecisionFromRecord(mapping, attributeMappings, edges, mappingIdToName, parentMappingIds, decisionMappingIds);
            if (tool) {
              tools.push(tool);
            }
            break;
          }
        }
      }
      const toolByName = /* @__PURE__ */ new Map();
      for (const tool of tools) {
        toolByName.set(tool.name, tool);
      }
      for (const tool of tools) {
        if (tool.type === defaults_1.TOOL_TYPES.DECISION && tool.targets) {
          for (const targetName of tool.targets) {
            if (targetName === "_end") {
              continue;
            }
            const targetTool = toolByName.get(targetName);
            if (targetTool) {
              if (!targetTool.depends) {
                targetTool.depends = [tool.name];
              } else if (!targetTool.depends.includes(tool.name)) {
                targetTool.depends.push(tool.name);
              }
            }
          }
        }
      }
      const supportedToolsToDelete = [];
      if (unsupportedTools.length === 0) {
        for (const mapping of resourceMappings) {
          const resourceCapId = mapping.get("resource_capability")?.getValue();
          if (resourceCapId === parentCapabilityId) {
            const source = mapping.asRecord().getSource();
            if (source instanceof sdk_build_core_1.CallExpressionShape && source.getCallee() === "Record") {
              supportedToolsToDelete.push(new sdk_build_core_1.Record({
                id: mapping.asRecord().getId(),
                properties: {},
                action: "DELETE",
                source: new sdk_build_core_1.DeletedShape({ source })
              }));
            }
          }
        }
        for (const { mapping } of supportedToolMappings) {
          const source = mapping.asRecord().getSource();
          if (source instanceof sdk_build_core_1.CallExpressionShape && source.getCallee() === "Record") {
            supportedToolsToDelete.push(new sdk_build_core_1.Record({
              id: mapping.asRecord().getId(),
              properties: {},
              action: "DELETE",
              source: new sdk_build_core_1.DeletedShape({ source })
            }));
          }
        }
        for (const attr of attributeMappings) {
          const attrSource = attr.asRecord().getSource();
          if (attrSource instanceof sdk_build_core_1.CallExpressionShape && attrSource.getCallee() === "Record") {
            supportedToolsToDelete.push(new sdk_build_core_1.Record({
              id: attr.asRecord().getId(),
              properties: {},
              action: "DELETE",
              source: new sdk_build_core_1.DeletedShape({ source: attrSource })
            }));
          }
        }
        for (const edge of edges) {
          const edgeSource = edge.asRecord().getSource();
          if (edgeSource instanceof sdk_build_core_1.CallExpressionShape && edgeSource.getCallee() === "Record") {
            supportedToolsToDelete.push(new sdk_build_core_1.Record({
              id: edge.asRecord().getId(),
              properties: {},
              action: "DELETE",
              source: new sdk_build_core_1.DeletedShape({ source: edgeSource })
            }));
          }
        }
        if (definitionAttributes) {
          for (const defAttr of definitionAttributes) {
            const defAttrCapId = defAttr.get("capability")?.getValue();
            if (defAttrCapId !== parentCapabilityId) {
              const defAttrSource = defAttr.asRecord().getSource();
              if (defAttrSource instanceof sdk_build_core_1.CallExpressionShape && defAttrSource.getCallee() === "Record") {
                supportedToolsToDelete.push(new sdk_build_core_1.Record({
                  id: defAttr.asRecord().getId(),
                  properties: {},
                  action: "DELETE",
                  source: new sdk_build_core_1.DeletedShape({ source: defAttrSource })
                }));
              }
            }
          }
        }
      }
      return { tools, unsupportedTools, unsupportedMetrics, supportedToolsToDelete };
    }
    __name(generateTools, "generateTools");
    function buildInlineScriptFromRecord(mapping, attributeMappings, edges, mappingIdToName, decisionMappingIds) {
      const mappingId = mapping.getId().getValue();
      const name = mapping.get("resource_name")?.getValue();
      if (!name) {
        return null;
      }
      const { inputs, outputs } = (0, tool_utils_1.getAttributeMappingsForTool)(mappingId, attributeMappings);
      let script;
      for (const input of inputs) {
        const attrName = input.get("resource_attribute_name")?.getValue();
        if (attrName === defaults_1.TOOL_ATTRIBUTE_IDS.INLINE_SCRIPT_INPUT) {
          const template = input.get("attribute_value_template")?.getValue();
          const scriptVal = input.get("attribute_value_script")?.getValue();
          script = template || scriptVal || "";
        }
      }
      let truncate = false;
      for (const output of outputs) {
        const truncateVal = output.get("truncate")?.getValue();
        if (truncateVal === true || truncateVal === "true") {
          truncate = true;
        }
      }
      const depends = (0, tool_utils_1.extractDependsFromEdges)(mappingId, edges, mappingIdToName, decisionMappingIds);
      const condition = (0, tool_utils_1.extractConditionFromMapping)(mapping);
      return {
        type: defaults_1.TOOL_TYPES.INLINE_SCRIPT,
        name,
        $id: mappingId,
        script,
        truncate,
        depends,
        condition
      };
    }
    __name(buildInlineScriptFromRecord, "buildInlineScriptFromRecord");
    function buildWebSearchFromRecord(mapping, attributeMappings, edges, mappingIdToName, decisionMappingIds) {
      const mappingId = mapping.getId().getValue();
      const name = mapping.get("resource_name")?.getValue();
      if (!name) {
        return null;
      }
      const { inputs, outputs } = (0, tool_utils_1.getAttributeMappingsForTool)(mappingId, attributeMappings);
      const inputByAttrId = /* @__PURE__ */ new Map();
      for (const input of inputs) {
        const attrId = input.get("resource_attribute_name")?.getValue();
        if (attrId) {
          inputByAttrId.set(attrId, input);
        }
      }
      const searchTypeAttrId = defaults_1.WEBSEARCH_AI_ANSWERS_CONFIG.INPUT_MAPPINGS.find((m) => m.fluentName === "searchType")?.attributeId;
      let searchType;
      if (searchTypeAttrId) {
        const searchTypeRecord = inputByAttrId.get(searchTypeAttrId);
        const val = searchTypeRecord?.get("attribute_value_template")?.getValue();
        if (val === "ai_answers" || val === "searching_and_scraping") {
          searchType = val;
        }
      }
      const inputMappingConfig = searchType === "searching_and_scraping" ? defaults_1.WEBSEARCH_SCRAPING_CONFIG.INPUT_MAPPINGS : defaults_1.WEBSEARCH_AI_ANSWERS_CONFIG.INPUT_MAPPINGS;
      const toolValues = {};
      const scriptedFieldValues = {};
      for (const attrConfig of inputMappingConfig) {
        const record = inputByAttrId.get(attrConfig.attributeId);
        if (record) {
          const valueType = record.get("attribute_value_type")?.getValue() || "template";
          if (valueType === "script" && (attrConfig.fluentName === "query" || attrConfig.fluentName === "sites")) {
            const scriptVal = record.get("attribute_value_script")?.getValue();
            if (scriptVal) {
              scriptedFieldValues[attrConfig.fluentName] = { scriptValue: scriptVal, valueType: "script" };
            }
          } else {
            const val = record.get("attribute_value_template")?.getValue();
            if (val !== void 0 && val !== "") {
              toolValues[attrConfig.fluentName] = val;
            }
          }
        }
      }
      let query;
      let queryScriptValue;
      let queryValueType;
      if (scriptedFieldValues["query"]) {
        query = "";
        queryScriptValue = scriptedFieldValues["query"].scriptValue;
        queryValueType = "script";
      } else {
        query = toolValues["query"] || "";
      }
      let sites;
      let sitesScriptValue;
      let sitesValueType;
      if (scriptedFieldValues["sites"]) {
        sites = "";
        sitesScriptValue = scriptedFieldValues["sites"].scriptValue;
        sitesValueType = "script";
      } else {
        sites = toolValues["sites"] || void 0;
      }
      let aiSearchProviders;
      const aiProvidersJson = toolValues["aiSearchProviders"];
      if (aiProvidersJson) {
        try {
          const parsed = JSON.parse(aiProvidersJson);
          const providerValue = parsed.value;
          if (providerValue && providerValue in defaults_1.AI_SEARCH_PROVIDERS) {
            aiSearchProviders = providerValue;
          } else {
            aiSearchProviders = providerValue || aiProvidersJson;
          }
        } catch {
          aiSearchProviders = aiProvidersJson;
        }
      }
      const numResultsStr = toolValues["numResults"];
      const numResults = numResultsStr ? parseInt(numResultsStr, 10) : void 0;
      const country = toolValues["country"] || void 0;
      const maxTokensStr = toolValues["maxTokens"];
      const maxTokens = maxTokensStr ? parseInt(maxTokensStr, 10) : void 0;
      const outputAttrIdToName = {};
      for (const outMapping of defaults_1.WEBSEARCH_AI_ANSWERS_CONFIG.OUTPUT_MAPPINGS) {
        outputAttrIdToName[outMapping.attributeId] = outMapping.name;
      }
      const outputNameToTruncateKey = {
        provider: "provider",
        response: "response",
        error: "error",
        errorcode: "errorcode",
        status: "status"
      };
      let truncateConfig;
      for (const output of outputs) {
        const attrId = output.get("resource_attribute_name")?.getValue();
        const outputName = outputAttrIdToName[attrId];
        if (outputName) {
          const truncateVal = output.get("truncate")?.getValue();
          const isTruncated = truncateVal === true || truncateVal === "true";
          if (isTruncated) {
            if (!truncateConfig) {
              truncateConfig = {};
            }
            const configKey = outputNameToTruncateKey[outputName] || outputName;
            truncateConfig[configKey] = true;
          }
        }
      }
      const depends = (0, tool_utils_1.extractDependsFromEdges)(mappingId, edges, mappingIdToName, decisionMappingIds);
      const condition = (0, tool_utils_1.extractConditionFromMapping)(mapping);
      return {
        type: defaults_1.TOOL_TYPES.WEB_SEARCH,
        name,
        $id: mappingId,
        searchType,
        query,
        queryScriptValue,
        queryValueType,
        sites,
        sitesScriptValue,
        sitesValueType,
        aiSearchProviders,
        numResults: numResults && !isNaN(numResults) ? numResults : void 0,
        country,
        maxTokens: maxTokens && !isNaN(maxTokens) ? maxTokens : void 0,
        truncate: false,
        truncateConfig,
        depends,
        condition
      };
    }
    __name(buildWebSearchFromRecord, "buildWebSearchFromRecord");
    function buildScriptFromRecord(mapping, attributeMappings, edges, mappingIdToName, capDefMap, decisionMappingIds, defAttrNameMap, capabilityMap) {
      const mappingId = mapping.getId().getValue();
      const name = mapping.get("resource_name")?.getValue();
      if (!name) {
        return null;
      }
      const { inputs: inputRecords, outputs: outputRecords } = (0, tool_utils_1.getAttributeMappingsForTool)(mappingId, attributeMappings);
      let scriptId;
      let scriptFunctionName;
      const metadataStr = mapping.get("metadata")?.getValue();
      if (metadataStr) {
        try {
          const metadata = JSON.parse(metadataStr);
          scriptId = metadata.scriptId;
          scriptFunctionName = metadata.scriptFunctionName;
        } catch {
        }
      }
      if (!scriptId) {
        const resourceCapId2 = mapping.get("resource_capability")?.getValue();
        const capDef = capDefMap.get(resourceCapId2);
        if (capDef) {
          const capDefMetadataStr = capDef.get("metadata")?.getValue();
          if (capDefMetadataStr) {
            try {
              const capDefMetadata = JSON.parse(capDefMetadataStr);
              if (capDefMetadata.scriptId) {
                scriptId = capDefMetadata.scriptId;
              }
              if (capDefMetadata.scriptFunctionName && !scriptFunctionName) {
                scriptFunctionName = capDefMetadata.scriptFunctionName;
              }
            } catch {
            }
          }
          if (!scriptId) {
            const api = capDef.get("api")?.getValue();
            if (api && api !== defaults_1.SCRIPT_TOOL_API_ID) {
              scriptId = api;
            }
          }
        }
      }
      const inputs = [];
      for (const inputRecord of inputRecords) {
        const rawAttrName = inputRecord.get("resource_attribute_name")?.getValue();
        const attrName = (0, tool_utils_1.resolveAttributeName)(rawAttrName, defAttrNameMap);
        const valueType = inputRecord.get("attribute_value_type")?.getValue() || "template";
        if (attrName) {
          if (valueType === "script") {
            const scriptValue = inputRecord.get("attribute_value_script")?.getValue();
            inputs.push({
              $id: rawAttrName,
              name: attrName,
              value: "",
              scriptValue: scriptValue || "",
              valueType: "script"
            });
          } else {
            const template = inputRecord.get("attribute_value_template")?.getValue();
            inputs.push({ $id: rawAttrName, name: attrName, value: template || "" });
          }
        }
      }
      let truncate = false;
      let output;
      for (const outputRec of outputRecords) {
        const truncateVal = outputRec.get("truncate")?.getValue();
        if (truncateVal === true || truncateVal === "true") {
          truncate = true;
        }
        const rawAttrName = outputRec.get("resource_attribute_name")?.getValue();
        if (rawAttrName) {
          const outputName = (0, tool_utils_1.resolveAttributeName)(rawAttrName, defAttrNameMap);
          output = {
            $id: rawAttrName,
            ...outputName && outputName !== "output" ? { name: outputName } : {}
          };
        }
      }
      const depends = (0, tool_utils_1.extractDependsFromEdges)(mappingId, edges, mappingIdToName, decisionMappingIds);
      const condition = (0, tool_utils_1.extractConditionFromMapping)(mapping);
      const resourceCapId = mapping.get("resource_capability")?.getValue();
      const capRecord = capabilityMap?.get(resourceCapId);
      const $capabilityId = capRecord ? capRecord.getId().getNowIdKey() ?? resourceCapId : resourceCapId;
      return {
        type: defaults_1.TOOL_TYPES.SCRIPT,
        name,
        $id: mappingId,
        $capabilityId,
        output,
        scriptId,
        scriptFunctionName,
        inputs: inputs.length > 0 ? inputs : void 0,
        truncate,
        depends,
        condition
      };
    }
    __name(buildScriptFromRecord, "buildScriptFromRecord");
    function buildSubflowFromRecord(mapping, attributeMappings, edges, mappingIdToName, capDefMap, decisionMappingIds, defAttrInfoMap, capabilityMap) {
      const mappingId = mapping.getId().getValue();
      const name = mapping.get("resource_name")?.getValue();
      if (!name) {
        return null;
      }
      const { inputs: inputRecords, outputs: outputRecords } = (0, tool_utils_1.getAttributeMappingsForTool)(mappingId, attributeMappings);
      let subflowId;
      const metadataStr = mapping.get("metadata")?.getValue();
      if (metadataStr) {
        try {
          const metadata = JSON.parse(metadataStr);
          subflowId = metadata.subflowId;
        } catch {
        }
      }
      if (!subflowId) {
        const resourceCapId2 = mapping.get("resource_capability")?.getValue();
        const capDef = capDefMap.get(resourceCapId2);
        if (capDef) {
          const api = capDef.get("api")?.getValue();
          if (api) {
            subflowId = api;
          }
        }
      }
      const resolveAttrInfo = /* @__PURE__ */ __name((rawAttrName) => {
        if (!rawAttrName) {
          return null;
        }
        const info = defAttrInfoMap?.get(rawAttrName);
        if (info) {
          return info;
        }
        return { name: rawAttrName, dataType: "string" };
      }, "resolveAttrInfo");
      const inputs = [];
      for (const inputRecord of inputRecords) {
        const rawAttrName = inputRecord.get("resource_attribute_name")?.getValue();
        const attrInfo = resolveAttrInfo(rawAttrName);
        const valueType = inputRecord.get("attribute_value_type")?.getValue() || "template";
        if (attrInfo) {
          if (valueType === "script") {
            const scriptValue = inputRecord.get("attribute_value_script")?.getValue();
            inputs.push({
              $id: rawAttrName,
              name: attrInfo.name,
              type: attrInfo.dataType,
              value: "",
              scriptValue: scriptValue || "",
              valueType: "script"
            });
          } else {
            const template = inputRecord.get("attribute_value_template")?.getValue();
            inputs.push({ $id: rawAttrName, name: attrInfo.name, type: attrInfo.dataType, value: template || "" });
          }
        }
      }
      const outputs = [];
      let truncate = false;
      for (const outputRecord of outputRecords) {
        const rawAttrName = outputRecord.get("resource_attribute_name")?.getValue();
        const attrInfo = resolveAttrInfo(rawAttrName);
        const truncateVal = outputRecord.get("truncate")?.getValue();
        const outputTruncate = truncateVal === true || truncateVal === "true";
        if (attrInfo) {
          const output = {
            $id: rawAttrName,
            name: attrInfo.name,
            type: attrInfo.dataType || "object"
          };
          if (outputTruncate) {
            output.truncate = true;
          }
          outputs.push(output);
        }
        if (outputTruncate) {
          truncate = true;
        }
      }
      const depends = (0, tool_utils_1.extractDependsFromEdges)(mappingId, edges, mappingIdToName, decisionMappingIds);
      const condition = (0, tool_utils_1.extractConditionFromMapping)(mapping);
      const resourceCapId = mapping.get("resource_capability")?.getValue();
      const capRecord = capabilityMap?.get(resourceCapId);
      const $capabilityId = capRecord ? capRecord.getId().getNowIdKey() ?? resourceCapId : resourceCapId;
      const isSingleDefaultOutput = outputs.length === 1 && outputs[0]?.name === "Output";
      return {
        type: defaults_1.TOOL_TYPES.SUBFLOW,
        name,
        $id: mappingId,
        $capabilityId,
        subflowId,
        inputs: inputs.length > 0 ? inputs : void 0,
        outputs: !isSingleDefaultOutput && outputs.length > 0 ? outputs : void 0,
        truncate: isSingleDefaultOutput ? truncate : void 0,
        depends,
        condition
      };
    }
    __name(buildSubflowFromRecord, "buildSubflowFromRecord");
    function buildFlowActionFromRecord(mapping, attributeMappings, edges, mappingIdToName, decisionMappingIds, capDefMap, defAttrInfoMap, capabilityMap) {
      const mappingId = mapping.getId().getValue();
      const name = mapping.get("resource_name")?.getValue();
      if (!name) {
        return null;
      }
      const { inputs: inputRecords, outputs: outputRecords } = (0, tool_utils_1.getAttributeMappingsForTool)(mappingId, attributeMappings);
      let actionId;
      const metadataStr = mapping.get("metadata")?.getValue();
      if (metadataStr) {
        try {
          const metadata = JSON.parse(metadataStr);
          actionId = metadata.actionId;
        } catch {
        }
      }
      if (!actionId && capDefMap) {
        const resourceCapId2 = mapping.get("resource_capability")?.getValue();
        const capDef = capDefMap.get(resourceCapId2);
        if (capDef) {
          const api = capDef.get("api")?.getValue();
          if (api) {
            actionId = api;
          }
        }
      }
      const resolveAttrInfo = /* @__PURE__ */ __name((rawAttrName) => {
        if (!rawAttrName) {
          return null;
        }
        const info = defAttrInfoMap?.get(rawAttrName);
        if (info) {
          return info;
        }
        return { name: rawAttrName, dataType: "string" };
      }, "resolveAttrInfo");
      const inputs = [];
      for (const inputRecord of inputRecords) {
        const rawAttrName = inputRecord.get("resource_attribute_name")?.getValue();
        const attrInfo = resolveAttrInfo(rawAttrName);
        const valueType = inputRecord.get("attribute_value_type")?.getValue() || "template";
        if (attrInfo) {
          if (valueType === "script") {
            const scriptValue = inputRecord.get("attribute_value_script")?.getValue();
            inputs.push({
              $id: rawAttrName,
              name: attrInfo.name,
              type: attrInfo.dataType,
              value: "",
              scriptValue: scriptValue || "",
              valueType: "script"
            });
          } else {
            const template = inputRecord.get("attribute_value_template")?.getValue();
            inputs.push({ $id: rawAttrName, name: attrInfo.name, type: attrInfo.dataType, value: template || "" });
          }
        }
      }
      const outputs = [];
      for (const outputRecord of outputRecords) {
        const rawAttrName = outputRecord.get("resource_attribute_name")?.getValue();
        const attrInfo = resolveAttrInfo(rawAttrName);
        const truncateVal = outputRecord.get("truncate")?.getValue();
        const isTruncated = truncateVal === true || truncateVal === "true";
        if (attrInfo) {
          outputs.push({
            $id: rawAttrName,
            name: attrInfo.name,
            type: attrInfo.dataType,
            ...isTruncated ? { truncate: true } : {}
          });
        }
      }
      const depends = (0, tool_utils_1.extractDependsFromEdges)(mappingId, edges, mappingIdToName, decisionMappingIds);
      const resourceCapId = mapping.get("resource_capability")?.getValue();
      const capRecord = capabilityMap?.get(resourceCapId);
      const $capabilityId = capRecord ? capRecord.getId().getNowIdKey() ?? resourceCapId : resourceCapId;
      return {
        type: defaults_1.TOOL_TYPES.FLOW_ACTION,
        name,
        $id: mappingId,
        $capabilityId,
        actionId,
        inputs: inputs.length > 0 ? inputs : void 0,
        outputs: outputs.length > 0 ? outputs : void 0,
        depends
      };
    }
    __name(buildFlowActionFromRecord, "buildFlowActionFromRecord");
    function buildSkillFromRecord(mapping, attributeMappings, edges, mappingIdToName, decisionMappingIds, defAttrNameMap) {
      const mappingId = mapping.getId().getValue();
      const name = mapping.get("resource_name")?.getValue();
      if (!name) {
        return null;
      }
      const { inputs: inputRecords, outputs: outputRecords } = (0, tool_utils_1.getAttributeMappingsForTool)(mappingId, attributeMappings);
      let skillId;
      let skillIdIsReference = false;
      let skillIdExplicitId;
      const metadataStr = mapping.get("metadata")?.getValue();
      if (metadataStr) {
        try {
          const metadata = JSON.parse(metadataStr);
          skillId = metadata.skillId;
        } catch {
        }
      }
      if (!skillId) {
        const resourceCapability = mapping.get("resource_capability")?.getValue();
        if (resourceCapability) {
          if (resourceCapability.endsWith("__capability")) {
            const baseSkillId = resourceCapability.slice(0, -"__capability".length);
            skillIdExplicitId = baseSkillId;
            skillIdIsReference = true;
          } else {
            skillId = resourceCapability;
          }
        }
      }
      const inputs = [];
      for (const inputRecord of inputRecords) {
        const rawAttrName = inputRecord.get("resource_attribute_name")?.getValue();
        if (!rawAttrName) {
          continue;
        }
        const valueType = inputRecord.get("attribute_value_type")?.getValue() || "template";
        if (valueType === "script") {
          const scriptValue = inputRecord.get("attribute_value_script")?.getValue();
          inputs.push({
            name: rawAttrName,
            value: "",
            scriptValue: scriptValue || "",
            valueType: "script"
          });
        } else {
          const template = inputRecord.get("attribute_value_template")?.getValue();
          inputs.push({ name: rawAttrName, value: template || "" });
        }
      }
      const skillOutputs = {};
      for (const outputRecord of outputRecords) {
        const rawAttrName = outputRecord.get("resource_attribute_name")?.getValue();
        if (!rawAttrName) {
          continue;
        }
        const resolvedName = (0, tool_utils_1.resolveAttributeName)(rawAttrName, defAttrNameMap);
        if (resolvedName === "Output") {
          continue;
        }
        const rawKey = resolvedName || rawAttrName;
        const attrName = defaults_1.SKILL_TOOL_CONFIG.OUTPUT_NAME_TO_TRUNCATE_KEY[rawKey] || rawKey;
        const truncateVal = outputRecord.get("truncate")?.getValue();
        const isTruncated = truncateVal === true || truncateVal === "true";
        skillOutputs[attrName] = {
          definitionAttributeId: rawAttrName,
          ...isTruncated ? { truncate: true } : {}
        };
      }
      const depends = (0, tool_utils_1.extractDependsFromEdges)(mappingId, edges, mappingIdToName, decisionMappingIds);
      return {
        type: defaults_1.TOOL_TYPES.SKILL,
        name,
        $id: mappingId,
        skillId,
        skillIdIsReference,
        skillIdExplicitId,
        inputs: inputs.length > 0 ? inputs : void 0,
        skillOutputs: Object.keys(skillOutputs).length > 0 ? skillOutputs : void 0,
        depends
      };
    }
    __name(buildSkillFromRecord, "buildSkillFromRecord");
    function buildDecisionFromRecord(mapping, _attributeMappings, edges, mappingIdToName, parentMappingIds, decisionMappingIds) {
      const mappingId = mapping.getId().getValue();
      const name = mapping.get("resource_name")?.getValue();
      if (!name) {
        return null;
      }
      const targetsSet = /* @__PURE__ */ new Set();
      const targets = [];
      const branches = [];
      let defaultTarget;
      for (const edge of edges) {
        const fromId = edge.get("from_resource")?.getValue();
        if (fromId !== mappingId) {
          continue;
        }
        const toId = edge.get("to_resource")?.getValue();
        const isEdgeToPrompt = parentMappingIds.has(toId);
        const targetName = isEdgeToPrompt ? "_end" : mappingIdToName.get(toId);
        if (!targetName) {
          continue;
        }
        if (!targetsSet.has(targetName)) {
          targetsSet.add(targetName);
          targets.push(targetName);
        }
        const edgeName = edge.get("name")?.getValue() || "";
        const applicabilityConditionExpr = edge.get("applicability_condition_expression")?.getValue();
        const applicabilityScript = edge.get("applicability_script")?.getValue();
        const edgeOrder = edge.get("order")?.getValue();
        const isDefaultBranch = edgeName === "Default branch" || edgeOrder === 1e3 || edgeOrder === "1000" || !applicabilityConditionExpr && (!applicabilityScript || applicabilityScript.includes("return true;"));
        if (isDefaultBranch) {
          defaultTarget = isEdgeToPrompt ? "_end" : targetName;
        } else {
          const applicabilityType = edge.get("applicability_type")?.getValue();
          const isScript = applicabilityType === "script";
          const condition = isScript ? applicabilityScript || "" : applicabilityConditionExpr || "";
          const conditionType = isScript ? "script" : "filter";
          branches.push({ name: edgeName, target: targetName, condition, conditionType });
        }
      }
      const depends = (0, tool_utils_1.extractDependsFromEdges)(mappingId, edges, mappingIdToName, decisionMappingIds);
      return {
        type: defaults_1.TOOL_TYPES.DECISION,
        name,
        $id: mappingId,
        targets: targets.length > 0 ? targets : void 0,
        branches: branches.length > 0 ? branches : void 0,
        default: defaultTarget,
        depends
      };
    }
    __name(buildDecisionFromRecord, "buildDecisionFromRecord");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tool-shape-builder.js
var require_tool_shape_builder = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tool-shape-builder.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.buildToolsArrowFunctionShape = buildToolsArrowFunctionShape;
    var defaults_1 = require_defaults();
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var tool_utils_1 = require_tool_utils();
    var TOOL_TYPE_TO_METHOD = {
      [defaults_1.TOOL_TYPES.INLINE_SCRIPT]: "InlineScript",
      [defaults_1.TOOL_TYPES.SCRIPT]: "Script",
      [defaults_1.TOOL_TYPES.FLOW_ACTION]: "FlowAction",
      [defaults_1.TOOL_TYPES.SUBFLOW]: "Subflow",
      [defaults_1.TOOL_TYPES.SKILL]: "Skill",
      [defaults_1.TOOL_TYPES.WEB_SEARCH]: "WebSearch",
      [defaults_1.TOOL_TYPES.DECISION]: "Decision"
    };
    function buildToolConfigProperties(source, tool, paramName, toolNameToVarName, inputNameMap, decisionNames) {
      const props = {};
      if (tool.$id) {
        props.$id = new sdk_build_plugins_1.NowIdShape({ source, id: tool.$id });
      }
      if (tool.$capabilityId) {
        props.$capabilityId = new sdk_build_plugins_1.NowIdShape({ source, id: tool.$capabilityId });
      }
      if (tool.depends && tool.depends.length > 0) {
        const nonDecisionDeps = tool.depends.filter((dep) => !decisionNames?.has(dep));
        if (nonDecisionDeps.length > 0) {
          const depShapes = nonDecisionDeps.map((dep) => {
            const varName = toolNameToVarName.get(dep);
            if (varName) {
              return new sdk_build_core_1.IdentifierShape({ source, name: varName });
            }
            return new sdk_build_core_1.StringLiteralShape({ source, literalText: dep });
          });
          props.depends = new sdk_build_core_1.ArrayShape({ source, elements: depShapes });
        }
      }
      switch (tool.type) {
        case defaults_1.TOOL_TYPES.INLINE_SCRIPT: {
          if (tool.script !== void 0) {
            props.script = tool.script;
          }
          if (tool.truncate) {
            props.truncate = true;
          }
          break;
        }
        case defaults_1.TOOL_TYPES.SCRIPT: {
          if (tool.scriptId) {
            props.scriptId = tool.scriptId;
          }
          if (tool.scriptFunctionName) {
            props.scriptFunctionName = tool.scriptFunctionName;
          }
          if (tool.inputs && tool.inputs.length > 0) {
            props.inputs = buildInputShapes(source, tool.inputs, paramName, toolNameToVarName, inputNameMap);
          }
          if (tool.output?.$id) {
            const outputProps = {
              $id: new sdk_build_plugins_1.NowIdShape({ source, id: tool.output.$id })
            };
            if (tool.output.name) {
              outputProps.name = tool.output.name;
            }
            if (tool.output.type) {
              outputProps.type = tool.output.type;
            }
            props.output = new sdk_build_core_1.ObjectShape({ source, properties: outputProps });
          }
          if (tool.truncate) {
            props.truncate = true;
          }
          break;
        }
        case defaults_1.TOOL_TYPES.FLOW_ACTION: {
          if (tool.actionId) {
            props.actionId = tool.actionId;
          }
          if (tool.inputs && tool.inputs.length > 0) {
            props.inputs = buildFlowActionInputShapes(source, tool.inputs, paramName, toolNameToVarName, inputNameMap);
          }
          if (tool.outputs && tool.outputs.length > 0) {
            props.outputs = buildOutputShapes(source, tool.outputs);
          }
          break;
        }
        case defaults_1.TOOL_TYPES.SUBFLOW: {
          if (tool.subflowIdIsReference && tool.subflowIdExplicitId) {
            props.subflowId = new tool_utils_1.RawCodeShape({
              source,
              code: `Now.ID['${tool.subflowIdExplicitId}']`
            });
          } else if (tool.subflowId) {
            props.subflowId = tool.subflowId;
          }
          if (tool.inputs && tool.inputs.length > 0) {
            props.inputs = buildInputShapes(source, tool.inputs, paramName, toolNameToVarName, inputNameMap);
          }
          if (tool.outputs && tool.outputs.length > 0) {
            props.outputs = buildOutputShapes(source, tool.outputs);
          }
          if (tool.truncate) {
            props.truncate = true;
          }
          break;
        }
        case defaults_1.TOOL_TYPES.SKILL: {
          if (tool.skillIdIsReference && tool.skillIdExplicitId) {
            props.skillId = new tool_utils_1.RawCodeShape({
              source,
              code: `Now.ID['${tool.skillIdExplicitId}']`
            });
          } else if (tool.skillId) {
            props.skillId = tool.skillId;
          }
          if (tool.inputs && tool.inputs.length > 0) {
            props.inputs = buildSkillInputShapes(source, tool.inputs, paramName, toolNameToVarName, inputNameMap);
          }
          if (tool.skillOutputs) {
            const outputProps = {};
            for (const [key, output] of Object.entries(tool.skillOutputs)) {
              const entryProps = {
                definitionAttributeId: typeof output.definitionAttributeId === "string" ? output.definitionAttributeId : output.definitionAttributeId
              };
              if (output["truncate"]) {
                entryProps["truncate"] = true;
              }
              outputProps[key] = new sdk_build_core_1.ObjectShape({ source, properties: entryProps });
            }
            props.outputs = new sdk_build_core_1.ObjectShape({ source, properties: outputProps });
          }
          break;
        }
        case defaults_1.TOOL_TYPES.WEB_SEARCH: {
          if (tool.searchType) {
            props.searchType = tool.searchType;
          }
          if (tool.queryValueType === "script" && tool.queryScriptValue) {
            props.query = new sdk_build_core_1.ObjectShape({
              source,
              properties: { type: "script", script: tool.queryScriptValue }
            });
          } else if (tool.query) {
            props.query = (0, tool_utils_1.templateValueToShape)(source, tool.query, paramName, toolNameToVarName, inputNameMap);
          }
          if (tool.sitesValueType === "script" && tool.sitesScriptValue) {
            props.sites = new sdk_build_core_1.ObjectShape({
              source,
              properties: { type: "script", script: tool.sitesScriptValue }
            });
          } else if (tool.sites) {
            props.sites = (0, tool_utils_1.templateValueToShape)(source, tool.sites, paramName, toolNameToVarName, inputNameMap);
          }
          if (tool.aiSearchProviders) {
            props.aiSearchProviders = tool.aiSearchProviders;
          }
          if (tool.numResults !== void 0) {
            props.numResults = tool.numResults;
          }
          if (tool.country) {
            props.country = tool.country;
          }
          if (tool.maxTokens !== void 0) {
            props.maxTokens = tool.maxTokens;
          }
          if (tool.truncateConfig) {
            props.truncate = tool.truncateConfig;
          }
          break;
        }
        case defaults_1.TOOL_TYPES.DECISION: {
          const targetsParam = "targets";
          if (tool.targets && tool.targets.length > 0) {
            const targetsCode = `[${tool.targets.map((t) => `'${t}'`).join(", ")}] as const`;
            props.targets = new tool_utils_1.RawCodeShape({ source, code: targetsCode });
          }
          if (tool.branches && tool.branches.length > 0) {
            const branchItems = tool.branches.map((b, idx) => {
              const condParts = [];
              const branchName = b.name || `${b.target}Branch${idx + 1}`;
              condParts.push(`name: '${branchName}'`);
              condParts.push(`to: ${targetsParam}.${b.target}`);
              if (b.condition) {
                if (b.conditionType === "script") {
                  const escapedScript = b.condition.replace(/\\/g, "\\\\").replace(/'/g, "\\'").replace(/\n/g, "\\n").replace(/\r/g, "\\r");
                  condParts.push(`condition: { script: '${escapedScript}' }`);
                } else {
                  const opMap = { "=": "is", "!=": "is not" };
                  const filterMatch = b.condition.match(/^(.+?)(!?=)(.*)$/);
                  if (filterMatch?.[1] && filterMatch[2]) {
                    const rawField = filterMatch[1];
                    const operator = opMap[filterMatch[2]] ?? filterMatch[2];
                    const value = filterMatch[3] ?? "";
                    const templateMatch = rawField.match(/^\{\{(.+?)\}\}$/);
                    const innerField = templateMatch ? templateMatch[1] : rawField;
                    const dotIdx = innerField.indexOf(".");
                    const prefix = dotIdx > 0 ? innerField.substring(0, dotIdx) : innerField;
                    const isKnownTool = toolNameToVarName.has(prefix);
                    const isKnownInput = inputNameMap?.has(innerField) || inputNameMap?.has(prefix);
                    const isInputPrefix = prefix === "input";
                    if (templateMatch && (isKnownTool || isKnownInput || isInputPrefix)) {
                      const fieldShape = (0, tool_utils_1.templateValueToShape)(source, `{{${innerField}}}`, paramName, toolNameToVarName, inputNameMap);
                      condParts.push(`condition: { field: ${fieldShape.getCode()}, operator: '${operator}', value: '${value}' }`);
                    } else {
                      const escapedField = rawField.replace(/'/g, "\\'");
                      condParts.push(`condition: { field: '${escapedField}', operator: '${operator}', value: '${value}' }`);
                    }
                  } else {
                    const escapedValue = b.condition.replace(/'/g, "\\'").replace(/\n/g, "\\n");
                    condParts.push(`condition: { expression: '${escapedValue}' }`);
                  }
                }
              } else {
                condParts.push(`condition: { script: 'return true;' }`);
              }
              return `{ ${condParts.join(", ")} }`;
            });
            const branchItemShapes = branchItems.map((item) => new tool_utils_1.RawCodeShape({ source, code: item }));
            props.branches = new sdk_build_plugins_1.ArrowFunctionShape({
              source,
              parameters: [targetsParam],
              statements: [],
              returnValue: new sdk_build_core_1.ArrayShape({ source, elements: branchItemShapes }),
              implicitReturn: true
            });
          } else {
            props.branches = new sdk_build_plugins_1.ArrowFunctionShape({
              source,
              parameters: [],
              statements: [],
              returnValue: new sdk_build_core_1.ArrayShape({ source, elements: [] }),
              implicitReturn: true
            });
          }
          if (tool.default) {
            props.default = new sdk_build_plugins_1.ArrowFunctionShape({
              source,
              parameters: [targetsParam],
              statements: [],
              returnValue: new sdk_build_core_1.PropertyAccessShape({ source, elements: [targetsParam, tool.default] }),
              implicitReturn: true
            });
          } else if (tool.targets && tool.targets.length > 0) {
            props.default = new sdk_build_plugins_1.ArrowFunctionShape({
              source,
              parameters: [targetsParam],
              statements: [],
              returnValue: new sdk_build_core_1.PropertyAccessShape({ source, elements: [targetsParam, tool.targets[0]] }),
              implicitReturn: true
            });
          }
          break;
        }
      }
      if (tool.condition) {
        props.condition = {
          ...tool.condition.type ? { type: tool.condition.type } : {},
          ...tool.condition.script ? { script: tool.condition.script } : {}
        };
      }
      return props;
    }
    __name(buildToolConfigProperties, "buildToolConfigProperties");
    function buildInputShapes(source, inputs, paramName, toolNameToVarName, inputNameMap) {
      return inputs.map((inp) => {
        const inputProps = {};
        if (inp.$id) {
          inputProps.$id = new sdk_build_plugins_1.NowIdShape({ source, id: inp.$id });
        }
        inputProps.name = inp.name;
        if (inp.type) {
          inputProps.type = inp.type;
        }
        if (inp.valueType === "script" && inp.scriptValue) {
          inputProps.value = new sdk_build_core_1.ObjectShape({
            source,
            properties: { type: "script", script: inp.scriptValue }
          });
        } else if (inp.value) {
          inputProps.value = (0, tool_utils_1.templateValueToShape)(source, inp.value, paramName, toolNameToVarName, inputNameMap);
        }
        return new sdk_build_core_1.ObjectShape({ source, properties: inputProps });
      });
    }
    __name(buildInputShapes, "buildInputShapes");
    function buildSkillInputShapes(source, inputs, paramName, toolNameToVarName, inputNameMap) {
      return inputs.map((inp) => {
        const inputProps = {};
        inputProps.definitionAttributeId = inp.name;
        if (inp.valueType === "script" && inp.scriptValue) {
          inputProps.value = new sdk_build_core_1.ObjectShape({
            source,
            properties: { type: "script", script: inp.scriptValue }
          });
        } else if (inp.value) {
          inputProps.value = (0, tool_utils_1.templateValueToShape)(source, inp.value, paramName, toolNameToVarName, inputNameMap);
        }
        return new sdk_build_core_1.ObjectShape({ source, properties: inputProps });
      });
    }
    __name(buildSkillInputShapes, "buildSkillInputShapes");
    function buildFlowActionInputShapes(source, inputs, paramName, toolNameToVarName, inputNameMap) {
      return inputs.map((inp) => {
        const inputProps = {};
        if (inp.$id) {
          inputProps.$id = new sdk_build_plugins_1.NowIdShape({ source, id: inp.$id });
        }
        inputProps.name = inp.name;
        if (inp.type) {
          inputProps.type = inp.type;
        }
        if (inp.valueType === "script" && inp.scriptValue) {
          inputProps.value = new sdk_build_core_1.ObjectShape({
            source,
            properties: { type: "script", script: inp.scriptValue }
          });
        } else if (inp.value) {
          inputProps.value = (0, tool_utils_1.templateValueToShape)(source, inp.value, paramName, toolNameToVarName, inputNameMap);
        }
        return new sdk_build_core_1.ObjectShape({ source, properties: inputProps });
      });
    }
    __name(buildFlowActionInputShapes, "buildFlowActionInputShapes");
    function buildOutputShapes(source, outputs) {
      return outputs.map((out) => {
        const outProps = {};
        if (out.$id) {
          outProps.$id = new sdk_build_plugins_1.NowIdShape({ source, id: out.$id });
        }
        outProps.name = out.name;
        outProps.type = out.type;
        if (out.truncate) {
          outProps.truncate = true;
        }
        return new sdk_build_core_1.ObjectShape({ source, properties: outProps });
      });
    }
    __name(buildOutputShapes, "buildOutputShapes");
    function buildToolsArrowFunctionShape(tools, source, inputNameMap, existingToolVarNames) {
      if (!tools || tools.length === 0) {
        return void 0;
      }
      const paramName = "t";
      const toolNameToVarName = /* @__PURE__ */ new Map();
      for (const tool of tools) {
        const varName = existingToolVarNames?.get(tool.name) ?? (0, tool_utils_1.sanitizeVarName)(tool.name);
        toolNameToVarName.set(tool.name, varName);
      }
      const decisionNames = /* @__PURE__ */ new Set();
      for (const tool of tools) {
        if (tool.type === defaults_1.TOOL_TYPES.DECISION) {
          decisionNames.add(tool.name);
        }
      }
      const sortedTools = (0, tool_utils_1.topologicalSortTools)(tools);
      const statements = [];
      const emittedToolNames = /* @__PURE__ */ new Set();
      for (const tool of sortedTools) {
        const methodName = TOOL_TYPE_TO_METHOD[tool.type];
        if (!methodName) {
          continue;
        }
        const configProps = buildToolConfigProperties(source, tool, paramName, toolNameToVarName, inputNameMap, decisionNames);
        const callExpression = new sdk_build_core_1.CallExpressionShape({
          source,
          callee: `${paramName}.${methodName}`,
          args: [
            new sdk_build_core_1.StringLiteralShape({ source, literalText: tool.name }),
            new sdk_build_core_1.ObjectShape({ source, properties: configProps })
          ]
        });
        const varName = toolNameToVarName.get(tool.name);
        statements.push(new sdk_build_core_1.VariableStatementShape({
          source,
          variableName: varName,
          initializer: callExpression
        }));
        emittedToolNames.add(tool.name);
      }
      const returnProps = {};
      for (const tool of sortedTools) {
        if (!emittedToolNames.has(tool.name) || tool.type === defaults_1.TOOL_TYPES.DECISION) {
          continue;
        }
        const varName = toolNameToVarName.get(tool.name);
        if (!varName) {
          continue;
        }
        returnProps[tool.name] = new sdk_build_core_1.IdentifierShape({ source, name: varName });
      }
      return new sdk_build_plugins_1.ArrowFunctionShape({
        source,
        parameters: [paramName],
        statements,
        returnValue: new sdk_build_core_1.ObjectShape({ source, properties: returnProps })
      });
    }
    __name(buildToolsArrowFunctionShape, "buildToolsArrowFunctionShape");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tool-processor.js
var require_tool_processor = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/tool-processor.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.buildToolsArrowFunctionShape = exports2.generateTools = exports2.processTools = exports2.createDecisionEdges = exports2.processDecisionTool = exports2.processSkillTool = exports2.processFlowActionTool = exports2.processWebSearchTool = exports2.processSubflowTool = exports2.processScriptTool = exports2.processInlineScriptTool = exports2.inputChainToTemplate = exports2.normalizeInputFieldName = exports2.RawCodeShape = void 0;
    var tool_utils_1 = require_tool_utils();
    Object.defineProperty(exports2, "RawCodeShape", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return tool_utils_1.RawCodeShape;
    }, "get") });
    Object.defineProperty(exports2, "normalizeInputFieldName", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return tool_utils_1.normalizeInputFieldName;
    }, "get") });
    Object.defineProperty(exports2, "inputChainToTemplate", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return tool_utils_1.inputChainToTemplate;
    }, "get") });
    var inline_script_tool_1 = require_inline_script_tool();
    Object.defineProperty(exports2, "processInlineScriptTool", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return inline_script_tool_1.processInlineScriptTool;
    }, "get") });
    var script_tool_1 = require_script_tool();
    Object.defineProperty(exports2, "processScriptTool", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return script_tool_1.processScriptTool;
    }, "get") });
    var subflow_tool_1 = require_subflow_tool();
    Object.defineProperty(exports2, "processSubflowTool", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return subflow_tool_1.processSubflowTool;
    }, "get") });
    var web_search_tool_1 = require_web_search_tool();
    Object.defineProperty(exports2, "processWebSearchTool", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return web_search_tool_1.processWebSearchTool;
    }, "get") });
    var flow_action_tool_1 = require_flow_action_tool();
    Object.defineProperty(exports2, "processFlowActionTool", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return flow_action_tool_1.processFlowActionTool;
    }, "get") });
    var skill_tool_1 = require_skill_tool();
    Object.defineProperty(exports2, "processSkillTool", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return skill_tool_1.processSkillTool;
    }, "get") });
    var decision_tool_1 = require_decision_tool();
    Object.defineProperty(exports2, "processDecisionTool", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return decision_tool_1.processDecisionTool;
    }, "get") });
    Object.defineProperty(exports2, "createDecisionEdges", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return decision_tool_1.createDecisionEdges;
    }, "get") });
    var tool_parser_1 = require_tool_parser();
    Object.defineProperty(exports2, "processTools", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return tool_parser_1.processTools;
    }, "get") });
    var tool_transform_1 = require_tool_transform();
    Object.defineProperty(exports2, "generateTools", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return tool_transform_1.generateTools;
    }, "get") });
    var tool_shape_builder_1 = require_tool_shape_builder();
    Object.defineProperty(exports2, "buildToolsArrowFunctionShape", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return tool_shape_builder_1.buildToolsArrowFunctionShape;
    }, "get") });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/attribute-processor.js
var require_attribute_processor = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/attribute-processor.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.validateName = validateName;
    exports2.generateInternalName = generateInternalName;
    exports2.processAttributes = processAttributes;
    var defaults_1 = require_defaults();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var tool_processor_1 = require_tool_processor();
    var { SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE } = defaults_1.TABLE_NAMES;
    function validateName(str, diagnostics, source, nameType = "Name") {
      const regex = /^[a-zA-Z0-9 ]+$/;
      if (!regex.test(str)) {
        const invalidChars = str.match(/[^a-zA-Z0-9 ]/g);
        const invalidCharsStr = invalidChars ? [...new Set(invalidChars)].join(", ") : "";
        diagnostics.error(source, `${nameType} '${str}' contains invalid character(s): ${invalidCharsStr}. ${nameType}s must contain only letters, numbers, and spaces. Please remove special characters, underscores, and punctuation.`);
      }
      return str;
    }
    __name(validateName, "validateName");
    function generateInternalName(displayName) {
      return displayName.toLowerCase().trim().replace(/\s+/g, "_");
    }
    __name(generateInternalName, "generateInternalName");
    async function processAttributes(definitionShape, capabilityRecord, capabilityDefinitionRecord, arg, factory, diagnostics, callExpression) {
      const attributeRecords = [];
      const inputNames = /* @__PURE__ */ new Set();
      if (definitionShape.has("inputs")) {
        const inputs = definitionShape.get("inputs");
        const inputElements = inputs.isArray() ? inputs.asArray().getElements() : [];
        if (inputElements.length > 0 || inputs.isArray()) {
          const glideRecordInputs = [];
          for (const inputAttr of inputElements) {
            if (inputAttr.isObject()) {
              const dataType = inputAttr.has("dataType") ? inputAttr.get("dataType")?.asString().getValue() : "string";
              if (dataType === "glide_record") {
                const displayName = inputAttr.has("name") ? inputAttr.get("name")?.asString().getValue() : "unknown";
                glideRecordInputs.push(displayName);
              }
            }
          }
          if (glideRecordInputs.length > 1) {
            diagnostics.error(inputs, `Only one input attribute can have data_type 'glide_record'. Found ${glideRecordInputs.length} glide_record inputs: [${glideRecordInputs.join(", ")}]. The glide_record type is designed to reference a single record. If you need multiple records, consider using 'json_array' or 'simple_array' data types instead.`);
            return { records: [], inputNames };
          }
          const normalizedInputNames = /* @__PURE__ */ new Map();
          for (const inputAttr of inputElements) {
            if (inputAttr.isObject()) {
              const displayName = inputAttr.has("name") ? inputAttr.get("name")?.asString().getValue() : "unknown";
              const normalizedName = (0, tool_processor_1.normalizeInputFieldName)(displayName ?? "unknown");
              if (inputNames.has(displayName)) {
                diagnostics.error(inputAttr, `Duplicate input name '${displayName}'. Each input must have a unique name.`);
                return { records: [], inputNames };
              }
              const existingName = normalizedInputNames.get(normalizedName);
              if (existingName && existingName !== displayName) {
                diagnostics.error(inputAttr, `Input name '${displayName}' conflicts with '${existingName}'. Both normalize to '${normalizedName}' internally. Input names must be unique when normalized to lowercase.`);
                return { records: [], inputNames };
              }
              inputNames.add(displayName);
              normalizedInputNames.set(normalizedName, displayName);
            }
          }
          for (const inputAttr of inputElements) {
            if (inputAttr.isObject()) {
              const displayName = inputAttr.has("name") ? inputAttr.get("name")?.asString().getValue() : "";
              const inputExplicitId = inputAttr.get("$id");
              if (!displayName || displayName.trim() === "") {
                diagnostics.error(inputAttr, "Attribute name is required and cannot be empty");
                continue;
              }
              const validatedDisplayName = validateName(displayName, diagnostics, inputAttr, "Attribute name");
              const generatedInternalName = generateInternalName(validatedDisplayName);
              const label = displayName;
              const description = inputAttr.has("description") ? inputAttr.get("description")?.asString().getValue() : "";
              let mandatory = false;
              if (inputAttr.has("mandatory")) {
                const mandatoryShape = inputAttr.get("mandatory");
                const mandatoryRawValue = mandatoryShape?.getValue();
                mandatory = mandatoryRawValue === true || mandatoryRawValue === "true" || mandatoryRawValue === "1";
              }
              const dataType = inputAttr.has("dataType") ? inputAttr.get("dataType")?.asString().getValue() : "string";
              const testValues = inputAttr.has("testValues") ? inputAttr.get("testValues")?.asString().getValue() : "";
              let truncate = false;
              if (inputAttr.has("truncate")) {
                const truncateShape = inputAttr.get("truncate");
                const truncateRawValue = truncateShape?.getValue();
                truncate = truncateRawValue === true || truncateRawValue === "true" || truncateRawValue === "1";
              }
              const nonTruncatableDataTypes = ["simple_array", "json_object", "json_array"];
              if (truncate && nonTruncatableDataTypes.includes(dataType || "")) {
                diagnostics.error(inputAttr.has("truncate") ? inputAttr.get("truncate") : inputAttr, `Input attribute '${displayName}': The 'truncate' property is not supported for dataType '${dataType}'. Only the following data types support truncate: string, numeric, boolean, record, glide_record. Please remove the 'truncate' property or change the dataType.`);
                return { records: [], inputNames };
              }
              let schema = "";
              if (dataType === "glide_record") {
                const tableName = inputAttr.has("tableName") ? inputAttr.get("tableName")?.asString().getValue() : "";
                let tableSysId = "";
                if (inputAttr.has("tableSysId")) {
                  const tableSysIdShape = inputAttr.get("tableSysId");
                  const rawValue = tableSysIdShape?.getValue();
                  if (rawValue && typeof rawValue === "object" && "id" in rawValue) {
                    tableSysId = typeof rawValue.id === "string" ? rawValue.id : "";
                  } else if (typeof rawValue === "string") {
                    tableSysId = rawValue;
                  }
                }
                if (!tableName || testValues && !tableSysId) {
                  diagnostics.error(inputAttr, `dataType 'glide_record' requires 'tableName'${testValues ? " and 'tableSysId' when 'testValues' is provided" : ""}. Missing: ${!tableName ? "tableName" : ""}${!tableName && testValues && !tableSysId ? " and " : ""}${testValues && !tableSysId ? "tableSysId" : ""}.`);
                  continue;
                }
                if (tableName) {
                  schema = JSON.stringify({ tableName, sysId: tableSysId || "" });
                } else if (inputAttr.has("schema")) {
                  const schemaValue = inputAttr.get("schema");
                  if (schemaValue?.isObject()) {
                    schema = JSON.stringify(schemaValue.getValue());
                  } else if (schemaValue?.isString()) {
                    schema = schemaValue.asString().getValue();
                  }
                }
              }
              const attrRecord = await factory.createRecord({
                source: inputAttr,
                table: SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
                explicitId: inputExplicitId,
                properties: arg.transform(({ $ }) => {
                  const properties = {
                    ...(0, skill_plugin_helpers_1.applyDefaults)(SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
                    name: $.val(generatedInternalName),
                    label: $.val(label),
                    description: $.val(description),
                    capability: $.val(capabilityRecord.getId()),
                    type: $.val("input"),
                    data_type: $.val(dataType),
                    mandatory: $.val(mandatory),
                    truncate: $.val(truncate),
                    default_value: $.val(testValues)
                    // test_values maps to default_value column
                    // NOTE: 'order' and 'hidden' intentionally omitted - let platform handle defaults
                  };
                  if (dataType === "glide_record" && schema) {
                    return { ...properties, schema: $.val(schema) };
                  }
                  return properties;
                })
              });
              attributeRecords.push(attrRecord);
            }
          }
        }
      }
      const standardOutputs = ["response", "provider", "errorcode", "status", "error"];
      const outputDescriptions = {
        response: "Response from the skill execution",
        provider: "Provider information for the skill",
        errorcode: "Error code if skill execution fails",
        status: "Status of the skill execution",
        error: "Error message if skill execution fails"
      };
      const standardOutputLabels = {
        response: "response",
        provider: "provider",
        errorcode: "errorCode",
        status: "status",
        error: "error"
      };
      for (const outputName of standardOutputs) {
        const attrRecord = await factory.createRecord({
          source: {
            path: callExpression.getOriginalFilePath(),
            content: ""
          },
          table: SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
          properties: arg.transform(({ $ }) => ({
            ...(0, skill_plugin_helpers_1.applyDefaults)(SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
            name: $.val(outputName),
            label: $.val(standardOutputLabels[outputName]),
            description: $.val(outputDescriptions[outputName] || ""),
            capability: $.val(capabilityRecord.getId()),
            type: $.val("output")
            // NOTE: 'order', 'hidden', 'contains_large_input' intentionally omitted - let platform handle defaults
          }))
        });
        attributeRecords.push(attrRecord);
      }
      if (definitionShape.has("outputs")) {
        const outputs = definitionShape.get("outputs");
        if (outputs.isArray()) {
          const outputElements = outputs.asArray().getElements();
          const glideRecordOutputs = [];
          for (const outputAttr of outputElements) {
            if (outputAttr.isObject()) {
              const dataType = outputAttr.has("dataType") ? outputAttr.get("dataType")?.asString().getValue() : "string";
              if (dataType === "glide_record") {
                const displayName = outputAttr.has("name") ? outputAttr.get("name")?.asString().getValue() : "unknown";
                glideRecordOutputs.push(displayName);
              }
            }
          }
          if (glideRecordOutputs.length > 1) {
            diagnostics.error(outputs, `Only one output attribute can have data_type 'glide_record'. Found ${glideRecordOutputs.length} glide_record outputs: [${glideRecordOutputs.join(", ")}]. The glide_record type is designed to reference a single record. If you need multiple records, consider using 'json_array' or 'simple_array' data types instead.`);
            return { records: [], inputNames };
          }
          const outputNames = /* @__PURE__ */ new Set();
          for (const outputAttr of outputElements) {
            if (outputAttr.isObject()) {
              const displayName = outputAttr.has("name") ? outputAttr.get("name")?.asString().getValue() : "unknown";
              if (outputNames.has(displayName)) {
                diagnostics.error(outputAttr, `Duplicate output name '${displayName}'. Each output must have a unique name.`);
                return { records: [], inputNames };
              }
              if (inputNames.has(displayName)) {
                diagnostics.error(outputAttr, `Output name '${displayName}' conflicts with an input name. Input and output names must be unique across both.`);
                return { records: [], inputNames };
              }
              outputNames.add(displayName);
            }
          }
          for (const outputAttr of outputElements) {
            if (outputAttr.isObject()) {
              const displayName = outputAttr.has("name") ? outputAttr.get("name")?.asString().getValue() : "";
              const outputExplicitId = outputAttr.get("$id");
              if (!displayName || displayName.trim() === "") {
                diagnostics.error(outputAttr, "Attribute name is required and cannot be empty");
                continue;
              }
              const validatedDisplayName = validateName(displayName, diagnostics, outputAttr, "Attribute name");
              const generatedInternalName = generateInternalName(validatedDisplayName);
              if (standardOutputs.includes(generatedInternalName)) {
                continue;
              }
              const label = displayName;
              const description = outputAttr.has("description") ? outputAttr.get("description")?.asString().getValue() : "";
              const dataType = outputAttr.has("dataType") ? outputAttr.get("dataType")?.asString().getValue() : "string";
              let schema = "";
              if (dataType === "glide_record") {
                const tableName = outputAttr.has("tableName") ? outputAttr.get("tableName")?.asString().getValue() : "";
                if (!tableName) {
                  diagnostics.error(outputAttr, `dataType 'glide_record' requires 'tableName'.`);
                  continue;
                }
                schema = JSON.stringify({ tableName, sysId: "" });
              }
              const attrRecord = await factory.createRecord({
                source: outputAttr,
                table: SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
                explicitId: outputExplicitId,
                properties: arg.transform(({ $ }) => {
                  const properties = {
                    ...(0, skill_plugin_helpers_1.applyDefaults)(SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
                    name: $.val(generatedInternalName),
                    label: $.val(label),
                    description: $.val(description),
                    capability: $.val(capabilityRecord.getId()),
                    type: $.val("output"),
                    data_type: $.val(dataType)
                    // NOTE: 'order', 'hidden' intentionally omitted - let platform handle defaults
                  };
                  if (dataType === "glide_record" && schema) {
                    return { ...properties, schema: $.val(schema) };
                  }
                  return properties;
                })
              });
              attributeRecords.push(attrRecord);
            }
          }
        }
      }
      return { records: attributeRecords, inputNames };
    }
    __name(processAttributes, "processAttributes");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/deployment-processor.js
var require_deployment_processor = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/deployment-processor.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processUIActionDeployment = processUIActionDeployment;
    exports2.generateScriptFromInputs = generateScriptFromInputs;
    exports2.generateInputAssignment = generateInputAssignment;
    var attribute_processor_1 = require_attribute_processor();
    async function processUIActionDeployment(uiActionConfig, skillRecord, capabilityRecord, promptConfig, arg, factory) {
      const skillId = skillRecord.getId().getValue();
      const skillName = skillRecord.get("name")?.getValue() || "Skill";
      const capabilityId = capabilityRecord.getId().getValue();
      const tableName = extractTableName(uiActionConfig);
      const defaultScript = generateUIActionScript(skillId, capabilityId, promptConfig);
      const defaultCondition = `new sn_nowassist_admin.NowAssistSkillConfig().isSkillConfigurationActive('${skillId}')`;
      const uiActionRecord = await factory.createRecord({
        source: uiActionConfig,
        table: "sys_ui_action",
        explicitId: uiActionConfig.asObject().get("$id"),
        properties: uiActionConfig.transform(({ $ }) => ({
          name: $.def(skillName),
          table: $,
          script: $.def(defaultScript).toCdata(),
          condition: $.def(defaultCondition),
          active: $.def(false),
          form_button: $.val(true),
          form_button_v2: $.val(true),
          show_insert: $.val(true),
          show_update: $.val(true)
        }))
      });
      const uxFormActionRecord = await factory.createRecord({
        source: uiActionConfig,
        table: "sys_ux_form_action",
        properties: arg.transform(({ $ }) => ({
          name: $.val(skillName),
          action_type: $.val("ui_action"),
          table: $.val(tableName),
          ui_action: $.val(uiActionRecord.getId()),
          active: $.val(true)
        }))
      });
      return {
        uiActionRecord,
        uxFormActionRecord
      };
    }
    __name(processUIActionDeployment, "processUIActionDeployment");
    function extractTableName(uiActionConfig) {
      const configObj = uiActionConfig.asObject();
      if (configObj.has("table")) {
        const tableValue = configObj.get("table");
        if (tableValue && tableValue.asString) {
          return tableValue.asString()?.getValue() || "";
        }
      }
      if (configObj.has("tableName")) {
        const tableNameValue = configObj.get("tableName");
        if (tableNameValue && tableNameValue.asString) {
          return tableNameValue.asString()?.getValue() || "";
        }
      }
      return "";
    }
    __name(extractTableName, "extractTableName");
    function generateUIActionScript(skillId, capabilityId, promptConfig) {
      let inputs = [];
      if (promptConfig && promptConfig.has && promptConfig.has("inputs")) {
        const inputsArg = promptConfig.get("inputs");
        if (inputsArg && inputsArg.isArray && inputsArg.isArray()) {
          const inputsArray = inputsArg.asArray();
          inputs = inputsArray.map((inputArg) => {
            if (inputArg.isObject()) {
              const inputObj = inputArg.asObject();
              return {
                name: inputObj.has("name") ? inputObj.get("name")?.asString()?.getValue() : "",
                dataType: inputObj.has("dataType") ? inputObj.get("dataType")?.asString()?.getValue() : "string",
                mandatory: inputObj.has("mandatory") ? inputObj.get("mandatory")?.asBoolean()?.getValue() : false,
                description: inputObj.has("description") ? inputObj.get("description")?.asString()?.getValue() : "",
                tableName: inputObj.has("tableName") ? inputObj.get("tableName")?.asString()?.getValue() : ""
              };
            }
            return { name: "", dataType: "string", mandatory: false, description: "", tableName: "" };
          });
        }
      }
      return generateScriptFromInputs(skillId, capabilityId, inputs);
    }
    __name(generateUIActionScript, "generateUIActionScript");
    function generateScriptFromInputs(skillId, capabilityId, inputs) {
      const inputAssignments = inputs.filter((input) => input.name).map((input) => {
        const inputName = (0, attribute_processor_1.generateInternalName)(input.name);
        const dataType = input.dataType || "string";
        const tableName = input.tableName || "";
        return generateInputAssignment(inputName, dataType, tableName);
      }).join("\n");
      const inputSection = inputAssignments ? `
${inputAssignments}` : "";
      return `var inputsPayload = {};${inputSection}

var capabilityId = '${capabilityId}'; 
var request = {
	executionRequests: [{
		payload: inputsPayload,
		capabilityId: capabilityId,
		meta: {
			skillConfigId: '${skillId}'
		}
	}], 
	mode: 'sync' 
};

try {
	var response = sn_one_extend.OneExtendUtil.executeSecure(request) || {}; 
	var skillResponse = ((response["capabilities"] || {})[capabilityId] || {})["response"]; 
	gs.addInfoMessage(JSON.stringify(skillResponse));
} catch(e) {
	gs.error(e);
	gs.addErrorMessage('Something went wrong while executing skill.');
}
action.setRedirectURL(current);`;
    }
    __name(generateScriptFromInputs, "generateScriptFromInputs");
    function generateInputAssignment(inputName, dataType, tableName) {
      let assignment = `// ${inputName} is a input of type: ${dataType}
`;
      if (dataType === "glide_record") {
        assignment += `inputsPayload['${inputName}'] = {
`;
        assignment += `  tableName: '${tableName}',
`;
        assignment += `  sysId: current.getValue('sys_id'),
`;
        assignment += `  queryString: ''
`;
        assignment += `};`;
      } else {
        assignment += `inputsPayload['${inputName}'] = current.getValue('${inputName}');`;
      }
      return assignment;
    }
    __name(generateInputAssignment, "generateInputAssignment");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/skill-processor.js
var require_skill_processor = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/skill-processor.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processSkill = processSkill;
    exports2.processAttributes = processAttributes;
    exports2.processDeploymentSettings = processDeploymentSettings;
    exports2.processProviderArray = processProviderArray;
    exports2.processProviderConfig = processProviderConfig;
    var defaults_1 = require_defaults();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var security_processor_1 = require_security_processor();
    var deployment_processor_1 = require_deployment_processor();
    var tool_processor_1 = require_tool_processor();
    function extractPromptValue(versionObj) {
      if (!versionObj.has("prompt")) {
        return "";
      }
      const promptShape = versionObj.get("prompt");
      if (promptShape?.isString()) {
        return promptShape.asString().getValue();
      }
      if (promptShape instanceof sdk_build_plugins_1.ArrowFunctionShape) {
        const statements = promptShape.getStatements();
        const returnVal = promptShape.getReturnValue();
        const candidates = returnVal ? [...statements, returnVal] : statements;
        for (const candidate of candidates) {
          if (candidate instanceof sdk_build_core_1.TemplateExpressionShape) {
            return convertTemplateToMustache(candidate);
          }
        }
        const singleExpr = statements[0] ?? returnVal;
        if (singleExpr?.isString()) {
          return singleExpr.asString().getValue();
        }
      }
      try {
        return promptShape?.asString().getValue() ?? "";
      } catch {
        return "";
      }
    }
    __name(extractPromptValue, "extractPromptValue");
    function convertTemplateToMustache(template) {
      let result = template.getLiteralText();
      for (const span of template.getSpans()) {
        const expression = span.getExpression();
        let replacement = "";
        if (expression instanceof sdk_build_core_1.PropertyAccessShape) {
          const elements = expression.getElements();
          const elementNames = elements.map((e) => e.getName());
          if (elementNames.length >= 4 && elementNames[1] === "tool") {
            const toolName = elementNames[2];
            const outputField = elementNames.slice(3).join(".");
            replacement = `{{${toolName}.${outputField}}}`;
          } else if (elementNames.length >= 3 && elementNames[1] === "input") {
            const fieldName = elementNames[2];
            const normalized = (0, tool_processor_1.normalizeInputFieldName)(fieldName);
            if (elementNames.length === 3) {
              replacement = `{{${normalized}}}`;
            } else {
              const rest = elementNames.slice(3).join(".");
              replacement = `{{${normalized}.${rest}}}`;
            }
          } else {
            replacement = expression.getCode();
          }
        } else if (expression instanceof sdk_build_core_1.ElementAccessExpressionShape) {
          const callee = expression.getCallee();
          const arg = expression.getArg();
          if (callee.endsWith(".input") && arg.isString()) {
            const fieldName = arg.asString().getValue();
            replacement = `{{${(0, tool_processor_1.normalizeInputFieldName)(fieldName)}}}`;
          } else if (callee.includes(".input.")) {
            const match = callee.match(/\.input\.(.+)$/);
            if (match && match[1]) {
              const chain = match[1];
              if (arg.isString()) {
                replacement = `{{${(0, tool_processor_1.inputChainToTemplate)(chain)}.${arg.asString().getValue()}}}`;
              } else {
                const argCode = arg.getCode();
                replacement = `{{${(0, tool_processor_1.inputChainToTemplate)(chain)}.[${argCode}]}}`;
              }
            } else {
              replacement = expression.getCode();
            }
          } else {
            replacement = expression.getCode();
          }
        } else {
          const code = expression.getCode();
          const inputMatch = code?.match(/^[a-zA-Z_$]+\.input\.(.+)$/);
          if (inputMatch && inputMatch[1]) {
            replacement = `{{${(0, tool_processor_1.inputChainToTemplate)(inputMatch[1])}}}`;
          } else {
            replacement = code || "";
          }
        }
        result += replacement + span.getLiteralText();
      }
      return result;
    }
    __name(convertTemplateToMustache, "convertTemplateToMustache");
    function validateName(str, diagnostics, source, nameType = "Name") {
      const regex = /^[a-zA-Z0-9 ]+$/;
      if (!regex.test(str)) {
        const invalidChars = str.match(/[^a-zA-Z0-9 ]/g);
        const invalidCharsStr = invalidChars ? [...new Set(invalidChars)].join(", ") : "";
        diagnostics.error(source, `${nameType} '${str}' contains invalid character(s): ${invalidCharsStr}. ${nameType}s must contain only letters, numbers, and spaces. Please remove special characters, underscores, and punctuation.`);
      }
      return str;
    }
    __name(validateName, "validateName");
    function generateInternalName(displayName) {
      return displayName.toLowerCase().trim().replace(/\s+/g, "_");
    }
    __name(generateInternalName, "generateInternalName");
    var { SN_NOWASSIST_SKILL_CONFIG, SN_NOWASSIST_SKILL_CONFIG_STATUS, SYS_ONE_EXTEND_CAPABILITY, SYS_ONE_EXTEND_CAPABILITY_DEFINITION, SYS_GENERATIVE_AI_CONFIG, SYS_GENERATIVE_AI_PROMPT_CONFIG, SYS_GENERATIVE_AI_VALIDATOR, SYS_GENERATIVE_AI_REQUEST_VALIDATOR, SYS_GENERATIVE_AI_RESPONSE_VALIDATOR, SYS_GEN_AI_FEATURE_MAPPING, SYS_GEN_AI_STRATEGY_MAPPING, SYS_ONE_EXTEND_DEFINITION_CONFIG, SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, SN_NOWASSIST_SKILL_CONFIG_VAR_SET, SYS_GEN_AI_SKILL, SYS_GEN_AI_SKILL_APPLICABILITY, SYS_GEN_AI_SKILL_CONFIG, SYS_VARIABLE_VALUE } = defaults_1.TABLE_NAMES;
    async function processSkill(callExpression, definitionArg, promptConfigArg, context) {
      const { factory, diagnostics, logger } = context;
      const $id = definitionArg.get("$id");
      const name = definitionArg.get("name")?.asString().getValue();
      const description = definitionArg.has("description") ? definitionArg.get("description")?.asString().getValue() : void 0;
      const state = definitionArg.has("state") ? definitionArg.get("state")?.asString().getValue() : void 0;
      if (!name || name.trim() === "") {
        diagnostics.error(definitionArg, "NowAssistSkillConfig requires a name");
        return { success: false };
      } else if (name.length > defaults_1.SKILL_NAME_MAX_LENGTH) {
        diagnostics.error(definitionArg, `Skill name must not exceed ${defaults_1.SKILL_NAME_MAX_LENGTH} characters (current length: ${name.length})`);
        return { success: false };
      }
      validateName(name, diagnostics, definitionArg, "Skill name");
      const providersData = [];
      const allPromptNames = /* @__PURE__ */ new Set();
      const providers = promptConfigArg.get("providers").asArray().getElements();
      if (providers.length === 0) {
        diagnostics.error(promptConfigArg, "providers array cannot be empty");
        return { success: false };
      }
      let defaultProviderName = promptConfigArg.has("defaultProvider") ? promptConfigArg.get("defaultProvider")?.asString().getValue() : void 0;
      for (let providerIndex = 0; providerIndex < providers.length; providerIndex++) {
        const providerObj = providers[providerIndex].asObject();
        const providerName = providerObj.get("provider")?.asString().getValue() || "Now LLM Service";
        let flowId;
        let flowType = "sys_hub_flow";
        if (providerObj.has("providerAPI")) {
          const providerAPIValue = providerObj.get("providerAPI");
          if (providerAPIValue?.isObject()) {
            const apiObj = providerAPIValue.asObject();
            flowType = apiObj.get("type")?.asString().getValue() || "sys_hub_flow";
            flowId = apiObj.get("id")?.asString().getValue() || defaults_1.NOW_LLM_GENERIC_PROVIDER_API;
          } else if (providerAPIValue?.isString()) {
            flowId = providerAPIValue.asString().getValue() || defaults_1.NOW_LLM_GENERIC_PROVIDER_API;
          } else {
            flowId = defaults_1.NOW_LLM_GENERIC_PROVIDER_API;
          }
        } else {
          const mapping = defaults_1.PROVIDER_MAPPINGS[providerName];
          if (mapping) {
            flowId = mapping.flow_id;
            flowType = mapping.flow_type;
          } else {
            diagnostics.error(providerObj, `Unknown provider "${providerName}". Please either:
1. Use a known provider: ${Object.keys(defaults_1.PROVIDER_MAPPINGS).join(", ")}
2. Provide explicit providerAPI: { type: 'sys_hub_flow', id: 'your_flow_sys_id' }`);
            return { success: false };
          }
        }
        if (!defaultProviderName && providerIndex === 0) {
          defaultProviderName = providerName;
        }
        const prompts = providerObj.get("prompts").asArray().getElements();
        const providerPrompts = [];
        let providerDefaultPromptName = providerObj.has("defaultPrompt") ? providerObj.get("defaultPrompt")?.asString().getValue() : void 0;
        const providerDefaultPromptVersion = providerObj.has("defaultPromptVersion") ? providerObj.get("defaultPromptVersion")?.asNumber().getValue() : void 0;
        for (let promptIndex = 0; promptIndex < prompts.length; promptIndex++) {
          const promptObj = prompts[promptIndex].asObject();
          const promptItemName = promptObj.get("name")?.asString().getValue() || "";
          validateName(promptItemName, diagnostics, promptObj, "Prompt name");
          if (allPromptNames.has(promptItemName)) {
            diagnostics.error(promptObj, `Duplicate prompt name '${promptItemName}'. Each prompt must have a unique name across the entire skill.`);
            return { success: false };
          }
          allPromptNames.add(promptItemName);
          if (!providerDefaultPromptName && promptIndex === 0) {
            providerDefaultPromptName = promptItemName;
          }
          const versions = promptObj.get("versions").asArray().getElements();
          if (versions.length === 0) {
            diagnostics.error(promptObj, `Prompt '${promptItemName}' must have at least one version in the versions array.`);
            return { success: false };
          }
          const versionNumbers = /* @__PURE__ */ new Set();
          for (const v of versions) {
            const versionObj = v.asObject();
            const versionNumber = versionObj.has("version") ? versionObj.get("version")?.asNumber().getValue() || 1 : 1;
            if (!Number.isInteger(versionNumber) || versionNumber < 1) {
              diagnostics.error(versionObj, `Version '${versionNumber}' of prompt '${promptItemName}' must be a positive integer (e.g., 1, 2, 3).`);
              return { success: false };
            }
            if (versionNumbers.has(versionNumber)) {
              diagnostics.error(versionObj, `Duplicate version '${versionNumber}' found in prompt '${promptItemName}'. Each version must have a unique version number within the same prompt.`);
              return { success: false };
            }
            versionNumbers.add(versionNumber);
          }
          if (versions.length > 1) {
            const versionData = versions.map((v, index) => {
              const versionObj = v.asObject();
              const versionNumber = versionObj.has("version") ? versionObj.get("version")?.asNumber().getValue() || 1 : 1;
              const promptState = versionObj.has("promptState") ? versionObj.get("promptState")?.asString().getValue() || "draft" : "draft";
              return { versionObj, versionNumber, promptState, index };
            });
            const sortedVersionsDesc = [...versionData].sort((a, b) => {
              return b.versionNumber - a.versionNumber;
            });
            const sortedVersions = [...versionData].sort((a, b) => {
              return a.versionNumber - b.versionNumber;
            });
            const highestVersion = sortedVersionsDesc[0];
            const lowerVersions = sortedVersionsDesc.slice(1);
            for (const versionInfo of lowerVersions) {
              if (versionInfo.promptState === "draft") {
                diagnostics.error(versionInfo.versionObj, `Version '${versionInfo.versionNumber}' of prompt '${promptItemName}' cannot be 'draft'. Only the highest version (${highestVersion.versionNumber}) can be 'draft'. Lower versions must be 'published' or 'archived'.`);
                return { success: false };
              }
            }
            for (const versionInfo of lowerVersions) {
              if (versionInfo.promptState !== "published" && versionInfo.promptState !== "archived") {
                diagnostics.error(versionInfo.versionObj, `Version '${versionInfo.versionNumber}' of prompt '${promptItemName}' must be 'published' or 'archived'. Lower versions should be 'published' or 'archived', only the highest version (${highestVersion.versionNumber}) can be 'draft' or 'finalized'.`);
                return { success: false };
              }
            }
            for (let i = 0; i < sortedVersions.length - 1; i++) {
              const currentVersion = sortedVersions[i];
              const nextVersion = sortedVersions[i + 1];
              if (currentVersion.promptState === "published" && nextVersion.promptState === "archived") {
                diagnostics.error(currentVersion.versionObj, `Version '${currentVersion.versionNumber}' of prompt '${promptItemName}' is 'published', but the next higher version (${nextVersion.versionNumber}) is 'archived'. A published version cannot be followed by an archived version. Valid pattern: archived -> ... -> archived -> published.`);
                return { success: false };
              }
            }
            const hasArchivedVersions = sortedVersions.some((v) => v.promptState === "archived");
            if (hasArchivedVersions) {
              let lastArchivedIndex = -1;
              for (let i = sortedVersions.length - 1; i >= 0; i--) {
                if (sortedVersions[i].promptState === "archived") {
                  lastArchivedIndex = i;
                  break;
                }
              }
              let hasPublishedAfterArchived = false;
              for (let i = lastArchivedIndex + 1; i < sortedVersions.length; i++) {
                if (sortedVersions[i].promptState === "published") {
                  hasPublishedAfterArchived = true;
                  break;
                }
              }
              if (!hasPublishedAfterArchived) {
                const lastArchivedVersion = sortedVersions[lastArchivedIndex];
                diagnostics.error(lastArchivedVersion.versionObj, `Prompt '${promptItemName}' has archived versions, but no published version after the last archived version (${lastArchivedVersion.versionNumber}). At least one version after all archived versions must be 'published'. Valid pattern: archived -> ... -> archived -> published.`);
                return { success: false };
              }
            }
          }
          providerPrompts.push({ promptObj, versions: versions.map((v) => v.asObject()) });
        }
        const providerPromptNames = providerPrompts.map((p) => p.promptObj.get("name")?.asString().getValue());
        if (providerDefaultPromptName && !providerPromptNames.includes(providerDefaultPromptName)) {
          diagnostics.error(providerObj, `defaultPrompt '${providerDefaultPromptName}' does not match any prompt name in provider '${providerName}'. Available prompts: ${providerPromptNames.join(", ")}`);
          return { success: false };
        }
        if (providerDefaultPromptVersion) {
          const defaultPromptData = providerPrompts.find((p) => p.promptObj.get("name")?.asString().getValue() === providerDefaultPromptName);
          if (!defaultPromptData) {
            diagnostics.error(providerObj, `defaultPromptVersion '${providerDefaultPromptVersion}' is specified, but the default prompt '${providerDefaultPromptName}' was not found. Available prompts: ${providerPrompts.map((p) => p.promptObj.get("name")?.asString().getValue()).join(", ")}`);
            return { success: false };
          }
          const defaultVersionObj = defaultPromptData.versions.find((v) => {
            const versionNumber = v.has("version") ? v.get("version")?.asNumber().getValue() || 1 : 1;
            return versionNumber === providerDefaultPromptVersion;
          });
          if (!defaultVersionObj) {
            diagnostics.error(providerObj, `defaultPromptVersion '${providerDefaultPromptVersion}' does not match any version in the default prompt '${providerDefaultPromptName}'. Available versions: ${defaultPromptData.versions.map((v) => v.has("version") ? v.get("version")?.asNumber().getValue() || 1 : 1).join(", ")}`);
            return { success: false };
          }
          const defaultVersionState = defaultVersionObj.has("promptState") ? defaultVersionObj.get("promptState")?.asString().getValue() || "draft" : "draft";
          if (defaultVersionState === "archived") {
            diagnostics.error(defaultVersionObj, `defaultPromptVersion '${providerDefaultPromptVersion}' of prompt '${providerDefaultPromptName}' has state 'archived'. Archived versions cannot be set as default.`);
            return { success: false };
          }
          if (defaultVersionState === "draft" && defaultPromptData.versions.length > 1) {
            diagnostics.error(defaultVersionObj, `defaultPromptVersion '${providerDefaultPromptVersion}' of prompt '${providerDefaultPromptName}' has state 'draft'. Draft versions cannot be set as default when there are multiple versions. Please use a 'published' or 'finalized' version as default.`);
            return { success: false };
          }
        }
        providersData.push({
          providerName,
          flowId,
          flowType,
          providerShape: providerObj,
          prompts: providerPrompts,
          defaultPromptName: providerDefaultPromptName,
          ...providerDefaultPromptVersion !== void 0 ? { defaultPromptVersion: providerDefaultPromptVersion } : {},
          isDefaultProvider: providerName === defaultProviderName
        });
      }
      const providerNames = providersData.map((p) => p.providerName);
      if (defaultProviderName && !providerNames.includes(defaultProviderName)) {
        diagnostics.error(promptConfigArg, `defaultProvider '${defaultProviderName}' does not match any provider name. Available providers: ${providerNames.join(", ")}`);
        return { success: false };
      }
      const deploymentSettingsArg = definitionArg.has("deploymentSettings") ? definitionArg.get("deploymentSettings") : void 0;
      const securityControls = definitionArg.has("securityControls") ? definitionArg.get("securityControls") : void 0;
      let enableUIAction = false;
      let uiActionIdShape;
      let uiActionAutoGenConfig = null;
      let enableNAP = false;
      let napRoles = "";
      let napConditionMode = "simple";
      let napConditionScript;
      let enableFlowAction = false;
      let skillFamilyShape;
      if (deploymentSettingsArg?.isObject()) {
        const deployObj = deploymentSettingsArg.asObject();
        if (deployObj.has("skillFamily")) {
          const familyValue = deployObj.get("skillFamily");
          if (familyValue && !familyValue.isUndefined()) {
            if (familyValue.isString()) {
              const strVal = familyValue.asString().getValue();
              if (!(0, sdk_build_core_1.isGUID)(strVal)) {
                diagnostics.error(familyValue, `'skillFamily' must be a valid sys_id (32-character GUID). Received: '${strVal}'`);
              } else {
                skillFamilyShape = familyValue;
              }
            } else {
              skillFamilyShape = familyValue;
            }
          }
        }
        if (deployObj.has("uiAction")) {
          const uiActionConfig = deployObj.get("uiAction");
          if (uiActionConfig && !uiActionConfig.isUndefined()) {
            if (uiActionConfig.isObject() && uiActionConfig.asObject().has("table")) {
              uiActionAutoGenConfig = uiActionConfig;
              enableUIAction = true;
            } else {
              enableUIAction = true;
              uiActionIdShape = uiActionConfig;
            }
          }
        }
        if (deployObj.has("nowAssistPanel")) {
          const napConfig = deployObj.get("nowAssistPanel");
          if (napConfig?.isObject()) {
            const napObj = napConfig.asObject();
            if (napObj.has("enabled")) {
              const enabledValue = napObj.get("enabled")?.asBoolean()?.getValue();
              enableNAP = enabledValue === true;
            }
            if (napObj.has("conditionMode")) {
              const conditionModeValue = napObj.get("conditionMode")?.asString()?.getValue();
              if (conditionModeValue === "simple" || conditionModeValue === "advanced") {
                napConditionMode = conditionModeValue;
              }
            }
            if (enableNAP && napObj.has("roles")) {
              const rolesField = napObj.get("roles");
              if (rolesField?.isArray()) {
                const rolesArray = rolesField.asArray().getElements();
                const roleStrings = [];
                for (const role of rolesArray) {
                  if (role.isString()) {
                    roleStrings.push(role.asString().getValue());
                  } else if (role.isObject()) {
                    const roleObj = role.asObject();
                    if (roleObj.has("name")) {
                      const nameVal = roleObj.get("name")?.asString()?.getValue();
                      if (nameVal) {
                        roleStrings.push(nameVal);
                      }
                    } else if (roleObj.has("sys_user_role")) {
                      const sysUserRole = roleObj.get("sys_user_role")?.asString()?.getValue();
                      if (sysUserRole) {
                        roleStrings.push(sysUserRole);
                      }
                    }
                  }
                }
                const validRoles = roleStrings.filter((r) => r.trim().length > 0);
                for (const roleValue of validRoles) {
                  if (defaults_1.RESTRICTED_ROLE_NAMES.includes(roleValue)) {
                    diagnostics.error(rolesField, `The '${roleValue}' role cannot be used for Now Assist Panel roles. This role has special system-level privileges and is restricted.`);
                  }
                  if (defaults_1.RESTRICTED_ROLE_SYS_IDS.includes(roleValue)) {
                    diagnostics.error(rolesField, `The 'maint' role (sys_id: ${roleValue}) cannot be used for Now Assist Panel roles. This role has special system-level privileges and is restricted.`);
                  }
                }
                if (validRoles.length > 0) {
                  napRoles = validRoles.join(",");
                  if (napConditionMode !== "advanced" && !validRoles.includes("now_assist_panel_user")) {
                    diagnostics.warn(rolesField, `'now_assist_panel_user' role is recommended for Now Assist Panel access. Without this role, users may not be able to access the skill in the panel.`);
                  }
                }
              }
            }
            if (napObj.has("conditionScript")) {
              napConditionScript = napObj.get("conditionScript")?.asString()?.getValue();
            }
            if (enableNAP && napRoles === "" && napConditionMode !== "advanced") {
              napRoles = defaults_1.SYS_GEN_AI_SKILL_APPLICABILITY_CREATE_ONLY_DEFAULTS.roles;
            }
          }
        }
        if (deployObj.has("flowAction")) {
          const flowActionValue = deployObj.get("flowAction")?.asBoolean()?.getValue();
          enableFlowAction = flowActionValue === true;
        }
      }
      const capabilityRecord = await factory.createRecord({
        source: {
          path: callExpression.getOriginalFilePath(),
          content: ""
        },
        table: SYS_ONE_EXTEND_CAPABILITY,
        explicitId: $id,
        properties: definitionArg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(SYS_ONE_EXTEND_CAPABILITY, $),
          description: $.from("description").def(""),
          name: $.from("name"),
          short_description: $.from("shortDescription").map((v) => v?.toString() || `${name} Skill`)
        }))
      });
      let napApplicabilityRecordId = null;
      let genAISkillRecord = null;
      let napApplicabilityRecord = null;
      if (enableNAP) {
        genAISkillRecord = await factory.createRecord({
          source: deploymentSettingsArg,
          table: SYS_GEN_AI_SKILL,
          properties: definitionArg.transform(({ $ }) => ({
            skill_name: $.from("name"),
            skill_document: $.val(capabilityRecord.getId()),
            skill_table: $.val(defaults_1.TABLE_NAMES.SYS_ONE_EXTEND_CAPABILITY)
          }))
        });
        napApplicabilityRecord = await factory.createRecord({
          source: deploymentSettingsArg,
          table: SYS_GEN_AI_SKILL_APPLICABILITY,
          properties: definitionArg.transform(({ $ }) => ({
            ...(0, skill_plugin_helpers_1.applyDefaults)(SYS_GEN_AI_SKILL_APPLICABILITY, $),
            skill_id: $.val(genAISkillRecord.getId()),
            roles: $.val(napRoles),
            condition_mode: $.val(napConditionMode),
            condition_script: $.val(napConditionScript || defaults_1.DEFAULT_CONDITION_SCRIPT).toCdata()
          }))
        });
        napApplicabilityRecordId = napApplicabilityRecord.getId();
      }
      let skillRecord = await factory.createRecord({
        source: callExpression,
        table: SN_NOWASSIST_SKILL_CONFIG,
        properties: definitionArg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(SN_NOWASSIST_SKILL_CONFIG, $),
          name: $.from("name"),
          description: $.from("description").def(""),
          skill_table: $.val(SYS_ONE_EXTEND_CAPABILITY),
          skill_id: $.val(capabilityRecord.getId().getValue()),
          state: $.from("state").map((v) => v?.ifString()?.getValue() === "published" ? "1" : "0"),
          nap_skill_applicability: $.val(napApplicabilityRecordId || ""),
          // Override skill_family if user provided one in deploymentSettings;
          // otherwise the TABLE_DEFAULTS value ("Others") is used.
          ...skillFamilyShape && { skill_family: $.val(skillFamilyShape) }
        }))
      });
      const skillRecordSysID = skillRecord.getId().getValue();
      const skillName = skillRecord.get("name")?.toString().getValue();
      const sanitizedName = (0, skill_plugin_helpers_1.sanitizeSkillName)(skillName);
      const internalName = `${sanitizedName}.${skillRecordSysID}`;
      skillRecord = skillRecord.merge({
        internal_name: internalName
      });
      const skillConfigStatusRecord = await factory.createRecord({
        source: {
          path: callExpression.getOriginalFilePath(),
          content: ""
        },
        table: SN_NOWASSIST_SKILL_CONFIG_STATUS,
        properties: definitionArg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(SN_NOWASSIST_SKILL_CONFIG_STATUS, $),
          skill_config: $.val(skillRecord.getId())
        }))
      });
      const promptRecords = [];
      let promptOrder = 0;
      for (const providerData of providersData) {
        const { providerName, flowId, flowType, providerShape, prompts, defaultPromptName, defaultPromptVersion, isDefaultProvider } = providerData;
        const capabilityDefinitionRecord2 = await factory.createRecord({
          source: providerShape,
          table: SYS_ONE_EXTEND_CAPABILITY_DEFINITION,
          properties: definitionArg.transform(({ $ }) => ({
            ...(0, skill_plugin_helpers_1.applyDefaults)(SYS_ONE_EXTEND_CAPABILITY_DEFINITION, $),
            name: $.val(`${name} (${providerName})`),
            capability: $.val(capabilityRecord.getId()),
            api_type: $.val(flowType),
            api: $.val(flowId),
            description: $.val(`Definition for ${name}`)
          }))
        });
        const definitionConfigRecord = await factory.createRecord({
          source: providerShape,
          table: SYS_ONE_EXTEND_DEFINITION_CONFIG,
          properties: definitionArg.transform(({ $ }) => ({
            ...(0, skill_plugin_helpers_1.applyDefaults)(SYS_ONE_EXTEND_DEFINITION_CONFIG, $),
            capability: $.val(capabilityRecord.getId()),
            definition: $.val(capabilityDefinitionRecord2.getId()),
            default: $.val(isDefaultProvider)
          }))
        });
        for (const promptData of prompts) {
          const { promptObj, versions } = promptData;
          const promptItemName = promptObj.has("name") ? promptObj.get("name")?.asString().getValue() || "" : "";
          const defaultVersionSpec = promptItemName === defaultPromptName ? defaultPromptVersion : void 0;
          const sortedVersions = [...versions].sort((a, b) => {
            const versionA = a.has("version") ? a.get("version")?.asNumber().getValue() || 1 : 1;
            const versionB = b.has("version") ? b.get("version")?.asNumber().getValue() || 1 : 1;
            return versionA - versionB;
          });
          const versionStates = sortedVersions.map((v) => {
            const state2 = v.has("promptState") ? v.get("promptState")?.asString().getValue() || "draft" : "draft";
            return state2;
          });
          let firstPublishedIndex = -1;
          for (let i = 0; i < versionStates.length; i++) {
            if (versionStates[i] === "published") {
              firstPublishedIndex = i;
              break;
            }
          }
          let lastPublishedIndex = -1;
          for (let i = versionStates.length - 1; i >= 0; i--) {
            if (versionStates[i] === "published") {
              lastPublishedIndex = i;
              break;
            }
          }
          let lastPublishedVersionRecordId = null;
          let defaultVersionIndex = 0;
          if (defaultVersionSpec) {
            const matchIndex = sortedVersions.findIndex((v) => {
              const ver = v.has("version") ? v.get("version")?.asNumber().getValue() : 1;
              return ver === defaultVersionSpec;
            });
            if (matchIndex !== -1) {
              defaultVersionIndex = matchIndex;
            }
          } else if (firstPublishedIndex !== -1) {
            defaultVersionIndex = firstPublishedIndex;
          }
          for (let versionIndex = 0; versionIndex < sortedVersions.length; versionIndex++) {
            const versionObj = sortedVersions[versionIndex];
            const versionExplicitId = versionObj.get("$id");
            const versionFilterConditionShape = versionObj.has("filterCondition") ? versionObj.get("filterCondition") : void 0;
            let filterPropertiesJson;
            if (versionFilterConditionShape && versionFilterConditionShape.isObject()) {
              const filterConditionValue = versionFilterConditionShape.getValue();
              if (filterConditionValue && typeof filterConditionValue === "object") {
                const inputAttributeMap = /* @__PURE__ */ new Map();
                const normalizedToDisplay = /* @__PURE__ */ new Map();
                if (definitionArg.has("inputs")) {
                  const inputs = definitionArg.get("inputs");
                  const inputElements = inputs && inputs.isArray() ? inputs.asArray().getElements() : [];
                  for (const inputAttr of inputElements) {
                    if (inputAttr.isObject()) {
                      const displayName = inputAttr.has("name") ? inputAttr.get("name")?.asString().getValue() : "";
                      const dataType = inputAttr.has("dataType") ? inputAttr.get("dataType")?.asString().getValue() : "string";
                      const internalName2 = displayName.toLowerCase().replace(/\s+/g, "_");
                      if (displayName) {
                        inputAttributeMap.set(displayName, {
                          dataType: dataType || "string",
                          internalName: internalName2
                        });
                        normalizedToDisplay.set(internalName2, displayName);
                      }
                    }
                  }
                }
                const platformFilterProperties = {};
                for (const [key, value] of Object.entries(filterConditionValue)) {
                  const isDotNotation = key.includes(".");
                  const baseName = isDotNotation ? key.split(".")[0] : null;
                  const displayName = isDotNotation ? normalizedToDisplay.get(baseName) : key;
                  const inputInfo = displayName ? inputAttributeMap.get(displayName) : void 0;
                  if (isDotNotation && !normalizedToDisplay.has(baseName)) {
                    diagnostics.error(versionObj, `filterCondition attribute '${key}' base name '${baseName}' does not match any input attribute. Available normalized input names: [${Array.from(normalizedToDisplay.keys()).join(", ")}]. Dot-notation keys must start with a normalized input name (lowercase, underscores for spaces).`);
                    continue;
                  }
                  if (!isDotNotation && !inputAttributeMap.has(key)) {
                    diagnostics.error(versionObj, `filterCondition attribute '${key}' does not match any input attribute. Available input attributes: [${Array.from(inputAttributeMap.keys()).join(", ")}]. Please ensure the filterCondition keys match the input attribute names exactly.`);
                    continue;
                  }
                  if (inputInfo && !defaults_1.SUPPORTED_FILTER_CONDITION_DATATYPES.includes(inputInfo.dataType)) {
                    diagnostics.error(versionObj, `filterCondition attribute '${key}' has unsupported data type '${inputInfo.dataType}'. Supported data types for filterCondition are: [${defaults_1.SUPPORTED_FILTER_CONDITION_DATATYPES.join(", ")}].`);
                    continue;
                  }
                  const platformKey = isDotNotation ? key : inputInfo.internalName;
                  platformFilterProperties[`{{${platformKey}}}`] = value;
                }
                if (Object.keys(platformFilterProperties).length > 0) {
                  filterPropertiesJson = JSON.stringify(platformFilterProperties);
                }
              }
            }
            const promptVersion = versionObj.has("version") ? versionObj.get("version")?.asNumber().getValue() : 1;
            const model = versionObj.has("model") ? versionObj.get("model")?.asString().getValue() : "llm_generic_small";
            let temperature = versionObj.has("temperature") ? versionObj.get("temperature")?.asNumber()?.getValue() ?? 0.2 : 0.2;
            if (temperature < 0 || temperature > 1) {
              temperature = 0.2;
            }
            const maxTokens = versionObj.has("maxTokens") ? versionObj.get("maxTokens")?.asNumber()?.getValue() ?? 500 : 500;
            if (maxTokens < 0 || maxTokens > defaults_1.MAX_TOKENS_UPPER_BOUND) {
              diagnostics.error(versionObj, `max_tokens value '${maxTokens}' is out of range. max_tokens must be between 0 and ${defaults_1.MAX_TOKENS_UPPER_BOUND}. Please adjust the value.`);
            }
            const prompt = extractPromptValue(versionObj);
            let promptState = versionObj.has("promptState") ? versionObj.get("promptState")?.asString().getValue() || "draft" : "draft";
            if (promptState === "published" && lastPublishedIndex !== -1 && versionIndex < lastPublishedIndex) {
              promptState = "archived";
            }
            const showInPromptLibrary = promptState === "published";
            const aiConfigRecord = await factory.createRecord({
              source: versionObj,
              table: SYS_GENERATIVE_AI_CONFIG,
              explicitId: versionExplicitId,
              properties: definitionArg.transform(({ $ }) => ({
                ...(0, skill_plugin_helpers_1.applyDefaults)(SYS_GENERATIVE_AI_CONFIG, $),
                name: $.val(promptItemName),
                definition: $.val(capabilityDefinitionRecord2.getId()),
                definition_table: $.val(SYS_ONE_EXTEND_CAPABILITY_DEFINITION),
                capability: $.val(capabilityRecord.getId()),
                model: $.val(model),
                prompt: $.val(prompt),
                max_tokens: $.val(maxTokens.toString()),
                state: $.val(promptState || "draft"),
                active: $.val(promptState === "published"),
                version: $.val(String(promptVersion)),
                show_in_prompt_library: $.val(showInPromptLibrary),
                description: $.val(description || ""),
                ...lastPublishedVersionRecordId ? { parent: $.val(lastPublishedVersionRecordId) } : {}
              }))
            });
            if (promptState === "published") {
              lastPublishedVersionRecordId = aiConfigRecord.getId();
            }
            const isDefaultPrompt = promptItemName === defaultPromptName;
            const isDefaultVersion = versionIndex === defaultVersionIndex;
            const promptConfigRecord = await factory.createRecord({
              source: versionObj,
              table: SYS_GENERATIVE_AI_PROMPT_CONFIG,
              properties: definitionArg.transform(({ $ }) => ({
                ...(0, skill_plugin_helpers_1.applyDefaults)(SYS_GENERATIVE_AI_PROMPT_CONFIG, $),
                ai_config: $.val(aiConfigRecord.getId()),
                definition: $.val(capabilityDefinitionRecord2.getId()),
                filter_type: $.val(filterPropertiesJson ? "capability_attribute" : "default"),
                is_default: $.val(isDefaultPrompt && isDefaultVersion),
                // Always include filter_properties to ensure it gets cleared when filterCondition is removed
                filter_properties: $.val(filterPropertiesJson ?? "")
              }))
            });
            promptRecords.push({
              promptItem: versionObj,
              promptItemName,
              providerName,
              capabilityDefinitionRecord: capabilityDefinitionRecord2,
              aiConfigRecord,
              promptConfigRecord,
              definitionConfigRecord,
              isDefaultPrompt,
              isDefaultProvider
            });
          }
          promptOrder++;
        }
      }
      if (state === "published") {
        const publishedPrompts = promptRecords.filter((record) => {
          const promptState = record.promptItem.has("promptState") ? record.promptItem.get("promptState")?.asString().getValue() : "draft";
          return promptState === "published";
        });
        if (publishedPrompts.length === 0) {
          diagnostics.error(definitionArg, `Cannot publish skill without at least one published prompt. Please ensure at least one prompt has promptState set to 'published'.`);
          return { success: false };
        }
      }
      if (state !== "published") {
        for (const record of promptRecords) {
          const promptState = record.promptItem.has("promptState") ? record.promptItem.get("promptState")?.asString().getValue() : "draft";
          if (promptState === "published") {
            diagnostics.error(definitionArg, `Cannot set prompt '${record.promptItemName}' state to 'published' because the skill state is not 'published'. Please set the skill state to 'published' before publishing any prompts.`);
            return { success: false };
          }
        }
      }
      const capabilityDefinitionRecord = promptRecords[0].capabilityDefinitionRecord;
      const { records: attributeRecords, inputNames } = await processAttributes(definitionArg, capabilityRecord, capabilityDefinitionRecord, definitionArg, factory, diagnostics, callExpression, $id, enableNAP);
      const uiActionRecords = [];
      if (uiActionAutoGenConfig) {
        try {
          const result = await (0, deployment_processor_1.processUIActionDeployment)(uiActionAutoGenConfig, skillRecord, capabilityRecord, definitionArg, definitionArg, factory);
          uiActionIdShape = result.uiActionRecord.getId();
          uiActionRecords.push(result.uiActionRecord, result.uxFormActionRecord);
        } catch (error) {
          diagnostics.error(definitionArg, `UI Action auto-generation error: ${error instanceof Error ? error.message : String(error)}`);
        }
      }
      const varSetRecords = [];
      if (enableUIAction || enableNAP || enableFlowAction) {
        const deployResult = await processDeploymentSettings(
          {
            EnableUIAction: enableUIAction,
            EnableNAP: enableNAP,
            EnableFlowAction: enableFlowAction,
            ...uiActionIdShape !== void 0 && { uiActionIdShape }
            // Only include if defined
          },
          skillRecord,
          deploymentSettingsArg,
          // Use deploymentSettingsArg as source
          definitionArg,
          // Pass definitionArg for transform method
          factory,
          genAISkillRecord
          // Pass the pre-created genAISkillRecord
        );
        varSetRecords.push(...deployResult.records);
      }
      const providerRecords = [];
      const skillSettings = definitionArg.has("skillSettings") ? definitionArg.get("skillSettings") : void 0;
      if (skillSettings?.isObject() && skillSettings.has("providers")) {
        const providers2 = skillSettings.get("providers");
        if (providers2?.isArray()) {
          const providerRecordsResult = await processProviderArray(providers2, promptRecords, definitionArg, factory, diagnostics);
          providerRecords.push(...providerRecordsResult);
        }
      } else if (promptConfigArg.has("providerConfig")) {
        const providerConfig = promptConfigArg.get("providerConfig");
        if (providerConfig?.isObject()) {
          const providerRecordsResult = await processProviderConfig(providerConfig, promptRecords, definitionArg, factory, diagnostics);
          providerRecords.push(...providerRecordsResult);
        }
      }
      const securityRecords = [];
      if (securityControls?.isObject()) {
        try {
          const secRecords = await (0, security_processor_1.processSecurityControls)(
            securityControls,
            name,
            skillRecord.getId().getValue(),
            // Pass skill record sys_id for ACL name and agent reference
            definitionArg,
            factory,
            diagnostics
          );
          securityRecords.push(...secRecords);
        } catch (error) {
          diagnostics.error(definitionArg, `Security controls error: ${error instanceof Error ? error.message : String(error)}`);
        }
      }
      const featureMappingRecord = await factory.createRecord({
        source: {
          path: callExpression.getOriginalFilePath(),
          content: ""
        },
        table: SYS_GEN_AI_FEATURE_MAPPING,
        properties: definitionArg.transform(({ $ }) => ({
          active: $.val(true),
          licensable: $.val(true),
          document: $.val(capabilityRecord.getId()),
          document_table: $.val("sys_one_extend_capability"),
          feature_name: $.from("name")
        }))
      });
      const strategyMappingRecord = await factory.createRecord({
        source: {
          path: callExpression.getOriginalFilePath(),
          content: ""
        },
        table: SYS_GEN_AI_STRATEGY_MAPPING,
        properties: definitionArg.transform(({ $ }) => ({
          active: $.val(true),
          feature: $.val(featureMappingRecord.getId()),
          strategy: $.val("CAPABILITY_EXECUTION")
        }))
      });
      const allPromptRecords = promptRecords.flatMap((pr) => [pr.capabilityDefinitionRecord, pr.aiConfigRecord, pr.promptConfigRecord, pr.definitionConfigRecord].filter((record) => record !== null));
      const napRecords = napApplicabilityRecord ? [napApplicabilityRecord] : [];
      const toolsShape = definitionArg.get("tools");
      const toolRecords = await (0, tool_processor_1.processTools)(toolsShape, capabilityRecord, name, definitionArg, callExpression, factory, diagnostics, inputNames);
      return {
        success: true,
        value: capabilityRecord.with(skillRecord, skillConfigStatusRecord, ...allPromptRecords, ...attributeRecords, featureMappingRecord, strategyMappingRecord, ...uiActionRecords, ...varSetRecords, ...providerRecords, ...securityRecords, ...napRecords, ...toolRecords)
      };
    }
    __name(processSkill, "processSkill");
    async function processAttributes(definitionShape, capabilityRecord, capabilityDefinitionRecord, arg, factory, diagnostics, callExpression, skillExplicitId, enableNAP = false) {
      const attributeRecords = [];
      const napIncompatibleDataTypes = ["simple_array", "json_object", "json_array"];
      const inputNames = /* @__PURE__ */ new Set();
      if (definitionShape.has("inputs")) {
        const inputs = definitionShape.get("inputs");
        const inputElements = inputs.isArray() ? inputs.asArray().getElements() : [];
        if (inputElements.length > 0 || inputs.isArray()) {
          const glideRecordInputs = [];
          for (const inputAttr of inputElements) {
            if (inputAttr.isObject()) {
              const dataType = inputAttr.has("dataType") ? inputAttr.get("dataType")?.asString().getValue() : "string";
              if (dataType === "glide_record") {
                const displayName = inputAttr.has("name") ? inputAttr.get("name")?.asString().getValue() : "unknown";
                glideRecordInputs.push(displayName);
              }
            }
          }
          if (glideRecordInputs.length > 1) {
            diagnostics.error(inputs, `Only one input attribute can have data_type 'glide_record'. Found ${glideRecordInputs.length} glide_record inputs: [${glideRecordInputs.join(", ")}]. The glide_record type is designed to reference a single record. If you need multiple records, consider using 'json_array' or 'simple_array' data types instead.`);
            return { records: [], inputNames };
          }
          const normalizedInputNames = /* @__PURE__ */ new Map();
          for (const inputAttr of inputElements) {
            if (inputAttr.isObject()) {
              const displayName = inputAttr.has("name") ? inputAttr.get("name")?.asString().getValue() : "unknown";
              const normalizedName = (0, tool_processor_1.normalizeInputFieldName)(displayName ?? "unknown");
              if (inputNames.has(displayName)) {
                diagnostics.error(inputAttr, `Duplicate input name '${displayName}'. Each input must have a unique name.`);
                return { records: [], inputNames };
              }
              const existingName = normalizedInputNames.get(normalizedName);
              if (existingName && existingName !== displayName) {
                diagnostics.error(inputAttr, `Input name '${displayName}' conflicts with '${existingName}'. Both normalize to '${normalizedName}' internally. Input names must be unique when normalized to lowercase.`);
                return { records: [], inputNames };
              }
              inputNames.add(displayName);
              normalizedInputNames.set(normalizedName, displayName);
            }
          }
          for (const inputAttr of inputElements) {
            if (inputAttr.isObject()) {
              const displayName = inputAttr.has("name") ? inputAttr.get("name")?.asString().getValue() : "";
              const inputIdShape = inputAttr.has("$id") ? inputAttr.get("$id") : void 0;
              if (!inputIdShape || !(inputIdShape instanceof sdk_build_plugins_1.NowIdShape)) {
                diagnostics.error(inputAttr, `Input attribute '$id' is required and must use Now.ID['key'].`);
                continue;
              }
              const inputExplicitId = inputIdShape;
              if (!displayName || displayName.trim() === "") {
                diagnostics.error(inputAttr, "Attribute name is required and cannot be empty");
                continue;
              }
              const validatedDisplayName = validateName(displayName, diagnostics, inputAttr, "Attribute name");
              const generatedInternalName = generateInternalName(validatedDisplayName);
              const label = displayName;
              const description = inputAttr.has("description") ? inputAttr.get("description")?.asString().getValue() : "";
              let mandatory = false;
              if (inputAttr.has("mandatory")) {
                const mandatoryShape = inputAttr.get("mandatory");
                const mandatoryRawValue = mandatoryShape?.getValue();
                mandatory = mandatoryRawValue === true || mandatoryRawValue === "true" || mandatoryRawValue === "1";
              }
              const dataType = inputAttr.has("dataType") ? inputAttr.get("dataType")?.asString().getValue() : "string";
              if (enableNAP && napIncompatibleDataTypes.includes(dataType || "")) {
                diagnostics.error(inputAttr.has("dataType") ? inputAttr.get("dataType") : inputAttr, `Input attribute '${displayName}': The dataType '${dataType}' is not compatible with Now Assist Panel deployment. The following data types are NOT supported with nowAssistPanel: ${napIncompatibleDataTypes.join(", ")}. Please change the dataType or remove the nowAssistPanel deployment setting.`);
                return { records: [], inputNames };
              }
              const testValues = inputAttr.has("testValues") ? inputAttr.get("testValues")?.asString().getValue() : "";
              if (testValues && testValues.trim() !== "") {
                if (dataType === "numeric") {
                  const numValue = Number(testValues);
                  if (isNaN(numValue)) {
                    diagnostics.error(inputAttr.has("testValues") ? inputAttr.get("testValues") : inputAttr, `Input attribute '${displayName}': testValues '${testValues}' is not valid for dataType 'numeric'. Please provide a valid number.`);
                    continue;
                  }
                } else if (dataType === "boolean") {
                  const lowerVal = testValues.toLowerCase();
                  const validBooleans = ["true", "false", "1", "0"];
                  if (!validBooleans.includes(lowerVal)) {
                    diagnostics.error(inputAttr.has("testValues") ? inputAttr.get("testValues") : inputAttr, `Input attribute '${displayName}': testValues '${testValues}' is not valid for dataType 'boolean'. Please provide 'true', 'false', '1', or '0'.`);
                    continue;
                  }
                } else if (dataType === "json_object" || dataType === "json_array" || dataType === "simple_array") {
                  try {
                    const parsed = JSON.parse(testValues);
                    if (dataType === "json_object" && (typeof parsed !== "object" || Array.isArray(parsed))) {
                      throw new Error("Expected JSON object");
                    }
                    if ((dataType === "json_array" || dataType === "simple_array") && !Array.isArray(parsed)) {
                      throw new Error("Expected JSON array");
                    }
                    if (dataType === "json_array" && Array.isArray(parsed) && !parsed.every((item) => typeof item === "object" && item !== null && !Array.isArray(item))) {
                      throw new Error("Expected array of objects");
                    }
                  } catch {
                    const expectedFormat = dataType === "json_object" ? "JSON object" : dataType === "json_array" ? 'JSON array of objects (e.g., [{"key": "value"}])' : 'JSON array (e.g., ["a", "b", "c"])';
                    diagnostics.error(inputAttr.has("testValues") ? inputAttr.get("testValues") : inputAttr, `Input attribute '${displayName}': testValues is not valid for dataType '${dataType}'. Please provide a valid ${expectedFormat}.`);
                    continue;
                  }
                }
              }
              let truncate = false;
              if (inputAttr.has("truncate")) {
                const truncateShape = inputAttr.get("truncate");
                const truncateRawValue = truncateShape?.getValue();
                truncate = truncateRawValue === true || truncateRawValue === "true" || truncateRawValue === "1";
              }
              const nonTruncatableDataTypes = ["simple_array", "json_object", "json_array"];
              if (truncate && nonTruncatableDataTypes.includes(dataType || "")) {
                diagnostics.error(inputAttr.has("truncate") ? inputAttr.get("truncate") : inputAttr, `Input attribute '${displayName}': The 'truncate' property is not supported for dataType '${dataType}'. Only the following data types support truncate: string, numeric, boolean, record, glide_record. Please remove the 'truncate' property or change the dataType.`);
                return { records: [], inputNames };
              }
              let schema = "";
              if (dataType === "glide_record") {
                const tableName = inputAttr.has("tableName") ? inputAttr.get("tableName")?.asString().getValue() : "";
                let tableSysId = "";
                if (inputAttr.has("tableSysId")) {
                  const tableSysIdShape = inputAttr.get("tableSysId");
                  const rawValue = tableSysIdShape?.getValue();
                  if (rawValue && typeof rawValue === "object" && "id" in rawValue) {
                    tableSysId = typeof rawValue.id === "string" ? rawValue.id : "";
                  } else if (typeof rawValue === "string") {
                    tableSysId = rawValue;
                  }
                }
                if (!tableName || testValues && !tableSysId) {
                  diagnostics.error(inputAttr, `dataType 'glide_record' requires 'tableName'${testValues ? " and 'tableSysId' when 'testValues' is provided" : ""}. Missing: ${!tableName ? "tableName" : ""}${!tableName && testValues && !tableSysId ? " and " : ""}${testValues && !tableSysId ? "tableSysId" : ""}.`);
                  continue;
                }
                if (tableName) {
                  schema = JSON.stringify({ tableName, sysId: tableSysId || "" });
                } else if (inputAttr.has("schema")) {
                  const schemaValue = inputAttr.get("schema");
                  if (schemaValue?.isObject()) {
                    schema = JSON.stringify(schemaValue.getValue());
                  } else if (schemaValue?.isString()) {
                    schema = schemaValue.asString().getValue();
                  }
                }
              }
              const attrRecord = await factory.createRecord({
                source: inputAttr,
                table: SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
                explicitId: inputExplicitId,
                properties: arg.transform(({ $ }) => {
                  const properties = {
                    ...(0, skill_plugin_helpers_1.applyDefaults)(SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
                    name: $.val(generatedInternalName),
                    label: $.val(label),
                    description: $.val(description),
                    capability: $.val(capabilityRecord.getId()),
                    type: $.val("input"),
                    data_type: $.val(dataType),
                    mandatory: $.val(mandatory),
                    truncate: $.val(truncate),
                    default_value: $.val(testValues)
                    // test_values maps to default_value column
                    // NOTE: 'order' and 'hidden' intentionally omitted - let platform handle defaults
                  };
                  if (dataType === "glide_record" && schema) {
                    return { ...properties, schema: $.val(schema) };
                  }
                  return properties;
                })
              });
              attributeRecords.push(attrRecord);
            }
          }
        }
      }
      const standardOutputs = defaults_1.STANDARD_OUTPUT_NAMES;
      const stdOutputIdMap = /* @__PURE__ */ new Map();
      const outputsShape = definitionShape.get("outputs");
      const customOutputElements = [];
      if (outputsShape.isArray()) {
        for (const elem of outputsShape.asArray().getElements()) {
          if (elem.isObject()) {
            const nameVal = elem.has("name") ? elem.get("name")?.asString().getValue() : "";
            const internalName = nameVal ? generateInternalName(nameVal) : "";
            if (nameVal && standardOutputs.includes(internalName)) {
              const idShape = elem.has("$id") ? elem.get("$id") : void 0;
              stdOutputIdMap.set(nameVal, idShape);
            } else {
              customOutputElements.push(elem);
            }
          }
        }
      }
      const skillIdStr = skillExplicitId ? String(skillExplicitId.getValue()) : void 0;
      for (const outputName of standardOutputs) {
        const outputIdShape = stdOutputIdMap.get(outputName);
        const derivedExplicitId = !outputIdShape && skillIdStr ? `${skillIdStr}__output_${outputName}` : void 0;
        const attrRecord = await factory.createRecord({
          source: {
            path: callExpression.getOriginalFilePath(),
            content: ""
          },
          table: SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
          ...outputIdShape ? { explicitId: outputIdShape } : derivedExplicitId ? { explicitId: derivedExplicitId } : {},
          properties: arg.transform(({ $ }) => ({
            ...(0, skill_plugin_helpers_1.applyDefaults)(SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
            name: $.val(outputName),
            label: $.val(defaults_1.STANDARD_OUTPUT_DEFAULTS[outputName]?.label || outputName),
            description: $.val(defaults_1.STANDARD_OUTPUT_DEFAULTS[outputName]?.description || ""),
            capability: $.val(capabilityRecord.getId()),
            type: $.val("output")
            // NOTE: 'order', 'hidden', 'contains_large_input' intentionally omitted - let platform handle defaults
          }))
        });
        attributeRecords.push(attrRecord);
      }
      if (customOutputElements.length > 0) {
        const glideRecordOutputs = [];
        for (const outputAttr of customOutputElements) {
          if (outputAttr.isObject()) {
            const dataType = outputAttr.has("dataType") ? outputAttr.get("dataType")?.asString().getValue() : "string";
            if (dataType === "glide_record") {
              const displayName = outputAttr.has("name") ? outputAttr.get("name")?.asString().getValue() : "unknown";
              glideRecordOutputs.push(displayName);
            }
          }
        }
        if (glideRecordOutputs.length > 1) {
          diagnostics.error(outputsShape, `Only one output attribute can have data_type 'glide_record'. Found ${glideRecordOutputs.length} glide_record outputs: [${glideRecordOutputs.join(", ")}]. The glide_record type is designed to reference a single record. If you need multiple records, consider using 'json_array' or 'simple_array' data types instead.`);
          return { records: [], inputNames };
        }
        const outputNames = /* @__PURE__ */ new Set();
        for (const outputAttr of customOutputElements) {
          if (outputAttr.isObject()) {
            const displayName = outputAttr.has("name") ? outputAttr.get("name")?.asString().getValue() : "unknown";
            if (outputNames.has(displayName)) {
              diagnostics.error(outputAttr, `Duplicate output name '${displayName}'. Each output must have a unique name.`);
              return { records: [], inputNames };
            }
            if (inputNames.has(displayName)) {
              diagnostics.error(outputAttr, `Output name '${displayName}' conflicts with an input name. Input and output names must be unique across both.`);
              return { records: [], inputNames };
            }
            outputNames.add(displayName);
          }
        }
        for (const outputAttr of customOutputElements) {
          if (outputAttr.isObject()) {
            const displayName = outputAttr.has("name") ? outputAttr.get("name")?.asString().getValue() : "";
            const outputIdShape = outputAttr.has("$id") ? outputAttr.get("$id") : void 0;
            if (!outputIdShape || !(outputIdShape instanceof sdk_build_plugins_1.NowIdShape)) {
              diagnostics.error(outputAttr, `Output attribute '$id' is required and must use Now.ID['key'].`);
              continue;
            }
            const outputExplicitId = outputIdShape;
            if (!displayName || displayName.trim() === "") {
              diagnostics.error(outputAttr, "Attribute name is required and cannot be empty");
              continue;
            }
            const validatedDisplayName = validateName(displayName, diagnostics, outputAttr, "Attribute name");
            const generatedInternalName = generateInternalName(validatedDisplayName);
            const label = displayName;
            const description = outputAttr.has("description") ? outputAttr.get("description")?.asString().getValue() : "";
            const dataType = outputAttr.has("dataType") ? outputAttr.get("dataType")?.asString().getValue() : "string";
            if (enableNAP && napIncompatibleDataTypes.includes(dataType || "")) {
              diagnostics.error(outputAttr.has("dataType") ? outputAttr.get("dataType") : outputAttr, `Output attribute '${displayName}': The dataType '${dataType}' is not compatible with Now Assist Panel deployment. The following data types are NOT supported with nowAssistPanel: ${napIncompatibleDataTypes.join(", ")}. Please change the dataType or remove the nowAssistPanel deployment setting.`);
              continue;
            }
            let schema = "";
            if (dataType === "glide_record") {
              const tableName = outputAttr.has("tableName") ? outputAttr.get("tableName")?.asString().getValue() : "";
              if (!tableName) {
                diagnostics.error(outputAttr, `dataType 'glide_record' requires 'tableName'.`);
                continue;
              }
              schema = JSON.stringify({ tableName, sysId: "" });
            }
            const attrRecord = await factory.createRecord({
              source: outputAttr,
              table: SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE,
              explicitId: outputExplicitId,
              properties: arg.transform(({ $ }) => {
                const properties = {
                  ...(0, skill_plugin_helpers_1.applyDefaults)(SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, $),
                  name: $.val(generatedInternalName),
                  label: $.val(label),
                  description: $.val(description),
                  capability: $.val(capabilityRecord.getId()),
                  type: $.val("output"),
                  data_type: $.val(dataType)
                  // NOTE: 'order', 'hidden' intentionally omitted - let platform handle defaults
                };
                if (dataType === "glide_record" && schema) {
                  return { ...properties, schema: $.val(schema) };
                }
                return properties;
              })
            });
            attributeRecords.push(attrRecord);
          }
        }
      }
      return { records: attributeRecords, inputNames };
    }
    __name(processAttributes, "processAttributes");
    async function processDeploymentSettings(deploymentSettings, skillRecord, deploymentSettingsShape, arg, factory, genAISkillRecord = null) {
      const records = [];
      const skillId = skillRecord.getId();
      const skillName = skillRecord.get("name")?.getValue() || "Skill";
      if (deploymentSettings.EnableNAP && genAISkillRecord) {
        records.push(genAISkillRecord);
        const genAISkillConfigRecord = await factory.createRecord({
          source: deploymentSettingsShape,
          table: SYS_GEN_AI_SKILL_CONFIG,
          properties: arg.transform(({ $ }) => ({
            skill_id: $.val(genAISkillRecord.getId()),
            hint: $.val(skillName)
          }))
        });
        records.push(genAISkillConfigRecord);
      }
      if (deploymentSettings.EnableFlowAction) {
        const flowActionVarSetRecord = await factory.createRecord({
          source: deploymentSettingsShape,
          table: SN_NOWASSIST_SKILL_CONFIG_VAR_SET,
          properties: arg.transform(({ $ }) => ({
            ...(0, skill_plugin_helpers_1.applyDefaults)(SN_NOWASSIST_SKILL_CONFIG_VAR_SET, $),
            name: $.val("View trigger"),
            skill_config: $.val(skillId),
            config_type: $.val(defaults_1.VAR_SET.CONFIG_TYPE_VIEW_TRIGGER_STEP)
          }))
        });
        records.push(flowActionVarSetRecord);
        const enableWorkflowTriggerRecord = await factory.createRecord({
          source: deploymentSettingsShape,
          table: SYS_VARIABLE_VALUE,
          properties: arg.transform(({ $ }) => ({
            document_key: $.val(flowActionVarSetRecord.getId()),
            document: $.val(SN_NOWASSIST_SKILL_CONFIG_VAR_SET),
            variable: $.val(defaults_1.VAR_SET.ENABLE_WORKFLOW_TRIGGER)
          }))
        });
        records.push(enableWorkflowTriggerRecord);
      }
      const hasStandardDeployOptions = deploymentSettings.EnableUIAction || deploymentSettings.EnableNAP;
      if (!hasStandardDeployOptions) {
        return { records };
      }
      const varSetRecord = await factory.createRecord({
        source: deploymentSettingsShape,
        table: SN_NOWASSIST_SKILL_CONFIG_VAR_SET,
        properties: arg.transform(({ $ }) => ({
          ...(0, skill_plugin_helpers_1.applyDefaults)(SN_NOWASSIST_SKILL_CONFIG_VAR_SET, $),
          name: $.val("__delivery_step__"),
          skill_config: $.val(skillId),
          config_type: $.val(defaults_1.VAR_SET.SKILL_BUILDER_DELIVERY_STEP),
          screen_table: $.val("sys_ux_macroponent")
        }))
      });
      records.push(varSetRecord);
      if (deploymentSettings.EnableUIAction) {
        const varValueRecord = await factory.createRecord({
          source: deploymentSettingsShape,
          table: SYS_VARIABLE_VALUE,
          properties: arg.transform(({ $ }) => ({
            document: $.val(SN_NOWASSIST_SKILL_CONFIG_VAR_SET),
            document_key: $.val(varSetRecord.getId()),
            value: $.val("1"),
            variable: $.val(defaults_1.VAR_SET.ENABLE_UI_ACTION)
          }))
        });
        records.push(varValueRecord);
      }
      if (deploymentSettings.EnableNAP) {
        const varValueRecord = await factory.createRecord({
          source: deploymentSettingsShape,
          table: SYS_VARIABLE_VALUE,
          properties: arg.transform(({ $ }) => ({
            document: $.val(SN_NOWASSIST_SKILL_CONFIG_VAR_SET),
            document_key: $.val(varSetRecord.getId()),
            value: $.val("1"),
            variable: $.val(defaults_1.VAR_SET.ENABLE_NAP)
          }))
        });
        records.push(varValueRecord);
      }
      if (deploymentSettings.uiActionIdShape) {
        const uiActionIdRecord = await factory.createRecord({
          source: deploymentSettingsShape,
          table: SYS_VARIABLE_VALUE,
          properties: arg.transform(({ $ }) => ({
            document: $.val(SN_NOWASSIST_SKILL_CONFIG_VAR_SET),
            document_key: $.val(varSetRecord.getId()),
            // Use $.val() with the Shape to properly resolve Now.ID to sys_id
            value: $.val(deploymentSettings.uiActionIdShape),
            variable: $.val(defaults_1.VAR_SET.UI_ACTION_ID)
          }))
        });
        records.push(uiActionIdRecord);
      }
      return { records };
    }
    __name(processDeploymentSettings, "processDeploymentSettings");
    function resolveValidatorScript(shape) {
      if (!shape || !shape.isDefined() || shape.isUndefined()) {
        return void 0;
      }
      if (shape.is(sdk_build_plugins_1.NowIncludeShape)) {
        return shape.getValue();
      }
      if (shape.isString()) {
        return shape.asString().getValue();
      }
      if (shape instanceof sdk_build_plugins_1.ArrowFunctionShape) {
        const params = shape.getParameters();
        const paramName = params.length > 0 ? params[0].getCode() : "payload";
        const returnVal = shape.getReturnValue();
        const stmts = shape.getStatements();
        let body;
        if (returnVal) {
          body = `    return ${returnVal.getCode()};`;
        } else if (stmts.length > 0) {
          body = stmts.map((s) => `    ${s.getCode()}`).join("\n");
        } else {
          body = "";
        }
        return `(function(${paramName}) {
${body}
})(${paramName});`;
      }
      return shape.getCode?.() || void 0;
    }
    __name(resolveValidatorScript, "resolveValidatorScript");
    async function processProviderArray(providersArray, promptRecords, arg, factory, diagnostics) {
      const records = [];
      if (!providersArray?.isArray()) {
        return records;
      }
      const providers = providersArray.asArray().getElements();
      const validatorCache = /* @__PURE__ */ new Map();
      for (let i = 0; i < providers.length; i++) {
        const provider = providers[i];
        if (!provider?.isObject()) {
          continue;
        }
        const providerName = provider.has("name") ? provider.get("name")?.asString()?.getValue() : void 0;
        const providerIdShape = provider.has("$id") ? provider.get("$id") : void 0;
        const preprocessorShape = provider.has("preprocessor") ? provider.get("preprocessor") : void 0;
        const postprocessorShape = provider.has("postprocessor") ? provider.get("postprocessor") : void 0;
        if (!providerName) {
          diagnostics.warn(provider, `Provider at index ${i} missing 'name' field`);
          continue;
        }
        const hasPreprocessor = preprocessorShape?.isDefined() && !preprocessorShape.isUndefined();
        const hasPostprocessor = postprocessorShape?.isDefined() && !postprocessorShape.isUndefined();
        if (!hasPreprocessor && !hasPostprocessor) {
          continue;
        }
        const matchingDefinitions = promptRecords;
        if (matchingDefinitions.length === 0) {
          diagnostics.warn(provider, `No matching definition found for provider: ${providerName}`);
          continue;
        }
        const preprocessorScript = resolveValidatorScript(preprocessorShape);
        const postprocessorScript = resolveValidatorScript(postprocessorShape);
        if (preprocessorScript) {
          const validatorKey = `${providerName}_request`;
          let validatorRecord = validatorCache.get(validatorKey);
          if (!validatorRecord) {
            validatorRecord = await factory.createRecord({
              source: provider,
              table: SYS_GENERATIVE_AI_VALIDATOR,
              ...providerIdShape ? { explicitId: providerIdShape } : {},
              properties: arg.transform(({ $ }) => ({
                name: $.val(`${providerName} - Request Validator`),
                validator_script: $.val(preprocessorScript).toCdata()
              }))
            });
            validatorCache.set(validatorKey, validatorRecord);
            records.push(validatorRecord);
          }
          for (const promptRecord of matchingDefinitions) {
            const definitionId = promptRecord.capabilityDefinitionRecord.getId();
            const requestValidatorRecord = await factory.createRecord({
              source: provider,
              table: SYS_GENERATIVE_AI_REQUEST_VALIDATOR,
              properties: arg.transform(({ $ }) => ({
                document_id: $.val(definitionId),
                document_table: $.val("sys_one_extend_capability_definition"),
                validator: $.val(validatorRecord.getId())
              }))
            });
            records.push(requestValidatorRecord);
          }
        }
        if (postprocessorScript) {
          const validatorKey = `${providerName}_response`;
          let validatorRecord = validatorCache.get(validatorKey);
          if (!validatorRecord) {
            validatorRecord = await factory.createRecord({
              source: provider,
              table: SYS_GENERATIVE_AI_VALIDATOR,
              ...providerIdShape ? { explicitId: providerIdShape } : {},
              properties: arg.transform(({ $ }) => ({
                name: $.val(`${providerName} - Response Validator`),
                active: $.val(true),
                validator_script: $.val(postprocessorScript).toCdata()
              }))
            });
            validatorCache.set(validatorKey, validatorRecord);
            records.push(validatorRecord);
          }
          for (const promptRecord of matchingDefinitions) {
            const definitionId = promptRecord.capabilityDefinitionRecord.getId();
            const requestValidatorRecord = await factory.createRecord({
              source: provider,
              table: SYS_GENERATIVE_AI_RESPONSE_VALIDATOR,
              properties: arg.transform(({ $ }) => ({
                active: $.val(true),
                definition: $.val(definitionId),
                definition_table: $.val("sys_one_extend_capability_definition"),
                validator: $.val(validatorRecord.getId())
              }))
            });
            records.push(requestValidatorRecord);
          }
        }
      }
      return records;
    }
    __name(processProviderArray, "processProviderArray");
    async function processProviderConfig(providerConfig, promptRecords, arg, factory, diagnostics) {
      const records = [];
      const providerNames = providerConfig.properties ? Object.keys(providerConfig.properties()) : [];
      const validatorCache = /* @__PURE__ */ new Map();
      for (const providerName of providerNames) {
        const provider = providerConfig.get(providerName);
        if (!provider?.isObject()) {
          continue;
        }
        const preprocessorShape = provider.has("preprocessor") ? provider.get("preprocessor") : void 0;
        const postprocessorShape = provider.has("postprocessor") ? provider.get("postprocessor") : void 0;
        const hasPreprocessor = preprocessorShape?.isDefined() && !preprocessorShape.isUndefined();
        const hasPostprocessor = postprocessorShape?.isDefined() && !postprocessorShape.isUndefined();
        if (!hasPreprocessor && !hasPostprocessor) {
          continue;
        }
        const matchingDefinitions = promptRecords;
        if (matchingDefinitions.length === 0) {
          diagnostics.warn(provider, `No matching definition found for provider: ${providerName}`);
          continue;
        }
        const preprocessorScript = resolveValidatorScript(preprocessorShape);
        const postprocessorScript = resolveValidatorScript(postprocessorShape);
        if (preprocessorScript) {
          const validatorKey = `${providerName}_request`;
          let validatorRecord = validatorCache.get(validatorKey);
          if (!validatorRecord) {
            validatorRecord = await factory.createRecord({
              source: provider,
              table: SYS_GENERATIVE_AI_VALIDATOR,
              properties: arg.transform(({ $ }) => ({
                name: $.val(`${providerName} - Request Validator`),
                validator_script: $.val(preprocessorScript).toCdata()
              }))
            });
            validatorCache.set(validatorKey, validatorRecord);
            records.push(validatorRecord);
          }
          for (const promptRecord of matchingDefinitions) {
            const definitionId = promptRecord.capabilityDefinitionRecord.getId();
            const requestValidatorRecord = await factory.createRecord({
              source: provider,
              table: SYS_GENERATIVE_AI_REQUEST_VALIDATOR,
              properties: arg.transform(({ $ }) => ({
                document_id: $.val(definitionId),
                document_table: $.val("sys_one_extend_capability_definition"),
                validator: $.val(validatorRecord.getId())
              }))
            });
            records.push(requestValidatorRecord);
          }
        }
        if (postprocessorScript) {
          const validatorKey = `${providerName}_response`;
          let validatorRecord = validatorCache.get(validatorKey);
          if (!validatorRecord) {
            validatorRecord = await factory.createRecord({
              source: provider,
              table: SYS_GENERATIVE_AI_VALIDATOR,
              properties: arg.transform(({ $ }) => ({
                name: $.val(`${providerName} - Response Validator`),
                validator_script: $.val(postprocessorScript).toCdata()
              }))
            });
            validatorCache.set(validatorKey, validatorRecord);
            records.push(validatorRecord);
          }
          for (const promptRecord of matchingDefinitions) {
            const definitionId = promptRecord.capabilityDefinitionRecord.getId();
            const requestValidatorRecord = await factory.createRecord({
              source: provider,
              table: SYS_GENERATIVE_AI_RESPONSE_VALIDATOR,
              properties: arg.transform(({ $ }) => ({
                definition: $.val(definitionId),
                definition_table: $.val("sys_one_extend_capability_definition"),
                validator: $.val(validatorRecord.getId())
              }))
            });
            records.push(requestValidatorRecord);
          }
        }
      }
      return records;
    }
    __name(processProviderConfig, "processProviderConfig");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/transform-logic.js
var require_transform_logic = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/transform-logic.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.GLUE_CODE_PREFIX = void 0;
    exports2.isGlueCode = isGlueCode;
    exports2.transformToShape = transformToShape;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var defaults_1 = require_defaults();
    var tool_processor_1 = require_tool_processor();
    var deployment_processor_1 = require_deployment_processor();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var { SN_NOWASSIST_SKILL_CONFIG_VAR_SET, SYS_ONE_EXTEND_CAPABILITY_DEFINITION, SYS_ONE_EXTEND_DEFINITION_CONFIG, SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, SYS_GENERATIVE_AI_CONFIG, SYS_GENERATIVE_AI_PROMPT_CONFIG, SYS_GENERATIVE_AI_VALIDATOR, SYS_GENERATIVE_AI_REQUEST_VALIDATOR, SYS_GENERATIVE_AI_RESPONSE_VALIDATOR, SYS_VARIABLE_VALUE, SYS_ONE_EXTEND_RESOURCE_MAPPING, SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, SYS_ONE_EXTEND_RESOURCE_EDGE, SYS_ONE_EXTEND_CAPABILITY, SYS_GEN_AI_SKILL_APPLICABILITY } = defaults_1.TABLE_NAMES;
    var { SYS_AGENT_ACCESS_ROLE_CONFIGURATION, SYS_AGENT_ACCESS_ROLE_MAPPING } = defaults_1.SECURITY_TABLE_NAMES;
    exports2.GLUE_CODE_PREFIX = "// @fluent-module";
    function isGlueCode(script) {
      return script.startsWith(exports2.GLUE_CODE_PREFIX);
    }
    __name(isGlueCode, "isGlueCode");
    var MUSTACHE_PATTERN = /\{\{([^}]+)\}\}/;
    function hasMustacheReferences(prompt) {
      return MUSTACHE_PATTERN.test(prompt);
    }
    __name(hasMustacheReferences, "hasMustacheReferences");
    function isValidJsIdentifier(name) {
      return /^[a-zA-Z_$][a-zA-Z0-9_$]*$/.test(name);
    }
    __name(isValidJsIdentifier, "isValidJsIdentifier");
    function formatPropertyAccess(base, propertyName) {
      if (isValidJsIdentifier(propertyName)) {
        return `${base}.${propertyName}`;
      }
      return `${base}['${propertyName.replace(/'/g, "\\'")}']`;
    }
    __name(formatPropertyAccess, "formatPropertyAccess");
    function mustacheToArrowFunction(prompt, inputNameMap, toolNames, source) {
      const mustacheGlobal = /\{\{([^}]+)\}\}/g;
      const templateBody = prompt.replace(mustacheGlobal, (match, ref) => {
        if (ref.startsWith("input.")) {
          const fieldName = ref.slice("input.".length);
          if (inputNameMap.has(fieldName)) {
            const originalName = inputNameMap.get(fieldName);
            return `\${${formatPropertyAccess("p.input", originalName)}}`;
          }
          return match;
        }
        if (ref.includes(".")) {
          const [toolName, ...outputParts] = ref.split(".");
          if (toolName && toolNames.has(toolName)) {
            const outputField = outputParts.join(".");
            let expr = "p.tool";
            expr = formatPropertyAccess(expr, toolName);
            if (outputField) {
              expr = formatPropertyAccess(expr, outputField);
            }
            return `\${${expr}}`;
          }
          return match;
        }
        if (inputNameMap.has(ref)) {
          const originalName = inputNameMap.get(ref);
          return `\${${formatPropertyAccess("p.input", originalName)}}`;
        }
        return match;
      });
      const parts = templateBody.split(/(\$\{[^}]+\})/);
      const escapedParts = parts.map((part) => {
        if (part.startsWith("${") && part.endsWith("}")) {
          return part;
        }
        return part.replace(/`/g, "\\`");
      });
      const escapedBody = escapedParts.join("");
      const arrowCode = "(p) => `" + escapedBody + "`";
      return new tool_processor_1.RawCodeShape({ source, code: arrowCode });
    }
    __name(mustacheToArrowFunction, "mustacheToArrowFunction");
    function convertMustachePromptsToArrowFunctions(providers, inputNameMap, toolNames, source) {
      for (const provider of providers) {
        for (const promptSettings of provider.prompts) {
          for (const version of promptSettings.versions) {
            if (typeof version.prompt === "string" && hasMustacheReferences(version.prompt)) {
              version.prompt = mustacheToArrowFunction(version.prompt, inputNameMap, toolNames, source);
            }
          }
        }
      }
    }
    __name(convertMustachePromptsToArrowFunctions, "convertMustachePromptsToArrowFunctions");
    async function transformToShape(record, descendantsDB, _transform, diagnostics, database) {
      const excludedRecords = [];
      const supportedRecords = [];
      descendantsDB.query().forEach((descendant) => {
        const table = descendant.getTable();
        const isAutoTunedConfig = table === SYS_GENERATIVE_AI_CONFIG && descendant.get("state")?.getValue() === "auto_tuned";
        const isExcludedMetricMapping = table === SYS_ONE_EXTEND_RESOURCE_MAPPING && defaults_1.EXCLUDED_METRIC_CAPABILITY_IDS.has(descendant.get("resource_capability").toString().getValue());
        if (isAutoTunedConfig || isExcludedMetricMapping) {
          excludedRecords.push(descendant);
        } else {
          supportedRecords.push(descendant);
        }
      });
      const descendants = new sdk_build_core_1.Database(supportedRecords);
      const skillConfig = record;
      const capabilities = descendants.query("sys_one_extend_capability");
      const capability = capabilities.find((cap) => cap.get("type")?.getValue() === "skill") ?? capabilities[0];
      if (!capability) {
        return {
          success: false,
          error: "Not a Now Assist skill - no capability found"
        };
      }
      const allAiConfigs = descendants.query(SYS_GENERATIVE_AI_CONFIG);
      const definitionConfigs = descendants.query(SYS_ONE_EXTEND_DEFINITION_CONFIG);
      const capabilityDefinitions = descendants.query(SYS_ONE_EXTEND_CAPABILITY_DEFINITION);
      const capabilityId = capability.getId().getValue();
      const capabilityDefinitionIds = /* @__PURE__ */ new Set();
      for (const capDef of capabilityDefinitions) {
        const capDefCapability = capDef.get("capability")?.getValue();
        if (capDefCapability === capabilityId) {
          const capDefId = capDef.getId().getValue();
          if (capDefId) {
            capabilityDefinitionIds.add(capDefId);
          }
        }
      }
      const aiConfigs = allAiConfigs.filter((config) => {
        const configDefinition = config.get("definition")?.getValue();
        return configDefinition && typeof configDefinition === "string" && capabilityDefinitionIds.has(configDefinition);
      });
      const defaultDefinitionConfig = definitionConfigs.find((config) => {
        const defaultValue = config.get("default")?.getValue();
        return defaultValue === true || defaultValue === "true";
      });
      const defaultDefinitionId = defaultDefinitionConfig?.get("definition")?.getValue();
      const promptConfigs = descendants.query(SYS_GENERATIVE_AI_PROMPT_CONFIG);
      const validators = descendants.query(SYS_GENERATIVE_AI_VALIDATOR);
      const requestValidators = descendants.query(SYS_GENERATIVE_AI_REQUEST_VALIDATOR).filter((rv) => {
        const docId = rv.get("document_id")?.getValue();
        return docId && typeof docId === "string" && capabilityDefinitionIds.has(docId);
      });
      const responseValidators = descendants.query(SYS_GENERATIVE_AI_RESPONSE_VALIDATOR).filter((rv) => {
        const defId = rv.get("definition")?.getValue();
        return defId && typeof defId === "string" && capabilityDefinitionIds.has(defId);
      });
      const definitionAttributes = descendants.query(SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE);
      const varSetRecords = descendants.query(SN_NOWASSIST_SKILL_CONFIG_VAR_SET);
      const variableValues = descendants.query(SYS_VARIABLE_VALUE);
      let allAclRecords = descendants.query("sys_security_acl");
      const allAclRoleRecords = descendants.query("sys_security_acl_role");
      const roleConfigRecords = descendants.query(SYS_AGENT_ACCESS_ROLE_CONFIGURATION);
      let internalName = skillConfig.get("internal_name")?.getValue();
      const skillSysId = skillConfig.getId()?.getValue();
      if (!internalName && skillSysId) {
        const skillName = skillConfig.get("name")?.toString().getValue();
        if (skillName) {
          internalName = `${(0, skill_plugin_helpers_1.sanitizeSkillName)(skillName)}.${skillSysId}`;
        }
      }
      if (allAclRecords.length === 0 && database && typeof internalName === "string") {
        const matchingAcl = database.get("sys_security_acl", { name: internalName });
        if (matchingAcl) {
          allAclRecords = [matchingAcl];
        } else if (skillSysId) {
          const allAcls = database.query("sys_security_acl");
          const suffixMatch = allAcls.find((acl) => {
            const aclName = acl.get("name")?.getValue();
            return typeof aclName === "string" && aclName.endsWith(`.${skillSysId}`);
          });
          if (suffixMatch) {
            allAclRecords = [suffixMatch];
          }
        }
      }
      let aclRecords = internalName ? allAclRecords.filter((acl) => acl.get("name")?.getValue() === internalName) : allAclRecords;
      if (aclRecords.length === 0 && skillSysId) {
        aclRecords = allAclRecords.filter((acl) => {
          const aclName = acl.get("name")?.getValue();
          return typeof aclName === "string" && aclName.endsWith(`.${skillSysId}`);
        });
      }
      let aclRoleRecords = [];
      if (aclRecords.length > 0) {
        const firstAclRecord = aclRecords[0];
        if (firstAclRecord) {
          const aclSysId = firstAclRecord.getId().getValue();
          aclRoleRecords = allAclRoleRecords.filter((aclRole) => {
            const aclRef = aclRole.get("sys_security_acl")?.toString().getValue();
            return aclRef === aclSysId;
          });
        }
      }
      let deployConfigData = null;
      if (variableValues.length > 0) {
        const deploymentVarSet = varSetRecords.find((varSet) => {
          const varSetId = varSet.getId().getValue();
          return variableValues.some((vv) => vv.get("document_key")?.getValue() === varSetId && [
            defaults_1.VAR_SET.ENABLE_UI_ACTION,
            defaults_1.VAR_SET.UI_ACTION_ID,
            defaults_1.VAR_SET.ENABLE_UI_BUILDER,
            defaults_1.VAR_SET.ENABLE_NAP
          ].includes(vv.get("variable")?.getValue()));
        });
        const flowActionVarSet = varSetRecords.find((varSet) => {
          const configType = varSet.get("config_type")?.getValue();
          if (configType === defaults_1.VAR_SET.CONFIG_TYPE_VIEW_TRIGGER_STEP) {
            return true;
          }
          const varSetId = varSet.getId().getValue();
          return variableValues.some((vv) => vv.get("document_key")?.getValue() === varSetId && vv.get("variable")?.getValue() === defaults_1.VAR_SET.ENABLE_WORKFLOW_TRIGGER);
        });
        if (deploymentVarSet || flowActionVarSet) {
          const deploymentSettings = {};
          if (deploymentVarSet) {
            const varSetId = deploymentVarSet.getId().getValue();
            const relatedVariables = variableValues.filter((vv) => vv.get("document_key")?.getValue() === varSetId);
            let uiActionId;
            let napEnabled = false;
            for (const varValue of relatedVariables) {
              const variable = varValue.get("variable")?.getValue();
              const value = varValue.get("value")?.getValue();
              if (variable === defaults_1.VAR_SET.UI_ACTION_ID) {
                uiActionId = value;
              } else if (variable === defaults_1.VAR_SET.ENABLE_NAP) {
                napEnabled = value === "1" || value === "true";
              }
            }
            if (uiActionId) {
              const uiActionRecords = descendants.query("sys_ui_action");
              const uiActionRecord = uiActionRecords.find((rec) => String(rec.getId().getValue()) === uiActionId);
              if (uiActionRecord) {
                const skillName = String(skillConfig.get("name")?.getValue() || "");
                const defaultCondition = `new sn_nowassist_admin.NowAssistSkillConfig().isSkillConfigurationActive('${skillSysId}')`;
                const inputAttrs2 = definitionAttributes.filter((attr) => attr.get("type")?.getValue() === "input" && attr.get("capability")?.getValue() === capabilityId);
                const deployInputs = inputAttrs2.map((attr) => {
                  const name = String(attr.get("name")?.getValue() || "");
                  const dataType = String(attr.get("data_type")?.getValue() || "string");
                  let tblName = "";
                  const schemaVal = attr.get("schema")?.getValue();
                  if (schemaVal) {
                    try {
                      const schema = typeof schemaVal === "string" ? JSON.parse(schemaVal) : schemaVal;
                      tblName = schema.tableName || "";
                    } catch {
                    }
                  }
                  return { name, dataType, mandatory: false, description: "", tableName: tblName };
                });
                const defaultScript = (0, deployment_processor_1.generateScriptFromInputs)(String(skillSysId), String(capabilityId), deployInputs);
                deploymentSettings.uiAction = uiActionRecord.transform(({ $ }) => ({
                  $id: $.val(sdk_build_plugins_1.NowIdShape.from(uiActionRecord)),
                  table: $.from("table"),
                  name: $.from("name").def(skillName),
                  condition: $.from("condition").def(defaultCondition),
                  script: $.from("script").def(defaultScript),
                  active: $.from("active").toBoolean().def(false)
                }));
              }
            }
            if (napEnabled) {
              const napApplicabilityRecords = descendants.query(SYS_GEN_AI_SKILL_APPLICABILITY);
              const napRoles = [];
              let napConditionMode;
              let napConditionScript;
              if (napApplicabilityRecords.length > 0) {
                const napRecord = napApplicabilityRecords[0];
                const rolesValue = napRecord?.get("roles")?.getValue();
                if (rolesValue) {
                  const parsedRoles = rolesValue.split(",").map((r) => r.trim()).filter((r) => r.length > 0);
                  napRoles.push(...parsedRoles);
                }
                const conditionModeValue = napRecord?.get("condition_mode")?.getValue();
                if (conditionModeValue === "simple" || conditionModeValue === "advanced") {
                  napConditionMode = conditionModeValue;
                }
                if (napConditionMode === "advanced") {
                  const scriptValue = napRecord?.get("condition_script")?.getValue();
                  if (scriptValue && scriptValue !== defaults_1.DEFAULT_CONDITION_SCRIPT) {
                    napConditionScript = scriptValue;
                  }
                }
              }
              deploymentSettings.nowAssistPanel = {
                enabled: true,
                // Include roles only when present. Empty roles (valid in advanced
                // mode) are omitted so the regenerated source matches the record.
                ...napRoles.length > 0 ? { roles: napRoles } : {},
                // Only include conditionMode if it differs from the default 'simple'
                ...napConditionMode && napConditionMode !== "simple" ? { conditionMode: napConditionMode } : {},
                // Include conditionScript when conditionMode is 'advanced'
                ...napConditionScript ? { conditionScript: napConditionScript } : {}
              };
            }
          }
          if (flowActionVarSet) {
            deploymentSettings.flowAction = true;
          }
          if (Object.keys(deploymentSettings).length > 0) {
            deployConfigData = deploymentSettings;
          }
        }
      }
      const skillFamilyValue = skillConfig.get("skill_family")?.getValue();
      if (skillFamilyValue && skillFamilyValue !== defaults_1.OTHERS_SKILL_FAMILY_SYS_ID) {
        if (!deployConfigData) {
          deployConfigData = {};
        }
        deployConfigData.skillFamily = skillFamilyValue;
      }
      const inputAttrs = definitionAttributes.filter((attr) => attr.get("type")?.getValue() === "input" && attr.get("capability")?.getValue() === capabilityId);
      const outputAttrs = definitionAttributes.filter((attr) => attr.get("type")?.getValue() === "output" && attr.get("capability")?.getValue() === capabilityId);
      const inputs = [];
      for (const inputAttr of inputAttrs) {
        const attrName = inputAttr.get("name")?.getValue()?.toString();
        if (attrName) {
          const mandatoryValue = inputAttr.get("mandatory")?.getValue();
          const mandatory = mandatoryValue === true || mandatoryValue === "true" || mandatoryValue === "1";
          const dataTypeValue = inputAttr.get("data_type")?.getValue();
          const dataType = typeof dataTypeValue === "string" ? dataTypeValue : "string";
          const truncateValue = inputAttr.get("truncate")?.getValue();
          const truncate = truncateValue === true || truncateValue === "true" || truncateValue === "1";
          const nonTruncatableTypes = ["simple_array", "json_object", "json_array"];
          const supportsTruncate = !nonTruncatableTypes.includes(dataType);
          const schemaValue = inputAttr.get("schema")?.getValue();
          let tableName = "", tableSysId = "";
          if (schemaValue) {
            try {
              const schema = typeof schemaValue === "string" ? JSON.parse(schemaValue) : schemaValue;
              tableName = schema.tableName || "";
              tableSysId = schema.sysId || "";
            } catch {
            }
          }
          const labelValue = inputAttr.get("label")?.getValue();
          const descriptionValue = inputAttr.get("description")?.getValue();
          const defaultValue = inputAttr.get("default_value")?.getValue();
          inputs.push({
            $id: sdk_build_plugins_1.NowIdShape.from(inputAttr),
            name: typeof labelValue === "string" ? labelValue : attrName,
            description: typeof descriptionValue === "string" ? descriptionValue : "",
            mandatory,
            dataType,
            ...supportsTruncate ? { truncate } : {},
            testValues: typeof defaultValue === "string" ? defaultValue : "",
            ...tableName || tableSysId ? { tableName, tableSysId } : {}
          });
        }
      }
      const outputs = [];
      for (const stdName of defaults_1.STANDARD_OUTPUT_NAMES) {
        const stdAttr = outputAttrs.find((attr) => attr.get("name")?.getValue()?.toString() === stdName);
        if (stdAttr) {
          const dataTypeValue = stdAttr.get("data_type")?.getValue();
          outputs.push({
            $id: sdk_build_plugins_1.NowIdShape.from(stdAttr),
            name: stdName,
            dataType: typeof dataTypeValue === "string" ? dataTypeValue : "string"
          });
        }
      }
      for (const outputAttr of outputAttrs) {
        const attrName = outputAttr.get("name")?.getValue()?.toString();
        if (attrName && !defaults_1.STANDARD_OUTPUT_NAMES.includes(attrName)) {
          const labelValue = outputAttr.get("label")?.getValue();
          const descriptionValue = outputAttr.get("description")?.getValue();
          const dataTypeValue = outputAttr.get("data_type")?.getValue();
          const schemaValue = outputAttr.get("schema")?.getValue();
          let tableName = "";
          if (schemaValue) {
            try {
              const schema = typeof schemaValue === "string" ? JSON.parse(schemaValue) : schemaValue;
              tableName = schema.tableName || "";
            } catch {
            }
          }
          outputs.push({
            $id: sdk_build_plugins_1.NowIdShape.from(outputAttr),
            name: typeof labelValue === "string" ? labelValue : attrName,
            description: typeof descriptionValue === "string" ? descriptionValue : "",
            dataType: typeof dataTypeValue === "string" ? dataTypeValue : "string",
            ...tableName ? { tableName } : {}
          });
        }
      }
      const definitionIdToProvider = /* @__PURE__ */ new Map();
      const definitionIdToProviderAPI = /* @__PURE__ */ new Map();
      for (const capabilityDefinition of capabilityDefinitions) {
        const definitionId = capabilityDefinition.getId().getValue();
        const definitionName = capabilityDefinition.get("name")?.getValue();
        if (definitionName && typeof definitionName === "string") {
          const match = definitionName.match(/\(([^)]+)\)$/);
          if (match && match[1]) {
            definitionIdToProvider.set(definitionId, match[1]);
          }
        }
        if (!definitionIdToProvider.has(definitionId)) {
          const filterPropertiesValue = capabilityDefinition.get("filter_properties")?.getValue();
          if (filterPropertiesValue) {
            try {
              const filterProperties = typeof filterPropertiesValue === "string" ? JSON.parse(filterPropertiesValue) : filterPropertiesValue;
              if (filterProperties.provider) {
                definitionIdToProvider.set(definitionId, filterProperties.provider);
              }
            } catch {
            }
          }
        }
        const apiValue = capabilityDefinition.get("api")?.getValue();
        const apiTypeValue = capabilityDefinition.get("api_type")?.getValue();
        if (apiValue && typeof apiValue === "string" && apiTypeValue && typeof apiTypeValue === "string") {
          definitionIdToProviderAPI.set(definitionId, { type: apiTypeValue, id: apiValue });
        }
      }
      const aiConfigIdToIsDefault = /* @__PURE__ */ new Map();
      const aiConfigIdToFilterProperties = /* @__PURE__ */ new Map();
      for (const promptConfig of promptConfigs) {
        const aiConfigId = promptConfig.get("ai_config")?.getValue();
        const isDefault = promptConfig.get("is_default")?.getValue();
        const filterProperties = promptConfig.get("filter_properties")?.getValue();
        if (aiConfigId && typeof aiConfigId === "string") {
          aiConfigIdToIsDefault.set(aiConfigId, isDefault === true || isDefault === "true" || isDefault === "1");
          if (filterProperties && typeof filterProperties === "string") {
            aiConfigIdToFilterProperties.set(aiConfigId, filterProperties);
          }
        }
      }
      const providerPromptsMap = /* @__PURE__ */ new Map();
      for (const config of aiConfigs) {
        const configId = config.getId().getValue();
        const definitionId = config.get("definition")?.getValue();
        const provider = definitionId && typeof definitionId === "string" ? definitionIdToProvider.get(definitionId) ?? "Now LLM Service" : "Now LLM Service";
        const providerAPI = definitionId && typeof definitionId === "string" ? definitionIdToProviderAPI.get(definitionId) : void 0;
        const nameValue = config.get("name")?.getValue();
        const modelValue = config.get("model")?.getValue();
        const promptValue = config.get("prompt")?.getValue();
        const stateValue2 = config.get("state")?.getValue();
        const versionValue = config.get("version")?.getValue();
        const promptName = typeof nameValue === "string" ? nameValue : "AI-generated prompt";
        const promptVersion = typeof versionValue === "string" ? parseInt(versionValue, 10) : typeof versionValue === "number" ? versionValue : 1;
        const tempValue = config.get("temperature")?.ifString()?.getValue() ?? config.get("temperature")?.ifNumber()?.getValue() ?? 0.2;
        const temperature = typeof tempValue === "string" ? parseFloat(tempValue) : tempValue;
        const maxTokensValue = config.get("max_tokens")?.ifString()?.getValue() ?? config.get("max_tokens")?.ifNumber()?.getValue();
        const maxTokens = typeof maxTokensValue === "string" ? parseInt(maxTokensValue, 10) : maxTokensValue ?? 500;
        const prompt = typeof promptValue === "string" ? promptValue : "";
        const versionSettings = {
          $id: sdk_build_plugins_1.NowIdShape.from(config),
          model: typeof modelValue === "string" ? modelValue : "llm_generic_small",
          temperature,
          ...provider !== "Now LLM Generic" ? { maxTokens } : {},
          prompt,
          // Always include promptState for all versions
          promptState: stateValue2 && typeof stateValue2 === "string" ? stateValue2 : "draft",
          // Include version if not 1 (default)
          ...promptVersion !== 1 ? { version: promptVersion } : {}
        };
        if (!providerPromptsMap.has(provider)) {
          providerPromptsMap.set(provider, {
            providerAPI,
            promptsByName: /* @__PURE__ */ new Map()
          });
        }
        const providerData = providerPromptsMap.get(provider);
        if (!providerData.promptsByName.has(promptName)) {
          providerData.promptsByName.set(promptName, { versions: [] });
        }
        const promptData = providerData.promptsByName.get(promptName);
        let versionFilterCondition;
        if (configId && aiConfigIdToFilterProperties.has(configId)) {
          const filterPropertiesJson = aiConfigIdToFilterProperties.get(configId);
          try {
            const filterProperties = JSON.parse(filterPropertiesJson);
            const transformedFilter = {};
            for (const [key, value] of Object.entries(filterProperties)) {
              const internalName2 = key.replace(/^\{\{|\}\}$/g, "");
              if (internalName2.includes(".")) {
                transformedFilter[internalName2] = value;
              } else {
                const matchingInput = inputs.find((inp) => inp.name.toLowerCase().replace(/\s+/g, "_") === internalName2);
                const displayName = matchingInput ? matchingInput.name : internalName2;
                transformedFilter[displayName] = value;
              }
            }
            if (Object.keys(transformedFilter).length > 0) {
              versionFilterCondition = transformedFilter;
            }
          } catch (e) {
          }
        }
        if (versionFilterCondition) {
          versionSettings.filterCondition = versionFilterCondition;
        }
        promptData.versions.push(versionSettings);
        if (configId && aiConfigIdToIsDefault.get(configId)) {
          providerData.defaultPromptName = promptName;
          providerData.defaultPromptVersion = promptVersion;
        }
      }
      const providerKeys = Array.from(providerPromptsMap.keys());
      const firstProviderName = providerKeys[0] ?? "Now LLM Generic";
      let defaultProviderName = firstProviderName;
      if (defaultDefinitionId && typeof defaultDefinitionId === "string") {
        defaultProviderName = definitionIdToProvider.get(defaultDefinitionId) ?? firstProviderName;
      }
      const providers = [];
      for (const [providerName, providerData] of providerPromptsMap) {
        const prompts = [];
        for (const [promptName, promptData] of providerData.promptsByName) {
          prompts.push({
            name: promptName,
            versions: promptData.versions
          });
        }
        if (!providerData.defaultPromptName && prompts[0]) {
          providerData.defaultPromptName = prompts[0].name;
        }
        providers.push({
          provider: providerName,
          // Only include providerAPI if it exists (custom providers or overrides)
          ...providerData.providerAPI ? { providerAPI: providerData.providerAPI } : {},
          prompts,
          // Only include defaultPrompt if multiple prompts exist
          ...prompts.length > 1 && providerData.defaultPromptName ? { defaultPrompt: providerData.defaultPromptName } : {},
          // Only include defaultPromptVersion if defaultPrompt is shown (multiple prompts exist)
          ...prompts.length > 1 && providerData.defaultPromptVersion ? { defaultPromptVersion: providerData.defaultPromptVersion } : {}
        });
      }
      const skillSettings = {};
      const providerMap = /* @__PURE__ */ new Map();
      for (const reqValidator of requestValidators) {
        const validatorRef = reqValidator.get("validator")?.getValue();
        const order = reqValidator.get("order")?.ifNumber()?.getValue() ?? 100;
        if (validatorRef && typeof validatorRef === "string") {
          const validatorRecord = validators.find((v) => v.getId().getValue() === validatorRef);
          if (validatorRecord) {
            const name = validatorRecord.get("name")?.getValue();
            const script = validatorRecord.get("validator_script")?.getValue();
            const validatorSysId = validatorRecord.getId().getValue();
            if (name && typeof name === "string" && script && typeof script === "string") {
              const providerName = name.replace(/ - Request Validator$/, "");
              if (!providerMap.has(providerName)) {
                providerMap.set(providerName, {
                  name: providerName,
                  order,
                  $id: sdk_build_plugins_1.NowIdShape.from(validatorRecord)
                });
              }
              const provider = providerMap.get(providerName);
              provider.preprocessor = script;
              if (!provider.order || order < provider.order) {
                provider.order = order;
              }
            }
          }
        }
      }
      for (const resValidator of responseValidators) {
        const validatorRef = resValidator.get("validator")?.getValue();
        const order = resValidator.get("order")?.ifNumber()?.getValue() || 100;
        if (validatorRef && typeof validatorRef === "string") {
          const validatorRecord = validators.find((v) => v.getId().getValue() === validatorRef);
          if (validatorRecord) {
            const name = validatorRecord.get("name")?.getValue();
            const script = validatorRecord.get("validator_script")?.getValue();
            const validatorSysId = validatorRecord.getId().getValue();
            if (name && typeof name === "string" && script && typeof script === "string") {
              const providerName = name.replace(/ - Response Validator$/, "");
              if (!providerMap.has(providerName)) {
                providerMap.set(providerName, {
                  name: providerName,
                  order,
                  $id: sdk_build_plugins_1.NowIdShape.from(validatorRecord)
                });
              }
              const provider = providerMap.get(providerName);
              provider.postprocessor = script;
              if (!provider.order || order < provider.order) {
                provider.order = order;
              }
            }
          }
        }
      }
      if (providerMap.size > 0) {
        skillSettings.providers = Array.from(providerMap.values()).sort((a, b) => (a.order || 100) - (b.order || 100));
      }
      const securityControls = {};
      let userAccessGenerated = false;
      if (aclRecords.length > 0) {
        const aclRecord = aclRecords[0];
        if (!aclRecord) {
          diagnostics.error(record, "ACL record is undefined during XML-to-Fluent transformation. Defaulting to authenticated user access. This may indicate a data integrity issue.");
        } else {
          const securityAttribute = aclRecord.get("security_attribute")?.getValue();
          const roles = aclRoleRecords.map((aclRoleRecord) => aclRoleRecord.get("sys_user_role")).filter((shape2) => shape2 !== void 0);
          const attrStr = securityAttribute ? String(securityAttribute).toLowerCase() : "";
          const isAuthenticated = attrStr.includes("authenticated") || attrStr === defaults_1.ACL_CONSTANTS.USER_IS_AUTHENTICATED;
          if (isAuthenticated) {
            securityControls.userAccess = {
              $id: sdk_build_plugins_1.NowIdShape.from(aclRecord),
              type: "authenticated",
              ...roles.length > 0 ? { roles } : {}
            };
            userAccessGenerated = true;
          } else if (roles.length > 0) {
            securityControls.userAccess = {
              $id: sdk_build_plugins_1.NowIdShape.from(aclRecord),
              type: "roles",
              roles
            };
            userAccessGenerated = true;
          }
        }
      }
      if (!userAccessGenerated) {
        securityControls.userAccess = {
          type: "authenticated"
        };
      }
      if (roleConfigRecords.length > 0) {
        const roleConfigRecord = roleConfigRecords[0];
        if (!roleConfigRecord) {
          diagnostics.error(record, "Role configuration record is undefined during XML-to-Fluent transformation. Role restrictions will not be set. This may indicate a data integrity issue.");
        } else {
          const roleList = roleConfigRecord.get("role_list")?.getValue();
          if (roleList) {
            const sysIds = typeof roleList === "string" ? roleList.split(",").map((r) => r.trim()).filter(Boolean) : Array.isArray(roleList) ? roleList : [roleList];
            if (sysIds.length > 0) {
              securityControls.roleRestrictions = sysIds;
            }
          }
          const parentSysId = roleConfigRecord.getId().getValue();
          const mappingRows = descendants.query(SYS_AGENT_ACCESS_ROLE_MAPPING).filter((row) => {
            const fkValue = row.get("agent_access_config")?.toString().getValue();
            return typeof fkValue === "string" && fkValue === parentSysId;
          });
          const roleShapes = mappingRows.map((row) => row.get("role")).filter((shape2) => shape2 !== void 0);
          if (roleShapes.length > 0) {
            securityControls.roleMap = roleShapes;
          }
        }
      }
      if (!securityControls.userAccess) {
        securityControls.userAccess = { type: "authenticated" };
      }
      const resourceMappings = descendants.query(SYS_ONE_EXTEND_RESOURCE_MAPPING);
      const attributeMappings = descendants.query(SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING);
      const edges = descendants.query(SYS_ONE_EXTEND_RESOURCE_EDGE);
      const resolvedCapabilities = [...capabilities];
      const resolvedDefinitionAttributes = [...definitionAttributes];
      if (database) {
        const capIds = new Set(capabilities.map((c) => c.getId().getValue()));
        const defAttrIds = new Set(definitionAttributes.map((a) => a.getId().getValue()));
        for (const rm of resourceMappings) {
          const rcId = rm.get("resource_capability")?.getValue();
          if (rcId && !capIds.has(rcId) && rcId !== capabilityId) {
            const cap = database.get(SYS_ONE_EXTEND_CAPABILITY, rcId);
            if (cap) {
              resolvedCapabilities.push(cap);
              capIds.add(rcId);
              const targetDefAttrs = database.query(SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, {
                capability: rcId
              });
              for (const attr of targetDefAttrs) {
                const attrId = attr.getId().getValue();
                if (!defAttrIds.has(attrId)) {
                  resolvedDefinitionAttributes.push(attr);
                  defAttrIds.add(attrId);
                }
              }
            }
          }
        }
      }
      const { tools, unsupportedTools, unsupportedMetrics, supportedToolsToDelete } = (0, tool_processor_1.generateTools)(resourceMappings, attributeMappings, edges, resolvedCapabilities, capabilityDefinitions, capabilityId, resolvedDefinitionAttributes);
      const inputNameMap = /* @__PURE__ */ new Map();
      for (const inp of inputs) {
        const normalized = inp.name.toLowerCase().replace(/ +/g, "_");
        inputNameMap.set(normalized, inp.name);
      }
      const toolNames = new Set(tools?.map((t) => t.name) ?? []);
      if (tools && tools.length > 0) {
        for (const inp of inputs) {
          if (toolNames.has(inp.name)) {
            diagnostics.error(skillConfig, `Skill input name '${inp.name}' conflicts with tool name '${inp.name}'. Input names and tool names must be unique to avoid ambiguous template references.`);
          }
        }
      }
      convertMustachePromptsToArrowFunctions(providers, inputNameMap, toolNames, skillConfig);
      const existingToolVarNames = /* @__PURE__ */ new Map();
      try {
        const sourceShape = skillConfig.getSource?.();
        const sourceNode = sourceShape?.getSource?.();
        const node = sourceNode && typeof sourceNode === "object" && "getKind" in sourceNode ? sourceNode : void 0;
        if (node && sdk_build_core_1.ts.Node.isCallExpression(node)) {
          const configObj = node.getArguments()[0];
          if (configObj && sdk_build_core_1.ts.Node.isObjectLiteralExpression(configObj)) {
            const toolsProp = configObj.getProperty("tools");
            if (toolsProp && sdk_build_core_1.ts.Node.isPropertyAssignment(toolsProp)) {
              const arrowFn = toolsProp.getInitializer();
              if (arrowFn && sdk_build_core_1.ts.Node.isArrowFunction(arrowFn)) {
                for (const stmt of arrowFn.getStatements()) {
                  if (!sdk_build_core_1.ts.Node.isVariableStatement(stmt)) {
                    continue;
                  }
                  const decl = stmt.getDeclarations()[0];
                  if (!decl) {
                    continue;
                  }
                  const varName = decl.getName();
                  const init = decl.getInitializer();
                  if (init && sdk_build_core_1.ts.Node.isCallExpression(init)) {
                    const toolNameArg = init.getArguments()[0];
                    if (toolNameArg && sdk_build_core_1.ts.Node.isStringLiteral(toolNameArg)) {
                      existingToolVarNames.set(toolNameArg.getLiteralValue(), varName);
                    }
                  }
                }
              }
            }
          }
        }
      } catch {
      }
      const toolsArrowFunction = tools?.length > 0 ? (0, tool_processor_1.buildToolsArrowFunctionShape)(tools, skillConfig, inputNameMap, existingToolVarNames) : void 0;
      const stateValue = skillConfig.get("state")?.getValue();
      const isPublished = stateValue === "1";
      const shape = new sdk_build_core_1.CallExpressionShape({
        source: skillConfig,
        callee: "NowAssistSkillConfig",
        args: [
          // Arg 1: Skill definition (metadata, inputs, outputs, tools, security)
          skillConfig.transform(({ $ }) => {
            const skillNameVal = skillConfig.get("name")?.getValue();
            const shortDescVal = capability.get("short_description")?.getValue() || "";
            const descVal = skillConfig.get("description")?.getValue() || capability.get("description")?.getValue() || "";
            const baseObj = {
              $id: $.val(sdk_build_plugins_1.NowIdShape.from(capability)),
              name: $.val(skillNameVal),
              shortDescription: $.val(shortDescVal),
              description: $.val(descVal),
              inputs: $.val(inputs),
              outputs: $.val(outputs)
            };
            if (isPublished) {
              baseObj["state"] = $.val("published");
            }
            if (toolsArrowFunction) {
              baseObj["tools"] = $.val(toolsArrowFunction);
            }
            if (skillSettings.providers && skillSettings.providers.length > 0) {
              const providersArray = skillSettings.providers.map((provider) => {
                const providerObj = {};
                if (provider.$id) {
                  providerObj["$id"] = provider.$id;
                }
                providerObj["name"] = provider.name;
                if (provider.order !== void 0) {
                  providerObj["order"] = provider.order;
                }
                if (provider.preprocessor) {
                  providerObj["preprocessor"] = provider.preprocessor;
                }
                if (provider.postprocessor) {
                  providerObj["postprocessor"] = provider.postprocessor;
                }
                return providerObj;
              });
              baseObj["skillSettings"] = $.val({ providers: providersArray });
            }
            if (securityControls.userAccess) {
              const userAccessObj = {
                type: securityControls.userAccess.type
              };
              if (securityControls.userAccess.$id) {
                userAccessObj["$id"] = securityControls.userAccess.$id;
              }
              if (securityControls.userAccess.roles && securityControls.userAccess.roles.length > 0) {
                userAccessObj["roles"] = securityControls.userAccess.roles;
              }
              const scObj = {
                userAccess: userAccessObj
              };
              if (securityControls.roleRestrictions && securityControls.roleRestrictions.length > 0) {
                scObj["roleRestrictions"] = securityControls.roleRestrictions;
              }
              if (securityControls.roleMap && securityControls.roleMap.length > 0) {
                scObj["roleMap"] = securityControls.roleMap;
              }
              baseObj["securityControls"] = $.val(scObj);
            }
            if (deployConfigData) {
              baseObj["deploymentSettings"] = $.val(deployConfigData);
            }
            return baseObj;
          }),
          // Arg 2: Prompt configuration (providers, defaultProvider)
          skillConfig.transform(({ $ }) => {
            const baseObj = {
              providers: $.val(providers)
            };
            if (providers.length > 1) {
              baseObj["defaultProvider"] = $.val(defaultProviderName);
            }
            return baseObj;
          })
        ]
      });
      const excludedMetricIds = new Set(excludedRecords.filter((r) => r.getTable() === SYS_ONE_EXTEND_RESOURCE_MAPPING).map((r) => r.getId().getValue()));
      const excludedMetricAttrMappings = attributeMappings.filter((a) => excludedMetricIds.has(a.get("resource_mapping")?.getValue()));
      if (unsupportedTools.length > 0) {
        const toolDefinitionAttributes = definitionAttributes.filter((attr) => attr.get("capability")?.getValue() !== capabilityId);
        const unhandled = [
          ...resourceMappings,
          ...attributeMappings,
          ...edges,
          ...toolDefinitionAttributes,
          ...supportedToolsToDelete
        ];
        return {
          success: "partial",
          value: shape,
          unhandledRecords: [...unhandled, ...excludedRecords]
        };
      }
      if (unsupportedMetrics.length > 0) {
        const metricIds = new Set(unsupportedMetrics.map((m) => m.getId().getValue()));
        const metricAttrMappings = attributeMappings.filter((a) => metricIds.has(a.get("resource_mapping")?.getValue()));
        return {
          success: "partial",
          value: shape,
          unhandledRecords: [
            ...unsupportedMetrics,
            ...metricAttrMappings,
            ...supportedToolsToDelete,
            ...excludedRecords,
            ...excludedMetricAttrMappings
          ]
        };
      }
      if (supportedToolsToDelete.length > 0) {
        return {
          success: "partial",
          value: shape,
          unhandledRecords: [...supportedToolsToDelete, ...excludedRecords, ...excludedMetricAttrMappings]
        };
      }
      if (excludedRecords.length > 0) {
        return {
          success: "partial",
          value: shape,
          unhandledRecords: [...excludedRecords, ...excludedMetricAttrMappings]
        };
      }
      return {
        success: true,
        value: shape
      };
    }
    __name(transformToShape, "transformToShape");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/now-assist-skill-plugin.js
var require_now_assist_skill_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/now-assist-skill-kit/now-assist-skill-plugin.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.NowAssistSkillPlugin = void 0;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var defaults_1 = require_defaults();
    var skill_processor_1 = require_skill_processor();
    var transform_logic_1 = require_transform_logic();
    var skill_plugin_helpers_1 = require_skill_plugin_helpers();
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var { SN_NOWASSIST_SKILL_CONFIG, SN_NOWASSIST_SKILL_CONFIG_STATUS, SN_NOWASSIST_SKILL_CONFIG_VAR_SET, SN_NOWASSIST_SKILL_CONFIG_APPLICABILITY, SN_NOWASSIST_SKILL_CONFIG_UI, SYS_GENERATIVE_AI_PROMPT_CONFIG, SYS_GENERATIVE_AI_CONFIG, SYS_GENERATIVE_AI_VALIDATOR, SYS_GENERATIVE_AI_REQUEST_VALIDATOR, SYS_GENERATIVE_AI_RESPONSE_VALIDATOR, SYS_GEN_AI_FEATURE_MAPPING, SYS_GEN_AI_STRATEGY_MAPPING, SYS_GEN_AI_SKILL, SYS_GEN_AI_SKILL_APPLICABILITY, SYS_GEN_AI_SKILL_CONFIG, SYS_ONE_EXTEND_CAPABILITY, SYS_ONE_EXTEND_CAPABILITY_DEFINITION, SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE, SYS_ONE_EXTEND_DEFINITION_CONFIG, SYS_VARIABLE_VALUE, SYS_ONE_EXTEND_RESOURCE_MAPPING, SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING, SYS_ONE_EXTEND_RESOURCE_EDGE } = defaults_1.TABLE_NAMES;
    var { SYS_AGENT_ACCESS_ROLE_CONFIGURATION, SYS_AGENT_ACCESS_ROLE_MAPPING, SYS_USER_ROLE } = defaults_1.SECURITY_TABLE_NAMES;
    function parseRecordCall(callExpr) {
      const expression = callExpr.getExpression();
      if (!sdk_build_core_1.ts.Node.isIdentifier(expression) || expression.getText() !== "Record") {
        return null;
      }
      const args = callExpr.getArguments();
      if (args.length === 0) {
        return null;
      }
      const configArg = args[0];
      if (!sdk_build_core_1.ts.Node.isObjectLiteralExpression(configArg)) {
        return null;
      }
      const tableProperty = configArg.getProperty("table");
      if (!tableProperty || !sdk_build_core_1.ts.Node.isPropertyAssignment(tableProperty)) {
        return null;
      }
      const tableValue = tableProperty.getInitializer();
      if (!tableValue || !sdk_build_core_1.ts.Node.isStringLiteral(tableValue)) {
        return null;
      }
      const dataProperty = configArg.getProperty("data");
      let dataObj;
      if (dataProperty && sdk_build_core_1.ts.Node.isPropertyAssignment(dataProperty)) {
        const init = dataProperty.getInitializer();
        if (init && sdk_build_core_1.ts.Node.isObjectLiteralExpression(init)) {
          dataObj = init;
        }
      }
      return {
        tableName: tableValue.getLiteralValue(),
        configObj: configArg,
        dataObj,
        filePath: callExpr.getSourceFile().getFilePath()
      };
    }
    __name(parseRecordCall, "parseRecordCall");
    function getStringField(obj, fieldName) {
      const prop = obj.getProperty(fieldName);
      if (!prop || !sdk_build_core_1.ts.Node.isPropertyAssignment(prop)) {
        return void 0;
      }
      const init = prop.getInitializer();
      if (!init || !sdk_build_core_1.ts.Node.isStringLiteral(init)) {
        return void 0;
      }
      return init.getLiteralValue();
    }
    __name(getStringField, "getStringField");
    function detectRecordApiToolRecords(compiler, skillCapabilityId) {
      const toolFilePaths = /* @__PURE__ */ new Set();
      const metricMappingIds = /* @__PURE__ */ new Set();
      const skillToolMappingIds = /* @__PURE__ */ new Set();
      const attributeMappingCandidates = [];
      const edgeCandidates = [];
      const sourceFiles = compiler.getSourceFiles().filter((sf) => {
        const fp = sf.getFilePath();
        if (!fp.includes("/fluent/") || !fp.endsWith(".now.ts")) {
          return false;
        }
        return fp.includes("sys_one_extend_resource_mapping") || fp.includes("sys_one_extend_resource_attribute_mapping") || fp.includes("sys_one_extend_resource_edge");
      });
      for (const sourceFile of sourceFiles) {
        const callExpressions = sourceFile.getDescendantsOfKind(sdk_build_core_1.ts.SyntaxKind.CallExpression);
        for (const callExpr of callExpressions) {
          const parsed = parseRecordCall(callExpr);
          if (!parsed) {
            continue;
          }
          const { tableName, configObj, dataObj, filePath } = parsed;
          if (tableName === SYS_ONE_EXTEND_RESOURCE_MAPPING && dataObj) {
            const resourceCapability = getStringField(dataObj, "resource_capability");
            const parentCapability = getStringField(dataObj, "parent_capability");
            if (resourceCapability && parentCapability && resourceCapability === parentCapability) {
              continue;
            }
            if (skillCapabilityId && parentCapability !== skillCapabilityId) {
              continue;
            }
            if (resourceCapability && defaults_1.EXCLUDED_METRIC_CAPABILITY_IDS.has(resourceCapability)) {
              const $idProp = configObj.getProperty("$id");
              if ($idProp && sdk_build_core_1.ts.Node.isPropertyAssignment($idProp)) {
                const idInit = $idProp.getInitializer();
                if (idInit && sdk_build_core_1.ts.Node.isElementAccessExpression(idInit)) {
                  const arg = idInit.getArgumentExpression();
                  if (arg && sdk_build_core_1.ts.Node.isStringLiteral(arg)) {
                    metricMappingIds.add(arg.getLiteralValue());
                  }
                }
              }
            } else {
              toolFilePaths.add(filePath);
              const $idProp = configObj.getProperty("$id");
              if ($idProp && sdk_build_core_1.ts.Node.isPropertyAssignment($idProp)) {
                const idInit = $idProp.getInitializer();
                if (idInit && sdk_build_core_1.ts.Node.isElementAccessExpression(idInit)) {
                  const arg = idInit.getArgumentExpression();
                  if (arg && sdk_build_core_1.ts.Node.isStringLiteral(arg)) {
                    skillToolMappingIds.add(arg.getLiteralValue());
                  }
                }
              }
            }
          } else if (tableName === SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING && dataObj) {
            const resourceMapping = getStringField(dataObj, "resource_mapping");
            if (resourceMapping) {
              attributeMappingCandidates.push({ filePath, resourceMapping });
            }
          } else if (tableName === SYS_ONE_EXTEND_RESOURCE_EDGE && dataObj) {
            const fromResource = getStringField(dataObj, "from_resource") || "";
            const toResource = getStringField(dataObj, "to_resource") || "";
            edgeCandidates.push({ filePath, fromResource, toResource });
          }
        }
      }
      if (skillCapabilityId && skillToolMappingIds.size === 0) {
        return [...toolFilePaths];
      }
      for (const { filePath, resourceMapping } of attributeMappingCandidates) {
        if (metricMappingIds.has(resourceMapping)) {
          continue;
        }
        if (skillCapabilityId && !skillToolMappingIds.has(resourceMapping)) {
          continue;
        }
        toolFilePaths.add(filePath);
      }
      for (const { filePath, fromResource, toResource } of edgeCandidates) {
        if (skillCapabilityId) {
          if (!skillToolMappingIds.has(fromResource) && !skillToolMappingIds.has(toResource)) {
            continue;
          }
        }
        toolFilePaths.add(filePath);
      }
      return [...toolFilePaths];
    }
    __name(detectRecordApiToolRecords, "detectRecordApiToolRecords");
    exports2.NowAssistSkillPlugin = sdk_build_core_1.Plugin.create({
      name: "NowAssistSkillPlugin",
      docs: [
        {
          apiName: "NowAssistSkillConfig",
          tags: ["sn_nowassist_skill_config"]
        }
      ],
      records: {
        // User's $id maps to this record, controlling identity via Now.ID['my_skill']
        // No coalesce needed since uniqueness is determined by user-provided $id
        [SN_NOWASSIST_SKILL_CONFIG]: {
          coalesce: ["skill_id"],
          relationships: {
            // SYS_ONE_EXTEND_CAPABILITY is the parent record that skill_id references
            // Using inverse: true since SN_NOWASSIST_SKILL_CONFIG.skill_id points TO capability
            [SYS_ONE_EXTEND_CAPABILITY]: {
              via: "skill_id",
              inverse: true,
              descendant: true,
              relationships: {
                // Capability Definition (defines HOW skill operates)
                [SYS_ONE_EXTEND_CAPABILITY_DEFINITION]: {
                  via: "capability",
                  descendant: true,
                  relationships: {
                    [SYS_ONE_EXTEND_DEFINITION_CONFIG]: {
                      via: "definition",
                      descendant: true
                    },
                    [SYS_GENERATIVE_AI_CONFIG]: {
                      via: "definition",
                      descendant: true,
                      relationships: {
                        [SYS_GENERATIVE_AI_PROMPT_CONFIG]: {
                          via: "ai_config",
                          descendant: true
                        }
                      }
                    },
                    // Validators (preprocessors/postprocessors)
                    [SYS_GENERATIVE_AI_REQUEST_VALIDATOR]: {
                      via: "document_id",
                      descendant: true,
                      relationships: {
                        [SYS_GENERATIVE_AI_VALIDATOR]: {
                          via: "validator",
                          inverse: true,
                          descendant: true
                        }
                      }
                    },
                    [SYS_GENERATIVE_AI_RESPONSE_VALIDATOR]: {
                      via: "definition",
                      descendant: true,
                      relationships: {
                        [SYS_GENERATIVE_AI_VALIDATOR]: {
                          via: "validator",
                          inverse: true,
                          descendant: true
                        }
                      }
                    }
                  }
                },
                // Definition Attributes (input/output fields)
                [SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE]: {
                  via: "capability",
                  descendant: true
                },
                // NAP Deployment
                [SYS_GEN_AI_FEATURE_MAPPING]: {
                  via: "document",
                  descendant: true,
                  relationships: {
                    [SYS_GEN_AI_STRATEGY_MAPPING]: {
                      via: "feature",
                      descendant: true
                    }
                  }
                },
                // Tool tables: resource mappings, attribute mappings, edges
                // Filter out metric resource mapping records
                [SYS_ONE_EXTEND_RESOURCE_MAPPING]: {
                  via: "parent_capability",
                  descendant: true,
                  relationships: {
                    [SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING]: {
                      via: "resource_mapping",
                      descendant: true
                    },
                    [SYS_ONE_EXTEND_RESOURCE_EDGE]: {
                      via: "from_resource",
                      descendant: true
                    },
                    [SYS_ONE_EXTEND_CAPABILITY]: {
                      via: "resource_capability",
                      descendant: true,
                      inverse: true
                    }
                  }
                },
                [SYS_GEN_AI_SKILL]: {
                  via: "skill_document",
                  descendant: true,
                  relationships: {
                    [SYS_GEN_AI_SKILL_APPLICABILITY]: {
                      via: "skill_id",
                      descendant: true
                    },
                    [SYS_GEN_AI_SKILL_CONFIG]: {
                      via: "skill_id",
                      descendant: true
                    }
                  }
                }
              }
            },
            // Direct children of SN_NOWASSIST_SKILL_CONFIG
            [SN_NOWASSIST_SKILL_CONFIG_STATUS]: {
              via: "skill_config",
              descendant: true
            },
            [SN_NOWASSIST_SKILL_CONFIG_VAR_SET]: {
              via: "skill_config",
              descendant: true,
              relationships: {
                [SYS_VARIABLE_VALUE]: {
                  via: "document_key",
                  descendant: true,
                  relationships: {
                    sys_ui_action: {
                      via: [{ parent: ["value", "(.+)"], child: ["sys_id", "(.+)"] }],
                      descendant: true,
                      relationships: {
                        sys_ux_form_action: {
                          via: "ui_action",
                          descendant: true
                        }
                      }
                    }
                  }
                }
              }
            },
            [SN_NOWASSIST_SKILL_CONFIG_APPLICABILITY]: {
              via: "skill_config",
              descendant: true
            },
            [SN_NOWASSIST_SKILL_CONFIG_UI]: {
              via: "skill_config",
              descendant: true
            },
            // Security: ACL and role configuration
            sys_security_acl: {
              via: [{ parent: ["internal_name", "\\.(.*)"], child: ["name", "\\.(.*)"] }],
              descendant: true,
              relationships: {
                sys_security_acl_role: {
                  via: "sys_security_acl",
                  descendant: true
                }
              }
            },
            [SYS_AGENT_ACCESS_ROLE_CONFIGURATION]: {
              via: "agent",
              descendant: true,
              relationships: {
                [SYS_AGENT_ACCESS_ROLE_MAPPING]: {
                  via: "agent_access_config",
                  descendant: true,
                  relationships: {
                    [SYS_USER_ROLE]: {
                      via: "role",
                      inverse: true
                    }
                  }
                }
              }
            }
          },
          async toShape(record, { descendants, transform, diagnostics, database }) {
            return (0, transform_logic_1.transformToShape)(record, descendants, transform, diagnostics, database);
          },
          async toFile(record, context) {
            const { config, transform, descendants, self, diagnostics, database } = context;
            const allDescendants = self.getDescendants(record, database);
            const skillName = record.get("name")?.toString().getValue();
            const sysId = record.getId().getValue();
            if (!skillName) {
              return { success: false };
            }
            const sanitizedName = (0, skill_plugin_helpers_1.sanitizeSkillName)(skillName);
            const internalName = `${sanitizedName}.${sysId}`;
            const recordBuilder = (0, sdk_build_core_1.unloadBuilder)(config);
            const updateName = await transform.getUpdateName(record);
            const builder = recordBuilder.record(record, updateName);
            record.entries().sort(([a], [b]) => a.localeCompare(b)).forEach(([prop, shape]) => {
              builder.field(prop, shape);
            });
            builder.field("internal_name", internalName);
            const serializeDescendant = /* @__PURE__ */ __name(async (descendantRecord) => {
              const descUpdateName = await transform.getUpdateName(descendantRecord);
              const descBuilder = (0, sdk_build_core_1.unloadBuilder)(config);
              const descRecordBuilder = descBuilder.record(descendantRecord, descUpdateName);
              descendantRecord.entries().sort(([a], [b]) => a.localeCompare(b)).forEach(([prop, shape]) => {
                descRecordBuilder.field(prop, shape);
              });
              return {
                source: descendantRecord,
                name: `${descUpdateName}.xml`,
                category: descendantRecord.getInstallCategory(),
                content: descBuilder.end()
              };
            }, "serializeDescendant");
            const descendantFiles = [];
            for (const rec of descendants.query(SYS_ONE_EXTEND_CAPABILITY)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_ONE_EXTEND_CAPABILITY_DEFINITION)) {
              const { self: _, ...contextWithoutSelf } = context;
              const result = await self.recordToFile(rec, contextWithoutSelf);
              if (result.success && result.value) {
                const files = Array.isArray(result.value) ? result.value : [result.value];
                descendantFiles.push(...files);
              } else {
                descendantFiles.push(await serializeDescendant(rec));
              }
            }
            for (const rec of descendants.query(SYS_ONE_EXTEND_DEFINITION_CONFIG)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_GENERATIVE_AI_CONFIG)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_GENERATIVE_AI_PROMPT_CONFIG)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_GENERATIVE_AI_VALIDATOR)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_GENERATIVE_AI_REQUEST_VALIDATOR)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_GENERATIVE_AI_RESPONSE_VALIDATOR)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_GEN_AI_FEATURE_MAPPING)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_GEN_AI_STRATEGY_MAPPING)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_GEN_AI_SKILL)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_GEN_AI_SKILL_APPLICABILITY)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_GEN_AI_SKILL_CONFIG)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SN_NOWASSIST_SKILL_CONFIG_STATUS)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const varSetRec of descendants.query(SN_NOWASSIST_SKILL_CONFIG_VAR_SET)) {
              descendantFiles.push(await serializeDescendant(varSetRec));
            }
            for (const rec of descendants.query(SYS_VARIABLE_VALUE)) {
              const { self: _, ...contextWithoutSelf } = context;
              const result = await self.recordToFile(rec, contextWithoutSelf);
              if (result.success && result.value) {
                const files = Array.isArray(result.value) ? result.value : [result.value];
                descendantFiles.push(...files);
              } else {
                descendantFiles.push(await serializeDescendant(rec));
              }
            }
            for (const rec of descendants.query(SN_NOWASSIST_SKILL_CONFIG_APPLICABILITY)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SN_NOWASSIST_SKILL_CONFIG_UI)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query("sys_ui_action")) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query("sys_ux_form_action")) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query("sys_security_acl")) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query("sys_security_acl_role")) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            const skillAclIds = new Set(descendants.query("sys_security_acl").map((r) => r.getId().getValue()));
            for (const rec of database.query("sys_security_acl_role")) {
              if (rec.getAction() === "DELETE") {
                const aclRef = rec.get("sys_security_acl")?.toString().getValue();
                if (aclRef && skillAclIds.has(aclRef)) {
                  descendantFiles.push(await serializeDescendant(rec));
                }
              }
            }
            for (const rec of descendants.query(SYS_AGENT_ACCESS_ROLE_CONFIGURATION)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_AGENT_ACCESS_ROLE_MAPPING)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            const allResourceMappings = descendants.query(SYS_ONE_EXTEND_RESOURCE_MAPPING);
            let hasFluentApiTools = false;
            let hasRecordApiTools = false;
            const recordApiToolFilePaths = /* @__PURE__ */ new Set();
            for (const mapping of allResourceMappings) {
              const resourceCapId = mapping.get("resource_capability")?.toString().getValue();
              const parentCapId = mapping.get("parent_capability")?.toString().getValue();
              if (resourceCapId === parentCapId) {
                continue;
              }
              if (resourceCapId && defaults_1.EXCLUDED_METRIC_CAPABILITY_IDS.has(resourceCapId)) {
                continue;
              }
              let isFluentApiTool = false;
              let isRecordApiTool = false;
              try {
                const originalNode = mapping.getOriginalNode();
                if (sdk_build_core_1.ts.Node.isObjectLiteralExpression(originalNode)) {
                  isFluentApiTool = true;
                } else if (sdk_build_core_1.ts.Node.isCallExpression(originalNode)) {
                  const expression = originalNode.getExpression();
                  if (sdk_build_core_1.ts.Node.isIdentifier(expression) && expression.getText() === "Record") {
                    isRecordApiTool = true;
                  }
                }
              } catch {
                isRecordApiTool = true;
              }
              if (isRecordApiTool) {
                hasRecordApiTools = true;
                try {
                  recordApiToolFilePaths.add(mapping.getOriginalFilePath());
                } catch {
                }
              } else if (isFluentApiTool) {
                hasFluentApiTools = true;
              }
            }
            if (hasFluentApiTools && hasRecordApiTools) {
              const rootDir = context.project.getRootDir();
              const relativeFiles = [...recordApiToolFilePaths].map((f) => sdk_build_core_1.path.relative(rootDir, f));
              const fileList = relativeFiles.map((f) => `  - ${f}`).join("\n");
              diagnostics.error(record, "Build conflict: Tools are defined in both the Fluent API and as Record API files.\nThe following Record API files contain tool records:\n" + fileList + "\n\nTo resolve this conflict:\n  1. If these files are leftover from a previous transform that included unsupported tools,\n     delete the files listed above and rebuild.\n  2. If unsupported tools (e.g., DocIntel, PI, Retriever) are still present in the skill,\n     remove them from the Now Assist Skill Kit UI, then re-transform before defining\n     tools in the Fluent API. Alternatively, remove the `tools` property from the\n     Fluent API to continue using Record API tools.");
            }
            for (const rec of allResourceMappings) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            for (const rec of descendants.query(SYS_ONE_EXTEND_RESOURCE_EDGE)) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            const serializedIds = new Set(descendantFiles.map((f) => f.source?.getId()?.getValue()).filter(Boolean));
            for (const rec of allDescendants) {
              if (rec.getAction() === "DELETE" && !serializedIds.has(rec.getId().getValue())) {
                descendantFiles.push(await serializeDescendant(rec));
              }
            }
            return {
              success: true,
              value: [
                {
                  source: record,
                  name: `${updateName}.xml`,
                  category: record.getInstallCategory(),
                  content: recordBuilder.end()
                },
                ...descendantFiles
              ]
            };
          }
        },
        // ===========================
        // sys_one_extend_* Tables
        // ===========================
        [SYS_ONE_EXTEND_CAPABILITY]: {
          // No coalesce needed - identity managed via derived explicitId in skill-processor.ts
          // ('{skillKey}__capability'). As platform allows duplicate/mutable names.
          //
          // During transform, the capability's explicit ID is registered in keys.ts via the
          // toFile handler (see above). This ensures the capability's sys_id is preserved
          // through transform → build → deploy cycles, preventing orphaned skills.
        },
        [SYS_ONE_EXTEND_DEFINITION_CONFIG]: {
          coalesce: ["definition", "capability"]
        },
        [SYS_ONE_EXTEND_DEFINITION_ATTRIBUTE]: {
          // No coalesce needed - identity managed via required $id (explicitId) on inputs/outputs.
          // Using 'name' in coalesce caused duplicates when users renamed attributes, since
          // the internal name changes (e.g., 'userRequest' → 'userrequest' → 'user_request').
          // The $id provides stable sys_id identity through rename operations.
        },
        // ===========================
        // sys_gen_ai_* Tables
        // ===========================
        [SYS_GENERATIVE_AI_CONFIG]: {},
        [SYS_GENERATIVE_AI_PROMPT_CONFIG]: {
          coalesce: ["ai_config"]
        },
        [SYS_GENERATIVE_AI_VALIDATOR]: {
          // No coalesce needed - identity managed via explicitId in provider-processor.ts
          // ('{definitionId}_request' or '{definitionId}_response'). This ensures validators
          // are updated (not duplicated) when provider names change.
        },
        [SYS_GENERATIVE_AI_REQUEST_VALIDATOR]: {
          coalesce: ["document_id", "document_table", "validator"]
        },
        [SYS_GENERATIVE_AI_RESPONSE_VALIDATOR]: {
          coalesce: ["definition", "definition_table", "validator"]
        },
        [SYS_GEN_AI_FEATURE_MAPPING]: {
          coalesce: ["feature_name", "document"]
        },
        [SYS_GEN_AI_STRATEGY_MAPPING]: {
          coalesce: ["strategy", "feature"]
        },
        [SYS_GEN_AI_SKILL]: {
          coalesce: ["skill_document", "skill_table"]
        },
        [SYS_GEN_AI_SKILL_APPLICABILITY]: {
          coalesce: /* @__PURE__ */ __name((properties) => ({
            skill_id: properties.get("skill_id").getValue()
          }), "coalesce")
        },
        [SYS_GEN_AI_SKILL_CONFIG]: {
          coalesce: /* @__PURE__ */ __name((properties) => ({
            skill_id: properties.get("skill_id").getValue()
          }), "coalesce")
        },
        // ===========================
        // sn_nowassist_* Tables
        // ===========================
        [SN_NOWASSIST_SKILL_CONFIG_STATUS]: {
          coalesce: ["skill_config"]
        },
        [SN_NOWASSIST_SKILL_CONFIG_APPLICABILITY]: {
          coalesce: ["skill_config"]
        },
        [SN_NOWASSIST_SKILL_CONFIG_VAR_SET]: {
          coalesce: ["name", "skill_config", "config_type"]
        },
        // ROLE MASK TABLE
        [SYS_AGENT_ACCESS_ROLE_CONFIGURATION]: {
          coalesce: ["agent", "agent_table", "action"]
        },
        [SYS_AGENT_ACCESS_ROLE_MAPPING]: {
          coalesce: ["agent_access_config", "role"]
        },
        // ===========================
        // Tool Tables
        // ===========================
        [SYS_ONE_EXTEND_RESOURCE_MAPPING]: {
          coalesce: /* @__PURE__ */ __name((properties) => {
            const parentCap = properties.get("parent_capability")?.getValue();
            const resourceCap = properties.get("resource_capability")?.getValue();
            if (parentCap && parentCap === resourceCap) {
              return {
                parent_capability: parentCap,
                resource_capability: resourceCap
              };
            }
            return {
              parent_capability: parentCap,
              resource_capability: resourceCap,
              resource_name: properties.get("resource_name")?.getValue()
            };
          }, "coalesce")
        },
        [SYS_ONE_EXTEND_RESOURCE_ATTRIBUTE_MAPPING]: {
          coalesce: ["resource_mapping", "resource_attribute_name"]
        },
        [SYS_ONE_EXTEND_RESOURCE_EDGE]: {
          coalesce: ["internal_name"]
        },
        // Custom toFile handler for capability definitions to resolve scriptId in metadata
        [SYS_ONE_EXTEND_CAPABILITY_DEFINITION]: {
          coalesce: ["capability", "api"],
          async toFile(record, { config, database, transform, descendants, diagnostics }) {
            const metadataField = record.get("metadata");
            if (!metadataField || !metadataField.getValue()) {
              return { success: false };
            }
            const metadataStr = metadataField.toString().getValue();
            let metadata;
            try {
              metadata = JSON.parse(metadataStr);
            } catch (e) {
              return { success: false };
            }
            if (metadata.scriptId) {
              const scriptIdValue = String(metadata.scriptId);
              if ((0, sdk_build_core_1.isGUID)(scriptIdValue)) {
              } else {
                const scriptIdKey = scriptIdValue;
                const scriptIncludeRecords = database.query("sys_script_include");
                const scriptInclude = scriptIncludeRecords.find((r) => {
                  const recordId = r.getId();
                  const recordIdKey = recordId.getNowIdKey?.();
                  return recordIdKey === scriptIdKey;
                });
                if (!scriptInclude) {
                  diagnostics.error(record, `Invalid scriptId '${scriptIdValue}' in capability definition metadata. Must be either a valid sys_id (GUID) or a resolvable Now.ID key like Now.ID['script_include_name']. The invalid scriptId will be preserved in the XML and may cause runtime errors.`);
                  return { success: false };
                }
                metadata.scriptId = scriptInclude.getId().getValue();
                const resolvedMetadataStr = JSON.stringify(metadata);
                const recordBuilder = (0, sdk_build_core_1.unloadBuilder)(config);
                const updateName = await transform.getUpdateName(record);
                const builder = recordBuilder.record(record, updateName);
                record.entries().sort(([a], [b]) => a.localeCompare(b)).forEach(([prop, shape]) => {
                  if (prop === "metadata") {
                    builder.field(prop, resolvedMetadataStr);
                  } else {
                    builder.field(prop, shape);
                  }
                });
                const definitionConfigs = descendants.query(SYS_ONE_EXTEND_DEFINITION_CONFIG);
                const descendantFiles = [];
                for (const configRecord of definitionConfigs) {
                  const configUpdateName = await transform.getUpdateName(configRecord);
                  const configBuilder = (0, sdk_build_core_1.unloadBuilder)(config);
                  const configRecordBuilder = configBuilder.record(configRecord, configUpdateName);
                  configRecord.entries().sort(([a], [b]) => a.localeCompare(b)).forEach(([prop, shape]) => {
                    configRecordBuilder.field(prop, shape);
                  });
                  descendantFiles.push({
                    source: configRecord,
                    name: `${configUpdateName}.xml`,
                    category: configRecord.getInstallCategory(),
                    content: configBuilder.end()
                  });
                }
                return {
                  success: true,
                  value: [
                    {
                      source: record,
                      name: `${updateName}.xml`,
                      category: record.getInstallCategory(),
                      content: recordBuilder.end()
                    },
                    ...descendantFiles
                  ]
                };
              }
            }
            return { success: false };
          }
        },
        // Custom toFile handler for variable values to resolve UIAction references
        [SYS_VARIABLE_VALUE]: {
          coalesce: ["document_key", "variable"],
          async toFile(record, { config, database, transform }) {
            const variable = record.get("variable")?.toString().getValue();
            const value = record.get("value")?.toString().getValue();
            if (variable === defaults_1.VAR_SET.UI_ACTION_ID && value && !(0, sdk_build_core_1.isGUID)(value)) {
              const uiActionRecords = database.query("sys_ui_action");
              const uiAction = uiActionRecords.find((r) => {
                const recordId = r.getId();
                const recordIdValue = recordId.getValue();
                const recordIdKey = recordId.getNowIdKey?.();
                return recordIdValue === value || recordIdKey === value;
              });
              if (uiAction) {
                const resolvedValue = uiAction.getId().getValue();
                const recordBuilder = (0, sdk_build_core_1.unloadBuilder)(config);
                const updateName = await transform.getUpdateName(record);
                const builder = recordBuilder.record(record, updateName);
                record.entries().sort(([a], [b]) => a.localeCompare(b)).forEach(([prop, shape]) => {
                  if (prop === "value") {
                    builder.field(prop, resolvedValue);
                  } else {
                    builder.field(prop, shape);
                  }
                });
                return {
                  success: true,
                  value: {
                    source: record,
                    name: `${updateName}.xml`,
                    category: record.getInstallCategory(),
                    content: recordBuilder.end()
                  }
                };
              }
            }
            return { success: false };
          }
        },
        sys_ui_action: {
          // No coalesce needed - identity managed via $id (Now.ID) explicitId from UIActionConfig
        },
        sys_ux_form_action: {
          coalesce: ["ui_action"]
        }
      },
      shapes: [
        {
          shape: sdk_build_core_1.CallExpressionShape,
          fileTypes: ["fluent"],
          async toRecord(callExpression, context) {
            const { diagnostics, compiler } = context;
            const calleeName = callExpression.getCallee()?.toString();
            if (calleeName !== "NowAssistSkillConfig") {
              return { success: false };
            }
            const definitionArg = callExpression.getArgument(0);
            if (!definitionArg?.isObject()) {
              diagnostics.error(callExpression, `${calleeName} requires a definition object as the first argument`);
              return { success: false };
            }
            const promptConfigArg = callExpression.getArgument(1);
            if (!promptConfigArg?.isObject()) {
              diagnostics.error(callExpression, `${calleeName} requires a prompt configuration object as the second argument`);
              return { success: false };
            }
            const toolsShape = definitionArg.get("tools");
            const hasFluentApiTools = toolsShape && !toolsShape.isUndefined();
            if (hasFluentApiTools) {
              const $idShape = definitionArg.get("$id");
              const skillCapabilityId = $idShape?.getValue()?.toString();
              const staleToolFiles = detectRecordApiToolRecords(compiler, skillCapabilityId);
              if (staleToolFiles.length > 0) {
                const rootDir = context.project.getRootDir();
                const relativeFiles = staleToolFiles.map((f) => sdk_build_core_1.path.relative(rootDir, f));
                const fileList = relativeFiles.map((f) => `  - ${f}`).join("\n");
                diagnostics.error(callExpression, "Build conflict: Tools are defined in both the Fluent API and as Record API files.\nThe following Record API files contain tool records:\n" + fileList + "\n\nTo resolve this conflict:\n  1. If these files are leftover from a previous transform that included unsupported tools,\n     delete the files listed above and rebuild.\n  2. If unsupported tools (e.g., DocIntel, PI, Retriever) are still present in the skill,\n     remove them from the Now Assist Skill Kit UI, then re-transform before defining\n     tools in the Fluent API. Alternatively, remove the `tools` property from the\n     Fluent API to continue using Record API tools.");
                return { success: false };
              }
            }
            return (0, skill_processor_1.processSkill)(callExpression, definitionArg, promptConfigArg, context);
          }
        },
        {
          shape: sdk_build_plugins_1.ArrowFunctionShape,
          async commit(shape, target, { commit }) {
            const creator = shape.getSource().getCreator();
            if (!creator || creator.getName() !== "NowAssistSkillPlugin") {
              return { success: false };
            }
            const arrowFunction = sdk_build_core_1.ts.Node.isArrowFunction(target) ? target : target.replaceWithText("() => {}").asKindOrThrow(sdk_build_core_1.ts.SyntaxKind.ArrowFunction);
            const nodeStatements = arrowFunction.getStatements().filter((s) => !sdk_build_core_1.ts.Node.isReturnStatement(s));
            const shapeStatements = shape.getStatements();
            const returnValue = shape.getReturnValue();
            const totalExpected = shapeStatements.length;
            const excess = nodeStatements.length - totalExpected;
            for (let i = 1; i <= excess; i++) {
              arrowFunction.removeStatement(nodeStatements.length - i);
            }
            for (const [i, statement] of shapeStatements.entries()) {
              const targetStatement = nodeStatements[i] ?? arrowFunction.addStatements("undefined")[0];
              await commit(statement, targetStatement);
            }
            const targetReturn = arrowFunction.getStatements().find((s) => sdk_build_core_1.ts.Node.isNode(s) && sdk_build_core_1.ts.Node.isReturnStatement(s));
            const targetReturnIdx = arrowFunction.getStatements().findIndex((s) => sdk_build_core_1.ts.Node.isNode(s) && sdk_build_core_1.ts.Node.isReturnStatement(s));
            if (returnValue) {
              if (targetReturn) {
                arrowFunction.removeStatement(targetReturnIdx);
              }
              arrowFunction.addStatements(`return  ${returnValue.getCode()}`);
            }
            return { success: true };
          }
        }
      ]
    });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/index.js
var require_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/plugin/index.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.NowAssistSkillPlugin = void 0;
    var now_assist_skill_plugin_1 = require_now_assist_skill_plugin();
    Object.defineProperty(exports2, "NowAssistSkillPlugin", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return now_assist_skill_plugin_1.NowAssistSkillPlugin;
    }, "get") });
    exports2.default = now_assist_skill_plugin_1.NowAssistSkillPlugin;
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/api/nowassist/nowassist.js
var require_nowassist = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/api/nowassist/nowassist.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.NowAssistSkillConfig = NowAssistSkillConfig;
    function NowAssistSkillConfig(definition, promptConfig) {
      return [definition, promptConfig];
    }
    __name(NowAssistSkillConfig, "NowAssistSkillConfig");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/api/nowassist/index.js
var require_nowassist2 = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/api/nowassist/index.js"(exports2) {
    "use strict";
    var __createBinding2 = exports2 && exports2.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
          return m[k];
        }, "get") };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar2 = exports2 && exports2.__exportStar || function(m, exports3) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports3, p)) __createBinding2(exports3, m, p);
    };
    Object.defineProperty(exports2, "__esModule", { value: true });
    __exportStar2(require_nowassist(), exports2);
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/api/index.js
var require_api = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/api/index.js"(exports2) {
    "use strict";
    var __createBinding2 = exports2 && exports2.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
          return m[k];
        }, "get") };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar2 = exports2 && exports2.__exportStar || function(m, exports3) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports3, p)) __createBinding2(exports3, m, p);
    };
    Object.defineProperty(exports2, "__esModule", { value: true });
    __exportStar2(require_nowassist2(), exports2);
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-nowassist-skill-plugin@1.0.21-ext-nask-plugins.38_@servicenow+sdk-build_028472842c8824addef9c074bfbb7f93/node_modules/@servicenow/sdk-nowassist-skill-plugin/dist/index.js
var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
  if (k2 === void 0) k2 = k;
  var desc = Object.getOwnPropertyDescriptor(m, k);
  if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
    desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return m[k];
    }, "get") };
  }
  Object.defineProperty(o, k2, desc);
} : function(o, m, k, k2) {
  if (k2 === void 0) k2 = k;
  o[k2] = m[k];
});
var __exportStar = exports && exports.__exportStar || function(m, exports2) {
  for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
};
var __importDefault = exports && exports.__importDefault || function(mod) {
  return mod && mod.__esModule ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = void 0;
var plugin_1 = require_plugin();
Object.defineProperty(exports, "default", { enumerable: true, get: /* @__PURE__ */ __name(function() {
  return __importDefault(plugin_1).default;
}, "get") });
__exportStar(require_api(), exports);
//# sourceMappingURL=nask.js.map
