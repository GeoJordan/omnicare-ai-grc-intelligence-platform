"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/constants.js
var require_constants = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/constants.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.DEFAULT_CONTEXT_PROCESSING_SCRIPT = exports2.TOOL_TYPE_TO_TARGET_TABLE_MAP = exports2.GEN_AI_AGENT_TYPE = exports2.SN_AIA_TRIGGER_MAPPING = exports2.SN_AIA_TRIGGER_CONFIG = exports2.ACL_DEFAULTS = exports2.PUBLIC_ACCESS_ROLE_SYS_ID = exports2.USER_IS_AUTHENTICATED = exports2.SYS_USER_ROLE = exports2.SYS_AGENT_ACCESS_ROLE_MAPPING = exports2.SYS_AGENT_ACCESS_ROLE_CONFIG = exports2.SYS_SECURITY_ACL_ROLE = exports2.SYS_SECURITY_ACL = exports2.SN_AI_AGENT_TOOL_M2M = exports2.SN_AI_AGENT_TOOL = exports2.SN_AI_AGENT_VERSION = exports2.SN_AI_AGENT_CONFIG = exports2.SN_AIA_LTM_CATEGORY_MAPPING = exports2.SN_AI_AGENT = exports2.SYS_ID_TO_MEMORY_CATEGORY_MAP = exports2.CHANNEL_SYS_ID_TO_NAME_MAP = exports2.OOB_CHANNEL_MAP = exports2.MEMORY_CATEGORY_TO_SYS_ID_MAP = exports2.OOB_TOOL_TYPE_TO_SYS_ID_MAP = exports2.STRATEGY_NAME_TO_SYS_ID_MAP = exports2.WEB_SEARCH_DEFAULT_INPUTS = exports2.WEB_SEARCH_TOOL_SYS_ID = exports2.REACT_STRATEGY_SYS_ID = void 0;
    exports2.REACT_STRATEGY_SYS_ID = "f0bff21f9f13c6108f431597d90a1c74";
    exports2.WEB_SEARCH_TOOL_SYS_ID = "576cf8eaffd922101dbaffffffffff66";
    exports2.WEB_SEARCH_DEFAULT_INPUTS = JSON.stringify([
      {
        name: "search_type",
        value: "ai_answers",
        metadata: {
          _MODE_: "user"
        }
      }
    ]);
    exports2.STRATEGY_NAME_TO_SYS_ID_MAP = {
      "AIA - Conversational": "3f258fb9ffa8a210d84cffffffffff7a",
      "Base Planner": "a43ada98ff1422106c36ffffffffff46",
      "Batch Planner": "7250a4099318e2505c21b7cd1dba10f7",
      "External Agent Executor": "de4aa3e60bd02210175740976877b262",
      "Hierarchical ReAct": "18a2de41ff632210309fffffffffff90",
      Quick: "8282596347d716103b6f9402516d4399",
      ReAct: exports2.REACT_STRATEGY_SYS_ID,
      "ReActive Planner": "b264c470ff1022106c36ffffffffff67",
      "Swarm Planner": "4b994008ff4c7210309fffffffffffb3",
      "Unified Planner": "cfca2666476a22106480e5e5316d43f2"
    };
    exports2.OOB_TOOL_TYPE_TO_SYS_ID_MAP = {
      web_automation: exports2.WEB_SEARCH_TOOL_SYS_ID,
      knowledge_graph: "3b8c4f4fc3e022100e94313a05013167",
      file_upload: "b064af48ffb822101dbaffffffffff20",
      rag: "8021ddea2b0d52101d72fb466e91bfd1"
    };
    exports2.MEMORY_CATEGORY_TO_SYS_ID_MAP = {
      device_and_software: "7634aaae43db52107c4d14020ab8f294",
      meetings_and_events: "66442eae43db52107c4d14020ab8f26f",
      projects: "6b49e262431312107c4d14020ab8f2a9",
      workplace: "5c446eae43db52107c4d14020ab8f218"
    };
    exports2.OOB_CHANNEL_MAP = {
      "Now Assist Panel": "b023fcb3537321106167ddeeff7b12a8"
    };
    exports2.CHANNEL_SYS_ID_TO_NAME_MAP = Object.fromEntries(Object.entries(exports2.OOB_CHANNEL_MAP).map(([name, sysId]) => [sysId, name]));
    exports2.SYS_ID_TO_MEMORY_CATEGORY_MAP = {
      "7634aaae43db52107c4d14020ab8f294": "device_and_software",
      "66442eae43db52107c4d14020ab8f26f": "meetings_and_events",
      "6b49e262431312107c4d14020ab8f2a9": "projects",
      "5c446eae43db52107c4d14020ab8f218": "workplace"
    };
    exports2.SN_AI_AGENT = "sn_aia_agent";
    exports2.SN_AIA_LTM_CATEGORY_MAPPING = "sn_aia_ltm_category_mapping";
    exports2.SN_AI_AGENT_CONFIG = "sn_aia_agent_config";
    exports2.SN_AI_AGENT_VERSION = "sn_aia_version";
    exports2.SN_AI_AGENT_TOOL = "sn_aia_tool";
    exports2.SN_AI_AGENT_TOOL_M2M = "sn_aia_agent_tool_m2m";
    exports2.SYS_SECURITY_ACL = "sys_security_acl";
    exports2.SYS_SECURITY_ACL_ROLE = "sys_security_acl_role";
    exports2.SYS_AGENT_ACCESS_ROLE_CONFIG = "sys_agent_access_role_configuration";
    exports2.SYS_AGENT_ACCESS_ROLE_MAPPING = "sys_agent_access_role_mapping";
    exports2.SYS_USER_ROLE = "sys_user_role";
    exports2.USER_IS_AUTHENTICATED = "239bdf0c47d23110ba46ff58936d4330";
    exports2.PUBLIC_ACCESS_ROLE_SYS_ID = "b0593b350a0a0aa7001d689e4542dc28";
    exports2.ACL_DEFAULTS = {
      operation: "execute",
      active: true,
      adminOverrides: true,
      advanced: false,
      decisionType: "allow",
      localOrExisting: "Existing"
    };
    exports2.SN_AIA_TRIGGER_CONFIG = "sn_aia_trigger_configuration";
    exports2.SN_AIA_TRIGGER_MAPPING = "sn_aia_trigger_agent_usecase_m2m";
    exports2.GEN_AI_AGENT_TYPE = "fc55e9f79f8222109570e67e8a0a1c34";
    exports2.TOOL_TYPE_TO_TARGET_TABLE_MAP = {
      action: "sys_hub_action_type_definition",
      capability: "sn_nowassist_skill_config",
      subflow: "sys_hub_flow",
      catalog: "sc_cat_item",
      crud: "sn_aia_tool",
      script: "sn_aia_tool",
      topic_block: "sys_cs_topic",
      desktop_automation: "sn_aia_tool",
      deep_research: "sn_aia_tool",
      file_upload: "sn_aia_tool",
      rag: "sn_aia_tool",
      mcp: "sn_aia_tool",
      knowledge_graph: "sn_aia_tool",
      topic: "sys_cs_topic",
      web_automation: "sn_aia_tool"
    };
    exports2.DEFAULT_CONTEXT_PROCESSING_SCRIPT = `/*
 * Available Variables:
 * - task: The task provided to agent to solve either by orchestrator or user(incase of only agent execution i.e agent is invoked as seperate entity instead from a workflow).
 * - user_utterance: The user utterance provided. and task = user_utterance in case of individual agent execution.
 * - agent_id: The agent id.
 * - context: An object that carries additional contextual information for the agent.
 * It always includes a "pageContext" and may include a "triggerContext",
 * depending on how and from where the agent is invoked.
 *
 * Behavior by invocation type:
 * ---------------------------------
 * - When invoked via the startAiAgentConversation API:
 * If a "contextMemory" parameter is provided in the API request,
 * it will be available here as the "triggerContext".
 * The "pageContext" is always present in this call, regardless
 * of whether the agent is executed individually or as part of a use case.
 * Example:
 * {
 * pageContext: { ... },
 * triggerContext: { ... } // Derived from contextMemory (if passed)
 * }
 *
 * - When invoked as part of a use case or workflow:
 * The "pageContext" and "triggerContext" objects available here
 * may have been *modified* by the use case's context processing script.
 * This means the use case may alter, extend, or overwrite values
 * from the original "pageContext" or "triggerContext"
 * before the context reaches the agent.
 * If the use case was originally triggered via the API,
 * the initial "triggerContext" is still derived from the
 * "contextMemory" parameter, but its contents may have been updated
 * by the use case logic.
 *
 * - When executed directly (standalone agent execution):
 * The "pageContext" will always be present (default or empty),
 * and the "triggerContext" will only be populated if explicitly
 * passed through the API call.
 * Return an modified context like: { pageContext : {},triggerContext:{} }
 */
(function(task, user_utterance, agent_id, context) {
	return {
		pageContext : context?.pageContext,
		triggerContext : context?.triggerContext
	};
})(task,user_utterance, agent_id, context);`;
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/shared/data-access-transform-helper.js
var require_data_access_transform_helper = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/shared/data-access-transform-helper.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.computeDataAccessValue = void 0;
    var computeDataAccessValue = /* @__PURE__ */ __name((dataAccessConfig, descendantMappings = []) => {
      if (!dataAccessConfig) {
        return void 0;
      }
      const roleListRaw = dataAccessConfig.get("role_list")?.getValue();
      let roleListSysIds;
      if (roleListRaw) {
        const sysIds = typeof roleListRaw === "string" ? roleListRaw.split(",").map((r) => r.trim()).filter(Boolean) : Array.isArray(roleListRaw) ? roleListRaw : [roleListRaw];
        if (sysIds.length > 0) {
          roleListSysIds = sysIds;
        }
      }
      const parentSysId = dataAccessConfig.getId().getValue();
      const mappingRows = descendantMappings.filter((row) => {
        const fkValue = row.get("agent_access_config")?.toString().getValue();
        return typeof fkValue === "string" && fkValue === parentSysId;
      });
      const roleShapes = mappingRows.map((row) => row.get("role")).filter((shape) => shape !== void 0);
      return dataAccessConfig.transform(({ $ }) => ({
        description: $.from("description").def(""),
        ...roleListSysIds && roleListSysIds.length > 0 ? { roleList: $.val(roleListSysIds) } : {},
        ...roleShapes.length > 0 ? { roleMap: $.val(roleShapes) } : {}
      }));
    }, "computeDataAccessValue");
    exports2.computeDataAccessValue = computeDataAccessValue;
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/ai-agent-transform-helper.js
var require_ai_agent_transform_helper = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/ai-agent-transform-helper.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.mapTriggerConfigs = exports2.computeSecurityAclValue = exports2.computeDataAccessValue = exports2.computeVersionDetailsValue = exports2.computeAgentConfigValue = exports2.mapToolsFromM2M = exports2.parseCrudInputsFields = exports2.extractSysId = void 0;
    var constants_1 = require_constants();
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var extractSysId = /* @__PURE__ */ __name((v, defaultValue = "") => {
      if (!v) {
        return defaultValue;
      }
      if (v.isRecord()) {
        return v.getId().getValue();
      }
      return v.ifString()?.getValue() || defaultValue;
    }, "extractSysId");
    exports2.extractSysId = extractSysId;
    var getTargetDocumentForType = /* @__PURE__ */ __name((toolType, expectedTypes, toolRecord) => {
      const types = Array.isArray(expectedTypes) ? expectedTypes : [expectedTypes];
      return types.includes(toolType ?? "") ? toolRecord?.get("target_document")?.ifString()?.getValue() : void 0;
    }, "getTargetDocumentForType");
    var includeIfNotDefault = /* @__PURE__ */ __name((value, defaultValue) => {
      if (Array.isArray(value) && Array.isArray(defaultValue)) {
        if (value.length === 0 && defaultValue.length === 0) {
          return void 0;
        }
        if (JSON.stringify(value) === JSON.stringify(defaultValue)) {
          return void 0;
        }
        return value;
      }
      if (typeof value === "object" && value !== null && typeof defaultValue === "object" && defaultValue !== null) {
        if (JSON.stringify(value) === JSON.stringify(defaultValue)) {
          return void 0;
        }
        return value;
      }
      if (value === defaultValue) {
        return void 0;
      }
      return value;
    }, "includeIfNotDefault");
    var transformCrudInputs = /* @__PURE__ */ __name((inputsArray) => {
      const crudInput = inputsArray.find((input) => input !== null && typeof input === "object" && "name" in input && input.name === "crudInputs");
      if (!crudInput || typeof crudInput !== "object" || !("value" in crudInput)) {
        return inputsArray;
      }
      const crudValue = crudInput.value;
      if (!crudValue || typeof crudValue !== "object") {
        return inputsArray;
      }
      const operationName = crudValue.operation;
      if (!operationName) {
        return inputsArray;
      }
      const table = crudValue.table?.value;
      if (!table) {
        return inputsArray;
      }
      const fieldValues = crudValue.fieldValues || [];
      const fieldMetadataMap = /* @__PURE__ */ new Map();
      for (const fieldValue of fieldValues) {
        const fieldName = fieldValue.field?.name;
        const fieldType = fieldValue.field?.type;
        if (fieldName) {
          const metadata = {
            mappedToColumn: fieldName
            // The field name is the column name
          };
          if (fieldType) {
            metadata.type = fieldType;
          }
          fieldMetadataMap.set(fieldName, metadata);
        }
      }
      const inputFields = inputsArray.filter((input) => {
        if (!input || typeof input !== "object" || !("name" in input)) {
          return false;
        }
        return input.name !== "crudInputs";
      }).map((input) => {
        const inputObj = input;
        const fieldName = inputObj.name || "";
        const metadata = fieldMetadataMap.get(fieldName);
        return {
          name: fieldName,
          description: inputObj.description || "",
          mandatory: inputObj.mandatory ?? false,
          ...metadata?.mappedToColumn !== void 0 && { mappedToColumn: metadata.mappedToColumn },
          ...metadata?.type !== void 0 && metadata.type !== "string" && { type: metadata.type }
        };
      });
      const result = {
        operationName,
        table,
        inputFields
      };
      const queryValue = crudValue.queryCondition ?? crudValue.query;
      if (queryValue !== void 0) {
        result.queryCondition = queryValue;
      }
      if (crudValue.returnFields !== void 0 && Array.isArray(crudValue.returnFields)) {
        result.returnFields = crudValue.returnFields.map((field) => {
          const cleanField = {
            name: field.name
          };
          if (field.type && field.type !== "" && field.type !== "string") {
            cleanField.type = field.type;
          }
          if (field.referenceTable && field.referenceTable !== null) {
            cleanField.referenceConfig = {
              table: field.referenceTable,
              field: field.referenceKey || field.referenceTableDefaultField || "sys_id"
            };
          }
          return cleanField;
        });
      }
      if (crudValue.limit !== void 0) {
        const parsedLimit = Number(crudValue.limit);
        result.limit = Number.isNaN(parsedLimit) ? crudValue.limit : parsedLimit;
      }
      if (crudValue.orderBy !== void 0) {
        result.orderBy = crudValue.orderBy;
      }
      if (crudValue.sortType !== void 0 && crudValue.sortType !== "") {
        result.sortType = crudValue.sortType;
      }
      return result;
    }, "transformCrudInputs");
    var mapToolTypeToExternal = /* @__PURE__ */ __name((toolType) => {
      if (toolType === "rag") {
        return "search_retrieval";
      }
      return toolType;
    }, "mapToolTypeToExternal");
    var reconstructValueLabel = /* @__PURE__ */ __name((input) => {
      const value = input?.["value"] || "";
      const label = input?.["label"] || value;
      return { value, label };
    }, "reconstructValueLabel");
    var reconstructValueLabelArray = /* @__PURE__ */ __name((input) => {
      const values = input?.["value"];
      if (!values) {
        return void 0;
      }
      const rawLabel = input?.["label"];
      if (!rawLabel) {
        return values.map((value) => {
          return { value, label: value };
        });
      }
      const labels = Array.isArray(rawLabel) ? rawLabel : rawLabel.split(", ");
      return values.map((value, index) => {
        const label = labels[index] || value;
        return { value, label };
      });
    }, "reconstructValueLabelArray");
    var transformRagInputs = /* @__PURE__ */ __name((inputsArray) => {
      const inputs = inputsArray;
      const inputsByName = {};
      for (const input of inputs) {
        const name = input["name"];
        if (name) {
          inputsByName[name] = input;
        }
      }
      const searchTypeValue = inputsByName["search_type"]?.["value"];
      if (!searchTypeValue) {
        return void 0;
      }
      const searchProfile = reconstructValueLabel(inputsByName["search_profile"]);
      const sources = reconstructValueLabelArray(inputsByName["sources"]) ?? [];
      const fields = reconstructValueLabelArray(inputsByName["fields"]);
      const limitStr = inputsByName["search_results_limit"]?.["value"];
      const searchResultsLimit = limitStr && limitStr !== "" ? Number(limitStr) : void 0;
      let searchType;
      if (searchTypeValue === "semantic" || searchTypeValue === "hybrid") {
        const semanticIndexes = reconstructValueLabelArray(inputsByName["semantic_index_names"]) ?? [];
        const thresholdRaw = inputsByName["document_match_threshold"]?.["value"];
        const documentMatchThreshold = thresholdRaw !== void 0 && thresholdRaw !== "" ? Number(thresholdRaw) : void 0;
        searchType = {
          type: searchTypeValue,
          semanticIndexes,
          ...documentMatchThreshold !== void 0 && { documentMatchThreshold }
        };
      } else {
        searchType = { type: "keyword" };
      }
      const result = {
        searchType,
        searchProfile,
        sources
      };
      if (fields && fields.length > 0) {
        result.fields = fields;
      }
      if (searchResultsLimit !== void 0 && !Number.isNaN(searchResultsLimit)) {
        result.searchResultsLimit = searchResultsLimit;
      }
      return result;
    }, "transformRagInputs");
    var parseCrudInputsFields = /* @__PURE__ */ __name((inputsShape) => {
      if (!inputsShape) {
        return {};
      }
      const inputsArray = inputsShape.ifString()?.parseJson()?.getValue();
      if (!inputsArray || !Array.isArray(inputsArray)) {
        return {};
      }
      const crudInputs = inputsArray.find((input) => {
        return input !== null && typeof input === "object" && "name" in input && input.name === "crudInputs";
      });
      const crudValue = crudInputs?.value;
      if (!crudValue || typeof crudValue !== "object") {
        return {};
      }
      const result = {};
      if (crudValue.queryCondition !== void 0) {
        result.queryCondition = crudValue.queryCondition;
      }
      if (crudValue.limit !== void 0) {
        result.limit = crudValue.limit;
      }
      if (crudValue.orderBy !== void 0) {
        result.orderBy = crudValue.orderBy;
      }
      if (crudValue.sortType !== void 0) {
        result.sortType = crudValue.sortType;
      }
      return result;
    }, "parseCrudInputsFields");
    exports2.parseCrudInputsFields = parseCrudInputsFields;
    var mapToolsFromM2M = /* @__PURE__ */ __name(async (toolM2Ms, toolRecords, transform) => {
      return Promise.all(toolM2Ms.map(async (m2m) => {
        let toolRecord = m2m.get("tool")?.ifRecord();
        const toolId = m2m.get("tool")?.ifString()?.getValue();
        if (!toolRecord && toolId) {
          toolRecord = toolRecords.find((t) => t.getId().getValue() === toolId);
        }
        let toolType;
        if (toolId) {
          for (const [type, sysId] of Object.entries(constants_1.OOB_TOOL_TYPE_TO_SYS_ID_MAP)) {
            if (sysId === toolId) {
              toolType = type;
              break;
            }
          }
        }
        if (!toolType) {
          toolType = toolRecord?.get("type")?.ifString()?.getValue();
        }
        let script;
        if (toolType === "script" && toolRecord?.has("script")) {
          const scriptValue = toolRecord.get("script")?.ifString()?.getValue();
          if (scriptValue && scriptValue !== "") {
            script = await sdk_build_plugins_1.NowIncludeShape.fromRecord(toolRecord, toolRecord.get("script"), transform);
          }
        }
        const maxAutoExecutionsValue = m2m.get("max_auto_executions")?.ifString()?.ifNotEmpty()?.toNumber()?.getValue() ?? 10;
        return m2m.transform(({ $ }) => ({
          // Common properties from M2M record only (no fallback to tool record)
          active: $.from("active").toBoolean().def(true),
          description: $.from("description"),
          name: $.from("name"),
          // m2mSysOverrides specifically maps to M2M table overrides
          m2mSysOverrides: $.from("sys_overrides").def(""),
          // Original tool details fields from tool record
          sysOverrides: toolRecord ? $.val(toolRecord.get("sys_overrides")?.getValue()).def("") : $.val(void 0),
          ...toolRecord ? {
            recordType: $.val(includeIfNotDefault(toolRecord.get("record_type")?.ifString()?.getValue() || "template", "template"))
          } : {},
          script: script ? $.val(script) : $.val(void 0),
          // Map target_document to type-specific property names based on tool type
          capabilityId: $.val(getTargetDocumentForType(toolType, "capability", toolRecord)),
          subflowId: $.val(getTargetDocumentForType(toolType, "subflow", toolRecord)),
          flowActionId: $.val(getTargetDocumentForType(toolType, "action", toolRecord)),
          catalogItemId: $.val(getTargetDocumentForType(toolType, "catalog", toolRecord)),
          virtualAgentId: $.val(getTargetDocumentForType(toolType, ["topic", "topic_block"], toolRecord)),
          // For crud/script tools, include targetDocument; exclude OOB types (identified by type alone) and reference-type tools
          targetDocument: $.val(toolType !== "capability" && toolType !== "subflow" && toolType !== "action" && toolType !== "catalog" && toolType !== "topic" && toolType !== "topic_block" && toolType !== "web_automation" && toolType !== "file_upload" && toolType !== "knowledge_graph" && toolType !== "rag" ? includeIfNotDefault(toolRecord?.get("target_document")?.ifString()?.getValue(), "") : void 0),
          // Include type if we have it (either from tool record or detected from OOB map)
          // Omit the field if we couldn't determine the type
          type: toolType ? $.val(mapToolTypeToExternal(toolType)) : $.val(void 0),
          // Fields from M2M configuration
          documentStatus: $.from("document_status").def(""),
          displayOutput: $.from("display_output").toBoolean().def(false),
          executionMode: $.from("execution_mode"),
          inputs: $.from("inputs").map((v) => {
            if (toolType !== "crud" && toolType !== "script" && toolType !== "rag") {
              return void 0;
            }
            const str = v.ifString()?.getValue();
            const parsed = str ? v.ifString()?.parseJson()?.getValue() : void 0;
            if (!parsed) {
              return void 0;
            }
            if (toolType === "rag" && Array.isArray(parsed)) {
              return transformRagInputs(parsed);
            }
            if (toolType === "crud" && Array.isArray(parsed)) {
              return transformCrudInputs(parsed);
            }
            const cleanedInputs = Array.isArray(parsed) ? parsed.map((input) => {
              const type = input["type"];
              const referenceConfig = input["referenceConfig"];
              const normalized = {
                name: input["name"] || "",
                description: input["description"] || "",
                mandatory: input["mandatory"] ?? false
              };
              if (input["value"] !== void 0 && input["value"] !== "") {
                normalized.value = input["value"];
              }
              if (input["invalidMessage"] !== void 0) {
                normalized.invalidMessage = input["invalidMessage"];
              }
              if (input["mappedToColumn"] !== void 0) {
                normalized.mappedToColumn = input["mappedToColumn"];
              }
              if (type !== void 0 && type !== "string") {
                normalized.type = type;
              }
              if (referenceConfig !== void 0) {
                normalized.referenceConfig = referenceConfig;
              }
              return normalized;
            }) : parsed;
            return includeIfNotDefault(cleanedInputs, []);
          }),
          maxAutoExecutions: $.val(maxAutoExecutionsValue),
          outputTransformationStrategy: $.from("output_transformation_strategy").def(""),
          postMessage: $.from("post_message").def(""),
          preMessage: $.from("pre_message").def(""),
          toolAttributes: $.from("tool_attributes").map((v) => {
            const str = v.ifString()?.getValue();
            const parsed = str ? v.ifString()?.parseJson()?.getValue() : void 0;
            if (!parsed || typeof parsed === "object" && Object.keys(parsed).length === 0) {
              return void 0;
            }
            return includeIfNotDefault(parsed, {});
          }),
          timeout: $.from("timeout").def(""),
          preRun: $.from("pre_run").toBoolean().def(false),
          transformationInstructions: $.from("transformation_instructions").def(""),
          postProcessingScript: $.from("post_processing_script").def(""),
          widgets: $.from("widgets").map((v) => {
            const val = v.ifString()?.getValue();
            if (!val || val === "") {
              return void 0;
            }
            const widgetArray = val.split(",").filter((w) => w.trim() !== "");
            return includeIfNotDefault(widgetArray, []);
          })
        }));
      }));
    }, "mapToolsFromM2M");
    exports2.mapToolsFromM2M = mapToolsFromM2M;
    var computeAgentConfigValue = /* @__PURE__ */ __name((config) => {
      if (!config) {
        return void 0;
      }
      const publicBool = config.get("public")?.toBoolean()?.getValue();
      const agentLearningBool = config.get("agent_learning")?.toBoolean()?.getValue();
      const docUrlValue = config.get("doc_url")?.getValue();
      const iconUrlValue = config.get("icon_url")?.getValue();
      const runAsUserValue = config.get("run_as_user")?.getValue();
      const agentConfigSysOverridesValue = config.get("sys_overrides")?.getValue();
      const agentDescriptorValue = config.get("agent_descriptor")?.getValue();
      return {
        public: publicBool || void 0,
        agentLearning: agentLearningBool || void 0,
        docUrl: docUrlValue === "" ? void 0 : docUrlValue,
        iconUrl: iconUrlValue === "" ? void 0 : iconUrlValue,
        runAsUser: runAsUserValue || "",
        // Keep empty string as default
        agentConfigSysOverrides: agentConfigSysOverridesValue === "" ? void 0 : agentConfigSysOverridesValue,
        agentDescriptor: agentDescriptorValue === "" ? void 0 : agentDescriptorValue
      };
    }, "computeAgentConfigValue");
    exports2.computeAgentConfigValue = computeAgentConfigValue;
    var computeVersionDetailsValue = /* @__PURE__ */ __name((versions) => {
      if (!versions || versions.length === 0) {
        return void 0;
      }
      return versions.map((version) => version.transform(({ $ }) => ({
        condition: $.from("condition").def(""),
        state: $.from("state"),
        name: $.from("version_name"),
        number: $.from("version_number").toNumber().def(1),
        instructions: $.from("instructions")
      })));
    }, "computeVersionDetailsValue");
    exports2.computeVersionDetailsValue = computeVersionDetailsValue;
    var data_access_transform_helper_1 = require_data_access_transform_helper();
    Object.defineProperty(exports2, "computeDataAccessValue", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return data_access_transform_helper_1.computeDataAccessValue;
    }, "get") });
    var computeSecurityAclValue = /* @__PURE__ */ __name((aclRecord, aclRoleRecords) => {
      if (!aclRecord) {
        return void 0;
      }
      const allRoles = aclRoleRecords.map((r) => {
        const roleRef = r.get("sys_user_role");
        if (roleRef?.isRecord()) {
          return roleRef.getId().getValue();
        }
        return roleRef?.ifString()?.getValue();
      }).filter((v) => !!v);
      const isPublic = allRoles.length > 0 && allRoles.every((r) => r === constants_1.PUBLIC_ACCESS_ROLE_SYS_ID);
      const roles = allRoles.filter((r) => r !== constants_1.PUBLIC_ACCESS_ROLE_SYS_ID);
      const $id = sdk_build_plugins_1.NowIdShape.from(aclRecord);
      if (isPublic) {
        return {
          $id,
          type: "Public"
        };
      }
      if (roles.length > 0) {
        return {
          $id,
          type: "Specific role",
          roles
        };
      }
      return {
        $id,
        type: "Any authenticated user"
      };
    }, "computeSecurityAclValue");
    exports2.computeSecurityAclValue = computeSecurityAclValue;
    var mapTriggerConfigs = /* @__PURE__ */ __name((triggerConfigs, triggerMappings) => {
      if (triggerConfigs.length === 0) {
        return void 0;
      }
      return triggerConfigs.map((tc) => {
        const triggerConfigId = tc.getId().getValue();
        const triggerMapping = triggerMappings.find((tm) => {
          const tmTriggerConfig = tm.get("trigger_configuration");
          if (tmTriggerConfig?.isRecord()) {
            return tmTriggerConfig.getId().getValue() === triggerConfigId;
          }
          return tmTriggerConfig?.ifString()?.getValue() === triggerConfigId;
        });
        const runDayOfMonthRaw = tc.get("run_dayofmonth")?.ifString()?.getValue();
        const runDayOfMonth = runDayOfMonthRaw ? Number(runDayOfMonthRaw) : void 0;
        const runDayOfWeekRaw = tc.get("run_dayofweek")?.ifString()?.getValue();
        const runDayOfWeek = runDayOfWeekRaw ? Number(runDayOfWeekRaw) : void 0;
        const repeatInterval = tc.get("run_period")?.ifString()?.getValue();
        const starting = tc.get("run_start")?.ifString()?.getValue();
        const time = tc.get("run_time")?.ifString()?.getValue();
        const triggerStrategy = tc.get("trigger_strategy")?.ifString()?.getValue();
        const schedule = runDayOfMonth || runDayOfWeek || repeatInterval || starting || time || triggerStrategy ? {
          ...runDayOfMonth && { runDayOfMonth },
          ...runDayOfWeek && { runDayOfWeek },
          ...repeatInterval && { repeatInterval },
          ...starting && { starting },
          ...time && { time },
          ...triggerStrategy && { triggerStrategy }
        } : void 0;
        const channelSysId = tc.get("channel")?.ifString()?.getValue();
        const channelValue = channelSysId ? constants_1.CHANNEL_SYS_ID_TO_NAME_MAP[channelSysId] || channelSysId : void 0;
        const portalValue = tc.get("portal")?.ifString()?.getValue();
        const includePortal = Boolean(portalValue && portalValue !== "undefined");
        return tc.transform(({ $ }) => ({
          name: $.from("name"),
          description: $.from("description").def(""),
          active: $.from("active").toBoolean().def(true),
          businessRule: $.from("business_rule").def(""),
          channel: $.val(channelValue),
          triggerCondition: $.from("condition").def(""),
          enableDiscovery: $.from("enable_discovery").toBoolean().def(false),
          notificationScript: $.from("notification_script").def(""),
          objectiveTemplate: $.from("objective_template"),
          ...includePortal ? { portal: $.from("portal") } : {},
          profile: $.from("profile").def(""),
          runAs: $.from("run_as").def(""),
          runAsScript: $.from("run_as_script").def(""),
          runAsUser: $.from("run_as_user").def(""),
          schedule: $.val(schedule),
          showNotifications: $.from("show_notifications").toBoolean().def(false),
          domain: $.from("sys_domain").def("global"),
          sysOverrides: $.from("sys_overrides").def(""),
          targetTable: $.from("target_table"),
          triggerFlow: $.from("trigger_flow").def(""),
          triggerFlowDefinitionType: $.from("trigger_flow_definition_type"),
          usecase: $.from("usecase").def(""),
          usecaseDiscovery: $.from("usecase_discovery").toBoolean().def(false),
          // Get m2mSysOverrides from the trigger mapping M2M record
          m2mSysOverrides: $.val(triggerMapping?.get("sys_overrides"))
        }));
      });
    }, "mapTriggerConfigs");
    exports2.mapTriggerConfigs = mapTriggerConfigs;
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/tool-input-helper.js
var require_tool_input_helper = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/tool-input-helper.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.generateToolInputs = generateToolInputs;
    exports2.isToolInputType = isToolInputType;
    exports2.generateRagInputs = generateRagInputs;
    function generateFieldMetadata(fieldName, fieldType, referenceConfig) {
      const label = fieldName;
      const type = fieldType || "string";
      const metadata = {
        id: fieldName,
        name: fieldName,
        label,
        type,
        sublabel: fieldName,
        excludedOperators: [],
        dictionaryAttributes: []
      };
      if (referenceConfig && type === "reference") {
        metadata.referenceTable = referenceConfig.table;
        metadata.referenceTableDefaultField = referenceConfig.field || null;
        metadata.referenceKey = null;
      }
      return metadata;
    }
    __name(generateFieldMetadata, "generateFieldMetadata");
    function generateToolInputs(toolInput) {
      if (!isToolInputType(toolInput)) {
        return [];
      }
      const inputs = [];
      for (const field of toolInput.inputFields) {
        inputs.push({
          name: field.name,
          value: field.value || "",
          description: field.description,
          mandatory: field.mandatory,
          invalidMessage: field.invalidMessage ?? null
        });
      }
      const tableRef = typeof toolInput.table === "string" ? { value: toolInput.table, displayValue: toolInput.table } : toolInput.table;
      const crudInputs = {
        operation: toolInput.operationName,
        table: tableRef
      };
      if (toolInput.queryCondition) {
        crudInputs.query = toolInput.queryCondition;
      }
      const fieldsWithMappings = toolInput.inputFields.filter((field) => field.mappedToColumn);
      if (fieldsWithMappings.length > 0) {
        crudInputs.fieldValues = fieldsWithMappings.map((field) => {
          const mappedColumn = field.mappedToColumn;
          if (!mappedColumn) {
            return null;
          }
          const column = generateFieldMetadata(mappedColumn, field.type, field.referenceConfig);
          const fieldDef = {
            id: column.id,
            sublabel: column.sublabel || column.id,
            excludedOperators: column.excludedOperators || [],
            label: column.label,
            type: column.type,
            dictionaryAttributes: column.dictionaryAttributes || [],
            name: column.name
          };
          if (column.referenceTable) {
            fieldDef.referenceTable = column.referenceTable;
            fieldDef.referenceTableDefaultField = column.referenceTableDefaultField;
            fieldDef.referenceKey = column.referenceKey;
          }
          return {
            field: fieldDef,
            value: {
              fieldValue: `{{${field.name}}}`
              // Use field.name as the variable name
            }
          };
        });
      }
      if (toolInput.returnFields && toolInput.returnFields.length > 0) {
        crudInputs.returnFields = toolInput.returnFields.map((field) => {
          const metadata = generateFieldMetadata(field.name, field.type);
          return {
            excludedOperators: metadata.excludedOperators || [],
            extendedOperators: null,
            name: field.name,
            label: metadata.label,
            type: field.type || metadata.type,
            referenceTable: null,
            referenceTableDefaultField: null,
            referenceKey: null,
            dictionaryAttributes: metadata.dictionaryAttributes || [],
            id: field.name,
            sublabel: field.name
          };
        });
      }
      if (toolInput.limit !== void 0) {
        crudInputs.limit = String(toolInput.limit);
      }
      if (toolInput.orderBy !== void 0) {
        crudInputs.orderBy = toolInput.orderBy;
      }
      if (toolInput.sortType !== void 0) {
        crudInputs.sortType = toolInput.sortType;
      }
      inputs.push({
        name: "crudInputs",
        value: crudInputs,
        description: `${toolInput.operationName.charAt(0).toUpperCase() + toolInput.operationName.slice(1)} operation configuration`,
        mandatory: true,
        invalidMessage: null
      });
      return inputs;
    }
    __name(generateToolInputs, "generateToolInputs");
    function isToolInputType(inputs) {
      return inputs !== null && inputs !== void 0 && typeof inputs === "object" && !Array.isArray(inputs) && "inputFields" in inputs && "operationName" in inputs && "table" in inputs;
    }
    __name(isToolInputType, "isToolInputType");
    function generateRagInputs(ragInput) {
      const inputs = [];
      const { searchType } = ragInput;
      inputs.push({
        name: "search_type",
        description: "Search type",
        value: searchType.type
      });
      const profileObj = ragInput.searchProfile;
      const profileValue = profileObj.value ?? profileObj.name;
      const profileLabel = profileObj.label;
      inputs.push({
        name: "query",
        description: `Search query for retrieving relevant information from ${profileLabel}`
      });
      inputs.push({
        name: "search_profile",
        value: profileValue,
        label: profileLabel
      });
      if (ragInput.sources && ragInput.sources.length > 0) {
        const sourceValues = ragInput.sources.map((source) => source.value);
        const sourceLabels = ragInput.sources.map((source) => source.label);
        inputs.push({
          name: "sources",
          value: sourceValues,
          label: sourceLabels
        });
      }
      if (ragInput.fields && ragInput.fields.length > 0) {
        const fieldValues = ragInput.fields.map((field) => field.value);
        const fieldLabels = ragInput.fields.map((field) => field.label);
        inputs.push({
          name: "fields",
          value: fieldValues,
          label: fieldLabels
        });
      }
      inputs.push({
        name: "search_results_limit",
        value: String(ragInput.searchResultsLimit ?? 10)
      });
      if (searchType.type !== "keyword") {
        if (searchType.semanticIndexes && searchType.semanticIndexes.length > 0) {
          const indexesArray = searchType.semanticIndexes;
          const indexValues = indexesArray.map((index) => {
            if (typeof index === "string") {
              return index;
            }
            if (index && typeof index === "object" && "value" in index) {
              return index.value;
            }
            return String(index);
          });
          const indexLabels = indexesArray.map((index) => {
            if (typeof index === "string") {
              return index;
            }
            if (index && typeof index === "object" && "label" in index) {
              return index.label;
            }
            return String(index);
          });
          inputs.push({
            name: "semantic_index_names",
            value: indexValues,
            label: indexLabels
          });
        }
        inputs.push({
          name: "document_match_threshold",
          value: searchType.documentMatchThreshold ?? 0
        });
      } else if (searchType.type === "keyword") {
        inputs.push({
          name: "document_match_threshold",
          value: 0
        });
      }
      return inputs;
    }
    __name(generateRagInputs, "generateRagInputs");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/crud-script-templates/lookup-script-template.js
var require_lookup_script_template = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/crud-script-templates/lookup-script-template.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.generateLookupScript = generateLookupScript;
    function generateLookupScript() {
      return String.raw`(function (inputs) {


    // parses query expression containing {{variables}}
    // by replacing {{variableName}} with the value of the variableName property in the inputs object
    // if the variableName property is not found in the inputs object, the variableName is returned as is
    function parseQuery(expression, inputs) {
        if (!expression) return '';
        return expression.replace(
            /{{\s*(\w+)\s*}}/g,
            function (match, variableName) {
                if (inputs && inputs.hasOwnProperty(variableName)) {
                    return inputs[variableName];
                }
                return '';
            }
        );
    }


    var table = inputs.crudInputs.table.value;
    var tableName = inputs.crudInputs.table.displayValue;
    var query = parseQuery(inputs.crudInputs.query, inputs);
    var sortType = inputs.crudInputs.sortType;
    var orderBy = inputs.crudInputs.orderBy;
    var limit = inputs.crudInputs.limit || 5;
    var fields = inputs.crudInputs.returnFields || [];


    var results = [];
    var protectedRecords = 0;
    var gr = new GlideRecordSecure(table);
    if (query) {
        gr.addEncodedQuery(query, true);
    } else {
        gs.error('[AIA error] No query provided');
        return {
            message: 'No query provided',
            status: sn_i18n.Message.getMessage('sn_aia', 'error'),
        };
    }


    if (orderBy && sortType === 'a_to_z') {
        gr.orderBy(orderBy);
    } else if (orderBy && sortType !== 'a_to_z') {
        gr.orderByDesc(orderBy);
    }


    gr.setLimit(limit);
    gr.query();
    while (gr.next()) {
        var fieldObj = {};
        if (gr.canRead()) {
            fields.forEach(function (field) {
                if (gr.canRead(field.id)) {
                    // store both value and display value for consistency with other field types
                    fieldObj[field.id] = {
                        value: gr.getValue(field.id),
                        displayValue: gr.getDisplayValue(field.id),
                    };
                }
            });
        } else {
            protectedRecords++;
        }
        results.push(fieldObj);
    }


    var recordCount = results.length;
    var total = recordCount + protectedRecords;
    var message = sn_i18n.Message.getMessage(
        'sn_aia',
        '{count} out of {total} records returned from the {table} table.',
        {
            count: recordCount.toString(),
            total: total.toString(),
            table: tableName,
        }
    );


    return {
        records: results,
        message: message,
        status: sn_i18n.Message.getMessage('sn_aia', 'success'),
    };
})(inputs);`;
    }
    __name(generateLookupScript, "generateLookupScript");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/crud-script-templates/create-script-template.js
var require_create_script_template = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/crud-script-templates/create-script-template.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.generateCreateScript = generateCreateScript;
    function generateCreateScript() {
      return String.raw`(function (inputs) {
    function getChoiceList(tableName, fieldName) {
        var choices = GlideChoiceList.getChoiceList(tableName, fieldName);
        if (!choices) return [];


        var choicesArray = [];
        for (var i = 0; i < choices.getSize(); i++) {
            choicesArray.push({
                value: choices.getChoice(i).getValue(),
                label: choices.getChoice(i).getLabel(),
            });
        }
        return choicesArray;
    }


    // parses {{value}} and obtains the correct value for a choice or reference field.
    function parseValue(table, value, field, inputs) {
        if (!value || typeof value === 'object') return '';
        // regex to parse {{var}} and check if the inputs contain that prop
        var fieldValue = value.replace(
            /{{\s*(\w+)\s*}}/g,
            function (match, variableName) {
                if (inputs && inputs.hasOwnProperty(variableName)) {
                    return inputs[variableName];
                }
                return '';
            }
        );
        if (!fieldValue) return field.type === 'string' ? '' : null;
        if (field.type === 'choice') {
            var choices = getChoiceList(table, field.id);
            for (var i = 0; i < choices.length; i++) {
                if (
                    choices[i].value == fieldValue ||
                    choices[i].label
                        .toLowerCase()
                        .includes(fieldValue.toLowerCase())
                ) {
                    return choices[i].value;
                }
            }
            // Neither value nor label matched
            gs.error(
                '[AIA AGENTS MSG] Invalid choice field value or label: {0}',
                fieldValue
            );
        } else if (field.referenceTable) {
            // First check if the input is a valid sys_id
            var refGr = new GlideRecordSecure(field.referenceTable);
            refGr.addQuery('sys_id', fieldValue);
            refGr.query();
            if (refGr.getRowCount() === 1) {
                return fieldValue; // User input is a valid sys_id
            }


            if (field.referenceTableDefaultField) {
                // If not, try to find a record with matching display value
                var refName = field.referenceTableDefaultField;
                refGr = new GlideRecordSecure(field.referenceTable);


                // Check for both exact match and display value containing the input
                var qr = refGr.addQuery(refName, fieldValue);
                qr.addOrCondition(refName, 'CONTAINS', fieldValue);
                refGr.setLimit(1);
                refGr.query();


                if (refGr.next()) {
                    return refGr.getUniqueValue(); // Return the sys_id
                }
            }
            // No match found
            gs.error(
                '[AIA AGENTS MSG] Invalid reference field value or label: {0}',
                fieldValue
            );
        }
        return fieldValue;
    }


    var table = inputs.crudInputs.table.value;
    var tableName = inputs.crudInputs.table.displayValue;
    var fieldValues = inputs.crudInputs.fieldValues
        ? inputs.crudInputs.fieldValues
        : [];
    // each field is passed as an {id, label} obj to be displayed when editing the tool
    // each value is not passed directly, but in an object containing the fieldValue prop
    fieldValues = fieldValues.map(function (item) {
        var fieldValue = item.value ? item.value.fieldValue : '';
        return {
            field: item.field.id,
            value: parseValue(table, fieldValue, item.field, inputs),
        };
    });


    var gr = new GlideRecordSecure(table);
    if (gr.canCreate()) {
        gr.initialize();
        fieldValues.forEach(function (item) {
            if (gr.isValidField(item.field)) {
                gr.setValue(item.field, item.value);
            }
        });
    } else {
        return {
            output: sn_i18n.Message.getMessage(
                'sn_aia',
                'Cannot create record due to security constraints.'
            ),
            status: sn_i18n.Message.getMessage('sn_aia', 'error'),
        };
    }
    var sysId = gr.insert();


    if (!sysId) {
        gs.error('[AIA AGENTS MSG] Failed to insert the record.');
        return {
            output: sn_i18n.Message.getMessage(
                'sn_aia',
                'Failed to insert the record'
            ),
            status: sn_i18n.Message.getMessage('sn_aia', 'error'),
        };
    }
    return {
        sysId: sysId,
        message: sn_i18n.Message.getMessage(
            'sn_aia',
            'Record successfully inserted into the {table} table.',
            {
                table: tableName,
            }
        ),
        status: sn_i18n.Message.getMessage('sn_aia', 'success'),
    };
})(inputs);`;
    }
    __name(generateCreateScript, "generateCreateScript");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/crud-script-templates/update-script-template.js
var require_update_script_template = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/crud-script-templates/update-script-template.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.generateUpdateScript = generateUpdateScript;
    function generateUpdateScript() {
      return String.raw`(function (inputs) {
    function getChoiceList(tableName, fieldName) {
        var choices = GlideChoiceList.getChoiceList(tableName, fieldName);
        if (!choices) return [];


        var choicesArray = [];
        for (var i = 0; i < choices.getSize(); i++) {
            choicesArray.push({
                value: choices.getChoice(i).getValue(),
                label: choices.getChoice(i).getLabel(),
            });
        }
        return choicesArray;
    }


    // parses query expression containing {{variables}}
    // by replacing {{variableName}} with the value of the variableName property in the inputs object
    // if the variableName property is not found in the inputs object, the variableName is returned as is
    function parseQuery(expression, inputs) {
        if (expression === '') return '';
        return expression.replace(
            /{{\s*(\w+)\s*}}/g,
            function (match, variableName) {
                if (inputs && inputs.hasOwnProperty(variableName)) {
                    return inputs[variableName].toLowerCase();
                }
                return match;
            }
        );
    }


    // parses {{value}} and obtains the correct value for a choice or reference field.
    function parseValue(table, value, field, inputs) {
        if (!value || typeof value === 'object') return '';
        // regex to parse {{var}} and check if the inputs contain that prop
        var fieldValue = value.replace(
            /{{\s*(\w+)\s*}}/g,
            function (match, variableName) {
                if (inputs && inputs.hasOwnProperty(variableName)) {
                    return inputs[variableName];
                }
                return '';
            }
        );
        if (!fieldValue) return field.type === 'string' ? '' : null;
        if (field.type === 'choice') {
            var choices = getChoiceList(table, field.id);
            for (var i = 0; i < choices.length; i++) {
                if (
                    choices[i].value == fieldValue ||
                    choices[i].label
                        .toLowerCase()
                        .includes(fieldValue.toLowerCase())
                ) {
                    return choices[i].value;
                }
            }
            // Neither value nor label matched
            gs.error(
                '[AIA AGENTS MSG] Invalid choice field value or label: {0}',
                fieldValue
            );
        } else if (field.referenceTable) {
            // First check if the input is a valid sys_id
            var refGr = new GlideRecordSecure(field.referenceTable);
            refGr.addQuery('sys_id', fieldValue);
            refGr.query();
            if (refGr.getRowCount() === 1) {
                return fieldValue; // User input is a valid sys_id
            }


            if (field.referenceTableDefaultField) {
                // If not, try to find a record with matching display value
                var refName = field.referenceTableDefaultField;
                refGr = new GlideRecordSecure(field.referenceTable);


                // Check for both exact match and display value containing the input
                var qr = refGr.addQuery(refName, fieldValue);
                qr.addOrCondition(refName, 'CONTAINS', fieldValue);
                refGr.setLimit(1);
                refGr.query();


                if (refGr.next()) {
                    return refGr.getUniqueValue(); // Return the sys_id
                }
            }
            // No match found
            gs.error(
                '[AIA AGENTS MSG] Invalid reference field value or label: {0}',
                fieldValue
            );
        }
        return fieldValue;
    }


    var table = inputs.crudInputs.table.value;
    var tableName = inputs.crudInputs.table.displayValue;
    var query = parseQuery(inputs.crudInputs.query, inputs);
    var fieldValues = inputs.crudInputs.fieldValues
        ? inputs.crudInputs.fieldValues
        : [];
    fieldValues = fieldValues.map(function (item) {
        var fieldValue = item.value ? item.value.fieldValue : '';
        return {
            field: item.field.id,
            value: parseValue(table, fieldValue, item.field, inputs),
        };
    });


    var gr = new GlideRecordSecure(table);
    if (query) {
        gr.addEncodedQuery(query, true);
    } else {
        gs.error('[AIA error] No query provided');
        return {
            message: 'No query provided',
            status: sn_i18n.Message.getMessage('sn_aia', 'error'),
        };
    }


    var updated = [];
    var failed = [];


    gr.query();
    while (gr.next()) {
        if (gr.canWrite()) {
            // Store original record ID
            var recordId = gr.getUniqueValue();


            // Set field values
            fieldValues.forEach(function (item) {
                if (gr.isValidField(item.field)) {
                    gr.setValue(item.field, item.value);
                }
            });


            // Attempt update
            var updatedId = gr.update();


            if (updatedId) {
                updated.push(updatedId);
            } else {
                // Update operation failed entirely
                failed.push(recordId);
            }
        }
    }


    var fullSuccessCount = updated.length;
    var completeFailCount = failed.length;


    var totalCount = fullSuccessCount + completeFailCount;


    // Create simple main message: X out of Y records updated
    var mainMessage;
    if (totalCount === 1) {
        // Single record case
        if (fullSuccessCount === 1) {
            mainMessage = sn_i18n.Message.getMessage(
                'sn_aia',
                '1 out of 1 record updated in the {table} table.',
                {
                    table: tableName,
                }
            );
        } else {
            mainMessage = sn_i18n.Message.getMessage(
                'sn_aia',
                '0 out of 1 record updated in the {table} table.',
                {
                    table: tableName,
                }
            );
        }
    } else {
        // Multiple records case
        mainMessage = sn_i18n.Message.getMessage(
            'sn_aia',
            '{successCount} out of {totalCount} records updated in the {table} table.',
            {
                successCount: fullSuccessCount.toString(),
                totalCount: totalCount.toString(),
                table: tableName,
            }
        );
    }


    // Create detailed messages for different update types
    var successMessage = '';
    var failureMessage = '';


    // Success message
    if (fullSuccessCount > 0) {
        var fullSuccessSingular = sn_i18n.Message.getMessage(
            'sn_aia',
            '{count} record completely updated: {ids}. ',
            {
                count: fullSuccessCount.toString(),
                ids: JSON.stringify(updated),
            }
        );
        var fullSuccessPlural = sn_i18n.Message.getMessage(
            'sn_aia',
            '{count} records completely updated: {ids}. ',
            {
                count: fullSuccessCount.toString(),
                ids: JSON.stringify(updated),
            }
        );


        successMessage = fullSuccessCount === 1 ? fullSuccessSingular : fullSuccessPlural;
    }


    // Complete failure message
    if (completeFailCount > 0) {
        var failureSingular = sn_i18n.Message.getMessage(
            'sn_aia',
            '{count} record failed to update: {ids}. ',
            {
                count: completeFailCount.toString(),
                ids: JSON.stringify(failed),
            }
        );
        var failurePlural = sn_i18n.Message.getMessage(
            'sn_aia',
            '{count} records failed to update: {ids}. ',
            {
                count: completeFailCount.toString(),
                ids: JSON.stringify(failed),
            }
        );


        failureMessage = completeFailCount === 1 ? failureSingular : failurePlural;
    }


    // Build detailed message
    var detailedMessage = '';
    if (successMessage) detailedMessage += successMessage;
    if (failureMessage) detailedMessage += failureMessage;


    var status;
    if (totalCount === 0) {
        status = sn_i18n.Message.getMessage('sn_aia', 'success'); // No records to update
    } else if (failed.length === 0) {
        status = sn_i18n.Message.getMessage('sn_aia', 'success'); // All records fully updated successfully
    } else {
        status = sn_i18n.Message.getMessage('sn_aia', 'error'); // Some records failed to update
    }


    return {
        message: mainMessage,
        detailedMessage: detailedMessage,
        status: status,
    };
})(inputs);`;
    }
    __name(generateUpdateScript, "generateUpdateScript");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/crud-script-templates/delete-script-template.js
var require_delete_script_template = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/crud-script-templates/delete-script-template.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.generateDeleteScript = generateDeleteScript;
    function generateDeleteScript() {
      return String.raw`(function (inputs) {


    // parses query expression containing {{variables}}
    // by replacing {{variableName}} with the value of the variableName property in the inputs object
    // if the variableName property is not found in the inputs object, the variableName is returned as is
    function parseQuery(expression, inputs) {
        if (!expression) return '';
        return expression.replace(
            /{{\s*(\w+)\s*}}/g,
            function (match, variableName) {
                if (inputs && inputs.hasOwnProperty(variableName)) {
                    return inputs[variableName];
                }
                return match;
            }
        );
    }


    // Helper function to log deletion failures
    function logDeletionFailure(recordId, query) {
        gs.error(
            '[AIA error] record not deleted: ' + recordId + ' from the encoded query: ' + query
        );
    }


    const table = inputs.crudInputs.table.value;
    const tableName = inputs.crudInputs.table.displayValue;
    const query = parseQuery(inputs.crudInputs.query, inputs);


    let protectedRecords = 0;
    let deletedRecords = 0;


    const gr = new GlideRecordSecure(table);
    if (query) {
        gr.addEncodedQuery(query, true);
    } else {
        gs.error('[AIA error] No query provided');
        return {
            message: 'No query provided',
            status: sn_i18n.Message.getMessage('sn_aia', 'error'),
        };
    }


    gr.query();
    while (gr.next()) {
        if (gr.canDelete()) {
            if (gr.deleteRecord()) {
                deletedRecords++;
            } else {
                logDeletionFailure(gr.getUniqueValue(), query);
                protectedRecords++;
            }
        } else {
            logDeletionFailure(gr.getUniqueValue(), query);
            protectedRecords++;
        }
    }
    const total = deletedRecords + protectedRecords;
    const message = sn_i18n.Message.getMessage(
        'sn_aia',
        '{count} out of {total} records deleted from the {table} table.',
        {
            count: deletedRecords.toString(),
            total: total.toString(),
            table: tableName,
        }
    );


    const status =
        deletedRecords === total
            ? sn_i18n.Message.getMessage('sn_aia', 'success')
            : sn_i18n.Message.getMessage('sn_aia', 'error');


    return {
        deletedRecords: deletedRecords,
        message: message,
        status: status,
    }; 
})(inputs);`;
    }
    __name(generateDeleteScript, "generateDeleteScript");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/crud-script-templates/crud-script-generator.js
var require_crud_script_generator = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/crud-script-templates/crud-script-generator.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.generateCrudScript = generateCrudScript;
    var tool_input_helper_1 = require_tool_input_helper();
    var lookup_script_template_1 = require_lookup_script_template();
    var create_script_template_1 = require_create_script_template();
    var update_script_template_1 = require_update_script_template();
    var delete_script_template_1 = require_delete_script_template();
    function extractToolInputType(inputsShape) {
      if (!inputsShape) {
        return null;
      }
      const value = inputsShape.getValue();
      if (value && (0, tool_input_helper_1.isToolInputType)(value)) {
        return value;
      }
      return null;
    }
    __name(extractToolInputType, "extractToolInputType");
    function generateCrudScript(inputsShape, toolType) {
      if (toolType !== "crud" || !inputsShape) {
        return "";
      }
      const toolInput = extractToolInputType(inputsShape);
      if (!toolInput || typeof toolInput !== "object") {
        return "";
      }
      const operation = toolInput["operationName"];
      if (!operation) {
        return "";
      }
      switch (operation) {
        case "lookup":
          return (0, lookup_script_template_1.generateLookupScript)();
        case "create":
          return (0, create_script_template_1.generateCreateScript)();
        case "update":
          return (0, update_script_template_1.generateUpdateScript)();
        case "delete":
          return (0, delete_script_template_1.generateDeleteScript)();
        default:
          return "";
      }
    }
    __name(generateCrudScript, "generateCrudScript");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/ai-agent-shapes-helper.js
var require_ai_agent_shapes_helper = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/ai-agent-shapes-helper.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.createAgentRecord = createAgentRecord;
    exports2.createAgentConfigRecord = createAgentConfigRecord;
    exports2.extractPublishedInstructions = extractPublishedInstructions;
    exports2.createAgentVersionRecords = createAgentVersionRecords;
    exports2.createDataAccessConfigRecord = createDataAccessConfigRecord;
    exports2.createTriggerConfigRecord = createTriggerConfigRecord;
    exports2.createTriggerMappingRecord = createTriggerMappingRecord;
    exports2.createAgentToolM2MRecord = createAgentToolM2MRecord;
    exports2.createAgentToolRecord = createAgentToolRecord;
    exports2.createCategoryMappingRecord = createCategoryMappingRecord;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var tool_input_helper_1 = require_tool_input_helper();
    var ai_agent_transform_helper_1 = require_ai_agent_transform_helper();
    var constants_1 = require_constants();
    var crud_script_generator_1 = require_crud_script_generator();
    function extractTargetDocument(toolDetails, toolType) {
      if (!toolType) {
        return "";
      }
      const fieldMap = {
        capability: "capabilityId",
        subflow: "subflowId",
        action: "flowActionId",
        catalog: "catalogItemId",
        topic: "virtualAgentId",
        topic_block: "virtualAgentId"
      };
      const fieldName = fieldMap[toolType];
      if (!fieldName) {
        return (0, ai_agent_transform_helper_1.extractSysId)(toolDetails.get("targetDocument")) || "";
      }
      const fieldValue = toolDetails.get(fieldName);
      return (0, ai_agent_transform_helper_1.extractSysId)(fieldValue) || "";
    }
    __name(extractTargetDocument, "extractTargetDocument");
    function serializeToJson(shape, toolType, defaultValue = "") {
      if (!shape) {
        return defaultValue;
      }
      if (shape.isArray()) {
        return JSON.stringify(shape.getElements().map((element) => element.getValue()));
      } else if (shape.isObject()) {
        const objectValue = shape.getValue();
        if (toolType === "crud" && (0, tool_input_helper_1.isToolInputType)(objectValue)) {
          const transformedInputs = (0, tool_input_helper_1.generateToolInputs)(objectValue);
          return JSON.stringify(transformedInputs);
        }
        if (toolType === "search_retrieval") {
          const transformedInputs = (0, tool_input_helper_1.generateRagInputs)(objectValue);
          return JSON.stringify(transformedInputs);
        }
        return JSON.stringify(objectValue);
      }
      const value = shape.getValue();
      return value !== void 0 ? String(value) : defaultValue;
    }
    __name(serializeToJson, "serializeToJson");
    function extractInputsArray(inputsShape) {
      if (inputsShape.isArray()) {
        return inputsShape.getElements().map((element) => element.getValue());
      }
      if (inputsShape.isString()) {
        const inputsStr = inputsShape.getValue();
        if (!inputsStr || inputsStr === "" || inputsStr === "[]") {
          return [];
        }
        return JSON.parse(inputsStr);
      }
      if (inputsShape.isObject()) {
        const value = inputsShape.getValue();
        if ((0, tool_input_helper_1.isToolInputType)(value)) {
          return (0, tool_input_helper_1.generateToolInputs)(value);
        }
        const objShape = inputsShape.asObject();
        const inputFieldsShape = objShape.get("inputFields");
        if (!inputFieldsShape) {
          return [];
        }
        if (inputFieldsShape.isArray()) {
          return inputFieldsShape.getElements().map((element) => element.getValue());
        }
        const fieldValue = inputFieldsShape.getValue();
        return Array.isArray(fieldValue) ? fieldValue : [];
      }
      return [];
    }
    __name(extractInputsArray, "extractInputsArray");
    function transformInputToSchemaEntry(input) {
      const schemaEntry = {};
      if (typeof input !== "object" || input === null) {
        return schemaEntry;
      }
      const inputObj = input;
      if (inputObj["name"] !== void 0) {
        schemaEntry.name = String(inputObj["name"]);
      }
      if (inputObj["description"] !== void 0) {
        if (inputObj["name"] === "crudInputs") {
          schemaEntry.description = "CRUD Input Variables. These are already pre-defined. Under no circumstances should you prompt the user for this information or attempt to change these values.";
        } else {
          schemaEntry.description = String(inputObj["description"]);
        }
      }
      if (inputObj["mandatory"] !== void 0) {
        schemaEntry.mandatory = Boolean(inputObj["mandatory"]);
      }
      return schemaEntry;
    }
    __name(transformInputToSchemaEntry, "transformInputToSchemaEntry");
    function generateInputSchemaFromInputs(inputsShape) {
      if (!inputsShape) {
        return "[]";
      }
      try {
        const inputsArray = extractInputsArray(inputsShape);
        if (inputsArray.length === 0) {
          return "[]";
        }
        const schema = inputsArray.map(transformInputToSchemaEntry);
        return JSON.stringify(schema);
      } catch {
        return "[]";
      }
    }
    __name(generateInputSchemaFromInputs, "generateInputSchemaFromInputs");
    async function createAgentRecord(factory, callExpression, computedInternalName, tableName, publishedInstructions, aclId) {
      const arg = callExpression.getArgument(0).asObject();
      const explicitId = arg.get("$id");
      return await factory.createRecord({
        source: callExpression,
        table: tableName,
        explicitId,
        properties: arg.transform(({ $ }) => ({
          name: $.from("name"),
          description: $.from("description"),
          role: $.from("agentRole"),
          record_type: $.from("recordType").def("template"),
          agent_type: $.from("agentType").def("internal"),
          instructions: $.val(publishedInstructions),
          sys_domain: $.from("sysDomain").def("global"),
          strategy: $.val(constants_1.REACT_STRATEGY_SYS_ID),
          processing_message: $.from("processingMessage"),
          post_processing_message: $.from("postProcessingMessage"),
          context_processing_script: $.from("contextProcessingScript").def(constants_1.DEFAULT_CONTEXT_PROCESSING_SCRIPT).toCdata(),
          advanced_mode: $.from("advancedMode").def(false),
          channel: $.from("channel").def("nap_and_va"),
          condition: $.from("condition"),
          compiled_handbook: $.from("compiledHandbook").def(""),
          external: $.from("external").def(false),
          parent: $.from("parent").def(""),
          source_id: $.from("sourceId").def(""),
          internal_name: $.val(computedInternalName),
          external_agent_configuration: $.from("externalAgentConfiguration").def(""),
          active: $.from("active").def(true),
          acl: $.val(aclId ?? "")
        }))
      });
    }
    __name(createAgentRecord, "createAgentRecord");
    async function createAgentConfigRecord(factory, callExpression, agentRecordId, arg, tableName) {
      return await factory.createRecord({
        source: callExpression,
        table: tableName,
        properties: arg.transform(({ $ }) => ({
          agent: $.val(agentRecordId),
          active: $.from("active").def(true),
          public: $.from("public").def(false),
          agent_learning: $.from("agentLearning").def(false),
          doc_url: $.from("docUrl"),
          icon_url: $.from("iconUrl"),
          run_as_user: $.from("runAsUser").def(""),
          sys_overrides: $.from("agentConfigSysOverrides").def(""),
          agent_descriptor: $.from("agentDescriptor").def(""),
          sys_domain: $.def("global")
        }))
      });
    }
    __name(createAgentConfigRecord, "createAgentConfigRecord");
    function extractPublishedInstructions(arg) {
      const versionDetailsArray = arg.get("versionDetails")?.ifArray();
      if (!versionDetailsArray) {
        return { instructions: "", publishedCount: 0 };
      }
      let publishedInstructions = "";
      let publishedCount = 0;
      const versionElements = versionDetailsArray.getElements();
      for (const versionElement of versionElements) {
        const versionObj = versionElement.asObject();
        const state = versionObj.get("state")?.ifString()?.getValue();
        if (state === "published") {
          publishedCount++;
          publishedInstructions = versionObj.get("instructions")?.ifString()?.getValue() || "";
        }
      }
      return { instructions: publishedInstructions, publishedCount };
    }
    __name(extractPublishedInstructions, "extractPublishedInstructions");
    async function createAgentVersionRecords(factory, callExpression, agentRecordId, tableName, agentTableName) {
      const arg = callExpression.getArgument(0).asObject();
      const versionDetailsArray = arg.get("versionDetails")?.ifArray();
      if (versionDetailsArray) {
        const versionElements = versionDetailsArray.getElements();
        return await Promise.all(versionElements.map(async (versionElement) => {
          const versionDetails = versionElement.asObject();
          return await factory.createRecord({
            source: callExpression,
            table: tableName,
            properties: versionDetails.transform(({ $ }) => ({
              state: $.from("state").def("published"),
              instructions: $.from("instructions"),
              version_name: $.from("name").def("V1"),
              condition: $.from("condition"),
              target_id: $.val(agentRecordId),
              sys_domain: $.def("global"),
              version_number: $.from("number").def(1),
              target_table: $.def(agentTableName)
            }))
          });
        }));
      }
      const defaultVersionRecord = await factory.createRecord({
        source: callExpression,
        table: tableName,
        properties: arg.transform(({ $ }) => ({
          state: $.def("draft"),
          instructions: $.def(""),
          version_name: $.from("name").def("V1"),
          target_id: $.val(agentRecordId),
          sys_domain: $.def("global"),
          version_number: $.def(1),
          target_table: $.def(agentTableName)
        }))
      });
      return [defaultVersionRecord];
    }
    __name(createAgentVersionRecords, "createAgentVersionRecords");
    function resolveRoleToSysId(roleValue, diagnostics) {
      const recordRef = roleValue.ifRecord();
      if (recordRef) {
        return recordRef.getId().getValue();
      }
      const roleStr = roleValue.asString()?.getValue();
      if (!roleStr) {
        return void 0;
      }
      if ((0, sdk_build_core_1.isGUID)(roleStr)) {
        return roleStr;
      }
      diagnostics.error(roleValue, `roleList must contain only role sys_ids (GUIDs), not role names. Got: '${roleStr}'. Use the role sys_id when using roleList (e.g., '2831a114c611228501d4ea6c309d626d' for admin), or use roleMap for role names.`);
      return void 0;
    }
    __name(resolveRoleToSysId, "resolveRoleToSysId");
    async function createDataAccessConfigRecord(factory, callExpression, agentRecordId, dataAccess, tableName, agentTableName, diagnostics, hasRunAsUser = false) {
      const roleListArray = dataAccess.get("roleList")?.ifArray();
      const restrictionGUIDs = [];
      if (roleListArray) {
        const roleElements = roleListArray.getElements();
        const resolvedSysIds = await Promise.all(roleElements.map((roleValue) => resolveRoleToSysId(roleValue, diagnostics)));
        for (const sysId of resolvedSysIds) {
          if (sysId) {
            restrictionGUIDs.push(sysId);
          }
        }
      }
      const roleListValue = restrictionGUIDs.join(",");
      const roleMapField = dataAccess.get("roleMap");
      const roleMapArray = roleMapField?.ifArray();
      const validatedRoleMapItems = [];
      if (roleMapArray) {
        for (const role of roleMapArray.getElements()) {
          if (role.ifString()) {
            const roleValue = role.asString().getValue();
            if ((0, sdk_build_core_1.isGUID)(roleValue)) {
              diagnostics.error(roleMapField, `roleMap must contain role names, not sys_ids. Got: '${roleValue}'. Use the role name (e.g., 'admin'), or move the sys_id to dataAccess.roleList.`);
            } else if (roleValue.trim() !== "") {
              validatedRoleMapItems.push(role);
            }
          } else {
            validatedRoleMapItems.push(role);
          }
        }
      }
      if (!hasRunAsUser && restrictionGUIDs.length === 0 && validatedRoleMapItems.length === 0) {
        diagnostics.error(dataAccess, "dataAccess must have at least one of roleList or roleMap non-empty: provide sys_ids in roleList, role names in roleMap, or both.");
      }
      const roleConfigRecord = await factory.createRecord({
        source: callExpression,
        table: tableName,
        properties: dataAccess.transform(({ $ }) => ({
          agent: $.val(agentRecordId.getValue()),
          agent_table: $.val(agentTableName),
          action: $.val("limit_to_roles"),
          role_list: $.val(roleListValue),
          description: $.from("description").def(""),
          name: $.val(agentRecordId.getValue())
        }))
      });
      const records = [roleConfigRecord];
      for (const role of validatedRoleMapItems) {
        const roleReference = role.ifString() ? await factory.createReference({
          source: role,
          table: constants_1.SYS_USER_ROLE,
          keys: { name: role.asString().getValue() }
        }) : role;
        const mappingRecord = await factory.createRecord({
          source: role,
          table: constants_1.SYS_AGENT_ACCESS_ROLE_MAPPING,
          properties: {
            agent_access_config: roleConfigRecord,
            role: roleReference
          }
        });
        records.push(mappingRecord);
      }
      return records;
    }
    __name(createDataAccessConfigRecord, "createDataAccessConfigRecord");
    async function createTriggerConfigRecord(factory, callExpression, triggerConfig, computedTriggerInternalName, tableName) {
      const schedule = triggerConfig.get("schedule")?.ifObject();
      return await factory.createRecord({
        source: callExpression,
        table: tableName,
        properties: triggerConfig.transform(({ $ }) => ({
          active: $.from("active").def(false),
          business_rule: $.from("businessRule").def(""),
          channel: $.from("channel").def(""),
          condition: $.from("triggerCondition").def(""),
          description: $.from("description"),
          enable_discovery: $.from("enableDiscovery").def(false),
          internal_name: $.val(computedTriggerInternalName),
          name: $.from("name"),
          notification_script: $.from("notificationScript").def(""),
          objective_template: $.from("objectiveTemplate").def(""),
          portal: $.from("portal").def(""),
          profile: $.from("profile").def(""),
          run_as: $.from("runAs"),
          run_as_script: $.from("runAsScript").def(""),
          run_as_user: $.from("runAsUser"),
          run_dayofmonth: $.val(schedule?.get("runDayOfMonth")?.getValue()),
          run_dayofweek: $.val(schedule?.get("runDayOfWeek")?.getValue()),
          run_period: $.val(schedule?.get("repeatInterval")?.getValue()),
          run_start: $.val(schedule?.get("starting")?.getValue()),
          run_time: $.val(schedule?.get("time")?.getValue()),
          show_notifications: $.from("showNotifications").def(false),
          sys_domain: $.from("domain").def("global"),
          sys_overrides: $.from("sysOverrides").def(""),
          target_table: $.from("targetTable"),
          trigger_flow: $.from("triggerFlow").def(""),
          trigger_flow_definition_type: $.from("triggerFlowDefinitionType"),
          trigger_strategy: $.val(schedule?.get("triggerStrategy")?.getValue()),
          usecase: $.from("usecase").def(""),
          usecase_discovery: $.from("usecaseDiscovery").def(false)
        }))
      });
    }
    __name(createTriggerConfigRecord, "createTriggerConfigRecord");
    async function createTriggerMappingRecord(factory, callExpression, agentRecordId, triggerConfig, linkedTriggerConfigId, tableName) {
      return await factory.createRecord({
        source: callExpression,
        table: tableName,
        properties: triggerConfig.transform(({ $ }) => ({
          active: $.from("active").def(false),
          objective_template: $.from("objectiveTemplate").toCdata(),
          related_resource_record: $.val(agentRecordId),
          related_resource_table: $.val("sn_aia_agent"),
          sys_domain: $.from("domain").def("global"),
          sys_overrides: $.from("m2mSysOverrides").def(""),
          trigger_configuration: $.val(linkedTriggerConfigId)
        }))
      });
    }
    __name(createTriggerMappingRecord, "createTriggerMappingRecord");
    async function createAgentToolM2MRecord(factory, callExpression, agentRecordId, toolConfig, toolSysId, tableName) {
      return await factory.createRecord({
        source: callExpression,
        table: tableName,
        properties: toolConfig.transform(({ $ }) => ({
          max_auto_executions: $.from("maxAutoExecutions").def(10),
          active: $.from("active").def(true),
          post_message: $.from("postMessage"),
          pre_message: $.from("preMessage"),
          sys_domain: $.def("global"),
          transformation_instructions: $.from("transformationInstructions"),
          execution_mode: $.from("executionMode").def("autopilot"),
          agent: $.val(agentRecordId),
          document_status: $.from("documentStatus"),
          tool: $.val(toolSysId),
          tool_attributes: $.from("toolAttributes").def("{}"),
          name: $.from("name"),
          output_transformation_strategy: $.from("outputTransformationStrategy"),
          inputs: $.from("inputs").map((inputs) => {
            if (toolSysId === constants_1.WEB_SEARCH_TOOL_SYS_ID) {
              const inputsArray = inputs.ifArray();
              if (!inputsArray || inputsArray.getElements().length === 0) {
                return constants_1.WEB_SEARCH_DEFAULT_INPUTS;
              }
            }
            const toolType = toolConfig.get("type")?.ifString()?.getValue();
            return serializeToJson(inputs, toolType, "[]");
          }).def(toolSysId === constants_1.WEB_SEARCH_TOOL_SYS_ID ? constants_1.WEB_SEARCH_DEFAULT_INPUTS : "[]"),
          display_output: $.from("displayOutput").def(false),
          description: $.from("description"),
          timeout: $.from("timeout"),
          pre_run: $.from("preRun").def(false),
          post_processing_script: $.from("postProcessingScript").def(""),
          widgets: $.from("widgets").map((v) => {
            if (v.isArray()) {
              return v.getElements().map((w) => {
                if (w.isRecord()) {
                  return w.getId().getValue();
                }
                return w.getValue();
              }).join(",");
            }
            return v.ifString()?.getValue() || "";
          }),
          // Use m2mSysOverrides for M2M record overrides, never fall back to sysOverrides
          // This ensures strict separation between tool record overrides and M2M record overrides
          sys_overrides: $.from("m2mSysOverrides").def("")
        }))
      });
    }
    __name(createAgentToolM2MRecord, "createAgentToolM2MRecord");
    function mapToolTypeToInternal(toolType) {
      if (toolType === "search_retrieval") {
        return "rag";
      }
      return toolType;
    }
    __name(mapToolTypeToInternal, "mapToolTypeToInternal");
    async function createAgentToolRecord(factory, callExpression, toolDetails, tableName) {
      const userToolType = toolDetails.get("type")?.ifString()?.getValue();
      const toolType = mapToolTypeToInternal(userToolType);
      const targetDocumentTable = toolType ? constants_1.TOOL_TYPE_TO_TARGET_TABLE_MAP[toolType] || "" : "";
      const targetDocument = extractTargetDocument(toolDetails, toolType);
      const inputsShape = toolDetails.get("inputs");
      const autoGeneratedScript = (0, crud_script_generator_1.generateCrudScript)(inputsShape, toolType);
      return await factory.createRecord({
        source: callExpression,
        table: tableName,
        properties: toolDetails.transform(({ $ }) => ({
          name: $.from("name"),
          target_document: $.val(targetDocument),
          target_document_table: $.val(targetDocumentTable),
          script: autoGeneratedScript ? $.val(autoGeneratedScript).toCdata() : $.from("script").def("").toCdata(),
          post_processing_script: $.from("postProcessingScript").def(""),
          record_type: $.from("recordType").def("template"),
          description: $.from("description"),
          active: $.from("active").def(true),
          sys_domain: $.def("global"),
          sys_overrides: $.from("sysOverrides").def(""),
          type: $.val(toolType),
          input_schema: $.val(generateInputSchemaFromInputs(inputsShape))
        }))
      });
    }
    __name(createAgentToolRecord, "createAgentToolRecord");
    async function createCategoryMappingRecord(factory, callExpression, agentRecordId, categorySysId) {
      const arg = callExpression.getArgument(0).asObject();
      return await factory.createRecord({
        source: callExpression,
        table: constants_1.SN_AIA_LTM_CATEGORY_MAPPING,
        properties: arg.transform(({ $ }) => ({
          entity_table: $.val("sn_aia_agent"),
          entity_id: $.val(agentRecordId),
          category: $.val(categorySysId)
        }))
      });
    }
    __name(createCategoryMappingRecord, "createCategoryMappingRecord");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/security-acl-processor.js
var require_security_acl_processor = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/security-acl-processor.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processSecurityAcl = processSecurityAcl;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var constants_1 = require_constants();
    var SYS_SECURITY_ACL = "sys_security_acl";
    async function processSecurityAcl(securityAcl, aclName, factory, diagnostics) {
      const records = [];
      const accessType = securityAcl.get("type").asString().getValue();
      if (accessType !== "Any authenticated user" && accessType !== "Specific role" && accessType !== "Public") {
        diagnostics.error(securityAcl, `securityAcl.type must be 'Any authenticated user', 'Specific role', or 'Public'. Got: '${accessType}'`);
      }
      const securityAttribute = accessType === "Any authenticated user" ? constants_1.USER_IS_AUTHENTICATED : "";
      let aclRoles = [];
      const rolesField = securityAcl.has("roles") ? securityAcl.get("roles") : void 0;
      if (accessType === "Specific role") {
        if (!rolesField || !rolesField.isArray()) {
          diagnostics.error(securityAcl, "securityAcl.roles must be an array when type is 'Specific role'");
        } else {
          aclRoles = rolesField.asArray().getElements();
          if (aclRoles.length === 0) {
            diagnostics.error(rolesField, "securityAcl.roles must contain at least one role");
          }
        }
      }
      const aclExplicitId = securityAcl.has("$id") ? securityAcl.get("$id") : void 0;
      const aclRecord = await factory.createRecord({
        source: securityAcl,
        table: SYS_SECURITY_ACL,
        ...aclExplicitId instanceof sdk_build_plugins_1.NowIdShape ? { explicitId: aclExplicitId } : {},
        properties: securityAcl.transform(({ $ }) => ({
          name: $.val(aclName),
          type: $.val(constants_1.GEN_AI_AGENT_TYPE),
          operation: $.val(constants_1.ACL_DEFAULTS.operation),
          active: $.val(constants_1.ACL_DEFAULTS.active),
          admin_overrides: $.val(constants_1.ACL_DEFAULTS.adminOverrides),
          advanced: $.val(constants_1.ACL_DEFAULTS.advanced),
          decision_type: $.val(constants_1.ACL_DEFAULTS.decisionType),
          local_or_existing: $.val(constants_1.ACL_DEFAULTS.localOrExisting),
          security_attribute: $.val(securityAttribute)
        }))
      });
      records.push(aclRecord);
      if (accessType === "Public") {
        const publicRoleReference = await factory.createReference({
          source: securityAcl,
          table: constants_1.SYS_USER_ROLE,
          guid: constants_1.PUBLIC_ACCESS_ROLE_SYS_ID
        });
        const publicRoleRecord = await factory.createRecord({
          source: securityAcl,
          table: constants_1.SYS_SECURITY_ACL_ROLE,
          properties: {
            sys_security_acl: aclRecord,
            sys_user_role: publicRoleReference
          }
        });
        records.push(publicRoleRecord);
      }
      for (const role of aclRoles) {
        const roleReference = role.isString() ? await factory.createReference({
          source: role,
          table: constants_1.SYS_USER_ROLE,
          keys: (0, sdk_build_core_1.isGUID)(role.asString().getValue()) ? void 0 : { name: role.asString().getValue() },
          guid: (0, sdk_build_core_1.isGUID)(role.asString().getValue()) ? role.asString().getValue() : void 0
        }) : role;
        const aclRoleRecord = await factory.createRecord({
          source: role,
          table: constants_1.SYS_SECURITY_ACL_ROLE,
          properties: {
            sys_security_acl: aclRecord,
            sys_user_role: roleReference
          }
        });
        records.push(aclRoleRecord);
      }
      return { aclRecord, allRecords: records };
    }
    __name(processSecurityAcl, "processSecurityAcl");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/ai-agent-plugin.js
var require_ai_agent_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/ai-agent-plugin.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.AiAgentPlugin = void 0;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var ai_agent_transform_helper_1 = require_ai_agent_transform_helper();
    var ai_agent_shapes_helper_1 = require_ai_agent_shapes_helper();
    var constants_1 = require_constants();
    var security_acl_processor_1 = require_security_acl_processor();
    function normalizeScript(script) {
      if (!script) {
        return "";
      }
      return script.replace(/\s+/g, " ").trim();
    }
    __name(normalizeScript, "normalizeScript");
    var NORMALIZED_DEFAULT_CONTEXT_PROCESSING = normalizeScript(constants_1.DEFAULT_CONTEXT_PROCESSING_SCRIPT);
    exports2.AiAgentPlugin = sdk_build_core_1.Plugin.create({
      name: "AiAgentPlugin",
      docs: [
        {
          apiName: "AiAgent",
          tags: ["sn_aia_agent"]
        }
      ],
      records: {
        [constants_1.SN_AI_AGENT]: {
          relationships: {
            [constants_1.SN_AI_AGENT_CONFIG]: {
              via: "agent",
              descendant: true
            },
            [constants_1.SN_AI_AGENT_VERSION]: {
              via: "target_id",
              descendant: true
            },
            [constants_1.SN_AI_AGENT_TOOL_M2M]: {
              via: "agent",
              descendant: true,
              relationships: {
                [constants_1.SN_AI_AGENT_TOOL]: {
                  via: "tool",
                  inverse: true,
                  descendant: true
                }
              }
            },
            [constants_1.SYS_AGENT_ACCESS_ROLE_CONFIG]: {
              via: "agent",
              descendant: true,
              relationships: {
                [constants_1.SYS_AGENT_ACCESS_ROLE_MAPPING]: {
                  via: "agent_access_config",
                  descendant: true,
                  relationships: {
                    [constants_1.SYS_USER_ROLE]: {
                      via: "role",
                      inverse: true
                    }
                  }
                }
              }
            },
            [constants_1.SN_AIA_TRIGGER_MAPPING]: {
              via: "related_resource_record",
              descendant: true,
              relationships: {
                [constants_1.SN_AIA_TRIGGER_CONFIG]: {
                  via: "trigger_configuration",
                  inverse: true,
                  descendant: true
                }
              }
            },
            [constants_1.SN_AIA_LTM_CATEGORY_MAPPING]: {
              via: "entity_id",
              descendant: true
            },
            [constants_1.SYS_SECURITY_ACL]: {
              via: [{ parent: ["internal_name", "\\.(.*)"], child: ["name", "\\.(.*)"] }],
              descendant: true,
              relationships: {
                [constants_1.SYS_SECURITY_ACL_ROLE]: {
                  via: "sys_security_acl",
                  descendant: true
                }
              }
            }
          },
          async toShape(record, { transform, descendants, database }) {
            let contextProcessingScript;
            if (record.has("context_processing_script")) {
              const scriptValue = record.get("context_processing_script").ifString()?.getValue();
              const normalizedScript = normalizeScript(scriptValue);
              if (scriptValue && normalizedScript !== NORMALIZED_DEFAULT_CONTEXT_PROCESSING) {
                contextProcessingScript = await sdk_build_plugins_1.NowIncludeShape.fromRecord(record, record.get("context_processing_script"), transform);
              }
            }
            const config = descendants.query(constants_1.SN_AI_AGENT_CONFIG)[0];
            const versions = descendants.query(constants_1.SN_AI_AGENT_VERSION);
            const toolM2Ms = descendants.query(constants_1.SN_AI_AGENT_TOOL_M2M);
            const toolRecords = descendants.query(constants_1.SN_AI_AGENT_TOOL);
            const dataAccessConfig = descendants.query(constants_1.SYS_AGENT_ACCESS_ROLE_CONFIG)[0];
            const roleMappings = descendants.query(constants_1.SYS_AGENT_ACCESS_ROLE_MAPPING);
            const triggerMappings = descendants.query(constants_1.SN_AIA_TRIGGER_MAPPING);
            const triggerConfigs = descendants.query(constants_1.SN_AIA_TRIGGER_CONFIG);
            const categoryMappings = descendants.query(constants_1.SN_AIA_LTM_CATEGORY_MAPPING);
            const internalName = record.get("internal_name").ifString()?.getValue() || "";
            const matchingAcl = database.get(constants_1.SYS_SECURITY_ACL, {
              name: internalName,
              type: constants_1.GEN_AI_AGENT_TYPE
            });
            const matchingAclId = matchingAcl?.getId().getValue();
            const aclRoleRecords = matchingAclId ? descendants.query(constants_1.SYS_SECURITY_ACL_ROLE).filter((r) => {
              const aclRef = r.get("sys_security_acl");
              const refId = aclRef?.isRecord() ? aclRef.getId().getValue() : aclRef?.ifString()?.getValue();
              return refId === matchingAclId;
            }) : [];
            const securityAclValue = (0, ai_agent_transform_helper_1.computeSecurityAclValue)(matchingAcl, aclRoleRecords);
            const tools = toolM2Ms.length > 0 ? await (0, ai_agent_transform_helper_1.mapToolsFromM2M)(toolM2Ms, toolRecords, transform) : [];
            const agentConfigValues = (0, ai_agent_transform_helper_1.computeAgentConfigValue)(config);
            const versionDetailsValue = (0, ai_agent_transform_helper_1.computeVersionDetailsValue)(versions);
            const dataAccessValue = (0, ai_agent_transform_helper_1.computeDataAccessValue)(dataAccessConfig, roleMappings);
            const agentTypeValue = record.get("agent_type")?.ifString()?.getValue() || "internal";
            const channelValue = record.get("channel")?.ifString()?.getValue() || "nap_and_va";
            return {
              success: true,
              value: new sdk_build_core_1.CallExpressionShape({
                source: record,
                callee: "AiAgent",
                args: [
                  record.transform(({ $ }) => ({
                    $id: $.val(sdk_build_plugins_1.NowIdShape.from(record) ?? record.getId().getValue()),
                    name: $.from("name"),
                    description: $.from("description"),
                    externalAgentConfiguration: $.from("external_agent_configuration").def(""),
                    external: $.from("external").toBoolean().def(false),
                    active: $.from("active").toBoolean().def(true),
                    parent: $.from("parent").def(""),
                    agentRole: $.from("role"),
                    recordType: $.from("record_type").def("template"),
                    contextProcessingScript: contextProcessingScript ? $.val(contextProcessingScript) : $.val(void 0),
                    agentType: $.val(agentTypeValue).def("internal"),
                    sysDomain: $.from("sys_domain").def("global"),
                    sourceId: $.from("source_id").def(""),
                    processingMessage: $.from("processing_message").def(""),
                    postProcessingMessage: $.from("post_processing_message").def(""),
                    advancedMode: $.from("advanced_mode").toBoolean().def(false),
                    channel: $.val(channelValue).def("nap_and_va"),
                    condition: $.from("condition").def(""),
                    compiledHandbook: $.from("compiled_handbook").def(""),
                    versionDetails: $.val(versionDetailsValue),
                    public: $.val(agentConfigValues?.public),
                    agentLearning: $.val(agentConfigValues?.agentLearning),
                    docUrl: $.val(agentConfigValues?.docUrl),
                    iconUrl: $.val(agentConfigValues?.iconUrl),
                    runAsUser: $.val(agentConfigValues?.runAsUser),
                    agentConfigSysOverrides: $.val(agentConfigValues?.agentConfigSysOverrides),
                    agentDescriptor: $.val(agentConfigValues?.agentDescriptor),
                    tools: $.val(tools.length > 0 ? tools : void 0),
                    securityAcl: $.val(securityAclValue),
                    dataAccess: $.val(dataAccessValue),
                    triggerConfig: $.val(triggerConfigs.length > 0 ? (0, ai_agent_transform_helper_1.mapTriggerConfigs)(triggerConfigs, triggerMappings) : []),
                    memoryCategories: $.val(categoryMappings.length > 0 ? categoryMappings.map((m) => {
                      const categorySysId = m.get("category")?.ifString()?.getValue();
                      if (!categorySysId) {
                        return void 0;
                      }
                      return constants_1.SYS_ID_TO_MEMORY_CATEGORY_MAP[categorySysId] ?? categorySysId;
                    }).filter(Boolean) : void 0)
                  }))
                ]
              })
            };
          },
          async toFile(record, context) {
            const { config, transform, descendants, database } = context;
            const agentAclIds = new Set(descendants.query(constants_1.SYS_SECURITY_ACL).map((r) => r.getId().getValue()));
            const deleteAclRoles = database.query(constants_1.SYS_SECURITY_ACL_ROLE).filter((rec) => {
              if (rec.getAction() !== "DELETE") {
                return false;
              }
              const aclRef = rec.get("sys_security_acl")?.toString().getValue();
              return aclRef ? agentAclIds.has(aclRef) : false;
            });
            if (deleteAclRoles.length === 0) {
              return { success: false };
            }
            const recordBuilder = (0, sdk_build_core_1.unloadBuilder)(config);
            const updateName = await transform.getUpdateName(record);
            const builder = recordBuilder.record(record, updateName);
            record.entries().sort(([a], [b]) => a.localeCompare(b)).forEach(([prop, shape]) => {
              builder.field(prop, shape);
            });
            const serializeDescendant = /* @__PURE__ */ __name(async (descendantRecord) => {
              const descUpdateName = await transform.getUpdateName(descendantRecord);
              const descBuilder = (0, sdk_build_core_1.unloadBuilder)(config);
              const descRecordBuilder = descBuilder.record(descendantRecord, descUpdateName);
              descendantRecord.entries().sort(([a], [b]) => a.localeCompare(b)).forEach(([prop, shape]) => {
                descRecordBuilder.field(prop, shape);
              });
              return {
                source: descendantRecord,
                name: `${descUpdateName}.xml`,
                category: descendantRecord.getInstallCategory(),
                content: descBuilder.end()
              };
            }, "serializeDescendant");
            const descendantFiles = [];
            for (const table of [
              constants_1.SN_AI_AGENT_CONFIG,
              constants_1.SN_AI_AGENT_VERSION,
              constants_1.SN_AI_AGENT_TOOL_M2M,
              constants_1.SN_AI_AGENT_TOOL,
              constants_1.SYS_AGENT_ACCESS_ROLE_CONFIG,
              constants_1.SYS_AGENT_ACCESS_ROLE_MAPPING,
              constants_1.SN_AIA_TRIGGER_MAPPING,
              constants_1.SN_AIA_TRIGGER_CONFIG,
              constants_1.SN_AIA_LTM_CATEGORY_MAPPING,
              constants_1.SYS_SECURITY_ACL,
              constants_1.SYS_SECURITY_ACL_ROLE
            ]) {
              for (const rec of descendants.query(table)) {
                descendantFiles.push(await serializeDescendant(rec));
              }
            }
            for (const rec of deleteAclRoles) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            return {
              success: true,
              value: [
                {
                  source: record,
                  name: `${updateName}.xml`,
                  category: record.getInstallCategory(),
                  content: recordBuilder.end()
                },
                ...descendantFiles
              ]
            };
          }
        },
        [constants_1.SN_AI_AGENT_CONFIG]: {
          coalesce: ["agent"]
        },
        [constants_1.SN_AI_AGENT_VERSION]: {
          coalesce: ["target_id", "version_name"]
        },
        [constants_1.SN_AI_AGENT_TOOL]: {
          composite: true,
          coalesce: ["name"],
          async toShape(record, { transform }) {
            let script;
            let postProcessingScript;
            if (record.has("script")) {
              script = await sdk_build_plugins_1.NowIncludeShape.fromRecord(record, record.get("script"), transform);
            }
            if (record.has("post_processing_script")) {
              postProcessingScript = await sdk_build_plugins_1.NowIncludeShape.fromRecord(record, record.get("post_processing_script"), transform);
            }
            return {
              success: true,
              value: record.transform(({ $ }) => ({
                name: $,
                description: $,
                targetDocument: $.from("target_document"),
                active: $.toBoolean().map((v) => {
                  const value = v.getValue();
                  return value === true ? void 0 : value;
                }),
                recordType: $.from("record_type").def("template"),
                type: $,
                script: script ? $.val(script) : $.from("script"),
                postProcessingScript: postProcessingScript ? $.val(postProcessingScript) : $.from("post_processing_script"),
                sysOverrides: $.from("sys_overrides")
              }))
            };
          }
        },
        [constants_1.SN_AI_AGENT_TOOL_M2M]: {
          coalesce: ["agent", "tool", "name"]
        },
        [constants_1.SYS_AGENT_ACCESS_ROLE_CONFIG]: {
          coalesce: ["agent"]
        },
        [constants_1.SYS_AGENT_ACCESS_ROLE_MAPPING]: {
          coalesce: ["agent_access_config", "role"]
        },
        [constants_1.SYS_SECURITY_ACL_ROLE]: {
          coalesce: ["sys_security_acl", "sys_user_role"]
        },
        [constants_1.SN_AIA_TRIGGER_CONFIG]: {
          coalesce: ["name"],
          async toShape(record, { transform }) {
            const usecaseRef = record.get("usecase")?.ifString()?.ifNotEmpty()?.getValue();
            if (usecaseRef) {
              return { success: false };
            }
            let runAsScript;
            if (record.has("run_as_script")) {
              runAsScript = await sdk_build_plugins_1.NowIncludeShape.fromRecord(record, record.get("run_as_script"), transform);
            }
            const runDayOfMonth = record.get("run_dayofmonth")?.ifString()?.ifNotEmpty()?.toNumber()?.getValue();
            const runDayOfWeek = record.get("run_dayofweek")?.ifString()?.ifNotEmpty()?.toNumber()?.getValue();
            const repeatInterval = record.get("run_period")?.ifString()?.getValue();
            const starting = record.get("run_start")?.ifString()?.getValue();
            const time = record.get("run_time")?.ifString()?.getValue();
            const triggerStrategy = record.get("trigger_strategy")?.ifString()?.getValue();
            const schedule = runDayOfMonth || runDayOfWeek || repeatInterval || starting || time || triggerStrategy ? {
              ...runDayOfMonth && { runDayOfMonth },
              ...runDayOfWeek && { runDayOfWeek },
              ...repeatInterval && { repeatInterval },
              ...starting && { starting },
              ...time && { time },
              ...triggerStrategy && { triggerStrategy }
            } : void 0;
            const channelSysId = record.get("channel")?.ifString()?.getValue();
            const channelValue = channelSysId ? constants_1.CHANNEL_SYS_ID_TO_NAME_MAP[channelSysId] || channelSysId : void 0;
            const portalValue = record.get("portal")?.ifString()?.getValue();
            const includePortal = Boolean(portalValue && portalValue !== "undefined");
            return {
              success: true,
              value: record.transform(({ $ }) => ({
                name: $,
                description: $,
                active: $.toBoolean().map((v) => {
                  const value = v.getValue();
                  return value === true ? void 0 : value;
                }),
                targetTable: $.from("target_table"),
                triggerCondition: $.from("condition").def(""),
                triggerFlowDefinitionType: $.from("trigger_flow_definition_type"),
                schedule: $.val(schedule),
                runAs: $.from("run_as"),
                runAsUser: $.from("run_as_user"),
                notificationScript: $.from("notification_script"),
                objectiveTemplate: $.from("objective_template"),
                runAsScript: runAsScript ? $.val(runAsScript) : $.from("run_as_script"),
                channel: $.val(channelValue),
                showNotifications: $.from("show_notifications").toBoolean().map((v) => {
                  const value = v.getValue();
                  return value === false ? void 0 : value;
                }),
                domain: $.from("sys_domain").def("global"),
                enableDiscovery: $.from("enable_discovery").toBoolean().map((v) => {
                  const value = v.getValue();
                  return value === false ? void 0 : value;
                }),
                businessRule: $.from("business_rule"),
                profile: $.from("profile"),
                ...includePortal ? { portal: $.from("portal") } : {},
                sysOverrides: $.from("sys_overrides")
              }))
            };
          }
        },
        [constants_1.SN_AIA_TRIGGER_MAPPING]: {
          coalesce: ["trigger_configuration", "related_resource_record"]
        },
        [constants_1.SN_AIA_LTM_CATEGORY_MAPPING]: {
          coalesce: ["entity_id", "category"]
        }
      },
      shapes: [
        {
          shape: sdk_build_core_1.CallExpressionShape,
          fileTypes: ["fluent"],
          async toRecord(callExpression, { factory, diagnostics, config }) {
            if (callExpression.getCallee() !== "AiAgent") {
              return { success: false };
            }
            const arg = callExpression.getArgument(0).asObject();
            const tools = arg.get("tools").ifArray();
            const dataAccess = arg.get("dataAccess")?.ifObject();
            const runAsUser = arg.get("runAsUser");
            const securityAclField = arg.get("securityAcl")?.ifObject();
            if (!securityAclField) {
              diagnostics.error(arg, "AI Agent must have a securityAcl field.");
              return { success: false };
            }
            const agentName = arg.get("name").ifString()?.getValue() || "";
            const sysDomain = arg.get("sysDomain")?.ifString()?.getValue() || "global";
            const scopeName = config.scope;
            const computedInternalName = `${sysDomain}.${scopeName}.${agentName}`;
            const { instructions: publishedInstructions, publishedCount } = (0, ai_agent_shapes_helper_1.extractPublishedInstructions)(arg);
            if (publishedCount > 1) {
              const versionDetailsField = arg.get("versionDetails");
              diagnostics.error(versionDetailsField || arg, `AI Agent '${agentName}' has multiple versions with state 'published'. Only one version can be published at a time.`);
              return { success: false };
            }
            let securityAclRecords;
            if (securityAclField) {
              securityAclRecords = await (0, security_acl_processor_1.processSecurityAcl)(securityAclField, computedInternalName, factory, diagnostics);
            }
            const agentRecord = await (0, ai_agent_shapes_helper_1.createAgentRecord)(factory, callExpression, computedInternalName, constants_1.SN_AI_AGENT, publishedInstructions, securityAclRecords?.aclRecord.getId().getValue());
            const agentConfigRecord = await (0, ai_agent_shapes_helper_1.createAgentConfigRecord)(factory, callExpression, agentRecord.getId(), arg, constants_1.SN_AI_AGENT_CONFIG);
            const agentVersionRecords = await (0, ai_agent_shapes_helper_1.createAgentVersionRecords)(factory, callExpression, agentRecord.getId(), constants_1.SN_AI_AGENT_VERSION, constants_1.SN_AI_AGENT);
            let dataAccessRecords;
            if (dataAccess) {
              const runAsUserValue = runAsUser?.ifString()?.getValue();
              dataAccessRecords = await (0, ai_agent_shapes_helper_1.createDataAccessConfigRecord)(factory, callExpression, agentRecord.getId(), dataAccess, constants_1.SYS_AGENT_ACCESS_ROLE_CONFIG, constants_1.SN_AI_AGENT, diagnostics, !!runAsUserValue);
            }
            const triggerConfigs = arg.get("triggerConfig")?.ifArray()?.getElements() ?? [];
            const triggerConfigRecords = [];
            for (const triggerConfig of triggerConfigs) {
              if (triggerConfig?.isObject()) {
                const triggerName = triggerConfig.get("name")?.ifString()?.getValue() || "";
                const triggerDomain = triggerConfig.get("domain")?.ifString()?.getValue() || "global";
                const computedTriggerInternalName = `${triggerDomain}.${scopeName}.${triggerName}`;
                const triggerConfigRecord = await (0, ai_agent_shapes_helper_1.createTriggerConfigRecord)(factory, callExpression, triggerConfig, computedTriggerInternalName, constants_1.SN_AIA_TRIGGER_CONFIG);
                triggerConfigRecords.push({ record: triggerConfigRecord, config: triggerConfig });
              }
            }
            const triggerMappingRecords = [];
            for (const { record: triggerConfigRecord, config: triggerConfig } of triggerConfigRecords) {
              const triggerMappingRecord = await (0, ai_agent_shapes_helper_1.createTriggerMappingRecord)(factory, callExpression, agentRecord.getId(), triggerConfig, triggerConfigRecord.getId(), constants_1.SN_AIA_TRIGGER_MAPPING);
              triggerMappingRecords.push(triggerMappingRecord);
            }
            if (tools) {
              const toolNames = /* @__PURE__ */ new Set();
              const toolElements = tools.getElements();
              for (const tool of toolElements) {
                const toolObj = tool.asObject();
                const toolName = toolObj.get("name")?.ifString()?.getValue();
                if (toolName) {
                  if (toolNames.has(toolName)) {
                    diagnostics.error(toolObj.get("name"), `Duplicate tool name "${toolName}". Each tool must have a unique name.`);
                    return { success: false };
                  }
                  toolNames.add(toolName);
                }
                const toolType = toolObj.get("type")?.ifString()?.getValue();
                if (toolType === "rag" || toolType === "search_retrieval") {
                  const inputs = toolObj.get("inputs")?.ifObject();
                  if (!inputs) {
                    diagnostics.error(toolObj, `RAG tool '${toolName}' must have an 'inputs' field. The inputs configuration is mandatory for RAG tools.`);
                    return { success: false };
                  }
                  const searchTypeShape = inputs.get("searchType")?.ifObject();
                  const thresholdShape = searchTypeShape?.get("documentMatchThreshold");
                  if (thresholdShape) {
                    const thresholdValue = thresholdShape.ifNumber()?.getValue();
                    if (thresholdValue !== void 0 && (thresholdValue < 0 || thresholdValue > 1)) {
                      diagnostics.error(thresholdShape, `documentMatchThreshold must be between 0 and 1 (inclusive). Got: ${thresholdValue}`);
                      return { success: false };
                    }
                  }
                }
              }
            }
            const toolRecords = tools ? await Promise.all(tools.map(async (tool) => {
              const toolObj = tool.asObject();
              const toolType = toolObj.get("type")?.ifString()?.getValue();
              const internalToolType = toolType === "search_retrieval" ? "rag" : toolType;
              const oobToolSysId = internalToolType ? constants_1.OOB_TOOL_TYPE_TO_SYS_ID_MAP[internalToolType] : void 0;
              if (oobToolSysId) {
                const agentToolM2MRecord2 = await (0, ai_agent_shapes_helper_1.createAgentToolM2MRecord)(factory, callExpression, agentRecord.getId(), toolObj, oobToolSysId, constants_1.SN_AI_AGENT_TOOL_M2M);
                return agentToolM2MRecord2;
              }
              const agentToolRecord = await (0, ai_agent_shapes_helper_1.createAgentToolRecord)(factory, callExpression, toolObj, constants_1.SN_AI_AGENT_TOOL);
              const agentToolM2MRecord = await (0, ai_agent_shapes_helper_1.createAgentToolM2MRecord)(factory, callExpression, agentRecord.getId(), toolObj, agentToolRecord.getId().getValue(), constants_1.SN_AI_AGENT_TOOL_M2M);
              return agentToolRecord.with(agentToolM2MRecord);
            })) : [];
            const memoryCategoriesArray = arg.get("memoryCategories")?.ifArray()?.getElements() ?? [];
            const validCategoryNames = memoryCategoriesArray.map((categoryShape) => categoryShape.ifString()?.getValue()).filter((name) => !!name && name in constants_1.MEMORY_CATEGORY_TO_SYS_ID_MAP);
            const categoryMappingRecords = await Promise.all(validCategoryNames.map((categoryName) => (0, ai_agent_shapes_helper_1.createCategoryMappingRecord)(factory, callExpression, agentRecord.getId(), constants_1.MEMORY_CATEGORY_TO_SYS_ID_MAP[categoryName] ?? "")));
            const childRecords = [
              agentConfigRecord,
              ...agentVersionRecords,
              ...toolRecords,
              ...triggerMappingRecords,
              ...categoryMappingRecords
            ];
            if (dataAccessRecords) {
              childRecords.push(...dataAccessRecords);
            }
            if (securityAclRecords) {
              childRecords.push(...securityAclRecords.allRecords);
            }
            childRecords.push(...triggerConfigRecords.map((tc) => tc.record));
            return {
              success: true,
              value: agentRecord.with(...childRecords)
            };
          }
        }
      ]
    });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/index.js
var require_ai_agent = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agent/index.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.AiAgentPlugin = void 0;
    var ai_agent_plugin_1 = require_ai_agent_plugin();
    Object.defineProperty(exports2, "AiAgentPlugin", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ai_agent_plugin_1.AiAgentPlugin;
    }, "get") });
    exports2.default = ai_agent_plugin_1.AiAgentPlugin;
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agentic-workflow/constants.js
var require_constants2 = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agentic-workflow/constants.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.OOB_CHANNEL_MAP = exports2.ACL_DEFAULTS = exports2.PUBLIC_ACCESS_ROLE_SYS_ID = exports2.USER_IS_AUTHENTICATED = exports2.GEN_AI_AGENTIC_USECASE_TYPE = exports2.SYS_SECURITY_ACL_ROLE = exports2.SYS_SECURITY_ACL = exports2.SYS_USER_ROLE = exports2.SYS_AGENT_ACCESS_ROLE_MAPPING = exports2.SYS_AGENT_ACCESS_ROLE_CONFIG = exports2.SN_AIA_TRIGGER_MAPPING = exports2.SN_AIA_TRIGGER_CONFIG = exports2.SN_AIA_USECASE_CONFIG = exports2.SN_AIA_VERSION = exports2.SN_AIA_TEAM = exports2.SN_AIA_TEAM_MEMBER = exports2.SN_AIA_USECASE = void 0;
    exports2.SN_AIA_USECASE = "sn_aia_usecase";
    exports2.SN_AIA_TEAM_MEMBER = "sn_aia_team_member";
    exports2.SN_AIA_TEAM = "sn_aia_team";
    exports2.SN_AIA_VERSION = "sn_aia_version";
    exports2.SN_AIA_USECASE_CONFIG = "sn_aia_usecase_config_override";
    exports2.SN_AIA_TRIGGER_CONFIG = "sn_aia_trigger_configuration";
    exports2.SN_AIA_TRIGGER_MAPPING = "sn_aia_trigger_agent_usecase_m2m";
    exports2.SYS_AGENT_ACCESS_ROLE_CONFIG = "sys_agent_access_role_configuration";
    exports2.SYS_AGENT_ACCESS_ROLE_MAPPING = "sys_agent_access_role_mapping";
    exports2.SYS_USER_ROLE = "sys_user_role";
    exports2.SYS_SECURITY_ACL = "sys_security_acl";
    exports2.SYS_SECURITY_ACL_ROLE = "sys_security_acl_role";
    exports2.GEN_AI_AGENTIC_USECASE_TYPE = "d9eb25b39fc222109570e67e8a0a1c6a";
    exports2.USER_IS_AUTHENTICATED = "239bdf0c47d23110ba46ff58936d4330";
    exports2.PUBLIC_ACCESS_ROLE_SYS_ID = "b0593b350a0a0aa7001d689e4542dc28";
    exports2.ACL_DEFAULTS = {
      operation: "execute",
      active: true,
      adminOverrides: true,
      advanced: false,
      decisionType: "allow",
      localOrExisting: "Existing"
    };
    exports2.OOB_CHANNEL_MAP = {
      "Now Assist Panel": "b023fcb3537321106167ddeeff7b12a8"
    };
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agentic-workflow/ai-agentic-workflow-transform-helper.js
var require_ai_agentic_workflow_transform_helper = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agentic-workflow/ai-agentic-workflow-transform-helper.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.computeSecurityAclValue = exports2.mapTriggerConfigs = exports2.computeDataAccessValue = exports2.computeVersionDetailsValue = exports2.computeTeamValue = exports2.reverseMapChannel = void 0;
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var constants_1 = require_constants2();
    var CHANNEL_SYS_ID_TO_NAME_MAP = Object.fromEntries(Object.entries(constants_1.OOB_CHANNEL_MAP).map(([name, sysId]) => [sysId, name]));
    var reverseMapChannel = /* @__PURE__ */ __name((sysId) => {
      if (!sysId) {
        return void 0;
      }
      return CHANNEL_SYS_ID_TO_NAME_MAP[sysId] || sysId;
    }, "reverseMapChannel");
    exports2.reverseMapChannel = reverseMapChannel;
    var computeTeamValue = /* @__PURE__ */ __name((teamRecord, teamMembers) => {
      if (!teamRecord) {
        return void 0;
      }
      const sysDomainVal = teamRecord.get("sys_domain")?.ifString()?.getValue();
      return {
        $id: sdk_build_plugins_1.NowIdShape.from(teamRecord) ?? teamRecord.getId().getValue(),
        name: teamRecord.get("name")?.ifString()?.getValue(),
        sysDomain: sysDomainVal !== "global" ? sysDomainVal : void 0,
        members: teamMembers.length > 0 ? teamMembers.map((tm) => tm.get("agent")?.ifString()?.getValue()) : void 0
      };
    }, "computeTeamValue");
    exports2.computeTeamValue = computeTeamValue;
    var computeVersionDetailsValue = /* @__PURE__ */ __name((versions) => {
      if (!versions || versions.length === 0) {
        return void 0;
      }
      return versions.map((v) => {
        const versionNumberStr = v.get("version_number")?.ifString()?.getValue();
        const versionNumber = versionNumberStr && versionNumberStr !== "" && !Number.isNaN(Number(versionNumberStr)) ? Number(versionNumberStr) : 1;
        return v.transform(({ $ }) => ({
          name: $.from("version_name"),
          number: $.val(versionNumber),
          state: $.from("state").def("draft"),
          instructions: $.from("instructions"),
          condition: $.from("condition").def("")
        }));
      });
    }, "computeVersionDetailsValue");
    exports2.computeVersionDetailsValue = computeVersionDetailsValue;
    var data_access_transform_helper_1 = require_data_access_transform_helper();
    Object.defineProperty(exports2, "computeDataAccessValue", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return data_access_transform_helper_1.computeDataAccessValue;
    }, "get") });
    var mapTriggerConfigs = /* @__PURE__ */ __name((triggerConfigs, triggerMappings) => {
      if (triggerConfigs.length === 0) {
        return [];
      }
      return triggerConfigs.map((tc) => {
        const triggerConfigId = tc.getId().getValue();
        const triggerMapping = triggerMappings.find((tm) => {
          const tmTriggerConfig = tm.get("trigger_configuration");
          if (tmTriggerConfig?.isRecord()) {
            return tmTriggerConfig.getId().getValue() === triggerConfigId;
          }
          return tmTriggerConfig?.ifString()?.getValue() === triggerConfigId;
        });
        const channelValue = (0, exports2.reverseMapChannel)(tc.get("channel")?.ifString()?.getValue());
        const sysOverridesValue = triggerMapping?.get("sys_overrides")?.ifString()?.ifNotEmpty()?.getValue();
        const runDayOfMonthValue = tc.get("run_dayofmonth")?.ifString()?.ifNotEmpty()?.toNumber()?.getValue();
        const runDayOfWeekValue = tc.get("run_dayofweek")?.ifString()?.ifNotEmpty()?.toNumber()?.getValue();
        const repeatIntervalValue = tc.get("run_period")?.ifString()?.ifNotEmpty()?.getValue();
        const startingValue = tc.get("run_start")?.ifString()?.ifNotEmpty()?.getValue();
        const timeValue = tc.get("run_time")?.ifString()?.ifNotEmpty()?.getValue();
        const rawTriggerStrategy = tc.get("trigger_strategy")?.ifString()?.ifNotEmpty()?.getValue();
        const triggerStrategyValue = rawTriggerStrategy === "undefined" ? void 0 : rawTriggerStrategy;
        const schedule = runDayOfMonthValue || runDayOfWeekValue || repeatIntervalValue || startingValue || timeValue || triggerStrategyValue ? {
          ...runDayOfMonthValue && { runDayOfMonth: runDayOfMonthValue },
          ...runDayOfWeekValue && { runDayOfWeek: runDayOfWeekValue },
          ...repeatIntervalValue && { repeatInterval: repeatIntervalValue },
          ...startingValue && { starting: startingValue },
          ...timeValue && { time: timeValue },
          ...triggerStrategyValue && { triggerStrategy: triggerStrategyValue }
        } : void 0;
        const portalValue = tc.get("portal")?.ifString()?.getValue();
        const profileValue = tc.get("profile")?.ifString()?.getValue();
        return tc.transform(({ $ }) => ({
          name: $,
          description: $.def(""),
          active: $.toBoolean().def(false),
          channel: $.val(channelValue),
          triggerCondition: $.from("condition").def(""),
          enableDiscovery: $.from("enable_discovery").toBoolean().def(false),
          notificationScript: $.from("notification_script").def(""),
          objectiveTemplate: $.from("objective_template").def(""),
          ...portalValue && portalValue !== "undefined" ? { portal: $.from("portal") } : {},
          ...profileValue && profileValue !== "undefined" ? { profile: $.from("profile") } : {},
          runAs: $.from("run_as").def(""),
          runAsScript: $.from("run_as_script").def(""),
          runAsUser: $.from("run_as_user").def(""),
          businessRule: $.from("business_rule").def(""),
          showNotifications: $.from("show_notifications").toBoolean().def(false),
          domain: $.from("sys_domain").def("global"),
          targetTable: $.from("target_table"),
          triggerFlowDefinitionType: $.from("trigger_flow_definition_type"),
          schedule: $.val(schedule),
          sysOverrides: $.val(sysOverridesValue)
        }));
      });
    }, "mapTriggerConfigs");
    exports2.mapTriggerConfigs = mapTriggerConfigs;
    var computeSecurityAclValue = /* @__PURE__ */ __name((aclRecord, aclRoleRecords) => {
      if (!aclRecord) {
        return void 0;
      }
      const securityAttribute = aclRecord.get("security_attribute")?.ifString()?.getValue();
      const isAuthenticated = securityAttribute === constants_1.USER_IS_AUTHENTICATED;
      const allRoles = aclRoleRecords.map((r) => {
        const roleRef = r.get("sys_user_role");
        if (roleRef?.isRecord()) {
          return roleRef.getId().getValue();
        }
        return roleRef?.ifString()?.getValue();
      }).filter((v) => !!v);
      const isPublic = allRoles.length > 0 && allRoles.every((r) => r === constants_1.PUBLIC_ACCESS_ROLE_SYS_ID);
      const roles = allRoles.filter((r) => r !== constants_1.PUBLIC_ACCESS_ROLE_SYS_ID);
      const $id = sdk_build_plugins_1.NowIdShape.from(aclRecord);
      if (isAuthenticated) {
        return {
          $id,
          type: "Any authenticated user"
        };
      }
      if (isPublic || roles.length === 0) {
        return {
          $id,
          type: "Public"
        };
      }
      return {
        $id,
        type: "Specific role",
        roles
      };
    }, "computeSecurityAclValue");
    exports2.computeSecurityAclValue = computeSecurityAclValue;
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agentic-workflow/ai-agentic-workflow-shapes-helper.js
var require_ai_agentic_workflow_shapes_helper = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agentic-workflow/ai-agentic-workflow-shapes-helper.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.resolveOOBValue = resolveOOBValue;
    exports2.resolveStringOrRecord = resolveStringOrRecord;
    exports2.resolveRoleToSysId = resolveRoleToSysId;
    exports2.extractPublishedInstructions = extractPublishedInstructions;
    exports2.validateTriggerConfig = validateTriggerConfig;
    exports2.createWorkflowRecord = createWorkflowRecord;
    exports2.createTeamRecord = createTeamRecord;
    exports2.createTeamMemberRecords = createTeamMemberRecords;
    exports2.createVersionRecords = createVersionRecords;
    exports2.createTriggerConfigRecord = createTriggerConfigRecord;
    exports2.createTriggerMappingRecord = createTriggerMappingRecord;
    exports2.createUseCaseConfigRecord = createUseCaseConfigRecord;
    exports2.createDataAccessConfigRecord = createDataAccessConfigRecord;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var constants_1 = require_constants2();
    function resolveOOBValue(value, oobMap, fieldName, diagnostics) {
      if (!value) {
        return void 0;
      }
      if (value.isRecord()) {
        if (diagnostics) {
          const supportedValues = Object.keys(oobMap).map((k) => `'${k}'`).join(", ");
          diagnostics.error(value, `${fieldName} field does not support Record references. Supported OOB values are: ${supportedValues}.`);
        }
        return void 0;
      }
      const rawValue = value.ifString()?.getValue();
      if (!rawValue) {
        return void 0;
      }
      if (oobMap[rawValue]) {
        return oobMap[rawValue];
      }
      if (/^[0-9a-f]{32}$/i.test(rawValue)) {
        return rawValue;
      }
      if (diagnostics) {
        const supportedValues = Object.keys(oobMap).map((k) => `'${k}'`).join(", ");
        diagnostics.error(value, `${fieldName} field contains unsupported value '${rawValue}'. Supported OOB values are: ${supportedValues}.`);
      }
      return void 0;
    }
    __name(resolveOOBValue, "resolveOOBValue");
    function resolveStringOrRecord(value, options) {
      if (!value) {
        return void 0;
      }
      if (value.isRecord()) {
        return value.getId().getValue();
      }
      const rawValue = value.ifString()?.getValue();
      if (!rawValue) {
        return void 0;
      }
      if (options?.nameMap?.[rawValue]) {
        return options.nameMap[rawValue];
      }
      if ((0, sdk_build_core_1.isGUID)(rawValue)) {
        return rawValue;
      }
      if (options?.requireGuid && options.diagnostics) {
        const fieldName = options.fieldName || "field";
        const tableName = options.tableName || "table";
        options.diagnostics.error(value, `${fieldName} field contains non-GUID value '${rawValue}'. If this is a name, please use Record('${tableName}', '${rawValue}') instead, or provide the sys_id directly.`);
      }
      return rawValue;
    }
    __name(resolveStringOrRecord, "resolveStringOrRecord");
    function resolveRoleToSysId(roleValue, diagnostics) {
      return resolveStringOrRecord(roleValue, {
        requireGuid: true,
        fieldName: "roleList",
        tableName: constants_1.SYS_USER_ROLE,
        diagnostics
      });
    }
    __name(resolveRoleToSysId, "resolveRoleToSysId");
    function extractPublishedInstructions(aiVersions, workflowName) {
      let publishedInstructions = "";
      let publishedCount = 0;
      if (aiVersions?.isArray()) {
        const versionElements = aiVersions.getElements();
        for (const versionElement of versionElements) {
          if (versionElement?.isObject()) {
            const state = versionElement.get("state")?.ifString()?.getValue();
            if (state === "published") {
              publishedCount++;
              if (publishedCount === 1) {
                publishedInstructions = versionElement.get("instructions")?.ifString()?.getValue() || "";
              } else if (publishedCount > 1) {
                return {
                  instructions: "",
                  error: `AI Agentic Workflow '${workflowName}' has multiple versions with state 'published'. Only one version can be published at a time.`,
                  errorSource: versionElement.get("state") || versionElement
                };
              }
            }
          }
        }
      }
      return { instructions: publishedInstructions };
    }
    __name(extractPublishedInstructions, "extractPublishedInstructions");
    function validateTriggerConfig(triggerConfig) {
      const channel = triggerConfig.get("channel");
      if (!channel || !channel.ifString()?.getValue()) {
        return { error: `'channel' is mandatory for all triggers`, errorSource: channel || triggerConfig };
      }
      const triggerFlowDefinitionType = triggerConfig.get("triggerFlowDefinitionType")?.ifString()?.getValue();
      const schedule = triggerConfig.get("schedule")?.ifObject();
      if (triggerFlowDefinitionType === "record_create" || triggerFlowDefinitionType === "record_create_or_update" || triggerFlowDefinitionType === "record_update") {
        const triggerCondition = triggerConfig.get("triggerCondition");
        if (!triggerCondition || !triggerCondition.ifString()?.getValue()) {
          return {
            error: `'triggerCondition' is required for trigger type '${triggerFlowDefinitionType}'`,
            errorSource: triggerCondition || triggerConfig
          };
        }
      } else if (triggerFlowDefinitionType === "daily") {
        const time = schedule?.get("time");
        if (!time || !time.ifString()?.getValue()) {
          return {
            error: `'schedule.time' is required for trigger type 'daily'`,
            errorSource: time || schedule || triggerConfig
          };
        }
      } else if (triggerFlowDefinitionType === "weekly") {
        const time = schedule?.get("time");
        const dayOfWeek = schedule?.get("runDayOfWeek");
        if (!time || !time.ifString()?.getValue()) {
          return {
            error: `'schedule.time' is required for trigger type 'weekly'`,
            errorSource: time || schedule || triggerConfig
          };
        }
        if (!dayOfWeek || dayOfWeek.getValue() === void 0) {
          return {
            error: `'schedule.runDayOfWeek' is required for trigger type 'weekly'`,
            errorSource: dayOfWeek || schedule || triggerConfig
          };
        }
      } else if (triggerFlowDefinitionType === "monthly") {
        const time = schedule?.get("time");
        const day = schedule?.get("runDayOfMonth");
        if (!time || !time.ifString()?.getValue()) {
          return {
            error: `'schedule.time' is required for trigger type 'monthly'`,
            errorSource: time || schedule || triggerConfig
          };
        }
        if (!day || day.getValue() === void 0) {
          return {
            error: `'schedule.runDayOfMonth' is required for trigger type 'monthly'`,
            errorSource: day || schedule || triggerConfig
          };
        }
      } else if (triggerFlowDefinitionType === "email") {
        const targetTable = triggerConfig.get("targetTable");
        if (!targetTable || !targetTable.ifString()?.getValue()) {
          return {
            error: `'targetTable' is required for trigger type 'email'`,
            errorSource: targetTable || triggerConfig
          };
        }
      }
      const starting = schedule?.get("starting");
      if (starting) {
        const startingValue = starting.ifString()?.getValue();
        if (startingValue) {
          const dateTimeRegex = /^\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}$/;
          if (!dateTimeRegex.test(startingValue)) {
            return {
              error: `'schedule.starting' field must be in format YYYY-MM-DD HH:MM:SS (e.g., '2024-01-15 09:00:00'). Got: '${startingValue}'`,
              errorSource: starting
            };
          }
        }
      }
      return void 0;
    }
    __name(validateTriggerConfig, "validateTriggerConfig");
    async function createWorkflowRecord(factory, callExpression, computedInternalName, publishedInstructions, teamRecordId, tableName, aclId) {
      const arg = callExpression.getArgument(0).asObject();
      return await factory.createRecord({
        source: callExpression,
        table: tableName,
        explicitId: arg.get("$id"),
        properties: arg.transform(({ $ }) => ({
          name: $,
          description: $,
          internal_name: $.val(computedInternalName),
          context_processing_script: $.from("contextProcessingScript").map((v) => v.if(sdk_build_plugins_1.ModuleFunctionShape)?.toString((n) => `${n}({{PARAMS}})`, ["task", "user_utterance", "workflow_id", "context"]) ?? v).toCdata().def(""),
          sys_domain: $.from("sysDomain").def("global"),
          strategy: $.val("b264c470ff1022106c36ffffffffff67"),
          execution_mode: $.from("executionMode").map((v) => v || "copilot").def("copilot"),
          base_plan: $.val(publishedInstructions),
          record_type: $.from("recordType").def("template"),
          team: $.val(teamRecordId),
          active: $.def(true),
          acl: $.val(aclId ?? "")
        }))
      });
    }
    __name(createWorkflowRecord, "createWorkflowRecord");
    async function createTeamRecord(factory, callExpression, teamObj, tableName, workflowDescription) {
      const teamExplicitId = teamObj.get("$id");
      return await factory.createRecord({
        source: callExpression,
        table: tableName,
        explicitId: teamExplicitId,
        properties: teamObj.transform(({ $ }) => ({
          name: $,
          description: $.val(workflowDescription).def(""),
          sys_domain: $.from("sysDomain").def("global"),
          active: $.def(true)
        }))
      });
    }
    __name(createTeamRecord, "createTeamRecord");
    async function createTeamMemberRecords(factory, callExpression, teamMembers, teamRecordId, workflowRecordId, workflowMemoryScope, workflowDomain, tableName, diagnostics) {
      const teamMemberRecords = [];
      if (!teamMembers?.isArray()) {
        return teamMemberRecords;
      }
      const teamMemberElements = teamMembers.getElements();
      for (const member of teamMemberElements) {
        const agentSysId = resolveStringOrRecord(member, {
          requireGuid: true,
          fieldName: "team.members",
          tableName: "sn_aia_agent",
          diagnostics
        });
        if (agentSysId) {
          const teamMemberRecord = await factory.createRecord({
            source: callExpression,
            table: tableName,
            properties: {
              agent: agentSysId,
              usecase: workflowRecordId,
              memory_scope: workflowMemoryScope,
              team: teamRecordId,
              sys_domain: workflowDomain
            }
          });
          teamMemberRecords.push(teamMemberRecord);
        }
      }
      return teamMemberRecords;
    }
    __name(createTeamMemberRecords, "createTeamMemberRecords");
    async function createVersionRecords(factory, callExpression, workflowRecordId, workflowDomain, aiVersions, tableName) {
      const versionRecords = [];
      if (aiVersions?.isArray()) {
        const aiVersionElements = aiVersions.getElements();
        for (const versionElement of aiVersionElements) {
          if (versionElement?.isObject()) {
            const record = await factory.createRecord({
              source: callExpression,
              table: tableName,
              properties: versionElement.transform(({ $ }) => ({
                target_id: $.val(workflowRecordId),
                sys_domain: $.val(workflowDomain),
                version_name: $.from("name"),
                target_table: $.val(constants_1.SN_AIA_USECASE),
                version_number: $.from("number").def(1),
                instructions: $.from("instructions").def(""),
                state: $.from("state").def("draft"),
                condition: $.from("condition").def("")
              }))
            });
            versionRecords.push(record);
          }
        }
      }
      return versionRecords;
    }
    __name(createVersionRecords, "createVersionRecords");
    async function createTriggerConfigRecord(factory, callExpression, triggerConfig, computedTriggerInternalName, workflowRecordId, tableName, diagnostics) {
      const schedule = triggerConfig.get("schedule")?.ifObject();
      return await factory.createRecord({
        source: callExpression,
        table: tableName,
        properties: triggerConfig.transform(({ $ }) => ({
          active: $.def(false),
          channel: $.from("channel").map((v) => resolveOOBValue(v, constants_1.OOB_CHANNEL_MAP, "channel", diagnostics)).def(""),
          condition: $.from("triggerCondition").map((v) => v.if(sdk_build_plugins_1.ModuleFunctionShape)?.toString((n) => `${n}({{PARAMS}})`, ["current"]) ?? v).toCdata().def(""),
          description: $.def(""),
          enable_discovery: $.from("enableDiscovery").def(false),
          name: $,
          internal_name: $.val(computedTriggerInternalName),
          notification_script: $.from("notificationScript").def(""),
          objective_template: $.from("objectiveTemplate").map((v) => v.if(sdk_build_plugins_1.ModuleFunctionShape)?.toString((n) => `${n}({{PARAMS}})`, []) ?? v).toCdata().def(""),
          portal: $.from("portal").map((v) => resolveStringOrRecord(v, {
            requireGuid: true,
            fieldName: "portal",
            tableName: "now_assist_deployment_channel",
            diagnostics
          })).def(""),
          profile: $.from("profile").map((v) => resolveStringOrRecord(v, {
            requireGuid: true,
            fieldName: "profile",
            tableName: "now_assist_deployment",
            diagnostics
          })).def(""),
          run_as: $.from("runAs").def(""),
          run_as_script: $.from("runAsScript").map((v) => v.if(sdk_build_plugins_1.ModuleFunctionShape)?.toString((n) => `${n}({{PARAMS}})`, []) ?? v).toCdata().def(""),
          run_as_user: $.from("runAsUser").map((v) => resolveStringOrRecord(v)).def(""),
          business_rule: $.from("businessRule").map((v) => resolveStringOrRecord(v)).def(""),
          run_dayofmonth: $.val(schedule?.get("runDayOfMonth")?.getValue()).def(""),
          run_dayofweek: $.val(schedule?.get("runDayOfWeek")?.getValue()).def(""),
          run_period: $.val(schedule?.get("repeatInterval")?.getValue()).def(""),
          run_start: $.val(schedule?.get("starting")?.getValue()).def(""),
          run_time: $.val(schedule?.get("time")?.getValue()).def(""),
          show_notifications: $.from("showNotifications").def(false),
          sys_domain: $.from("domain").def("global"),
          target_table: $.from("targetTable").def(""),
          trigger_flow_definition_type: $.from("triggerFlowDefinitionType").def(""),
          trigger_strategy: $.val(schedule?.get("triggerStrategy")?.getValue()).def(""),
          usecase: $.val(workflowRecordId),
          // usecase_discovery mirrors enable_discovery — both controlled by the single enableDiscovery API field
          usecase_discovery: $.from("enableDiscovery").def(false)
        }))
      });
    }
    __name(createTriggerConfigRecord, "createTriggerConfigRecord");
    async function createTriggerMappingRecord(factory, callExpression, workflowRecordId, triggerConfigShape, linkedTriggerConfigId, tableName) {
      const triggerDomain = triggerConfigShape.get("domain")?.ifString()?.getValue() || "global";
      return await factory.createRecord({
        source: callExpression,
        table: tableName,
        properties: triggerConfigShape.transform(({ $ }) => ({
          active: $.val(false),
          objective_template: $.from("objectiveTemplate").map((v) => v.if(sdk_build_plugins_1.ModuleFunctionShape)?.toString((n) => `${n}({{PARAMS}})`, []) ?? v).toCdata().def(""),
          related_resource_record: $.val(workflowRecordId),
          related_resource_table: $.val(constants_1.SN_AIA_USECASE),
          sys_domain: $.val(triggerDomain),
          sys_overrides: $.from("sysOverrides").map((v) => resolveStringOrRecord(v)).def(""),
          trigger_configuration: $.val(linkedTriggerConfigId)
        }))
      });
    }
    __name(createTriggerMappingRecord, "createTriggerMappingRecord");
    async function createUseCaseConfigRecord(factory, callExpression, workflowRecordId, tableName) {
      const arg = callExpression.getArgument(0).asObject();
      return await factory.createRecord({
        source: callExpression,
        table: tableName,
        properties: arg.transform(({ $ }) => ({
          active: $.from("active").def(true),
          run_as_user: $.from("runAs").def(""),
          usecase_configuration: $.val(workflowRecordId),
          sys_domain: $.from("sysDomain").def("global")
        }))
      });
    }
    __name(createUseCaseConfigRecord, "createUseCaseConfigRecord");
    async function createDataAccessConfigRecord(factory, callExpression, workflowRecordId, dataAccess, tableName, diagnostics, hasRunAsUser = false) {
      const roleListArray = dataAccess.get("roleList")?.ifArray();
      const restrictionGUIDs = [];
      if (roleListArray) {
        const roleElements = roleListArray.getElements();
        for (const roleValue of roleElements) {
          const resolvedSysId = resolveRoleToSysId(roleValue, diagnostics);
          if (resolvedSysId) {
            restrictionGUIDs.push(resolvedSysId);
          }
        }
      }
      const roleListValue = restrictionGUIDs.join(",");
      const roleMapField = dataAccess.get("roleMap");
      const roleMapArray = roleMapField?.ifArray();
      const validatedRoleMapItems = [];
      if (roleMapArray) {
        for (const role of roleMapArray.getElements()) {
          if (role.ifString()) {
            const roleValue = role.asString().getValue();
            if ((0, sdk_build_core_1.isGUID)(roleValue)) {
              diagnostics.error(roleMapField, `roleMap must contain role names, not sys_ids. Got: '${roleValue}'. Use the role name (e.g., 'admin'), or move the sys_id to dataAccess.roleList.`);
            } else if (roleValue.trim() !== "") {
              validatedRoleMapItems.push(role);
            }
          } else {
            validatedRoleMapItems.push(role);
          }
        }
      }
      if (!hasRunAsUser && restrictionGUIDs.length === 0 && validatedRoleMapItems.length === 0) {
        diagnostics.error(dataAccess, "dataAccess must have at least one of roleList or roleMap non-empty: provide sys_ids in roleList, role names in roleMap, or both.");
      }
      const roleConfigRecord = await factory.createRecord({
        source: callExpression,
        table: tableName,
        properties: dataAccess.transform(({ $ }) => ({
          agent: $.val(workflowRecordId.getValue()),
          agent_table: $.val(constants_1.SN_AIA_USECASE),
          action: $.val("limit_to_roles"),
          role_list: $.val(roleListValue),
          description: $.from("description").def(""),
          name: $.val(workflowRecordId.getValue())
        }))
      });
      const records = [roleConfigRecord];
      for (const role of validatedRoleMapItems) {
        const roleReference = role.ifString() ? await factory.createReference({
          source: role,
          table: constants_1.SYS_USER_ROLE,
          keys: { name: role.asString().getValue() }
        }) : role;
        const mappingRecord = await factory.createRecord({
          source: role,
          table: constants_1.SYS_AGENT_ACCESS_ROLE_MAPPING,
          properties: {
            agent_access_config: roleConfigRecord,
            role: roleReference
          }
        });
        records.push(mappingRecord);
      }
      return records;
    }
    __name(createDataAccessConfigRecord, "createDataAccessConfigRecord");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agentic-workflow/security-acl-processor.js
var require_security_acl_processor2 = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agentic-workflow/security-acl-processor.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processSecurityAcl = processSecurityAcl;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var constants_1 = require_constants2();
    var SYS_SECURITY_ACL = "sys_security_acl";
    async function processSecurityAcl(securityAcl, aclName, factory, diagnostics) {
      const records = [];
      const accessType = securityAcl.get("type").asString().getValue();
      if (accessType !== "Any authenticated user" && accessType !== "Specific role" && accessType !== "Public") {
        diagnostics.error(securityAcl, `securityAcl.type must be 'Any authenticated user', 'Specific role', or 'Public'. Got: '${accessType}'`);
      }
      const securityAttribute = accessType === "Any authenticated user" ? constants_1.USER_IS_AUTHENTICATED : "";
      let aclRoles = [];
      const rolesField = securityAcl.has("roles") ? securityAcl.get("roles") : void 0;
      if (accessType === "Specific role") {
        if (!rolesField || !rolesField.isArray()) {
          diagnostics.error(securityAcl, "securityAcl.roles must be an array when type is 'Specific role'");
        } else {
          aclRoles = rolesField.asArray().getElements();
          if (aclRoles.length === 0) {
            diagnostics.error(rolesField, "securityAcl.roles must contain at least one role");
          }
        }
      } else if (accessType === "Any authenticated user" && rolesField && rolesField.isArray()) {
        aclRoles = rolesField.asArray().getElements();
      }
      const aclExplicitId = securityAcl.has("$id") ? securityAcl.get("$id") : void 0;
      const aclRecord = await factory.createRecord({
        source: securityAcl,
        table: SYS_SECURITY_ACL,
        ...aclExplicitId instanceof sdk_build_plugins_1.NowIdShape ? { explicitId: aclExplicitId } : {},
        properties: securityAcl.transform(({ $ }) => ({
          name: $.val(aclName),
          type: $.val(constants_1.GEN_AI_AGENTIC_USECASE_TYPE),
          operation: $.val(constants_1.ACL_DEFAULTS.operation),
          active: $.val(constants_1.ACL_DEFAULTS.active),
          admin_overrides: $.val(constants_1.ACL_DEFAULTS.adminOverrides),
          advanced: $.val(constants_1.ACL_DEFAULTS.advanced),
          decision_type: $.val(constants_1.ACL_DEFAULTS.decisionType),
          local_or_existing: $.val(constants_1.ACL_DEFAULTS.localOrExisting),
          security_attribute: $.val(securityAttribute)
        }))
      });
      records.push(aclRecord);
      if (accessType === "Public") {
        const publicRoleReference = await factory.createReference({
          source: securityAcl,
          table: constants_1.SYS_USER_ROLE,
          guid: constants_1.PUBLIC_ACCESS_ROLE_SYS_ID
        });
        const publicRoleRecord = await factory.createRecord({
          source: securityAcl,
          table: constants_1.SYS_SECURITY_ACL_ROLE,
          properties: {
            sys_security_acl: aclRecord,
            sys_user_role: publicRoleReference
          }
        });
        records.push(publicRoleRecord);
      }
      for (const role of aclRoles) {
        const roleReference = role.isString() ? await factory.createReference({
          source: role,
          table: constants_1.SYS_USER_ROLE,
          keys: (0, sdk_build_core_1.isGUID)(role.asString().getValue()) ? void 0 : { name: role.asString().getValue() },
          guid: (0, sdk_build_core_1.isGUID)(role.asString().getValue()) ? role.asString().getValue() : void 0
        }) : role;
        const aclRoleRecord = await factory.createRecord({
          source: role,
          table: constants_1.SYS_SECURITY_ACL_ROLE,
          properties: {
            sys_security_acl: aclRecord,
            sys_user_role: roleReference
          }
        });
        records.push(aclRoleRecord);
      }
      return { aclRecord, allRecords: records };
    }
    __name(processSecurityAcl, "processSecurityAcl");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agentic-workflow/ai-agentic-workflow-plugin.js
var require_ai_agentic_workflow_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agentic-workflow/ai-agentic-workflow-plugin.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.AiAgenticWorkflowPlugin = void 0;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var ai_agentic_workflow_transform_helper_1 = require_ai_agentic_workflow_transform_helper();
    var ai_agentic_workflow_shapes_helper_1 = require_ai_agentic_workflow_shapes_helper();
    var constants_1 = require_constants2();
    var security_acl_processor_1 = require_security_acl_processor2();
    exports2.AiAgenticWorkflowPlugin = sdk_build_core_1.Plugin.create({
      name: "AiAgenticWorkflowPlugin",
      docs: [
        {
          apiName: "AiAgenticWorkflow",
          tags: ["sn_aia_agentic_workflow"]
        }
      ],
      records: {
        [constants_1.SN_AIA_USECASE]: {
          relationships: {
            [constants_1.SN_AIA_TEAM]: {
              via: "team",
              inverse: true,
              descendant: true,
              relationships: {
                [constants_1.SN_AIA_TEAM_MEMBER]: {
                  via: "team",
                  descendant: true
                }
              }
            },
            [constants_1.SN_AIA_USECASE_CONFIG]: {
              via: "usecase_configuration",
              descendant: true
            },
            [constants_1.SN_AIA_TRIGGER_CONFIG]: {
              via: "usecase",
              descendant: true,
              relationships: {
                [constants_1.SN_AIA_TRIGGER_MAPPING]: {
                  via: "trigger_configuration",
                  descendant: true
                }
              }
            },
            [constants_1.SN_AIA_VERSION]: {
              via: "target_id",
              descendant: true
            },
            [constants_1.SYS_AGENT_ACCESS_ROLE_CONFIG]: {
              via: "agent",
              descendant: true,
              relationships: {
                [constants_1.SYS_AGENT_ACCESS_ROLE_MAPPING]: {
                  via: "agent_access_config",
                  descendant: true,
                  relationships: {
                    [constants_1.SYS_USER_ROLE]: {
                      via: "role",
                      inverse: true
                    }
                  }
                }
              }
            },
            [constants_1.SYS_SECURITY_ACL]: {
              via: [{ parent: ["internal_name", "\\.(.*)"], child: ["name", "\\.(.*)"] }],
              descendant: true,
              relationships: {
                [constants_1.SYS_SECURITY_ACL_ROLE]: {
                  via: "sys_security_acl",
                  descendant: true
                }
              }
            }
          },
          async toShape(record, { descendants, database, transform }) {
            const scriptValue = record.get("context_processing_script")?.ifString()?.ifNotEmpty();
            const contextProcessingScript = scriptValue ? await sdk_build_plugins_1.NowIncludeShape.fromRecord(record, record.get("context_processing_script"), transform) : void 0;
            const teams = descendants.query(constants_1.SN_AIA_TEAM);
            const teamRecord = teams.length > 0 ? teams[0] : void 0;
            const teammembers = descendants.query(constants_1.SN_AIA_TEAM_MEMBER);
            const triggerConfigs = descendants.query(constants_1.SN_AIA_TRIGGER_CONFIG);
            const triggerMappings = descendants.query(constants_1.SN_AIA_TRIGGER_MAPPING);
            const versions = descendants.query(constants_1.SN_AIA_VERSION);
            const useCaseConfigs = descendants.query(constants_1.SN_AIA_USECASE_CONFIG);
            const useCaseConfig = useCaseConfigs.length > 0 ? useCaseConfigs[0] : void 0;
            const allRoleConfigs = descendants.query(constants_1.SYS_AGENT_ACCESS_ROLE_CONFIG);
            const roleMappings = descendants.query(constants_1.SYS_AGENT_ACCESS_ROLE_MAPPING);
            const dataAccessConfig = allRoleConfigs.find((config) => config.get("agent_table")?.getValue() === constants_1.SN_AIA_USECASE);
            const internalName = record.get("internal_name").ifString()?.getValue() || "";
            const matchingAcl = database.get(constants_1.SYS_SECURITY_ACL, {
              name: internalName,
              type: constants_1.GEN_AI_AGENTIC_USECASE_TYPE
            });
            const matchingAclId = matchingAcl?.getId().getValue();
            const aclRoleRecords = matchingAclId ? descendants.query(constants_1.SYS_SECURITY_ACL_ROLE).filter((r) => {
              const aclRef = r.get("sys_security_acl");
              const refId = aclRef?.isRecord() ? aclRef.getId().getValue() : aclRef?.ifString()?.getValue();
              return refId === matchingAclId;
            }) : [];
            const securityAclValue = (0, ai_agentic_workflow_transform_helper_1.computeSecurityAclValue)(matchingAcl, aclRoleRecords);
            const teamValue = (0, ai_agentic_workflow_transform_helper_1.computeTeamValue)(teamRecord, teammembers);
            const triggerConfigValue = (0, ai_agentic_workflow_transform_helper_1.mapTriggerConfigs)(triggerConfigs, triggerMappings);
            const versionsValue = (0, ai_agentic_workflow_transform_helper_1.computeVersionDetailsValue)(versions);
            const dataAccessValue = (0, ai_agentic_workflow_transform_helper_1.computeDataAccessValue)(dataAccessConfig, roleMappings);
            const runAsValue = useCaseConfig?.get("run_as_user")?.ifString()?.ifNotEmpty()?.getValue();
            const memoryScopeValue = teammembers.length > 0 ? (teammembers.find((tm) => tm.get("memory_scope")?.ifString()?.getValue() !== void 0 && tm.get("memory_scope")?.ifString()?.getValue() !== "global") ?? teammembers[0])?.get("memory_scope")?.ifString()?.getValue() : void 0;
            return {
              success: true,
              value: new sdk_build_core_1.CallExpressionShape({
                source: record,
                callee: "AiAgenticWorkflow",
                args: [
                  record.transform(({ $ }) => ({
                    $id: $.val(sdk_build_plugins_1.NowIdShape.from(record) ?? record.getId().getValue()),
                    name: $.from("name"),
                    description: $.from("description"),
                    contextProcessingScript: contextProcessingScript ? $.val(contextProcessingScript) : $.from("context_processing_script").def(""),
                    sysDomain: $.from("sys_domain").def("global"),
                    active: $.from("active").toBoolean().def(true),
                    recordType: $.from("record_type").def("template"),
                    executionMode: $.from("execution_mode").map((v) => v || "copilot").def("copilot"),
                    securityAcl: $.val(securityAclValue),
                    runAs: $.val(runAsValue).def(""),
                    dataAccess: $.val(dataAccessValue),
                    team: $.val(teamValue),
                    ...triggerConfigs.length > 0 ? { triggerConfig: $.val(triggerConfigValue) } : {},
                    memoryScope: $.val(memoryScopeValue).def("global"),
                    versions: $.val(versionsValue)
                  }))
                ]
              })
            };
          },
          async toFile(record, context) {
            const { config, transform, descendants, database } = context;
            const workflowAclIds = new Set(descendants.query(constants_1.SYS_SECURITY_ACL).map((r) => r.getId().getValue()));
            const deleteAclRoles = database.query(constants_1.SYS_SECURITY_ACL_ROLE).filter((rec) => {
              if (rec.getAction() !== "DELETE") {
                return false;
              }
              const aclRef = rec.get("sys_security_acl")?.toString().getValue();
              return aclRef ? workflowAclIds.has(aclRef) : false;
            });
            if (deleteAclRoles.length === 0) {
              return { success: false };
            }
            const recordBuilder = (0, sdk_build_core_1.unloadBuilder)(config);
            const updateName = await transform.getUpdateName(record);
            const builder = recordBuilder.record(record, updateName);
            record.entries().sort(([a], [b]) => a.localeCompare(b)).forEach(([prop, shape]) => {
              builder.field(prop, shape);
            });
            const serializeDescendant = /* @__PURE__ */ __name(async (descendantRecord) => {
              const descUpdateName = await transform.getUpdateName(descendantRecord);
              const descBuilder = (0, sdk_build_core_1.unloadBuilder)(config);
              const descRecordBuilder = descBuilder.record(descendantRecord, descUpdateName);
              descendantRecord.entries().sort(([a], [b]) => a.localeCompare(b)).forEach(([prop, shape]) => {
                descRecordBuilder.field(prop, shape);
              });
              return {
                source: descendantRecord,
                name: `${descUpdateName}.xml`,
                category: descendantRecord.getInstallCategory(),
                content: descBuilder.end()
              };
            }, "serializeDescendant");
            const descendantFiles = [];
            for (const table of [
              constants_1.SN_AIA_TEAM,
              constants_1.SN_AIA_TEAM_MEMBER,
              constants_1.SN_AIA_USECASE_CONFIG,
              constants_1.SN_AIA_TRIGGER_CONFIG,
              constants_1.SN_AIA_TRIGGER_MAPPING,
              constants_1.SN_AIA_VERSION,
              constants_1.SYS_AGENT_ACCESS_ROLE_CONFIG,
              constants_1.SYS_AGENT_ACCESS_ROLE_MAPPING,
              constants_1.SYS_SECURITY_ACL,
              constants_1.SYS_SECURITY_ACL_ROLE
            ]) {
              for (const rec of descendants.query(table)) {
                descendantFiles.push(await serializeDescendant(rec));
              }
            }
            for (const rec of deleteAclRoles) {
              descendantFiles.push(await serializeDescendant(rec));
            }
            return {
              success: true,
              value: [
                {
                  source: record,
                  name: `${updateName}.xml`,
                  category: record.getInstallCategory(),
                  content: recordBuilder.end()
                },
                ...descendantFiles
              ]
            };
          }
        },
        [constants_1.SN_AIA_TEAM]: {
          coalesce: ["name"]
        },
        [constants_1.SN_AIA_TEAM_MEMBER]: {
          coalesce: ["agent", "team"]
        },
        [constants_1.SN_AIA_VERSION]: {
          coalesce: ["target_id", "version_name"]
        },
        [constants_1.SN_AIA_TRIGGER_MAPPING]: {
          coalesce: ["trigger_configuration", "related_resource_record"]
        },
        [constants_1.SN_AIA_TRIGGER_CONFIG]: {
          coalesce: ["name", "usecase"]
        },
        [constants_1.SN_AIA_USECASE_CONFIG]: {
          coalesce: ["usecase_configuration"]
        },
        [constants_1.SYS_AGENT_ACCESS_ROLE_CONFIG]: {
          coalesce: ["agent"]
        },
        [constants_1.SYS_AGENT_ACCESS_ROLE_MAPPING]: {
          coalesce: ["agent_access_config", "role"]
        },
        [constants_1.SYS_SECURITY_ACL_ROLE]: {
          coalesce: ["sys_security_acl", "sys_user_role"]
        }
      },
      shapes: [
        {
          shape: sdk_build_core_1.CallExpressionShape,
          fileTypes: ["fluent"],
          async toRecord(callExpression, { factory, diagnostics, config }) {
            if (callExpression.getCallee() !== "AiAgenticWorkflow") {
              return { success: false };
            }
            const arg = callExpression.getArgument(0).asObject();
            const workflowName = arg.get("name").ifString()?.getValue() || "";
            const sysDomain = arg.get("sysDomain")?.ifString()?.getValue() || "global";
            const scopeName = config.scope;
            const computedWorkflowInternalName = `${sysDomain}.${scopeName}.${workflowName}`;
            const aiVersions = arg.get("versions");
            const { instructions: publishedInstructions, error: versionError, errorSource } = (0, ai_agentic_workflow_shapes_helper_1.extractPublishedInstructions)(aiVersions, workflowName);
            if (versionError) {
              diagnostics.error(errorSource || aiVersions, versionError);
              return { success: false };
            }
            const securityAclField = arg.get("securityAcl")?.ifObject();
            if (!securityAclField) {
              diagnostics.error(arg, "AI Agentic Workflow must have a securityAcl field.");
              return { success: false };
            }
            const dataAccess = arg.get("dataAccess")?.ifObject();
            const runAsUser = arg.get("runAs");
            const teamObj = arg.get("team");
            let teamRecord;
            const teamMembers = teamObj?.isObject() ? teamObj.get("members") : void 0;
            if (teamObj?.isObject()) {
              const workflowDescription = arg.get("description");
              teamRecord = await (0, ai_agentic_workflow_shapes_helper_1.createTeamRecord)(factory, callExpression, teamObj, constants_1.SN_AIA_TEAM, workflowDescription);
            }
            let securityAclRecords;
            if (securityAclField) {
              securityAclRecords = await (0, security_acl_processor_1.processSecurityAcl)(securityAclField, computedWorkflowInternalName, factory, diagnostics);
            }
            const workflowRecord = await (0, ai_agentic_workflow_shapes_helper_1.createWorkflowRecord)(factory, callExpression, computedWorkflowInternalName, publishedInstructions, teamRecord?.getId(), constants_1.SN_AIA_USECASE, securityAclRecords?.aclRecord.getId().getValue());
            const workflowMemoryScope = arg.get("memoryScope")?.ifString()?.getValue() || "global";
            const workflowDomain = arg.get("sysDomain")?.ifString()?.getValue() || "global";
            const teamMemberRecords = teamMembers && teamRecord ? await (0, ai_agentic_workflow_shapes_helper_1.createTeamMemberRecords)(factory, callExpression, teamMembers, teamRecord.getId(), workflowRecord.getId(), workflowMemoryScope, workflowDomain, constants_1.SN_AIA_TEAM_MEMBER, diagnostics) : [];
            const aiVersionRecords = await (0, ai_agentic_workflow_shapes_helper_1.createVersionRecords)(factory, callExpression, workflowRecord.getId(), workflowRecord.get("sys_domain"), aiVersions, constants_1.SN_AIA_VERSION);
            const triggerConfigs = arg.get("triggerConfig")?.ifArray()?.getElements() ?? [];
            const triggerConfigRecords = [];
            for (const triggerConfig of triggerConfigs) {
              if (triggerConfig?.isObject()) {
                const validationResult = (0, ai_agentic_workflow_shapes_helper_1.validateTriggerConfig)(triggerConfig);
                if (validationResult) {
                  diagnostics.error(validationResult.errorSource, validationResult.error);
                  return { success: false };
                }
                const triggerName = triggerConfig.get("name")?.ifString()?.getValue() || "";
                const triggerDomain = triggerConfig.get("domain")?.ifString()?.getValue() || "global";
                const computedTriggerInternalName = `${triggerDomain}.${scopeName}.${triggerName}`;
                const triggerConfigRecord = await (0, ai_agentic_workflow_shapes_helper_1.createTriggerConfigRecord)(factory, callExpression, triggerConfig, computedTriggerInternalName, workflowRecord.getId(), constants_1.SN_AIA_TRIGGER_CONFIG, diagnostics);
                triggerConfigRecords.push(triggerConfigRecord);
              }
            }
            const useCaseConfigRecord = await (0, ai_agentic_workflow_shapes_helper_1.createUseCaseConfigRecord)(factory, callExpression, workflowRecord.getId(), constants_1.SN_AIA_USECASE_CONFIG);
            const triggerMappingRecords = [];
            for (let i = 0; i < triggerConfigRecords.length; i++) {
              const linkedTriggerConfig = triggerConfigRecords[i];
              const correspondingTriggerConfig = triggerConfigs[i];
              if (linkedTriggerConfig && correspondingTriggerConfig?.isObject()) {
                const triggerMappingRecord = await (0, ai_agentic_workflow_shapes_helper_1.createTriggerMappingRecord)(factory, callExpression, workflowRecord.getId(), correspondingTriggerConfig, linkedTriggerConfig.getId(), constants_1.SN_AIA_TRIGGER_MAPPING);
                triggerMappingRecords.push(triggerMappingRecord);
              }
            }
            let dataAccessRecords;
            if (dataAccess) {
              const runAsUserValue = runAsUser?.ifString()?.getValue();
              dataAccessRecords = await (0, ai_agentic_workflow_shapes_helper_1.createDataAccessConfigRecord)(factory, callExpression, workflowRecord.getId(), dataAccess, constants_1.SYS_AGENT_ACCESS_ROLE_CONFIG, diagnostics, !!runAsUserValue);
            }
            const childRecords = [
              teamRecord,
              ...teamMemberRecords,
              ...aiVersionRecords,
              useCaseConfigRecord,
              ...triggerConfigRecords,
              ...triggerMappingRecords
            ];
            if (dataAccessRecords) {
              childRecords.push(...dataAccessRecords);
            }
            if (securityAclRecords) {
              childRecords.push(...securityAclRecords.allRecords);
            }
            return {
              success: true,
              value: workflowRecord.with(...childRecords.filter((r) => r !== void 0))
            };
          }
        }
      ]
    });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agentic-workflow/index.js
var require_ai_agentic_workflow = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/plugin/ai-agentic-workflow/index.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.AiAgenticWorkflowPlugin = void 0;
    var ai_agentic_workflow_plugin_1 = require_ai_agentic_workflow_plugin();
    Object.defineProperty(exports2, "AiAgenticWorkflowPlugin", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return ai_agentic_workflow_plugin_1.AiAgenticWorkflowPlugin;
    }, "get") });
    exports2.default = ai_agentic_workflow_plugin_1.AiAgenticWorkflowPlugin;
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/AiAgent.js
var require_AiAgent = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/AiAgent.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.AiAgent = AiAgent;
    function AiAgent(config) {
      return config;
    }
    __name(AiAgent, "AiAgent");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/constants.js
var require_constants3 = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/constants.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/data-access-config.js
var require_data_access_config = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/data-access-config.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/security-acl-config.js
var require_security_acl_config = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/security-acl-config.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/tool-config.js
var require_tool_config = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/tool-config.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/trigger-config.js
var require_trigger_config = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/trigger-config.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/version-config.js
var require_version_config = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/version-config.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/AiAgenticWorkflow.js
var require_AiAgenticWorkflow = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/AiAgenticWorkflow.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.AiAgenticWorkflow = AiAgenticWorkflow;
    function AiAgenticWorkflow(config) {
      return config;
    }
    __name(AiAgenticWorkflow, "AiAgenticWorkflow");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/index.js
var require_api = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/api/index.js"(exports2) {
    "use strict";
    var __createBinding2 = exports2 && exports2.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
          return m[k];
        }, "get") };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar2 = exports2 && exports2.__exportStar || function(m, exports3) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports3, p)) __createBinding2(exports3, m, p);
    };
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.AiAgenticWorkflow = void 0;
    __exportStar2(require_AiAgent(), exports2);
    __exportStar2(require_constants3(), exports2);
    __exportStar2(require_data_access_config(), exports2);
    __exportStar2(require_security_acl_config(), exports2);
    __exportStar2(require_tool_config(), exports2);
    __exportStar2(require_trigger_config(), exports2);
    __exportStar2(require_version_config(), exports2);
    var AiAgenticWorkflow_1 = require_AiAgenticWorkflow();
    Object.defineProperty(exports2, "AiAgenticWorkflow", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return AiAgenticWorkflow_1.AiAgenticWorkflow;
    }, "get") });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-aiaf-plugin@1.0.0-aiaffluent.30_@servicenow+sdk-build-core@packages+bui_708b6c883101573ab798d22cc3e4f7ce/node_modules/@servicenow/sdk-aiaf-plugin/dist/index.js
var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
  if (k2 === void 0) k2 = k;
  var desc = Object.getOwnPropertyDescriptor(m, k);
  if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
    desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return m[k];
    }, "get") };
  }
  Object.defineProperty(o, k2, desc);
} : function(o, m, k, k2) {
  if (k2 === void 0) k2 = k;
  o[k2] = m[k];
});
var __exportStar = exports && exports.__exportStar || function(m, exports2) {
  for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AiAgenticWorkflowPlugin = exports.AiAgentPlugin = void 0;
var ai_agent_1 = require_ai_agent();
Object.defineProperty(exports, "AiAgentPlugin", { enumerable: true, get: /* @__PURE__ */ __name(function() {
  return ai_agent_1.AiAgentPlugin;
}, "get") });
var ai_agentic_workflow_1 = require_ai_agentic_workflow();
Object.defineProperty(exports, "AiAgenticWorkflowPlugin", { enumerable: true, get: /* @__PURE__ */ __name(function() {
  return ai_agentic_workflow_1.AiAgenticWorkflowPlugin;
}, "get") });
__exportStar(require_api(), exports);
exports.default = [ai_agent_1.AiAgentPlugin, ai_agentic_workflow_1.AiAgenticWorkflowPlugin];
//# sourceMappingURL=aiaf.js.map
