"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.loadExternalPlugins = void 0;
// biome-ignore lint/style/noRestrictedImports: intentionally Node.js-only — browser bundles replace this module with the virtual module in esbuild-plugins.ts
const path_1 = __importDefault(require("path"));
const fs_1 = __importDefault(require("fs"));
/**
 * External plugins loader (Node)
 *
 * Loads pre-bundled external plugins at runtime using dynamic require().
 *
 * In browser bundles (LSP, web), this entire module is replaced by a
 * virtual esbuild module generated in esbuild-plugins.ts
 *
 * Any changes made here must also be made in the generated module
 *
 * @param outputName - Sanitized filename on disk
 * @param npmPackageName - npm package name, used for the node_modules fallback
 */
const loadExternalPlugins = function loadExternalPlugins(outputName, npmPackageName) {
    const bundledPath = path_1.default.join(__dirname, `${outputName}.js`);
    if (fs_1.default.existsSync(bundledPath)) {
        const pluginModule = require(bundledPath);
        if (!pluginModule.default) {
            throw new Error(`Bundled plugin ${outputName} has no default export`);
        }
        return [pluginModule.default].flat();
    }
    // Development fallback: load from node_modules
    let pluginModule;
    try {
        pluginModule = require(npmPackageName);
    }
    catch (error) {
        const message = error instanceof Error ? error.message : String(error);
        throw new Error(`Failed to load external plugin ${npmPackageName}: ${message}`);
    }
    if (!pluginModule.default) {
        throw new Error(`Plugin ${npmPackageName} has no default export`);
    }
    return [pluginModule.default].flat();
};
exports.loadExternalPlugins = loadExternalPlugins;
//# sourceMappingURL=loader.js.map