"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/constants/playbook-plugin-constants.js
var require_playbook_plugin_constants = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/constants/playbook-plugin-constants.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.TABLE_NAME_SUFFIX = exports2.LANE_CALLEE_NAMES = exports2.ACTIVITY_CALLEE_NAMES = exports2.CURRENT_ACTIVITY_CALLEE = exports2.DATAPILL_CALLEE = exports2.PARENT_RECORD_MAPPER_KEY = exports2.PARENT_RECORD = exports2.PROCESS_INPUT_TABLE = exports2.TRIGGER_INSTANCE_CALLEE_NAMES = exports2.TRIGGER_INSTANCE_GLOBAL_NAME = exports2.TRIGGER_INSTANCE_API_NAME = exports2.VARIANT_TABLE = exports2.TRIGGER_INSTANCE_TABLE = exports2.KEYWORDS_PLATFORM_TOKEN = exports2.ACTIVITY_DEFAULTS = exports2.SYS_ELEMENT_MAPPING_TABLE = exports2.SYS_VARIABLE_VALUE_TABLE = exports2.ACTIVITY_GENERATED_CALLEE = exports2.ACTIVITY_API_NAME = exports2.ACTIVITY_TABLE = exports2.DURATION_TYPE_FROM_DB = exports2.DURATION_TYPE_TO_DB = exports2.TIMER_ATTRIBUTES_TABLE = exports2.LANE_DEFAULTS = exports2.LANE_GENERATED_CALLEE = exports2.LANE_API_NAME = exports2.LANE_TABLE = exports2.PLAYBOOK_INPUT_ATTRIBUTES = exports2.PLAYBOOK_OUTPUT_TABLE = exports2.PLAYBOOK_DEFAULTS = exports2.PLAYBOOK_KNOWN_PROCESS_TYPE_NAMES = exports2.PLAYBOOK_DEFAULT_PROCESS_TYPE_DB = exports2.PLAYBOOK_PROCESS_TYPE_FROM_DB = exports2.PLAYBOOK_PROCESS_TYPE_TO_DB = exports2.PLAYBOOK_PROCESS_TYPE_TABLE = exports2.PLAYBOOK_API_NAME = exports2.PLAYBOOK_TABLE = void 0;
    exports2.getPlaybookProcessTypeSysId = getPlaybookProcessTypeSysId;
    exports2.getPlaybookProcessTypeIdentifier = getPlaybookProcessTypeIdentifier;
    exports2.isPlaybookProcessTypeSysId = isPlaybookProcessTypeSysId;
    exports2.encodeKeywordsToken = encodeKeywordsToken;
    exports2.decodeKeywordsToken = decodeKeywordsToken;
    exports2.PLAYBOOK_TABLE = "sys_pd_process_definition";
    exports2.PLAYBOOK_API_NAME = "PlaybookDefinition";
    exports2.PLAYBOOK_PROCESS_TYPE_TABLE = "sys_pd_process_type";
    exports2.PLAYBOOK_PROCESS_TYPE_TO_DB = {
      "Standard playbook": "52c6d3eeff1be2102c2fffffffffff44"
    };
    exports2.PLAYBOOK_PROCESS_TYPE_FROM_DB = {
      [exports2.PLAYBOOK_PROCESS_TYPE_TO_DB["Standard playbook"]]: "Standard playbook"
    };
    exports2.PLAYBOOK_DEFAULT_PROCESS_TYPE_DB = exports2.PLAYBOOK_PROCESS_TYPE_TO_DB["Standard playbook"];
    exports2.PLAYBOOK_KNOWN_PROCESS_TYPE_NAMES = Object.keys(exports2.PLAYBOOK_PROCESS_TYPE_TO_DB);
    function getPlaybookProcessTypeSysId(processType) {
      return exports2.PLAYBOOK_PROCESS_TYPE_TO_DB[processType];
    }
    __name(getPlaybookProcessTypeSysId, "getPlaybookProcessTypeSysId");
    function getPlaybookProcessTypeIdentifier(sysId) {
      return exports2.PLAYBOOK_PROCESS_TYPE_FROM_DB[sysId];
    }
    __name(getPlaybookProcessTypeIdentifier, "getPlaybookProcessTypeIdentifier");
    function isPlaybookProcessTypeSysId(value) {
      return /^[0-9a-f]{32}$/i.test(value);
    }
    __name(isPlaybookProcessTypeSysId, "isPlaybookProcessTypeSysId");
    exports2.PLAYBOOK_DEFAULTS = {
      ACTIVE: true,
      ACCESS: "public",
      STATUS: "draft",
      RESTARTABLE: "RESTARTABLE_FALSE",
      ALLOW_AS_NESTED: false,
      PUBLIC_ACCESS: false,
      RUN_STRATEGY: "run_once",
      RUN_STRATEGY_ON_PROCESS_DEFINITION: true,
      EXECUTION_TYPE: "record_driven",
      PROCESS_TYPE: "Standard playbook",
      SCHEMA_VERSION: 3,
      /** Maximum schema version Fluent supports; used as the default when none is specified. */
      MAX_SUPPORTED_SCHEMA_VERSION: 3,
      START_NON_BLOCKING: true,
      DATA_RETENTION_PERIOD_OVERRIDE: "6_week",
      VIEW_TYPE: "DIAGRAM",
      PERMISSION: "{}"
    };
    exports2.PLAYBOOK_OUTPUT_TABLE = "sys_pd_process_output";
    exports2.PLAYBOOK_INPUT_ATTRIBUTES = "element_mapping_provider=com.snc.pd.designer.elementmapping.PlaybookInputElementMapper";
    exports2.LANE_TABLE = "sys_pd_lane";
    exports2.LANE_API_NAME = "Lane";
    exports2.LANE_GENERATED_CALLEE = "wfa.playbook.lane";
    exports2.LANE_DEFAULTS = {
      ACTIVE: true,
      RESTART_RULE: "RUN_ONLY_ONCE",
      ORDER: 1,
      PERMISSION: "{}",
      START_RULE: "immediate",
      CONDITION_TO_RUN: "",
      STARTS_AFTER_ACTIVITIES: "",
      STARTS_AFTER_BRANCHES: "",
      STARTS_AFTER_LANES: "",
      PROCESS_DEFINITION: "",
      NAME: ""
    };
    exports2.TIMER_ATTRIBUTES_TABLE = "sys_pd_timer_attributes";
    exports2.DURATION_TYPE_TO_DB = {
      explicit: "explicit_duration",
      relative: "relative_duration",
      percentage: "percentage_duration"
    };
    exports2.DURATION_TYPE_FROM_DB = {
      explicit_duration: "explicit",
      relative_duration: "relative",
      percentage_duration: "percentage"
    };
    exports2.ACTIVITY_TABLE = "sys_pd_activity";
    exports2.ACTIVITY_API_NAME = "Activity";
    exports2.ACTIVITY_GENERATED_CALLEE = "wfa.playbook.activity";
    exports2.SYS_VARIABLE_VALUE_TABLE = "sys_variable_value";
    exports2.SYS_ELEMENT_MAPPING_TABLE = "sys_element_mapping";
    exports2.ACTIVITY_DEFAULTS = {
      ACTIVE: true,
      START_RULE: "immediate",
      AI_AGENT_RUN_AS: "playbook_user"
    };
    exports2.KEYWORDS_PLATFORM_TOKEN = "123TEXTQUERY321";
    var KEYWORDS_FIELD_REGEX = /(?<=^|\^(?:OR)?)keywords\b/g;
    var KEYWORDS_TOKEN_FIELD_REGEX = new RegExp(`(?<=^|\\^(?:OR)?)${exports2.KEYWORDS_PLATFORM_TOKEN}\\b`, "g");
    function encodeKeywordsToken(condition) {
      return condition.replace(KEYWORDS_FIELD_REGEX, exports2.KEYWORDS_PLATFORM_TOKEN);
    }
    __name(encodeKeywordsToken, "encodeKeywordsToken");
    function decodeKeywordsToken(condition) {
      return condition.replace(KEYWORDS_TOKEN_FIELD_REGEX, "keywords");
    }
    __name(decodeKeywordsToken, "decodeKeywordsToken");
    exports2.TRIGGER_INSTANCE_TABLE = "sys_pd_trigger_instance";
    exports2.VARIANT_TABLE = "sys_pd_process_variant";
    exports2.TRIGGER_INSTANCE_API_NAME = "wfa.playbook.trigger";
    exports2.TRIGGER_INSTANCE_GLOBAL_NAME = "PlaybookTrigger";
    exports2.TRIGGER_INSTANCE_CALLEE_NAMES = [exports2.TRIGGER_INSTANCE_API_NAME, exports2.TRIGGER_INSTANCE_GLOBAL_NAME];
    exports2.PROCESS_INPUT_TABLE = "sys_pd_process_input";
    exports2.PARENT_RECORD = {
      ELEMENT: "parent_record",
      LABEL: "Parent record",
      INTERNAL_TYPE: "reference",
      MAX_LENGTH: 32,
      ORDER: 1,
      /** Platform-specific attribute that enables the playbook input element mapper */
      ATTRIBUTES: "element_mapping_provider=com.snc.pd.designer.elementmapping.PlaybookInputElementMapper"
    };
    exports2.PARENT_RECORD_MAPPER_KEY = "parentRecord";
    exports2.DATAPILL_CALLEE = "wfa.playbook.dataPill";
    exports2.CURRENT_ACTIVITY_CALLEE = "wfa.playbook.currentActivity";
    exports2.ACTIVITY_CALLEE_NAMES = /* @__PURE__ */ new Set([exports2.ACTIVITY_API_NAME, "wfa.playbook.activity"]);
    exports2.LANE_CALLEE_NAMES = /* @__PURE__ */ new Set([exports2.LANE_API_NAME, "wfa.playbook.lane"]);
    exports2.TABLE_NAME_SUFFIX = ".:TN";
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/playbook-diagnostics-plugin.js
var require_playbook_diagnostics_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/playbook-diagnostics-plugin.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.PlaybookDiagnosticsPlugin = void 0;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var MAX_TRIGGER_COUNT = 10;
    exports2.PlaybookDiagnosticsPlugin = sdk_build_core_1.Plugin.create({
      name: "PlaybookDiagnosticsPlugin",
      shapes: [
        {
          shape: sdk_build_core_1.CallExpressionShape,
          fileTypes: ["fluent"],
          toRecord(callExpression, { diagnostics }) {
            if (callExpression.getCallee() !== playbook_plugin_constants_1.PLAYBOOK_API_NAME) {
              return { success: false };
            }
            const config = callExpression.getArgument(0)?.asObject();
            if (!config) {
              return { success: false };
            }
            const processTypeShape = config.get("processType");
            const processTypeExamples = playbook_plugin_constants_1.PLAYBOOK_KNOWN_PROCESS_TYPE_NAMES.map((value) => `"${value}"`).join(", ");
            if (processTypeShape?.isDefined() && !processTypeShape.isUndefined()) {
              const processTypeValue = processTypeShape.ifString()?.getValue();
              if (processTypeValue === void 0) {
                diagnostics.error(processTypeShape, `processType must be a name string. Use a process type name such as ${processTypeExamples}.`);
              } else if (processTypeValue.trim() === "") {
                diagnostics.error(processTypeShape, `processType must be a non-empty name string. Use a process type name such as ${processTypeExamples}.`);
              } else if ((0, playbook_plugin_constants_1.isPlaybookProcessTypeSysId)(processTypeValue)) {
                diagnostics.error(processTypeShape, `processType must be a name, not a sys_id. Use a process type name such as ${processTypeExamples}.`);
              } else if (!(0, playbook_plugin_constants_1.getPlaybookProcessTypeSysId)(processTypeValue)) {
                diagnostics.error(processTypeShape, `Unsupported processType "${processTypeValue}". We do not have that process type defined in the SDK mapping. Supported values: ${processTypeExamples}.`);
              }
            }
            const parentTableShape = config.get("parentTable");
            const parentTableVal = parentTableShape?.ifString()?.getValue();
            if (parentTableShape && parentTableVal === "") {
              diagnostics.error(parentTableShape, "parentTable must be a valid table name.");
            }
            const triggersObject = callExpression.getArgument(1);
            if (triggersObject && !triggersObject.isUndefined() && triggersObject.is(sdk_build_core_1.ObjectShape)) {
              const triggersValue = triggersObject.get("triggers");
              if (triggersValue && triggersValue.is(sdk_build_core_1.ArrayShape)) {
                const triggers = triggersValue.getElements();
                if (triggers.length > MAX_TRIGGER_COUNT) {
                  diagnostics.error(triggersValue, `Playbook has ${triggers.length} triggers. The maximum allowed is ${MAX_TRIGGER_COUNT}.`);
                }
                if (parentTableVal) {
                  for (const element of triggers) {
                    if (!element.is(sdk_build_core_1.CallExpressionShape)) {
                      continue;
                    }
                    const triggerCall = element.as(sdk_build_core_1.CallExpressionShape);
                    const triggerInputs = triggerCall.getArgument(2)?.ifObject()?.asObject();
                    if (!triggerInputs) {
                      continue;
                    }
                    const triggerTable = triggerInputs.get("table")?.ifString()?.getValue();
                    if (triggerTable !== parentTableVal) {
                      const mapperArg = triggerCall.getArgument(3);
                      let hasParentRecord = false;
                      if (mapperArg && !mapperArg.isUndefined() && mapperArg.is(sdk_build_plugins_1.ArrowFunctionShape)) {
                        const returnVal = mapperArg.as(sdk_build_plugins_1.ArrowFunctionShape).getReturnValue();
                        if (returnVal?.is(sdk_build_core_1.ObjectShape)) {
                          const pr = returnVal.as(sdk_build_core_1.ObjectShape).get(playbook_plugin_constants_1.PARENT_RECORD_MAPPER_KEY);
                          hasParentRecord = !!pr && !pr.isUndefined();
                        }
                      }
                      if (!hasParentRecord) {
                        const context = triggerTable ? `Trigger table '${triggerTable}' differs from` : "Scheduled trigger has no record table matching";
                        diagnostics.error(triggerInputs, `${context} playbook parentTable '${parentTableVal}'. parentRecord is required in the trigger's mapper callback.`);
                      }
                    }
                  }
                }
              }
            }
            return { success: false };
          }
        }
      ],
      docs: []
    });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/playbook-datapill-diagnostics-plugin.js
var require_playbook_datapill_diagnostics_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/playbook-datapill-diagnostics-plugin.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.PlaybookDataPillDiagnosticsPlugin = void 0;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var ACTIVITY_CONFIG_PILL_PATHS = /* @__PURE__ */ new Set([
      "conditionToRun",
      "startWithDelay.duration",
      "startWithDelay.relativeDatetime",
      "startWithDelay.percentage",
      "startWithDelay.percentageDatetime"
    ]);
    var LANE_CONFIG_PILL_PATHS = /* @__PURE__ */ new Set([
      "conditionToRun",
      "startWithDelay.duration",
      "startWithDelay.relativeDatetime",
      "startWithDelay.percentage",
      "startWithDelay.percentageDatetime"
    ]);
    var TRIGGER_CALLEE_NAMES = new Set(playbook_plugin_constants_1.TRIGGER_INSTANCE_CALLEE_NAMES);
    function getCalleeName(node) {
      const expr = node.getExpression();
      if (sdk_build_core_1.ts.Node.isIdentifier(expr) || sdk_build_core_1.ts.Node.isPropertyAccessExpression(expr)) {
        return expr.getText();
      }
      return void 0;
    }
    __name(getCalleeName, "getCalleeName");
    function isDescendantOrSelf(node, ancestor) {
      let current = node;
      while (current) {
        if (current === ancestor) {
          return true;
        }
        current = current.getParent();
      }
      return false;
    }
    __name(isDescendantOrSelf, "isDescendantOrSelf");
    function findAncestorCallWithArgIndex(node, calleeNames) {
      let current = node.getParent();
      while (current) {
        if (sdk_build_core_1.ts.Node.isCallExpression(current)) {
          const callee = getCalleeName(current);
          if (callee && calleeNames.has(callee)) {
            const args = current.getArguments();
            for (let i = 0; i < args.length; i++) {
              const arg = args[i];
              if (arg && isDescendantOrSelf(node, arg)) {
                return { call: current, argIndex: i };
              }
            }
            return { call: current, argIndex: -1 };
          }
        }
        current = current.getParent();
      }
      return void 0;
    }
    __name(findAncestorCallWithArgIndex, "findAncestorCallWithArgIndex");
    function buildConfigPropertyPath(node, configArg) {
      const parts = [];
      let current = node.getParent();
      while (current && current !== configArg) {
        if (sdk_build_core_1.ts.Node.isPropertyAssignment(current)) {
          parts.unshift(current.getName());
        }
        current = current.getParent();
      }
      return parts.join(".");
    }
    __name(buildConfigPropertyPath, "buildConfigPropertyPath");
    function validateDataPillPosition(pillNode, diagnostics) {
      const activityAncestor = findAncestorCallWithArgIndex(pillNode, playbook_plugin_constants_1.ACTIVITY_CALLEE_NAMES);
      if (activityAncestor) {
        const { argIndex, call } = activityAncestor;
        if (argIndex === 2 || argIndex === 3) {
          return;
        }
        if (argIndex === 1) {
          const configArg = call.getArguments()[1];
          if (configArg) {
            const path = buildConfigPropertyPath(pillNode, configArg);
            if (ACTIVITY_CONFIG_PILL_PATHS.has(path)) {
              return;
            }
          }
          diagnostics.error(pillNode, "wfa.playbook.dataPill() is not allowed in activity config fields. Pills can only be used in inputs, experience properties, conditionToRun, or startWithDelay timer fields.");
          return;
        }
        diagnostics.error(pillNode, "wfa.playbook.dataPill() is not allowed in this position within an Activity.");
        return;
      }
      const laneAncestor = findAncestorCallWithArgIndex(pillNode, playbook_plugin_constants_1.LANE_CALLEE_NAMES);
      if (laneAncestor) {
        const { argIndex, call } = laneAncestor;
        if (argIndex === 0) {
          const configArg = call.getArguments()[0];
          if (configArg) {
            const path = buildConfigPropertyPath(pillNode, configArg);
            if (LANE_CONFIG_PILL_PATHS.has(path)) {
              return;
            }
          }
          diagnostics.error(pillNode, "wfa.playbook.dataPill() is not allowed in lane config fields. Pills can only be used in conditionToRun or startWithDelay timer fields.");
          return;
        }
        diagnostics.error(pillNode, "wfa.playbook.dataPill() is not allowed in this position within a Lane.");
        return;
      }
      const triggerAncestor = findAncestorCallWithArgIndex(pillNode, TRIGGER_CALLEE_NAMES);
      if (triggerAncestor) {
        const { argIndex } = triggerAncestor;
        if (argIndex === 3) {
          return;
        }
        diagnostics.error(pillNode, "wfa.playbook.dataPill() is not allowed in this position within a PlaybookTrigger. Pills can only be used in the playbook inputs mapper (4th argument).");
        return;
      }
      diagnostics.error(pillNode, "wfa.playbook.dataPill() is not allowed in PlaybookDefinition config or declarations. Pills can only be used inside Activity inputs, experience properties, condition fields, or trigger mappers.");
    }
    __name(validateDataPillPosition, "validateDataPillPosition");
    exports2.PlaybookDataPillDiagnosticsPlugin = sdk_build_core_1.Plugin.create({
      name: "PlaybookDataPillDiagnosticsPlugin",
      shapes: [
        {
          shape: sdk_build_core_1.CallExpressionShape,
          fileTypes: ["fluent"],
          toRecord(callExpression, { diagnostics }) {
            if (callExpression.getCallee() !== playbook_plugin_constants_1.PLAYBOOK_API_NAME) {
              return { success: false };
            }
            const rootNode = callExpression.getOriginalNode();
            if (!sdk_build_core_1.ts.Node.isCallExpression(rootNode)) {
              return { success: false };
            }
            const dataPillCalls = rootNode.getDescendantsOfKind(sdk_build_core_1.ts.SyntaxKind.CallExpression).filter((node) => getCalleeName(node) === playbook_plugin_constants_1.DATAPILL_CALLEE);
            for (const pillCall of dataPillCalls) {
              validateDataPillPosition(pillCall, diagnostics);
            }
            return { success: false };
          }
        }
      ],
      docs: []
    });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/lane-diagnostics-plugin.js
var require_lane_diagnostics_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/lane-diagnostics-plugin.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.LaneDiagnosticsPlugin = void 0;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    exports2.LaneDiagnosticsPlugin = sdk_build_core_1.Plugin.create({
      name: "LaneDiagnosticsPlugin",
      shapes: [
        {
          shape: sdk_build_core_1.CallExpressionShape,
          fileTypes: ["fluent"],
          toRecord(callExpression) {
            if (callExpression.getCallee() !== playbook_plugin_constants_1.LANE_API_NAME) {
              return { success: false };
            }
            return { success: false };
          }
        }
      ],
      docs: []
    });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/constants/activity-ootb-maps.js
var require_activity_ootb_maps = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/constants/activity-ootb-maps.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.EXPERIENCE_PROPERTY_SYS_ID_TO_FLUENT = exports2.INPUT_SYS_ID_TO_FLUENT = exports2.DECISION_BRANCHES_VAR_SYS_ID = exports2.DECISION_DEF_SYS_ID = exports2.DECISION_ACTIVITY_DEF_KEY = exports2.ACTIVITY_DEF_EXPERIENCE_DISPLAY_PREFS = exports2.ACTIVITY_DEF_INPUT_DISPLAY_PREFS = exports2.ACTIVITY_DEF_SYS_ID_TO_KEY = void 0;
    exports2.ACTIVITY_DEF_SYS_ID_TO_KEY = {
      b0c35e2b1b630010affd0e55cc4bcbca: "ActivityDefinitions.Core.Instruction",
      c9839e38b701311004c164fdde11a904: "ActivityDefinitions.Core.RecordForm",
      "569b840c1bbf0010affd0e55cc4bcbac": "ActivityDefinitions.Core.UpdateRecord",
      "295188c01b3f0010affd0e55cc4bcb01": "ActivityDefinitions.Core.CreateNewRecord",
      "24af882e435011107fd97ff8abb8f233": "ActivityDefinitions.Core.NewRecordFormWithList",
      "0b7f23cf77045110612c1b1ad910612b": "ActivityDefinitions.Core.RecordList",
      "226ed8f6c7ad5110bfbaf89f51c260b1": "ActivityDefinitions.Core.Decision",
      ceac99e01b3f0010affd0e55cc4bcb83: "ActivityDefinitions.Core.Placeholder",
      "7058962f1b630010affd0e55cc4bcbba": "ActivityDefinitions.Core.KnowledgeArticle",
      "097b1efcb701311004c164fdde11a9dd": "ActivityDefinitions.Core.AutocompletingRecordForm",
      "5009aca043713110333c145f15b8f212": "ActivityDefinitions.Core.WaitForCondition",
      f16277690703001070e493d0fad30018: "ActivityDefinitions.Core.NewRecordForm",
      caee4d471b0c1010affd0e55cc4bcbaf: "ActivityDefinitions.Core.EmailForm",
      "51b422671ba30010affd0e55cc4bcbad": "ActivityDefinitions.Core.SendEmail",
      d8f4d66b1b630010affd0e55cc4bcbc7: "ActivityDefinitions.Core.TwoStepInstruction",
      da424d3f7f255210d9ed9b627c866524: "ActivityDefinitions.Core.ChecklistTask",
      e1a2a6431b530150db4bba63cc4bcb97: "ActivityDefinitions.Core.AskForMultiLevelApproval",
      a31c8f06a37201106e1d0ace26fcda76: "ActivityDefinitions.Core.RequestManagerApproval",
      b54fdecb1b130150db4bba63cc4bcb58: "ActivityDefinitions.Core.RequestAdHocApproval"
    };
    exports2.ACTIVITY_DEF_INPUT_DISPLAY_PREFS = {
      // Instruction
      b0c35e2b1b630010affd0e55cc4bcbca: /* @__PURE__ */ new Set(["message", "wait"]),
      // RecordForm
      c9839e38b701311004c164fdde11a904: /* @__PURE__ */ new Set(["assigned_to", "assignment_group"]),
      // UpdateRecord
      "569b840c1bbf0010affd0e55cc4bcbac": /* @__PURE__ */ new Set(["record", "table_name", "values"]),
      // CreateNewRecord
      "295188c01b3f0010affd0e55cc4bcb01": /* @__PURE__ */ new Set(["table_name", "values"]),
      // NewRecordFormWithList
      "24af882e435011107fd97ff8abb8f233": /* @__PURE__ */ new Set(["wait"]),
      // RecordList
      "0b7f23cf77045110612c1b1ad910612b": /* @__PURE__ */ new Set(["assignment_group", "assigned_to", "wait"]),
      // Placeholder — no inputs
      ceac99e01b3f0010affd0e55cc4bcb83: /* @__PURE__ */ new Set([]),
      // KnowledgeArticle
      "7058962f1b630010affd0e55cc4bcbba": /* @__PURE__ */ new Set([
        "assignment_group",
        "assigned_to",
        "title",
        "knowledge_article",
        "wait"
      ]),
      // AutocompletingRecordForm
      "097b1efcb701311004c164fdde11a9dd": /* @__PURE__ */ new Set([
        "table",
        "record",
        "completion_condition",
        "assignment_group",
        "assigned_to"
      ]),
      // WaitForCondition
      "5009aca043713110333c145f15b8f212": /* @__PURE__ */ new Set([
        "table",
        "record",
        "completion_condition",
        "assignment_group",
        "assigned_to"
      ]),
      // NewRecordForm
      f16277690703001070e493d0fad30018: /* @__PURE__ */ new Set(["table", "form_view", "template_fields"]),
      // EmailForm
      caee4d471b0c1010affd0e55cc4bcbaf: /* @__PURE__ */ new Set([
        "wait",
        "to",
        "cc",
        "subject",
        "body",
        "assignment_group",
        "assigned_to",
        "to_email_address",
        "cc_email_address",
        "bcc",
        "bcc_email_address",
        "table",
        "target_record"
      ]),
      // SendEmail
      "51b422671ba30010affd0e55cc4bcbad": /* @__PURE__ */ new Set(["to", "cc", "bcc", "subject", "body", "target_record"]),
      // TwoStepInstruction
      d8f4d66b1b630010affd0e55cc4bcbc7: /* @__PURE__ */ new Set([
        "assignment_group",
        "assigned_to",
        "initial_message",
        "completed_message",
        "skipped_message"
      ]),
      // ChecklistTask
      da424d3f7f255210d9ed9b627c866524: /* @__PURE__ */ new Set([
        "checklist_items",
        "checklist_name",
        "task",
        "show_sla",
        "can_skip",
        "assignment_group",
        "assigned_to",
        "checklist_owner"
      ]),
      // AskForMultiLevelApproval
      e1a2a6431b530150db4bba63cc4bcb97: /* @__PURE__ */ new Set([
        "assigned_to",
        "assigned_to_comments",
        "assigned_to_due_date",
        "table",
        "record"
      ]),
      // RequestManagerApproval
      a31c8f06a37201106e1d0ace26fcda76: /* @__PURE__ */ new Set(["table", "variable", "approver", "due_date", "comments"]),
      // RequestAdHocApproval
      b54fdecb1b130150db4bba63cc4bcb58: /* @__PURE__ */ new Set(["approver", "approver_group", "table", "record"])
    };
    exports2.ACTIVITY_DEF_EXPERIENCE_DISPLAY_PREFS = {
      // Instruction
      b0c35e2b1b630010affd0e55cc4bcbca: /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "footer",
        "is_automated"
      ]),
      // RecordForm
      c9839e38b701311004c164fdde11a904: /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "pending_title",
        "pending_description",
        "record_fields",
        "footer",
        "form_view",
        "form_fields",
        "attachment_source",
        "attachments_read_only",
        "show_sla",
        "show_checklist",
        "is_automated"
      ]),
      // UpdateRecord
      "569b840c1bbf0010affd0e55cc4bcbac": /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "pending_title",
        "pending_description",
        "record_fields",
        "footer",
        "form_view",
        "form_fields",
        "attachment_source",
        "attachments_read_only",
        "show_sla",
        "show_checklist",
        "is_automated"
      ]),
      // CreateNewRecord
      "295188c01b3f0010affd0e55cc4bcb01": /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "pending_title",
        "pending_description",
        "record_fields",
        "footer",
        "form_view",
        "form_fields",
        "attachment_source",
        "attachments_read_only",
        "show_sla",
        "show_checklist",
        "is_automated"
      ]),
      // NewRecordFormWithList
      "24af882e435011107fd97ff8abb8f233": /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "record_fields",
        "list_title",
        "table",
        "list_query",
        "ui_view",
        "columns",
        "max_columns",
        "row_count",
        "is_automated"
      ]),
      // RecordList
      "0b7f23cf77045110612c1b1ad910612b": /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "record_fields",
        "list_title",
        "table",
        "list_query",
        "ui_view",
        "columns",
        "max_columns",
        "row_count",
        "is_automated"
      ]),
      // Placeholder — no experience properties
      ceac99e01b3f0010affd0e55cc4bcb83: /* @__PURE__ */ new Set([]),
      // KnowledgeArticle
      "7058962f1b630010affd0e55cc4bcbba": /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "knowledge_table",
        "knowledge_record",
        "title",
        "footer",
        "is_automated"
      ]),
      // AutocompletingRecordForm
      "097b1efcb701311004c164fdde11a9dd": /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "pending_title",
        "pending_description",
        "record_fields",
        "footer",
        "form_view",
        "form_fields",
        "attachment_source",
        "attachments_read_only",
        "show_sla",
        "show_checklist",
        "is_automated"
      ]),
      // WaitForCondition
      "5009aca043713110333c145f15b8f212": /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "pending_title",
        "pending_description",
        "record_fields",
        "footer",
        "form_view",
        "form_fields",
        "attachment_source",
        "attachments_read_only",
        "show_sla",
        "show_checklist",
        "is_automated"
      ]),
      // NewRecordForm
      f16277690703001070e493d0fad30018: /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "pending_description",
        "pending_title",
        "record_fields",
        "footer",
        "form_view",
        "form_fields",
        "template_fields",
        "attachment_source",
        "attachments_read_only",
        "show_sla",
        "show_checklist",
        "is_automated"
      ]),
      // EmailForm
      caee4d471b0c1010affd0e55cc4bcbaf: /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "pending_title",
        "pending_description",
        "record_fields",
        "footer",
        "form_view",
        "form_fields",
        "attachment_source",
        "attachments_read_only",
        "show_sla",
        "show_checklist",
        "is_automated"
      ]),
      // SendEmail
      "51b422671ba30010affd0e55cc4bcbad": /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "pending_title",
        "pending_description",
        "record_fields",
        "footer",
        "form_view",
        "form_fields",
        "attachment_source",
        "attachments_read_only",
        "show_sla",
        "show_checklist",
        "is_automated"
      ]),
      // TwoStepInstruction
      d8f4d66b1b630010affd0e55cc4bcbc7: /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "footer",
        "is_automated"
      ]),
      // ChecklistTask
      da424d3f7f255210d9ed9b627c866524: /* @__PURE__ */ new Set([
        "associated_table",
        "associated_record",
        "experience_status_table",
        "experience_status_record",
        "tagline",
        "icon",
        "title",
        "description",
        "pending_title",
        "pending_description",
        "record_fields",
        "footer",
        "form_view",
        "form_fields",
        "attachment_source",
        "attachments_read_only",
        "show_sla",
        "show_checklist",
        "is_automated"
      ]),
      // AskForMultiLevelApproval — no experienceDisplayPreferences defined (all experience props are defaults)
      e1a2a6431b530150db4bba63cc4bcb97: /* @__PURE__ */ new Set([]),
      // RequestManagerApproval — no experienceDisplayPreferences defined
      a31c8f06a37201106e1d0ace26fcda76: /* @__PURE__ */ new Set([]),
      // RequestAdHocApproval — no experienceDisplayPreferences defined
      b54fdecb1b130150db4bba63cc4bcb58: /* @__PURE__ */ new Set([])
    };
    exports2.DECISION_ACTIVITY_DEF_KEY = "ActivityDefinitions.Core.Decision";
    exports2.DECISION_DEF_SYS_ID = "226ed8f6c7ad5110bfbaf89f51c260b1";
    exports2.DECISION_BRANCHES_VAR_SYS_ID = "074e5876c7ad5110bfbaf89f51c26087";
    exports2.INPUT_SYS_ID_TO_FLUENT = {
      "6c9c4ee31b630010affd0e55cc4bcbbf": {
        name: "message",
        type: "string"
      },
      c23322e91bbb001094630f28cc4bcbae: {
        name: "wait",
        type: "string"
      },
      "3499c6631b630010affd0e55cc4bcb90": {
        name: "assigned_to",
        type: "string"
      },
      "4f9fdae51bbb001094630f28cc4bcb66": {
        name: "assignment_group",
        type: "string"
      },
      "024026291bbb001094630f28cc4bcb03": {
        name: "wait",
        type: "string"
      },
      "8d839b4b1b805010affd0e55cc4bcb93": {
        name: "assigned_to",
        type: "string"
      },
      "05839b4b1b805010affd0e55cc4bcb8a": {
        name: "assignment_group",
        type: "string"
      },
      "26e45b8b1b805010affd0e55cc4bcb4e": {
        name: "data_definition",
        type: "string"
      },
      c9839b4b1b805010affd0e55cc4bcbb2: {
        name: "wait",
        type: "string"
      },
      e7d270ccc3c632002841b63b12d3ae8b: {
        name: "values",
        type: "template_value"
      },
      e3d270ccc3c632002841b63b12d3ae98: {
        name: "record",
        type: "string"
      },
      "1fd27c8cc3c632002841b63b12d3aeb9": {
        name: "table_name",
        type: "string"
      },
      "8b52fc8cc3c632002841b63b12d3ae36": {
        name: "values",
        type: "template_value"
      },
      "0352fc8cc3c632002841b63b12d3ae12": {
        name: "table_name",
        type: "string"
      },
      "0f4e18f6c7ad5110bfbaf89f51c260f0": {
        name: "type",
        type: "string"
      },
      "074e5876c7ad5110bfbaf89f51c26087": {
        name: "branches",
        type: "string"
      },
      e11d767eff50311029c46b6d253bf192: {
        name: "table",
        type: "string"
      },
      "611d767eff50311029c46b6d253bf199": {
        name: "record",
        type: "string"
      },
      "651d767eff50311029c46b6d253bf19c": {
        name: "completion_condition",
        type: "string"
      },
      "711d7a7eff50311029c46b6d253bf1cd": {
        name: "assignment_group",
        type: "string"
      },
      b51d7a7eff50311029c46b6d253bf1d1: {
        name: "assigned_to",
        type: "string"
      },
      a1e8dea4db9f0010efc65404ce9619f8: {
        name: "table",
        type: "string"
      },
      "79e81ae4db9f0010efc65404ce96192a": {
        name: "form_view",
        type: "string"
      },
      "7b04659dff412010748653bd6338f1b0": {
        name: "template_fields",
        type: "string"
      },
      df09be471b405010affd0e55cc4bcb45: {
        name: "assignment_group",
        type: "string"
      },
      "5b09be471b405010affd0e55cc4bcb4f": {
        name: "assigned_to",
        type: "string"
      },
      "9309be471b405010affd0e55cc4bcb58": {
        name: "to",
        type: "glide_list"
      },
      d54898f4532220109f15ddeeff7b1276: {
        name: "to_email_address",
        type: "string"
      },
      eb09be471b405010affd0e55cc4bcb60: {
        name: "cc",
        type: "glide_list"
      },
      dd4898f4532220109f15ddeeff7b12aa: {
        name: "cc_email_address",
        type: "string"
      },
      "2709be471b405010affd0e55cc4bcb69": {
        name: "bcc",
        type: "glide_list"
      },
      "5d4898f4532220109f15ddeeff7b12b7": {
        name: "bcc_email_address",
        type: "string"
      },
      "6f09be471b405010affd0e55cc4bcb71": {
        name: "subject",
        type: "string"
      },
      a709be471b405010affd0e55cc4bcb77: {
        name: "body",
        type: "string"
      },
      e8e44799536220109f15ddeeff7b1250: {
        name: "table",
        type: "string"
      },
      ef09be471b405010affd0e55cc4bcb7c: {
        name: "target_record",
        type: "string"
      },
      "2b09be471b405010affd0e55cc4bcb85": {
        name: "wait",
        type: "string"
      },
      bf8226e31ba30010affd0e55cc4bcb5e: {
        name: "to",
        type: "glide_list"
      },
      a8e081cd53e220109f15ddeeff7b1293: {
        name: "to_email_address",
        type: "string"
      },
      c49226e31ba30010affd0e55cc4bcb83: {
        name: "cc",
        type: "glide_list"
      },
      e8e081cd53e220109f15ddeeff7b12a6: {
        name: "cc_email_address",
        type: "string"
      },
      "049226e31ba30010affd0e55cc4bcb8b": {
        name: "bcc",
        type: "glide_list"
      },
      a8e081cd53e220109f15ddeeff7b12b3: {
        name: "bcc_email_address",
        type: "string"
      },
      "089226e31ba30010affd0e55cc4bcb9f": {
        name: "subject",
        type: "string"
      },
      c89226e31ba30010affd0e55cc4bcba4: {
        name: "body",
        type: "string"
      },
      "8c9226e31ba30010affd0e55cc4bcba9": {
        name: "target_record",
        type: "string"
      },
      cef40b99536220109f15ddeeff7b12a3: {
        name: "table",
        type: "string"
      },
      f8b3aae91bbb001094630f28cc4bcb2b: {
        name: "assignment_group",
        type: "string"
      },
      ffcd02671b630010affd0e55cc4bcbdf: {
        name: "assigned_to",
        type: "string"
      },
      a73fce671b630010affd0e55cc4bcbbb: {
        name: "initial_message",
        type: "string"
      },
      "233fce671b630010affd0e55cc4bcbcd": {
        name: "completed_message",
        type: "string"
      },
      e33fce671b630010affd0e55cc4bcbd2: {
        name: "skipped_message",
        type: "string"
      },
      "65e9b08e7f795210086a9b627c8665ee": {
        name: "checklist_owner",
        type: "string"
      },
      c213d6667fb99210086a9b627c8665ac: {
        name: "checklist_items",
        type: "string"
      },
      "42fd14f47f711210086a9b627c866508": {
        name: "checklist_name",
        type: "string"
      },
      e00efc3b7fa15210d9ed9b627c8665eb: {
        name: "task",
        type: "string"
      },
      "280efc3b7fa15210d9ed9b627c8665ee": {
        name: "show_sla",
        type: "boolean"
      },
      "2c0efc3b7fa15210d9ed9b627c8665f2": {
        name: "can_skip",
        type: "boolean"
      },
      a80e307b7fa15210d9ed9b627c86650f: {
        name: "assignment_group",
        type: "string"
      },
      bc0e307b7fa15210d9ed9b627c866512: {
        name: "assigned_to",
        type: "string"
      },
      d54f6c471b97cd10db4bba63cc4bcb0f: {
        name: "assigned_to",
        type: "string"
      },
      "594f6c471b97cd10db4bba63cc4bcb18": {
        name: "assigned_to_comments",
        type: "string"
      },
      "514f6c471b97cd10db4bba63cc4bcb28": {
        name: "assigned_to_due_date",
        type: "string"
      },
      a14f6c471b97cd10db4bba63cc4bcb2b: {
        name: "table",
        type: "string"
      },
      e14f6c471b97cd10db4bba63cc4bcb2e: {
        name: "record",
        type: "string"
      },
      "1ec899171b934150db4bba63cc4bcb3a": {
        name: "table",
        type: "string"
      },
      "5ac899171b934150db4bba63cc4bcb2b": {
        name: "approver",
        type: "glide_list"
      },
      a86e10711bbbc1d0ca592f092a4bcb91: {
        name: "approver_group",
        type: "string"
      },
      c3aeb10a1bf749d0db4bba63cc4bcbc0: {
        name: "record",
        type: "string"
      },
      b0258702a37201106e1d0ace26fcda16: {
        name: "table",
        type: "string"
      },
      bc6abcd9a39b011038310ace26fcda9d: {
        name: "variable",
        type: "string"
      },
      "78258702a37201106e1d0ace26fcda21": {
        name: "approver",
        type: "string"
      },
      "7c258702a37201106e1d0ace26fcda29": {
        name: "due_date",
        type: "string"
      },
      "7c258702a37201106e1d0ace26fcda2e": {
        name: "comments",
        type: "string"
      },
      "197e16a51bbb001094630f28cc4bcb4e": {
        name: "assignment_group",
        type: "string"
      },
      fd0756eb1b630010affd0e55cc4bcbbd: {
        name: "assigned_to",
        type: "string"
      },
      "310756eb1b630010affd0e55cc4bcbc5": {
        name: "title",
        type: "string"
      },
      "1087daeb1b630010affd0e55cc4bcbee": {
        name: "knowledge_article",
        type: "string"
      },
      c3e49aad1b7b001094630f28cc4bcbd1: {
        name: "wait",
        type: "string"
      }
    };
    exports2.EXPERIENCE_PROPERTY_SYS_ID_TO_FLUENT = {
      eb0474b8c763001085f5532bb4c260f5: {
        name: "associated_record",
        type: "string"
      },
      "78f334b8c763001085f5532bb4c26009": {
        name: "associated_table",
        type: "string"
      },
      "267e68875b00001070e4492c11f91a34": {
        name: "description",
        type: "string"
      },
      "55628762c3211010381b60bc0eba8fec": {
        name: "experience_status_record",
        type: "string"
      },
      "9c6b7e22c3211010381b60bc0eba8f98": {
        name: "experience_status_table",
        type: "string"
      },
      "8793d6feb70100101a6ddb91de11a98f": {
        name: "footer",
        type: "string"
      },
      "24bdf44673030010fe4a6da44cf6a707": {
        name: "icon",
        type: "string"
      },
      ec5d96eac7e60010bfbaf89f51c2605d: {
        name: "is_automated",
        type: "boolean"
      },
      "661d304673030010fe4a6da44cf6a7b1": {
        name: "tagline",
        type: "string"
      },
      "49ed24875b00001070e4492c11f91aa5": {
        name: "title",
        type: "string"
      },
      "8c1ef8e40f100010f5bf5ae9c4767ed6": {
        name: "associated_record",
        type: "string"
      },
      "77edf4e40f100010f5bf5ae9c4767edb": {
        name: "associated_table",
        type: "string"
      },
      "4f0e5d7e736100104a905ee515f6a70b": {
        name: "attachment_source",
        type: "string"
      },
      "576de7780f1300102920c912d4767e52": {
        name: "attachments_read_only",
        type: "boolean"
      },
      "1d27b10ac7200010bfbaf89f51c26016": {
        name: "description",
        type: "string"
      },
      "5e5272b2c3211010381b60bc0eba8f13": {
        name: "experience_status_record",
        type: "string"
      },
      "4612be72c3211010381b60bc0eba8f47": {
        name: "experience_status_table",
        type: "string"
      },
      "71ae56b6b74100101a6ddb91de11a9a1": {
        name: "footer",
        type: "string"
      },
      fbc1f11b5b40001070e4492c11f91a45: {
        name: "form_fields",
        type: "field_list"
      },
      "7cb449e85b41001070e4492c11f91a06": {
        name: "form_view",
        type: "string"
      },
      ad4db04673030010fe4a6da44cf6a798: {
        name: "icon",
        type: "string"
      },
      "63fd1eeac7e60010bfbaf89f51c26059": {
        name: "is_automated",
        type: "boolean"
      },
      "6688ff5d0700101070e493d0fad300f6": {
        name: "pending_description",
        type: "string"
      },
      e9ae23190700101070e493d0fad300c1: {
        name: "pending_title",
        type: "string"
      },
      "05bee8875b00001070e4492c11f91afb": {
        name: "record_fields",
        type: "field_list"
      },
      d803775a5b01001031a67d6f7281c7aa: {
        name: "show_checklist",
        type: "boolean"
      },
      "54c49bc60f3200102920c912d4767ee2": {
        name: "show_sla",
        type: "boolean"
      },
      "9a0c340673030010fe4a6da44cf6a7de": {
        name: "tagline",
        type: "string"
      },
      "13597d8ac7200010bfbaf89f51c2609e": {
        name: "title",
        type: "string"
      },
      d18061f287d300102252c88fc0e3eccb: {
        name: "associated_record",
        type: "string"
      },
      "5fa0e1f287d300102252c88fc0e3ec66": {
        name: "associated_table",
        type: "string"
      },
      ca4591f25b21001070e4492c11f91a01: {
        name: "columns",
        type: "field_list"
      },
      "47ceb192777220102a272f589a10616d": {
        name: "description",
        type: "string"
      },
      "9d2ff6a7c3611010381b60bc0eba8f3c": {
        name: "experience_status_record",
        type: "string"
      },
      "8cfe36a7c3611010381b60bc0eba8fd2": {
        name: "experience_status_table",
        type: "string"
      },
      "3f9d744673030010fe4a6da44cf6a7b9": {
        name: "icon",
        type: "string"
      },
      "091e9aeac7e60010bfbaf89f51c260ea": {
        name: "is_automated",
        type: "boolean"
      },
      "6f2f7ecf5b21001070e4492c11f91a77": {
        name: "list_query",
        type: "string"
      },
      "12e9e761870300102252c88fc0e3ecb2": {
        name: "list_title",
        type: "string"
      },
      d6d1f7585b92001022527d6f7281c7cc: {
        name: "max_columns",
        type: "string"
      },
      bc00dc415ba2001022527d6f7281c7c5: {
        name: "record_fields",
        type: "field_list"
      },
      "66c5b3885b16001022527d6f7281c77c": {
        name: "row_count",
        type: "string"
      },
      "29255db25b21001070e4492c11f91ac5": {
        name: "table",
        type: "string"
      },
      "01fcbc0673030010fe4a6da44cf6a7d6": {
        name: "tagline",
        type: "string"
      },
      "1d8b12fe5b61001070e4492c11f91a19": {
        name: "title",
        type: "string"
      },
      "320f94505b92001022527d6f7281c7e1": {
        name: "ui_view",
        type: "string"
      },
      "3b81ced9b77500101a6ddb91de11a9b1": {
        name: "associated_table",
        type: "string"
      },
      f8a48a5db77500101a6ddb91de11a950: {
        name: "associated_record",
        type: "string"
      },
      "58017272c3211010381b60bc0eba8fd1": {
        name: "experience_status_table",
        type: "string"
      },
      c641b672c3211010381b60bc0eba8f6c: {
        name: "experience_status_record",
        type: "string"
      },
      "7e14425db77500101a6ddb91de11a938": {
        name: "knowledge_table",
        type: "string"
      },
      "33bef999b77500101a6ddb91de11a925": {
        name: "knowledge_record",
        type: "string"
      },
      "9d3bd5bac7120010bfbaf89f51c260bb": {
        name: "title",
        type: "string"
      },
      "307e92b6b74100101a6ddb91de11a9b4": {
        name: "footer",
        type: "string"
      },
      "98cd9aeac7e60010bfbaf89f51c2602e": {
        name: "is_automated",
        type: "boolean"
      },
      "6c6499fbff001010393e53bd6338f1c8": {
        name: "associated_table",
        type: "string"
      },
      "346499fbff001010393e53bd6338f1eb": {
        name: "associated_record",
        type: "string"
      },
      "737ebe67c3611010381b60bc0eba8ff9": {
        name: "experience_status_table",
        type: "string"
      },
      "25be72a7c3611010381b60bc0eba8f71": {
        name: "experience_status_record",
        type: "string"
      },
      "786499fbff001010393e53bd6338f1fc": {
        name: "tagline",
        type: "string"
      },
      fc64d9fbff001010393e53bd6338f10d: {
        name: "icon",
        type: "string"
      },
      "986499fbff001010393e53bd6338f13d": {
        name: "title",
        type: "string"
      },
      "546499fbff001010393e53bd6338f14e": {
        name: "description",
        type: "string"
      },
      e86499fbff001010393e53bd6338f1ab: {
        name: "pending_description",
        type: "string"
      },
      "7864d9fbff001010393e53bd6338f138": {
        name: "pending_title",
        type: "string"
      },
      "906459fbff001010393e53bd6338f1ce": {
        name: "record_fields",
        type: "field_list"
      },
      e86499fbff001010393e53bd6338f1b7: {
        name: "footer",
        type: "string"
      },
      "306499fbff001010393e53bd6338f1da": {
        name: "form_view",
        type: "string"
      },
      "0964d9fbff001010393e53bd6338f151": {
        name: "form_fields",
        type: "field_list"
      },
      "07a68b3dff812010748653bd6338f153": {
        name: "template_fields",
        type: "string"
      },
      "586499fbff001010393e53bd6338f16b": {
        name: "attachment_source",
        type: "string"
      },
      "246499fbff001010393e53bd6338f188": {
        name: "attachments_read_only",
        type: "boolean"
      },
      a46499fbff001010393e53bd6338f177: {
        name: "show_sla",
        type: "boolean"
      },
      fc64d9fbff001010393e53bd6338f11f: {
        name: "show_checklist",
        type: "boolean"
      },
      ac6499fbff001010393e53bd6338f199: {
        name: "is_automated",
        type: "boolean"
      }
    };
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/playbook-shapes.js
var require_playbook_shapes = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/playbook-shapes.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.TriggerInstanceShape = exports2.ActivityShape = exports2.LaneShape = void 0;
    exports2.getFactoryCallConfig = getFactoryCallConfig;
    exports2.containsDataPill = containsDataPill;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    function getFactoryCallConfig(shape) {
      const resolved = shape.isResolvable() ? shape.asResolvable().resolve() : shape;
      const directCE = resolved.if(sdk_build_core_1.CallExpressionShape);
      if (directCE) {
        return directCE.getArgument(0)?.ifObject();
      }
      if (resolved instanceof sdk_build_core_1.Record) {
        const source = resolved.getSource();
        if (source instanceof sdk_build_core_1.CallExpressionShape) {
          return source.getArgument(0)?.ifObject();
        }
      }
      return void 0;
    }
    __name(getFactoryCallConfig, "getFactoryCallConfig");
    function containsDataPill(shape) {
      if (shape instanceof sdk_build_core_1.CallExpressionShape && shape.getCallee() === playbook_plugin_constants_1.DATAPILL_CALLEE) {
        return true;
      }
      if (shape instanceof sdk_build_core_1.TemplateExpressionShape) {
        return shape.getSpans().some((span) => {
          const expr = span.getExpression();
          return expr instanceof sdk_build_core_1.CallExpressionShape && expr.getCallee() === playbook_plugin_constants_1.DATAPILL_CALLEE;
        });
      }
      return false;
    }
    __name(containsDataPill, "containsDataPill");
    var LaneShape = class extends sdk_build_core_1.CallExpressionShape {
      static {
        __name(this, "LaneShape");
      }
      config;
      activities;
      nowIdKeyToGuid;
      playbookBodyParamsName;
      constructor({ source, config, activities, nowIdKeyToGuid, playbookBodyParamsName }) {
        super({ source, callee: "LaneShape", args: [config] });
        this.config = config;
        this.activities = activities;
        this.nowIdKeyToGuid = nowIdKeyToGuid;
        this.playbookBodyParamsName = playbookBodyParamsName;
      }
      getConfig() {
        return this.config;
      }
      getActivities() {
        return this.activities;
      }
      getNowIdKeyToGuid() {
        return this.nowIdKeyToGuid;
      }
      getPlaybookBodyParamsName() {
        return this.playbookBodyParamsName;
      }
      getSysId() {
        return this.config.get("$id")?.as(sdk_build_plugins_1.NowIdShape);
      }
    };
    exports2.LaneShape = LaneShape;
    var ActivityShape = class extends sdk_build_core_1.CallExpressionShape {
      static {
        __name(this, "ActivityShape");
      }
      activityDef;
      config;
      inputs;
      experienceProperties;
      nowIdKeyToGuid;
      playbookBodyParamsNames;
      constructor({ source, activityDefinition, config, inputs, experienceProperties, nowIdKeyToGuid, playbookBodyParamsNames }) {
        super({ source, callee: "ActivityShape", args: [config] });
        this.activityDef = activityDefinition;
        this.config = config;
        this.inputs = inputs;
        this.experienceProperties = experienceProperties;
        this.nowIdKeyToGuid = nowIdKeyToGuid;
        this.playbookBodyParamsNames = playbookBodyParamsNames ?? [];
      }
      /** The activity definition shape (e.g. PropertyAccessShape for ActivityDefinitions.Core.RecordForm) */
      getActivityDefinition() {
        return this.activityDef;
      }
      /** The config object: { $id, label, order, startRule, ... } */
      getConfig() {
        return this.config;
      }
      getInputs() {
        return this.inputs;
      }
      getExperienceProperties() {
        return this.experienceProperties;
      }
      getNowIdKeyToGuid() {
        return this.nowIdKeyToGuid;
      }
      getPlaybookBodyParamsNames() {
        return this.playbookBodyParamsNames;
      }
      getSysId() {
        return this.config.get("$id")?.as(sdk_build_plugins_1.NowIdShape);
      }
    };
    exports2.ActivityShape = ActivityShape;
    var TriggerInstanceShape = class extends sdk_build_core_1.CallExpressionShape {
      static {
        __name(this, "TriggerInstanceShape");
      }
      triggerType;
      config;
      triggerInputs;
      parentRecordInputSysId;
      mapperMappings;
      playbookInputSysIds;
      isSameTableAsParent;
      constructor({ source, triggerType, config, triggerInputs, parentRecordInputSysId, mapperMappings, playbookInputSysIds, isSameTableAsParent }) {
        super({ source, callee: "TriggerInstanceShape", args: [triggerType, config, triggerInputs] });
        this.triggerType = triggerType;
        this.config = config;
        this.triggerInputs = triggerInputs;
        this.parentRecordInputSysId = parentRecordInputSysId;
        this.mapperMappings = mapperMappings;
        this.playbookInputSysIds = playbookInputSysIds;
        this.isSameTableAsParent = isSameTableAsParent ?? false;
      }
      getTriggerType() {
        return this.triggerType;
      }
      getConfig() {
        return this.config;
      }
      getTriggerInputs() {
        return this.triggerInputs;
      }
      /**
       * Sys_id of the sys_pd_process_input glidevar for `parentRecord`, when the
       * parent playbook has `parentTable` set. Populated by the definition plugin
       * and consumed by the trigger instance plugin to emit the linking
       * sys_variable_value record. Internal wiring — not a user-visible argument.
       */
      getParentRecordInputSysId() {
        return this.parentRecordInputSysId;
      }
      /**
       * Mapper values keyed by playbook input element name, parsed from the
       * trigger's playbookInputs mapper callback. Each value contains the
       * serialized string and an isPill flag (true = sys_element_mapping,
       * false = sys_variable_value). Populated by the trigger-array processing
       * transform and consumed by the trigger instance plugin.
       */
      getMapperMappings() {
        return this.mapperMappings;
      }
      /**
       * Map of playbook input element names to their sys_pd_process_input sys_ids.
       * Needed so the trigger instance plugin can create sys_variable_value records
       * for hardcoded mapper values.
       */
      getPlaybookInputSysIds() {
        return this.playbookInputSysIds;
      }
      /**
       * True when the playbook has parentTable set and this trigger fires on that
       * same table. Used by the trigger instance plugin to decide whether to emit
       * the default {{triggerRecord}} sys_element_mapping for parent_record.
       */
      getIsSameTableAsParent() {
        return this.isSameTableAsParent;
      }
      getSysId() {
        return this.config.get("$id")?.as(sdk_build_plugins_1.NowIdShape);
      }
    };
    exports2.TriggerInstanceShape = TriggerInstanceShape;
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/table-field-resolver.js
var require_table_field_resolver = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/table-field-resolver.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.resolveTableFieldNames = resolveTableFieldNames;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var tableFieldNamesCache = /* @__PURE__ */ new Map();
    function resolveTableFieldNames(compiler, rootDir, tableName) {
      if (tableFieldNamesCache.has(tableName)) {
        return tableFieldNamesCache.get(tableName);
      }
      const tempFilePath = `${rootDir}/src/$$TEMP_FIELD_NAMES_RESOLVER$$.ts`;
      const typeDeclaration = `type T = import('@servicenow/sdk-core/db').Data<'${tableName}'>`;
      const tempSource = compiler.createSourceFile(tempFilePath, typeDeclaration, {
        overwrite: true,
        scriptKind: sdk_build_core_1.ts.ScriptKind.TS
      });
      try {
        const typeAlias = tempSource.getTypeAlias("T");
        if (!typeAlias) {
          return void 0;
        }
        const resolvedType = typeAlias.getType();
        const properties = resolvedType.getProperties();
        if (properties.length === 0) {
          return void 0;
        }
        const fieldNames = new Set(properties.map((p) => p.getName()));
        tableFieldNamesCache.set(tableName, fieldNames);
        return fieldNames;
      } finally {
        compiler.removeSourceFile(tempSource);
      }
    }
    __name(resolveTableFieldNames, "resolveTableFieldNames");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/dependent-field-validator.js
var require_dependent_field_validator = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/dependent-field-validator.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.validateDependentFields = validateDependentFields;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var table_field_resolver_1 = require_table_field_resolver();
    var FIELD_DEPENDENT_COLUMN_TYPES = /* @__PURE__ */ new Set(["FieldListColumn", "TemplateValueColumn"]);
    function validateDependentFields(columnDefinitions, userValues, compiler, rootDir, diagnostics) {
      for (const propName of userValues.keys()) {
        const columnShape = columnDefinitions.get(propName, false);
        if (!columnShape) {
          continue;
        }
        const callExpr = columnShape.if(sdk_build_core_1.CallExpressionShape);
        if (!callExpr) {
          continue;
        }
        const callee = callExpr.getCallee();
        if (!FIELD_DEPENDENT_COLUMN_TYPES.has(callee)) {
          continue;
        }
        const columnConfig = callExpr.getArgument(0)?.ifObject();
        if (!columnConfig) {
          continue;
        }
        const dependentFieldName = columnConfig.get("dependent", false)?.ifString()?.getValue();
        if (!dependentFieldName) {
          continue;
        }
        const tableNameShape = userValues.get(dependentFieldName);
        if (!tableNameShape) {
          continue;
        }
        const tableName = tableNameShape.ifString()?.getValue();
        if (!tableName) {
          continue;
        }
        const validFields = (0, table_field_resolver_1.resolveTableFieldNames)(compiler, rootDir, tableName);
        if (!validFields) {
          continue;
        }
        const valueShape = userValues.get(propName);
        if (isDataPill(valueShape)) {
          continue;
        }
        if (callee === "FieldListColumn") {
          validateFieldListValues(valueShape, validFields, propName, tableName, diagnostics);
        } else if (callee === "TemplateValueColumn") {
          validateTemplateValueKeys(valueShape, validFields, propName, tableName, diagnostics);
        }
      }
    }
    __name(validateDependentFields, "validateDependentFields");
    function validateFieldListValues(valueShape, validFields, propName, tableName, diagnostics) {
      const arrayShape = valueShape.ifArray() ?? valueShape.if(sdk_build_core_1.CallExpressionShape)?.getArgument(0)?.ifArray();
      if (!arrayShape) {
        return;
      }
      for (const element of arrayShape.getElements()) {
        const fieldName = element.ifString()?.getValue();
        if (!fieldName) {
          continue;
        }
        if (!validFields.has(fieldName)) {
          diagnostics.error(element, `'${propName}': field '${fieldName}' does not exist on table '${tableName}'.`);
        }
      }
    }
    __name(validateFieldListValues, "validateFieldListValues");
    function validateTemplateValueKeys(valueShape, validFields, propName, tableName, diagnostics) {
      const fieldNames = extractFieldNamesFromString(valueShape) ?? // TemplateValue wrapper: TemplateValue({ field: value, ... })
      extractFieldNamesFromTemplateValue(valueShape);
      if (!fieldNames) {
        return;
      }
      for (const fieldName of fieldNames) {
        if (!validFields.has(fieldName)) {
          diagnostics.error(valueShape, `'${propName}': field '${fieldName}' does not exist on table '${tableName}'.`);
        }
      }
    }
    __name(validateTemplateValueKeys, "validateTemplateValueKeys");
    function extractFieldNamesFromString(valueShape) {
      const strValue = valueShape.ifString()?.getValue();
      if (!strValue) {
        return void 0;
      }
      const fieldNames = [];
      for (const pair of strValue.split("^")) {
        const eqIndex = pair.indexOf("=");
        if (eqIndex > 0) {
          fieldNames.push(pair.substring(0, eqIndex));
        }
      }
      return fieldNames.length > 0 ? fieldNames : void 0;
    }
    __name(extractFieldNamesFromString, "extractFieldNamesFromString");
    function extractFieldNamesFromTemplateValue(valueShape) {
      if (!(valueShape instanceof sdk_build_core_1.TemplateValueShape)) {
        return void 0;
      }
      const keys = valueShape.getTemplateValue().keys();
      return keys.length > 0 ? keys : void 0;
    }
    __name(extractFieldNamesFromTemplateValue, "extractFieldNamesFromTemplateValue");
    function isDataPill(shape) {
      const call = shape.if(sdk_build_core_1.CallExpressionShape);
      return call !== void 0 && call.getCallee() === playbook_plugin_constants_1.DATAPILL_CALLEE;
    }
    __name(isDataPill, "isDataPill");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/label-utils.js
var require_label_utils = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/label-utils.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.HTML_TAG_REGEX = void 0;
    exports2.stripHtmlTags = stripHtmlTags;
    exports2.HTML_TAG_REGEX = /<\/?[^>]+(>|$)/;
    function stripHtmlTags(value) {
      return value.replace(/<\/?[^>]+(>|$)/g, "");
    }
    __name(stripHtmlTags, "stripHtmlTags");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/activity-diagnostics-plugin.js
var require_activity_diagnostics_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/activity-diagnostics-plugin.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.ActivityDiagnosticsPlugin = void 0;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var activity_ootb_maps_1 = require_activity_ootb_maps();
    var playbook_shapes_1 = require_playbook_shapes();
    var dependent_field_validator_1 = require_dependent_field_validator();
    var label_utils_1 = require_label_utils();
    exports2.ActivityDiagnosticsPlugin = sdk_build_core_1.Plugin.create({
      name: "ActivityDiagnosticsPlugin",
      docs: [],
      shapes: [
        {
          shape: playbook_shapes_1.ActivityShape,
          fileTypes: ["fluent"],
          toRecord(activityShape, { diagnostics, compiler, project }) {
            const isDecision = activityShape.getActivityDefinition().getCode() === activity_ootb_maps_1.DECISION_ACTIVITY_DEF_KEY;
            const config = activityShape.getConfig();
            if (isDecision) {
              const conditionToRunShape = config.get("conditionToRun");
              if (conditionToRunShape.ifDefined() !== void 0) {
                diagnostics.error(conditionToRunShape, "Decision activities must not define `conditionToRun`.");
              }
            }
            const labelShape = config.get("label")?.ifString();
            const labelValue = labelShape?.getValue();
            if (labelValue !== void 0) {
              const labelSource = labelShape ?? config.get("label") ?? config;
              if (label_utils_1.HTML_TAG_REGEX.test(labelValue)) {
                diagnostics.warn(labelSource, "Activity `label` contains HTML which is not supported. HTML will be stripped from the label.");
              }
              if ((0, label_utils_1.stripHtmlTags)(labelValue).trim() === "") {
                diagnostics.error(labelSource, "Activity `label` cannot be empty or contain only whitespace.");
              }
            }
            const activityDefConfig = (0, playbook_shapes_1.getFactoryCallConfig)(activityShape.getActivityDefinition());
            if (activityDefConfig) {
              const rootDir = project.getRootDir();
              const activityTypeConfig = (0, playbook_shapes_1.getFactoryCallConfig)(activityDefConfig.get("activityType"));
              const backingExperienceProperties = activityTypeConfig?.get("experienceProperties", false)?.ifObject();
              const experienceProperties = activityShape.getExperienceProperties();
              if (backingExperienceProperties && experienceProperties) {
                (0, dependent_field_validator_1.validateDependentFields)(backingExperienceProperties, experienceProperties, compiler, rootDir, diagnostics);
              }
              const flowShape = activityDefConfig.get("flow");
              const actionShape = activityDefConfig.get("action");
              const backingShape = flowShape?.ifDefined() ?? actionShape;
              const backingConfig = (0, playbook_shapes_1.getFactoryCallConfig)(backingShape);
              const backingInputs = backingConfig?.get("inputs", false)?.ifObject();
              const inputs2 = activityShape.getInputs();
              if (backingInputs && inputs2) {
                (0, dependent_field_validator_1.validateDependentFields)(backingInputs, inputs2, compiler, rootDir, diagnostics);
              }
            }
            if (!isDecision) {
              return { success: false };
            }
            const inputs = activityShape.getInputs();
            const branchesRaw = inputs?.get("branches");
            if (!branchesRaw || branchesRaw.isUndefined()) {
              diagnostics.error(activityShape, "Decision activity requires a branches array in inputs");
              return { success: false };
            }
            const branchesShape = branchesRaw.ifArray();
            if (!branchesShape) {
              diagnostics.error(branchesRaw, "Decision branches must be an array");
              return { success: false };
            }
            const branchElements = branchesShape.getElements();
            if (branchElements.length < 2) {
              diagnostics.error(branchesShape, "Decision activity must have at least 2 branches");
              return { success: false };
            }
            const hasNonElseBranch = branchElements.some((elem) => {
              const id = elem.ifObject()?.get("id")?.ifString()?.getValue();
              return id !== "else";
            });
            if (!hasNonElseBranch) {
              diagnostics.error(branchesShape, "Decision activity must have at least one conditional branch (not just else)");
            }
            const seenIds = /* @__PURE__ */ new Set();
            for (const elem of branchElements) {
              const branch = elem.ifObject();
              if (!branch) {
                continue;
              }
              const idShape = branch.get("id")?.ifString();
              const id = idShape?.getValue();
              const isElse = id === "else";
              if (id !== void 0) {
                if (seenIds.has(id)) {
                  diagnostics.error(idShape ?? branch, `Duplicate branch id '${id}': branch ids must be unique`);
                } else {
                  seenIds.add(id);
                }
              }
              if (isElse) {
                const conditionShape = branch.get("condition");
                const hasCondition = conditionShape?.ifString() || conditionShape?.if(sdk_build_core_1.TemplateExpressionShape);
                if (hasCondition) {
                  diagnostics.hint(conditionShape, `Else branch should not have a condition \u2014 it always matches`);
                }
              }
            }
            const lastBranch = branchElements[branchElements.length - 1]?.ifObject();
            const lastBranchIsElse = lastBranch?.get("id")?.ifString()?.getValue() === "else";
            const hasElse = branchElements.some((elem) => elem.ifObject()?.get("id")?.ifString()?.getValue() === "else");
            if (!hasElse) {
              diagnostics.error(branchesShape, "Decision activity must have an else branch as the last element");
            } else if (!lastBranchIsElse) {
              diagnostics.error(branchesShape, "Else branch must be the last branch in the array");
            }
            const nowIdKeyToGuid = activityShape.getNowIdKeyToGuid();
            if (nowIdKeyToGuid) {
              for (const elem of branchElements) {
                const branch = elem.ifObject();
                if (!branch) {
                  continue;
                }
                const conditionShape = branch.get("condition");
                const templateExpr = conditionShape?.if(sdk_build_core_1.TemplateExpressionShape);
                if (!templateExpr) {
                  continue;
                }
                templateExpr.getValue((expression) => {
                  const resolved = expression.isResolvable() ? expression.asResolvable().resolve() : expression;
                  if (resolved.is(sdk_build_plugins_1.NowIdShape)) {
                    const key = String(resolved.as(sdk_build_plugins_1.NowIdShape).getValue());
                    if (!nowIdKeyToGuid.has(key)) {
                      const branchId = branch.get("id")?.ifString()?.getValue() ?? "(unknown)";
                      diagnostics.error(conditionShape, `Branch '${branchId}' condition references '${key}' which is not a prior activity. Conditions can only reference activities defined before this Decision.`);
                    }
                  }
                  return "";
                });
              }
            }
            return { success: false };
          }
        }
      ]
    });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/utils/slugify.js
var require_slugify = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/utils/slugify.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.slugifyString = slugifyString;
    function slugifyString(value, opts = {}) {
      const { prefixIfDigit = "_", fallback = "" } = opts;
      let str = value == null ? "" : String(value);
      str = str.trim().normalize("NFKD").replace(new RegExp("\\p{Diacritic}", "gu"), "");
      str = str.replace(/[^a-zA-Z0-9]+/g, "_");
      str = str.replace(/_+/g, "_").replace(/^_+|_+$/g, "");
      if (/^[0-9]/.test(str)) {
        str = `${prefixIfDigit}${str}`;
      }
      if (str === "") {
        return fallback;
      }
      return str.toLowerCase();
    }
    __name(slugifyString, "slugifyString");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/utils/unique-names.js
var require_unique_names = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/utils/unique-names.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.deduplicateNames = deduplicateNames;
    function deduplicateNames(entries) {
      const used = /* @__PURE__ */ new Set();
      const result = new Array(entries.length);
      const claim = /* @__PURE__ */ __name((base) => {
        let candidate = base;
        let suffix = 1;
        while (used.has(candidate)) {
          candidate = `${base}_${suffix}`;
          suffix++;
        }
        used.add(candidate);
        return candidate;
      }, "claim");
      entries.forEach((entry, i) => {
        if (entry.isExplicit) {
          result[i] = claim(entry.name);
        }
      });
      entries.forEach((entry, i) => {
        if (!entry.isExplicit) {
          result[i] = claim(entry.name);
        }
      });
      return result;
    }
    __name(deduplicateNames, "deduplicateNames");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/start-rule-resolver.js
var require_start_rule_resolver = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/start-rule-resolver.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.RUN_AFTER_CALLEES = exports2.START_RULE_TO_RUN_CALLEE = exports2.RUN_CALLEE_TO_START_RULE = void 0;
    exports2.isRunAfterCallee = isRunAfterCallee;
    exports2.resolveStartRuleToDatabase = resolveStartRuleToDatabase;
    exports2.resolveStartRuleToShape = resolveStartRuleToShape;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    exports2.RUN_CALLEE_TO_START_RULE = {
      "wfa.playbook.run.Immediately": "immediate",
      "wfa.playbook.run.After": "after_items",
      "wfa.playbook.run.Manually": "manual"
    };
    exports2.START_RULE_TO_RUN_CALLEE = {
      immediate: "wfa.playbook.run.Immediately",
      after_items: "wfa.playbook.run.After",
      manual: "wfa.playbook.run.Manually"
    };
    exports2.RUN_AFTER_CALLEES = /* @__PURE__ */ new Set(["wfa.playbook.run.After"]);
    function isRunAfterCallee(callee) {
      return exports2.RUN_AFTER_CALLEES.has(callee);
    }
    __name(isRunAfterCallee, "isRunAfterCallee");
    function resolveStartRuleToDatabase(startRule) {
      if (startRule instanceof sdk_build_core_1.CallExpressionShape) {
        return exports2.RUN_CALLEE_TO_START_RULE[startRule.getCallee()] ?? startRule;
      }
      return startRule;
    }
    __name(resolveStartRuleToDatabase, "resolveStartRuleToDatabase");
    var TABLE_CONFIG = {
      [playbook_plugin_constants_1.LANE_TABLE]: { startsAfterField: "starts_after_lanes", defaultStartRule: playbook_plugin_constants_1.LANE_DEFAULTS.START_RULE },
      [playbook_plugin_constants_1.ACTIVITY_TABLE]: { startsAfterField: "starts_after_activities", defaultStartRule: playbook_plugin_constants_1.ACTIVITY_DEFAULTS.START_RULE }
    };
    function resolveStartRuleToShape(record, database, guidToVarName, branchGuidMap) {
      const config = TABLE_CONFIG[record.getTable()];
      if (!config) {
        return { startRuleShape: record.get("start_rule") };
      }
      const startRuleRaw = record.get("start_rule")?.ifString()?.getValue() ?? "";
      const startRuleValue = exports2.START_RULE_TO_RUN_CALLEE[startRuleRaw] ? startRuleRaw : record.get("start_rule_name")?.ifString()?.getValue() ?? config.defaultStartRule;
      const runCallee = exports2.START_RULE_TO_RUN_CALLEE[startRuleValue];
      const isRunAfter = isRunAfterCallee(runCallee);
      const startRuleShape = isRunAfter ? resolveRunAfterShape(record, database, guidToVarName, branchGuidMap) : runCallee ? new sdk_build_core_1.CallExpressionShape({ source: record, callee: runCallee, args: [] }) : record.get("start_rule");
      return { startRuleShape };
    }
    __name(resolveStartRuleToShape, "resolveStartRuleToShape");
    function resolveRunAfterShape(record, database, guidToVarName, branchGuidMap) {
      const args = [];
      const table = record.getTable();
      const startsAfterBranchesValue = record.get("starts_after_branches")?.ifString()?.getValue() ?? "";
      const branchEntries = startsAfterBranchesValue.split(",").map((g) => g.trim()).filter(Boolean);
      const branchActivityGuids = new Set(branchEntries.map((entry) => {
        const dotIndex = entry.indexOf(".");
        return dotIndex >= 0 ? entry.substring(0, dotIndex) : "";
      }).filter(Boolean));
      const startsAfterLanesValue = record.get("starts_after_lanes")?.ifString()?.getValue() ?? "";
      const laneGuids = startsAfterLanesValue.split(",").map((g) => g.trim()).filter(Boolean);
      for (const guid of laneGuids) {
        const varName = guidToVarName?.get(guid);
        if (varName) {
          args.push(new sdk_build_core_1.IdentifierShape({ source: record, name: varName }));
        } else {
          const depRecord = database.get(table, guid);
          if (depRecord) {
            args.push(sdk_build_plugins_1.NowIdShape.from(depRecord));
          }
        }
      }
      const startsAfterActivitiesValue = record.get("starts_after_activities")?.ifString()?.getValue() ?? "";
      const activityGuids = startsAfterActivitiesValue.split(",").map((g) => g.trim()).filter(Boolean);
      for (const guid of activityGuids) {
        if (branchActivityGuids.has(guid)) {
          continue;
        }
        const varName = guidToVarName?.get(guid);
        if (varName) {
          args.push(new sdk_build_core_1.IdentifierShape({ source: record, name: varName }));
        } else {
          const activityRecord = database.get(playbook_plugin_constants_1.ACTIVITY_TABLE, guid);
          if (activityRecord) {
            args.push(sdk_build_plugins_1.NowIdShape.from(activityRecord));
          }
        }
      }
      for (const entry of branchEntries) {
        const dotIndex = entry.indexOf(".");
        const branchGuid = dotIndex >= 0 ? entry.substring(dotIndex + 1) : entry;
        const branchInfo = branchGuidMap?.get(branchGuid);
        if (branchInfo) {
          args.push(new sdk_build_core_1.PropertyAccessShape({
            source: record,
            elements: [branchInfo.activityVarName, "branches", branchInfo.fluentBranchId]
          }));
        }
      }
      return new sdk_build_core_1.CallExpressionShape({ source: record, callee: exports2.START_RULE_TO_RUN_CALLEE.after_items, args });
    }
    __name(resolveRunAfterShape, "resolveRunAfterShape");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/constants/playbook-trigger-types.js
var require_playbook_trigger_types = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/constants/playbook-trigger-types.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.INPUT_SYS_ID_TO_FLUENT = exports2.TRIGGER_TYPE_SYS_ID_TO_IDENTIFIER = void 0;
    exports2.TRIGGER_TYPE_SYS_ID_TO_IDENTIFIER = {
      ab6951170f1200108c87f4f0ff767e4f: "PlaybookTriggerTypes.RecordCreate",
      b08951170f1200108c87f4f0ff767e54: "PlaybookTriggerTypes.RecordUpdate",
      "789911170f1200108c87f4f0ff767e27": "PlaybookTriggerTypes.RecordCreateOrUpdate",
      d249fae6ff3f12108f25ffffffffff89: "PlaybookTriggerTypes.Scheduled"
    };
    exports2.INPUT_SYS_ID_TO_FLUENT = {
      a8ae38d1ff2200108c8733af793bf15c: {
        name: "table",
        type: "string"
      },
      "2a8a7851ff2200108c8733af793bf131": {
        name: "condition",
        type: "string"
      },
      "38c838790f0010108c87f4f0ff767e5a": {
        name: "runTriggerOnExtendedTables",
        type: "boolean"
      },
      "82be38d1ff2200108c8733af793bf1a7": {
        name: "table",
        type: "string"
      },
      "52ce78d1ff2200108c8733af793bf1b3": {
        name: "condition",
        type: "string"
      },
      ea38f4790f0010108c87f4f0ff767edb: {
        name: "runTrigger",
        type: "string"
      },
      fb1af07d0f0010108c87f4f0ff767e00: {
        name: "runTriggerOnExtendedTables",
        type: "boolean"
      },
      "506eded1a3d312104818f363041e61b1": {
        name: "triggerOnUniqueChange",
        type: "boolean"
      },
      "28eef8d1ff2200108c8733af793bf14a": {
        name: "table",
        type: "string"
      },
      "610f3cd1ff2200108c8733af793bf11d": {
        name: "condition",
        type: "string"
      },
      "0e12b4790f0010108c87f4f0ff767e90": {
        name: "runTrigger",
        type: "string"
      },
      "726a747d0f0010108c87f4f0ff767e4b": {
        name: "runTriggerOnExtendedTables",
        type: "boolean"
      },
      "6fc42b59a35712104818f363041e6179": {
        name: "triggerOnUniqueChange",
        type: "boolean"
      },
      "12d6b834ff5022108f25ffffffffff70": {
        name: "table",
        type: "string"
      },
      b927b074ff5022108f25ffffffffff5c: {
        name: "condition",
        type: "string"
      },
      ddd78be6ff7f12108f25ffffffffffb5: {
        name: "startDateAndTime",
        type: "string"
      },
      "65474fa6ff7f12108f25ffffffffff35": {
        name: "recurrence",
        type: "string"
      },
      "6d4cfa6eff3f12108f25ffffffffff0e": {
        name: "frequency",
        type: "number"
      },
      "963e7622ff7f12108f25ffffffffff2b": {
        name: "daySelection",
        type: "string"
      },
      e21ff662ff7f12108f25ffffffffffc0: {
        name: "daysOfTheWeek",
        type: "string"
      },
      "908efa22ff7f12108f25ffffffffff5c": {
        name: "dayOfMonth",
        type: "number"
      },
      "298fb2a2ff7f12108f25ffffffffff54": {
        name: "relativeWeek",
        type: "number"
      },
      "88fdbeeeff3f12108f25ffffffffff08": {
        name: "relativeWeekday",
        type: "number"
      },
      "70adf6eeff3f12108f25ffffffffffa0": {
        name: "repeatMonth",
        type: "number"
      },
      a64f3e62ff7f12108f25ffffffffffb2: {
        name: "repeat",
        type: "string"
      },
      "2bbe3262ff7f12108f25ffffffffffa8": {
        name: "end",
        type: "string"
      },
      "445d72eeff3f12108f25ffffffffff41": {
        name: "endDateTime",
        type: "string"
      },
      "3b09b4f4ff5022108f25ffffffffff88": {
        name: "limit",
        type: "number"
      },
      "0587c3e6ff7f12108f25ffffffffff06": {
        name: "timeZone",
        type: "string"
      }
    };
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/playbook-pill-transformer.js
var require_playbook_pill_transformer = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/playbook-pill-transformer.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.STANDARD_PILL_REGEX = void 0;
    exports2.transformStandardPills = transformStandardPills;
    exports2.transformConditionPills = transformConditionPills;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    exports2.STANDARD_PILL_REGEX = /\{\{([^}]+)\}\}/g;
    var CONDITION_OPERATORS = [
      // Multi-word operators
      "NOT LIKE",
      "NOT IN",
      // Word operators (longest first)
      "ISNOTEMPTY",
      "VALCHANGES",
      "EMPTYSTRING",
      "STARTSWITH",
      "ENDSWITH",
      "ISEMPTY",
      "ANYTHING",
      "BETWEEN",
      "DYNAMIC",
      "NSAMEAS",
      "SAMEAS",
      "LIKE",
      "IN",
      // Symbol operators (multi-char before single-char to prevent partial matches)
      "!=",
      ">=",
      "<=",
      "=",
      ">",
      "<"
    ];
    var CONDITION_SPLIT_REGEX = new RegExp("(" + ["\\^OR", "\\^NQ", "\\^", ...CONDITION_OPERATORS].join("|") + ")");
    function transformStandardPills(value, source, context) {
      const matches = [...value.matchAll(exports2.STANDARD_PILL_REGEX)];
      if (matches.length === 0) {
        return void 0;
      }
      const parts = buildStandardPillParts(value, matches, context);
      if (!parts.some((p) => p.type === "pill")) {
        return void 0;
      }
      const first = parts[0];
      if (parts.length === 1 && first?.type === "pill") {
        return buildDataPillCall(source, first.ref, first.isTableName);
      }
      return buildTemplateFromParts(parts, source);
    }
    __name(transformStandardPills, "transformStandardPills");
    function buildStandardPillParts(value, matches, context) {
      const parts = [];
      let textStart = 0;
      for (const match of matches) {
        let pillContent = match[1];
        const pillStart = match.index;
        if (pillContent === void 0 || pillStart === void 0) {
          continue;
        }
        const pillEnd = pillStart + match[0].length;
        const isTableName = pillContent.endsWith(playbook_plugin_constants_1.TABLE_NAME_SUFFIX);
        if (isTableName) {
          pillContent = pillContent.slice(0, -playbook_plugin_constants_1.TABLE_NAME_SUFFIX.length);
        }
        const ref = resolveStandardPillContent(pillContent, context);
        if (!ref) {
          continue;
        }
        if (pillStart > textStart) {
          parts.push({ type: "text", text: value.substring(textStart, pillStart) });
        }
        parts.push({ type: "pill", ref, isTableName });
        textStart = pillEnd;
      }
      if (textStart < value.length) {
        parts.push({ type: "text", text: value.substring(textStart) });
      }
      return parts;
    }
    __name(buildStandardPillParts, "buildStandardPillParts");
    function resolveStandardPillContent(content, context) {
      if (content === "pd.state") {
        return "params.state";
      }
      if (content.startsWith("pd.inputs.")) {
        return `params.inputs.${content.substring("pd.inputs.".length)}`;
      }
      if (content === "pd.inputRecord") {
        return "params.parentRecord";
      }
      if (content.startsWith("pd.inputRecord.")) {
        return `params.parentRecord.${content.substring("pd.inputRecord.".length)}`;
      }
      if (content === "triggerRecord") {
        return "trigger.current";
      }
      if (content.startsWith("triggerRecord.")) {
        const path = content.substring("triggerRecord.".length);
        return `trigger.current.${path}`;
      }
      if (content.startsWith("act.")) {
        const rest = content.substring("act.".length);
        const dotIndex = rest.indexOf(".");
        if (dotIndex === -1) {
          return void 0;
        }
        const sysId = rest.substring(0, dotIndex);
        const path = rest.substring(dotIndex + 1);
        if (sysId === context.currentActivitySysId && (path === "label" || path === "description")) {
          return `${playbook_plugin_constants_1.CURRENT_ACTIVITY_CALLEE}.${path}`;
        }
        const effectivePath = path === "outputs.sys_id" ? "sysId" : path;
        return resolveActivityRef(sysId, effectivePath, context);
      }
      if (content.startsWith("vl.")) {
        return content;
      }
      return void 0;
    }
    __name(resolveStandardPillContent, "resolveStandardPillContent");
    function transformConditionPills(value, source, context) {
      const parts = scanForConditionPills(value, context);
      if (!parts.some((p) => p.type === "pill")) {
        return void 0;
      }
      return buildTemplateFromParts(parts, source);
    }
    __name(transformConditionPills, "transformConditionPills");
    function scanForConditionPills(value, context) {
      const segments = value.split(CONDITION_SPLIT_REGEX);
      const parts = [];
      for (let i = 0; i < segments.length; i++) {
        const segment = segments[i];
        if (!segment) {
          continue;
        }
        const isToken = i % 2 === 0;
        const ref = isToken ? resolveConditionToken(segment, context) : void 0;
        if (ref) {
          parts.push({ type: "pill", ref });
        } else {
          const last = parts[parts.length - 1];
          if (last?.type === "text") {
            last.text += segment;
          } else {
            parts.push({ type: "text", text: segment });
          }
        }
      }
      return parts;
    }
    __name(scanForConditionPills, "scanForConditionPills");
    function resolveConditionToken(token, context) {
      if (token.startsWith("inputs.")) {
        return `params.inputs.${token.substring("inputs.".length)}`;
      }
      if (token.startsWith("input_record.")) {
        return `params.parentRecord.${token.substring("input_record.".length)}`;
      }
      if (token.startsWith("activity_")) {
        const rest = token.substring("activity_".length);
        const dotIndex = rest.indexOf(".");
        if (dotIndex === -1) {
          return void 0;
        }
        const sysId = rest.substring(0, dotIndex);
        const path = rest.substring(dotIndex + 1);
        return resolveActivityRef(sysId, path, context);
      }
      return void 0;
    }
    __name(resolveConditionToken, "resolveConditionToken");
    function resolveActivityRef(sysId, path, context) {
      const mapping = context.activityMap.get(sysId);
      if (!mapping) {
        return void 0;
      }
      if (mapping.laneVarName && mapping.laneVarName !== context.currentLaneVarName) {
        return `${mapping.laneVarName}.${mapping.varName}.${path}`;
      }
      return `${mapping.varName}.${path}`;
    }
    __name(resolveActivityRef, "resolveActivityRef");
    function buildTemplateFromParts(parts, source) {
      let headText = "";
      const spans = [];
      for (const part of parts) {
        if (part.type === "text") {
          if (spans.length === 0) {
            headText += part.text;
          } else {
            const prev = spans[spans.length - 1];
            if (prev) {
              spans[spans.length - 1] = new sdk_build_core_1.TemplateSpanShape({
                source,
                expression: prev.getExpression(),
                literalText: prev.getLiteralText() + part.text
              });
            }
          }
        } else {
          spans.push(new sdk_build_core_1.TemplateSpanShape({
            source,
            expression: buildDataPillCall(source, part.ref, part.isTableName),
            literalText: ""
          }));
        }
      }
      return new sdk_build_core_1.TemplateExpressionShape({ source, literalText: headText, spans });
    }
    __name(buildTemplateFromParts, "buildTemplateFromParts");
    function buildDataPillCall(source, ref, isTableName) {
      if (ref.startsWith("vl.")) {
        return new sdk_build_core_1.CallExpressionShape({
          source,
          callee: playbook_plugin_constants_1.DATAPILL_CALLEE,
          args: [new sdk_build_core_1.StringLiteralShape({ source, literalText: `{{${ref}}}` })]
        });
      }
      const base = ref.split(".");
      const elements = isTableName ? [...base, "_tableName"] : base;
      return new sdk_build_core_1.CallExpressionShape({
        source,
        callee: playbook_plugin_constants_1.DATAPILL_CALLEE,
        args: [new sdk_build_core_1.PropertyAccessShape({ source, elements })]
      });
    }
    __name(buildDataPillCall, "buildDataPillCall");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/transform-utils.js
var require_transform_utils = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/transform-utils.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.convertToTemplateValue = convertToTemplateValue;
    exports2.convertValueToType = convertValueToType;
    exports2.hasUnsupportedFeatures = hasUnsupportedFeatures;
    exports2.isPlaybookUnsupported = isPlaybookUnsupported;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var activity_ootb_maps_1 = require_activity_ootb_maps();
    var playbook_trigger_types_1 = require_playbook_trigger_types();
    var playbook_pill_transformer_1 = require_playbook_pill_transformer();
    var start_rule_resolver_1 = require_start_rule_resolver();
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    function convertToBoolean(value) {
      const valueAsString = value.toString().getValue();
      if (valueAsString === "0" || valueAsString === "1") {
        return new sdk_build_core_1.BooleanShape({ source: value, value: valueAsString === "1" });
      }
      return value.toBoolean();
    }
    __name(convertToBoolean, "convertToBoolean");
    function convertToFieldList(value) {
      const valueAsString = value.toString().getValue();
      const elements = valueAsString ? valueAsString.split(",").map((s) => s.trim()).filter(Boolean) : [];
      return new sdk_build_core_1.CallExpressionShape({
        source: value,
        callee: "FieldList",
        args: [new sdk_build_core_1.ArrayShape({ source: value, elements })]
      });
    }
    __name(convertToFieldList, "convertToFieldList");
    function convertToListColumnArray(value) {
      const valueAsString = value.toString().getValue();
      const elements = valueAsString ? valueAsString.split(",").map((s) => s.trim()).filter(Boolean) : [];
      return new sdk_build_core_1.ArrayShape({
        source: value,
        elements
      });
    }
    __name(convertToListColumnArray, "convertToListColumnArray");
    function convertToTemplateValue(value, valueAsString, pillContext) {
      const source = value.getSource();
      const templateString = valueAsString === void 0 ? value.toString() : sdk_build_core_1.Shape.from(source, valueAsString).asString();
      const templateValue = sdk_build_core_1.TemplateValueShape.from(source, templateString);
      const entries = templateValue.getTemplateValue().entries({ resolve: false });
      if (entries.length === 0 && templateString.getValue() !== "") {
        return pillContext ? (0, playbook_pill_transformer_1.transformStandardPills)(templateString.getValue(), source, pillContext) ?? templateString : templateString;
      }
      if (!pillContext) {
        return templateValue;
      }
      const reconstructedValue = {};
      for (const [field, fieldShape] of entries) {
        const transformed = fieldShape.ifString() !== void 0 ? (0, playbook_pill_transformer_1.transformStandardPills)(fieldShape.toString().getValue(), fieldShape, pillContext) : void 0;
        reconstructedValue[field] = transformed ?? fieldShape;
      }
      return new sdk_build_core_1.TemplateValueShape({
        source,
        value: reconstructedValue
      });
    }
    __name(convertToTemplateValue, "convertToTemplateValue");
    function convertValueToType(value, type) {
      switch (type) {
        case "boolean":
          return convertToBoolean(value);
        case "field_list":
          return convertToFieldList(value);
        case "glide_list":
          return convertToListColumnArray(value);
        case "template_value":
          return convertToTemplateValue(value);
        default:
          return value;
      }
    }
    __name(convertValueToType, "convertValueToType");
    function hasUnsupportedFeatures(descendants, record, logger) {
      const playbookId = record.getId().getValue();
      const reasons = [];
      const schemaVersionField = record.get("schema_version");
      if (schemaVersionField.isUndefined()) {
        reasons.push(`Playbook has no schema_version, falling back to Record() api. Playbook = ${playbookId}`);
      } else {
        const schemaVersion = schemaVersionField.toNumber().getValue();
        if (schemaVersion < 3) {
          reasons.push(`Playbook has schema_version ${schemaVersion} (minimum supported is 3), falling back to Record() api. Playbook = ${playbookId}`);
        }
      }
      const executionType = record.get("execution_type")?.ifString()?.getValue() ?? "";
      if (executionType === "on_demand") {
        reasons.push(`Playbook has execution_type 'on_demand' (not yet supported by Fluent), falling back to Record() api. Playbook = ${playbookId}`);
      }
      const processTypeField = record.get("process_type")?.ifString()?.getValue() ?? "";
      if (processTypeField !== "" && processTypeField !== playbook_plugin_constants_1.PLAYBOOK_DEFAULT_PROCESS_TYPE_DB) {
        reasons.push(`Playbook has custom process type (not yet supported by Fluent), falling back to Record() api. Playbook = ${playbookId}`);
      }
      for (const trigger of descendants.query(playbook_plugin_constants_1.TRIGGER_INSTANCE_TABLE)) {
        const typeGuid = trigger.get("trigger_type")?.ifString()?.getValue() ?? "";
        if (typeGuid && !playbook_trigger_types_1.TRIGGER_TYPE_SYS_ID_TO_IDENTIFIER[typeGuid]) {
          reasons.push(`Playbook has unsupported trigger type (sys_id: ${typeGuid}), falling back to Record() api. Playbook = ${playbookId}`);
        }
      }
      for (const timer of descendants.query(playbook_plugin_constants_1.TIMER_ATTRIBUTES_TABLE)) {
        const timerSchedule = timer.get("timer_schedule")?.ifString()?.getValue() ?? "";
        if (timerSchedule !== "") {
          reasons.push(`Playbook has timer with timer_schedule set (not yet supported by Fluent), falling back to Record() api. Playbook = ${playbookId}`);
          break;
        }
      }
      for (const activity of descendants.query(playbook_plugin_constants_1.ACTIVITY_TABLE)) {
        const defSysId = activity.get("activity_definition")?.ifString()?.getValue() ?? "";
        if (defSysId && !activity_ootb_maps_1.ACTIVITY_DEF_SYS_ID_TO_KEY[defSysId]) {
          reasons.push(`Playbook has unrecognized activity definition (sys_id: ${defSysId}), falling back to Record() api. Playbook = ${playbookId}`);
        }
        const startRuleRaw = activity.get("start_rule")?.ifString()?.getValue() ?? "";
        const startRule = start_rule_resolver_1.START_RULE_TO_RUN_CALLEE[startRuleRaw] ? startRuleRaw : activity.get("start_rule_name")?.ifString()?.getValue() ?? "";
        if (startRule === "manual") {
          reasons.push(`Playbook has an optional activity (manual start rule, not yet supported by Fluent), falling back to Record() api. Playbook = ${playbookId}`);
        }
      }
      const permissionField = record.get("permission")?.ifString()?.getValue() ?? "";
      if (permissionField !== "" && permissionField !== "{}") {
        reasons.push(`Playbook has custom permission configuration (not yet supported by Fluent), falling back to Record() api. Playbook = ${playbookId}`);
      }
      for (const lane of descendants.query(playbook_plugin_constants_1.LANE_TABLE)) {
        const lanePermission = lane.get("permission")?.ifString()?.getValue() ?? "";
        if (lanePermission !== "" && lanePermission !== "{}") {
          reasons.push(`Playbook lane has custom permission configuration (not yet supported by Fluent), falling back to Record() api. Playbook = ${playbookId}`);
          break;
        }
      }
      const publicAccessField = record.get("public_access");
      if (!publicAccessField.isUndefined()) {
        const publicAccess = convertToBoolean(publicAccessField).getValue();
        if (publicAccess) {
          reasons.push(`Playbook has public_access enabled (not yet supported by Fluent), falling back to Record() api. Playbook = ${playbookId}`);
        }
      }
      const variantRecords = descendants.query(playbook_plugin_constants_1.VARIANT_TABLE);
      if (variantRecords.length > 0) {
        reasons.push(`Playbook has variants (not yet supported by Fluent), falling back to Record() api. Playbook = ${playbookId}`);
      }
      const evaluateVariantChildrenAfter = record.get("evaluate_variant_children_after")?.ifString()?.getValue() ?? "";
      if (evaluateVariantChildrenAfter !== "") {
        reasons.push(`Playbook has evaluate_variant_children_after set (not yet supported by Fluent), falling back to Record() api. Playbook = ${playbookId}`);
      }
      const launcherStringFields = [
        "launcher_title",
        "launcher_description",
        "launcher_record_form_view",
        "launcher_template_fields",
        "launcher_inputs"
      ];
      const hasLauncherStringValue = launcherStringFields.some((field) => (record.get(field)?.ifString()?.getValue() ?? "") !== "");
      const showRecordFormField = record.get("launcher_show_record_form");
      const showRecordForm = !showRecordFormField.isUndefined() && convertToBoolean(showRecordFormField).getValue();
      if (hasLauncherStringValue || showRecordForm) {
        reasons.push(`Playbook has launcher field(s) set (not yet supported by Fluent), falling back to Record() api. Playbook = ${playbookId}`);
      }
      for (const reason of reasons) {
        logger.warn(reason);
      }
      return reasons.length > 0;
    }
    __name(hasUnsupportedFeatures, "hasUnsupportedFeatures");
    var unsupportedFeaturesCache = /* @__PURE__ */ new WeakMap();
    function isPlaybookUnsupported(record, database, logger, descendants) {
      let playbookSysId = record.get("process_definition")?.ifString()?.getValue();
      let cacheForDb = unsupportedFeaturesCache.get(database);
      if (!cacheForDb) {
        cacheForDb = /* @__PURE__ */ new Map();
        unsupportedFeaturesCache.set(database, cacheForDb);
      }
      if (playbookSysId) {
        if (!cacheForDb.has(playbookSysId)) {
          return true;
        }
        return cacheForDb.get(playbookSysId);
      }
      if (descendants && record && logger) {
        const result = hasUnsupportedFeatures(descendants, record, logger);
        playbookSysId = record.getId().getValue();
        cacheForDb.set(playbookSysId, result);
        return result;
      }
      return true;
    }
    __name(isPlaybookUnsupported, "isPlaybookUnsupported");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/reference-resolver.js
var require_reference_resolver = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/reference-resolver.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.resolveStartsAfterLanes = resolveStartsAfterLanes;
    exports2.resolveStartsAfterActivities = resolveStartsAfterActivities;
    exports2.resolveStartsAfterBranches = resolveStartsAfterBranches;
    exports2.extractIdFromCallExpression = extractIdFromCallExpression;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var start_rule_resolver_1 = require_start_rule_resolver();
    function resolveStartsAfterLanes(config, laneVariables, nowIdKeyToGuid) {
      const startRuleShape = config.get("startRule");
      if (startRuleShape.is(sdk_build_core_1.CallExpressionShape) && (0, start_rule_resolver_1.isRunAfterCallee)(startRuleShape.as(sdk_build_core_1.CallExpressionShape).getCallee())) {
        return resolveRunAfterArgs(startRuleShape.as(sdk_build_core_1.CallExpressionShape), laneVariables, nowIdKeyToGuid);
      }
      return resolveExplicitStartsAfter(config, "startsAfterLanes", nowIdKeyToGuid);
    }
    __name(resolveStartsAfterLanes, "resolveStartsAfterLanes");
    function resolveStartsAfterActivities(config, activityVariables, nowIdKeyToGuid) {
      const startRuleShape = config.get("startRule");
      if (startRuleShape.is(sdk_build_core_1.CallExpressionShape) && (0, start_rule_resolver_1.isRunAfterCallee)(startRuleShape.as(sdk_build_core_1.CallExpressionShape).getCallee())) {
        return resolveRunAfterActivityArgs(startRuleShape.as(sdk_build_core_1.CallExpressionShape), activityVariables, nowIdKeyToGuid);
      }
      return resolveExplicitStartsAfter(config, "startsAfterActivities", nowIdKeyToGuid);
    }
    __name(resolveStartsAfterActivities, "resolveStartsAfterActivities");
    function resolveStartsAfterBranches(config, activityVariables, nowIdKeyToGuid, branchIdMap) {
      const startRuleShape = config.get("startRule");
      if (!startRuleShape.is(sdk_build_core_1.CallExpressionShape) || !(0, start_rule_resolver_1.isRunAfterCallee)(startRuleShape.as(sdk_build_core_1.CallExpressionShape).getCallee())) {
        return void 0;
      }
      const runAfterCall = startRuleShape.as(sdk_build_core_1.CallExpressionShape);
      const runAfterArgs = runAfterCall.getArguments(false);
      const branchGuids = [];
      for (const arg of runAfterArgs) {
        if (!arg.is(sdk_build_core_1.PropertyAccessShape)) {
          continue;
        }
        const propAccess = arg.as(sdk_build_core_1.PropertyAccessShape);
        const elements = propAccess.getElements();
        if (elements.length !== 3 || elements[1].getName() !== "branches") {
          continue;
        }
        const varName = propAccess.getElement(0).getName();
        const branchId = propAccess.getElement(2).getName();
        const activityCallExpr = activityVariables.get(varName);
        if (!activityCallExpr) {
          continue;
        }
        const idShape = extractIdFromCallExpression(activityCallExpr);
        if (!(idShape instanceof sdk_build_plugins_1.NowIdShape)) {
          continue;
        }
        const activitySysId = nowIdKeyToGuid.get(String(idShape.getValue()));
        if (!activitySysId) {
          continue;
        }
        const branchMap = branchIdMap.get(activitySysId);
        if (!branchMap) {
          continue;
        }
        const branchGuid = branchMap.get(branchId);
        if (branchGuid) {
          branchGuids.push(`${activitySysId}.${branchGuid}`);
        }
      }
      return branchGuids.length > 0 ? branchGuids.join(",") : void 0;
    }
    __name(resolveStartsAfterBranches, "resolveStartsAfterBranches");
    function resolveRunAfterArgs(runAfterCall, variables, nowIdKeyToGuid) {
      const runAfterArgs = runAfterCall.getArguments();
      const guids = runAfterArgs.flatMap((arg) => {
        let callExpr;
        if (arg.is(sdk_build_core_1.CallExpressionShape)) {
          callExpr = arg.as(sdk_build_core_1.CallExpressionShape);
        } else if (arg.isIdentifier()) {
          const name = arg.asIdentifier().getName();
          callExpr = variables.get(name);
        }
        if (callExpr) {
          const idShape = extractIdFromCallExpression(callExpr);
          if (idShape instanceof sdk_build_plugins_1.NowIdShape) {
            const guid = nowIdKeyToGuid.get(String(idShape.getValue()));
            return guid ? [guid] : [];
          }
        }
        return [];
      });
      return guids.length > 0 ? guids.join(",") : void 0;
    }
    __name(resolveRunAfterArgs, "resolveRunAfterArgs");
    function resolveRunAfterActivityArgs(runAfterCall, variables, nowIdKeyToGuid) {
      const resolvedArgs = runAfterCall.getArguments();
      const guids = [];
      for (const arg of resolvedArgs) {
        let callExpr;
        if (arg.is(sdk_build_core_1.CallExpressionShape)) {
          callExpr = arg.as(sdk_build_core_1.CallExpressionShape);
        } else if (arg.isIdentifier()) {
          const name = arg.asIdentifier().getName();
          callExpr = variables.get(name);
        }
        if (callExpr) {
          const idShape = extractIdFromCallExpression(callExpr);
          if (idShape instanceof sdk_build_plugins_1.NowIdShape) {
            const guid = nowIdKeyToGuid.get(String(idShape.getValue()));
            if (guid) {
              guids.push(guid);
            }
          }
        }
      }
      const unresolvedArgs = runAfterCall.getArguments(false);
      for (const arg of unresolvedArgs) {
        if (!arg.is(sdk_build_core_1.PropertyAccessShape)) {
          continue;
        }
        const propAccess = arg.as(sdk_build_core_1.PropertyAccessShape);
        const elements = propAccess.getElements();
        if (elements.length !== 3 || elements[1].getName() !== "branches") {
          continue;
        }
        const varName = propAccess.getElement(0).getName();
        const activityCallExpr = variables.get(varName);
        if (!activityCallExpr) {
          continue;
        }
        const idShape = extractIdFromCallExpression(activityCallExpr);
        if (idShape instanceof sdk_build_plugins_1.NowIdShape) {
          const guid = nowIdKeyToGuid.get(String(idShape.getValue()));
          if (guid && !guids.includes(guid)) {
            guids.push(guid);
          }
        }
      }
      return guids.length > 0 ? guids.join(",") : void 0;
    }
    __name(resolveRunAfterActivityArgs, "resolveRunAfterActivityArgs");
    function extractIdFromCallExpression(callExpr) {
      const callee = callExpr.getCallee();
      let argObj;
      if (callee === playbook_plugin_constants_1.LANE_API_NAME || callee === "wfa.playbook.lane") {
        argObj = callExpr.getArgument(0).ifObject();
      } else if (callee === playbook_plugin_constants_1.ACTIVITY_API_NAME || callee === "wfa.playbook.activity") {
        argObj = callExpr.getArgument(1).ifObject();
      }
      if (!argObj) {
        return void 0;
      }
      const configObj = argObj.get("config")?.isUndefined() === false ? argObj.get("config").ifObject() : argObj;
      return configObj?.get("$id");
    }
    __name(extractIdFromCallExpression, "extractIdFromCallExpression");
    function resolveExplicitStartsAfter(config, fieldName, nowIdKeyToGuid) {
      const shape = config.get(fieldName);
      if (shape.isUndefined()) {
        return void 0;
      }
      const arr = shape.ifArray();
      if (!arr) {
        return void 0;
      }
      const guids = arr.getElements().map((el) => {
        if (el instanceof sdk_build_plugins_1.NowIdShape) {
          return nowIdKeyToGuid.get(String(el.getValue()));
        }
        return el.ifString()?.getValue();
      }).filter((g) => !!g);
      return guids.length > 0 ? guids.join(",") : void 0;
    }
    __name(resolveExplicitStartsAfter, "resolveExplicitStartsAfter");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/arrow-body-parser.js
var require_arrow_body_parser = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/arrow-body-parser.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.parseArrowBodyEntries = parseArrowBodyEntries;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    function parseArrowBodyEntries(arrow) {
      const statements = arrow.getStatements();
      const variables = /* @__PURE__ */ new Map();
      let returnObj;
      for (const stmt of statements) {
        if (stmt.is(sdk_build_core_1.VariableStatementShape)) {
          const varStmt = stmt.as(sdk_build_core_1.VariableStatementShape);
          const init = varStmt.getInitializer();
          if (init.is(sdk_build_core_1.CallExpressionShape)) {
            variables.set(varStmt.getVariableName().getName(), init.as(sdk_build_core_1.CallExpressionShape));
          }
        }
      }
      const rv = arrow.getReturnValue();
      if (rv?.isObject()) {
        returnObj = rv;
        if (statements.length === 0) {
          for (const [key, val] of returnObj.asObject().entries()) {
            if (val.is(sdk_build_core_1.CallExpressionShape)) {
              variables.set(key, val.as(sdk_build_core_1.CallExpressionShape));
            }
          }
        }
      } else if (!rv) {
        const lastStmt = statements[statements.length - 1];
        if (lastStmt?.isObject()) {
          returnObj = lastStmt;
          for (const [key, val] of returnObj.asObject().entries()) {
            if (val.is(sdk_build_core_1.CallExpressionShape)) {
              variables.set(key, val.as(sdk_build_core_1.CallExpressionShape));
            }
          }
        }
      }
      return { variables, returnObj };
    }
    __name(parseArrowBodyEntries, "parseArrowBodyEntries");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/playbook-pill-resolver.js
var require_playbook_pill_resolver = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/playbook-pill-resolver.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.DeferredTemplatePillShape = exports2.DeferredPillShape = void 0;
    exports2.resolvePlaybookPill = resolvePlaybookPill;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var reference_resolver_1 = require_reference_resolver();
    var arrow_body_parser_1 = require_arrow_body_parser();
    var tnSuffix = /* @__PURE__ */ __name((isTableName) => isTableName ? playbook_plugin_constants_1.TABLE_NAME_SUFFIX : "", "tnSuffix");
    var DeferredPillShape = class extends sdk_build_core_1.StringShape {
      static {
        __name(this, "DeferredPillShape");
      }
      ref;
      pillPath;
      isCondition;
      isTableName;
      constructor({ source, ref, pillPath, isCondition = false, isTableName = false }) {
        super({ source });
        this.ref = ref;
        this.pillPath = pillPath;
        this.isCondition = isCondition;
        this.isTableName = isTableName;
      }
      getValue() {
        const sysId = this.ref.getValue();
        if (this.isCondition) {
          return `activity_${sysId}.${this.pillPath}`;
        }
        return `{{act.${sysId}.${this.pillPath}${tnSuffix(this.isTableName)}}}`;
      }
    };
    exports2.DeferredPillShape = DeferredPillShape;
    var DeferredTemplatePillShape = class extends sdk_build_core_1.StringShape {
      static {
        __name(this, "DeferredTemplatePillShape");
      }
      parts;
      constructor({ source, parts }) {
        super({ source });
        this.parts = parts;
      }
      getValue() {
        return this.parts.map((p) => typeof p === "string" ? p : p.getValue()).join("");
      }
    };
    exports2.DeferredTemplatePillShape = DeferredTemplatePillShape;
    function isDataPillCall(shape) {
      return shape instanceof sdk_build_core_1.CallExpressionShape && shape.getCallee() === playbook_plugin_constants_1.DATAPILL_CALLEE;
    }
    __name(isDataPillCall, "isDataPillCall");
    function extractPillReference(dataPillCall) {
      return dataPillCall.hasArgument(0) ? dataPillCall.getArgument(0, false) : void 0;
    }
    __name(extractPillReference, "extractPillReference");
    function buildPath(elements, fromIndex) {
      return elements.slice(fromIndex).map((el) => el.getName()).join(".");
    }
    __name(buildPath, "buildPath");
    function resolveParamsPill(elements, format, shape, diagnostics, allowedSources, isTableName) {
      const second = elements[1]?.getName();
      if (second === "inputs") {
        if (!allowedSources.paramsInputs) {
          diagnostics.error(shape, "params.inputs pills are not allowed in this context");
          return void 0;
        }
        const path = buildPath(elements, 2);
        if (!path) {
          diagnostics.error(shape, "params.inputs must be followed by a field path (e.g., params.inputs.varName)");
          return void 0;
        }
        return format === "standard" ? `{{pd.inputs.${path}${tnSuffix(isTableName)}}}` : `inputs.${path}`;
      }
      if (second === "state") {
        if (!allowedSources.paramsState || format === "condition") {
          diagnostics.error(shape, "params.state is not allowed in this context");
          return void 0;
        }
        if (elements.length > 2) {
          diagnostics.error(shape, "params.state is terminal, additional property access is not allowed");
          return void 0;
        }
        return "{{pd.state}}";
      }
      if (second === "parentRecord") {
        if (!allowedSources.paramsParentRecord) {
          diagnostics.error(shape, "params.parentRecord pills are not allowed in this context");
          return void 0;
        }
        const path = buildPath(elements, 2);
        if (path) {
          return format === "standard" ? `{{pd.inputRecord.${path}${tnSuffix(isTableName)}}}` : `input_record.${path}`;
        }
        return format === "standard" ? `{{pd.inputRecord${tnSuffix(isTableName)}}}` : "input_record";
      }
      return void 0;
    }
    __name(resolveParamsPill, "resolveParamsPill");
    function findActivityInLane(laneCallExpr, activityName) {
      const laneDef = laneCallExpr.getArgument(0)?.ifObject();
      const activitiesShape = laneDef?.get("activities");
      if (!activitiesShape?.is(sdk_build_plugins_1.ArrowFunctionShape)) {
        return void 0;
      }
      const { variables } = (0, arrow_body_parser_1.parseArrowBodyEntries)(activitiesShape.as(sdk_build_plugins_1.ArrowFunctionShape));
      const activityCallExpr = variables.get(activityName);
      if (activityCallExpr && playbook_plugin_constants_1.ACTIVITY_CALLEE_NAMES.has(activityCallExpr.getCallee())) {
        return activityCallExpr;
      }
      return void 0;
    }
    __name(findActivityInLane, "findActivityInLane");
    async function resolveActivityPill(refShape, elements, format, factory, diagnostics, allowedSources, isTableName) {
      const root = elements[0];
      if (!root) {
        return void 0;
      }
      let resolved;
      try {
        resolved = root.resolve(true);
      } catch {
        return void 0;
      }
      if (!(resolved instanceof sdk_build_core_1.CallExpressionShape)) {
        return void 0;
      }
      const callee = resolved.getCallee();
      let activityCallExpr;
      let pathStartIndex;
      if (playbook_plugin_constants_1.ACTIVITY_CALLEE_NAMES.has(callee)) {
        activityCallExpr = resolved;
        pathStartIndex = 1;
      } else if (playbook_plugin_constants_1.LANE_CALLEE_NAMES.has(callee)) {
        if (elements.length < 3) {
          diagnostics.error(refShape, "Cross-lane data pill must include an activity name and property path (e.g., lane1.act1.outputs.field)");
          return void 0;
        }
        const activityName = elements[1]?.getName();
        if (!activityName) {
          return void 0;
        }
        activityCallExpr = findActivityInLane(resolved, activityName);
        if (!activityCallExpr) {
          diagnostics.error(refShape, `Cannot find activity "${activityName}" in lane for cross-lane data pill reference`);
          return void 0;
        }
        pathStartIndex = 2;
      } else {
        return void 0;
      }
      const idShape = (0, reference_resolver_1.extractIdFromCallExpression)(activityCallExpr);
      if (!idShape) {
        diagnostics.error(refShape, "Cannot resolve activity $id for data pill reference");
        return void 0;
      }
      const explicitId = String(idShape.getValue());
      const pillPath = buildPath(elements, pathStartIndex);
      if (pillPath.startsWith("sysId.") || pillPath.startsWith("state.")) {
        diagnostics.error(refShape, `Activity ${pillPath} cannot be used as a pill in this context`);
        return void 0;
      }
      if (pillPath === "sysId") {
        if (!allowedSources.previousActivityOutputs) {
          diagnostics.error(refShape, "Activity sysId pills are not allowed in this context");
          return void 0;
        }
        if (format === "condition") {
          diagnostics.error(refShape, "Activity sysId is only available in standard pill format, not in conditions");
          return void 0;
        }
      } else if (pillPath === "state") {
        if (!allowedSources.previousActivityOutputs) {
          diagnostics.error(refShape, "Activity state pills are not allowed in this context");
          return void 0;
        }
      } else if (pillPath.startsWith("outputs")) {
        if (!allowedSources.previousActivityOutputs) {
          diagnostics.error(refShape, "Activity output pills are not allowed in this context");
          return void 0;
        }
      }
      const effectivePillPath = pillPath === "sysId" ? "outputs.sys_id" : pillPath;
      const ref = await factory.createReference({
        source: refShape,
        table: playbook_plugin_constants_1.ACTIVITY_TABLE,
        guid: explicitId
      });
      return new DeferredPillShape({
        source: refShape.getSource(),
        ref,
        pillPath: effectivePillPath,
        isCondition: format === "condition",
        isTableName
      });
    }
    __name(resolveActivityPill, "resolveActivityPill");
    async function resolvePlaybookPill(shape, context) {
      if (isDataPillCall(shape)) {
        return resolveDataPillCall(shape, context);
      }
      if (shape instanceof sdk_build_core_1.TemplateExpressionShape) {
        return resolveTemplatePill(shape, context);
      }
      return shape;
    }
    __name(resolvePlaybookPill, "resolvePlaybookPill");
    async function resolveDataPillCall(call, context) {
      const refShape = extractPillReference(call);
      if (!refShape) {
        context.diagnostics.error(call, "wfa.playbook.dataPill() requires a reference argument");
        return call;
      }
      if (refShape instanceof sdk_build_core_1.PropertyAccessShape) {
        const allElements = refShape.getElements();
        const lastElementName = allElements[allElements.length - 1]?.getName();
        const isTableName = lastElementName === "_tableName";
        const elements = isTableName ? allElements.slice(0, -1) : allElements;
        if (isTableName && context.format === "condition") {
          context.diagnostics.error(call, "Table name pills are not supported in conditions");
          return call;
        }
        const rootName = elements[0]?.getName() ?? "";
        const effectiveCode = elements.map((el) => el.getName()).join(".");
        if (effectiveCode.startsWith(playbook_plugin_constants_1.CURRENT_ACTIVITY_CALLEE + ".")) {
          const property = effectiveCode.slice(playbook_plugin_constants_1.CURRENT_ACTIVITY_CALLEE.length + 1);
          if (property === "label" || property === "description") {
            if (!context.allowedSources.currentActivityLabelDesc) {
              context.diagnostics.error(call, "wfa.playbook.currentActivity can only be used in activity inputs or experience properties");
              return call;
            }
            if (!context.currentActivityId) {
              context.diagnostics.error(call, "wfa.playbook.currentActivity is not available in this context");
              return call;
            }
            return new DeferredPillShape({
              source: call.getSource(),
              ref: context.currentActivityId,
              pillPath: property,
              isCondition: context.format === "condition"
            });
          }
        }
        if (context.triggerParamName && rootName === context.triggerParamName) {
          const second = elements[1]?.getName();
          if (second === "current") {
            if (elements.length === 2) {
              return new sdk_build_core_1.StringLiteralShape({
                source: call.getSource(),
                literalText: `{{triggerRecord${tnSuffix(isTableName)}}}`
              });
            }
            if (elements.length >= 3) {
              const path = elements.slice(2).map((el) => el.getName()).join(".");
              return new sdk_build_core_1.StringLiteralShape({
                source: call.getSource(),
                literalText: `{{triggerRecord.${path}${tnSuffix(isTableName)}}}`
              });
            }
          }
          context.diagnostics.error(call, "Only trigger.current and its fields are supported in trigger data pills. Use trigger.current or trigger.current.field");
          return call;
        }
        if (context.playbookBodyParamsNames.includes(rootName)) {
          const resolved = resolveParamsPill(elements, context.format, call, context.diagnostics, context.allowedSources, isTableName);
          if (resolved !== void 0) {
            return new sdk_build_core_1.StringLiteralShape({ source: call.getSource(), literalText: resolved });
          }
          const secondName = elements[1]?.getName();
          const isKnownParamsProperty = secondName === "inputs" || secondName === "state" || secondName === "parentRecord";
          if (!isKnownParamsProperty) {
            context.diagnostics.error(call, `Unsupported property "${secondName}" on PlaybookBodyParams in data pill`);
          }
          return call;
        }
        const activityPill = await resolveActivityPill(refShape, elements, context.format, context.factory, context.diagnostics, context.allowedSources, isTableName);
        if (activityPill) {
          return activityPill;
        }
        context.diagnostics.error(call, `Cannot resolve data pill reference: "${refShape.getCode()}"`);
        return call;
      }
      if (refShape instanceof sdk_build_core_1.IdentifierShape) {
        context.diagnostics.error(call, `Data pill reference must be a property access (e.g., params.inputs.field), got identifier "${refShape.getName()}"`);
        return call;
      }
      if (refShape instanceof sdk_build_core_1.StringLiteralShape) {
        if (!context.allowedSources.automationPlan) {
          context.diagnostics.error(call, "Literal string data pill can only be used in experience properties");
          return call;
        }
        if (!refShape.getValue().startsWith("{{vl.")) {
          context.diagnostics.error(call, "Literal string data pill must reference an automation plan");
          return call;
        }
        return refShape;
      }
      context.diagnostics.error(call, `Unsupported data pill reference expression (shape: ${refShape?.constructor?.name})`);
      return call;
    }
    __name(resolveDataPillCall, "resolveDataPillCall");
    async function resolveTemplatePill(template, context) {
      const spans = template.getSpans();
      if (spans.length === 0) {
        return template;
      }
      const parts = [];
      let hasDeferred = false;
      const headText = template.getLiteralText();
      if (headText) {
        parts.push(headText);
      }
      for (const span of spans) {
        const resolvedExpr = await resolvePlaybookPill(span.getExpression(), context);
        if (resolvedExpr instanceof DeferredPillShape) {
          parts.push(resolvedExpr);
          hasDeferred = true;
        } else if (resolvedExpr instanceof DeferredTemplatePillShape) {
          parts.push(resolvedExpr);
          hasDeferred = true;
        } else {
          parts.push(resolvedExpr instanceof sdk_build_core_1.StringShape ? resolvedExpr.getValue() : String(resolvedExpr.getValue()));
        }
        const trailingText = span.getLiteralText();
        if (trailingText) {
          parts.push(trailingText);
        }
      }
      if (hasDeferred) {
        return new DeferredTemplatePillShape({ source: template.getSource(), parts });
      }
      const fullString = parts.join("");
      return new sdk_build_core_1.StringLiteralShape({ source: template.getSource(), literalText: fullString });
    }
    __name(resolveTemplatePill, "resolveTemplatePill");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/timer-attributes-transform.js
var require_timer_attributes_transform = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/timer-attributes-transform.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.findTimerRecord = findTimerRecord;
    exports2.timerAttributesToShape = timerAttributesToShape;
    exports2.timerAttributesToRecord = timerAttributesToRecord;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    function formatDuration(duration) {
      const days = duration.days ?? 0;
      const hours = duration.hours ?? 0;
      const minutes = duration.minutes ?? 0;
      const seconds = duration.seconds ?? 0;
      const totalMs = ((days * 24 + hours) * 60 + minutes) * 60 * 1e3 + seconds * 1e3;
      const date = new Date(Date.UTC(1970, 0, 1) + totalMs);
      const Y = date.getUTCFullYear().toString().padStart(4, "0");
      const M = (date.getUTCMonth() + 1).toString().padStart(2, "0");
      const D = date.getUTCDate().toString().padStart(2, "0");
      const h = date.getUTCHours().toString().padStart(2, "0");
      const m = date.getUTCMinutes().toString().padStart(2, "0");
      const s = date.getUTCSeconds().toString().padStart(2, "0");
      return `${Y}-${M}-${D} ${h}:${m}:${s}`;
    }
    __name(formatDuration, "formatDuration");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var playbook_pill_resolver_1 = require_playbook_pill_resolver();
    var playbook_pill_transformer_1 = require_playbook_pill_transformer();
    function findTimerRecord(timerRecords, sourceId, sourceType) {
      return timerRecords.find((timer) => {
        const source = timer.get("source")?.ifString()?.getValue();
        const type = timer.get("source_type")?.ifString()?.getValue();
        const active = timer.get("active")?.ifString()?.getValue();
        const isActive = active === void 0 || active === "" || active === "true";
        return source === sourceId && type === sourceType && isActive;
      });
    }
    __name(findTimerRecord, "findTimerRecord");
    function timerAttributesToShape(timerRecord, pillContext) {
      const durationType = timerRecord.get("duration_type")?.ifString()?.getValue() ?? "";
      const fluentType = playbook_plugin_constants_1.DURATION_TYPE_FROM_DB[durationType];
      if (!fluentType) {
        return new sdk_build_core_1.ObjectShape({ source: timerRecord, properties: {} });
      }
      if (fluentType === "explicit") {
        const timerDuration = timerRecord.get("timer_duration")?.ifString()?.getValue() ?? "";
        const durationPill = pillContext ? (0, playbook_pill_transformer_1.transformStandardPills)(timerDuration, timerRecord, pillContext) : void 0;
        if (durationPill) {
          return new sdk_build_core_1.ObjectShape({
            source: timerRecord,
            properties: { type: fluentType, duration: durationPill }
          });
        }
        const duration = (0, sdk_build_core_1.parseGlideDuration)(timerDuration);
        return new sdk_build_core_1.ObjectShape({
          source: timerRecord,
          properties: {
            type: fluentType,
            ...duration ? { duration } : {}
          }
        });
      }
      if (fluentType === "relative") {
        const timerDuration = timerRecord.get("timer_duration")?.ifString()?.getValue() ?? "";
        const durationPill = pillContext ? (0, playbook_pill_transformer_1.transformStandardPills)(timerDuration, timerRecord, pillContext) : void 0;
        const duration = durationPill ?? (0, sdk_build_core_1.parseGlideDuration)(timerDuration);
        const rawRelativeDatetime = timerRecord.get("timer_relative_duration_datetime")?.ifString()?.getValue() ?? "";
        const relativeDatetimePill = pillContext ? (0, playbook_pill_transformer_1.transformStandardPills)(rawRelativeDatetime, timerRecord, pillContext) : void 0;
        return timerRecord.transform(({ $ }) => ({
          type: $.val(fluentType),
          ...duration ? { duration: $.val(duration) } : {},
          relativeDatetime: relativeDatetimePill ? $.val(relativeDatetimePill) : $.from("timer_relative_duration_datetime"),
          relativeOperator: $.from("timer_relative_duration_operator")
        }));
      }
      const rawPercentage = timerRecord.get("timer_percentage")?.ifString()?.getValue() ?? "";
      const percentagePill = pillContext ? (0, playbook_pill_transformer_1.transformStandardPills)(rawPercentage, timerRecord, pillContext) : void 0;
      const rawPercentageDatetime = timerRecord.get("percentage_timer_input_datetime")?.ifString()?.getValue() ?? "";
      const percentageDatetimePill = pillContext ? (0, playbook_pill_transformer_1.transformStandardPills)(rawPercentageDatetime, timerRecord, pillContext) : void 0;
      return timerRecord.transform(({ $ }) => ({
        type: $.val(fluentType),
        percentage: percentagePill ? $.val(percentagePill) : $.from("timer_percentage").toNumber(),
        percentageDatetime: percentageDatetimePill ? $.val(percentageDatetimePill) : $.from("percentage_timer_input_datetime")
      }));
    }
    __name(timerAttributesToShape, "timerAttributesToShape");
    async function timerAttributesToRecord(startWithDelayShape, laneRecord, processDefinition, sourceType, factory, diagnostics, pillContext) {
      const typeVal = startWithDelayShape.get("type")?.ifString()?.getValue();
      if (!typeVal) {
        return void 0;
      }
      const dbDurationType = playbook_plugin_constants_1.DURATION_TYPE_TO_DB[typeVal];
      if (!dbDurationType) {
        return void 0;
      }
      if (typeVal === "percentage") {
        const pctShape = startWithDelayShape.get("percentage");
        const pctVal = pctShape?.ifNumber()?.getValue();
        if (pctShape && pctVal !== void 0 && (pctVal <= 0 || pctVal > 100)) {
          diagnostics.error(pctShape, "percentage must be greater than 0 and at most 100");
        }
      }
      const properties = {
        active: "true",
        duration_type: dbDurationType,
        source: laneRecord.getId().getValue(),
        source_type: sourceType,
        process_definition: processDefinition
      };
      if (typeVal === "explicit" || typeVal === "relative") {
        const durationShape = startWithDelayShape.get("duration");
        if (durationShape) {
          const resolvedDuration = await (0, playbook_pill_resolver_1.resolvePlaybookPill)(durationShape, pillContext);
          const durationObj = resolvedDuration.ifObject();
          if (durationObj) {
            properties["timer_duration"] = formatDuration({
              days: durationObj.get("days")?.ifNumber()?.getValue() ?? 0,
              hours: durationObj.get("hours")?.ifNumber()?.getValue() ?? 0,
              minutes: durationObj.get("minutes")?.ifNumber()?.getValue() ?? 0,
              seconds: durationObj.get("seconds")?.ifNumber()?.getValue() ?? 0
            });
          } else if (!resolvedDuration.isUndefined()) {
            properties["timer_duration"] = resolvedDuration;
          }
        }
      }
      if (typeVal === "relative") {
        const relativeDatetimeShape = startWithDelayShape.get("relativeDatetime");
        if (relativeDatetimeShape) {
          properties["timer_relative_duration_datetime"] = await (0, playbook_pill_resolver_1.resolvePlaybookPill)(relativeDatetimeShape, pillContext);
        }
        properties["timer_relative_duration_operator"] = startWithDelayShape.get("relativeOperator")?.ifString()?.getValue() ?? "";
      }
      if (typeVal === "percentage") {
        const percentageShape = startWithDelayShape.get("percentage");
        if (percentageShape) {
          const resolvedPercentage = await (0, playbook_pill_resolver_1.resolvePlaybookPill)(percentageShape, pillContext);
          const pctNum = resolvedPercentage.ifNumber()?.getValue();
          if (pctNum !== void 0) {
            properties["timer_percentage"] = String(pctNum);
          } else if (!resolvedPercentage.isUndefined()) {
            properties["timer_percentage"] = resolvedPercentage;
          } else {
            properties["timer_percentage"] = "";
          }
        } else {
          properties["timer_percentage"] = "";
        }
        const percentageDatetimeShape = startWithDelayShape.get("percentageDatetime");
        if (percentageDatetimeShape) {
          properties["percentage_timer_input_datetime"] = await (0, playbook_pill_resolver_1.resolvePlaybookPill)(percentageDatetimeShape, pillContext);
        }
      }
      return factory.createRecord({
        source: startWithDelayShape,
        table: playbook_plugin_constants_1.TIMER_ATTRIBUTES_TABLE,
        properties
      });
    }
    __name(timerAttributesToRecord, "timerAttributesToRecord");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/activity-config-transform.js
var require_activity_config_transform = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/activity-config-transform.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.activityRecordToShape = activityRecordToShape;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var slugify_js_1 = require_slugify();
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var activity_ootb_maps_1 = require_activity_ootb_maps();
    var start_rule_resolver_1 = require_start_rule_resolver();
    var transform_utils_1 = require_transform_utils();
    var timer_attributes_transform_1 = require_timer_attributes_transform();
    var playbook_pill_transformer_1 = require_playbook_pill_transformer();
    function resolveVarEntryToShape(entry, allowedFields, elementMappingByField, varRecord, pillContext) {
      if (allowedFields !== void 0 && !allowedFields.has(entry.name)) {
        return void 0;
      }
      const value = varRecord.get("value");
      const str = value.ifString()?.getValue();
      const pillStr = elementMappingByField.get(entry.name);
      const effectiveStr = pillStr || str;
      if (entry.type === "template_value") {
        return (0, transform_utils_1.convertToTemplateValue)(value, effectiveStr, pillContext);
      }
      const transformed = effectiveStr && pillContext ? (0, playbook_pill_transformer_1.transformStandardPills)(effectiveStr, varRecord, pillContext) : void 0;
      return transformed ?? (0, transform_utils_1.convertValueToType)(value, entry.type);
    }
    __name(resolveVarEntryToShape, "resolveVarEntryToShape");
    function activityRecordToShape(record, database, guidToVarName, branchGuidMap, pillContext, timerRecords) {
      const { startRuleShape } = (0, start_rule_resolver_1.resolveStartRuleToShape)(record, database, guidToVarName, branchGuidMap);
      const activityPillContext = pillContext ? { ...pillContext, currentActivitySysId: record.getId().getValue() } : void 0;
      const timerRecord = timerRecords ? (0, timer_attributes_transform_1.findTimerRecord)(timerRecords, record.getId().getValue(), "sys_pd_activity") : void 0;
      const isAiAgentEnabled = record.get("enable_ai_agent")?.toBoolean().getValue();
      const activityDefSysId = record.get("activity_definition")?.ifString()?.getValue() ?? "";
      const isDecisionActivity = activityDefSysId === activity_ootb_maps_1.DECISION_DEF_SYS_ID;
      const configShape = record.transform(({ $ }) => ({
        $id: $.val(sdk_build_plugins_1.NowIdShape.from(record)),
        label: $.from("label"),
        name: $.from("name"),
        order: $.from("order").toNumber(),
        description: $.from("description"),
        startRule: $.val(startRuleShape),
        ...isDecisionActivity ? {} : {
          conditionToRun: $.from("condition_to_run").map((v) => {
            if (!activityPillContext) {
              return v;
            }
            const str = v.ifString()?.getValue();
            if (!str) {
              return v;
            }
            return (0, playbook_pill_transformer_1.transformConditionPills)(str, record, activityPillContext) ?? v;
          })
        },
        restartRule: $.from("restart_rule"),
        // AI-agent fields are internal round-trip-only and only meaningful when the agent
        // is enabled. When enable_ai_agent is true we surface enableAiAgent plus each
        // populated field; when it is false we omit the entire AI block. Within the block,
        // empty strings/lists are omitted while populated values and the booleans are
        // preserved as-is. aiAgentRunAs is the one field with a stable platform default
        // ('playbook_user'), so we omit it when it matches that default.
        ...isAiAgentEnabled ? {
          enableAiAgent: $.from("enable_ai_agent").toBoolean(),
          aiAgentObjective: $.from("ai_agent_objective").def(""),
          aiAgentObjAdditionalDetail: $.from("ai_agent_obj_additional_detail").def(""),
          aiAgentExecutionMode: $.from("ai_agent_execution_mode").def(""),
          aiAgentRunWithRoles: $.from("ai_agent_run_with_roles").map((v) => {
            const str = v.ifString()?.getValue() ?? "";
            return str ? str.split(",").map((s) => s.trim()).filter(Boolean) : [];
          }).def([]),
          aiAgentRunAs: $.from("ai_agent_run_as").def(playbook_plugin_constants_1.ACTIVITY_DEFAULTS.AI_AGENT_RUN_AS),
          aiAgentUser: $.from("ai_agent_user").def(""),
          conversationalAgents: $.from("conversational_agents").map((v) => {
            const str = v.ifString()?.getValue() ?? "";
            return str ? str.split(",").map((s) => s.trim()).filter(Boolean) : [];
          }).def([]),
          launchAgentWhenActivityStart: $.from("launch_agent_when_activity_start").toBoolean()
        } : {},
        ...timerRecord ? {
          startWithDelay: $.val((0, timer_attributes_transform_1.timerAttributesToShape)(timerRecord, activityPillContext))
        } : {}
      }));
      const activityDefKey = activity_ootb_maps_1.ACTIVITY_DEF_SYS_ID_TO_KEY[activityDefSysId];
      if (!activityDefKey) {
        return { success: false };
      }
      const allowedInputFields = activity_ootb_maps_1.ACTIVITY_DEF_INPUT_DISPLAY_PREFS[activityDefSysId];
      const allowedExperienceFields = activity_ootb_maps_1.ACTIVITY_DEF_EXPERIENCE_DISPLAY_PREFS[activityDefSysId];
      const elements = activityDefKey.split(".");
      if (elements.length < 2) {
        return { success: false };
      }
      const activityDefShape = new sdk_build_core_1.PropertyAccessShape({
        source: record,
        elements
      });
      const variableValueRecords = database.query(playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE, {
        document_key: record.getId().getValue()
      });
      const elementMappingRecords = database.query(playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE, {
        id: record.getId().getValue()
      });
      const inputElementMappingByField = /* @__PURE__ */ new Map();
      const expPropElementMappingByField = /* @__PURE__ */ new Map();
      for (const emRecord of elementMappingRecords) {
        const field = emRecord.get("field").ifString()?.getValue();
        const value = emRecord.get("value").ifString()?.getValue();
        const table = emRecord.get("table").ifString()?.getValue() ?? "";
        if (field && value) {
          if (table.includes("sys_pd_activity_type_prop")) {
            expPropElementMappingByField.set(field, value);
          } else {
            inputElementMappingByField.set(field, value);
          }
        }
      }
      const inputs = {};
      const experienceProperties = {};
      let branchMap;
      for (const varRecord of variableValueRecords) {
        const variableSysId = varRecord.get("variable").ifString()?.getValue();
        if (!variableSysId) {
          continue;
        }
        if (variableSysId === activity_ootb_maps_1.DECISION_BRANCHES_VAR_SYS_ID) {
          const jsonStr = varRecord.get("value").ifString()?.getValue();
          if (jsonStr) {
            try {
              const parsed = JSON.parse(jsonStr);
              if (Array.isArray(parsed)) {
                branchMap = /* @__PURE__ */ new Map();
                const branchObjects = parsed.map((branch) => {
                  const branchGuid = String(branch["id"] ?? "");
                  const rawLabel = String(branch["label"] ?? "");
                  const label = branchGuid === "ELSE" ? "Else" : rawLabel;
                  const fluentId = branchGuid === "ELSE" ? "else" : (0, slugify_js_1.slugifyString)(label) || branchGuid;
                  const props = {
                    id: fluentId,
                    label
                  };
                  if (branch["condition"] !== void 0 && branch["condition"] !== "") {
                    const decoded = (0, playbook_plugin_constants_1.decodeKeywordsToken)(String(branch["condition"]));
                    const transformed = activityPillContext ? (0, playbook_pill_transformer_1.transformConditionPills)(decoded, varRecord, activityPillContext) : void 0;
                    props["condition"] = transformed ?? decoded;
                  }
                  if (branchGuid) {
                    branchMap?.set(branchGuid, fluentId);
                  }
                  return props;
                });
                inputs["branches"] = new sdk_build_core_1.ArrayShape({ source: varRecord, elements: branchObjects });
              }
            } catch {
            }
          }
          continue;
        }
        const inputEntry = activity_ootb_maps_1.INPUT_SYS_ID_TO_FLUENT[variableSysId];
        if (inputEntry) {
          const shape = resolveVarEntryToShape(inputEntry, allowedInputFields, inputElementMappingByField, varRecord, activityPillContext);
          if (shape !== void 0) {
            inputs[inputEntry.name] = shape;
          }
          continue;
        }
        const experiencePropertyEntry = activity_ootb_maps_1.EXPERIENCE_PROPERTY_SYS_ID_TO_FLUENT[variableSysId];
        if (experiencePropertyEntry) {
          const shape = resolveVarEntryToShape(experiencePropertyEntry, allowedExperienceFields, expPropElementMappingByField, varRecord, activityPillContext);
          if (shape !== void 0) {
            experienceProperties[experiencePropertyEntry.name] = shape;
          }
        }
      }
      const args = [activityDefShape, configShape];
      const hasInputs = Object.keys(inputs).length > 0;
      const hasExperienceProps = Object.keys(experienceProperties).length > 0;
      if (hasInputs || hasExperienceProps) {
        args.push(new sdk_build_core_1.ObjectShape({ source: record, properties: hasInputs ? inputs : {} }));
      }
      if (hasExperienceProps) {
        args.push(new sdk_build_core_1.ObjectShape({ source: record, properties: experienceProperties }));
      }
      const result = {
        success: true,
        value: new sdk_build_core_1.CallExpressionShape({
          source: record,
          callee: playbook_plugin_constants_1.ACTIVITY_GENERATED_CALLEE,
          args
        })
      };
      if (branchMap) {
        result.branchMap = branchMap;
      }
      return result;
    }
    __name(activityRecordToShape, "activityRecordToShape");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/template-value-normalizer.js
var require_template_value_normalizer = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/template-value-normalizer.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.normalizeTemplateValue = normalizeTemplateValue;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    function normalizeTemplateValue(shape) {
      if (!(shape instanceof sdk_build_core_1.TemplateValueShape)) {
        return shape;
      }
      const source = shape.getSource();
      const entries = shape.getTemplateValue().entries();
      if (entries.length === 0) {
        return shape;
      }
      const parts = [];
      for (let i = 0; i < entries.length; i++) {
        const entry = entries[i];
        if (i > 0) {
          parts.push({ type: "literal", text: "^" });
        }
        parts.push({ type: "literal", text: `${entry[0]}=` });
        flattenValueIntoParts(entry[1], parts);
      }
      return assembleTemplateExpression(source, parts);
    }
    __name(normalizeTemplateValue, "normalizeTemplateValue");
    function flattenValueIntoParts(value, parts) {
      if (value instanceof sdk_build_core_1.CallExpressionShape && value.getCallee() === playbook_plugin_constants_1.DATAPILL_CALLEE) {
        parts.push({ type: "expr", shape: value });
        return;
      }
      if (value instanceof sdk_build_core_1.TemplateExpressionShape) {
        const headText = value.getLiteralText();
        if (headText) {
          parts.push({ type: "literal", text: escapeCarets(headText) });
        }
        for (const span of value.getSpans()) {
          flattenValueIntoParts(span.getExpression(), parts);
          const trailingText = span.getLiteralText();
          if (trailingText) {
            parts.push({ type: "literal", text: escapeCarets(trailingText) });
          }
        }
        return;
      }
      const strShape = value.ifString();
      if (strShape) {
        parts.push({ type: "literal", text: escapeCarets(strShape.getValue()) });
        return;
      }
      const numShape = value.ifNumber();
      if (numShape) {
        parts.push({ type: "literal", text: String(numShape.getValue()) });
        return;
      }
      parts.push({ type: "literal", text: escapeCarets(value.toString().getValue()) });
    }
    __name(flattenValueIntoParts, "flattenValueIntoParts");
    function assembleTemplateExpression(source, parts) {
      const merged = [];
      for (const part of parts) {
        const last = merged[merged.length - 1];
        if (part.type === "literal" && last?.type === "literal") {
          last.text += part.text;
        } else {
          merged.push({ ...part });
        }
      }
      if (merged.every((p) => p.type === "literal")) {
        const text = merged.map((p) => p.text).join("");
        return sdk_build_core_1.Shape.from(source, text).asString();
      }
      let headText = "";
      let startIdx = 0;
      const first = merged[0];
      if (first?.type === "literal") {
        headText = first.text;
        startIdx = 1;
      }
      const spans = [];
      for (let i = startIdx; i < merged.length; i++) {
        const part = merged[i];
        if (part.type !== "expr") {
          continue;
        }
        let trailingText = "";
        const next = merged[i + 1];
        if (next?.type === "literal") {
          trailingText = next.text;
          i++;
        }
        spans.push(new sdk_build_core_1.TemplateSpanShape({ source, expression: part.shape, literalText: trailingText }));
      }
      return new sdk_build_core_1.TemplateExpressionShape({ source, literalText: headText, spans });
    }
    __name(assembleTemplateExpression, "assembleTemplateExpression");
    function escapeCarets(text) {
      return text.replace(/\^/g, "^^");
    }
    __name(escapeCarets, "escapeCarets");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/activity-plugin.js
var require_activity_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/activity-plugin.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.ActivityPlugin = void 0;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var activity_ootb_maps_1 = require_activity_ootb_maps();
    var playbook_shapes_1 = require_playbook_shapes();
    var activity_diagnostics_plugin_1 = require_activity_diagnostics_plugin();
    var label_utils_1 = require_label_utils();
    var start_rule_resolver_1 = require_start_rule_resolver();
    var slugify_js_1 = require_slugify();
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var activity_config_transform_1 = require_activity_config_transform();
    var transform_utils_1 = require_transform_utils();
    var timer_attributes_transform_1 = require_timer_attributes_transform();
    var playbook_pill_resolver_1 = require_playbook_pill_resolver();
    var template_value_normalizer_1 = require_template_value_normalizer();
    var playbook_shapes_2 = require_playbook_shapes();
    var playbook_pill_transformer_1 = require_playbook_pill_transformer();
    function extractVariableSysId(shape) {
      const obj = shape.if(sdk_build_core_1.CallExpressionShape)?.getArgument(0)?.ifObject() ?? shape.ifObject();
      return obj?.get("sysId", false)?.ifString()?.getValue();
    }
    __name(extractVariableSysId, "extractVariableSysId");
    function extractColumnDefaultValue(shape) {
      const obj = shape.if(sdk_build_core_1.CallExpressionShape)?.getArgument(0)?.ifObject() ?? shape.ifObject();
      const value = obj?.get("defaultValue", false);
      return value?.getValue();
    }
    __name(extractColumnDefaultValue, "extractColumnDefaultValue");
    function resolveActivityDef(activityDefShape) {
      const activityDefConfig = (0, playbook_shapes_1.getFactoryCallConfig)(activityDefShape);
      if (!activityDefConfig) {
        return void 0;
      }
      const activityDefSysId = activityDefConfig.get("$id", false)?.ifString()?.getValue();
      if (!activityDefSysId) {
        return void 0;
      }
      const flowShape = activityDefConfig.get("flow");
      const actionShape = activityDefConfig.get("action");
      const backingShape = flowShape?.ifDefined() ?? actionShape;
      const backingType = flowShape?.ifDefined() ? "flow" : actionShape?.ifDefined() ? "action" : void 0;
      const backingConfig = (0, playbook_shapes_1.getFactoryCallConfig)(backingShape);
      const activityTypeConfig = (0, playbook_shapes_1.getFactoryCallConfig)(activityDefConfig.get("activityType"));
      return {
        activityDefSysId,
        backingInputs: backingConfig?.get("inputs", false)?.ifObject(),
        backingExperienceProperties: activityTypeConfig?.get("experienceProperties", false)?.ifObject(),
        backingSysId: backingConfig?.get("$id", false)?.ifString()?.getValue(),
        backingType,
        activityTypeSysId: activityTypeConfig?.get("$id", false)?.ifString()?.getValue(),
        defaultInputs: activityDefConfig.get("defaultInputs", false)?.ifObject()?.getValue() ?? {},
        defaultExperienceProperties: activityDefConfig.get("defaultExperienceProperties", false)?.ifObject()?.getValue() ?? {}
      };
    }
    __name(resolveActivityDef, "resolveActivityDef");
    exports2.ActivityPlugin = sdk_build_core_1.Plugin.create({
      name: "ActivityPlugin",
      records: {
        [playbook_plugin_constants_1.ACTIVITY_TABLE]: {
          relationships: {
            [playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE]: {
              via: "document_key",
              descendant: true
            },
            [playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE]: {
              via: "id",
              descendant: true
            }
          },
          toShape(record, { database }, guidIdToVarName) {
            if ((0, transform_utils_1.isPlaybookUnsupported)(record, database)) {
              return { success: false };
            }
            return (0, activity_config_transform_1.activityRecordToShape)(record, database, guidIdToVarName);
          }
        },
        [playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE]: {
          coalesce: ["document_key", "variable"]
        },
        [playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE]: {
          coalesce: ["field", "table", "id"]
        }
      },
      shapes: [
        {
          shape: playbook_shapes_1.ActivityShape,
          fileTypes: ["fluent"],
          async toRecord(activityShape, { factory, transform, diagnostics }) {
            const config = activityShape.getConfig();
            const injectedName = config.get("uniqueName")?.ifString()?.getValue();
            let computedName = injectedName;
            if (!computedName) {
              const explicitName = config.get("name")?.ifString()?.getValue();
              const labelVal = config.get("label")?.ifString()?.getValue();
              computedName = explicitName ?? (labelVal ? (0, slugify_js_1.slugifyString)((0, label_utils_1.stripHtmlTags)(labelVal)) : "");
            }
            const resolvedActivityDef = resolveActivityDef(activityShape.getActivityDefinition());
            if (!resolvedActivityDef) {
              return { success: false };
            }
            const { activityDefSysId, backingInputs, backingExperienceProperties, backingSysId, backingType, activityTypeSysId, defaultInputs, defaultExperienceProperties } = resolvedActivityDef;
            const isDecision = String(activityDefSysId) === activity_ootb_maps_1.DECISION_DEF_SYS_ID;
            await transform.toRecord(activityShape, activity_diagnostics_plugin_1.ActivityDiagnosticsPlugin);
            const playbookBodyParamsNames = activityShape.getPlaybookBodyParamsNames();
            const conditionPillContext = {
              format: "condition",
              factory,
              diagnostics,
              playbookBodyParamsNames,
              allowedSources: {
                paramsInputs: true,
                paramsState: false,
                paramsParentRecord: true,
                previousActivityOutputs: true,
                currentActivityLabelDesc: false,
                automationPlan: false
              }
            };
            const conditionToRunShape = config.get("conditionToRun");
            const resolvedConditionToRun = conditionToRunShape && !conditionToRunShape.isUndefined() ? await (0, playbook_pill_resolver_1.resolvePlaybookPill)(conditionToRunShape, conditionPillContext) : void 0;
            const activityRecord = await factory.createRecord({
              source: activityShape,
              table: playbook_plugin_constants_1.ACTIVITY_TABLE,
              explicitId: config.get("$id"),
              properties: config.transform(({ $ }) => ({
                activity_definition: $.val(activityDefSysId),
                // Mirrors platform BR "Strip HTML from label": HTML is not supported on
                // sys_pd_activity.label and the BR silently strips it on insert/update.
                // ActivityDiagnosticsPlugin emits a warning when this strip applies.
                label: $.map((label) => {
                  const str = label.ifString()?.getValue();
                  return str !== void 0 ? (0, label_utils_1.stripHtmlTags)(str) : label;
                }),
                name: $.val(computedName),
                order: $,
                active: $.def(playbook_plugin_constants_1.ACTIVITY_DEFAULTS.ACTIVE),
                description: $,
                start_rule_name: $.from("startRule").map((startRule) => (0, start_rule_resolver_1.resolveStartRuleToDatabase)(startRule)),
                starts_after_activities: $.from("startsAfterActivities"),
                starts_after_branches: $.from("startsAfterBranches"),
                starts_after_lanes: $.from("startsAfterLanes"),
                condition_to_run: resolvedConditionToRun ? $.val(resolvedConditionToRun) : $.from("conditionToRun"),
                restart_rule: $.from("restartRule"),
                lane: $.def(""),
                process_definition: $.from("processDefinition"),
                // Preserve AI-agent fields on round-trip
                enable_ai_agent: $.from("enableAiAgent"),
                ai_agent_objective: $.from("aiAgentObjective"),
                ai_agent_obj_additional_detail: $.from("aiAgentObjAdditionalDetail"),
                ai_agent_execution_mode: $.from("aiAgentExecutionMode"),
                ai_agent_run_with_roles: $.from("aiAgentRunWithRoles"),
                ai_agent_run_as: $.from("aiAgentRunAs").def(playbook_plugin_constants_1.ACTIVITY_DEFAULTS.AI_AGENT_RUN_AS),
                ai_agent_user: $.from("aiAgentUser"),
                conversational_agents: $.from("conversationalAgents"),
                launch_agent_when_activity_start: $.from("launchAgentWhenActivityStart")
              }))
            });
            const relatedRecords = [];
            const activitySysId = activityRecord.getId().getValue();
            const startWithDelayShape = config.get("startWithDelay");
            if (startWithDelayShape && !startWithDelayShape.isUndefined()) {
              const timerPillContext = {
                format: "standard",
                factory,
                diagnostics,
                currentActivityId: activityRecord.getId(),
                playbookBodyParamsNames,
                allowedSources: {
                  paramsInputs: true,
                  paramsState: true,
                  paramsParentRecord: true,
                  previousActivityOutputs: true,
                  currentActivityLabelDesc: false,
                  automationPlan: false
                }
              };
              const processDefinition = config.get("processDefinition")?.ifString()?.getValue() ?? "";
              const timerRecord = await (0, timer_attributes_transform_1.timerAttributesToRecord)(startWithDelayShape.asObject(), activityRecord, processDefinition, "sys_pd_activity", factory, diagnostics, timerPillContext);
              if (timerRecord) {
                relatedRecords.push(timerRecord);
              }
            }
            const inputPillContext = {
              format: "standard",
              factory,
              diagnostics,
              currentActivityId: activityRecord.getId(),
              playbookBodyParamsNames,
              allowedSources: {
                paramsInputs: true,
                paramsState: true,
                paramsParentRecord: true,
                previousActivityOutputs: true,
                currentActivityLabelDesc: true,
                automationPlan: false
              }
            };
            const experiencePropertyPillContext = {
              format: "standard",
              factory,
              diagnostics,
              currentActivityId: activityRecord.getId(),
              playbookBodyParamsNames,
              allowedSources: {
                paramsInputs: true,
                paramsState: true,
                paramsParentRecord: true,
                previousActivityOutputs: true,
                currentActivityLabelDesc: true,
                automationPlan: true
              }
            };
            const inputs = activityShape.getInputs();
            if (backingInputs && inputs) {
              for (const fluentName of inputs.keys()) {
                if (isDecision && fluentName === "branches") {
                  continue;
                }
                const varEntryShape = backingInputs.get(fluentName, false);
                if (!varEntryShape) {
                  continue;
                }
                const sysId = extractVariableSysId(varEntryShape);
                if (!sysId) {
                  continue;
                }
                const rawInputShape = inputs.get(fluentName);
                const inputShape = rawInputShape instanceof sdk_build_core_1.TemplateValueShape ? (0, template_value_normalizer_1.normalizeTemplateValue)(rawInputShape) : rawInputShape;
                const isPill = (0, playbook_shapes_2.containsDataPill)(inputShape);
                const resolvedValue = await (0, playbook_pill_resolver_1.resolvePlaybookPill)(inputShape, inputPillContext);
                relatedRecords.push(await factory.createRecord({
                  source: inputShape,
                  table: playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE,
                  properties: {
                    document: playbook_plugin_constants_1.ACTIVITY_TABLE,
                    document_key: activitySysId,
                    variable: sysId,
                    value: isPill ? "" : resolvedValue
                  }
                }));
                if (backingSysId) {
                  const inputTablePrefix = backingType === "action" ? "sys_hub_action_input" : "sys_hub_flow_input";
                  relatedRecords.push(await factory.createRecord({
                    source: inputShape,
                    table: playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE,
                    properties: {
                      field: fluentName,
                      id: activitySysId,
                      table: `var__m_${inputTablePrefix}_${backingSysId}`,
                      value: isPill ? resolvedValue : ""
                    }
                  }));
                }
              }
            }
            if (backingInputs) {
              const userProvidedInputs = inputs ? new Set(inputs.keys()) : /* @__PURE__ */ new Set();
              for (const [fluentName, defaultValue] of Object.entries(defaultInputs)) {
                if (userProvidedInputs.has(fluentName)) {
                  continue;
                }
                const varEntryShape = backingInputs.get(fluentName, false);
                if (!varEntryShape) {
                  continue;
                }
                const sysId = extractVariableSysId(varEntryShape);
                if (!sysId) {
                  continue;
                }
                const rawValue = String(defaultValue);
                const isPill = [...rawValue.matchAll(playbook_pill_transformer_1.STANDARD_PILL_REGEX)].length > 0;
                const stringValue = isPill ? rawValue.replace(/\{\{act\.act_instance\./g, `{{act.${activitySysId}.`) : rawValue;
                relatedRecords.push(await factory.createRecord({
                  source: activityShape,
                  table: playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE,
                  properties: {
                    document: playbook_plugin_constants_1.ACTIVITY_TABLE,
                    document_key: activitySysId,
                    variable: sysId,
                    value: isPill ? "" : stringValue
                  }
                }));
                if (backingSysId) {
                  const inputTablePrefix = backingType === "action" ? "sys_hub_action_input" : "sys_hub_flow_input";
                  relatedRecords.push(await factory.createRecord({
                    source: activityShape,
                    table: playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE,
                    properties: {
                      field: fluentName,
                      id: activitySysId,
                      table: `var__m_${inputTablePrefix}_${backingSysId}`,
                      value: isPill ? stringValue : ""
                    }
                  }));
                }
              }
            }
            if (isDecision && backingInputs) {
              const branchesShape = inputs?.get("branches")?.ifArray();
              const branchesEntryShape = backingInputs.get("branches", false);
              const branchesSysId = branchesEntryShape ? extractVariableSysId(branchesEntryShape) : void 0;
              if (branchesShape && branchesSysId) {
                const branchesJson = [];
                for (const [index, elem] of branchesShape.getElements().entries()) {
                  const branch = elem.ifObject();
                  const rawId = branch?.get("id")?.ifString()?.getValue() ?? "";
                  const rawLabel = branch?.get("label")?.ifString()?.getValue() ?? "";
                  const label = rawId === "else" ? "Else" : rawLabel;
                  const id = rawId === "else" ? "ELSE" : (0, sdk_build_plugins_1.generateId)(activitySysId, "branch", (0, slugify_js_1.slugifyString)(label) || rawId);
                  const conditionShape = branch?.get("condition");
                  let condition = "";
                  if (conditionShape) {
                    const resolved = await (0, playbook_pill_resolver_1.resolvePlaybookPill)(conditionShape, conditionPillContext);
                    const rawCondition = resolved.ifString()?.getValue() ?? resolved.getValue()?.toString() ?? "";
                    condition = (0, playbook_plugin_constants_1.encodeKeywordsToken)(rawCondition);
                  }
                  branchesJson.push({ id, label, condition, order: index + 1, activitiesCount: 0 });
                }
                relatedRecords.push(await factory.createRecord({
                  source: branchesShape,
                  table: playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE,
                  properties: {
                    document: playbook_plugin_constants_1.ACTIVITY_TABLE,
                    document_key: activitySysId,
                    variable: branchesSysId,
                    value: JSON.stringify(branchesJson)
                  }
                }));
              }
              const hasType = inputs && [...inputs.keys()].includes("type");
              if (!hasType) {
                const typeEntryShape = backingInputs.get("type", false);
                const typeSysId = typeEntryShape ? extractVariableSysId(typeEntryShape) : void 0;
                if (typeSysId) {
                  relatedRecords.push(await factory.createRecord({
                    source: activityShape,
                    table: playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE,
                    properties: {
                      document: playbook_plugin_constants_1.ACTIVITY_TABLE,
                      document_key: activitySysId,
                      variable: typeSysId,
                      value: "match_first"
                    }
                  }));
                }
              }
            }
            const experienceProperties = activityShape.getExperienceProperties();
            if (backingExperienceProperties && experienceProperties) {
              for (const fluentName of experienceProperties.keys()) {
                const varEntryShape = backingExperienceProperties.get(fluentName, false);
                if (!varEntryShape) {
                  continue;
                }
                const sysId = extractVariableSysId(varEntryShape);
                if (!sysId) {
                  continue;
                }
                const rawExpPropShape = experienceProperties.get(fluentName);
                const expPropShape = rawExpPropShape instanceof sdk_build_core_1.TemplateValueShape ? (0, template_value_normalizer_1.normalizeTemplateValue)(rawExpPropShape) : rawExpPropShape;
                const isPill = (0, playbook_shapes_2.containsDataPill)(expPropShape);
                const resolvedValue = await (0, playbook_pill_resolver_1.resolvePlaybookPill)(expPropShape, experiencePropertyPillContext);
                relatedRecords.push(await factory.createRecord({
                  source: expPropShape,
                  table: playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE,
                  properties: {
                    document: playbook_plugin_constants_1.ACTIVITY_TABLE,
                    document_key: activitySysId,
                    variable: sysId,
                    value: isPill ? "" : resolvedValue
                  }
                }));
                if (activityTypeSysId) {
                  relatedRecords.push(await factory.createRecord({
                    source: expPropShape,
                    table: playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE,
                    properties: {
                      field: fluentName,
                      id: activitySysId,
                      table: `var__m_sys_pd_activity_type_prop_${activityTypeSysId}`,
                      value: isPill ? resolvedValue : ""
                    }
                  }));
                }
              }
            }
            if (backingExperienceProperties) {
              const userProvidedExpProps = experienceProperties ? new Set(experienceProperties.keys()) : /* @__PURE__ */ new Set();
              for (const fluentName of backingExperienceProperties.keys()) {
                if (userProvidedExpProps.has(fluentName)) {
                  continue;
                }
                const varEntryShape = backingExperienceProperties.get(fluentName, false);
                if (!varEntryShape) {
                  continue;
                }
                const defaultValue = fluentName in defaultExperienceProperties ? defaultExperienceProperties[fluentName] : extractColumnDefaultValue(varEntryShape);
                if (defaultValue === void 0) {
                  continue;
                }
                const sysId = extractVariableSysId(varEntryShape);
                if (!sysId) {
                  continue;
                }
                const rawValue = String(defaultValue);
                const isPill = [...rawValue.matchAll(playbook_pill_transformer_1.STANDARD_PILL_REGEX)].length > 0;
                const stringValue = isPill ? rawValue.replace(/\{\{act\.act_instance\./g, `{{act.${activitySysId}.`) : rawValue;
                relatedRecords.push(await factory.createRecord({
                  source: activityShape,
                  table: playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE,
                  properties: {
                    document: playbook_plugin_constants_1.ACTIVITY_TABLE,
                    document_key: activitySysId,
                    variable: sysId,
                    value: isPill ? "" : stringValue
                  }
                }));
                if (activityTypeSysId) {
                  relatedRecords.push(await factory.createRecord({
                    source: activityShape,
                    table: playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE,
                    properties: {
                      field: fluentName,
                      id: activitySysId,
                      table: `var__m_sys_pd_activity_type_prop_${activityTypeSysId}`,
                      value: isPill ? stringValue : ""
                    }
                  }));
                }
              }
            }
            return {
              success: true,
              value: activityRecord.with(...relatedRecords)
            };
          }
        }
      ],
      docs: []
    });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/lane-config-transform.js
var require_lane_config_transform = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/lane-config-transform.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.resolveStartRule = void 0;
    exports2.buildLaneToShapeResult = buildLaneToShapeResult;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var activity_config_transform_1 = require_activity_config_transform();
    var start_rule_resolver_1 = require_start_rule_resolver();
    var timer_attributes_transform_1 = require_timer_attributes_transform();
    var playbook_pill_transformer_1 = require_playbook_pill_transformer();
    var start_rule_resolver_2 = require_start_rule_resolver();
    Object.defineProperty(exports2, "resolveStartRule", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return start_rule_resolver_2.resolveStartRuleToDatabase;
    }, "get") });
    function buildLaneToShapeResult(record, database, guidToVarName, timerRecords, globalActivityMap, branchGuidMap) {
      const { startRuleShape } = (0, start_rule_resolver_1.resolveStartRuleToShape)(record, database, guidToVarName, branchGuidMap);
      const currentLaneVarName = guidToVarName?.get(record.getId().getValue());
      const pillContext = globalActivityMap ? {
        activityMap: globalActivityMap,
        ...currentLaneVarName ? { currentLaneVarName } : {}
      } : void 0;
      const timerRecord = timerRecords ? (0, timer_attributes_transform_1.findTimerRecord)(timerRecords, record.getId().getValue(), "sys_pd_lane") : void 0;
      const configShape = record.transform(({ $ }) => ({
        $id: $.val(sdk_build_plugins_1.NowIdShape.from(record)),
        label: $.from("label"),
        name: $.from("name"),
        order: $.from("order").toNumber(),
        description: $.from("description").def(""),
        startRule: $.val(startRuleShape),
        conditionToRun: $.from("condition_to_run").def(playbook_plugin_constants_1.LANE_DEFAULTS.CONDITION_TO_RUN).map((v) => {
          if (!pillContext) {
            return v;
          }
          const str = v.ifString()?.getValue();
          if (!str) {
            return v;
          }
          return (0, playbook_pill_transformer_1.transformConditionPills)(str, record, pillContext) ?? v;
        }),
        restartRule: $.from("restart_rule"),
        ...timerRecord ? {
          startWithDelay: $.val((0, timer_attributes_transform_1.timerAttributesToShape)(timerRecord, pillContext))
        } : {}
      }));
      const activityRecords = database.query(playbook_plugin_constants_1.ACTIVITY_TABLE, { lane: record.getId().getValue() });
      const activitiesArrow = buildActivityCollectionToShape(record, activityRecords, database, globalActivityMap, currentLaneVarName, timerRecords);
      const laneArg = new sdk_build_core_1.ObjectShape({
        source: record,
        properties: {
          config: configShape,
          activities: activitiesArrow
        }
      });
      return {
        success: true,
        value: new sdk_build_core_1.CallExpressionShape({
          source: record,
          callee: playbook_plugin_constants_1.LANE_GENERATED_CALLEE,
          args: [laneArg]
        })
      };
    }
    __name(buildLaneToShapeResult, "buildLaneToShapeResult");
    function buildActivityCollectionToShape(source, activityRecords, database, globalActivityMap, currentLaneVarName, timerRecords) {
      const sortedRecords = topologicalSort(activityRecords, (rec) => {
        const raw = rec.get("starts_after_activities")?.ifString()?.getValue() ?? "";
        return raw.split(",").map((g) => g.trim()).filter(Boolean);
      });
      const activityGuidToVarName = /* @__PURE__ */ new Map();
      for (const [i, rec] of sortedRecords.entries()) {
        const varName = `activity_${i}`;
        const guid = rec.getId().getValue();
        activityGuidToVarName.set(guid, varName);
        globalActivityMap?.set(guid, {
          varName,
          ...currentLaneVarName ? { laneVarName: currentLaneVarName } : {}
        });
      }
      const pillContext = globalActivityMap ? {
        activityMap: globalActivityMap,
        ...currentLaneVarName ? { currentLaneVarName } : {}
      } : void 0;
      const branchGuidMap = /* @__PURE__ */ new Map();
      const childShapesWithIndex = [];
      for (const [i, rec] of sortedRecords.entries()) {
        const result = (0, activity_config_transform_1.activityRecordToShape)(rec, database, activityGuidToVarName, branchGuidMap, pillContext, timerRecords);
        if (result.success) {
          childShapesWithIndex.push({ shape: result.value, originalIndex: i });
          if (result.branchMap) {
            const activityGuid = rec.getId().getValue();
            const activityVarName = activityGuidToVarName.get(activityGuid) ?? `activity_${i}`;
            for (const [branchGuid, fluentBranchId] of result.branchMap) {
              branchGuidMap.set(branchGuid, { activityVarName, fluentBranchId });
            }
          }
        }
      }
      if (childShapesWithIndex.length === 0) {
        return new sdk_build_plugins_1.ArrowFunctionShape({
          source,
          parameters: [],
          returnValue: new sdk_build_core_1.ObjectShape({ source, properties: {} }),
          implicitReturn: true
        });
      }
      const statements = [];
      const returnEntries = {};
      for (const { shape, originalIndex } of childShapesWithIndex) {
        const varName = `activity_${originalIndex}`;
        statements.push(new sdk_build_core_1.VariableStatementShape({ source, variableName: varName, initializer: shape }));
        returnEntries[varName] = new sdk_build_core_1.IdentifierShape({ source, name: varName });
      }
      const returnValue = new sdk_build_core_1.ObjectShape({ source, properties: returnEntries });
      return new sdk_build_plugins_1.ArrowFunctionShape({
        source,
        parameters: [],
        statements,
        returnValue
      });
    }
    __name(buildActivityCollectionToShape, "buildActivityCollectionToShape");
    function topologicalSort(records, getDependencyGuids) {
      const guidToRecord = /* @__PURE__ */ new Map();
      for (const rec of records) {
        guidToRecord.set(rec.getId().getValue(), rec);
      }
      const sorted = [];
      const visited = /* @__PURE__ */ new Set();
      const visiting = /* @__PURE__ */ new Set();
      function visit(rec) {
        const guid = rec.getId().getValue();
        if (visited.has(guid)) {
          return;
        }
        if (visiting.has(guid)) {
          return;
        }
        visiting.add(guid);
        for (const depGuid of getDependencyGuids(rec)) {
          const depRecord = guidToRecord.get(depGuid);
          if (depRecord) {
            visit(depRecord);
          }
        }
        visiting.delete(guid);
        visited.add(guid);
        sorted.push(rec);
      }
      __name(visit, "visit");
      for (const rec of records) {
        visit(rec);
      }
      return sorted;
    }
    __name(topologicalSort, "topologicalSort");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/child-collection-torecord.js
var require_child_collection_torecord = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/child-collection-torecord.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processChildCollectionToRecord = processChildCollectionToRecord;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var arrow_body_parser_1 = require_arrow_body_parser();
    async function processChildCollectionToRecord(config) {
      const { collectionArrow, parentRecord, parentFkField, calleeNames, childTypeMap, resolveReferences, resolvedRefField, resolveBranchReferences, resolvedBranchRefField, resolvedBranchActivityField, extractBranchIdMap, computePerChildMergeProps, validateChildren, externalNowIdKeyToGuid, externalBranchIdMap, transform, diagnostics, errorMessage, errorSource } = config;
      const { variables, returnObj } = (0, arrow_body_parser_1.parseArrowBodyEntries)(collectionArrow);
      if (!returnObj) {
        diagnostics.error(errorSource, errorMessage);
        return [];
      }
      if (!returnObj.isObject()) {
        return [];
      }
      const returnObjEntries = returnObj.asObject();
      const nowIdKeyToGuid = externalNowIdKeyToGuid ?? /* @__PURE__ */ new Map();
      const records = [];
      const allowedCalleeNames = calleeNames ?? [...childTypeMap.keys()];
      const children = [];
      for (const [, childValue] of returnObjEntries.entries()) {
        const resolved = childValue.isIdentifier() ? variables.get(childValue.asIdentifier().getName()) ?? childValue : childValue;
        if (!resolved.is(sdk_build_core_1.CallExpressionShape)) {
          continue;
        }
        const callExpr = resolved.as(sdk_build_core_1.CallExpressionShape);
        const callee = callExpr.getCallee();
        if (!allowedCalleeNames.includes(callee)) {
          continue;
        }
        const typeConfig = childTypeMap.get(callee);
        if (!typeConfig) {
          continue;
        }
        const argIndex = typeConfig.configArgIndex ?? 0;
        const callArg = callExpr.getArgument(argIndex).asObject();
        const configShape = callArg.get("config");
        const childConfig = configShape?.isUndefined() === false ? configShape.asObject() : callArg;
        children.push({ callExpr, childConfig, typeConfig, callee });
      }
      const allChildConfigs = children.map((c) => c.childConfig);
      const allChildCallees = children.map((c) => c.callee);
      if (validateChildren) {
        validateChildren(children, variables);
      }
      const branchIdMap = externalBranchIdMap ?? /* @__PURE__ */ new Map();
      for (const [index, { callExpr, childConfig, typeConfig }] of children.entries()) {
        const createShape = typeConfig.createShape;
        const childPlugin = typeConfig.childPlugin;
        const mergeProps = {
          [parentFkField]: parentRecord.getId().getValue()
        };
        if (resolveReferences && resolvedRefField) {
          const resolvedRef = resolveReferences(childConfig, variables, nowIdKeyToGuid);
          if (resolvedRef !== void 0) {
            mergeProps[resolvedRefField] = resolvedRef;
          }
        }
        if (resolveBranchReferences && resolvedBranchRefField) {
          const resolvedBranch = resolveBranchReferences(childConfig, variables, nowIdKeyToGuid, branchIdMap);
          if (resolvedBranch !== void 0) {
            mergeProps[resolvedBranchRefField] = resolvedBranch;
            if (resolvedBranchActivityField) {
              const actSysIds = [
                ...new Set(resolvedBranch.split(",").map((ref) => ref.split(".")[0]).filter(Boolean))
              ];
              if (actSysIds.length > 0) {
                mergeProps[resolvedBranchActivityField] = actSysIds.join(",");
              }
            }
          }
        }
        if (computePerChildMergeProps) {
          Object.assign(mergeProps, computePerChildMergeProps(allChildConfigs, index, allChildCallees));
        }
        const configWithParent = childConfig.merge(mergeProps);
        const shape = createShape(callExpr, configWithParent, { nowIdKeyToGuid });
        const result = await transform.toRecord(shape, childPlugin);
        if (result.success) {
          const idShape = childConfig.get("$id");
          if (idShape instanceof sdk_build_plugins_1.NowIdShape) {
            nowIdKeyToGuid.set(String(idShape.getValue()), result.value.getId().getValue());
          }
          if (extractBranchIdMap) {
            const mapping = extractBranchIdMap(callExpr, result.value);
            if (mapping) {
              branchIdMap.set(result.value.getId().getValue(), mapping);
            }
          }
          records.push(result.value);
        }
      }
      return records;
    }
    __name(processChildCollectionToRecord, "processChildCollectionToRecord");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/graph-utils.js
var require_graph_utils = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/graph-utils.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.detectRedundantEdges = detectRedundantEdges;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var start_rule_resolver_1 = require_start_rule_resolver();
    var NOW_ID_PATTERN = /^Now\.ID\[['"]([^'"]+)['"]\]$/;
    function getNowIdKey(shape) {
      return shape.getCode().match(NOW_ID_PATTERN)?.[1];
    }
    __name(getNowIdKey, "getNowIdKey");
    function getNodeIdKey(config) {
      return getNowIdKey(config.get("$id"));
    }
    __name(getNodeIdKey, "getNodeIdKey");
    function getConfigWithId(callExpr) {
      for (const argIndex of [0, 1]) {
        const arg = callExpr.getArgument(argIndex)?.ifObject();
        if (!arg) {
          continue;
        }
        const nested = arg.get("config")?.isUndefined() === false ? arg.get("config").ifObject() : arg;
        if (nested && getNowIdKey(nested.get("$id")) !== void 0) {
          return nested;
        }
      }
      return void 0;
    }
    __name(getConfigWithId, "getConfigWithId");
    function getRunAfterDepKeys(config, variables) {
      const startRuleShape = config.get("startRule");
      if (!startRuleShape?.is(sdk_build_core_1.CallExpressionShape)) {
        return [];
      }
      const runAfterCall = startRuleShape.as(sdk_build_core_1.CallExpressionShape);
      if (!(0, start_rule_resolver_1.isRunAfterCallee)(runAfterCall.getCallee())) {
        return [];
      }
      const keys = [];
      for (const arg of runAfterCall.getArguments()) {
        let callExpr;
        if (arg.is(sdk_build_core_1.CallExpressionShape)) {
          callExpr = arg.as(sdk_build_core_1.CallExpressionShape);
        } else if (arg.isIdentifier()) {
          callExpr = variables.get(arg.asIdentifier().getName());
        }
        if (!callExpr) {
          continue;
        }
        const cfg = getConfigWithId(callExpr);
        const key = cfg ? getNowIdKey(cfg.get("$id")) : void 0;
        if (key) {
          keys.push(key);
        }
      }
      return keys;
    }
    __name(getRunAfterDepKeys, "getRunAfterDepKeys");
    function buildReachabilitySet(nodeId, graph, cache) {
      if (cache.has(nodeId)) {
        return cache.get(nodeId);
      }
      const reachable = /* @__PURE__ */ new Set();
      cache.set(nodeId, reachable);
      for (const dep of graph.get(nodeId) ?? []) {
        reachable.add(dep);
        for (const transitive of buildReachabilitySet(dep, graph, cache)) {
          reachable.add(transitive);
        }
      }
      return reachable;
    }
    __name(buildReachabilitySet, "buildReachabilitySet");
    function detectRedundantEdges(children, variables) {
      const graph = /* @__PURE__ */ new Map();
      const nodeCallExprs = /* @__PURE__ */ new Map();
      for (const { callExpr, childConfig } of children) {
        const id = getNodeIdKey(childConfig);
        if (!id) {
          continue;
        }
        const deps = getRunAfterDepKeys(childConfig, variables);
        graph.set(id, deps);
        nodeCallExprs.set(id, callExpr);
      }
      const reachabilityCache = /* @__PURE__ */ new Map();
      const redundantEdges = [];
      for (const [nodeId, deps] of graph.entries()) {
        if (deps.length < 2) {
          continue;
        }
        for (const dep of deps) {
          const otherDeps = deps.filter((d) => d !== dep);
          for (const otherDep of otherDeps) {
            const reachable = buildReachabilitySet(otherDep, graph, reachabilityCache);
            if (reachable.has(dep)) {
              const callExpr = nodeCallExprs.get(nodeId);
              const callee = callExpr.getCallee();
              const nodeTypeLabel = callee.split(".").at(-1)?.replace(/^\w/, (c) => c.toUpperCase()) ?? callee;
              redundantEdges.push({
                nodeId,
                redundantDepId: dep,
                throughId: otherDep,
                shape: callExpr,
                nodeTypeLabel
              });
              break;
            }
          }
        }
      }
      return redundantEdges;
    }
    __name(detectRedundantEdges, "detectRedundantEdges");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/order-uniqueness-validator.js
var require_order_uniqueness_validator = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/order-uniqueness-validator.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.validateOrderUniqueness = validateOrderUniqueness;
    function validateOrderUniqueness(children, diagnostics) {
      const seen = /* @__PURE__ */ new Map();
      for (const { childConfig } of children) {
        const orderShape = childConfig.get("order");
        const orderValue = orderShape?.ifNumber()?.getValue();
        if (orderValue === void 0) {
          continue;
        }
        const label = childConfig.get("label")?.ifString()?.getValue();
        const prior = seen.get(orderValue);
        if (prior) {
          diagnostics.error(orderShape ?? childConfig, `Duplicate order ${orderValue}: '${label}' and '${prior.label}' share order ${orderValue}`);
        } else {
          seen.set(orderValue, { label, shape: orderShape ?? childConfig });
        }
      }
    }
    __name(validateOrderUniqueness, "validateOrderUniqueness");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/lane-plugin.js
var require_lane_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/lane-plugin.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.LanePlugin = void 0;
    exports2.extractDecisionBranchIdMap = extractDecisionBranchIdMap;
    exports2.laneRecordToShape = laneRecordToShape;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var activity_ootb_maps_1 = require_activity_ootb_maps();
    var activity_plugin_1 = require_activity_plugin();
    var playbook_shapes_1 = require_playbook_shapes();
    var lane_config_transform_1 = require_lane_config_transform();
    var transform_utils_1 = require_transform_utils();
    var timer_attributes_transform_1 = require_timer_attributes_transform();
    var child_collection_torecord_1 = require_child_collection_torecord();
    var reference_resolver_1 = require_reference_resolver();
    var slugify_js_1 = require_slugify();
    var unique_names_js_1 = require_unique_names();
    var label_utils_1 = require_label_utils();
    var graph_utils_1 = require_graph_utils();
    var playbook_pill_resolver_1 = require_playbook_pill_resolver();
    var order_uniqueness_validator_1 = require_order_uniqueness_validator();
    function extractDecisionBranchIdMap(callExpr, resultRecord) {
      const actDefKey = callExpr.getArgument(0, false)?.getCode();
      if (actDefKey !== activity_ootb_maps_1.DECISION_ACTIVITY_DEF_KEY) {
        return void 0;
      }
      const related = resultRecord.getRelated();
      const branchesRecord = related.find((r) => r.getTable() === playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE && String(r.get("variable")?.getValue() ?? "") === activity_ootb_maps_1.DECISION_BRANCHES_VAR_SYS_ID);
      if (!branchesRecord) {
        return void 0;
      }
      const branchesJsonStr = String(branchesRecord.get("value")?.getValue() ?? "");
      if (!branchesJsonStr) {
        return void 0;
      }
      let branchesJson;
      try {
        branchesJson = JSON.parse(branchesJsonStr);
      } catch {
        return void 0;
      }
      const inputsObj = callExpr.getArgument(2)?.ifObject();
      const branchesArray = inputsObj?.get("branches")?.ifArray();
      if (!branchesArray) {
        return void 0;
      }
      const fluentBranches = branchesArray.getElements();
      const mapping = /* @__PURE__ */ new Map();
      const count = Math.min(fluentBranches.length, branchesJson.length);
      for (let i = 0; i < count; i++) {
        const fluentElem = fluentBranches[i];
        const jsonElem = branchesJson[i];
        if (!fluentElem || !jsonElem) {
          continue;
        }
        const fluentId = fluentElem.ifObject()?.get("id")?.ifString()?.getValue();
        const generatedId = jsonElem.id;
        if (fluentId && generatedId) {
          mapping.set(fluentId, generatedId);
        }
      }
      return mapping.size > 0 ? mapping : void 0;
    }
    __name(extractDecisionBranchIdMap, "extractDecisionBranchIdMap");
    function laneRecordToShape(record, database, guidToVarName, timerRecords, globalActivityMap, branchGuidMap) {
      return (0, lane_config_transform_1.buildLaneToShapeResult)(record, database, guidToVarName, timerRecords, globalActivityMap, branchGuidMap);
    }
    __name(laneRecordToShape, "laneRecordToShape");
    exports2.LanePlugin = sdk_build_core_1.Plugin.create({
      name: "LanePlugin",
      records: {
        [playbook_plugin_constants_1.LANE_TABLE]: {
          relationships: {
            [playbook_plugin_constants_1.ACTIVITY_TABLE]: {
              via: "lane",
              descendant: true
            }
          },
          toShape(record, { database }, guidToVarName, timerRecords) {
            if ((0, transform_utils_1.isPlaybookUnsupported)(record, database)) {
              return { success: false };
            }
            return (0, lane_config_transform_1.buildLaneToShapeResult)(record, database, guidToVarName, timerRecords);
          }
        }
      },
      shapes: [
        {
          shape: playbook_shapes_1.LaneShape,
          fileTypes: ["fluent"],
          async toRecord(laneShape, { factory, transform, diagnostics }) {
            const config = laneShape.getConfig();
            const injectedName = config.get("uniqueName")?.ifString()?.getValue();
            let computedName = injectedName;
            if (!computedName) {
              const explicitName = config.get("name")?.ifString()?.getValue();
              const labelVal = config.get("label")?.ifString()?.getValue();
              computedName = explicitName ?? (labelVal ? (0, slugify_js_1.slugifyString)(labelVal) : "");
            }
            const playbookBodyParamsNames = [];
            const lanesParamName = laneShape.getPlaybookBodyParamsName();
            if (lanesParamName) {
              playbookBodyParamsNames.push(lanesParamName);
            }
            const conditionPillContext = {
              format: "condition",
              factory,
              diagnostics,
              playbookBodyParamsNames,
              allowedSources: {
                paramsInputs: true,
                paramsState: false,
                paramsParentRecord: true,
                previousActivityOutputs: true,
                currentActivityLabelDesc: false,
                automationPlan: false
              }
            };
            const conditionToRunShape = config.get("conditionToRun");
            const resolvedConditionToRun = conditionToRunShape && !conditionToRunShape.isUndefined() ? await (0, playbook_pill_resolver_1.resolvePlaybookPill)(conditionToRunShape, conditionPillContext) : void 0;
            const laneRecord = await factory.createRecord({
              source: laneShape,
              table: playbook_plugin_constants_1.LANE_TABLE,
              explicitId: config.get("$id"),
              properties: config.transform(({ $ }) => ({
                label: $,
                name: $.val(computedName),
                order: $,
                active: $.def(playbook_plugin_constants_1.LANE_DEFAULTS.ACTIVE),
                description: $.def(""),
                start_rule: $.from("startRule").map((startRule) => (0, lane_config_transform_1.resolveStartRule)(startRule)),
                start_rule_name: $.from("startRule").map((startRule) => (0, lane_config_transform_1.resolveStartRule)(startRule)),
                starts_after_activities: $.from("startsAfterActivities"),
                starts_after_lanes: $.from("startsAfterLanes"),
                starts_after_branches: $.from("startsAfterBranches"),
                condition_to_run: resolvedConditionToRun ? $.val(resolvedConditionToRun) : $.from("conditionToRun"),
                restart_rule: $.from("restartRule"),
                process_definition: $.from("processDefinition"),
                permission: $.def(playbook_plugin_constants_1.LANE_DEFAULTS.PERMISSION)
              }))
            });
            let laneTimerRecord;
            const startWithDelayShape = config.get("startWithDelay");
            if (startWithDelayShape && !startWithDelayShape.isUndefined()) {
              const timerPillContext = {
                format: "standard",
                factory,
                diagnostics,
                playbookBodyParamsNames,
                allowedSources: {
                  paramsInputs: true,
                  paramsState: true,
                  paramsParentRecord: true,
                  previousActivityOutputs: true,
                  currentActivityLabelDesc: false,
                  automationPlan: false
                }
              };
              const processDefinition = config.get("processDefinition")?.ifString()?.getValue() ?? "";
              laneTimerRecord = await (0, timer_attributes_transform_1.timerAttributesToRecord)(startWithDelayShape.asObject(), laneRecord, processDefinition, "sys_pd_lane", factory, diagnostics, timerPillContext);
            }
            const activitiesArrow = laneShape.getActivities();
            const activitiesParamName = activitiesArrow?.getParameters()[0]?.getName();
            if (activitiesParamName && !playbookBodyParamsNames.includes(activitiesParamName)) {
              playbookBodyParamsNames.push(activitiesParamName);
            }
            const laneNowIdKeyToGuid = laneShape.getNowIdKeyToGuid();
            const activityTypeConfig = {
              configArgIndex: 1,
              createShape: /* @__PURE__ */ __name((source, cfg, context) => {
                const callExpr = source.as(sdk_build_core_1.CallExpressionShape);
                const arg2 = callExpr.getArgument(2)?.ifObject();
                const arg3 = callExpr.getArgument(3)?.ifObject();
                const argsObj = callExpr.getArgument(1)?.ifObject();
                return new playbook_shapes_1.ActivityShape({
                  source,
                  activityDefinition: callExpr.getArgument(0, false),
                  config: cfg,
                  inputs: arg2 ?? argsObj?.get("inputs")?.ifObject(),
                  experienceProperties: arg3 ?? argsObj?.get("experienceProperties")?.ifObject(),
                  playbookBodyParamsNames,
                  ...context?.nowIdKeyToGuid !== void 0 ? { nowIdKeyToGuid: context.nowIdKeyToGuid } : {}
                });
              }, "createShape"),
              childPlugin: activity_plugin_1.ActivityPlugin
            };
            const childTypeMap = /* @__PURE__ */ new Map([
              [playbook_plugin_constants_1.ACTIVITY_API_NAME, activityTypeConfig],
              ["wfa.playbook.activity", activityTypeConfig]
            ]);
            const activityRecords = activitiesArrow ? await (0, child_collection_torecord_1.processChildCollectionToRecord)({
              collectionArrow: activitiesArrow,
              parentRecord: laneRecord,
              parentFkField: "lane",
              calleeNames: ["wfa.playbook.activity", playbook_plugin_constants_1.ACTIVITY_API_NAME],
              childTypeMap,
              resolveReferences: reference_resolver_1.resolveStartsAfterActivities,
              resolvedRefField: "startsAfterActivities",
              resolveBranchReferences: reference_resolver_1.resolveStartsAfterBranches,
              resolvedBranchRefField: "startsAfterBranches",
              extractBranchIdMap: extractDecisionBranchIdMap,
              validateChildren: /* @__PURE__ */ __name((children, variables) => {
                (0, order_uniqueness_validator_1.validateOrderUniqueness)(children, diagnostics);
                const redundantEdges = (0, graph_utils_1.detectRedundantEdges)(children, variables);
                for (const { nodeId, redundantDepId, throughId, shape, nodeTypeLabel } of redundantEdges) {
                  diagnostics.warn(shape, `${nodeTypeLabel} "${nodeId}" has a redundant start rule dependency on "${redundantDepId}" because it is already reachable through "${throughId}".`);
                }
              }, "validateChildren"),
              computePerChildMergeProps: /* @__PURE__ */ __name((allConfigs, index) => {
                const entries = allConfigs.map((c) => {
                  const explicit = c.get("name")?.ifString()?.getValue();
                  return explicit ? { name: explicit, isExplicit: true } : {
                    name: (0, slugify_js_1.slugifyString)((0, label_utils_1.stripHtmlTags)(c.get("label")?.ifString()?.getValue() ?? "")),
                    isExplicit: false
                  };
                });
                const uniqueName = (0, unique_names_js_1.deduplicateNames)(entries)[index] ?? "";
                const entry = entries[index];
                if (entry?.isExplicit && entry.name !== uniqueName) {
                  diagnostics.warn(allConfigs[index] ?? laneShape, `The name "${entry.name}" is already in use by another activity; it has been renamed to "${uniqueName}" to keep names unique.`);
                }
                const processDefinition = String(laneRecord.get("process_definition")?.getValue() ?? "");
                if (!processDefinition) {
                  diagnostics.error(laneShape, "Process definition in lane is required");
                }
                return { uniqueName, processDefinition };
              }, "computePerChildMergeProps"),
              externalNowIdKeyToGuid: laneNowIdKeyToGuid,
              transform,
              diagnostics,
              errorMessage: "Lane activities function must return an object literal",
              errorSource: laneShape
            }) : [];
            return {
              success: true,
              value: laneRecord.with(...laneTimerRecord ? [laneTimerRecord] : [], ...activityRecords)
            };
          }
        }
      ],
      docs: []
    });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/child-collection-toshape.js
var require_child_collection_toshape = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/child-collection-toshape.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.buildChildCollectionToShape = buildChildCollectionToShape;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    function topologicalSort(records, getDependencyGuids) {
      const guidToRecord = /* @__PURE__ */ new Map();
      for (const record of records) {
        guidToRecord.set(record.getId().getValue(), record);
      }
      const sorted = [];
      const visited = /* @__PURE__ */ new Set();
      const visiting = /* @__PURE__ */ new Set();
      function visit(record) {
        const guid = record.getId().getValue();
        if (visited.has(guid)) {
          return;
        }
        if (visiting.has(guid)) {
          return;
        }
        visiting.add(guid);
        for (const depGuid of getDependencyGuids(record)) {
          const depRecord = guidToRecord.get(depGuid);
          if (depRecord) {
            visit(depRecord);
          }
        }
        visiting.delete(guid);
        visited.add(guid);
        sorted.push(record);
      }
      __name(visit, "visit");
      for (const record of records) {
        visit(record);
      }
      return sorted;
    }
    __name(topologicalSort, "topologicalSort");
    function buildChildCollectionToShape(config) {
      const { source, childRecords, database, varPrefix, recordToShape, getDependencyGuids } = config;
      const sortedRecords = getDependencyGuids ? topologicalSort(childRecords, getDependencyGuids) : childRecords;
      const guidToVarName = /* @__PURE__ */ new Map();
      for (const [i, record] of sortedRecords.entries()) {
        guidToVarName.set(record.getId().getValue(), `${varPrefix}_${i}`);
      }
      const childShapesWithIndex = [];
      for (const [i, record] of sortedRecords.entries()) {
        const result = recordToShape(record, database, guidToVarName);
        if (result.success) {
          childShapesWithIndex.push({ shape: result.value, originalIndex: i });
        }
      }
      if (childShapesWithIndex.length === 0) {
        return new sdk_build_plugins_1.ArrowFunctionShape({
          source,
          parameters: [],
          returnValue: new sdk_build_core_1.ObjectShape({ source, properties: {} }),
          implicitReturn: true
        });
      }
      const statements = [];
      const returnEntries = {};
      for (const { shape, originalIndex } of childShapesWithIndex) {
        const varName = `${varPrefix}_${originalIndex}`;
        statements.push(new sdk_build_core_1.VariableStatementShape({ source, variableName: varName, initializer: shape }));
        returnEntries[varName] = new sdk_build_core_1.IdentifierShape({ source, name: varName });
      }
      const returnValue = new sdk_build_core_1.ObjectShape({ source, properties: returnEntries });
      return new sdk_build_plugins_1.ArrowFunctionShape({
        source,
        parameters: [],
        statements,
        returnValue
      });
    }
    __name(buildChildCollectionToShape, "buildChildCollectionToShape");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/playbook-config-transform.js
var require_playbook_config_transform = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/playbook-config-transform.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.buildPlaybookConfigToShape = buildPlaybookConfigToShape;
    exports2.buildPlaybookRecordProperties = buildPlaybookRecordProperties;
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    function resolvePlaybookProcessTypeName(record, database) {
      const processType = record.get("process_type");
      const processTypeReference = processType.ifRecordId();
      if (processTypeReference?.hasPrimaryKey()) {
        return processTypeReference.getPrimaryKey();
      }
      const processTypeSysId = processType.ifString()?.getValue();
      if (!processTypeSysId) {
        return void 0;
      }
      return (0, playbook_plugin_constants_1.getPlaybookProcessTypeIdentifier)(processTypeSysId) ?? database?.query(playbook_plugin_constants_1.PLAYBOOK_PROCESS_TYPE_TABLE).find((processTypeRecord) => processTypeRecord.getId().getValue() === processTypeSysId)?.get("name")?.ifString()?.getValue();
    }
    __name(resolvePlaybookProcessTypeName, "resolvePlaybookProcessTypeName");
    function buildPlaybookConfigToShape(record, database) {
      return record.transform(({ $ }) => ({
        $id: $.val(sdk_build_plugins_1.NowIdShape.from(record)),
        label: $.from("label"),
        name: $.from("name"),
        description: $.from("description"),
        active: $.from("active").toBoolean().def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.ACTIVE),
        status: $.from("status").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.STATUS),
        schemaVersion: $.from("schema_version").toNumber().def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.MAX_SUPPORTED_SCHEMA_VERSION),
        restartable: $.from("restartable").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.RESTARTABLE),
        access: $.from("access").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.ACCESS),
        runStrategy: $.from("run_strategy").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.RUN_STRATEGY),
        processType: $.val(resolvePlaybookProcessTypeName(record, database)).def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.PROCESS_TYPE),
        allowAsNested: $.from("allow_as_nested").toBoolean().def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.ALLOW_AS_NESTED),
        executionType: $.from("execution_type").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.EXECUTION_TYPE),
        dataRetentionPeriodOverride: $.from("data_retention_period_override").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.DATA_RETENTION_PERIOD_OVERRIDE),
        snapshot: $.from("snapshot").def(""),
        designerState: $.from("designer_state").def(""),
        nowAssistPrompt: $.from("now_assist_prompt").def(""),
        nowAssistKb: $.from("now_assist_kb").def(""),
        copiedFrom: $.from("copied_from").def(""),
        copiedFromLabel: $.from("copied_from_label").def("")
      }));
    }
    __name(buildPlaybookConfigToShape, "buildPlaybookConfigToShape");
    function buildPlaybookRecordProperties(config, computedName, nameProvided, resolvedProcessType) {
      return config.transform(({ $ }) => ({
        // User-configurable fields
        label: $,
        // Use explicit name if provided, otherwise use computed name from label
        name: nameProvided ? $ : $.val(computedName),
        description: $.from("description").def(""),
        restartable: $.from("restartable").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.RESTARTABLE),
        run_strategy: $.from("runStrategy").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.RUN_STRATEGY),
        process_type: $.val(resolvedProcessType),
        allow_as_nested: $.from("allowAsNested").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.ALLOW_AS_NESTED),
        execution_type: $.from("executionType").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.EXECUTION_TYPE),
        run_strategy_on_process_definition: $.def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.RUN_STRATEGY_ON_PROCESS_DEFINITION),
        access: $.from("access").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.ACCESS),
        permission: $.def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.PERMISSION),
        public_access: $.def(false),
        start_non_blocking: $.def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.START_NON_BLOCKING),
        run_on_pad_engine: $.def(false),
        data_retention_period_override: $.from("dataRetentionPeriodOverride").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.DATA_RETENTION_PERIOD_OVERRIDE),
        snapshot: $.from("snapshot").def(""),
        designer_state: $.from("designerState").def(""),
        now_assist_prompt: $.from("nowAssistPrompt").def(""),
        now_assist_kb: $.from("nowAssistKb").def(""),
        copied_from: $.from("copiedFrom").def(""),
        copied_from_label: $.from("copiedFromLabel").def(""),
        // Hidden fields - always use defaults
        view_type: $.def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.VIEW_TYPE),
        engine_major_version: $.def(""),
        schema_version: $.from("schemaVersion").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.MAX_SUPPORTED_SCHEMA_VERSION),
        active: $.from("active").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.ACTIVE),
        status: $.from("status").def(playbook_plugin_constants_1.PLAYBOOK_DEFAULTS.STATUS)
      }));
    }
    __name(buildPlaybookRecordProperties, "buildPlaybookRecordProperties");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/DefinitionResolver.js
var require_DefinitionResolver = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/DefinitionResolver.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.getFactoryCallConfig = getFactoryCallConfig;
    exports2.extractVariableSysId = extractVariableSysId;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    function getFactoryCallConfig(shape) {
      const resolved = shape.isResolvable() ? shape.asResolvable().resolve() : shape;
      const directCE = resolved.if(sdk_build_core_1.CallExpressionShape);
      if (directCE) {
        return directCE.getArgument(0)?.ifObject();
      }
      if (resolved instanceof sdk_build_core_1.Record) {
        const source = resolved.getSource();
        if (source instanceof sdk_build_core_1.CallExpressionShape) {
          return source.getArgument(0)?.ifObject();
        }
      }
      return void 0;
    }
    __name(getFactoryCallConfig, "getFactoryCallConfig");
    function extractVariableSysId(shape) {
      const obj = shape.if(sdk_build_core_1.CallExpressionShape)?.getArgument(0)?.ifObject() ?? shape.ifObject();
      return obj?.get("sysId", false)?.ifString()?.getValue();
    }
    __name(extractVariableSysId, "extractVariableSysId");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/playbook-trigger-instance-plugin.js
var require_playbook_trigger_instance_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/playbook-trigger-instance-plugin.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.PlaybookTriggerInstancePlugin = void 0;
    exports2.triggerInstanceRecordToShape = triggerInstanceRecordToShape;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var playbook_trigger_types_1 = require_playbook_trigger_types();
    var playbook_shapes_1 = require_playbook_shapes();
    var DefinitionResolver_1 = require_DefinitionResolver();
    var playbook_pill_transformer_1 = require_playbook_pill_transformer();
    var sdk_build_plugins_2 = require("@servicenow/sdk-build-plugins");
    var transform_utils_1 = require_transform_utils();
    function triggerInstanceRecordToShape(record, descendants) {
      const triggerTypeGuid = record.get("trigger_type")?.ifString()?.getValue() ?? "";
      const identifier = playbook_trigger_types_1.TRIGGER_TYPE_SYS_ID_TO_IDENTIFIER[triggerTypeGuid];
      if (!identifier) {
        return { success: false };
      }
      const triggerTypeShape = new sdk_build_core_1.IdentifierShape({
        source: record,
        name: identifier
      });
      const configShape = record.transform(({ $ }) => ({
        $id: $.val(sdk_build_plugins_1.NowIdShape.from(record)),
        label: $.from("label")
      }));
      const triggerSysId = record.getId().getValue();
      const variableValueRecords = descendants.query(playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE).filter((vv) => {
        const docKey = vv.get("document_key")?.ifString()?.getValue() ?? "";
        return docKey === triggerSysId;
      });
      const inputProperties = {};
      for (const vvRecord of variableValueRecords) {
        const variableGuid = vvRecord.get("variable")?.ifString()?.getValue() ?? "";
        const value = vvRecord.get("value")?.ifString()?.getValue() ?? "";
        if (value === "") {
          continue;
        }
        const resolved = playbook_trigger_types_1.INPUT_SYS_ID_TO_FLUENT[variableGuid];
        if (!resolved) {
          continue;
        }
        switch (resolved.type) {
          case "boolean":
            inputProperties[resolved.name] = new sdk_build_core_1.BooleanShape({
              source: vvRecord,
              value: value === "true" || value === "1"
            });
            break;
          case "number":
            inputProperties[resolved.name] = new sdk_build_core_1.NumberShape({
              source: vvRecord,
              value: Number(value)
            });
            break;
          default:
            inputProperties[resolved.name] = sdk_build_core_1.Shape.from(vvRecord, value);
            break;
        }
      }
      const triggerInputsShape = new sdk_build_core_1.ObjectShape({
        source: record,
        properties: inputProperties
      });
      const mapperArrow = buildMapperArrow(descendants, triggerSysId, record);
      const args = [triggerTypeShape, configShape, triggerInputsShape];
      if (mapperArrow) {
        args.push(mapperArrow);
      }
      return {
        success: true,
        value: new sdk_build_core_1.CallExpressionShape({
          source: record,
          callee: playbook_plugin_constants_1.TRIGGER_INSTANCE_API_NAME,
          args
        })
      };
    }
    __name(triggerInstanceRecordToShape, "triggerInstanceRecordToShape");
    function buildMapperArrow(descendants, triggerSysId, source) {
      const elementMappings = descendants.query(playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE).filter((em) => em.get("id")?.ifString()?.getValue() === triggerSysId);
      const variableValues = descendants.query(playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE).filter((vv) => vv.get("document_key")?.ifString()?.getValue() === triggerSysId);
      const pillFields = /* @__PURE__ */ new Set();
      const properties = {};
      const pillContext = {
        activityMap: /* @__PURE__ */ new Map()
        // empty for trigger context
      };
      for (const em of elementMappings) {
        const field = em.get("field")?.ifString()?.getValue();
        const value = em.get("value")?.ifString()?.getValue();
        if (field && value) {
          const fluentKey = field === playbook_plugin_constants_1.PARENT_RECORD.ELEMENT ? playbook_plugin_constants_1.PARENT_RECORD_MAPPER_KEY : field;
          pillFields.add(fluentKey);
          if (fluentKey === playbook_plugin_constants_1.PARENT_RECORD_MAPPER_KEY && value === "{{triggerRecord}}") {
            continue;
          }
          const shape = (0, playbook_pill_transformer_1.transformStandardPills)(value, em, pillContext);
          if (shape) {
            properties[fluentKey] = shape;
          }
        }
      }
      const processInputs = descendants.query(playbook_plugin_constants_1.PROCESS_INPUT_TABLE);
      for (const vv of variableValues) {
        const variableSysId = vv.get("variable")?.ifString()?.getValue();
        const value = vv.get("value")?.ifString()?.getValue();
        if (!variableSysId || !value) {
          continue;
        }
        const processInput = processInputs.find((pi) => pi.getId().getValue() === variableSysId);
        if (!processInput) {
          continue;
        }
        const elementName = processInput.get("element")?.ifString()?.getValue();
        const fluentKey = elementName === playbook_plugin_constants_1.PARENT_RECORD.ELEMENT ? playbook_plugin_constants_1.PARENT_RECORD_MAPPER_KEY : elementName;
        if (!fluentKey || pillFields.has(fluentKey)) {
          continue;
        }
        properties[fluentKey] = sdk_build_core_1.Shape.from(vv, value);
      }
      if (Object.keys(properties).length === 0) {
        return void 0;
      }
      const parameters = Object.keys(properties).some((k) => pillFields.has(k)) ? [new sdk_build_core_1.IdentifierShape({ source, name: "trigger" })] : [];
      const returnObject = new sdk_build_core_1.ObjectShape({ source, properties });
      return new sdk_build_plugins_2.ArrowFunctionShape({
        source,
        parameters,
        returnValue: returnObject,
        implicitReturn: true
      });
    }
    __name(buildMapperArrow, "buildMapperArrow");
    exports2.PlaybookTriggerInstancePlugin = sdk_build_core_1.Plugin.create({
      name: "PlaybookTriggerInstancePlugin",
      records: {
        [playbook_plugin_constants_1.TRIGGER_INSTANCE_TABLE]: {
          relationships: {
            [playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE]: {
              via: "document_key",
              descendant: true
            },
            [playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE]: {
              via: "id",
              descendant: true
            }
          },
          toShape(record, { descendants, database }) {
            if ((0, transform_utils_1.isPlaybookUnsupported)(record, database)) {
              return { success: false };
            }
            return triggerInstanceRecordToShape(record, descendants);
          }
        },
        [playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE]: {
          coalesce: ["document_key", "variable"]
        },
        [playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE]: {
          coalesce: ["id", "field", "table"]
        }
      },
      shapes: [
        {
          shape: playbook_shapes_1.TriggerInstanceShape,
          fileTypes: ["fluent"],
          async toRecord(triggerShape, { factory }) {
            const triggerType = triggerShape.getTriggerType();
            const config = triggerShape.getConfig();
            const triggerInputs = triggerShape.getTriggerInputs();
            const triggerTypeConfig = (0, DefinitionResolver_1.getFactoryCallConfig)(triggerType);
            if (!triggerTypeConfig) {
              return { success: false };
            }
            const triggerTypeGuid = triggerTypeConfig.get("$id", false)?.ifString()?.getValue();
            if (!triggerTypeGuid) {
              return { success: false };
            }
            const triggerTypeInputs = triggerTypeConfig.get("inputs", false)?.ifObject();
            const labelVal = config.get("label")?.ifString()?.getValue() ?? "";
            const name = labelVal ? labelVal.toLowerCase().replace(/[^a-z0-9]+/g, "_").replace(/^_|_$/g, "") : "";
            const processDefinitionSysId = config.get("processDefinition")?.ifString()?.getValue() ?? "";
            const triggerRecord = await factory.createRecord({
              source: triggerShape,
              table: playbook_plugin_constants_1.TRIGGER_INSTANCE_TABLE,
              explicitId: config.get("$id"),
              properties: {
                trigger_type: triggerTypeGuid,
                label: labelVal,
                name,
                process_definition: processDefinitionSysId
              }
            });
            const childRecords = [];
            for (const [elementName, shape] of triggerInputs.entries()) {
              const inputShape = triggerTypeInputs?.get(elementName);
              const variableDefGuid = inputShape ? (0, DefinitionResolver_1.extractVariableSysId)(inputShape) : void 0;
              if (!variableDefGuid) {
                continue;
              }
              const value = shape.ifString()?.getValue() ?? String(shape.getValue?.() ?? "");
              const vvRecord = await factory.createRecord({
                source: shape,
                table: playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE,
                properties: {
                  document: playbook_plugin_constants_1.TRIGGER_INSTANCE_TABLE,
                  document_key: triggerRecord.getId().getValue(),
                  variable: variableDefGuid,
                  value
                }
              });
              childRecords.push(vvRecord);
            }
            const mapperMappings = triggerShape.getMapperMappings();
            const playbookInputSysIds = triggerShape.getPlaybookInputSysIds();
            const parentRecordInputSysId = triggerShape.getParentRecordInputSysId();
            let parentRecordSet = false;
            const triggerSysId = triggerRecord.getId().getValue();
            const glideVarTable = `var__m_${playbook_plugin_constants_1.PROCESS_INPUT_TABLE}_${processDefinitionSysId}`;
            if (mapperMappings && parentRecordInputSysId) {
              const parentRecordMapping = mapperMappings.get(playbook_plugin_constants_1.PARENT_RECORD_MAPPER_KEY);
              if (parentRecordMapping) {
                parentRecordSet = true;
                if (parentRecordMapping.isPill) {
                  const emRecord = await factory.createRecord({
                    source: triggerShape,
                    table: playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE,
                    properties: {
                      field: playbook_plugin_constants_1.PARENT_RECORD.ELEMENT,
                      id: triggerSysId,
                      table: glideVarTable,
                      value: parentRecordMapping.value
                    }
                  });
                  childRecords.push(emRecord);
                  const vvRecord = await factory.createRecord({
                    source: triggerShape,
                    table: playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE,
                    properties: {
                      document: playbook_plugin_constants_1.TRIGGER_INSTANCE_TABLE,
                      document_key: triggerSysId,
                      variable: parentRecordInputSysId,
                      value: ""
                    }
                  });
                  childRecords.push(vvRecord);
                } else {
                  const vvRecord = await factory.createRecord({
                    source: triggerShape,
                    table: playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE,
                    properties: {
                      document: playbook_plugin_constants_1.TRIGGER_INSTANCE_TABLE,
                      document_key: triggerSysId,
                      variable: parentRecordInputSysId,
                      value: parentRecordMapping.value
                    }
                  });
                  childRecords.push(vvRecord);
                  const emRecord = await factory.createRecord({
                    source: triggerShape,
                    table: playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE,
                    properties: {
                      field: playbook_plugin_constants_1.PARENT_RECORD.ELEMENT,
                      id: triggerSysId,
                      table: glideVarTable,
                      value: ""
                    }
                  });
                  childRecords.push(emRecord);
                }
              }
            }
            if (!parentRecordSet && triggerShape.getIsSameTableAsParent()) {
              const emRecord = await factory.createRecord({
                source: triggerShape,
                table: playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE,
                properties: {
                  field: playbook_plugin_constants_1.PARENT_RECORD.ELEMENT,
                  id: triggerSysId,
                  table: glideVarTable,
                  value: "{{triggerRecord}}"
                }
              });
              childRecords.push(emRecord);
              if (parentRecordInputSysId) {
                const vvRecord = await factory.createRecord({
                  source: triggerShape,
                  table: playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE,
                  properties: {
                    document: playbook_plugin_constants_1.TRIGGER_INSTANCE_TABLE,
                    document_key: triggerSysId,
                    variable: parentRecordInputSysId,
                    value: ""
                  }
                });
                childRecords.push(vvRecord);
              }
            }
            if (mapperMappings && processDefinitionSysId && playbookInputSysIds) {
              const glideVarTable2 = `var__m_${playbook_plugin_constants_1.PROCESS_INPUT_TABLE}_${processDefinitionSysId}`;
              const triggerSysId2 = triggerRecord.getId().getValue();
              for (const [elementName, mapperValue] of mapperMappings) {
                if (elementName === playbook_plugin_constants_1.PARENT_RECORD_MAPPER_KEY) {
                  continue;
                }
                const inputSysId = playbookInputSysIds.get(elementName);
                if (!inputSysId) {
                  continue;
                }
                if (mapperValue.isPill) {
                  const emRecord = await factory.createRecord({
                    source: triggerShape,
                    table: playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE,
                    properties: {
                      field: elementName,
                      id: triggerSysId2,
                      table: glideVarTable2,
                      value: mapperValue.value
                    }
                  });
                  childRecords.push(emRecord);
                  const vvRecord = await factory.createRecord({
                    source: triggerShape,
                    table: playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE,
                    properties: {
                      document: playbook_plugin_constants_1.TRIGGER_INSTANCE_TABLE,
                      document_key: triggerSysId2,
                      variable: inputSysId,
                      value: mapperValue.value
                    }
                  });
                  childRecords.push(vvRecord);
                } else {
                  const emRecord = await factory.createRecord({
                    source: triggerShape,
                    table: playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE,
                    properties: {
                      field: elementName,
                      id: triggerSysId2,
                      table: glideVarTable2,
                      value: ""
                    }
                  });
                  childRecords.push(emRecord);
                  const vvRecord = await factory.createRecord({
                    source: triggerShape,
                    table: playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE,
                    properties: {
                      document: playbook_plugin_constants_1.TRIGGER_INSTANCE_TABLE,
                      document_key: triggerSysId2,
                      variable: inputSysId,
                      value: mapperValue.value
                    }
                  });
                  childRecords.push(vvRecord);
                }
              }
            }
            return {
              success: true,
              value: triggerRecord.with(...childRecords)
            };
          }
        }
      ],
      docs: []
    });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/playbook-trigger-instance-diagnostics-plugin.js
var require_playbook_trigger_instance_diagnostics_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/playbook-trigger-instance-diagnostics-plugin.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.PlaybookTriggerInstanceDiagnosticsPlugin = void 0;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var playbook_shapes_1 = require_playbook_shapes();
    function getNumericValue(shape) {
      const value = shape.getValue?.();
      const num = typeof value === "number" ? value : Number(value);
      return Number.isNaN(num) ? void 0 : num;
    }
    __name(getNumericValue, "getNumericValue");
    function validateIntegerRange(shape, name, min, max, diagnostics) {
      const num = getNumericValue(shape);
      if (num === void 0) {
        diagnostics.error(shape, `${name} must be a number`);
      } else if (!Number.isInteger(num) || num < min || num > max) {
        diagnostics.error(shape, `${name} must be an integer between ${min} and ${max}`);
      }
    }
    __name(validateIntegerRange, "validateIntegerRange");
    var VALID_DAYS_OF_WEEK = /^[1-7]+$/;
    exports2.PlaybookTriggerInstanceDiagnosticsPlugin = sdk_build_core_1.Plugin.create({
      name: "PlaybookTriggerInstanceDiagnosticsPlugin",
      shapes: [
        {
          shape: playbook_shapes_1.TriggerInstanceShape,
          fileTypes: ["fluent"],
          toRecord(triggerShape, { diagnostics }) {
            const triggerInputs = triggerShape.getTriggerInputs();
            let repeatMonth, dayOfMonth;
            for (const [name, shape] of triggerInputs.entries()) {
              if (shape.isUndefined()) {
                continue;
              }
              switch (name) {
                case "limit":
                  validateIntegerRange(shape, "limit", 1, 1e3, diagnostics);
                  break;
                case "frequency":
                  validateIntegerRange(shape, "frequency", 1, Infinity, diagnostics);
                  break;
                case "dayOfMonth":
                  dayOfMonth = getNumericValue(shape);
                  break;
                case "repeatMonth":
                  repeatMonth = getNumericValue(shape);
                  break;
                case "daysOfTheWeek": {
                  const value = shape.ifString()?.getValue() ?? "";
                  if (!VALID_DAYS_OF_WEEK.test(value)) {
                    diagnostics.error(shape, "daysOfTheWeek must only contain digits 1-7");
                  } else if (new Set(value).size !== value.length) {
                    diagnostics.error(shape, "daysOfTheWeek must not contain duplicate digits");
                  }
                  break;
                }
              }
            }
            if (dayOfMonth !== void 0 && !Number.isNaN(dayOfMonth) && repeatMonth !== void 0 && !Number.isNaN(repeatMonth)) {
              if (repeatMonth === 2 && dayOfMonth > 29) {
                diagnostics.error(triggerShape, "Cannot choose dayOfMonth > 29 for February");
              } else if ((repeatMonth === 4 || repeatMonth === 6 || repeatMonth === 9 || repeatMonth === 11) && dayOfMonth > 30) {
                diagnostics.error(triggerShape, "Selected repeatMonth has 30 days, cannot choose dayOfMonth > 30");
              }
            }
            return { success: false };
          }
        }
      ],
      docs: []
    });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/playbook-inputs-mapper-transform.js
var require_playbook_inputs_mapper_transform = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/playbook-inputs-mapper-transform.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.parseMapperCallback = parseMapperCallback;
    exports2.emitMissingRequiredInputDiagnostics = emitMissingRequiredInputDiagnostics;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var table_field_resolver_1 = require_table_field_resolver();
    var playbook_pill_resolver_1 = require_playbook_pill_resolver();
    var playbook_shapes_1 = require_playbook_shapes();
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    async function parseMapperCallback(options) {
      const { mapperArrow, tableName, validInputs, compiler, rootDir, diagnostics, factory } = options;
      const mappings = /* @__PURE__ */ new Map();
      const params = mapperArrow.getParameters();
      const triggerParamName = params[0]?.getName() ?? "trigger";
      const tableFields = tableName ? (0, table_field_resolver_1.resolveTableFieldNames)(compiler, rootDir, tableName) : void 0;
      const returnExpr = extractReturnedObject(mapperArrow);
      if (!returnExpr) {
        for (const [inputName, info] of validInputs) {
          if (info.mandatory) {
            diagnostics.error(mapperArrow, `Required playbook input "${inputName}" is not mapped. The mapper must return an object.`);
          }
        }
        return mappings;
      }
      const pillContext = {
        format: "standard",
        factory,
        diagnostics,
        playbookBodyParamsNames: [],
        triggerParamName,
        allowedSources: {
          paramsInputs: false,
          paramsState: false,
          paramsParentRecord: false,
          previousActivityOutputs: false,
          currentActivityLabelDesc: false,
          automationPlan: false
        }
      };
      for (const [key, value] of returnExpr.entries({ resolve: false })) {
        if (key === playbook_plugin_constants_1.PARENT_RECORD_MAPPER_KEY) {
          const isPill2 = (0, playbook_shapes_1.containsDataPill)(value);
          const resolved2 = await (0, playbook_pill_resolver_1.resolvePlaybookPill)(value, pillContext);
          const serialized2 = resolved2.ifString()?.getValue();
          if (serialized2 === void 0) {
            diagnostics.error(value, "parentRecord must be a wfa.playbook.dataPill() call or a raw sysId string");
          } else if (isPill2 && !/^\{\{[^}]+\}\}$/.test(serialized2)) {
            diagnostics.error(value, "parentRecord must be a bare wfa.playbook.dataPill() call or a plain sys_id string. Template literals with embedded pills are not supported.");
          } else {
            mappings.set(playbook_plugin_constants_1.PARENT_RECORD_MAPPER_KEY, { value: serialized2, isPill: isPill2 });
          }
          continue;
        }
        if (!validInputs.has(key)) {
          diagnostics.error(value, `"${key}" is not a declared playbook input. Declare it in the PlaybookDefinition inputs.`);
          continue;
        }
        const isPill = (0, playbook_shapes_1.containsDataPill)(value);
        const resolved = await (0, playbook_pill_resolver_1.resolvePlaybookPill)(value, pillContext);
        const serialized = resolved.ifString()?.getValue();
        if (serialized === void 0) {
          diagnostics.error(value, "Mapper values must be wfa.playbook.dataPill() calls, template literals, or hardcoded values");
          continue;
        }
        if (isPill && tableFields && serialized.startsWith("{{triggerRecord.")) {
          const fieldMatch = serialized.match(/triggerRecord\.([^}]+)/);
          if (fieldMatch) {
            const fieldPath = fieldMatch[1];
            const topLevelField = fieldPath.split(".")[0];
            if (topLevelField && !tableFields.has(topLevelField)) {
              diagnostics.error(value, `Field "${topLevelField}" does not exist on table "${tableName}". Check the spelling or declare the column on the table.`);
            }
          }
        }
        mappings.set(key, { value: serialized, isPill });
      }
      for (const [inputName, info] of validInputs) {
        if (info.mandatory && !mappings.has(inputName)) {
          diagnostics.error(mapperArrow, `Required playbook input "${inputName}" is not mapped by the trigger's playbookInputs callback. Map it (e.g. \`${inputName}: wfa.playbook.dataPill(trigger.current.<field>)\`) or remove \`mandatory: true\` from its declaration.`);
        }
      }
      return mappings;
    }
    __name(parseMapperCallback, "parseMapperCallback");
    function emitMissingRequiredInputDiagnostics(triggerCallSite, validInputs, diagnostics) {
      for (const [inputName, info] of validInputs) {
        if (info.mandatory) {
          diagnostics.error(triggerCallSite, `Required playbook input "${inputName}" is not mapped. Add a playbookInputs callback to the trigger (\`(trigger) => ({ ${inputName}: wfa.playbook.dataPill(trigger.current.<field>) })\`) or remove \`mandatory: true\` from its declaration.`);
        }
      }
    }
    __name(emitMissingRequiredInputDiagnostics, "emitMissingRequiredInputDiagnostics");
    function extractReturnedObject(mapperArrow) {
      const directReturn = mapperArrow.getReturnValue();
      if (directReturn && directReturn.is(sdk_build_core_1.ObjectShape)) {
        return directReturn.as(sdk_build_core_1.ObjectShape);
      }
      const statements = mapperArrow.getStatements();
      if (statements.length === 1) {
        const only = statements[0];
        if (only && only.is(sdk_build_core_1.ObjectShape)) {
          return only.as(sdk_build_core_1.ObjectShape);
        }
      }
      return void 0;
    }
    __name(extractReturnedObject, "extractReturnedObject");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/trigger-array-processing.js
var require_trigger_array_processing = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/transforms/trigger-array-processing.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.processTriggersArrayToRecord = processTriggersArrayToRecord;
    exports2.buildTriggersArrayToShape = buildTriggersArrayToShape;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var playbook_shapes_1 = require_playbook_shapes();
    var playbook_trigger_instance_plugin_1 = require_playbook_trigger_instance_plugin();
    var playbook_trigger_instance_diagnostics_plugin_1 = require_playbook_trigger_instance_diagnostics_plugin();
    var playbook_inputs_mapper_transform_1 = require_playbook_inputs_mapper_transform();
    async function processTriggersArrayToRecord({ triggersObject, parentRecord, parentTable, parentRecordInputRecord, playbookInputRecords, compiler, rootDir, diagnostics, transform, factory }) {
      if (triggersObject.isUndefined()) {
        return [];
      }
      if (!triggersObject.is(sdk_build_core_1.ObjectShape)) {
        return [];
      }
      const obj = triggersObject.as(sdk_build_core_1.ObjectShape);
      const triggersValue = obj.get("triggers");
      if (!triggersValue || !triggersValue.is(sdk_build_core_1.ArrayShape)) {
        return [];
      }
      const validInputs = /* @__PURE__ */ new Map();
      const playbookInputSysIds = /* @__PURE__ */ new Map();
      if (playbookInputRecords) {
        for (const r of playbookInputRecords) {
          const elementName = r.get("element")?.ifString()?.getValue();
          if (!elementName) {
            continue;
          }
          const mandatory = Boolean(r.get("mandatory")?.toBoolean()?.getValue());
          validInputs.set(elementName, { mandatory });
          const sysId = r.getId().getValue();
          if (sysId) {
            playbookInputSysIds.set(elementName, sysId);
          }
        }
      }
      const triggersArray = triggersValue.as(sdk_build_core_1.ArrayShape);
      const elements = triggersArray.getElements();
      const records = [];
      for (const element of elements) {
        if (!element.is(sdk_build_core_1.CallExpressionShape)) {
          continue;
        }
        const callExpr = element.as(sdk_build_core_1.CallExpressionShape);
        if (!playbook_plugin_constants_1.TRIGGER_INSTANCE_CALLEE_NAMES.includes(callExpr.getCallee())) {
          continue;
        }
        const triggerType = callExpr.getArgument(0);
        const configArg = callExpr.getArgument(1)?.asObject();
        const triggerInputsArg = callExpr.getArgument(2)?.asObject();
        if (!configArg || !triggerInputsArg) {
          continue;
        }
        const configWithParent = configArg.merge({
          processDefinition: parentRecord.getId().getValue()
        });
        const triggerTable = triggerInputsArg.get("table")?.ifString()?.getValue() ?? "";
        let mapperMappings;
        const mapperArg = callExpr.getArgument(3);
        if (mapperArg && !mapperArg.isUndefined() && mapperArg.is(sdk_build_plugins_1.ArrowFunctionShape)) {
          const mapperArrow = mapperArg.as(sdk_build_plugins_1.ArrowFunctionShape);
          mapperMappings = await (0, playbook_inputs_mapper_transform_1.parseMapperCallback)({
            mapperArrow,
            tableName: triggerTable,
            validInputs,
            compiler,
            rootDir,
            diagnostics,
            factory
          });
        } else {
          (0, playbook_inputs_mapper_transform_1.emitMissingRequiredInputDiagnostics)(callExpr, validInputs, diagnostics);
        }
        if (parentTable && triggerTable && triggerTable !== parentTable) {
          if (!mapperMappings?.has(playbook_plugin_constants_1.PARENT_RECORD_MAPPER_KEY)) {
            diagnostics.error(callExpr, `parentRecord is required when the trigger table ("${triggerTable}") differs from the playbook parentTable ("${parentTable}"). Add parentRecord to the trigger's mapper callback  (e.g. (trigger) => ({ parentRecord: wfa.playbook.dataPill(trigger.current.record) })).`);
          }
          if (mapperMappings?.get(playbook_plugin_constants_1.PARENT_RECORD_MAPPER_KEY)?.value === "{{triggerRecord}}") {
            diagnostics.error(callExpr, "parentRecord should not be the trigger record when the trigger table differs from the playbook parentTable.");
          }
        }
        if (parentTable && triggerTable && triggerTable === parentTable && mapperMappings?.has(playbook_plugin_constants_1.PARENT_RECORD_MAPPER_KEY)) {
          diagnostics.error(callExpr, `parentRecord should not be specified when the trigger table ("${triggerTable}") matches the playbook parentTable ("${parentTable}").`);
        }
        const shape = new playbook_shapes_1.TriggerInstanceShape({
          source: callExpr,
          triggerType,
          config: configWithParent,
          triggerInputs: triggerInputsArg,
          parentRecordInputSysId: parentRecordInputRecord?.getId().getValue(),
          mapperMappings,
          playbookInputSysIds,
          isSameTableAsParent: !!parentTable && !!triggerTable && triggerTable === parentTable
        });
        await transform.toRecord(shape, playbook_trigger_instance_diagnostics_plugin_1.PlaybookTriggerInstanceDiagnosticsPlugin);
        const result = await transform.toRecord(shape, playbook_trigger_instance_plugin_1.PlaybookTriggerInstancePlugin);
        if (result.success) {
          records.push(result.value);
        }
      }
      return records;
    }
    __name(processTriggersArrayToRecord, "processTriggersArrayToRecord");
    function buildTriggersArrayToShape({ source, triggerRecords, descendants }) {
      if (triggerRecords.length === 0) {
        return void 0;
      }
      const triggerShapes = [];
      for (const record of triggerRecords) {
        const result = (0, playbook_trigger_instance_plugin_1.triggerInstanceRecordToShape)(record, descendants);
        if (result.success) {
          triggerShapes.push(result.value);
        }
      }
      if (triggerShapes.length === 0) {
        return void 0;
      }
      return new sdk_build_core_1.ArrayShape({
        source,
        elements: triggerShapes
      });
    }
    __name(buildTriggersArrayToShape, "buildTriggersArrayToShape");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/playbook-io-utils.js
var require_playbook_io_utils = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/utils/playbook-io-utils.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.buildPlaybookVariableRecords = buildPlaybookVariableRecords;
    exports2.buildPlaybookVariableShapes = buildPlaybookVariableShapes;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var column_1 = require("@servicenow/sdk-build-plugins/column");
    var GenericColumn = { name: "GenericColumn" };
    async function buildPlaybookVariableRecords(options) {
      const { variablesConfig, parentRecord, factory, diagnostics, parentTable, varTable } = options;
      const records = [];
      const tableName = `var__m_${varTable}_${parentRecord.getId().getValue()}`;
      const entries = variablesConfig.entries();
      for (const [i, [k, v]] of entries.entries()) {
        const callExpr = v.as(sdk_build_core_1.CallExpressionShape);
        const config = callExpr.getArgument(0).asObject();
        const orderFromShape = config.get("order")?.ifNumber()?.getValue();
        const internalType = callExpr.getCallee() === GenericColumn.name ? config.get("columnType")?.ifString()?.getValue() ?? "string" : column_1.COLUMN_API_TO_TYPE[callExpr.getCallee()] ?? "string";
        const maxLength = config.get("maxLength")?.ifNumber()?.getValue() ?? (0, column_1.getDefaultMaxLength)(internalType);
        const referenceTable = internalType === "reference" || internalType === "records" ? config.get("referenceTable")?.ifString()?.getValue() : void 0;
        const props = config.transform(({ $ }) => ({
          element: $.val(k),
          label: $.def(k),
          mandatory: $.toBoolean().def(false),
          active: $.val(true),
          order: $.val(orderFromShape ?? (i + 1) * 100),
          hint: $.def(""),
          model: $.val(parentRecord),
          model_id: $.val(parentRecord),
          model_table: $.val(parentTable),
          name: $.val(tableName),
          default_value: $.from("default").def(""),
          internal_type: $.val(internalType),
          max_length: $.val(maxLength),
          attributes: $.val(""),
          ...referenceTable ? { reference: $.val(referenceTable) } : {}
        }));
        const varRecord = await factory.createRecord({
          source: callExpr,
          table: varTable,
          properties: props
        });
        const updatedCallExpr = (0, column_1.addFieldsToColumn)({ name: k, table: tableName, display: false }, callExpr);
        const docRecords = await (0, column_1.getDocumentationRecords)(updatedCallExpr, {}, factory, diagnostics, true);
        records.push(...docRecords);
        records.push(varRecord);
      }
      return records;
    }
    __name(buildPlaybookVariableRecords, "buildPlaybookVariableRecords");
    function buildPlaybookVariableShapes(variableRecords, descendants) {
      const shapes = {};
      const sorted = [...variableRecords].sort((a, b) => {
        const aOrder = Number(a.get("order")?.ifString()?.getValue() ?? 0);
        const bOrder = Number(b.get("order")?.ifString()?.getValue() ?? 0);
        return aOrder - bOrder;
      });
      for (const variable of sorted) {
        const name = variable.get("element").asString().getValue();
        const internalType = variable.get("internal_type").asString().getValue();
        const callExpressionName = column_1.COLUMN_TYPE_TO_API[internalType] ?? GenericColumn.name;
        const variableName = variable.get("name").ifString()?.getValue();
        const documentation = variableName ? descendants.query("sys_documentation", { name: variableName, element: name }) : [];
        const hint = documentation[0]?.get("hint")?.ifString()?.getValue() ?? "";
        const defaultMaxLength = (0, column_1.getDefaultMaxLength)(internalType);
        const maxLength = Number(variable.get("max_length").asString().getValue());
        const orderValue = Number(variable.get("order")?.ifString()?.getValue() ?? 0);
        const referenceTable = variable.get("reference")?.ifString()?.getValue();
        shapes[name] = new sdk_build_core_1.CallExpressionShape({
          source: variable,
          callee: callExpressionName,
          args: [
            variable.transform(({ $ }) => ({
              label: $,
              mandatory: $.toBoolean().def(false),
              maxLength: $.val(maxLength).def(defaultMaxLength),
              default: $.from("default_value").def(""),
              ...hint ? { hint: $.val(hint) } : {},
              ...orderValue > 0 ? { order: $.val(orderValue) } : {},
              ...referenceTable && (internalType === "reference" || internalType === "records") ? { referenceTable: $.val(referenceTable) } : {}
            }))
          ]
        });
      }
      return shapes;
    }
    __name(buildPlaybookVariableShapes, "buildPlaybookVariableShapes");
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/playbook-definition-plugin.js
var require_playbook_definition_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/plugins/playbook-definition-plugin.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.PlaybookDefinitionPlugin = void 0;
    var sdk_build_core_1 = require("@servicenow/sdk-build-core");
    var sdk_build_plugins_1 = require("@servicenow/sdk-build-plugins");
    var playbook_plugin_constants_1 = require_playbook_plugin_constants();
    var slugify_js_1 = require_slugify();
    var unique_names_js_1 = require_unique_names();
    var label_utils_1 = require_label_utils();
    var lane_plugin_1 = require_lane_plugin();
    var activity_plugin_1 = require_activity_plugin();
    var playbook_shapes_1 = require_playbook_shapes();
    var child_collection_toshape_1 = require_child_collection_toshape();
    var child_collection_torecord_1 = require_child_collection_torecord();
    var playbook_config_transform_1 = require_playbook_config_transform();
    var reference_resolver_1 = require_reference_resolver();
    var graph_utils_1 = require_graph_utils();
    var order_uniqueness_validator_1 = require_order_uniqueness_validator();
    var activity_config_transform_1 = require_activity_config_transform();
    var trigger_array_processing_1 = require_trigger_array_processing();
    var transform_utils_1 = require_transform_utils();
    var playbook_io_utils_1 = require_playbook_io_utils();
    function buildInterleavedBodyToShape(source, laneRecords, decisionRecords, database, timerRecords, globalActivityMap) {
      const entries = [];
      for (const rec of laneRecords) {
        const order = rec.get("order")?.ifString()?.getValue();
        entries.push({ type: "lane", record: rec, order: order !== void 0 ? Number(order) : 0 });
      }
      for (const rec of decisionRecords) {
        const order = rec.get("order")?.ifString()?.getValue();
        entries.push({ type: "decision", record: rec, order: order !== void 0 ? Number(order) : 0 });
      }
      entries.sort((a, b) => a.order - b.order);
      let laneCounter = 0;
      let decisionCounter = 0;
      const guidToVarName = /* @__PURE__ */ new Map();
      const varNames = [];
      for (const entry of entries) {
        if (entry.type === "lane") {
          const varName = `lane_${laneCounter++}`;
          guidToVarName.set(entry.record.getId().getValue(), varName);
          varNames.push(varName);
        } else {
          const varName = `decision_${decisionCounter++}`;
          guidToVarName.set(entry.record.getId().getValue(), varName);
          varNames.push(varName);
        }
      }
      for (const entry of entries) {
        if (entry.type === "decision") {
          const varName = guidToVarName.get(entry.record.getId().getValue());
          globalActivityMap.set(entry.record.getId().getValue(), { varName });
        }
      }
      const pillContext = { activityMap: globalActivityMap };
      const branchGuidMap = /* @__PURE__ */ new Map();
      for (const entry of entries) {
        if (entry.type !== "decision") {
          continue;
        }
        const rec = entry.record;
        const varName = guidToVarName.get(rec.getId().getValue());
        const result = (0, activity_config_transform_1.activityRecordToShape)(rec, database, guidToVarName, branchGuidMap, pillContext, timerRecords);
        if (result.success && result.branchMap) {
          for (const [branchGuid, fluentBranchId] of result.branchMap) {
            branchGuidMap.set(branchGuid, { activityVarName: varName, fluentBranchId });
          }
        }
      }
      const statements = [];
      const returnEntries = {};
      for (const [i, entry] of entries.entries()) {
        const rec = entry.record;
        const varName = varNames[i];
        if (entry.type === "decision") {
          const result = (0, activity_config_transform_1.activityRecordToShape)(rec, database, guidToVarName, branchGuidMap, pillContext, timerRecords);
          if (result.success) {
            statements.push(new sdk_build_core_1.VariableStatementShape({ source, variableName: varName, initializer: result.value }));
            returnEntries[varName] = new sdk_build_core_1.IdentifierShape({ source, name: varName });
          }
        } else {
          const result = (0, lane_plugin_1.laneRecordToShape)(rec, database, guidToVarName, timerRecords, globalActivityMap, branchGuidMap);
          if (result.success) {
            statements.push(new sdk_build_core_1.VariableStatementShape({ source, variableName: varName, initializer: result.value }));
            returnEntries[varName] = new sdk_build_core_1.IdentifierShape({ source, name: varName });
          }
        }
      }
      if (statements.length === 0) {
        return new sdk_build_plugins_1.ArrowFunctionShape({
          source,
          parameters: [],
          returnValue: new sdk_build_core_1.ObjectShape({ source, properties: {} }),
          implicitReturn: true
        });
      }
      return new sdk_build_plugins_1.ArrowFunctionShape({
        source,
        parameters: [],
        statements,
        returnValue: new sdk_build_core_1.ObjectShape({ source, properties: returnEntries })
      });
    }
    __name(buildInterleavedBodyToShape, "buildInterleavedBodyToShape");
    exports2.PlaybookDefinitionPlugin = sdk_build_core_1.Plugin.create({
      name: "PlaybookDefinitionPlugin",
      records: {
        [playbook_plugin_constants_1.PLAYBOOK_PROCESS_TYPE_TABLE]: {
          coalesce: ["name"]
        },
        [playbook_plugin_constants_1.TIMER_ATTRIBUTES_TABLE]: {
          coalesce: ["source", "source_type"]
        },
        [playbook_plugin_constants_1.PROCESS_INPUT_TABLE]: {
          coalesce: ["element", "model"]
        },
        [playbook_plugin_constants_1.PLAYBOOK_OUTPUT_TABLE]: {
          coalesce: ["element", "model"]
        },
        [playbook_plugin_constants_1.PLAYBOOK_TABLE]: {
          relationships: {
            [playbook_plugin_constants_1.PLAYBOOK_PROCESS_TYPE_TABLE]: {
              via: "process_type",
              inverse: true
            },
            [playbook_plugin_constants_1.PROCESS_INPUT_TABLE]: {
              via: "model",
              descendant: true,
              relationships: {
                sys_documentation: {
                  via: { name: "name", element: "element" },
                  descendant: true
                },
                sys_choice: {
                  via: { name: "name", element: "element" },
                  descendant: true
                }
              }
            },
            [playbook_plugin_constants_1.PLAYBOOK_OUTPUT_TABLE]: {
              via: "model",
              descendant: true,
              relationships: {
                sys_documentation: {
                  via: { name: "name", element: "element" },
                  descendant: true
                },
                sys_choice: {
                  via: { name: "name", element: "element" },
                  descendant: true
                }
              }
            },
            [playbook_plugin_constants_1.TRIGGER_INSTANCE_TABLE]: {
              via: "process_definition",
              descendant: true,
              relationships: {
                [playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE]: {
                  via: "document_key",
                  descendant: true
                },
                [playbook_plugin_constants_1.SYS_ELEMENT_MAPPING_TABLE]: {
                  via: "id",
                  descendant: true
                }
              }
            },
            [playbook_plugin_constants_1.LANE_TABLE]: {
              via: "process_definition",
              descendant: true,
              relationships: {
                [playbook_plugin_constants_1.ACTIVITY_TABLE]: {
                  via: "lane",
                  descendant: true,
                  relationships: {
                    [playbook_plugin_constants_1.SYS_VARIABLE_VALUE_TABLE]: {
                      via: "document_key",
                      descendant: true
                    },
                    sys_element_mapping: {
                      via: "id",
                      descendant: true
                    }
                  }
                }
              }
            },
            [playbook_plugin_constants_1.TIMER_ATTRIBUTES_TABLE]: {
              via: "process_definition",
              descendant: true
            },
            [playbook_plugin_constants_1.ACTIVITY_TABLE]: {
              via: "process_definition",
              descendant: true
            },
            [playbook_plugin_constants_1.VARIANT_TABLE]: {
              via: "process_definition",
              descendant: true
            }
          },
          async toShape(record, { descendants, database, logger }) {
            if ((0, transform_utils_1.isPlaybookUnsupported)(record, database, logger, descendants)) {
              return { success: false };
            }
            const configShape = (0, playbook_config_transform_1.buildPlaybookConfigToShape)(record, database);
            const processInputRecords = descendants.query(playbook_plugin_constants_1.PROCESS_INPUT_TABLE);
            const parentRecordInput = processInputRecords.find((r) => r.get("element")?.ifString()?.getValue() === playbook_plugin_constants_1.PARENT_RECORD.ELEMENT);
            const parentTableRef = parentRecordInput?.get("reference")?.ifString()?.getValue();
            const finalConfigShape = parentTableRef ? configShape.merge({ parentTable: parentTableRef }) : configShape;
            const timerRecords = descendants.query(playbook_plugin_constants_1.TIMER_ATTRIBUTES_TABLE);
            const globalActivityMap = /* @__PURE__ */ new Map();
            const configExtensions = {};
            const nonParentInputRecords = processInputRecords.filter((r) => r.get("element")?.ifString()?.getValue() !== playbook_plugin_constants_1.PARENT_RECORD.ELEMENT);
            const inputs = (0, playbook_io_utils_1.buildPlaybookVariableShapes)(nonParentInputRecords, descendants);
            if (inputs && Object.keys(inputs).length > 0) {
              configExtensions["inputs"] = new sdk_build_core_1.ObjectShape({
                source: record,
                properties: inputs
              });
            }
            const outputs = (0, playbook_io_utils_1.buildPlaybookVariableShapes)(descendants.query(playbook_plugin_constants_1.PLAYBOOK_OUTPUT_TABLE), descendants);
            if (outputs && Object.keys(outputs).length > 0) {
              configExtensions["outputs"] = new sdk_build_core_1.ObjectShape({
                source: record,
                properties: outputs
              });
            }
            const triggerRecords = descendants.query(playbook_plugin_constants_1.TRIGGER_INSTANCE_TABLE);
            const triggersArray = (0, trigger_array_processing_1.buildTriggersArrayToShape)({
              source: record,
              triggerRecords,
              descendants
            });
            const finalConfigWithIO = finalConfigShape.merge(configExtensions);
            const triggersCallbackShape = new sdk_build_core_1.ObjectShape({
              source: record,
              properties: {
                triggers: triggersArray ?? new sdk_build_core_1.ArrayShape({
                  source: record,
                  elements: []
                })
              }
            });
            const laneRecords = descendants.query(playbook_plugin_constants_1.LANE_TABLE);
            const allActivities = descendants.query(playbook_plugin_constants_1.ACTIVITY_TABLE);
            const stageLevelDecisions = allActivities.filter((rec) => {
              const laneVal = rec.get("lane")?.ifString()?.getValue() ?? "";
              return laneVal === "";
            });
            if (stageLevelDecisions.length === 0) {
              const lanesArrow2 = (0, child_collection_toshape_1.buildChildCollectionToShape)({
                source: record,
                childRecords: laneRecords,
                database,
                varPrefix: "lane",
                recordToShape: /* @__PURE__ */ __name((rec, db, guidMap) => (0, lane_plugin_1.laneRecordToShape)(rec, db, guidMap, timerRecords, globalActivityMap), "recordToShape"),
                getDependencyGuids: /* @__PURE__ */ __name((rec) => {
                  const raw = rec.get("starts_after_lanes")?.ifString()?.getValue() ?? "";
                  return raw.split(",").map((g) => g.trim()).filter(Boolean);
                }, "getDependencyGuids")
              });
              const bodyShape2 = new sdk_build_core_1.ObjectShape({
                source: record,
                properties: { lanes: lanesArrow2 }
              });
              return {
                success: true,
                value: new sdk_build_core_1.CallExpressionShape({
                  source: record,
                  callee: playbook_plugin_constants_1.PLAYBOOK_API_NAME,
                  args: [finalConfigWithIO, triggersCallbackShape, bodyShape2]
                })
              };
            }
            const lanesArrow = buildInterleavedBodyToShape(record, laneRecords, stageLevelDecisions, database, timerRecords, globalActivityMap);
            const bodyShape = new sdk_build_core_1.ObjectShape({
              source: record,
              properties: { lanes: lanesArrow }
            });
            return {
              success: true,
              value: new sdk_build_core_1.CallExpressionShape({
                source: record,
                callee: playbook_plugin_constants_1.PLAYBOOK_API_NAME,
                args: [finalConfigWithIO, triggersCallbackShape, bodyShape]
              })
            };
          }
        }
      },
      shapes: [
        {
          shape: sdk_build_core_1.CallExpressionShape,
          fileTypes: ["fluent"],
          async toRecord(callExpression, { factory, transform, diagnostics, compiler, project, logger }) {
            if (callExpression.getCallee() !== playbook_plugin_constants_1.PLAYBOOK_API_NAME) {
              return { success: false };
            }
            const config = callExpression.getArgument(0).asObject();
            const playbookLabel = config.get("label")?.ifString()?.getValue() ?? "";
            logger.info(`Playbook "${playbookLabel}" built. For any new changes to take effect at runtime, open Playbook Designer and activate the playbook again.`);
            const nameShape = config.get("name");
            const nameProvided = nameShape?.isDefined() && !nameShape?.isUndefined();
            let computedName;
            if (!nameProvided) {
              const labelVal = config.get("label")?.ifString()?.getValue();
              computedName = labelVal ? (0, slugify_js_1.slugifyString)(labelVal) : "";
            }
            const processTypeShape = config.get("processType");
            const processTypeName = processTypeShape.ifString()?.getValue()?.trim();
            const mappedProcessTypeSysId = processTypeName && processTypeName !== "" ? (0, playbook_plugin_constants_1.getPlaybookProcessTypeSysId)(processTypeName) : void 0;
            if (processTypeName && processTypeName !== "" && !mappedProcessTypeSysId) {
              diagnostics.error(processTypeShape, `Unsupported processType "${processTypeName}". We do not have that process type defined in the SDK mapping.`);
              return { success: false };
            }
            const resolvedProcessType = processTypeName && processTypeName !== "" ? mappedProcessTypeSysId : playbook_plugin_constants_1.PLAYBOOK_DEFAULT_PROCESS_TYPE_DB;
            const playbookRecord = await factory.createRecord({
              source: callExpression,
              table: playbook_plugin_constants_1.PLAYBOOK_TABLE,
              explicitId: config.get("$id"),
              properties: (0, playbook_config_transform_1.buildPlaybookRecordProperties)(config, computedName, !!nameProvided, resolvedProcessType)
            });
            const parentTableVal = config.get("parentTable")?.ifString()?.getValue();
            let parentRecordInputRecord;
            if (parentTableVal) {
              parentRecordInputRecord = await factory.createRecord({
                source: callExpression,
                table: playbook_plugin_constants_1.PROCESS_INPUT_TABLE,
                properties: {
                  element: playbook_plugin_constants_1.PARENT_RECORD.ELEMENT,
                  label: playbook_plugin_constants_1.PARENT_RECORD.LABEL,
                  internal_type: playbook_plugin_constants_1.PARENT_RECORD.INTERNAL_TYPE,
                  reference: parentTableVal,
                  order: playbook_plugin_constants_1.PARENT_RECORD.ORDER,
                  mandatory: false,
                  active: true,
                  max_length: playbook_plugin_constants_1.PARENT_RECORD.MAX_LENGTH,
                  model: playbookRecord,
                  model_id: playbookRecord,
                  model_table: "",
                  name: `var__m_${playbook_plugin_constants_1.PROCESS_INPUT_TABLE}_${playbookRecord.getId().getValue()}`,
                  attributes: playbook_plugin_constants_1.PARENT_RECORD.ATTRIBUTES
                }
              });
            }
            const triggersCallback = callExpression.getArgument(1);
            const relatedRecords = [];
            {
              const inputsConfig = config.get("inputs")?.ifObject()?.asObject();
              if (inputsConfig) {
                const RESERVED_INPUT_NAMES = [playbook_plugin_constants_1.PARENT_RECORD.ELEMENT, playbook_plugin_constants_1.PARENT_RECORD_MAPPER_KEY];
                const parentRecordKey = inputsConfig.keys().find((k) => RESERVED_INPUT_NAMES.includes(k));
                if (parentRecordKey) {
                  diagnostics.error(inputsConfig.get(parentRecordKey), `"${parentRecordKey}" is a reserved input name. Use parentRecord in the input mapper to set the parent record instead.`);
                } else if (!parentTableVal) {
                  diagnostics.error(inputsConfig, "Defining inputs requires a parentTable in the playbook config.");
                } else {
                  const inputRecords = await (0, playbook_io_utils_1.buildPlaybookVariableRecords)({
                    variablesConfig: inputsConfig,
                    parentRecord: playbookRecord,
                    factory,
                    diagnostics,
                    parentTable: playbook_plugin_constants_1.PLAYBOOK_TABLE,
                    varTable: playbook_plugin_constants_1.PROCESS_INPUT_TABLE
                  });
                  const patchedInputs = inputRecords.map((r) => {
                    if (r.getTable() !== playbook_plugin_constants_1.PROCESS_INPUT_TABLE) {
                      return r;
                    }
                    return r.merge({
                      column_label: r.get("label").getValue(),
                      attributes: playbook_plugin_constants_1.PLAYBOOK_INPUT_ATTRIBUTES,
                      model_table: "",
                      use_reference_qualifier: "simple",
                      virtual_type: "script"
                    });
                  });
                  relatedRecords.push(...patchedInputs);
                }
              }
              const outputsConfig = config.get("outputs")?.ifObject()?.asObject();
              if (outputsConfig) {
                if (!parentTableVal) {
                  diagnostics.error(outputsConfig, "Defining outputs requires a parentTable in the playbook config.");
                } else {
                  const outputRecords = await (0, playbook_io_utils_1.buildPlaybookVariableRecords)({
                    variablesConfig: outputsConfig,
                    parentRecord: playbookRecord,
                    factory,
                    diagnostics,
                    parentTable: playbook_plugin_constants_1.PLAYBOOK_TABLE,
                    varTable: playbook_plugin_constants_1.PLAYBOOK_OUTPUT_TABLE
                  });
                  const patchedOutputs = outputRecords.map((r) => {
                    if (r.getTable() !== playbook_plugin_constants_1.PLAYBOOK_OUTPUT_TABLE) {
                      return r;
                    }
                    return r.merge({
                      column_label: r.get("label").getValue(),
                      model_table: "",
                      use_reference_qualifier: "simple",
                      virtual_type: "script"
                    });
                  });
                  relatedRecords.push(...patchedOutputs);
                }
              }
            }
            const playbookInputOnlyRecords = relatedRecords.filter((r) => r.getTable() === playbook_plugin_constants_1.PROCESS_INPUT_TABLE);
            const triggerRecords = triggersCallback ? await (0, trigger_array_processing_1.processTriggersArrayToRecord)({
              triggersObject: triggersCallback,
              parentRecord: playbookRecord,
              parentTable: parentTableVal,
              parentRecordInputRecord,
              playbookInputRecords: playbookInputOnlyRecords,
              compiler,
              rootDir: project.getRootDir(),
              diagnostics,
              transform,
              factory
            }) : [];
            const bodyArg = callExpression.getArgument(2);
            const lanesArrow = bodyArg?.asObject().get("lanes");
            const lanesArrowFn = lanesArrow?.is(sdk_build_plugins_1.ArrowFunctionShape) ? lanesArrow.as(sdk_build_plugins_1.ArrowFunctionShape) : void 0;
            const playbookBodyParamsName = lanesArrowFn?.getParameters()[0]?.getName();
            const playbookBodyParamsNames = [];
            if (playbookBodyParamsName) {
              playbookBodyParamsNames.push(playbookBodyParamsName);
            }
            const activityTypeConfig = {
              configArgIndex: 1,
              createShape: /* @__PURE__ */ __name((source, cfg, context) => {
                const callExpr = source.as(sdk_build_core_1.CallExpressionShape);
                const arg2 = callExpr.getArgument(2)?.ifObject();
                const arg3 = callExpr.getArgument(3)?.ifObject();
                const argsObj = callExpr.getArgument(1)?.ifObject();
                return new playbook_shapes_1.ActivityShape({
                  source,
                  activityDefinition: callExpr.getArgument(0, false),
                  config: cfg,
                  inputs: arg2 ?? argsObj?.get("inputs")?.ifObject(),
                  experienceProperties: arg3 ?? argsObj?.get("experienceProperties")?.ifObject(),
                  ...context?.nowIdKeyToGuid !== void 0 ? { nowIdKeyToGuid: context.nowIdKeyToGuid } : {},
                  playbookBodyParamsNames
                });
              }, "createShape"),
              childPlugin: activity_plugin_1.ActivityPlugin
            };
            const laneTypeConfig = {
              createShape: /* @__PURE__ */ __name((source, cfg, context) => {
                const laneArg = source.is(sdk_build_core_1.CallExpressionShape) ? source.as(sdk_build_core_1.CallExpressionShape).getArgument(0)?.ifObject() : void 0;
                const activitiesShape = laneArg?.get("activities");
                const activities = activitiesShape?.is(sdk_build_plugins_1.ArrowFunctionShape) ? activitiesShape.as(sdk_build_plugins_1.ArrowFunctionShape) : void 0;
                return new playbook_shapes_1.LaneShape({
                  source,
                  config: cfg,
                  activities,
                  ...context?.nowIdKeyToGuid !== void 0 ? { nowIdKeyToGuid: context.nowIdKeyToGuid } : {},
                  playbookBodyParamsName
                });
              }, "createShape"),
              childPlugin: lane_plugin_1.LanePlugin
            };
            const childTypeMap = /* @__PURE__ */ new Map([
              ["wfa.playbook.lane", laneTypeConfig],
              ["Lane", laneTypeConfig],
              [playbook_plugin_constants_1.ACTIVITY_API_NAME, activityTypeConfig],
              ["wfa.playbook.activity", activityTypeConfig]
            ]);
            const allRecords = lanesArrow?.is(sdk_build_plugins_1.ArrowFunctionShape) ? await (0, child_collection_torecord_1.processChildCollectionToRecord)({
              collectionArrow: lanesArrow.as(sdk_build_plugins_1.ArrowFunctionShape),
              parentRecord: playbookRecord,
              parentFkField: "processDefinition",
              calleeNames: ["wfa.playbook.lane", "Lane", playbook_plugin_constants_1.ACTIVITY_API_NAME, "wfa.playbook.activity"],
              childTypeMap,
              resolveReferences: reference_resolver_1.resolveStartsAfterLanes,
              resolvedRefField: "startsAfterLanes",
              resolveBranchReferences: reference_resolver_1.resolveStartsAfterBranches,
              resolvedBranchRefField: "startsAfterBranches",
              resolvedBranchActivityField: "startsAfterActivities",
              extractBranchIdMap: lane_plugin_1.extractDecisionBranchIdMap,
              computePerChildMergeProps: /* @__PURE__ */ __name((allConfigs, index, allCallees) => {
                const activityCallees = /* @__PURE__ */ new Set([playbook_plugin_constants_1.ACTIVITY_API_NAME, "wfa.playbook.activity"]);
                const entries = allConfigs.map((c, i) => {
                  const explicit = c.get("name")?.ifString()?.getValue();
                  if (explicit) {
                    return { name: explicit, isExplicit: true };
                  }
                  const label = c.get("label")?.ifString()?.getValue() ?? "";
                  const slug = (0, slugify_js_1.slugifyString)(activityCallees.has(allCallees[i] ?? "") ? (0, label_utils_1.stripHtmlTags)(label) : label);
                  return { name: slug, isExplicit: false };
                });
                const uniqueName = (0, unique_names_js_1.deduplicateNames)(entries)[index] ?? "";
                const entry = entries[index];
                if (entry?.isExplicit && entry.name !== uniqueName) {
                  diagnostics.warn(allConfigs[index] ?? callExpression, `The name "${entry.name}" is already in use by another activity; it has been renamed to "${uniqueName}" to keep names unique.`);
                }
                return { uniqueName };
              }, "computePerChildMergeProps"),
              validateChildren: /* @__PURE__ */ __name((children, variables) => {
                (0, order_uniqueness_validator_1.validateOrderUniqueness)(children, diagnostics);
                const redundantEdges = (0, graph_utils_1.detectRedundantEdges)(children, variables);
                for (const { nodeId, redundantDepId, throughId, shape, nodeTypeLabel } of redundantEdges) {
                  diagnostics.warn(shape, `${nodeTypeLabel} "${nodeId}" has a redundant start rule dependency on "${redundantDepId}" because it is already reachable through "${throughId}".`);
                }
              }, "validateChildren"),
              transform,
              diagnostics,
              errorMessage: "PlaybookDefinition lanes function must return an object literal",
              errorSource: callExpression
            }) : [];
            const allChildRecords = [
              ...allRecords,
              ...triggerRecords,
              ...relatedRecords,
              ...parentRecordInputRecord ? [parentRecordInputRecord] : []
            ];
            return {
              success: true,
              value: playbookRecord.with(...allChildRecords)
            };
          }
        }
      ],
      docs: []
    });
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/index.js
var require_playbook = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/playbook/index.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    exports2.playbookPlugins = void 0;
    var playbook_diagnostics_plugin_js_1 = require_playbook_diagnostics_plugin();
    var playbook_datapill_diagnostics_plugin_js_1 = require_playbook_datapill_diagnostics_plugin();
    var lane_diagnostics_plugin_js_1 = require_lane_diagnostics_plugin();
    var activity_diagnostics_plugin_js_1 = require_activity_diagnostics_plugin();
    var playbook_definition_plugin_js_1 = require_playbook_definition_plugin();
    var playbook_trigger_instance_plugin_js_1 = require_playbook_trigger_instance_plugin();
    var playbook_trigger_instance_diagnostics_plugin_js_1 = require_playbook_trigger_instance_diagnostics_plugin();
    var lane_plugin_js_1 = require_lane_plugin();
    var activity_plugin_js_1 = require_activity_plugin();
    exports2.playbookPlugins = [
      playbook_datapill_diagnostics_plugin_js_1.PlaybookDataPillDiagnosticsPlugin,
      lane_diagnostics_plugin_js_1.LaneDiagnosticsPlugin,
      activity_diagnostics_plugin_js_1.ActivityDiagnosticsPlugin,
      playbook_trigger_instance_diagnostics_plugin_js_1.PlaybookTriggerInstanceDiagnosticsPlugin,
      playbook_trigger_instance_plugin_js_1.PlaybookTriggerInstancePlugin,
      lane_plugin_js_1.LanePlugin,
      activity_plugin_js_1.ActivityPlugin,
      playbook_definition_plugin_js_1.PlaybookDefinitionPlugin,
      playbook_diagnostics_plugin_js_1.PlaybookDiagnosticsPlugin
    ];
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/index.js
var require_plugin = __commonJS({
  "../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/plugin/index.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    var index_js_1 = require_playbook();
    exports2.default = [...index_js_1.playbookPlugins];
  }
});

// ../../node_modules/.pnpm/@servicenow+sdk-playbook-plugin@1.0.1-ext-playbook-plugin.16_@servicenow+sdk-build-core_d14af2ff2a7b4a2632b2428e9260c2ad/node_modules/@servicenow/sdk-playbook-plugin/dist/index.js
var __importDefault = exports && exports.__importDefault || function(mod) {
  return mod && mod.__esModule ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = void 0;
var plugin_1 = require_plugin();
Object.defineProperty(exports, "default", { enumerable: true, get: /* @__PURE__ */ __name(function() {
  return __importDefault(plugin_1).default;
}, "get") });
//# sourceMappingURL=playbook.js.map
