"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getEslintConfig = exports.getLocalModuleEslintConfig = exports.getPluginRules = void 0;
const globals = __importStar(require("globals"));
const omit_1 = __importDefault(require("lodash/omit"));
const util_1 = require("@servicenow/glide/util");
const eslint_utils_1 = require("@eslint-community/eslint-utils");
const BROWSER_API_WARNING = 'Web APIs are not supported by the now platform';
const GLIDE_API_WARNING = 'Glide APIs are not supported by the now platform when used from a 3rd party dependency';
const NO_GLOBAL_THIS = 'ES2020 `globalThis` variable is not supported by the now platform';
const glideAPIs = util_1.GlideAPIs;
const globalsAllowList = ['console', 'fetch'];
const coreNodeModules = [
    'assert',
    'buffer',
    'child_process',
    'cluster',
    'crypto',
    'dgram',
    'dns',
    'domain',
    'events',
    'freelist',
    'fs',
    'http',
    'https',
    'module',
    'net',
    'os',
    'path',
    'punycode',
    'querystring',
    'readline',
    'repl',
    'smalloc',
    'stream',
    'string_decoder',
    'sys',
    'timers',
    'tls',
    'tracing',
    'tty',
    'url',
    'util',
    'vm',
    'zlib',
];
const getNodeGlobals = () => Object.keys(globals.nodeBuiltin).filter((key) => !globalsAllowList.includes(key));
function createTrackMap(isNodeGlobals) {
    const trackMapItems = isNodeGlobals ? getNodeGlobals() : coreNodeModules;
    const trackMap = {};
    for (const item of trackMapItems) {
        trackMap[item] = {
            [eslint_utils_1.ReferenceTracker.READ]: {
                name: item,
            },
        };
    }
    return trackMap;
}
/**
 * Custom rule: disallow unsupported Node.js built-in APIs.
 * Replaces @servicenow/eslint-plugin-sdk-app-plugin/no-unsupported-node-builtins.
 */
const noUnsupportedNodeBuiltins = {
    meta: {
        docs: {
            description: 'disallow unsupported Node.js built-in APIs',
        },
        messages: {
            forbidden: 'The {{name}} Node.js API is not supported in now platform.',
        },
        schema: [],
        type: 'problem',
    },
    create(context) {
        return {
            // biome-ignore lint/suspicious/noExplicitAny: ESLint rule listener typing
            'Program:exit'(node) {
                const sourceCode = context.sourceCode;
                const tracker = new eslint_utils_1.ReferenceTracker(sourceCode.getScope(node), { mode: 'legacy' });
                const trackMap = createTrackMap(false);
                const globalsTrackMap = createTrackMap(true);
                const references = [
                    ...tracker.iterateCjsReferences(trackMap),
                    ...tracker.iterateEsmReferences(trackMap),
                    ...tracker.iterateGlobalReferences(globalsTrackMap),
                ];
                for (const { node: refNode, info } of references) {
                    context.report({
                        node: refNode,
                        messageId: 'forbidden',
                        data: info,
                    });
                }
            },
        };
    },
};
/**
 * Custom rule: disallow dynamic import() syntax.
 * Replaces eslint-plugin-es-x/no-dynamic-import.
 */
const noDynamicImport = {
    meta: {
        docs: {
            description: 'disallow dynamic import() syntax',
        },
        messages: {
            forbidden: "Dynamic 'import()' syntax is not supported on the now platform.",
        },
        schema: [],
        type: 'problem',
    },
    create(context) {
        return {
            ImportExpression(node) {
                context.report({ node, messageId: 'forbidden' });
            },
        };
    },
};
const getPluginRules = () => {
    return {
        'no-dynamic-import': noDynamicImport,
        'no-unsupported-node-builtins': noUnsupportedNodeBuiltins,
    };
};
exports.getPluginRules = getPluginRules;
const getRestrictedGlobals = () => {
    const globalRules = [{ name: 'globalThis', message: NO_GLOBAL_THIS }];
    const browserRules = Object.keys(globals.browser)
        .filter((key) => !globalsAllowList.includes(key))
        .map((key) => {
        return {
            name: key,
            message: BROWSER_API_WARNING,
        };
    });
    return globalRules.concat(browserRules);
};
const getGlideGlobalEslintRules = () => {
    const glideGlobals = glideAPIs.defaults;
    return Object.keys(glideGlobals).map((glideGlobal) => {
        return {
            name: glideGlobal,
            message: GLIDE_API_WARNING,
        };
    });
};
const getGlideNamespaceEslintRules = () => {
    const namespacedAPIs = (0, omit_1.default)(glideAPIs, ['defaults']);
    // Deduplicate rules — the Glide API data contains duplicate object.property entries
    // across namespaces, and ESLint v9 rejects arrays with duplicate items.
    const deduplicatedRuleKeys = new Set();
    const rules = [];
    const addRule = (rule) => {
        const key = `${rule.object}.${rule.property}`;
        if (!deduplicatedRuleKeys.has(key)) {
            deduplicatedRuleKeys.add(key);
            rules.push(rule);
        }
    };
    for (const [namespace, classes] of Object.entries(namespacedAPIs)) {
        for (const [className, properties] of Object.entries(classes)) {
            addRule({
                object: namespace,
                property: className,
                message: GLIDE_API_WARNING,
            });
            for (const property of properties) {
                addRule({
                    object: className,
                    property,
                    message: GLIDE_API_WARNING,
                });
            }
        }
    }
    return rules;
};
/**
 * ESLint config for local modules — Rhino compatibility rules only (no Glide restrictions).
 * Local modules can use Glide APIs since they run in the ServiceNow scope.
 */
const getLocalModuleEslintConfig = (level = 'error') => {
    const pluginRules = (0, exports.getPluginRules)();
    const restrictedGlobals = getRestrictedGlobals();
    const rules = {
        ...Object.fromEntries(Object.keys(pluginRules).map((name) => [name, level])),
        'no-restricted-globals': [level, ...restrictedGlobals],
    };
    return {
        env: {
            node: true,
        },
        parserOptions: {
            ecmaVersion: 'latest',
            sourceType: 'module',
        },
        rules,
    };
};
exports.getLocalModuleEslintConfig = getLocalModuleEslintConfig;
/**
 * ESLint config for 3rd party dependencies — Rhino compatibility + Glide API restrictions.
 * 3rd party code must not use Glide APIs (they are platform-internal).
 */
const getEslintConfig = (level = 'warn') => {
    const pluginRules = (0, exports.getPluginRules)();
    const restrictedGlobals = getRestrictedGlobals();
    const rules = {
        ...Object.fromEntries(Object.keys(pluginRules).map((name) => [name, level])),
        ...getDependencyConfig(level, restrictedGlobals),
    };
    return {
        env: {
            node: true,
        },
        parserOptions: {
            ecmaVersion: 'latest',
            sourceType: 'module',
        },
        rules,
    };
};
exports.getEslintConfig = getEslintConfig;
function getDependencyConfig(level, restrictedGlobals) {
    const globalRules = getGlideGlobalEslintRules();
    const namespaceRules = getGlideNamespaceEslintRules();
    return {
        'no-restricted-modules': [
            level,
            {
                paths: [{ name: '@servicenow/glide', message: GLIDE_API_WARNING }],
                patterns: ['@servicenow/glide/*'],
            },
        ],
        'no-restricted-imports': [
            level,
            {
                paths: [{ name: '@servicenow/glide', message: GLIDE_API_WARNING }],
                patterns: ['@servicenow/glide/*'],
            },
        ],
        'no-restricted-globals': [level, ...globalRules, ...restrictedGlobals],
        'no-restricted-properties': [level, ...namespaceRules],
    };
}
//# sourceMappingURL=Rules.js.map