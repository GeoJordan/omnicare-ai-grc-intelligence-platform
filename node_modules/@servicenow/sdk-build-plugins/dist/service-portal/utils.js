"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DEFAULT_ORDER = void 0;
exports.createWidgetToShape = createWidgetToShape;
exports.createIncludeShape = createIncludeShape;
exports.createWidgetToRecord = createWidgetToRecord;
exports.getRolesString = getRolesString;
exports.getIncludeRecords = getIncludeRecords;
const now_include_plugin_1 = require("../now-include-plugin");
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const utils_1 = require("../utils");
const service_portal_1 = require("@servicenow/sdk-core/runtime/service-portal");
const now_id_plugin_1 = require("../now-id-plugin");
function convertOptionSchemaKeys(optionSchema, convertKey) {
    if (!Array.isArray(optionSchema)) {
        return optionSchema;
    }
    return optionSchema.map((option) => Object.fromEntries(Object.entries(option).map(([k, v]) => [convertKey(k), v])));
}
const toCamelCase = (key) => key.replace(/_([a-z])/g, (_, c) => c.toUpperCase());
/**
 * Shared toShape implementation for SPWidget and SPHeaderFooter
 */
async function createWidgetToShape(record, descendants, callee, tablePrefix = 'sp_widget', includeStaticField = false) {
    const folderName = record.get('id')?.asString()?.getValue()?.replace(/[\s-]/g, '_') ||
        record.get('name')?.asString()?.getValue()?.toLowerCase().replace(/[\s-]/g, '_');
    const dependencies = descendants.query('m2m_sp_widget_dependency');
    const angularProviders = descendants.query('m2m_sp_ng_pro_sp_widget');
    const templates = descendants.query('sp_ng_template').map((template) => {
        return {
            $id: template.getId(),
            id: template.get('id'),
            htmlTemplate: template.get('template'),
        };
    });
    const category = (0, utils_1.reverseObject)(service_portal_1.WidgetCategories)[record.get('category').ifString()?.getValue() || ''];
    const serverScript = await createIncludeShape(record, record.get('script'), 'server_script', 'js', folderName, tablePrefix);
    const clientScript = await createIncludeShape(record, record.get('client_script'), 'client_script', 'js', folderName, tablePrefix);
    const css = await createIncludeShape(record, record.get('css'), 'style', 'scss', folderName, tablePrefix);
    const htmlTemplate = await createIncludeShape(record, record.get('template'), 'template', 'html', folderName, tablePrefix);
    const linkScript = await createIncludeShape(record, record.get('link'), 'link-script', 'js', folderName, tablePrefix);
    return {
        success: true,
        value: new sdk_build_core_1.CallExpressionShape({
            source: record,
            callee,
            args: [
                record.transform(({ $ }) => {
                    const baseTransform = {
                        $id: $.val(now_id_plugin_1.NowIdShape.from(record)),
                        name: $,
                        category: $.val(category).def('custom'),
                        clientScript: $.val(clientScript).def(''),
                        serverScript: $.val(serverScript).def(''),
                        controllerAs: $.from('controller_as').def('c'),
                        htmlTemplate: $.val(htmlTemplate).def(''),
                        customCss: $.val(css).def(''),
                        dataTable: $.from('data_table')
                            .map((v) => {
                            const val = v.ifString()?.getValue();
                            return val && val.trim() !== '' ? val : 'sp_instance';
                        })
                            .def('sp_instance'),
                        demoData: $.from('demo_data')
                            .map((d) => {
                            const json = d.ifString()?.getValue();
                            const parsed = (0, utils_1.noThrow)(() => json && JSON.parse(json));
                            return parsed instanceof Error ? json : parsed;
                        })
                            .def(''),
                        description: $.def(''),
                        docs: $.def(''),
                        fields: $.from('field_list')
                            .map((fields) => {
                            const fieldStr = fields.ifString()?.getValue();
                            const fieldArray = fieldStr?.split(',').filter((f) => f.trim());
                            return fieldArray && fieldArray.length > 0 ? fieldArray : undefined;
                        })
                            .def(undefined),
                        hasPreview: $.from('has_preview').toBoolean().def(false),
                        id: $.def(''),
                        internal: $.toBoolean().def(false),
                        linkScript: $.val(linkScript).def(''),
                        roles: $.map((role) => {
                            const roleStr = role.ifString()?.getValue();
                            const roleArray = roleStr?.split(',').filter((r) => r.trim());
                            return roleArray && roleArray.length > 0 ? roleArray : undefined;
                        }).def(undefined),
                        servicenow: $.toBoolean().def(false),
                        optionSchema: $.from('option_schema')
                            .map((v) => {
                            const json = v.ifString()?.getValue();
                            const parsed = (0, utils_1.noThrow)(() => json && JSON.parse(json));
                            const result = parsed instanceof Error ? json : parsed;
                            return convertOptionSchemaKeys(result, toCamelCase);
                        })
                            .def(''),
                        public: $.toBoolean().def(false),
                        dependencies: $.val(dependencies.length > 0 ? dependencies.map((dep) => dep.get('sp_dependency')) : undefined),
                        angularProviders: $.val(angularProviders.length > 0
                            ? angularProviders.map((ap) => ap.get('sp_angular_provider'))
                            : undefined),
                        templates: $.val(templates.length > 0 ? templates : undefined),
                    };
                    return includeStaticField
                        ? { ...baseTransform, static: $.from('static').toBoolean().def(false) }
                        : baseTransform;
                }),
            ],
        }),
    };
}
/**
 * Creates a NowIncludeShape with a custom file path suffix and extension
 */
async function createIncludeShape(record, content, suffix, extension, folderName, tablePrefix = 'sp_widget') {
    const baseName = `${tablePrefix}_${folderName}`;
    const includedText = content instanceof sdk_build_core_1.Shape ? content.toString().getValue() : content;
    return new now_include_plugin_1.NowIncludeShape({
        source: record,
        path: `./${baseName}/${suffix}.${extension}`,
        includedText,
    });
}
exports.DEFAULT_ORDER = 100;
/**
 * Shared toRecord implementation for SPWidget and SPHeaderFooter
 */
async function createWidgetToRecord(callExpression, { diagnostics, factory, config, }, options) {
    const { table, includeStaticField = false } = options;
    const widget = callExpression.getArgument(0).asObject();
    const dependencies = widget
        .get('dependencies')
        .ifArray()
        ?.map((dep) => (dep.isString() ? dep.getValue() : dep.ifRecord()?.getId()))
        .filter((dep) => dep) ?? [];
    const angularProviders = widget
        .get('angularProviders')
        .ifArray()
        ?.map((ap) => (ap.isString() ? ap.getValue() : ap.ifRecord()?.getId()))
        .filter((ap) => ap) ?? [];
    const templates = widget
        .get('templates')
        .ifArray()
        ?.map((shp) => shp.getValue()) ?? [];
    const widgetId = widget.get('id').ifString();
    if (widgetId && !/^[a-zA-Z0-9_-]+$/g.test(widgetId.getValue())) {
        diagnostics.error(widgetId.getOriginalNode(), `Invalid value: must contain only alphanumeric, -, or _ characters`);
    }
    const clientScript = widget.get('clientScript');
    const clientScriptValue = clientScript instanceof now_include_plugin_1.NowIncludeShape ? clientScript.getValue() : clientScript.ifString()?.getValue();
    if (clientScriptValue && clientScriptValue.trim().length > 0) {
        const clientScriptPattern = /^(function|api\.controller\s?=\s?function)\s?([$a-z_][$0-9a-z_]*)?\s?\(.*\)\s?\n?{/i;
        if (!clientScriptPattern.test(clientScriptValue.trim())) {
            diagnostics.error(clientScript.getOriginalNode(), `Client controller must contain a JavaScript function. Example: api.controller = function($scope) { ... }`);
        }
    }
    const htmlTemplate = widget.get('htmlTemplate');
    const htmlTemplateValue = htmlTemplate instanceof now_include_plugin_1.NowIncludeShape ? htmlTemplate.getValue() : htmlTemplate.ifString()?.getValue();
    if (htmlTemplateValue && htmlTemplateValue.length > 0 && htmlTemplateValue.indexOf('href="#"') > 0) {
        diagnostics.error(htmlTemplate.getOriginalNode(), `Do not use href="#" in the Service Portal, use href="javascript:void(0)" instead`);
    }
    const roles = widget
        .get('roles')
        .ifArray()
        ?.map((role) => {
        if (role.isString()) {
            return role;
        }
        if (role.isRecord()) {
            return role.get('name');
        }
        return undefined;
    })
        .filter((role) => role);
    if (roles) {
        roles
            .filter((role) => (0, sdk_build_core_1.isGUID)(role.getValue()))
            .forEach((role) => diagnostics.error(role.getOriginalNode(), `expecting role names or role records created by the Role or Record plugins, not sys_ids`));
    }
    const controller = widget.get('controllerAs').ifString()?.getValue() || 'c';
    const servicenow = (widget.get('servicenow').ifBoolean()?.getValue() &&
        (config.scope.startsWith('sn_') || config.scope.startsWith('snc_'))) ||
        false;
    const widgetRecord = await factory.createRecord({
        source: callExpression,
        table,
        explicitId: widget.get('$id'),
        properties: widget.transform(({ $ }) => {
            const baseTransform = {
                name: $,
                category: $.map((v) => {
                    const catKey = v.ifString()?.getValue() || '';
                    return service_portal_1.WidgetCategories[catKey];
                }).def('custom'),
                client_script: $.from('clientScript').def(options.getDefaultClientScript(controller)),
                script: $.from('serverScript').def(options.defaultServerScript),
                controller_as: $.from('controllerAs').def('c'),
                template: $.from('htmlTemplate').def(options.defaultHtmlTemplate),
                css: $.from('customCss').def(''),
                data_table: $.from('dataTable').def('sp_instance'),
                demo_data: $.from('demoData')
                    .map((v) => (v.ifString() || v instanceof now_include_plugin_1.NowIncludeShape ? v : JSON.stringify(v.getValue())))
                    .def(''),
                description: $.def(''),
                docs: $.map((v) => (v.isString() ? v : v.ifRecord()?.getId())),
                field_list: $.from('fields')
                    .map((v) => v.getValue()?.toString())
                    .def(''),
                has_preview: $.from('hasPreview').toBoolean().def(false),
                id: $.def(''),
                internal: $.def(false),
                servicenow: $.val(servicenow),
                link: $.from('linkScript').def(options.defaultLinkScript),
                roles: $.val(roles?.map((r) => r.getValue()).toString()).def(''),
                option_schema: $.from('optionSchema')
                    .map((v) => {
                    const json = v.getValue();
                    const converted = convertOptionSchemaKeys(json, (key) => key.replace(/[A-Z]/g, (c) => `_${c.toLowerCase()}`));
                    return converted && JSON.stringify(converted);
                })
                    .def(''),
                public: $.def(false),
            };
            return includeStaticField
                ? { ...baseTransform, static: $.from('static').toBoolean().def(false) }
                : baseTransform;
        }),
    });
    return {
        success: true,
        value: widgetRecord.with(...(await Promise.all(templates.map(async (template) => {
            return await factory.createRecord({
                source: callExpression,
                table: 'sp_ng_template',
                explicitId: template.$id,
                properties: {
                    id: template.id,
                    sp_widget: widgetRecord.getId(),
                    template: template.htmlTemplate,
                },
            });
        }))), ...(await Promise.all(dependencies.map(async (dep) => {
            return await factory.createRecord({
                source: callExpression,
                table: 'm2m_sp_widget_dependency',
                properties: {
                    sp_dependency: dep,
                    sp_widget: widgetRecord.getId(),
                },
            });
        }))), ...(await Promise.all(angularProviders.map(async (ap) => {
            return await factory.createRecord({
                source: callExpression,
                table: 'm2m_sp_ng_pro_sp_widget',
                properties: {
                    sp_angular_provider: ap,
                    sp_widget: widgetRecord.getId(),
                },
            });
        })))),
    };
}
/**
 * Converts a roles Shape (array of strings, Role() expressions, or Record references)
 * to a deduplicated comma-separated string of role names.
 * @param rolesShape - Shape containing the roles array
 * @param diagnostics - Optional diagnostics for reporting invalid role entries
 * @returns Comma-separated string of unique role names, or empty string if no roles
 */
function getRolesString(rolesShape, diagnostics) {
    const roles = rolesShape
        .ifArray()
        ?.getElements()
        .map((role) => {
        if (role.isString()) {
            return role.getValue();
        }
        if (role instanceof sdk_build_core_1.CallExpressionShape) {
            const name = role.getArgument(0).asObject().get('name');
            if (name.isString()) {
                return name.getValue();
            }
        }
        if (role.isRecord()) {
            const name = role.get('name');
            if (name?.isString()) {
                return name.getValue();
            }
        }
        if (diagnostics) {
            diagnostics.error(role, 'roles must be strings or role records');
        }
        return undefined;
    })
        .filter((r) => r !== undefined && r.trim() !== '');
    if (!roles || roles.length === 0) {
        return '';
    }
    return [...new Set(roles)].join(',');
}
async function getIncludeRecords(shape, parentId, factory, m2mTable, includeTable, parentTable, diagnostics) {
    return (await Promise.all(shape?.ifArray()?.map(async (include) => {
        const includeProps = include.ifObject()?.get('include');
        let includeId;
        if (includeProps?.isString()) {
            includeId = includeProps.toString().getValue();
        }
        else if (includeProps?.isRecord()) {
            includeId = includeProps.getId();
        }
        else {
            const necessaryPlugin = includeTable === 'sp_js_include' ? 'JsInclude' : 'CssInclude';
            diagnostics.error(includeProps, `include must contain a valid ${necessaryPlugin}, Record<'${includeTable}'> or sys_id'`);
            return undefined;
        }
        const m2mProps = {
            [includeTable]: includeId,
            [parentTable]: parentId,
        };
        return await factory.createRecord({
            source: shape,
            table: m2mTable,
            properties: {
                order: include.ifObject()?.get('order').asNumber().getValue() || exports.DEFAULT_ORDER,
                ...m2mProps,
            },
        });
    }) ?? [])).filter((rec) => rec);
}
//# sourceMappingURL=utils.js.map