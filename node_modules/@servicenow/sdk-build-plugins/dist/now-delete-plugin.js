"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.NowDeletePlugin = void 0;
const sdk_build_core_1 = require("@servicenow/sdk-build-core");
const call_expression_plugin_1 = require("./call-expression-plugin");
exports.NowDeletePlugin = sdk_build_core_1.Plugin.create({
    name: 'NowDeletePlugin',
    nodes: [
        {
            node: 'CallExpression',
            async toShape(node, { transform }) {
                const result = await transform.toShape(node, call_expression_plugin_1.CallExpressionPlugin);
                if (!result.success ||
                    !result.value.is(sdk_build_core_1.CallExpressionShape) ||
                    result.value.getCallee() !== 'Now.del') {
                    return { success: false };
                }
                const callExpression = result.value;
                const table = callExpression
                    .getArgument(0)
                    .asString('The first argument to Now.del() must be a table name')
                    .getValue();
                const guidOrKeys = callExpression
                    .getArgument(1)
                    .as([sdk_build_core_1.StringShape, sdk_build_core_1.ObjectShape], 'The second argument to Now.del() must be a sys_id GUID or a coalesce keys object');
                const [guid, keysShape] = guidOrKeys.isString()
                    ? [guidOrKeys.asString().getValue(), undefined] // Now.del(table, guid)
                    : [undefined, guidOrKeys.asObject()]; // Now.del(table, keys)
                // Convert keysShape to plain object
                const keys = keysShape
                    ? Object.fromEntries(keysShape
                        .entries()
                        .map(([k, v]) => [
                        k,
                        typeof v === 'string'
                            ? v
                            : (v.ifRecord()?.getId() ??
                                v.ifRecordId() ??
                                v.ifDefined()?.toString().getValue()) ||
                                'NULL',
                    ]))
                    : undefined;
                // Create DELETE record directly (no factory needed)
                const record = new sdk_build_core_1.Record({
                    source: node,
                    action: 'DELETE',
                    installCategory: 'author_elective_update',
                    properties: keys ?? {},
                    id: new sdk_build_core_1.RecordId({
                        source: node,
                        table,
                        guid: guid ?? (0, sdk_build_core_1.GUID)(),
                        keys,
                    }),
                });
                return {
                    success: true,
                    value: record,
                };
            },
        },
    ],
});
//# sourceMappingURL=now-delete-plugin.js.map