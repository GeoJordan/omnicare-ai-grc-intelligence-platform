import type { ProxyItem } from "./config.js";
declare const _default: (server: any, { proxy, silent, httpProxy, }: {
    proxy?: ProxyItem[];
    silent?: boolean;
    httpProxy?: string;
}) => Promise<void>;
export default _default;
