export interface ProxyItem {
    from: string;
    to: string;
    /** Per-entry options forwarded to `@fastify/http-proxy`. Setting `undici` here overrides the global `httpProxy` for this entry. */
    opts?: Record<string, unknown>;
}
export interface ServerConfig {
    routerOptions?: {
        ignoreTrailingSlash?: boolean;
        [key: string]: unknown;
    };
    disableRequestLogging?: boolean;
    logger?: boolean | Record<string, unknown>;
    https?: boolean;
    [key: string]: unknown;
}
export interface DevServerConfig {
    silent: boolean;
    force: boolean;
    proxy: ProxyItem[];
    dirs: string[];
    dirname?: string;
    spa: boolean | string;
    port: number;
    forcePort?: boolean;
    host: string;
    basePath?: string;
    httpProxy?: string;
    extend?: (server: any, config: DevServerConfig) => void | Promise<void>;
    server: ServerConfig;
    onListen?: (server: any) => void;
    index?: string | boolean;
}
export declare const serverDefaults: Readonly<{
    routerOptions: {
        ignoreTrailingSlash: boolean;
    };
    disableRequestLogging: true;
}>;
export declare const defaults: DevServerConfig;
export declare const normalize: (rollupOptions?: Partial<DevServerConfig> | string) => DevServerConfig;
