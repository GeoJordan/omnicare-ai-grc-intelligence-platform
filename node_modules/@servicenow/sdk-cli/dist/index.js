"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const yargs = __importStar(require("yargs"));
const usage_1 = require("./usage");
const epilogue_1 = require("./epilogue");
const semver_1 = require("semver");
const logger_1 = require("./logger");
const package_json_1 = __importDefault(require("../package.json"));
try {
    yargs
        .check(() => {
        const neededVersion = package_json_1.default['engines']['node'];
        if (!(0, semver_1.satisfies)(process.version, neededVersion)) {
            logger_1.logger.error(`now-sdk requires node version to be ${neededVersion}`);
            process.exit(1);
        }
        return true;
    })
        .usage(usage_1.usage)
        .option('debug', {
        describe: 'Print debug output',
        alias: 'd',
        default: false,
        type: 'boolean',
    })
        .command({
        command: 'auth',
        describe: 'Configure authentication to instance',
        builder: (y) => require('./command/auth').builder(y),
        handler: (argv) => require('./command/auth').handler(argv),
    })
        .command({
        command: 'init',
        describe: 'Initialize a new ServiceNow custom application, apply a template to an existing application, or convert a legacy ServiceNow application from an instance or directory within the current directory structure.',
        aliases: ['create'],
        builder: (y) => require('./command/init').builder(y),
        handler: (argv) => require('./command/init').handler(argv),
    })
        .command({
        command: 'download <directory>',
        describe: 'Download application metadata from instance.',
        builder: (y) => require('./command/download').builder(y),
        handler: (argv) => require('./command/download').handler(argv),
    })
        .command({
        command: 'build [source]',
        describe: 'Compile sources into app files and generate installable package',
        builder: (y) => require('./command/build').builder(y),
        handler: (argv) => require('./command/build').handler(argv),
    })
        .command({
        command: 'install',
        describe: 'Install or update application on instance',
        aliases: ['deploy'],
        builder: (y) => require('./command/install').builder(y),
        handler: (argv) => require('./command/install').handler(argv),
    })
        .command({
        command: 'dependencies [sysIds..]',
        describe: 'Download configured dependencies in now.config.json and typescript type definitions for use in the application',
        builder: (y) => require('./command/dependencies').builder(y),
        handler: (argv) => require('./command/dependencies').handler(argv),
    })
        .command({
        command: 'transform',
        describe: 'Download and convert XML records from instance or from a local path into Fluent source code',
        builder: (y) => require('./command/transform').builder(y),
        handler: (argv) => require('./command/transform').handler(argv),
    })
        .command({
        command: 'run [script]',
        describe: false,
        builder: (y) => require('./command/run').builder(y),
        handler: (argv) => require('./command/run').handler(argv),
    })
        .command({
        command: 'clean [source]',
        describe: 'Clean output directory',
        builder: (y) => require('./command/clean').builder(y),
        handler: (argv) => require('./command/clean').handler(argv),
    })
        .command({
        command: 'pack [source]',
        describe: 'Zip built app into installable artifact',
        builder: (y) => require('./command/pack').builder(y),
        handler: (argv) => require('./command/pack').handler(argv),
    })
        .command({
        command: 'explain [topic]',
        describe: 'Display documentation for a Fluent SDK topic',
        builder: (y) => require('./command/explain').builder(y),
        handler: (argv) => require('./command/explain').handler(argv),
    })
        .command({
        command: 'move',
        describe: false,
        builder: (y) => require('./command/move-to-app').builder(y),
        handler: (argv) => require('./command/move-to-app').handler(argv),
    })
        .command({
        command: 'query <table>',
        describe: 'Query records from a ServiceNow table on the instance',
        builder: (y) => require('./command/query').builder(y),
        handler: (argv) => require('./command/query').handler(argv),
    })
        .demandCommand()
        .middleware(async (argv) => {
        if (argv.debug) {
            logger_1.logger.setLevel('debug');
            logger_1.logger.debug('Debug logging enabled');
        }
    })
        .epilogue(epilogue_1.epilogue)
        .strictCommands()
        .help()
        .updateStrings({
        'Positionals:': 'Parameters:',
    })
        .alias('h', 'help')
        .alias('v', 'version')
        .scriptName('now-sdk').argv;
}
catch (error) {
    logger_1.logger.error(error);
}
//# sourceMappingURL=index.js.map