"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPromptAnswers = getPromptAnswers;
exports.getPromptAnswersForInitConvert = getPromptAnswersForInitConvert;
exports.getPromptAnswersForInitApply = getPromptAnswersForInitApply;
const prompts_1 = require("@inquirer/prompts");
const logger_1 = require("../../logger");
const sdk_api_1 = require("@servicenow/sdk-api");
const appNamePrompt = async (defaultAppName = 'Now SDK Example App') => {
    function normalize(value) {
        return value.trim();
    }
    const raw = await (0, prompts_1.input)({
        message: 'Name of ServiceNow Application: ',
        default: defaultAppName,
        validate: (value) => sdk_api_1.ProjectInitValidation.validateAppName(normalize(value)) ?? true,
    });
    return normalize(raw);
};
const templatePrompt = async (templates) => {
    const choices = [];
    let lastCategory;
    for (const template of templates) {
        if (template.category && lastCategory !== template.category) {
            choices.push(new prompts_1.Separator(`-- ${template.category} --`));
            lastCategory = template.category;
        }
        choices.push({
            name: template.name,
            value: template.id,
            description: template.description,
        });
    }
    const raw = await (0, prompts_1.select)({
        message: 'Select a template:',
        pageSize: choices.length,
        choices,
    });
    return raw;
};
const globalPrompt = async () => {
    const raw = await (0, prompts_1.select)({
        message: 'Create a Global/Scoped App?',
        choices: [
            {
                name: 'Scoped',
                value: 'scoped',
                description: 'A scope protects application functionality by identifying and restricting access to application files and data. This ensures the application does not disrupt core business services and that other applications cannot interfere with its normal functioning.',
            },
            {
                name: 'Global',
                value: 'global',
                description: 'Global scope allows the application to be accessible to other globally scoped applications. They do not offer the same protections as scoped apps. Global applications are eligible for upload to the application repository, but not to the ServiceNow Store.',
            },
        ],
    });
    return raw;
};
const scopeNamePrompt = async (defaultScopeName) => {
    function normalize(value) {
        return value.trim();
    }
    const raw = await (0, prompts_1.input)({
        message: 'Scope name: ',
        ...(defaultScopeName != null && { default: defaultScopeName }),
        transformer: normalize,
        // intentionally not validating the vendorPrefix here, so that users
        // can optionally override the whole scope (useful for internal/maint devs)
        validate: (value) => sdk_api_1.ProjectInitValidation.validateScopeName(normalize(value)) ?? true,
    });
    return normalize(raw);
};
const packageNamePrompt = (generatedName) => (0, prompts_1.input)({
    message: `NPM package name: `,
    default: generatedName,
    validate(pkgName) {
        const error = sdk_api_1.ProjectInitValidation.validateNpmPackageName(pkgName);
        if (error) {
            return error;
        }
        return true;
    },
});
const needPrompt = (context, requiredFields) => {
    return requiredFields.some((field) => !context[field]);
};
const DEFAULT_VENDOR_PREFIX = 'x_';
async function getVendorPrefix(credential) {
    if (!credential) {
        return { prefix: DEFAULT_VENDOR_PREFIX, fetchedFromInstance: false };
    }
    try {
        const prefix = await new sdk_api_1.Connector(credential).appCreatorVendorPrefix();
        if (!prefix) {
            logger_1.logger.debug(`Vendor prefix not available from instance. Using default prefix '${DEFAULT_VENDOR_PREFIX}'`);
            return { prefix: DEFAULT_VENDOR_PREFIX, fetchedFromInstance: false };
        }
        return { prefix, fetchedFromInstance: true };
    }
    catch (error) {
        logger_1.logger.debug(`Could not fetch vendor prefix from instance: ${error}. Using default prefix '${DEFAULT_VENDOR_PREFIX}'`);
        return { prefix: DEFAULT_VENDOR_PREFIX, fetchedFromInstance: false };
    }
}
async function warnIfScopePrefixMismatch(scopeName, credential, cachedResult) {
    if (scopeName === 'global') {
        return;
    }
    const { prefix: vendorPrefix, fetchedFromInstance } = cachedResult ?? (await getVendorPrefix(credential));
    // Only validate for x_ prefixes (skip sn_ and other internal prefixes)
    if (fetchedFromInstance && vendorPrefix.startsWith('x_') && !scopeName.startsWith(vendorPrefix)) {
        logger_1.logger.warn(`The scope name '${scopeName}' does not start with your instance's vendor prefix '${vendorPrefix}'. ` +
            `Applications with non-matching prefixes may not install correctly on this instance.`);
    }
}
async function promptScopeWithValidation(defaultScopeName, vendorPrefixResult, skipGlobalPrompt = false) {
    if (!skipGlobalPrompt) {
        const scopeType = await globalPrompt();
        if (scopeType === 'global') {
            return 'global';
        }
    }
    const scopeName = await scopeNamePrompt(defaultScopeName);
    // Only validate if we fetched the prefix from the instance and it starts with x_
    // Skip validation for sn_ and other internal prefixes (internal developers know what they're doing)
    if (!vendorPrefixResult.fetchedFromInstance || !vendorPrefixResult.prefix.startsWith('x_')) {
        return scopeName;
    }
    // Check if scope matches vendor prefix and prompt for confirmation if not
    if (!scopeName.startsWith(vendorPrefixResult.prefix)) {
        const warningMessage = `The scope name '${scopeName}' does not start with your instance's vendor prefix '${vendorPrefixResult.prefix}'. ` +
            `Applications with non-matching prefixes may not install correctly on this instance.`;
        logger_1.logger.warn(warningMessage);
        // In non-interactive mode (piped input), just warn and continue
        if (!process.stdin.isTTY) {
            return scopeName;
        }
        const continueAnyway = await (0, prompts_1.confirm)({
            message: 'Do you want to continue with this scope name?',
            default: false,
        });
        if (!continueAnyway) {
            // Recursively prompt again, skipping the global/scoped question
            return promptScopeWithValidation(defaultScopeName, vendorPrefixResult, true);
        }
    }
    return scopeName;
}
async function getPromptAnswers(context, credential, templates) {
    if (!needPrompt(context, ['appName', 'packageName', 'scopeName']) &&
        context.template &&
        context.template in templates) {
        // Validate scope prefix even when all args provided via CLI
        await warnIfScopePrefixMismatch(context.scopeName, credential);
        return { ...context, templateId: context.template };
    }
    const hasExistingTemplate = templates.some(({ id }) => id === context.template);
    const templateId = context.template && hasExistingTemplate ? context.template : await templatePrompt(templates);
    const appName = context.appName ?? (await appNamePrompt(context.appName));
    const packageName = context.packageName ?? (await packageNamePrompt(sdk_api_1.ProjectFactory.createNpmPackageName(appName)));
    // Fetch vendor prefix for default scope suggestion and validation
    const vendorPrefixResult = await getVendorPrefix(credential);
    const defaultScopeName = `${vendorPrefixResult.prefix}${packageName.toLowerCase().replaceAll(/\W/g, '_')}`
        .slice(0, 18)
        .replace(/_$/, '');
    let scopeName;
    if (context.scopeName) {
        // Scope provided via CLI arg - just warn if mismatch
        scopeName = context.scopeName;
        await warnIfScopePrefixMismatch(scopeName, credential, vendorPrefixResult);
    }
    else {
        // Interactive prompt - allow user to re-enter if mismatch
        scopeName = await promptScopeWithValidation(defaultScopeName, vendorPrefixResult);
    }
    return {
        appName,
        packageName,
        scopeName,
        templateId,
    };
}
async function getPromptAnswersForInitConvert(context) {
    if (!needPrompt(context, ['packageName'])) {
        return { ...context };
    }
    logger_1.logger.info(`Please provide the missing required fields:`);
    const packageName = await packageNamePrompt('');
    return {
        packageName,
    };
}
async function getPromptAnswersForInitApply(context, templates) {
    const hasExistingTemplate = templates.some(({ id }) => id === context.template);
    const templateId = context.template && hasExistingTemplate ? context.template : await templatePrompt(templates);
    const template = templates.find(({ id }) => id === templateId);
    const params = {};
    if (template?.params) {
        logger_1.logger.info(`Template "${template.name}" requires additional parameters:`);
        for (const [paramName, param] of Object.entries(template.params)) {
            const answer = await (0, prompts_1.input)({
                message: `${param.label}: `,
                default: param.default,
                validate: (value) => (value.trim().length === 0 ? 'Value cannot be empty' : true),
            });
            params[paramName] = answer.trim();
        }
    }
    return { templateId, params };
}
//# sourceMappingURL=prompts.js.map