"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getUISessionToken = getUISessionToken;
const UISession_1 = require("./UISession");
const logger_1 = require("../../logger");
async function getUISessionToken(creds) {
    const { username, password, instanceUrl } = creds;
    const session = new UISession_1.UISession(instanceUrl);
    try {
        // verify basic auth credentials and get user token
        return session.login(username, password);
    }
    catch (error) {
        logger_1.logger.error(`Login to instance ${instanceUrl} failed: ${error.message}`);
        return;
    }
}
//# sourceMappingURL=index.js.map