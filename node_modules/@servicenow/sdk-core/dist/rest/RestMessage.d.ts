export type RestMessageHeader = Now.Internal.WithID<{
    /** Header name, e.g. 'Content-Type'. Must be unique within the parent message. */
    name: string;
    /** Header value. Variable substitution supported via ${varName} */
    value: string;
}>;
export type RestMessageQueryParam = Now.Internal.WithID<{
    /**
     * Query parameter key appended to the request URL (e.g. `'limit'` → `?limit=...`).
     * The value can be a static string or reference a variable substitution via `${varName}`.
     * Must be unique within the parent function.
     */
    name: string;
    /**
     * Query parameter value. May be a static string (e.g. `'json'`) or a variable
     * substitution reference (e.g. `'${sys_id}'`) that is filled in at runtime via
     * `RESTMessageV2.setStringParameter()`.
     */
    value?: string;
    /** Display order in the HTTP Query Parameters list */
    order?: number;
}>;
export type RestMessageParamSubstitution = Now.Internal.WithID<{
    /**
     * Variable name used as the `${varName}` placeholder. The same variable can be
     * referenced in the endpoint URL, HTTP headers, HTTP query parameter values, or
     * the request body. At runtime the value is supplied via
     * `RESTMessageV2.setStringParameter(name, value)`.
     * Must be unique within the parent function.
     */
    name: string;
    /**
     * How the substituted value is escaped before being inserted.
     * Defaults to `'noEscaping'`.
     *
     * - `'noEscaping'` — value inserted as-is (default)
     * - `'escapeXml'`  — value is XML-escaped before insertion
     */
    escapeType?: 'noEscaping' | 'escapeXml';
}>;
export type RestMessageFnHeader = Now.Internal.WithID<{
    /** Header name. Must be unique within the parent function. */
    name: string;
    /** Header value. Variable substitution supported via ${varName} */
    value: string;
}>;
/** Auth credential fields shared by message-level and function-level auth */
type RestMessageAuthCredentials = {
    /**
     * sys_id of sys_auth_profile_basic. Required when authenticationType is `'basic'`.
     */
    basicAuthProfile?: string;
    /** sys_id of oauth_entity_profile. Required when authenticationType is `'oauth2'`. */
    oauthProfile?: string;
};
/** Authentication fields for the root RestMessage record */
export type RestMessageAuth = RestMessageAuthCredentials & {
    /**
     * Authentication strategy applied to all functions unless overridden at function level.
     * Defaults to `'noAuthentication'`.
     *
     * - `'noAuthentication'` — no credentials sent (default)
     * - `'basic'` — HTTP Basic auth via a stored credential profile (`basicAuthProfile`)
     * - `'oauth2'` — OAuth 2.0 via a named profile (`oauthProfile`)
     */
    authenticationType?: 'noAuthentication' | 'basic' | 'oauth2';
};
/** Authentication fields for a RestMessageFn record */
export type RestMessageFnAuth = RestMessageAuthCredentials & {
    /**
     * Authentication strategy for this function.
     * Defaults to `'inheritFromParent'`, which uses the parent message's authentication.
     * Any other value fully overrides the parent auth for this function only.
     *
     * - `'inheritFromParent'` — use parent message authentication (default)
     * - `'noAuthentication'` — override to no credentials for this function
     * - `'basic'` — HTTP Basic auth via a stored credential profile (`basicAuthProfile`)
     * - `'oauth2'` — OAuth 2.0 via a named profile (`oauthProfile`)
     */
    authenticationType?: 'inheritFromParent' | 'noAuthentication' | 'basic' | 'oauth2';
};
export type RestMessageFn = {
    /** Function name, used in scripts to identify the HTTP method. Must be unique within the parent message. */
    name: string;
    /** HTTP method for this function */
    httpMethod: 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE' | 'HEAD';
    /** Overrides parent message endpoint for this function. Variable substitution supported */
    endpoint?: string;
    /** Request body. Variable substitution supported via ${varName} */
    content?: string;
    /** sys_id of ecc_agent MID server to route this function through */
    midServer?: string;
    /** Lock function to prevent editing on instance */
    lock?: boolean;
    /** Headers specific to this function */
    headers?: RestMessageFnHeader[];
    /** Variable substitutions — the `${varName}` placeholders used in this function's endpoint, content, and headers */
    variables?: RestMessageParamSubstitution[];
    /** HTTP query parameters appended to the request URL */
    queryParams?: RestMessageQueryParam[];
} & RestMessageFnAuth;
export type RestMessage = Now.Internal.WithIdAndMetadata<{
    /** Display name for the REST message */
    name: string;
    /** Base endpoint URL. Variable substitution supported via ${varName} */
    endpoint: string;
    /** Human-readable description */
    description?: string;
    /** Controls from which scopes this message can be called */
    access?: 'packagePrivate' | 'public';
    /** Headers sent with every function in this message */
    headers?: RestMessageHeader[];
    /** HTTP method functions for this message */
    functions?: RestMessageFn[];
} & RestMessageAuth>;
/**
 * Defines an Outbound REST Message integration (`sys_rest_message`), including
 * authentication, shared headers, and one or more HTTP method functions.
 *
 * @param config - an object containing the following properties:
 *   * **$id** - unique id for the record, typically using `Now.ID["value"]`
 *   * **name** - display name for the REST message
 *   * **endpoint** - base endpoint URL; variable substitution supported via `${varName}`
 *   * **description**? - human-readable description
 *   * **access**? - `'packagePrivate'` (default) or `'public'`
 *   * **authenticationType**? - `'noAuthentication'` (default) | `'basic'` | `'oauth2'`
 *   * **basicAuthProfile**? - sys_id of sys_auth_profile_basic (for `authenticationType: 'basic'`)
 *   * **oauthProfile**? - sys_id of oauth_entity_profile (for `authenticationType: 'oauth2'`)
 *   * **headers**? - shared headers applied to all functions
 *   * **functions**? - array of HTTP method functions; each defaults to `authenticationType: 'inheritFromParent'`
 */
export declare function RestMessage(config: RestMessage): RestMessage;
export {};
