import { Table, GuidColumn } from '../../db'

export const sp_instance_menu = Table({
    allowWebServiceAccess: true,
    audit: true,
    extends: 'sp_instance',
    label: 'Instance with Menu',
    name: 'sp_instance_menu',
    schema: {
        sys_id: GuidColumn({
            label: 'Sys ID',
            primary: true,
        }),
    },
})
