"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.unzipSync = exports.zipSync = exports.gunzipSync = exports.gunzip = exports.gzipSync = exports.zipDirectory = exports.unzipAppPackage = void 0;
const fflate_1 = require("fflate");
Object.defineProperty(exports, "unzipSync", { enumerable: true, get: function () { return fflate_1.unzipSync; } });
Object.defineProperty(exports, "zipSync", { enumerable: true, get: function () { return fflate_1.zipSync; } });
Object.defineProperty(exports, "gzipSync", { enumerable: true, get: function () { return fflate_1.gzipSync; } });
Object.defineProperty(exports, "gunzipSync", { enumerable: true, get: function () { return fflate_1.gunzipSync; } });
const fs_1 = require("./fs");
const path_1 = require("./path");
const ZIP_STATUS_FILE = 'status.json';
const unzipAppPackage = async (fs, zipPath, targetPath, logger, ignore) => {
    const zipArrBuf = new Uint8Array(fs.readFileSync(zipPath));
    const unzipResult = (0, fflate_1.unzipSync)(zipArrBuf);
    //remove unnecessary package inventory file if present
    if (unzipResult['package_inventory.csv']) {
        delete unzipResult['package_inventory.csv'];
    }
    // check the status of download operation
    if (ZIP_STATUS_FILE in unzipResult) {
        const response = JSON.parse(Buffer.from(unzipResult[ZIP_STATUS_FILE]).toString('utf8'));
        if (response.status === 'error') {
            throw new Error(`Application download failed with error, ${response.message}`);
        }
        if (response.status === 'warning') {
            logger?.warn(`Application download completed with warning: ${response.message}`);
        }
        delete unzipResult[ZIP_STATUS_FILE];
    }
    const files = [];
    for (const [filePath, data] of Object.entries(unzipResult)) {
        const filename = path_1.path.basename(filePath);
        if (ignore?.(filename)) {
            logger?.info(`Skipping file ${filename} due to excludeFilePatterns`);
            continue;
        }
        const fullPath = path_1.path.join(targetPath, filePath);
        // Create directories if necessary
        fs_1.FileSystem.ensureDirSync(fs, path_1.path.dirname(fullPath));
        // Write file content to the filesystem
        fs.writeFileSync(fullPath, data);
        files.push(fullPath);
    }
    return { files };
};
exports.unzipAppPackage = unzipAppPackage;
const zipDirectory = async (fs, source, destination) => {
    // Using our own glob for browser compatibility
    const wrappedFs = new fs_1.TsMorphFileSystemWrapper(fs);
    const files = wrappedFs.globSync(['**'], {
        cwd: source,
    });
    if (files.length === 0) {
        throw new Error(`No files found in ${source} directory to create zip.`);
    }
    const fileMap = {};
    for (const file of files) {
        const fp = path_1.path.normalize(file);
        const data = new Uint8Array(fs.readFileSync(fp));
        const entryName = fp.replace(source, '');
        fileMap[entryName] = [data, { level: 6 }];
    }
    const zipped = (0, fflate_1.zipSync)(fileMap);
    fs.writeFileSync(destination, zipped);
    return destination;
};
exports.zipDirectory = zipDirectory;
const gunzip = (data) => new Promise((resolve, reject) => {
    return (0, fflate_1.gunzip)(data, (err, data) => {
        if (err) {
            reject(err);
        }
        else {
            resolve(data);
        }
    });
});
exports.gunzip = gunzip;
//# sourceMappingURL=compression.js.map