import { FileSystem } from './fs';
export interface PackageInventoryConfig {
    build: string;
    type: 'scoped' | 'global';
    appVersion: string;
    version?: string;
}
/**
 * Generate SHA-256 hash for file content (matches ServiceNow's package_inventory.csv format)
 */
export declare function sha256(content: string | Uint8Array): Promise<string>;
/**
 * Generate package_inventory.csv from files in a directory on disk (for pack command)
 */
export declare function generatePackageInventoryFromDirectory(fs: FileSystem, appOutputDir: string, config: PackageInventoryConfig): Promise<string>;
