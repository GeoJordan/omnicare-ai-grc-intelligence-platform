import { unzipSync, zipSync, gzipSync, gunzipSync } from 'fflate';
import { FileSystem } from './fs';
import type { Logger } from './logger';
export declare const unzipAppPackage: (fs: FileSystem, zipPath: string, targetPath: string, logger?: Logger, ignore?: (filename: string) => boolean) => Promise<{
    files: string[];
}>;
export declare const zipDirectory: (fs: FileSystem, source: string, destination: string) => Promise<string>;
declare const gunzip: (data: Uint8Array) => Promise<Uint8Array>;
export { gzipSync, gunzip, gunzipSync, zipSync, unzipSync };
